# 前端独立实证审计报告 — 2026-06-24

> 方法：不引用任何历史审计结论，全部基于本次实跑/实测数据
> 范围：`apps/mobile`（H5，461 个 .vue）
> 部署目标：仅 H5（manifest 只配 h5）

---

## 一、已验证的健康项 ✅

| 项 | 结论 | 证据 |
|----|------|------|
| 类型安全 | 0 错误 | `vue-tsc --noEmit` 通过 |
| 生产构建 | **通过** | `uni build -p h5` 0 错误、build complete（旧的 operator-panel/Pinia 报错已不存在） |
| mock 数据流 | 已清零 | 461 vue 全过 scan-mock-imports；`env.ts` MOCK_FLAG=false 强制真实 API |
| 联调路径 | 正确 | 前端 `/api` + data 层 `/v1/xxx` = `/api/v1/xxx`，匹配后端 `setGlobalPrefix('api/v1')`；vite proxy `/api→:3000` |
| 静态图片 | 已优化 | 54 张图 34M→9.6M（省 71%），备份 `.backup/static-20260624` |
| 请求封装 | 合格 | `utils/request.ts` 含 token 注入、`{code,data}`/裸数据兼容、错误处理 |
| 后端规模 | 充足 | 137 controller / 1560 端点 |

---

## 二、核心问题：61% 页面是 V0 半成品 🔴

实测页面风格分布（461 个 .vue）：

| 类型 | 数量 | 占比 | 特征 |
|------|------|------|------|
| **A 类（达标）** | 134 | 29% | `<view>/<text>`、scoped 语义 class、三态组件齐全。代表：`pkg-shop/product` |
| **B 类（V0 直出）** | **281** | **61%** | `<div>`+Tailwind 原子类、onClick 机械换 @tap、**无三态、无骨架、无防重复**。代表：`pkg-im/group-detail`（965行/48 div/0 view/0 三态） |
| 混合 | 46 | 10% | 两种风格混用，需统一 |

**B 类集中模块**（按 div 文件数）：pkg-im(78)、pkg-circle(42)、pkg-mine(19)、pkg-classics(14)、pkg-course(12)。

**这是"还原度不高、混入老旧原型、体验差"的根本原因** —— 大半前端是 V0 React 直出、仅做了机械语法替换，未真正按 uniapp/Vue3 标准重写。

---

## 三、其他系统性问题

1. **三态缺失**：仅 9/250 数据驱动页三态完整；B 类页几乎全缺 loading/error/empty。统一组件已存在（`app-skeleton/app-error/app-empty`）但采用率低 → 加载时白屏，体感"走不通"。
2. **巨型组件**：10+ 文件超 1000 行（最大 1586），复用层薄（58 组件 / 仅 3 composable），逻辑大量重复。
3. **风格双轨**：281 页 div+Tailwind vs 134 页 view+scoped，class 体系不统一。

---

## 四、分阶段作战计划

### P0 — 跑通（上线硬门槛）
- [x] build:h5 通过
- [x] 联调路径核对（/api/v1 匹配）
- [ ] **启动后端(postgres/redis)+前端实测核心链路**，逐个复现运行时"走不通"点并修复（登录→首页→商品→下单→支付→IM）

### P1 — 体验达标（用户痛点核心）
- [ ] 281 个 B 类页按模块批量补三态（loading 用 AppSkeleton / error 用 AppError+重试 / empty 用 AppEmpty）
- [ ] 写操作页补防重复提交锁

### P2 — 质量与还原度（顶级水平）
- [ ] B 类 div+Tailwind → view+语义 class 统一
- [ ] 对比 V0 原型（`V0-6.24` 311 page.tsx）修正还原偏差
- [ ] 拆分巨型组件、抽取 composable

---

## 五、执行策略

- **按模块推进**，每模块：实测页面 → 修阻断 → 补三态 → 统一风格 → 对比原型校验。
- 优先级：交易链路(shop/order/pay) > 高频(home/circle/im/mine) > 长尾。
- 每模块改完跑 `vue-tsc` + `build` 验证不回归。
