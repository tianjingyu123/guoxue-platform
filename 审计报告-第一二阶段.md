# 国学平台后端审计报告（第一、二阶段）

> 审计范围：`apps/server`（后端）+ `apps/admin`（后台）+ `apps/mobile`（移动端）
> 方法：通读源码 + 4 路并行专项审计 + 确定性工具（tsc / ESLint / pnpm audit / 测试套件）
> 性质：**只读审计，未修改任何代码**

---

## 〇、整体评估（一句话）

**工程底子远好于"弱模型生成"的预期**：后端骨架生产级、类型 0 错误、测试 4336 全过、鉴权基座设计专业。**真正的风险集中在三处**：① 一批**资金一致性致命缺陷**（提现不扣币等）；② **移动端 ~90% 是未接后端的空壳**；③ 数据库**删除连环炸 + 零软删除**。

### 客观工具结果
| 工具 | 结果 |
|---|---|
| TypeScript 类型检查 | ✅ 0 错误 |
| ESLint | ⚠️ 4 个错误 |
| 测试套件 | ✅ 345 套件 / 4336 用例全过（但鉴权层 0 覆盖、7 个假测试）|
| 依赖漏洞 (pnpm audit) | 🟠 49 个（12 高危 / 33 中 / 4 低）|
| 硬编码密钥 | ✅ 无；.env 已被 git 忽略；密钥走环境变量+启动校验 |

---

## 一、致命级问题（Critical）— 必须最优先

### A. 资金一致性（直接经济损失）

**C1. 用户提现全程从不扣减余额 → 可无限套现** 🔴最严重
- 位置：`wallet.service.ts:64-88`、`finance.service.ts:397-428`
- 问题：提现申请只建一条记录，审批/打款全程只改状态，**从无任何扣币/冻结**。用户提现成功后余额不变，可继续消费同一笔钱并反复提现。
- 修法：`submitWithdraw` 同事务内 `coin.freeze(amount)`；打款时从冻结结算；驳回时解冻。三步均加状态条件防重。

**C2. 签到/每日任务积分从不入账**
- 位置：`checkin.service.ts:34-49,137-143`
- 问题：算出奖励分只写进签到记录，**从不写入积分钱包**，用户签到得分却无法消费。
- 修法：事务内调用 `pointsService.earnPoints()` 真正入账。

**C3. 课程购买无事务 + 缺唯一约束 → 并发重复下单**
- 位置：`course.service.ts:514-575`
- 问题：防重依赖一个**并不存在**的唯一约束（`Order` 表无 `userId+type+targetId` 唯一键），并发可重复建单。
- 修法：Redis 锁或加部分唯一索引。

**C4. 圈子付费入圈：扣币与建会员关系非同一事务 → 钱货两空**
- 位置：`circle.service.ts:461-537,558-571`
- 修法：扣币与开通同事务，或失败补偿退款。

**C5. 付费提问扣币无事务 → 丢钱**
- 位置：`question.service.ts:40-67`（先扣币后建记录，中间异常则钱没了）

**C6. 围观付费事务边界错误 → 可重复扣费**
- 位置：`question.service.ts:133-145`（外层事务内调的 `coin.spend` 走的是独立事务，回滚无效；且无幂等去重）

**C7. 退款金额完全无校验 → 任意金额退款**
- 位置：`shop.dto.ts:516-525,495-512`、`shop.service.ts:797-808`
- 问题：退款金额由前端任意指定，不比对订单实付额、不判负/超额，直传支付网关。
- 修法：金额加 `@IsPositive`，service 强制 `退款额 ≤ 已付-已退`。

### B. 安全（可被外部利用）

**C8. 实名核验接口沦为"身份证撞库预言机" + OCR 明文泄露**
- 位置：`identity.controller.ts:20/28/36`、`identity.service.ts:85-116`
- 问题：任意登录用户可传**他人**姓名+身份证号核验真伪（撞库），OCR 原样返回身份证号/地址全量明文。属严重隐私滥用。
- 修法：核验绑定本人、禁止自由传任意身份证、OCR 脱敏、加严格频控+审计。

**C9. AI 数据探索把 AI 生成的 SQL 拼接后执行（注入/越权读）**
- 位置：`data-explorer.service.ts:147,153`
- 问题：自然语言→AI 生成整条 SQL→拼接执行，黑名单不足以防绕过。
- 修法：改为 AI 只产结构化意图，后端参数化拼装 + 字段表名白名单 + 只读账号。

**C10. AI 翻译/语种识别接口未鉴权 → 匿名刷付费 API**
- 位置：`ai.controller.ts:110,119`（缺 `JwtAuthGuard`，同模块其他接口都有）
- 修法：补登录守卫 + 限流。

**C15. 查看任意他人订单物流（姓名/电话/地址）** ←横向越权
- 位置：`shop.controller.ts:711-720`、`shop.service.ts:1271-1280`
- 问题：`getLogistics` 漏传 userId，归属校验被短路。改个订单号即可拉他人收货信息。
- 修法：传入 `req.user.id` 做归属校验（同文件其他方法是对的，属遗漏）。

### C. 业务红线与数据安全

**C11. 运势功能是纯随机数造假并落库**
- 位置：`fortune.service.ts:124-149`
- 问题：运势五维分（事业/财运/健康等）= `Math.random()*40+60`，幸运方位/颜色/数字全随机，还存入数据库伪装成真实结果。**直接违反项目 CLAUDE.md "禁止纯随机生成"红线**。
- 修法：接入八字/历法干支引擎重写（**需业务决策算法口径**）。

**C12. 数据库 138 处级联删除 + 全库零软删除 → 删除连环炸**
- 位置：schema 全局（`User`/`Circle`/`Station` 等关系）
- 问题：删 1 个用户会物理连锁删除其约 35 张子表（订单、钱包流水、分账、收益…），删了找不回。
- 修法（**架构级，需决策**）：资金类子表 Cascade 改 `Restrict`/`SetNull`，核心实体引入软删除。

### D. 稳定性（高并发会宕机）

**C13. 推荐/向量定时任务全表加载 → 内存溢出**
- 位置：`collab-matrix.task.ts:27`（每小时全量订单）、`tfidf-vector.provider.ts:127`、`vector.service.ts:284`
- 修法：游标分批 + 启用 pgvector。

**C14. 首页接口零缓存 + 假分页 → 压垮数据库**
- 位置：`home.service.ts`（全站最高频接口，每次打 6+ 次库，按 id 字符串排序，page≥2 切片到空）
- 修法：整体加缓存（30-60s）+ 重写为真实分页。

---

## 二、高级问题（High）— 严重，需尽快

### 安全
- 分佣比例/费率写入无范围校验（可 100 倍发放）`commission.dto.ts`
- 商家详情明文返回身份证号 `merchant.service.ts:50`
- 用户/管理端提现列表明文返回完整银行卡号 `commission.service.ts:362,313`
- 支付密码重置不校验短信验证码 `payment-password.service.ts:43`
- 短信验证码可爆破（验证失败不消耗）`sms.service.ts:150`
- 支付密码可爆破（无失败锁定）`payment-password.service.ts:34`
- 上传 SVG/HTML 存储型 XSS `upload.controller.ts:30`、`local-storage.provider.ts:12`
- 批量上传完全不做魔数校验 `upload.service.ts:25`
- 任务池写接口无角色限制（任意用户改任务/认领/转交）`task.controller.ts:44,66,83,97`
- AI 语音转写 audioUrl SSRF（可探测内网/云元数据）`media-ai.service.ts:139`
- 多个写操作用内联/Record 类型 body 绕过校验 `system.controller.ts:666,700`、`finance.controller.ts:95`、`bigscreen.controller.ts:77`、`shop.controller.ts:852`
- 售后退款金额 / 资金冻结金额 / 会员发放天数 均无校验
- 换绑手机号把新旧完整手机号明文写日志 `account.service.ts:96`

### 正确性 / 可靠性
- BullMQ 全局无重试配置 → processor 抛错即进死信 `queue.module.ts:31`
- 大量外部调用无超时（银联/微信账单/DeepSeek直连/腾讯云审核/IM/地图/快递/TTS/邮件/COS）→ 第三方抖动拖垮同步业务
- 站长提现审批 TOCTOU（并发可双批）`commission.service.ts:335`
- 风控 scanFraud 无锁 + createMany 无去重 → 重复风控记录 `risk-control.service.ts:151`
- 课程 studentCount 被当浏览量自增 → 可无限刷量 `course.service.ts:106`
- 微信充值回调存在并发重复入账窗口 `coin.service.ts:336`
- 悬赏解付/退款币操作与状态更新非原子 `bounty.service.ts:75,92,229`

### 数据 / 性能
- 多个定时任务全表加载大表（user-interest/ctr-calc/ai-event-bus 等）
- 多处读 N+1（推荐 feed / 直播分钟采集 / 知识图谱入库 / 竞赛答题）
- 全局分页 pageSize 无上限（`pageSize=100000` 可拉全表）
- 手写缓存（discover/search/content/classic）缺击穿锁+空值缓存
- 诗词 `poetry.getHome` 无缓存 + 全表 groupBy
- 关键索引缺失：`LiveRoom(status,viewCount)`、内容四表 `scheduledAt`、`Circle(status,memberCount)`、`BountyReview(status,createdAt)`

### 测试
- **16 个鉴权守卫整层 0 单元测试**（越权/分站隔离/角色校验从未被验证）
- 7 个 spec 形同虚设（只 `toBeDefined()`，含命理 fortune）

---

## 三、中 / 低级问题（择要）

**中级**：AI 接口无按用户配额（成本滥用）；备份用 shell 字符串拼接；CORS+credentials 默认白名单含 localhost；admin 端手机号/卡号不脱敏；支付日志泄露第三方报文；金额普遍缺 `@IsPositive`（防御纵深）；内容写操作缺 `@MaxLength`；缓存 TTL 无抖动（雪崩风险）；秒杀名额无原子防超卖；多处续费/签到时区与并发问题；分页公共封装仅 5 文件使用、70 个 service 各自手写。

**低级**：refreshToken 软撤销改密后旧 token 短时有效；部分 AI 接口已登录但无限流；短信无每日上限；`/metrics` 无鉴权；`as any` 358 处（集中在命理计算器，类型洗白）；动词式 RESTful 路径零散；2 处错误响应绕过统一格式。

---

## 四、数据模型设计问题（248 表）

- 资金明细塞 Json（`HuifuSplitRecord.receivers`）→ 无法对账/聚合 → 拆子表
- `User` 超宽表（~104 字段）→ 冷字段拆 1:1 扩展表
- 130 张表有 createdAt 却缺 updatedAt（状态机表也缺）
- `ProductSku.skuCode` 缺唯一约束；`Order` 多个关联字段是裸标量无外键
- `Station` 注释承诺"到期软删除"但字段根本不存在（功能与 schema 不符）

---

## 五、前后端覆盖度（第一阶段结论）

- **admin 后台**：60 个板块全部真功能（无空壳）；技术债 = 空数据态仅 34%、防重复提交 <3%、一个奇门排盘 mock 端点、30+ 页面绕过统一 API 层。
- **移动端**：314 页面仅 ~11% 真连后端；5 个 Tab 有 3 个是静态壳；订单全链路 0 接口；课程/IM/电子书/诗词等大量空壳；18 个数据文件无 API 对象。**好消息**：电子书/诗词/排盘结果/古籍听书等后端接口现成，仅"未接线"。
- **后端有、前端没用**：1541 接口远超前端使用量，大量 AI 网关/运营接口闲置。

---

## 六、第三阶段：修复计划（按"出事后果"排序）

> 原则：安全 + 数据完整性 > 正确性 > 性能 > 风格。每项遵循：先写复现失败测试 → 修 → 跑测试/类型/扫描 → 通俗小结。

### 批次 A｜紧急止血（资金 + 安全致命）★最高优先，多为快速修复
C1 提现扣币、C2 签到积分、C7 退款金额校验、C3-C6 各扣币事务化、C8 实名核验绑本人、C10 AI 翻译鉴权、C15 订单物流越权。
（C9 NL2SQL、C11 运势、C12 删除连环炸 属结构性/需决策，见下。）

### 批次 B｜高危安全加固（快速修复为主）
爆破防护（短信/支付密码失败锁定）、上传 XSS+魔数校验、SSRF 白名单、任务池鉴权、敏感数据脱敏（身份证/银行卡/手机号）、各金额范围校验、内联 body 改 DTO、依赖漏洞升级（12 高危，尤其后端 multer）。

### 批次 C｜数据完整性与可靠性（结构性，含需决策项）
- **C12 级联删除→Restrict + 软删除**（架构级，需你确认）
- 资金 Json 拆表、coin 服务事务化改造、BullMQ 重试、外部调用统一超时封装。

### 批次 D｜性能（结构性）
C13 定时任务分批+pgvector、C14 首页缓存+分页、补关键索引、修 N+1、全局分页上限、手写缓存补防护。

### 批次 E｜质量债
补 16 个鉴权守卫测试、修 7 个假测试、admin 三态 UI、防重复提交、分页收敛到公共封装。

### 批次 F｜移动端接线（最大功能缺口，需大量业务确认）
把空壳页面接到后端，逐模块确认预期行为；优先"后端现成只差接线"的模块（电子书/诗词/排盘/古籍）。

### 需要你（业务方）拍板的事项
1. **运势算法口径**（C11）：按八字命理引擎算？还是其他规则？
2. **删除策略大改**（C12）：是否做软删除 + 资金表防级联删除（工作量大但安全关键）。
3. **移动端范围**：哪些模块要优先接线、哪些当前不需要。
4. **半成品功能上线意向**：直播/电商/课程/IM 哪些是近期真要上线的（决定投入顺序）。

---

## 七、完成标准（Definition of Done）
- 所有"致命/高"问题已解决；
- 测试通过 + 类型检查 + Lint + 安全扫描通过（或对例外书面说明）；
- 前端每个功能都有可用后端支撑，无空壳；
- 接口风格统一、错误处理一致；
- 关键模块有简要文档。
