-- 迁移保留说明：
-- 此迁移最初依据本机 Docker 5432 的旧副本生成；真实生产服务实际连接本机 PostgreSQL 5433。
-- 真实生产库在 77 条迁移基线上已有历史业务对象，缺少的结构由后续专用迁移使用
-- IF NOT EXISTS / ADD COLUMN IF NOT EXISTS 等安全增量语句补齐。
-- 因此这里只保留一个空事务用于连续迁移登记，避免错误副本生成的批量 DDL 与真实库冲突。
BEGIN;
COMMIT;
