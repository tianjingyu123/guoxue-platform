// ── 排盘正确性闸门 ──
//
// 从 `tool-calculation.service.ts` 抽出（2026-09-19）。
// 抽出的理由：`/tools/:id/mock` 端点也必须守同一道闸门，
// 而注册表服务若为了拿这两张表去 import 计算服务，
// 会把整包 calculator 拖进它的依赖图。闸门是纯数据，独立成模块最干净。

/**
 * ══ 2026-09-20 批量下架：无核验价值的四类 ══
 *
 * 决策人：「把错误的和没有核验价值的计算器算法删除，以免误导我们后期的算法。
 * 后端计算器当时本来就是在没有目的的情况下整理的……把有价值的算法留下但是要审查，
 * 后期可以作为智能体广场的智能体的后台算法支持，但是要有把握，宁缺毋滥。」
 *
 * ══ 一个说明问题的数字 ══
 *
 * 工具目录 142 个，**前端只引用 18 个，124 个是后端独有、C 端根本进不去**。
 * 这印证了「没有目的整理出来」——多数工具从未接入产品。
 *
 * ══ 本次删除的判据（三条任一成立即删）══
 *
 * 1. **无计算**——输入只是选择器或关键词，输出是查一张常量表。没有算法可验证。
 * 2. **无可验证基准且无内部不变量**——既对不了前端、对不了典籍定数，
 *    也没有几何或算术上的必然性。
 * 3. **与已有工具重复**。
 *
 * 已删十六个，分四类：
 *
 * | 类别 | 工具 | 理由 |
 * |---|---|---|
 * | 相术类 | 面相、面相流年、面相十二宫、手相 | 输入主观描述→输出描述文本，无计算无基准；且「相貌定命运」断语风险高 |
 * | 运势模板 | 生肖运势、星座运势、流年运势、时辰运势 | 按周期套模板，无典籍定数；与八字/紫微流年重复 |
 * | 数字吉凶 | 手机号分析、数字能量 | 数字八星磁场是现代发明，无典籍依据 |
 * | 纯内容检索 | 国学经典、名句检索、古地名、书法字体、姓氏源流、宅相风水 | 是知识不是算法，属知识库职责 |
 *
 * **这些不进 `REMOVED_WRONG`**——它们不是算错了，是不该以「计算器」的形态存在。
 * 内容有价值的部分（如形煞表）应迁入知识库，不在此处保留一个假的计算入口。
 *
 * ══ 第二批（同日，121 → 115）══
 *
 * 第一批靠「前端零引用 + 人工归类」筛出来。第二批换了个能自动跑的判据：
 * **给两组差异极大的入参，输出是否变化**。不变即意味着输入到不了输出——
 * 要么是静态表冒充计算，要么是参数根本没接进去。
 *
 * 全量跑完 121 个，25 个输出完全不变。但**其中大部分是假阳性**：
 * 通用入参没命中它们的字段名，两次都走了默认值。
 * （这个坑我在 qizheng 和 shanxiang-qimen 上各栽过一次——
 * 测试传 `degree` 而函数收 `zuoShan`，24 个用例跑的是同一张盘还全绿。
 * 所以**判据报红之后必须先确认入参形状**，不能直接下结论。）
 *
 * 逐个核对入参后，真正「函数体内读不到任何输入字段」的有 5 个，
 * 加上一个经实测确认算错的，本批共删 6 个：
 *
 * | 工具 | 性质 | 理由 |
 * |---|---|---|
 * | 二十四山、六爻断卦、梅花外象、奇门应讯、紫微星曜 | 判据① 无计算 | 输入只是一个查询键，输出＝该条目＋全表＋一段拼接文本 |
 * | 玄空九宫 | **算错** | 飞星恒逆飞，运盘必然顺飞；见 `REMOVED_WRONG` 条目 |
 *
 * 前五个同样不进 `REMOVED_WRONG`（不是算错）；玄空九宫进。
 *
 * ── 同批审过但**保留**的 ──
 *
 * - `jinsuo`（金锁玉关）：八方砂水表与《金锁玉关》总诀逐条吻合
 *   （坎砂主聪明、坤砂主旺人、乾水主吉、兑水主吉……八宫全对），核心是对的。
 *   **但只做到八宫粒度**——金锁玉关本是二十四山砂水诀，
 *   `mts` 字段列了每宫三山却未差异化使用。保留待核验，不因此删。
 * - `yanggong`、`xuankong-jiugong` 以外的默认值兜底型：
 *   报红只是通用入参没命中，`yanggong` 另有专项 spec 且通过。
 *
 * ══ 第五批（同日，106 → 103）：一条必须守住的分界 ══
 *
 * 判据①「无计算」会把**所有纯词典**一网打尽。但继续往下删的时候发现，
 * 这些词典里的内容并不是一回事，**混在一起删会毁掉不可再生的东西**：
 *
 * | 类型 | 例子 | 能否再生 | 处置 |
 * |---|---|---|---|
 * | **现代释义改写** | 「紫微：帝王之星，好面子重排场」「角宿：天生领袖气质」 | 可以——换个人写一遍就有 | **删** |
 * | **典籍原文** | 《周易》卦辞彖象爻辞、《灵棋经》卦诗、六壬课经断语、神煞歌诀 | **不能**——原文只有一份 | **保留待迁知识库** |
 *
 * 判断办法：看它是**转述**还是**引文**。
 * 「乾。元亨利贞。」「青龙在天寅位上，木德星君司春令」是引文；
 * 「智谋出众但心思不定」是转述。引文错一个字就是错的，转述没有唯一答案。
 *
 * ── 本批删除（3 个，与第二批那 5 个完全同类：现代释义 + 查询键）──
 * `liuyao-shendong`（六爻神动释义）、`xingxiu-mingli`（二十八宿性格）、
 * `ziwei-sihua-fei`（紫微四化飞释义）。
 * 对应第二批已删的 `liuyao-duangua`、`ershisi-shan`、`ziwei-xingyao`，口径一致。
 *
 * ── 本批**不删**、另立一类「纯词典·内容为典籍原文·待迁知识库」──
 * `zhouyi-64gua`、`liuyao-guaci`、`liushisi-tupu`（周易卦辞彖象爻辞）、
 * `lingqi-jing`（《灵棋经》卦诗）、`daliuren-kejing`（六壬课经）、
 * `liuren-shensha`（神煞歌诀）、`qimen-chuanyin`（穿壬案例——案例材料本就稀缺）。
 *
 * 它们**已被闸门挡住、不出结果**，留着零成本；删了内容就只剩 git 历史。
 * 正解是把文本迁进知识库，再删这层假的计算入口——**迁移之前不动**。
 *
 * ⚠️ 附带查出一个尚未处理的**单一真源问题**：
 * **仓库里有 14 个文件各带一份 64 卦表**（dayan-shifa / heluo / jiaoshi-yilin /
 * jinqianke / kongming / liushisi-gua / liushisi-tupu / liuyao-guaci / liuyao-najia /
 * liuyao-shendong(已删) / meihua / meihua-duangua / xuankong-dagua / zhouyi-64gua），
 * 其中 7 份含真卦辞。按「同一概念有 n 份实现就有至少 n−1 份是错的」，
 * 这里几乎必然藏着错字与错配。**尚未逐份比对，列为待办。**
 *
 * ══ 余下工具的处置原则 ══
 *
 * 仍有一百余个未核验工具。它们**已被 `VERIFIED_TOOLS` 闸门挡住**（不放行结果），
 * 故不急于删——逐个核验，核过一个放行一个；核不动的（无基准可依）再删。
 * 这与「宁缺毋滥」一致：**不放行等于没有，但保留源码留了重建的余地。**
 */

/**
 * 算法校验闸门（2026-09-18）
 *
 * 决策人：「后端如果大面积错误要做纠正，不能让错误算法和思路留在咱们系统里。」
 *
 * 已经实测出两个算错的：
 * - `jinkoujue`：拿前端记录的竞品黄金基准一比，**只有日柱对**——
 *   月将差了半年（2025-09-29 秋分后该用辰将，它给谷雨后的酉将），
 *   月将是「月将加时」的根，连带将神、贵神、人元全错。
 * - `xuankong-feixing`：山向盘顺逆判定用错了阴阳来源，
 *   八运子山午向该是「双星到向」被算成「旺山旺向」（详见接续文档 §2.43）。
 *
 * 两次都是同一个模式：这批 calculator 与前端各自独立实现，
 * 前端经过实测校准、后端没有，而且**两边各自都能自洽地跑出一个盘，不比对就看不出来**。
 * 所以其余未经比对的，一律不能默认它是对的。
 *
 * 闸门的做法：**只有核验过的工具允许出结果**，其余明确拒绝并说明原因，
 * 而不是返回一个可能错误的盘——错的盘比没有盘更糟，用户会照着它做决定。
 * 核验一个就把它加进 VERIFIED 一个（核验＝与前端/教科书基准逐项比对通过）。
 */
export const VERIFIED_TOOLS = new Set([
  // 走 packages/shared 的同一份算法，前后端一致（§2.45 逐项核过）
  "qimen", "qimen-yang", "daliuren", "liuyao", "meihua", "xiaoliuren",
  // 独立引擎包，与前端同源
  "bazi", "ziwei",
  // 纯历法/查表类，无流派分歧
  "wannianli",
  /**
   * 电子罗盘。⚠️ 它当初是以「纯查表、无流派分歧」为由进的白名单，**并未逐层核过**，
   * 2026-09-19 为完善罗盘功能去读它，查出**地盘正针整盘偏一个山**（§2.88）。
   * 判据是它自己前后矛盾：`SHAN_INFO` 自带的每山中心度数喂回 `degreeToShan`，
   * 24/24 全不符且都恰好偏 15°。连带修了三针偏移、穿山龙取法、
   * 透地/分金起算点、六十四卦与二十八宿无读数共五处，现有 19 个断值用例守着。
   *
   * 教训：**「纯查表类」不是免检理由**——查表一样会错在下标上。
   */
  "dianzi-luopan",
  /**
   * 八宅（2026-09-19 修复后核验通过，§2.67）。修之前有四处错：
   * ① 命卦公式（后两位数字和再 11 减），1990 男算成坤——东四/西四判反，全盘吉凶颠倒；
   * ② 八方游星表**八行错七行**，艮行与兑行还一字不差（抄表漏行的痕迹）；
   * ③ 朝向拿 DIR_ORDER 的索引去查 BA_GUA 数组，坐坎算出朝乾；
   * ④ 宅命匹配比「东四命」==="东四宅"，恒为 false。
   *
   * 现在游星改由翻卦变爻法现算（上·中·下·中·上·中·下），三个独立来源互证：
   * 变爻推导、公开的大游年歌诀、前端 `pkg-paipan3/lib/bazhai-data.ts`（本来就是对的）。
   * 命卦另有第三方 App 实测基准（1990 男＝坎、女＝艮）。
   * 还补了原先缺失的**命盘**（以命卦起游星，管个人吉方）。
   */
  "bazhai",
  /**
   * 立极尺（2026-09-19 定案后核验通过，§2.87）。
   * 尺长原取 40.96/48.0 cm，源码无出处；现定为门公尺 **42.9 cm**、丁兰尺 **38.78 cm**，
   * 依据是通行「鲁班尺吉数尺度」表的判别检验——42.9 命中 14 个区间中的 13，
   * 原实现只命中 3（50.4→4、46.08→4、42.0→5）。另补齐《鲁班经》四小字两级。
   */
  "lijichi",
  /**
   * 五运六气（2026-09-19 修复后核验通过，§2.82）。修之前有四处错，
   * 且都长在**正确的骨架上**——天干化运、地支化气两张基础表逐项对过《素问》全对，
   * 错的全在推算层：
   * ① 客气整体错位两步（司天被摆在初之气），导致「终之气 ≠ 在泉」**自相矛盾**——
   *    同一次计算里 zaiQuan 报阳明燥金、终之气却排出厥阴风木，1984/2020/2024/2025/2026 五年全违；
   * ② 主运/客运五步套同一个太过不及（甲年排出「土太过·金太过·水太过·木太过·火太过」），
   *    而运气学是太少相生、逐步交替；
   * ③ 太乙天符永远报不出来（天符排在它前面而取 `find` 第一个命中），
   *    戊午己丑己未乙酉四年被降级成天符；同天符/同岁会写死 false 从未实现；
   *    岁会判定又过宽，把壬寅癸巳庚申辛亥误判进来（寅巳申亥是长生位不是正位）；
   * ④ 顺逆恒为「平」——`entry.rel.includes("顺")` 判错了字段，
   *    rel 的五个取值无一含「顺」「逆」二字，两个分支永不命中。
   *
   * 现有三重判据把关：内部不变量（三之气≡司天、终之气≡在泉、太少交替）、
   * 《素问》同化定数（天符12/岁会8/太乙天符4/同天符6/同岁会6，按甲子一轮逐年点名）、
   * 以及与`modules/paipan/engine/wuyunliuqi-engine.ts`（原前端引擎，2026-09-21 迁入服务端） 逐条对照——
   * 前端这几处本来就是对的，这是第 6 次「后端错·前端对」。
   */
  "wuyun-liuqi",
  /**
   * 姓名解析（2026-09-19 核验通过，§2.96）。
   *
   * 外部基准：旧版 App「姓名详解」实测「张识华」
   * ——天12(木) 地33(火) 人30(水) 外15(土) 总44(火)，三才木水火。
   * 我们的实现**五格五项＋五行五项 10/10 全中**，三才亦同。
   *
   * 这一条同时钉死三件事：五格公式、康熙笔画表、三才五行取法。
   * 尤其是笔画——旧版取的是康熙（張11 識19 華14，非简体张7识7华6），
   * 若仍用原来那个 `(code-0x4e00)/1200+3` 码点兜底，张得 7 画、天格会是 8 而非 12。
   *
   * 修之前的三处：①三份实现三种错法（前两份 `人格≡总格`、`外格≡1` 恒成立，
   * 只有单姓单名算对）；②三处都拿 Unicode 码点算笔画，77 个常用姓名用字 36% 走此兜底；
   * ③本地表名为 KANGXI_STROKES 实存简体。均已修，现有 20 个断值用例守着（含红线 5 条）。
   *
   * ⚠️ 放行的是**五格/三才/笔画这一层**。八十一数理的断语属编辑内容、
   * 生肖字根与四维评分尚无基准，不在本次核验范围。
   */
  "xingming-jiexi",
]);

/**
 * 已实测算错、**实现已删除**的工具（决策人 2026-09-18：不准确的计算器直接删除，以免后期造成误导）。
 *
 * 留这张表不是留算法，是留一句说得清的拒绝理由——
 * 直接「工具不存在」会让调用方以为是配置问题，反复重试。
 */
export const REMOVED_WRONG: Record<string, string> = {
  /**
   * 2026-09-20 删除。这条与其它几条不同：**不是算错了，是实现的术根本不对。**
   *
   * 会员账号实测旧版「孔明神卦」（页内标题「诸葛神卦」）：
   * 卦象显示 **5 个 0/1**（如 1 1 0 1 1）——**五爻，2⁵ = 32 卦**；
   * 卦名是**专名**（如「开发卦」），配**四句诗**
   * （「蚌中珠献瑞　石内玉生光／进财宝望吉　有祸不成殃」）
   * 与**十项论断**（总断/求职/升学/投机/恋爱/婚姻/迁居/旅行/健康/失物）。
   *
   * 而本实现用的是**周易六十四卦**（乾为天/坤为地…）加周易卦辞、彖辞、大象。
   * 两套完全不同的术，不存在「改一改就对」的可能。
   *
   * 删除前另查出三处结构性错误（都已在 git 历史里，重建时不要重蹈）：
   *   ① `GUA_YAO_BITS`(64条) + `BITS_TO_GUA`(**只15条**)：六十四卦六爻组合必须两两不同
   *      （0–63 各一次），而 64 个卦只落在 **15 个互异值**上（「爻象0」被坤/师/泰/谦/临/复/
   *      明夷/升 8 个卦共用），**49/64 个卦的爻象是错的**；变卦翻转动爻后反查，
   *      **384 种组合里 189 种（49%）查不到**，被 `?? guaNum` 兜底成「变卦 = 本卦」。
   *   ② `xiaGua: name.slice(2,3)` 取的是卦名**第 3 个字**（水雷屯→「屯」），
   *      56 个非纯卦全错；纯卦碰巧取到象字，所以抽样看不出来。
   *   ③ 爻名 `${位}${dongYao % 2 === 0 ? "六" : "九"}`：位与阴阳顺序反（「二六」应「六二」），
   *      且**九/六 取自爻位奇偶而非本卦该爻的实际阴阳**——乾为天六爻皆阳，第二爻会得「六」。
   *
   * **重建方法**：采旧版 32 卦的「卦名 + 五爻编码 + 四句诗 + 十项论断」。
   * 旧版是摇卦（随机），需反复摇到集齐，约 32 组；页面文本可用 `uiautomator dump` 直接读。
   * 六十四卦那套数据（卦辞/彖/象）与本术无关，不要再拿来用。
   */
  kongmingshengua:
    "实现的术不对：旧版「孔明神卦」是五爻 32 卦体系（卦名如「开发卦」＋四句诗＋十项论断），" +
    "而本实现用的是周易六十四卦加卦辞彖辞象辞，两者无法互换。" +
    "另有三处结构性错误（卦爻编码 64 卦只有 15 个互异爻象致变卦 49% 退回本卦、" +
    "下卦取了卦名第 3 字、爻名九六按爻位奇偶而非本卦阴阳）。" +
    "重建需采旧版 32 卦的卦名/五爻编码/四句诗/十项论断。",
  jinkoujue: "月将过宫算错（秋分后误用谷雨月将），将神贵神人元连带全错，实现已删除",
  "xuankong-feixing": "山向盘顺逆飞判定用错阴阳来源，格局判定随之出错；正确实现已迁至 shared，本处实现已删除",
  // 2026-09-19：原 qimen-yin.calculator.ts 已删除，盘源改接共享引擎（见 qimen.calculator.ts）。
  // 但转盘/飞盘的流派口径未定，仍不放行——先对齐 C 端只是为了消除「同一时刻两个盘」，不等于已核验。
  "qimen-yin":
    "原实现恒判阴遁、不用节气、按月支查自造表定局（时家做成了月家），已删除；" +
    "现改接共享奇门引擎并对齐 C 端参数，但阴盘走转盘还是飞盘的流派口径待决策人确认，暂不放行",
  // 2026-09-19：核心计算整个是编的，已删除。
  // 最直接的证据是**十六神落在非整数宫位**（实测文昌 4.5 宫、地主 7.5 宫、四神 10.5 宫、
  // 太簇 13.5 宫）——源头是 `gong: (gongXianIdx + i * 1.5 - 1) % 16 + 1` 里那个 1.5 步长，
  // 而十六神各有定位，宫位必是整数，不存在「第 7.5 宫」。
  // 三算（主算/客算/定算）也是拍出来的：主算=文昌宫×3+太乙数%10、客算=始击宫×4+…，
  // 乘数 3/4/2 无任何依据；文昌宫、始击宫本身又由 gongXianIdx 加常数 +2/+5 得来。
  // 太乙积年与五元六纪的口径同样无从核对。重建需要可靠的太乙典籍基准，暂不具备。
  /**
   * 2026-09-19：行星位置整个是编的，已删除。
   *
   * 各行星黄经写作 `太阳黄经 + (距 J2000 天数 × 常数 + 常数) % 360`——
   * **把行星当成相对太阳匀速偏移**。真实行星是椭圆轨道、速度随距离变化，
   * 且内行星有规律的**逆行**，这个模型永远不会逆行。
   *
   * 判据不需要星历表，是**几何必然**：内行星永远靠近太阳——
   * 水星距日黄经差不超过 ±28°、金星不超过 ±47°。
   * 实测 2020–2026 共 56 个样本，**违反 44 个**，
   * 金星出现过距日 104°、114°、123° 这种几何上不可能的值。
   *
   * 这条判据还是**原点无关**的：两星角距不随二十八宿起点的假设改变
   * （两者走同一换算），所以不必先确定宿度口径就能定案。
   *
   * **同族的 `qizheng` 是对的**——它用 `sweph`（瑞士星历表）真算，
   * 同一判据同一批样本**违反 0/56**，且能给出土星逆行（编造版产生不了）。
   * 故本工具删除后应改走 `qizheng`，不要重建第二份星历实现。
   */
  "qizheng-siyu":
    "行星黄经由「太阳黄经＋天数×常数」生成，把行星当成相对太阳匀速偏移，永不逆行；" +
    "内行星距日角距（水星≤28°、金星≤47°，几何必然）在 56 个样本中违反 44 个，" +
    "金星出现距日 123° 这种不可能值。实现已删除；同族 qizheng 用瑞士星历表真算，改走它",
  taiyi:
    "十六神落宫算出非整数（文昌4.5宫、地主7.5宫等），三算公式为凭空设定的乘加，" +
    "核心计算不可信，实现已删除；重建需先取得可靠的太乙排盘基准",
  // 2026-09-19：是一张静态查表冒充排盘，已删除。三条硬证据：
  // ① **完全不随时间变化**——2020 年与 2026 年给出一模一样的盘，
  //    而奇门是时家之学，门星神逐时辰而变，不变即等于没排；
  // ② 52 个条目里只有 24 个是正经二十四山，其余 28 项来自**四个不同体系**——
  //    玄空替卦、风水兼向（11 项）、透地龙（10 项）、择日神煞到山（5 项），
  //    被混装进同一个数组，再给每项硬安上奇门的门星神；
  // ③ 52 条里有 47 条的 `tianPanGan` 字段装的是九星名、与 `jiuXing` 字段完全重复，
  //    根本不是天干。
  // 注：同族的 shanxiang-qimen 是真排盘（输出随时间变化），未受影响。
  // 2026-09-19：九宫整个是编的，已删除。最硬的一条是**本卦不进九宫**——
  // 天盘由 `(pos + dongYao - 1) % 8` 生成，只跟宫位和动爻有关，与本卦无涉。
  // 实测：乾为天／坤为地／水火既济／雷风恒四个完全不同的本卦，只要动爻相同，
  // 排出的八宫（天盘、地盘、成卦、阖辟、往来）**一模一样，种数=1**。
  // 卦是这门术的输入，输入到不了输出，等于没排。
  //
  // 地基同样是歪的：爻象表 `GUA_YAO` 注释写 `[下,中,上]`，实为 `[上,中,下]`
  // （逐卦核对：按注释解释 4/8 相符，按 [上,中,下] 解释 8/8；那 4 个"相符"的
  // 是乾坤离坎四个上下对称卦，本就分不出方向）。于是所有下游取爻都读错了一头：
  // 互卦取的是爻 2/1/6 这种错位组合，变卦又把动爻 1–3（初二三爻，属**下卦**）
  // 判成了上卦。中宫则无归藏，直接令天盘=地盘=上卦、往来写死「不动」。
  // 源码里另有两处「简化」注释（`简化：天盘=本卦之各变爻卦`、
  // `简化：按动爻偏移`）——这个气味已第五次命中。
  //
  // 前端引擎（2026-09-21 已迁至 `modules/paipan/engine/xiaochengtu-engine.ts`） 是对的且注明「已对照竞品黄金基准
  // 逐宫验证」，载有真正的霍斐然布卦法（本卦上卦→离9、下卦→坎1，变卦上卦→震3、
  // 下卦→兑7，本互上→巽4、下→坤2，变互上→艮8、下→乾6，中宫四正/正隅归藏，
  // 吉凶取卦气升降四式而非阖辟）。重建时照它移植，不要再自行推演。
  xiaochengtu:
    "九宫天盘由 `(宫位+动爻)%8` 凭空生成，本卦不参与——四个不同本卦只要动爻相同即排出" +
    "完全相同的八宫；爻象表方向标注与实际相反，互卦与变卦连带取错爻位；中宫无归藏。" +
    "实现已删除；重建须照前端已验证的霍斐然布卦法移植",
  "qimen-shangong":
    "静态查表冒充排盘：结果不随时间变化（奇门为时家之学），52 条目混装了玄空替卦、" +
    "风水兼向、透地龙、择日神煞四个体系，天盘干字段装的是九星名；实现已删除",
  /**
   * 2026-09-20：飞星恒逆飞，已删除。这是玄空家族里第二个算错的
   * （第一个是 `xuankong-feixing`，山向盘顺逆用错阴阳来源）。
   *
   * `flyStar()` 的宫位路线 `[5,6,7,8,9,1,2,3,4]`（中→乾→兑→艮→离→坎→坤→震→巽）
   * 是对的，**错在步进方向**：`val = center - i` 一律递减。
   *
   * 运盘（元旦盘）**必然顺飞，不存在逆飞的运盘**，所以这一条不是流派分歧。
   * 实测八运：得「坎3·坤2·震1」，正确应为「坎4·坤5·震6」（八入中顺飞）。
   * 九宫全错，且错法整齐——正是「集合对·方向反」。
   *
   * 更根本的问题是**三盘共用同一个 `flyStar`**：运星、山星、向星走同一条恒逆的路。
   * 山星向星的顺逆本应由坐山阴阳决定（这正是 `xuankong-feixing` 栽的同一个坑），
   * 此处连阴阳参数都没有。实测给 `yun=shanXing=xiangXing=8`，
   * 九宫全是 `3-3 / 2-2 / 1-1` 这样三星同值——三个盘完全重合。
   *
   * 另有编造气味两处：`FEI_XING_TRAIL` 与 `LUO_SHU_TRAIL` 两个常量数组
   * 挂着 `eslint-disable no-unused-vars` 摆在函数开头，**从未被使用**。
   * 看着像有依据，实际是死代码——与「简化」注释同族。
   */
  "xuankong-jiugong":
    "飞星恒逆飞（`val = center - i`），而运盘必然顺飞；实测八运得「坎3坤2震1」，" +
    "应为「坎4坤5震6」，九宫全错。且运/山/向三盘共用同一函数、无阴阳顺逆之分，" +
    "同入中数则三盘完全重合。实现已删除；玄空正确实现在 shared",
  /**
   * 2026-09-20：月柱整个是拍的，已删除。
   *
   * `monthGan = GAN[(year-4)*2 % 10 + (month-1)]`、`monthZhi = ZHI[(month+1)%12]`，
   * 注释写着「简化月柱计算」——「简化」这个气味第七次命中。
   *
   * 两条硬证据：
   * ① 与五虎遁比对 **24/24 全不符**（2024–2025 逐月），月干恒偏两位；
   * ② **下标越界静默回落**：`|| GAN[0]` 兜底，24 个月里有 6 个触发，
   *    于是输出过 **「甲丑」**——阳干配阴支，**六十甲子里不存在这个干支**。
   *
   * 第②条是最该记住的：一个工具输出了一个**不可能存在的值**，
   * 而整条链路没有任何地方拦住它。凡有 `|| 默认值` 兜底的下标运算，
   * 都要问一句「兜底掩盖了什么」。
   *
   * 讽刺的是同一个文件里日柱是对的——它调了 `bazi-engine` 的 `calcRiZhu`。
   * 月柱本可以照做，却自己拍了一个。
   */
  "bazi-fanpai":
    "月柱为「简化」拍算，与五虎遁比对 2024–2025 逐月 24/24 全不符，月干恒偏两位；" +
    "且下标越界由 `|| GAN[0]` 静默兜底，输出过「甲丑」这种六十甲子中不存在的干支。" +
    "月柱错则整个反推时辰的结论无效。实现已删除；月柱应照同文件日柱那样走 bazi-engine",
  /**
   * 2026-09-20：定局公式是编的，已删除。
   *
   * `juShu = (jieQiIdx % 9) + 1`、`yangDun = jieQiIdx < 11`，
   * 注释自承「节气定局（简化）」。
   *
   * 奇门定局是 **24 节气各配上中下三元、共 72 局**，
   * 每个节气的三元局数有定数（冬至一七四、小寒二八五、大寒三九六……），
   * 绝不是「节气序号模 9 加 1」。
   *
   * 与已核验的共享奇门引擎逐月比对：**12/12 全不符**，
   * 且本实现给出的局数序列是 1,3,5,7,9,2,4,6,8,1,3,5——
   * 一眼可见的机械等差，与三元无关。
   * 遁型分界也错：阳遁应为冬至至芒种（12 个节气），此处写 `< 11`。
   *
   * 另有死代码 `ganToGong()` 挂着 `eslint-disable no-unused-vars` 从未被调用。
   */
  "qimen-zhongshen":
    "定局用 `(节气序号 % 9) + 1`，而奇门是 24 节气配上中下三元共 72 局；" +
    "与已核验共享奇门引擎逐月比对 12/12 全不符，局数呈 1,3,5,7,9,2,4,6,8 机械等差；" +
    "阳遁分界亦少一个节气。实现已删除；应直接走共享奇门引擎取局",
  /**
   * 2026-09-20：日干支、建除、农历日期三层全错，已删除。
   *
   * ① **日干支用平均月长推算**：
   *    `dayCount = (year-2000)*365.25 + (month-1)*30.44` 取整。
   *    平均月长 30.44 天没有哪个月真的是这个数，误差逐月累积。
   *    与 `bazi-engine` 的 `calcRiZhu` 比对 2024–2026 共 **108 个样本，108 个全错**。
   *    日干支错 → 冲煞（冲新娘生肖）判定连带全错。
   *
   * ② **建除十二神按公历日号排**：`jcIdx = (d + monthZhiIdx - 1) % 12`，
   *    其中 `d` 是公历日号。建除本应由**月建之支与日支**决定
   *    （日支与月支相同之日为「建」，而后逐日顺排），与公历日号无关。
   *    而 `monthZhiIdx = targetMonth - 1` 又把公历月当月支序号——
   *    1 月得子（正月应为寅），偏两位且不按节气。
   *
   * ③ **农历日期是编的**：`lunarDate` 直接拿公历日号套「初/十/廿」＋数字拼字符串，
   *    根本没有查农历。用户会看到一个煞有介事却不存在的农历日子。
   *
   * 三层里任何一层都足以单独否定这个工具。择日类工具必须建立在真历法上。
   */
  "jiaqu-zeri":
    "日干支用「年×365.25＋月×30.44」平均长度推算，与 bazi-engine 比对 108 个样本全错，" +
    "冲煞判定连带失效；建除十二神按公历日号排而非月建与日支；农历日期由公历日号拼字符串编造。" +
    "实现已删除；择日类须走真历法（bazi-engine 的 calcRiZhu 与节气表）",
  /**
   * 2026-09-20：九宫整个是凭空生成的，已删除。
   *
   * 被「输出中的干支必须是六十甲子之一」这条扫描抓出来——
   * 它输出过 **戊亥 / 庚巳 / 丙丑**，全是阳干配阴支，六十甲子中不存在。
   * 根源是 `tianGan = \`${dayGan}${hourZhi}\``：**直接拿日干去配时支**，
   * 而时干必须由日干经五鼠遁推出。
   *
   * 其余三处同样无依据：
   * ① 定局 `juShu = (jieQiApprox % 9) + 1`——与已删的 `qimen-zhongshen` **同一个编造公式**；
   * ② 门/星/神由 `(juShu + i + 3|0|5) % 8|9` 生成，三个常数无任何出处，
   *    **值符值使完全不参与**——而值符随时干转宫正是时家奇门的核心；
   * ③ 只排**八宫**（`for i < 8`），奇门是九宫。
   */
  "qimen-shike":
    "时干直接拿日干配时支，输出过「戊亥」「庚巳」「丙丑」等六十甲子中不存在的干支；" +
    "定局用 `(节气序号 % 9) + 1`（与 qimen-zhongshen 同一编造公式）；" +
    "门星神由 `(局数+i+常数) % 8` 生成、值符值使不参与；且只排八宫。" +
    "实现已删除；应走共享奇门引擎",
  /**
   * 2026-09-20：定局表与双山表是好的，但**排盘部分整个是编的**，已删除。
   *
   * ══ 先说清楚哪些是好的，重建时别丢 ══
   *
   * `JU_72`（24 山 × 三元 = 72 局）与 `SHUANG_SHAN`、`SHAN_XIAN_TIAN` 三张表可用，
   * 其中 `JU_72` 的遁型分界在 2026-09-19 已修过（原注释「阳局范围 亥→巽」整体偏一个节气）。
   * **删除前的版本在 git 历史里，重建时取这三张表**，不要重新推演。
   *
   * ══ 错在哪 ══
   *
   * ① **用事干支取错了一头**。本实现用**坐山**的双山支
   *    （`SHUANG_SHAN[zuoShan]`），而前端 `pkg-paipan/lib/shanxiang-engine.ts`
   *    用**向**的双山支。前端那份带竞品黄金基准
   *    （2022 丑山未向 → 干支壬寅**丁未**、旬首甲辰、值符天冲、值使伤门），
   *    实测本实现同一课例给出「癸丑」——基准支持前端。
   *
   * ② **旬首算法不看天干**：`xunShou()` 只用地支
   *    `DI_ZHI[Math.floor(zhiIdx / 2) * 2]`，而旬首必须由干支共同决定
   *    （旬首支序号 = `(zi - gi + 12) % 12`）。
   *    实测它对自己给出的「癸丑」也算错——得甲寅，应为甲辰。
   *
   * ③ **四个宫标记全是宫号算术**：
   *    `isRuMu: pos % 5 === 0`、`isJiXing: pos % 7 === 2`、
   *    `kongWang: pos === (juShu % 9) + 1`、`maXing: pos === ((juShu + 3) % 9) + 1`。
   *    入墓由干落墓宫定、击刑有六仪定数、空亡由旬首定、马星由用事支三合定，
   *    没有一个跟宫号取模有关。而这四个标记**直接喂进格局断语**
   *    （坐山得位／朝向有气／山向犯空），所以断语跟着错。
   *
   * ④ **星门神天盘干全由 `(pos ± juShu + 常数) % n` 生成**，值符值使不参与——
   *    与已删的 `qimen-shike` 同型。
   *
   * ══ 重建路径 ══
   *
   * 照前端做法：本文件的 `JU_72` 定局 → 用事干支取**向**的双山支＋年干五鼠遁
   * → 交给**共享奇门引擎**排盘，不要再自己写一遍九宫。
   *
   * ══ 附一条给我自己的教训 ══
   *
   * 这个工具我在 2026-09-19 修过遁型分界，**修完没有通查其余部分**。
   * 修一处不等于审一遍——下次动到某个工具，顺手把它全文过一遍。
   */
  "shanxiang-qimen":
    "用事干支取坐山双山支（前端黄金基准取向的双山支，实测本实现给癸丑、基准为丁未）；" +
    "旬首算法只看地支不看天干，对自身输出也算错（得甲寅应甲辰）；" +
    "入墓/击刑/空亡/马星四个标记全是宫号取模（pos%5、pos%7、局数%9），且直接喂进格局断语；" +
    "星门神天盘干由 `(宫号±局数+常数)%n` 生成、值符值使不参与。实现已删除。" +
    "重建取 git 历史中的 JU_72／SHUANG_SHAN／SHAN_XIAN_TIAN 三表，排盘交共享奇门引擎",
};
