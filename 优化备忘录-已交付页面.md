# 优化备忘录（写给已接手联调的同事 / Claude Code）

> **用途**：本备忘录记录 v0 在做"原型(Next/React) → Vue3(uni-app)"高保真校准时，在**已经迁移完成、可能已进入你真实项目做前后端联调**的页面上，发现的视觉细节偏差。
> 这些偏差只看接口/功能跑不出来，必须对照原型逐像素核对才能发现。请你在你的项目里按下面的清单和方法核对修正。
>
> **最重要的原则**：下面的问题**因页而异、程度不同**，请**逐页实测核对原型**后再改，**不要全局批量替换**，否则会引入新的失真。

---

## 一、根因：AppIcon 的 `size` 默认单位是 `rpx`（最易踩坑）

`components/common/app-icon.vue` 的 `size` 属性**默认单位是 rpx**：

```
size?: number      // 默认单位 rpx
unit?: 'rpx'|'px'  // 默认 'rpx'
dim = size + unit  // 即 :size="20" → 20rpx → 在 750rpx 设计稿下 = 实际 10px
```

**陷阱**：原型(React)里图标用 Tailwind 的 `w-5 h-5`，**单位是 px**（`w-5`=20px、`w-4`=16px、`w-6`=24px）。
迁移时若直接把 `w-5`(20px) 写成 `:size="20"`，因为默认是 rpx，实际只渲染出 **10px —— 正好是原型的一半**，图标明显偏小。

### 换算口诀（750rpx 设计稿，1px = 2rpx）

| 原型 Tailwind | 像素 | 正确的 `:size`（rpx） |
|---|---|---|
| `w-3` | 12px | `:size="24"` |
| `w-3.5` | 14px | `:size="28"` |
| `w-4` | 16px | `:size="32"` |
| `w-5` | 20px | `:size="40"` |
| `w-6` | 24px | `:size="48"` |
| `w-7` | 28px | `:size="56"` |

> 经验：项目里大量 `:size="16/20/24/28"` 很可能是"把 px 当成了 size 值直传"，实际偏小一半。**但不是绝对**——有的页面图标本就该是小尺寸。**务必逐处对照原型的 `w-?` 类名核对**。

### 已实测确认的两个样本（程度不同，印证"必须逐页核对"）
- **直播间观看页底栏**：图标 `:size="20"` 实测仅 10px，原型 `w-5`=20px，**小一半**（严重）。→ 已修为 `:size="40"`。
- **圈子卡片(post-card)**：图标 `:size="16"` 实测 14px，原型 `w-4`=16px，**仅差 2px**（轻微）。

---

## 二、已由 v0 校准完成的页面（请同步这些修改到你的项目）

### ✅ 整个 `pkg-live` 分包（24 页）的 AppIcon 半尺寸 bug —— 已全部修复
**结论（已用浏览器逐包实测确认）**：半尺寸图标 bug **集中在 `pkg-live` 分包**（该包迁移者把原型 px 值直接当 `:size` 传，渲染减半，实测仅 6–12px）。
已对以下页按"原型 px × 2 = rpx"批量修正并实测复验（图标恢复 14–20px，对齐原型）：
- `watch`、`console`（逐个图标精校 + 浏览器实测）
- `analytics / create / earnings / end / host-data / hosts / obs-guide / obs / preview / products / replay-detail / replay-home / replays / reviews / schedule / settings / stream-config / team / theme / manage / plaza`（整页 `:size` ×2）
- **已正确、未改动**：`vertical`、`replay-comment`、`horizontal`（`vertical/replay-comment` 已用 40/56 等正确值；`horizontal` 用 `unit="px"`）。

### ✅ 非 live 分包 —— 经实测确认**图标尺寸基本正确，无需批量改**
逐包实测 `pkg-mall / pkg-course / pkg-shop / pkg-order / pkg-account / pkg-paipan / pkg-circle / pkg-mine` 等代表页，图标 median 均为 **14–22px（健康范围）**。
原因：这些包的迁移者用的是**已 ×2 的正确 rpx 值**（如 `:size="40"`=20px、`:size="32"`=16px）。
→ 所以**不要对非 live 包做整页 ×2**，否则图标会翻倍过大。如发现个别页偏差，仍按"逐页对照原型 `w-?`"单独处理。

### ✅ 直播间观看页 `vue3/src/pkg-live/watch/index.vue`（竖屏 id=1）的其它细节
对照原型 `app/live/[id]/page.tsx` 竖屏分支，已修正：

1. **底栏 6 个图标尺寸**：`:size="20"` → `:size="40"`（原型 `w-5`=20px）；送出箭头 `:size="12"` → `:size="24"`（原型 `w-3`=12px）。**这是"图标偏小"的主因。**
2. **底栏圆按钮**：`72rpx` → `80rpx`（原型 `w-10 h-10`=40px）；`flex` 改为 `0 1 auto`（原型按钮与输入框在窄屏下**等比收缩**，不是按钮固定、输入框独自压缩）。
3. **点赞图标色**：`#C41E3A`(故宫红) → `#ef4444`(纯红 red-500)。原型点赞用纯红，不是品牌故宫红。
4. **购物车按钮**：补 `act-btn--cart` 红粉渐变底 + 图标 `#f87171`(red-400)；角标底 `#C41E3A` → `#ef4444`。
5. **系统消息横幅三色**：进场=`rgba(255,255,255,.1)`淡白+白80%字；礼物=琥珀橙渐变(`amber-500/30→orange-500/30`)+`#fde68a`字；购买=红粉渐变(`red-500/30→pink-500/30`)+`#fbcfe8`字。礼物图标(emoji)在**末尾**，不是开头。原版误用统一金色块。
6. **弹幕列表**：统一为"故宫红标签 + 白90%正文"单一结构。删除了原版多余的 `system` 黑气泡分支和 `gift` 特殊分支（原型 `mockDanmaku` 全是普通弹幕，无"系统/欢迎"项；系统消息只走上面的独立横幅）。
   - 对应数据修正：`vue3/src/lib/live-data.ts` 的 `liveWatchComments` 删除了 `type:'system'` 的"欢迎来到直播间"假数据。

---

## 三、待你核对的高风险点（v0 尚未逐页处理，按方法自查）

> 这些是"很可能存在、但需你逐页对照原型确认"的同类问题。判断标准都是：**打开原型对应页 → 看元素的 Tailwind 类 → 按换算表核对 Vue 值**。

1. ~~全项目 AppIcon `:size`~~ **已系统排查完毕**：bug 仅限 `pkg-live`（已全修），非 live 包实测正确。无需再全项目排查图标尺寸。
2. **故宫红 vs 纯红混用**：
   - 品牌色/主按钮/选中态 = 故宫红 `#C41E3A`（对，保持）。
   - 但**点赞心、LIVE直播徽标、未读红点、删除/危险操作、红包** —— 原型多用**纯红 `#ef4444`(red-500) / `#dc2626`(red-600)**，迁移时容易被统一错写成故宫红。逐处核对原型的 `red-500/red-600` vs `text-primary`。
   - ⚠️ 反例：`mine/my-likes` 收藏页的点赞心，原型本就是 `text-[#C41E3A]` 故宫红，**那里用故宫红是对的**。所以**不能一刀切**。
3. **圆按钮/容器的 px→rpx**：原型 `w-10`=40px 应是 80rpx，常被误写成 72rpx。核对各页圆形按钮直径。

---

## 四、推荐的核对方法（屡试不爽）

1. 同时跑两端：原型 `http://localhost:3000/<route>`、Vue `http://localhost:5173`（用 `uni.reLaunch({url})` 跳转）。
2. 视口统一 **375×812**（iPhone 标准）。
3. 用浏览器 `getBoundingClientRect()` 量**两端同一元素的实际渲染像素**，而不是只读源码值。例如：
   ```js
   document.querySelector('.act-btn .app-icon').getBoundingClientRect().width
   ```
4. 两端数值对齐后再截图肉眼复核颜色/间距/形状。
5. 修一处、验一处，避免批量替换引入新问题。

---

*维护者：v0。每完成一页校准，会在"第二节"追加条目。*
