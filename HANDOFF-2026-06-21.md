# 项目交接文档（2026-06-21）

> 给接手的新 v0 账号。读完本文 + 三个权威文件，你就能无缝接续工作、不走弯路。
>
> **第一原则：永远以文件系统真实状态为准，绝不凭记忆或任何文字清单（包括本文）下结论。** 本文的"已完成/剩余"清单都可能过时，动手前必须用下面的命令重新核实。

---

## 0. 三个权威真源（开工必读，优先级高于本文）

| 文件 | 作用 | 怎么用 |
|---|---|---|
| `vue3/compare/route-map.json` | **每页迁移笔记的唯一权威源**。每条含 `proto`/`vue`/`status`/`owner`/`note`，note 里写清了该页的结构、mock、补的图标、compare%、坑。 | 查某页是否已迁：`grep '"proto": "/<名>"' vue3/compare/route-map.json`。读 note 了解实现细节。 |
| `vue3/src/pages.json` | uni-app 页面注册表。**注册了才算真正接入**。 | 数已注册页：`grep -cE '"path":' vue3/src/pages.json` |
| `vue3/src/utils/router.ts` 的 `ROUTE_MAP` | 原型路径 → vue 路径的静态映射。 | 查某原型路由迁到哪：`grep '<名>' vue3/src/utils/router.ts` |

判断"某页是否已迁"必须 **route-map + find 真实 .vue + pages.json 注册** 三者一致才算数。

---

## 1. 项目背景

- **目标**：把 Next.js 原型（`proto-ref-app/**/page.tsx`）逐页 **1:1 高保真**迁移到 uni-app(Vue3) H5（`vue3/src/**`）。
- **唯一真源是原型**：照抄其结构、配色、文案、布局，**不自由发挥、不重新设计**。
- **技术栈**：uni-app + Vue3 `<script setup>` + TypeScript。样式用 `rpx` 单位、uni 标签（`view`/`text`/`scroll-view`/`image`）。
- **图标**：自建 IconFont 体系，组件 `@/components/common/app-icon.vue`，图标库 `vue3/src/lib/icons-registry.ts`（lucide 路径数据，缺则手动补）。
- **分包结构**：`vue3/src/pkg-*`（pkg-circle/pkg-mine/pkg-order/pkg-agent/pkg-operator/pkg-settings/pkg-auth/pkg-common 等），按业务域归类。

### 多账号协作（历史背景，了解即可）
- **A 账号**：早期 `golden` 页，**锁定不动**。
- **B 账号**：`reviewed` 页，**已部署到正式项目**。若改动这些页须在 `vue3/compare/REVIEW-FIXES.md` 留备忘录。
- **v0（你和我）**：`migrate` 页，新迁移的页面，owner=`V`。

---

## 2. 当前进度快照（2026-06-21，已用文件系统核实）

- **pages.json 已注册：311 页**
- **route-map migrate 记录：206 条**（owner=V 的新迁页）
- **原型总页数：约 534 页**（含约 8 个 redirect 桩页 + 大量旧套/暂缓/demo，**不是都要迁**）
- **工作区状态：干净**（所有工作已 commit 落盘）

### 大板块完成情况（主体已完成）
circle 圈子 / 首页 / 排盘 paipan / 支付 / 详情 / mine 我的 / live 直播(含 creator) / shop 商城 / operator 运营(含线上分站 station) / course 课程 / classics 古籍 / im 聊天 / settings 设置 / ebook 电子书 / order 订单 / agent 智能体 / account 账户 / profile / poetry / mall / wallet(含 bill+bank-cards) / auth / search —— **均已完成**。

### 本轮会话（2026-06-21）我完成的长尾页（最近提交在前）
- `bots` 智能体广场 + `bots/chat` Bot对话页（pkg-agent，提交 02d1996，补 grid-3x3/scan/hand/paperclip 图标）
- `announcements` 平台公告（pkg-operator，用户裁定"照迁不跳"）
- `become-partner` 成为合伙人（pkg-operator）
- `appeal` 交易申诉（pkg-order）
- `image-viewer` 全屏图片查看器 + `scan` 扫码结果（pkg-common 新建分包）
- `interests-guide` 兴趣引导 + `verification` 实名认证 + `welcome` 注册欢迎
- 更早：bounty/qa/help/expert/report/activity/notices/ai-cover/topics/authors/editor/drafts/favorites/scan 等长尾

> 完整 per-page 细节一律查 `route-map.json`，**勿信本清单**——它会过时。

---

## 3. 剩余待迁清单（动手前务必先按第4节流程核实活页）

### 功能页（优先做）
- **`booking/[expertId]`**（411行，预约页，**下一个做**）—— 顶层无 page，是子路径动态页。
- **`call/[id]`**（618行，通话页）—— 同上，动态页。

### 法律文档类（最后做，结构相似可批量）
`legal/` 6页（child-privacy / data-collection-list / privacy-policy / teen-mode-intro / third-party-sdk / user-agreement）+ `privacy` + `terms`(及 terms/merchant、terms/service) + `common/legal-doc`。

### 不迁（用户已裁定，勿动）
- 开发 demo：`common/achievement-demo`、`common/ai-assist-demo`、`common/discussion-demo`、`list-demo`、`guoxue-design`
- admin 后台 2 页（batch-coupon-send / user-audit，功能已在管理端）
- notice 旧版 2 页（与已迁 notices 新版功能重复）
- `media-guidelines`

### 归属待裁定（动前先问用户）
- **线下驿站 offline 19 页**（线下物理站点承接线下课程培训，独立板块，一页未迁）

### 暂缓（空占位分包，按既有约定 skip）
短视频 `pkg-video`、书院 `pkg-institute`、赛事、商家 merchant、运势、同城、排盘工具。

> ⚠️ **术语辨析（勿混淆）**：线上分站(station)+运营商(operator)=用户当代理线上推广分站赚佣金（已全迁入 pkg-operator）；线下驿站(offline)=线下物理站点（独立板块未迁）。两者无关。

---

## 4. 单页迁移标准流程（严格按此做）

```
① 核实活页+未迁（防污染三道闸，任一不过即 skip）
   - 桩页关：wc -l 看行数，含 redirect() 且 <25 行 → 是桩页，真页是 redirect 目标，不迁
   - 别名/新旧套关：先 grep router.ts 的 ROUTE_MAP，确认没被别名迁过（同名页很多已用别名迁！）
   - 落盘关：find vue3/src 确认未已迁，绝不重做
② 定位真源 proto-ref-app/<x>/page.tsx → 读全文（大文件分段读）
③ 核验图标：对照 icons-registry.ts，缺的 lucide 图标手动补（body/kind/viewBox）
④ 建 mock 数据（内联或就近），写 .vue（<script setup> + app-icon + rpx + uni 标签 + navigateBack(delta数字)）
⑤ 三处注册：
   - vue3/src/pages.json        加分包页面注册
   - vue3/src/utils/router.ts   ROUTE_MAP 加 '原型路径': '/pkgxxx/xxx/index'
   - vue3/compare/route-map.json 追加 pair（owner=V, status=migrate, 详细 note）
⑥ git commit
⑦ 验证落盘：git log --oneline -1 + git show HEAD:<文件> 确认真提交了（见第6节教训！）
⑧ 验收：node compare/capture-and-diff.mjs --filter=<x>，看 nav:ok + 截图人眼比对
```

### 关键约定
- **AppIcon 导入路径**：`@/components/common/app-icon.vue`（勿写错）
- **navigateBack()** 收数字 delta，不收字符串路径
- **reLaunch 进首页**用 `/pages/index/index`
- **含中文的数据文件**（route-map.json 等）编辑时传 `dangerously_disable_autofix: true` 防二次乱码
- **批量节奏**：可一次写完一整批再统一注册、统一跑 diff 提效；但每页都要走完三道闸核实

---

## 5. A/B 质量分级与验收标准

- **A 级**（高频品牌页：登录/首页/排盘结果/支付/详情/认证等）：需 **1:1 还原**。拿不准默认 A。
- **B 级**（低频展示页）：结构/布局/主色/文案不走形即可。

### 验收方式
- 工具：`cd vue3 && node compare/capture-and-diff.mjs --filter=<关键词>`
- 它在 375×812 下分别截原型(localhost:3000)和 vue(localhost:5173)，输出 nav 状态 + 像素 diff%。
- **diff% 仅用于排序找可疑页，不作为唯一通过依据**。受 nav 高度、动态数据、异步图片(头像/banner)影响，**3~20% 多为假阳性**。
- **以人眼并排截图为准**：读 `vue3/compare/output/<page>__proto.png` 和 `__vue.png`，核对布局、间距、配色、图标、文案。
- 截图通常捕获**默认态**——若原型默认态是某特定状态（如 scan 无 content 默认是 error 态），vue 要对齐原型默认行为。

### 本批各页实测 compare%（供参考，都已人眼确认达标）
image-viewer 0.29% / scan 1.71% / verification 3.95% / appeal 3.90% / bots/chat 4.44% / interests-guide 9.12%(emoji字形) / become-partner 9.40%(渐变) / announcements 10.11% / bots广场 28.77%(mock数据顺序+异步头像，布局1:1)

---

## 6. 血泪教训（务必内化，否则会重蹈覆辙）

### ⚠️ 教训1：环境会重置，vue3 的 node_modules 会丢失
- **症状**：dev server 起不来（日志报 `uni: command not found`）、compare 显示 `vue=000` / nav 失败、甚至"上一轮的提交内容凭空消失"。
- **排查**：
  1. `curl -s -o /dev/null -w '%{http_code}' http://localhost:5173` 查端口
  2. `ps aux | grep vite` 看进程
  3. 看后台 dev 启动日志，若 `uni: command not found` → `cd vue3 && pnpm install` 重装依赖
  4. `cd vue3 && pnpm dev:h5`（后台运行）重启 dev server，等 uni 首次编译（较慢，可能 1-2 分钟）
  5. compare 报 playwright 浏览器缺失 → `npx playwright install chromium`

### ⚠️ 教训2：提交未必真落盘，必须验证
- 本轮发现"提交 bots 广场页"实际**没成功**（git 里查无此 commit），整页文件+注册全丢，被迫重做。
- **铁律**：每次 commit 后必须 `git log --oneline -1` 看到提交 + `git show HEAD:<关键文件>` 确认文件在 HEAD 里，**别假设提交成功**。

### ⚠️ 教训3：同名页很多已用别名迁，勿凭目录名/记忆判断
- 已确认别名迁（勿重复迁）：customer-service→pkg-agent、shop/after-sale→pkg-account、address→pkg-account/addresses、content→pkg-mine/content-cooperation、publish→pkg-circle/publish。
- 动手前**必查 router.ts 的 ROUTE_MAP**。

### ⚠️ 教训4：指南/记忆清单会过时，只信文件系统
- 曾因信旧指南清单误判古籍/电子书未完成（实际已全完成），差点重做。
- 判断进度永远用 `find` 数 .vue + 查 route-map + 查 pages.json，**绝不依赖任何文字清单**（包括本文）。

### ⚠️ 教训5：功能重叠页要问用户，不擅自取舍
- announcements 与已迁 notices 功能重叠但 UI 不同，我没擅自 skip，而是问了用户 → 用户裁定"照迁不跳"。涉及取舍的决策（尤其大批量页、功能疑似重复页、归属不明页）要用 AskUserQuestions 确认。

---

## 7. 样式避坑

- **渐变插值色彩空间**：原型 Tailwind v4 的**工具类渐变**（`bg-gradient-to-* from-/to-`）默认在 **OKLAB 空间**插值。uni/原生 CSS 写 `linear-gradient(135deg, ...)` 默认走 sRGB，金(#C9A96E)→红(#C41E3A)中间会偏粉。**修法**：对应原型工具类渐变的，CSS 写 `linear-gradient(135deg in oklab, ...)`。但原型若用 arbitrary 显式渐变（`bg-[linear-gradient(...)]`）则是普通 sRGB，vue 也用 sRGB。**逐个用 `agent-browser eval "getComputedStyle($el).backgroundImage"` 实测，勿一刀切。**
- **品牌色值**（照抄原型 globals.css）：`--gugong-red:#c41e3a`、`--jewel-gold:#c9a96e`。

---

## 8. 常用命令速查

```bash
# 进度核实
grep -cE '"path":' vue3/src/pages.json                      # 已注册页数
grep -c '"status": "migrate"' vue3/compare/route-map.json    # migrate 记录数
grep '"proto": "/<名>"' vue3/compare/route-map.json          # 查某页是否已迁
find vue3/src -name '*.vue' -not -path '*/components/*' | wc -l

# dev server（compare 前必须两端都活着）
cd vue3 && pnpm dev:h5                                        # vue (5173)，后台运行
# 原型 next dev 跑在 3000

# 验收
cd vue3 && node compare/capture-and-diff.mjs --filter=<关键词>
# 看图：vue3/compare/output/<page>__proto.png 与 __vue.png

# 提交后必做
git log --oneline -1
git show HEAD:<文件路径> | head            # 确认真落盘
```

---

## 9. 开工检查清单

- [ ] 读完本文 + `vue3/compare/route-map.json`（抽几条 note 看格式）+ 记忆 `v0_memories/user/migration-progress.md`
- [ ] 确认 vue3 dev server 能起来（5173 返回 200），起不来先 `pnpm install`（教训1）
- [ ] 跑通一次 `capture-and-diff.mjs --filter=bots` 验收流程，确认 playwright 正常
- [ ] 理解"防污染三道闸"和"提交后验证落盘"两条铁律
- [ ] 从 `booking/[expertId]` 开始（记得先走三道闸核实它没被别名迁、是活页）
- [ ] 涉及取舍/归属不明的页（如 offline 19 页），先问用户再动手

---

*本文基于 2026-06-21 文件系统真实状态编写。接手后请以实际 `route-map.json` / `pages.json` / `find` 结果为准，本文仅作导航。*
