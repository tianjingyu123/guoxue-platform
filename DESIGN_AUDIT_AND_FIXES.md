# 热卜国学 · 设计审计与优化方案（Vue3 转化前必修）

> **本文档的定位**：这是 Next.js 原型在转化为 Vue3 之前**必须先解决的设计缺陷清单与修复方案**。
> 目标是让原型达到「国际级获奖标准」，再以修复后的原型为唯一参照物进行 100% 高保真 Vue3 还原。
>
> **执行顺序**：先按本文档修复原型（P0 → P1 → P2 → P3）→ 全部验收通过 → 再开始 `VUE3_MIGRATION_HANDOFF.md` 的迁移工作。
>
> **重要**：本文档的修复在 **Next.js 原型**上进行，不是在 Vue3 上。修复完成后原型即为「黄金参照物」。

---

## 〇、审计总结论

基础骨架扎实，品牌方向正确（现代东方美学：故宫红 + 宣纸底 + 太极符号），动效系统与设计令牌架构达到行业标准。但存在「系统架构完美、执行落地混乱」的割裂，距国际一流有三道门槛：

1. **字体系统形同虚设**（中文字体未加载，品牌气质打折 40%）
2. **设计令牌落地率仅约 40%**（大量硬编码 `bg-white`，深色模式崩溃）
3. **信息架构职能重叠**（首页与发现页定位不清）

修复以上问题后，可冲击 Red Dot / iF 入围乃至获奖水平。

---

## 一、P0 严重问题（影响全局，必须最先修复）

### P0-1 · 字体系统失效 【成本最低、收益最大】

**问题**：`app/layout.tsx` 加载的是 `Geist`（Latin 拉丁字符集），而 `app/globals.css` 定义的品牌字体是 `Noto Sans SC`（思源黑体）+ `Noto Serif SC`（思源宋体）。两者完全不匹配，导致：
- 中文字体根本没被加载，回退到系统字体（PingFang / Microsoft YaHei）
- 全局 103 处 `font-serif`（宋体标题）调用全部失效
- 「宋体标题 + 黑体正文」这一东方排版根基消失

**修复方案**（`app/layout.tsx`）：
```ts
import { Noto_Sans_SC, Noto_Serif_SC } from 'next/font/google'

const notoSans = Noto_Sans_SC({
  subsets: ['latin'],
  weight: ['400', '500', '700'],
  variable: '--font-noto-sans-sc',
  display: 'swap',
})
const notoSerif = Noto_Serif_SC({
  subsets: ['latin'],
  weight: ['400', '500', '700', '900'],
  variable: '--font-noto-serif-sc',
  display: 'swap',
})
```
将 `notoSans.variable` 和 `notoSerif.variable` 挂到 `<html>` 或 `<body>` 的 `className`。
然后确认 `globals.css` 的 `@theme` 里 `--font-sans` / `--font-serif` 指向这两个 CSS 变量。

**验收**：标题用 `font-serif` 时呈现思源宋体的横细竖粗笔锋；正文呈现思源黑体；DevTools 的 computed font-family 含 "Noto Serif SC" / "Noto Sans SC"。

---

### P0-2 · 深色模式 bg-white 系统性破损

**问题**：全局 `bg-white` 硬编码 **1,414 处**、`text-gray-*/zinc-*/slate-*` **457 处**、`bg-[#...]` 硬编码 **1,466 处**。这些绕过了设计令牌，在深色模式下会变成刺眼白块，深色体验完全崩溃。

**修复方案**（系统性替换，从共享组件开始向页面辐射）：
| 硬编码 | 替换为语义令牌 |
|:--|:--|
| `bg-white`（卡片/面板） | `bg-card` |
| `bg-white`（页面底） | `bg-background` |
| `bg-white`（浮层/Sheet） | `bg-popover` |
| `text-gray-900 / text-zinc-900` | `text-foreground` |
| `text-gray-500 / text-zinc-500` | `text-muted-foreground` |
| `border-[#...]` 浅灰边 | `border-border` |
| 骨架屏 `bg-white / bg-[#F2EFEA]` | `bg-secondary` 或 `bg-muted/40` |

**执行建议**：先改 `components/cards/*`、`components/ui/*`、骨架屏、底部 Sheet 等高复用组件，再逐板块清理页面。可写脚本统计每个文件的命中数，按高频优先。

**验收**：切换深色模式，全部页面无白色突破块；骨架屏不闪白；文字对比度达标（WCAG AA）。

---

### P0-3 · 底部导航「发现」Tab 信息架构混乱

**问题**：底部 5 Tab 为 首页 / 圈子 / 排盘 / 发现 / 我的。「发现」用了 `ShoppingBag`（购物袋）图标却指向内容聚合页 `/discover`，且与「首页」职能高度重叠（两个页面都是「宫格入口 + 信息流」）。商城作为核心变现板块反而埋在二级入口。

**两个候选方案（择一，建议先与产品确认）**：

- **方案 A（低风险，改图标 + 分职能）**：
  - 「发现」改用 `Compass` 或 `Sparkles` 图标，定位明确为「**内容探索**」（课程/古籍/视频/榜单/智能体），移除与首页重复的购物类入口。
  - 「首页」定位为「**今日推荐 + 个性化 Feed**」，宫格保留全功能入口。
  - 两者形成「个性化首页 vs 全量探索」的清晰区隔。

- **方案 B（高收益，商城提级）**：
  - 底部 Tab 改为 首页 / 商城 / 排盘 / 圈子 / 我的，把「商城」提升为一级 Tab（参考淘宝/京东），直接体现「内容 + 电商」双核定位。
  - 内容探索并入「首页」的分区 Tab。

**验收**：每个底部 Tab 职责单一、图标与内容语义一致；用户能在 3 秒内说清每个 Tab 是做什么的。

---

## 二、P1 高优先级（品牌一致性）

### P1-4 · 分类导航彩虹色违背品牌
- **位置**：`app/courses/page.tsx`（约 25-32 行 categoryNav）
- **问题**：8 个分类图标各用一个随机色（`#c0392b`/`#8e44ad`/`#16a085`/`#2980b9`/`#27ae60`...），是 2012 Flat Design 配色，与「克制东方」冲突。
- **修复**：统一为品牌色系的深浅变体（primary / accent金 / ink 墨色 的明度梯度），或全部用故宫红 + 不同图标区分。参考得到/Coursera 的系统化分类色。

### P1-5 · agent-gradient-cool 紫色遗漏
- **位置**：`components/home-feed.tsx`（约 112 行引用）；定义在 `globals.css`
- **问题**：`agent-gradient-cool` 是紫蓝渐变（`#8b5cf6 → #a78bfa`），是上轮紫色清理的漏网之鱼。
- **修复**：改用 `agent-gradient-warm`（金色）或 `agent-gradient-earth`（土色）。

### P1-6 · VIP pro 等级用紫色
- **位置**：`app/vip/page.tsx`（约 39 行 `levelColors.pro`）
- **问题**：`bg-purple-500` 违反紫色禁令且与品牌色冲突。
- **修复**：pro 等级改用 `bg-accent`（珠宝金，更显高级）或 `bg-primary`。

### P1-7 · shimmer 动画重复定义
- **位置**：`app/globals.css` 第 57 行与第 629 行各定义一次 `shimmer`，后者覆盖前者，导致 `skewX(-12deg)` 光泽斜切丢失。
- **修复**：合并为一个版本，保留 `skewX(-12deg)` 增强光泽质感。

---

## 三、P2 中优先级（体验瑕疵）

### P2-8 · 圆角系统无规则
- 现状：`rounded-full`(2974) / `rounded-xl`(1543) / `rounded-lg`(1214) / `rounded-2xl`(589) / `rounded-md`(101) 五种并存。
- 修复：建立语义映射并写入规范——按钮/标签=`rounded-full`，卡片/弹层=`rounded-xl`，输入框/小控件=`rounded-lg`。逐步收敛。

### P2-9 · 间距系统不统一
- 现状：`px-4`(1964) / `px-3`(457) / `px-6`(159) / `px-5`(76) 混用，页面内容边距有 4px 级差异。
- 修复：统一移动端内容区水平边距为 `px-4`(16px)；仅子元素内边距用 `px-3`。

### P2-10 · 骨架屏深色破相
- 位置：`app/circles/page.tsx`（约 49 行）等
- 修复：骨架屏 `bg-white` → `bg-secondary` / `bg-muted/40`（属 P0-2 子项，单独列出确保不遗漏）。

### P2-11 · 页面过渡无统一动效
- 现状：`components/page-wrapper.tsx` 有过渡组件但几乎无人引用。
- 修复：将其应用到全部一级页面，统一「淡入 + 轻微上滑」进场，减少动效偏好下自动降级（globals.css 已有 prefers-reduced-motion 支持）。

### P2-12 · 空状态覆盖率偏低
- 现状：531 路由中约 72 个有空状态（~14%）。
- 修复：优先补高频场景——我的课程为空、圈子无帖子、搜索无结果、订单为空、收藏为空。统一空状态插画 + 引导按钮风格。

---

## 四、P3 精益求精

| # | 位置 | 问题 | 建议 |
|:--|:--|:--|:--|
| 13 | `app/discover/page.tsx` | 品类标签 + 8 宫格两套导航并存，选择困难 | 宫格作主入口，品类标签降级为内容流筛选器 |
| 14 | `components/home/quick-entry-grid.tsx`(~127) | AI 角标两个分支样式相同，逻辑冗余 | 简化为单一样式 |
| 15 | `app/live/page.tsx` | 直播入口深度 2 级，强实时功能入口偏深 | 首页宫格加「正在直播」状态提示/红点 |

---

## 五、出色的设计（务必在 Vue3 还原中 100% 保留）

1. **太极图排盘中心按钮**：底部导航中央凸起太极图，选中时 9 秒缓慢旋转 + 呼吸光晕。行业标杆级原创设计，文化符号精准、触摸区合规。**这是产品设计顶峰，Vue3 还原时动效必须 1:1 复刻。**
2. **三层设计令牌架构**：原始调色板 → 语义令牌 → Tailwind 映射 + 深色模式 + 角色专属色。
3. **直播双形态卡片**：横屏知识授课(16:9) + 竖屏电商带货(9:16) 自适应。
4. **文化动效系统**：太极呼吸、金色光晕、直播红点脉冲、弹幕、礼物飘动、飞入购物车——有文化仪式感，是差异化核心。

---

## 六、验收标准（修复后逐项核对）

- [ ] **P0-1**：中文字体（思源黑体/宋体）正确加载，`font-serif` 标题呈现宋体笔锋。
- [ ] **P0-2**：深色模式下全站无白块突破、骨架屏不闪白、文字对比度达 WCAG AA。
- [ ] **P0-3**：底部 5 Tab 职责单一、图标语义正确，首页与发现页定位清晰不重叠。
- [ ] **P1**：无任何紫色品牌违规（agent-gradient-cool / VIP pro / 其它）；分类色统一为品牌色系。
- [ ] **P1-7**：shimmer 动画唯一定义，光泽斜切正常。
- [ ] **P2**：圆角/间距按规范收敛；页面过渡统一；高频场景空状态齐全。
- [ ] 全局：设计令牌使用率显著提升（目标 90%+），`next build` 通过、控制台无报错。
- [ ] 在 514×1111 移动视口逐页核验视觉一致性。

---

## 七、对国际获奖标准的差距对照

| 维度 | 现状 | 国际顶尖 | 修复后预期 |
|:--|:--|:--|:--|
| 设计系统落地率 | ~40% | 95%+ | 90%+（修 P0-2） |
| 字体系统 | 执行失效 | 精确分层 | 完整（修 P0-1） |
| 信息架构清晰度 | 职能重叠 | 单一职责 | 清晰（修 P0-3） |
| 品牌符号原创性 | 太极图·顶级 | 各有独创 | 保持顶级 |
| 动效品质 | 文化动效出色 | 全链路流畅 | 优秀（修 P2-11） |
| 深色模式 | 实现破损 | 双完整 | 完整（修 P0-2） |

**结论**：修复 P0 三项即可从「入围难获奖」跃升到「有力竞争 Red Dot / iF」；再清理 P1/P2 可冲击 Apple Design Awards 与 Awwvards 上榜水平。

---

*修复在 Next.js 原型上进行。完成并验收后，以此「黄金原型」启动 `VUE3_MIGRATION_HANDOFF.md` 的 100% 高保真 Vue3 还原。*
