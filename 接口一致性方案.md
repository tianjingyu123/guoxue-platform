# 前后端接口一致性 —— 完整方案

> 制定日期：2026-06-24　适用：guoxue-platform（NestJS 后端 + uniapp Vue3 H5 前端，前后端均由 AI 开发）
> 目标：在持续变化、上千接口、分模块渐进开发的前提下，稳健保证前后端接口一致，最终上线零接口错误。

---

## 一、核心问题与原则

**问题本质**：前后端都会改、接口上千、分模块跨多会话完成、且开发者（AI）会跨会话遗忘、会犯一致性错误。靠"对一次接口"或"开发者自己记住"都不可靠。

**根本原则**：把"接口一致"从**依赖开发者的记忆与细心**，外部化为**代码与工具的强制**。开发者（我）按"不可信执行者"来设计系统——系统不依赖我的可靠性。

**方向定义**（已论证）：
- **后端是契约的 owner，前端适配后端**（后端是数据/业务事实来源、是多端共享的稳定方、能用机器生成契约）。
- **但契约内容由前端需求驱动设计**（消费者驱动）：需求自下而上（前端→后端），实现自上而下（后端→前端）。
- 冲突仲裁：命名/路径/结构形式 → 前端改；前端真需要而后端缺 → 后端补；前端诉求违背领域模型 → 前端改。

---

## 二、现状评估（实测，2026-06-24）

| 项 | 现状 | 结论 |
|----|------|------|
| 后端规模 | 137 controller / 1697 端点 / 89 DTO | 成熟，适合做事实来源 |
| 响应格式 | 全局 `transform.interceptor` 统一 `{code,data,message}` + `useGlobalInterceptors` | ✅ 已统一 |
| Swagger 覆盖 | @ApiTags 137、@ApiOperation 1452、@ApiResponse 1320、@ApiProperty 1808，SwaggerModule 已装 | ✅ **OpenAPI 契约几乎现成** |
| 共享类型 | `packages/shared/types/{common,user,order,api}.ts`（仅 6 文件） | 薄，待契约生成填充 |
| 前端调用 | 461 页；接口走 `lib/*-data.ts` 的 `xxxApi` + `utils/request.ts`；mock 已关 | data 层散乱、命名与后端漂移 |
| 前端请求封装 | `request.ts` 兼容 `{code,data}` 与裸数据两种 | 后端已统一，兼容分支应收敛为一种 |
| 已知不一致 | 前端 `account-data`/`/account/profile` ↔ 后端 `mine.controller`/`/mine/profile` | 命名漂移典型案例 |
| 页面质量 | 461 页中 281(61%) 为 V0 直出 B 类（无三态、div+tailwind） | 需按模块打磨 |

**关键判断**：后端基建（统一响应 + 高 Swagger 覆盖）远好于预期。所以本方案重心**不是"补后端"，而是"建生成管线 + 整顿前端 + 建校验网"**。

---

## 三、一致性保证机制（三层）

### 第 0 层：接口规范 + 统一术语表（宪法）
- 产出《接口规范》文档（路径/方法/响应/错误码/字段命名/分页/鉴权 7 项）+《统一术语表》（每个领域概念全项目唯一用词，如"用户中心域=mine"）。
- 以**后端现状提炼**而非凭空发明，显性化成文档存仓库。
- 术语表做成**机器可读**（yaml），供 lint 校验。

### 第 1 层：防漂移（应对前后端都会改）
```
后端 Controller+DTO(@Api*装饰器) ──SwaggerModule──▶ openapi.json（事实来源）
                                                        │ orval/openapi-typescript
                                                        ▼
                              前端 src/api/*（typed client：类型+请求函数）
                                                        │ 页面 import
                                                        ▼
                              页面调 mineApi.getProfile()，不手写 url
```
- 前端**不手写接口**，全部从后端契约生成 → 命名/路径/参数/返回类型机器保证一致。
- 一键 `check:contract`：对比前端调用与后端 openapi，列出"前端要/后端无、参数不符、返回字段不符"，红绿灯。
- 类型贯通：后端改接口→重新生成→前端 `vue-tsc` 编译报错，漏不掉。

### 第 2 层：分模块推进（应对项目大、不一蹴而就）
- **模块台账**（见六）：每模块状态可见、可控。
- **单模块流水线**（见五）：固定 6 步，DoD 全绿才算锁定。

### 第 3 层：防回归（应对改 A 坏 B、保证上线完美）
- 每个验收模块沉淀：**契约快照测试**（钉住接口请求/返回结构）+ **关键流程 e2e**。
- 测试网只增不减；任何改动跑全量回归，碰坏立刻红灯。
- 上线信心 = 全量回归一键通过，而非人肉测上千接口。

### 针对"开发者是我（会失忆）"的特化
| 我的局限 | 外部化机制 |
|---------|-----------|
| 忘规范 | 规范+术语表存仓库；CLAUDE.md 钉"开工必读" |
| 手敲接口写错 | 前端从契约生成，不手写 |
| 改完没发现不一致 | `check:contract` 一键校验 |
| 类型漏看 | 生成类型 + vue-tsc 强制 |
| 跨会话丢进度 | 台账+契约+测试在仓库，新会话读取即接上 |
| 前后端改动不同步 | 原子提交（前后端同改一个 commit） |

---

## 四、接口规范 + 术语表（框架，待细化成正式文档）

**接口规范 7 项**：
1. 路径：`/api/v1` 前缀、资源复数、kebab-case、层级 `/users/:id/orders`
2. 方法语义：GET 查 / POST 增 / PUT 全量改 / PATCH 部分改 / DELETE 删
3. 响应：统一 `{code:0,data,message}`（后端已做，前端收敛 request.ts）
4. 错误码：HTTP 状态码 + 业务 code 对照表
5. 字段命名：camelCase、时间 ISO8601、布尔 `isXxx`、id 类型统一
6. 分页：`{page,pageSize,total,list}`
7. 鉴权：`Authorization: Bearer`，标注需登录端点

**统一术语表**（示例，需扫全后端提炼完整版）：

| 领域 | 唯一用词 | 禁止别名 |
|------|---------|---------|
| 用户中心 | `mine` | account, user-center |
| 用户资料 | `profile` | info, userInfo |
| 钱包 | `wallet` | balance(指字段), purse |
| 订单 | `order` | trade, deal |

---

## 五、单模块流水线（6 步 + DoD）

```
①需求盘点 → ②契约对账 → ③后端补齐 → ④生成client → ⑤页面打磨接线 → ⑥运行时验证+锁定
 前端页面要什么  按规范裁定   按规范实现   契约→typed   换api/补三态/   真跑+Network核验
 (消费者需求)    谁不合规谁改  +Swagger标注  client      修UI/对原型     +契约测试→🔒
```

**Definition of Done（全绿才算锁定）**：
- [ ] `check:contract` 通过（接口/参数/返回零漂移）
- [ ] `vue-tsc` 零类型错误
- [ ] 三态 + 交互 + 响应式达标
- [ ] 关键流程本地真跑通（Network 核验，非静态猜）
- [ ] 契约快照测试 + 流程 e2e 沉淀
- [ ] `build` 不回归

---

## 六、模块台账（示例，进项目仓库持续维护）

| 模块 | 接口数 | 契约对齐 | UI达标 | 运行时验证 | 测试锁定 | 状态 |
|------|-------|---------|--------|-----------|---------|------|
| pkg-mine | ~18 | ⬜ | ⬜ | ⬜ | ⬜ | 样板进行中 |
| pkg-shop | ~40 | ⬜ | ⬜ | ⬜ | ⬜ | 未开始 |
| pkg-circle | ~35 | ⬜ | ⬜ | ⬜ | ⬜ | 未开始 |
| ...（全模块铺开） | | | | | | |

---

## 七、pkg-mine 样板落地（第一个模块，顺手建全局工具链）

**为何选它**：账户中心，24 页、关联 15 个 data 文件、有专属 `mine.controller`，与登录鉴权强相关（打通它顺带解决全局鉴权卡点）。

1. **①需求盘点**：24 页逐页列"展示什么/提交什么"→ 页面↔数据需求表。
2. **②契约对账**：需求 × `mine.controller` 等后端路由，按规范逐条定性（前端 `account-data` 等乱拆的 → 收敛对齐后端 `mine`）。产出对账表 + 后端改动清单。
3. **③后端补齐**：补缺接口、统一返回，补全 mine 相关 Swagger。
4. **④生成 client**：搭 OpenAPI→前端生成管线，产出 `src/api/mine.ts`；15 个 data 文件收敛。
5. **⑤页面打磨**：24 页换新 api、补三态、修 V0 UI、对比 V0 原型(`Desktop/V0-6.24`)。
6. **⑥验证锁定**：本地起前后端真跑、Network 核验、写测试、🔒。

**顺手建立的全局资产**：《接口规范+术语表》、OpenAPI→前端生成管线、`check:contract` 命令、契约/e2e 测试模板、模块台账、CLAUDE.md 开工协议。

---

## 八、工具链清单（一次性建好，全模块复用）

| 工具 | 作用 | 选型 |
|------|------|------|
| OpenAPI 导出 | 后端生成 openapi.json | @nestjs/swagger（已装） |
| 前端 client 生成 | spec→typed client | orval 或 openapi-typescript-codegen |
| 契约校验 | 前端调用 vs spec 红绿灯 | 自研脚本 `check:contract` |
| 规范 lint | 后端接口违反规范报错 | 自研脚本（查术语表/命名） |
| 契约测试 | 钉住接口形状 | jest 快照 |
| 流程 e2e | 关键链路真跑 | Playwright（已装 puppeteer，可加 playwright） |
| 模块台账 | 进度管理 | 仓库内 markdown |

---

## 九、执行路线图

**阶段 0（地基，1 次性）**：扫后端提炼《接口规范+术语表》；搭 OpenAPI→前端生成管线；写 `check:contract`。
**阶段 1（样板）**：pkg-mine 走完 6 步 → 验收 → 沉淀模板与协议。
**阶段 2（铺开）**：按优先级逐模块复制流水线（交易链路 shop/order/pay 优先 → 高频 home/circle/im/mine → 长尾）。每模块锁定即永不回退。
**阶段 3（收口）**：全模块锁定 → 全量回归一键通过 → 上线。

**优先级排序**：登录鉴权/账户(mine) → 交易(shop/order/wallet) → 内容高频(home/circle/classics/course) → 长尾。

---

## 十、风险与对策

| 风险 | 对策 |
|------|------|
| 后端 Swagger 虽多但局部不准 | 生成后抽样核对；@ApiResponse 缺失处补 |
| 前端动态 url 多，生成 client 后改造量大 | 按模块渐进替换，老代码不强制一次性迁移 |
| 跨会话丢上下文 | 台账+契约+规范+测试全在仓库；CLAUDE.md 开工必读 |
| 我（AI）I/O/记忆不可靠 | 关键产出落盘+机器校验，不靠记忆；本文件即外部记忆 |
| 一蹴而就冲动 | 严守"做一个锁一个"，DoD 不绿不锁 |

---

## 结论

后端基建已具备（统一响应 + 高 Swagger 覆盖），**一致性方案的关键不是补后端，而是：建立后端契约→前端生成的管线 + 一键校验 + 分模块流水线 + 回归测试网**，并把这一切落盘成"外部记忆"，让一致性"违反不了"，而非"靠我维持"。先以 pkg-mine 为样板把机制立起来，再分模块复制至全量，最终全量回归通过即达上线水平。
