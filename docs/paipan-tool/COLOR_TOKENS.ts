/**
 * 排盘工具 — 全局颜色令牌
 *
 * 唯一颜色引用源。所有排盘相关的前端代码，颜色必须从此文件引用。
 * 禁止在任何组件中硬编码色值（#xxx 或 rgb()）。
 *
 * 用途：Claude 写代码时 import，V0 设计时直接取色值。
 */

// ============================================================
// 五行色（功能性标注专用，不可用于装饰）
// ============================================================
export const WUXING = {
  mu: '#4CAF50',   // 木 — 绿
  huo: '#F44336',  // 火 — 红
  tu: '#FF9800',   // 土 — 橙
  jin: '#FFC107',  // 金 — 黄
  shui: '#2196F3', // 水 — 蓝
} as const;

// ============================================================
// 吉凶色（结果判定专用）
// ============================================================
export const JIXIONG = {
  ji: '#2E7D32',   // 吉 — 深绿（不用亮绿，保持庄重）
  xiong: '#C62828', // 凶 — 深红
  ping: '#616161',  // 平 — 灰
} as const;

// ============================================================
// 天干色（排盘四柱显示专用，按传统五行配色）
// ============================================================
export const TIANGAN: Record<string, string> = {
  甲: WUXING.mu, 乙: WUXING.mu,
  丙: WUXING.huo, 丁: WUXING.huo,
  戊: WUXING.tu, 己: WUXING.tu,
  庚: WUXING.jin, 辛: WUXING.jin,
  壬: WUXING.shui, 癸: WUXING.shui,
};

// ============================================================
// 地支色（排盘四柱显示专用，按藏干主气五行）
// ============================================================
export const DIZHI: Record<string, string> = {
  寅: WUXING.mu, 卯: WUXING.mu,       // 木
  巳: WUXING.huo, 午: WUXING.huo,      // 火
  辰: WUXING.tu, 戌: WUXING.tu, 丑: WUXING.tu, 未: WUXING.tu, // 土
  申: WUXING.jin, 酉: WUXING.jin,      // 金
  亥: WUXING.shui, 子: WUXING.shui,    // 水
};

// ============================================================
// 界面色（UI装饰用，严格限制范围）
// ============================================================
export const UI = {
  // 背景
  bg: '#F5F5F5',       // 页面底色（浅灰）
  cardBg: '#FFFFFF',   // 卡片/面板底色（纯白）
  headerBg: '#FAFAFA', // 表头/工具栏底色

  // 文字
  textPrimary: '#1A1A1A',   // 主文字（近黑不用纯黑）
  textSecondary: '#666666', // 辅助文字
  textHint: '#999999',      // 提示/占位文字

  // 边框
  border: '#E8E8E8',        // 卡片/表格分割线
  borderLight: '#F0F0F0',   // 更淡的分割

  // 品牌/强调
  brand: '#C41E3A',         // 中国红（主品牌色）
  brandLight: '#FFF0F0',    // 品牌色浅底（选中态背景）

  // 功能（最小集，不要新增）
  link: '#1890FF',          // 链接色（仅用于可点击链接）
  warning: '#FAAD14',       // 警告色（仅用于重要提示）
} as const;

// ============================================================
// 奇门盘面专用色
// ============================================================
export const QIMEN = {
  gongBorder: '#333333',  // 宫位边框
  gongBg: '#FFF8E1',      // 宫位底色（暖黄宣纸感）
  starColor: '#C62828',   // 九星文字色
  menColor: '#1565C0',    // 八门文字色
  shenColor: '#6A1B9A',   // 八神文字色
  emptyGong: '#F5F5F5',   // 空亡宫底色
  maStar: '#FF6D00',      // 马星标记色
} as const;

// ============================================================
// 图表色（ECharts / 可视化专用）
// ============================================================
export const CHART = {
  series: [
    '#C41E3A', '#4CAF50', '#FF9800', '#2196F3',
    '#FFC107', '#9C27B0', '#00BCD4', '#795548',
    '#607D8B', '#E91E63',
  ],
  wuxingRadar: {
    mu: 'rgba(76,175,80,0.3)',
    huo: 'rgba(244,67,54,0.3)',
    tu: 'rgba(255,152,0,0.3)',
    jin: 'rgba(255,193,7,0.3)',
    shui: 'rgba(33,150,243,0.3)',
  },
  timeline: '#C41E3A',   // 大运时间轴主色
} as const;
