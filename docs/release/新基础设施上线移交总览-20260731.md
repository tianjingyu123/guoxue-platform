# 新基础设施上线移交总览

更新时间：2026-08-01
执行仓库：`D:\gx-deploy-91`
权威现场清单：`docs/operations/新基础设施与正式凭据交接清单-20260731.md`

## 当前结论

两台 CVM、CLB、托管 PostgreSQL、Redis、COS、CDN、预发布域名与监控栈已经完成部署和现场演练；备份隔离恢复、节点容量测试及 CLB 双向单节点摘除/恢复均已通过。代码、数据库基线、固定发布包、原子激活、版本回滚、运行实例身份和九证据聚合门禁已达到“可形成正式固定发布”的标准。当前服务器仍运行临时预部署目录，工作树尚未冻结，支付、正式数据迁移、长期证书、合规与签名发布尚未完成，当前不能标记为正式上线，也不得关闭旧站或删除旧库。

最终上线必须同时满足：

1. 所有 P0 人工交接项已完成并签字；
2. 生产固定包来自干净工作树，且本地和服务器逐文件验真一致；
3. 新环境数据对账、支付退款、备份恢复、告警和回滚演练通过；
4. `launch-decision.json` 的机器判定为 `GO`，并与九份来源报告一并归档；
5. 不同技术与业务负责人完成九项 P0 双签，`final-launch-decision.json` 最终判定为 `GO`。

## 已完成的代码与门禁

2026-07-31 本机完整代码门禁退出码为 0，关键结果如下：

2026-08-01 00:23（UTC-07:00）再次执行 `pnpm release:gate:code`，45.9 秒退出码为 `0`：上线缺口继续保持 P0 `0`、P1 `0`，基础设施迁移门禁升至 `76/76`，新增固定版本回滚行为测试已进入统一代码门禁。Windows 本机执行结果为 `2` 条通过、`2` 条因缺少可靠 POSIX 软链接与权限语义而跳过；这两条成功路径已由 `.github/workflows/ci.yml` 的 Ubuntu `release-safety` 任务强制在合并前执行，仍不得替代新服务器到位后的只读回滚现场演练。

2026-08-01 00:56（UTC-07:00）完成工作树覆盖反向审计：上线缺口扫描器由“仅已跟踪文件”改为同时覆盖未被 `.gitignore` 排除的未跟踪生产源码，并加入两条隔离 Git 仓库回归测试及统一代码门禁。当前实扫 `2144` 个生产源码文件，其中已跟踪 `2138` 个、未跟踪 `6` 个，P0 `0`、P1 `0`；基础设施迁移门禁因此升至 `77/77`。这项保护确保正式源代码基线冻结前，新源码不能绕过假令牌、501 未实现响应和关键未接通标记检查。

2026-08-01 01:00（UTC-07:00）执行更新后的 `pnpm release:gate:code`，53.6 秒退出码为 `0`。新增未跟踪源码回归测试、迁移/接入/切流/回滚/证据链、交互、多端、商家履约、运行时、Nginx、监控、数据库基线和依赖安全门禁协同通过；本次仅修改发布审计、测试、脚本编排与交接文档，不影响 00:39 已完成的六端构建成品。

2026-08-01 01:31（UTC-07:00）完成正式源码身份锁定：严格完整门禁必须同时接收预期正式分支和 40 位源提交 SHA，冻结报告会记录并核对实际分支、Git 分支、预期分支与预期提交；GitHub 打包任务绑定仓库默认分支和 `${{ github.sha }}`，固定发布包验真器拒绝缺失、错分支或错提交的冻结证据。源码冻结行为测试 `10/10` 通过，固定包验真测试 Windows 本机 `6` 条通过、`1` 条 POSIX 软链接语义跳过，基础设施迁移门禁 `77/77` 通过。随后再次执行 `pnpm release:gate:code`，51.7 秒退出码为 `0`；上线缺口仍为 P0 `0`、P1 `0`，实扫 `2144` 个生产源码文件（已跟踪 `2138`、未跟踪 `6`）。

2026-08-01 01:49（UTC-07:00）补齐 Node.js 24 与 pnpm 10 参数兼容：Node 24 会抢先解析脚本参数中的 `--env-file`，pnpm 10 又会把迁移手册使用的参数分隔符 `--` 原样传给脚本。完整上线门禁、基础设施接入审计和主机预检入口现以 Node 参数边界启动，完整门禁显式忽略 pnpm 分隔符；迁移手册原样命令已实测进入项目门禁，并在缺少预期分支时按设计于构建前退出 `2`，不再由 Node 异常退出 `9`。源码冻结与完整门禁入口回归现为 `12/12`，基础设施迁移门禁 `77/77`；统一 `pnpm release:gate:code` 再次于 49.4 秒退出 `0`。

2026-08-01 02:08（UTC-07:00）完成参数边界的服务器侧闭环：`docker/setup-server.sh` 与 `scripts/release/verify-production-cutover.sh` 的基础设施接入审计和主机预检均通过 `node --` 明确结束 Node 选项解析，两个脚本的 Bash 语法检查通过。随后用迁移手册同形态命令分别实测 `release:audit-infra-intake` 与 `release:audit-host-preflight`：二者均已进入项目脚本，并按设计因测试清单/目录不存在退出 `2`/`1`，没有再次出现 Node 参数异常退出 `9`，也未产生测试报告残留。接入清单测试 `9/9`、主机预检测试 `3/3`、源码冻结与完整门禁入口测试 `12/12`、基础设施迁移门禁 `77/77` 全部通过；最终再次执行统一 `pnpm release:gate:code`，51.2 秒退出 `0`，实扫 `2144` 个生产源码文件且 P0/P1 继续保持 `0/0`。

2026-08-01 02:18（UTC-07:00）补齐 Windows 源码基线到 Linux 新机的 Shell 换行保护：仓库根 `.gitattributes` 必须明确声明 `*.sh text eol=lf`，基础设施迁移门禁会直接阻断缺失或被改坏的配置，避免 CRLF 脚本进入固定发布包后在迁移现场失效。该保护使基础设施迁移门禁升至 `78/78`；更新后的统一 `pnpm release:gate:code` 于 47.8 秒退出 `0`，确认换行保护已进入总链路而非仅单项检查。

2026-08-01 02:30（UTC-07:00）继续修复 Windows 固定包迁移到 Linux 时的 Shell 执行位边界：Git 中生产 Shell 脚本当前为 `100644`，固定包不依赖可执行位恢复；部署前备份和恢复前快照现均显式通过 `bash` 调用 `pg-backup.sh`，避免新机执行数据库迁移或灾备恢复时因 `Permission denied` 中断。基础设施迁移门禁新增对应阻断项并升至 `79/79`；统一 `pnpm release:gate:code` 于 50.2 秒退出 `0`，P0/P1 继续保持 `0`。

2026-08-01 02:44（UTC-07:00）完成 Shell 执行位与数据库值班入口的全链路收口：服务器定时备份、数据库运维入口以及停用部署兼容入口均改为显式通过 `bash` 调用；历史 `backup-db.sh`、`restore-db.sh` 仅保留为兼容包装器并统一委托受保护的 `db-ops.sh`，不再保留可绕过并发锁、SHA-256、归档验证、恢复前快照和目标库同值确认的独立恢复捷径。Shell 语法、托管库/自建库/非法目标/缺失目标/并发锁五条真实备份分支全部通过；随后继续清理服务器初始化、权限报错、证书补办及常用运维脚本头部的直接执行提示，并新增可复制指引不依赖执行位的阻断项。基础设施迁移门禁最终升至 `83/83`；统一 `pnpm release:gate:code` 于 48.9 秒退出 `0`，实扫 `2144` 个生产源码文件且 P0/P1 保持 `0/0`。

2026-08-01 03:18（UTC-07:00）补齐运行手册的同类执行位风险：备份、恢复及带数据库迁移的部署示例全部改为显式 `bash` 调用，并新增门禁阻止运维文档重新出现依赖压缩包执行位的可复制命令。基础设施迁移门禁升至 `84/84`；源码冻结报告经 Node 重新解析为有效 UTF-8 JSON，当前记录 `312` 个状态条目、`0` 个暂存、`0` 个冲突，正式固定包仍只因工作树未冻结及 `kiki-prompt*.md` 三个根目录文件待人工归属而保持安全阻断。随后统一 `pnpm release:gate:code` 于 57.6 秒退出 `0`，P0/P1 继续保持 `0/0`。

- 上线缺口：P0 `0`、P1 `0`；P2 `11` 项均为明确标注的研发预告或即将开放能力。
- 基础设施迁移门禁：`84/84` 通过；覆盖 Windows 到 Linux 的 Shell 强制 LF 换行、关键运维脚本及运行手册现场可复制指引不依赖执行位、历史数据库入口只委托统一受保护链路、工作树未冻结阶段的已跟踪与未跟踪生产源码扫描、首次初始化目录与 `releases/<release-id>` 强绑定、实际主机预检的脱敏版本证据、私有接入清单按部署架构安全初始化且拒绝覆盖、正式环境与新数据库/缓存/域名/对象存储接入清单逐项绑定、首次初始化、系统重启与自定义路径持久绑定、安全保留回滚镜像、数据库备份并发锁、正式激活、数据库最终对账、生产镜像内锁定版本 Prisma 迁移、CI/镜像 pnpm 版本一致性、Windows 重启或换机后的 Corepack 稳定运行入口、五端成品源码提交与确定性内容指纹绑定、下载或转交后的独立复验、客户端审计/独立验真/配置绑定三份证据在同一 Artifact 完整交接、独立验真报告封入固定发布包并与原审计逐目标绑定、切流后 GitHub 与人工应急共用同一只读九证据验收脚本、PR 与主分支在 Ubuntu 前置执行统一迁移上线门禁、版本回滚独立行为测试、完整健康验收失败自动回滚、运行实例发布标识一致性、同一固定包可重入恢复、部署架构显式绑定、自建/托管数据地址防误配、源库/目标库/DNS 权限实测、同版迁移演练、停写窗口、旧环境回退保留、本地验收材料隔离、历史运维文档降级与安全回滚口径以及最终双人签核。
- 正式环境检查新增部署架构感知门禁及 `6/6` 回归：完整上线必须显式选择 `standard` 或 `tencent`；腾讯云托管模式拒绝 `postgres`、`redis`、回环地址和本地存储兜底，数据库与缓存连接协议异常也会直接阻断。`release:gate:full` 不再静默读取仓库内 `docker/.env.production`，必须显式传入正式环境文件、部署架构与新基础设施接入清单。
- 新基础设施接入清单生成与验收回归 `9/9` 通过：采购阶段允许证书和资源地址待交接但阻断低配及缺少数据保护；launch 阶段必须将正式环境里的数据库、Redis、公开域名和对象存储逐项绑定到同一套新资源，任何错连都会阻断，机器报告只保存字段级差异和 SHA-256 指纹，不记录地址或凭据。
- 源代码冻结与完整门禁运行入口回归 `12/12` 通过：完整上线门禁会在耗时构建前生成 `source-freeze-readiness.json`，核对源提交、预期正式分支、实际 Git 分支、暂存/未暂存/未跟踪条目、冲突、`git diff --check` 与真实敏感文件名，并按一级目录、扩展名汇总未跟踪源码，单独标记根目录待归属文件、超过 `1 MiB` 的未跟踪文件及二进制发布物；严格门禁缺少预期分支/40 位提交或实际身份不一致均会阻断。Windows 环境会依次复用当前 pnpm、Node 自带 Corepack 或有效 pnpm shim，不再依赖重启前残留的项目内 `pnpm.cjs`；Node 24 不会再抢占正式环境文件参数，pnpm 10 的参数分隔符也不会被误判为未知参数。正式门禁拒绝脏工作树，普通代码门禁仍允许在开发工作树执行。
- `artifacts/` 已在 Git 与 Docker 两侧明确排除：约 `487 MB` 的本地构建包、截图、旧包审计副本和迁移演练材料仍保留在磁盘供验收追溯，但不会混入源码基线、Docker 上下文或固定发布包。排除后实际未跟踪源码/配置/测试文件由 `4340` 个收敛为 `93` 个。
- 冻结审计报告统一以 UTF-8 JSON 输出；Windows PowerShell 5.x 人工读取时必须显式使用 `Get-Content -Encoding UTF8`，机器判定继续使用 Node 解析，避免中文内容被系统默认 ANSI 编码误读。
- 数据库迁移核验证据测试：`6/6` 通过；覆盖逐表计数、业务完整性、生产镜像内 Prisma 状态检查、失败不落成功证据、迁移二次确认以及数据库凭据不进入 Docker 参数。
- 公网运行时门禁：`11/11` 通过。
- 监控告警门禁：`14/14` 通过。
- Nginx 与域名迁移门禁：`16/16` 通过。
- 数据库空库基线与 `prisma/schema.prisma` 一致。
- 导航审计：注册页面 `438` 个，字面量跳转 `290` 个，无重复路由。
- 九证据聚合与最终双人验收测试：`17/17` 通过，覆盖正常 `GO`、主机预检失败、新基础设施未达到 `launch` 阶段、数据库核验不是 `final`、Prisma 迁移状态未通过、客户端配置指纹不一致、运行版本不一致、现场证据过期、固定包允许 `dirty`、版本保留审计执行破坏性操作、来源报告缺失、机器判定非 `GO`、人工检查缺项、同一人冒充双签以及证据路径逃逸等 `BLOCK`。
- 固定包与原子激活测试共 `6` 条；Windows 本机 `5` 条通过、`1` 条因 Git Bash 不提供真实 POSIX 软链接而跳过，该条必须在 Linux CI/生产预演中通过。覆盖提交 SHA 一致/不一致、候选验真失败自动清理、独立五端验真报告与发布标识不一致时阻断，以及同一固定包首次部署失败后复核既有正式目录并安全重试。
- 固定版本回滚新增独立行为测试：Windows 本机验证非法版本号与二次确认不一致均在接触服务器状态前阻断；Ubuntu `release-safety` 门禁进一步验证数据库迁移边界阻断及只读回滚复核不改变 `current` 和发布历史。该自动化只验证脚本行为，不替代新服务器到位后的 `ROLLBACK_VERIFY_ONLY=true` 现场演练。
- 版本保留与当前状态一致性测试：`1/1` 通过；当前版本以 `current/.release-id` 为唯一权威身份，兼容指针落后或保留发布包遭篡改都会阻断上线，审计只报告清理候选而不自动删除。
- 五端客户端成品审计与交付验真测试：`7/7` 通过；覆盖 Admin、H5、微信小程序、App、Harmony 正式公网配置固化、源码提交强绑定、成品内容变化导致 SHA-256 指纹同步变化、源码映射残留时失败且仍落盘审计报告，以及下载成品被改动、发布标识或源码提交不一致时独立复验阻断。
- 客户端配置绑定测试：`3/3` 通过；覆盖 CI 与服务器配置一致、不一致以及发布标识/源提交不一致三类路径，绑定文件不保存原始 URL。
- 生产触发机器门禁测试：`10/10` 通过，覆盖默认分支、发布标识同值确认、源提交格式、部署就绪开关、数据库迁移独立确认、生产 SSH 主机指纹和部署架构必填及合法性。
- 手动生产工作流只允许从默认分支发起，并在固定包发布前使用 GitHub `production` Environment 的正式公网变量重新构建、审计五端客户端，归档带源提交、逐端字节数和确定性 SHA-256 内容指纹的 `client-artifact-audit.json` 以及五端可发布成品；同一任务会立即用独立验真器重算五端指纹并保存 `client-artifact-verification.json`，下载、转交或提交商店前还必须再次运行同一验真器，阻断拿错包、漏文件和转交后篡改。打包任务会在独立干净检出中再次生成严格 `source-freeze-readiness.json`，并将冻结证据、不含原始 URL 的 `client-config-binding.json`、客户端审计报告和独立验真报告一并写入固定包；包级验真还会逐端核对目录、文件数、字节数和内容指纹。服务器必须在重新构建前用共享生产环境重算指纹；临时公网配置文件不会进入归档，冻结证据、固定包清单、CI 源提交、五端成品指纹和服务器期望提交必须完全一致。
- DNS 生效后的 `.github/workflows/verify-production.yml` 会先确认最终数据库迁移核验证据和权限受控的共享基础设施接入清单存在，再只读刷新主机预检、新基础设施接入、客户端配置绑定、环境、运行时与版本保留证据并完成九证据聚合；它不启动容器、不执行数据库迁移，也不改 DNS，仅 `launch-decision.json` 为 `GO` 时通过。
- 发布与回滚不再分别维护 `current` 和独立版本号文件：`current-release-id` 固定跟随 `current/.release-id`，切换中断不会造成两个当前版本身份互相冲突。
- 部署不再仅凭旧实例仍能返回 `alive` 就判定成功：轻量存活响应必须同时报告本次 `RELEASE_ID`，版本不一致会持续等待并进入既有自动回滚路径；只有新实例身份一致后才允许切换 `current`。
- systemd 不保存初始化进程里的临时版本号；每次开机、停止和重载都由 `scripts/release/current-compose.sh` 从权威 `current/.release-id` 重新绑定 Compose 的 `RELEASE_ID`。服务单元同时固定初始化时的 `PLATFORM_ROOT`、`RUNTIME_DIR`、`ENV_FILE`，避免自定义路径在重启后回退到默认目录；缺失、格式异常或 `current` 指向非发布目录都会拒绝启动。
- 生产机磁盘清理只允许删除悬空镜像与过期构建缓存，禁止 `docker system prune -a` 删除旧版有标签镜像；数据库备份以文件锁拒绝并发任务，避免计划任务重叠生成冲突归档。
- “部署完成”只会在完整公网与依赖健康检查通过后输出；新镜像已启动但后置验证失败时，部署脚本会恢复旧镜像及其旧 `RELEASE_ID`，并再次核对回滚实例的轻量存活与版本身份。
- 固定包首次启动若因瞬时容器、监控或依赖问题失败，会保留已逐文件验真的正式目录；再次提交完全相同的包时，激活脚本会比对清单并重新逐文件验真后恢复执行。相同发布标识但包清单不同仍会硬阻断，禁止覆盖。
- 隔离 QA 固定包已完成包级 SHA-256、逐文件清单和必要运行脚本验真；QA 包已清理，未部署到任何环境。
- `pnpm release:verify:local` 最新一次当前工作树全量验证于 2026-08-01 00:28（UTC-07:00）启动，退出码为 `0`，耗时 664.2 秒；覆盖整套代码上线门禁、全仓 lint、类型检查、引擎/算法/服务端测试以及 Server、Admin、H5、微信小程序、App、Harmony 生产构建。六类成品均在本次任务中刷新，说明本地发布链路可从总门禁连续执行到六端成品，不是分项结果的简单拼接。构建日志仅保留 Dart Sass legacy JS API 弃用提示，未产生阻断错误；该提示应在 DCloud 构建链允许升级时统一治理，不影响本次生产成品生成。

2026-07-31 16:15—16:23 又按组件独立复核了生产构建，Server、Admin、H5、微信小程序、App、Harmony 均退出码为 `0`，对应成品目录时间同步刷新。微信小程序主包为 `1.90 MB`，未超过 `2 MB` 上限，但已进入体积警戒区，后续新增主包依赖必须先拆分或清理资源。严格商店门禁当前为 `21/23`：仅“正式语音/实时音视频原生 SDK 注册”和“鸿蒙正式签名并覆盖安装旧版真机验证”保持阻断；这两项必须取得受控供应商/签名资料后完成，不能用占位配置伪造通过。

迁移现场文档已完成防误操作收口：`docker/RUNBOOK.md` 降级为组件运维参考，旧部署检查表和灾备策略均显式让位于本总览及权威现场清单；应用回退只允许调用已验真固定发布包，数据库默认不逆向、不用旧库覆盖当前库，灾难恢复只能先恢复到新建隔离空库并完成对账。所有可复制的初始化、部署、健康检查、备份和回滚命令必须显式携带同一 `DEPLOY_TARGET` 与 `/opt/guoxue/shared/.env.production`。

当前构建仍会输出 DCloud 依赖链的 Sass legacy JS API 弃用提示；它不影响当前产物，但应在 DCloud 编译链支持新 Sass API 后升级并移除，生产环境禁止运行 Vite `dev`/`preview`。

代码门禁命令：

```bash
pnpm release:gate:code
```

## 新环境外部阻断项

2026-08-01 03:23（UTC-07:00）再次从当前完整工作树执行 `pnpm release:verify:local`，耗时 678.6 秒并以退出码 `0` 完成：代码门禁、全仓 Lint、类型检查、引擎/算法/服务端测试及 Server、Admin、H5、微信小程序、App、Harmony 六端生产构建全部通过。运行时门禁已升至 `12/12`，新增 CLB 真实 IP 与共享 NAT 分层限流保护；DCloud 构建仍仅有已知 Sass legacy JS API 弃用提示。

同轮从当前外部网络对两台 CVM 进行 TCP 暴露面复核：只有受控 SSH 22 可达；80/443、应用 3000、PostgreSQL 5432、Redis 6379 及 Prometheus、Grafana、Alertmanager、Loki、Tempo 等监控端口均不可达。节点 Nginx 80 仍只用于 CLB 回源，监控 Web 端口继续绑定 `127.0.0.1`。

以下事项不能由本地代码代替，未完成时只能做隔离预览：

| 类别       | 必须交接或验证                                      | 当前状态      |
| ---------- | --------------------------------------------------- | ------------- |
| 源代码基线 | 变更复核、受控提交、默认分支合并与干净工作树固定包  | 待冻结/待复核 |
| 计算与网络 | 新服务器、VPC/子网、安全组、负载均衡、受控 SSH      | 已配置并演练  |
| 数据服务   | 新 PostgreSQL、新 Redis、TLS、备份保留与隔离恢复    | 已配置；保留期待决策 |
| 公网入口   | 新 API/H5/后台/资源域名、备案、证书、DNS 与回滚权限 | 预发布已通；长期证书/合规待办 |
| 对象存储   | 私有 Bucket、CORS、防盗链、生命周期与签名链接       | 私有回源已通；安全策略待决策 |
| 正式凭据   | 应用密钥、AI、支付、监控、企业微信告警等字段        | 监控/告警已通；支付等仍待办 |
| 数据迁移   | 源库导出、停写窗口、表行数、业务对账与旧库保留      | 待现场执行    |
| 收款闭环   | 至少一条真实支付、退款、回调与账务核对              | 待联调        |
| 多端发布   | 小程序/App/Harmony 签名、隐私清单、商店账号与提审   | 待外部授权    |
| 实时语音   | 供应商账号、原生 SDK/bridge、真机基座与正式包验证   | 待开通        |

2026-08-01 02:59（UTC-07:00）最新机器冻结盘点已写入 `artifacts/release-audit/source-freeze-precheck-20260801.json`：当前工作树共有 309 个状态条目（231 个已跟踪未暂存变更、78 个未跟踪目录/文件入口，展开为 119 个实际未跟踪文件），暂存为 `0`、冲突为 `0`，当前分支为 `deploy/20260724-content-ui-batch`、源提交为 `9f2092e5e6565f92ca40b1a2559287e1f79a4ea7`。未发现真实敏感文件名、超过 `1 MiB` 的未跟踪源码或二进制发布物。3 份根目录 `kiki-prompt*.md` 属于本机控制台交接说明，文件仍保留在磁盘，但现已同时由 Git 与 Docker 构建排除，不再进入源码冻结候选。两个废弃商户页面的删除已由商户履约专项门禁验证，旧 `alertmanager.yml` 的删除则由无凭据模板与运行时渲染链路替代，均未发现残留运行路由风险。本地验收材料仍完整保留但不进入版本库；其余变更已通过本地全量验证，但尚未形成可追溯的正式源代码基线。生产固定包会按设计拒绝脏工作树，迁移前必须先完成变更复核、受控提交和默认分支合并，再从干净工作树生成固定发布包。不得通过放宽 `dirty` 校验、临时复制目录或删除未跟踪文件绕过该步骤。

任何密码、Secret、私钥、证书正文或完整数据库连接串都不得写入本文、Git、聊天、截图或构建日志。

## 明天拿到新资源后的执行顺序

### 1. 只读预检

先上传固定包和校验文件，不解冻生产发布，不修改主机配置：

```bash
export PROJECT_DIR=/opt/guoxue/package-check
export ENV_FILE=/opt/guoxue/shared/.env.production
export DOMAIN=new.example.com
bash scripts/release/preflight-host.sh
```

预检失败时停止初始化；不得通过降低 CPU、内存、磁盘、时钟同步或包身份要求绕过。

### 2. 初始化与完整环境验收

按迁移手册完成 Docker、目录、证书、私有配置和监控初始化，随后执行完整环境检查。报告必须写入本次发布专属证据目录，不得包含密钥值。

### 3. 数据库恢复和对账

使用受控导出、SHA-256、目标库非空保护和恢复后逐表行数/业务完整性/序列校验。首次恢复前创建目标库快照；旧库至少保留 72 小时。回滚只恢复流量，不用旧库覆盖切流后新数据。

### 4. 固定包激活

生产包必须由干净工作树生成，发布标识在本地、归档、服务器目录、容器环境和健康报告中完全一致，提交 SHA 必须与发起任务的默认分支 GitHub 源提交一致。生产任务还必须用正式公网变量重建并审计 Admin、H5、微信小程序、App、Harmony 五端成品，并在服务器实际构建前通过客户端公开配置指纹复核；数据库迁移默认关闭，只有已评审并另行输入 `migrate:<release_id>` 确认值的迁移才允许显式启用。

### 5. 预览验收和演练

在 DNS 切流前通过受控 Host/预览入口完成：核心用户流程、支付退款、对象存储、WebSocket、监控告警、备份恢复、单节点摘除和版本回滚演练。

### 6. DNS 切流与最终证据

DNS 已指向新环境后，手动运行 GitHub Actions 的 `Verify Production Cutover`，输入与发布完全一致的 `release_id` 和确认值。该工作流只负责 SSH 调用固定发布包内的 `verify-production-cutover.sh`，GitHub 与人工应急不再维护两套命令。脚本会确认同一版本的最终数据库迁移核验证据和 `/opt/guoxue/shared/infrastructure-intake.json` 存在，在服务器刷新主机预检、新基础设施接入、客户端配置绑定、完整环境、公网运行时和版本保留报告，并完成九证据聚合。`launch-decision.json` 为 `GO` 后，还必须完成结构化 P0 双签并生成 `final-launch-decision.json`；只有最终判定为 `GO`，才允许把结论改为“正式上线”。

## 九份机器证据

同一 `TARGET_RELEASE_ID` 的证据目录必须包含：

1. `host-preflight-readiness.json`：实际新服务器 CPU、内存、磁盘、inode、时钟、基础命令、Node、PostgreSQL 客户端、Docker、Compose、DNS、端口、环境文件权限与固定发布包身份的脱敏预检证据；
2. `infrastructure-intake-readiness.json`：新服务器、数据服务、域名、对象存储、责任人、恢复演练，以及源库/目标库/DNS 权限、迁移演练、停写窗口和旧环境回退保留的 `launch` 阶段验收；
3. `package-verification.json`：固定包本地/服务器验真；
4. `release-directory-verification.json`：已部署目录验真；
5. `client-config-binding-verification.json`：当前服务器公开客户端配置与 CI 审计输入的指纹、发布标识和源提交复核；
6. `database-migration-verification.json`：最终一致性快照、逐表计数与业务完整性对账；
7. `environment-readiness.json`：完整生产环境检查；
8. `runtime-verification.json`：公网真实实例、依赖和入口验收；
9. `retention-audit.json`：当前/前序/迁移版本保护和磁盘余量。

聚合命令：

```bash
node /opt/guoxue/current/scripts/release/aggregate-launch-evidence.mjs \
  --release-id "$TARGET_RELEASE_ID" \
  --evidence-dir "/opt/guoxue/release-evidence/$TARGET_RELEASE_ID" \
  --max-age-hours 24 \
  --report "/opt/guoxue/release-evidence/$TARGET_RELEASE_ID/launch-decision.json"
```

任一来源缺失、过期、版本不一致、允许降级、存在错误或磁盘余量不足，聚合器都会输出 `BLOCK` 并返回非零退出码。

机器聚合通过后，在同一目录执行 `finalize-launch-acceptance.mjs --init` 初始化 `production-cutover-acceptance.json`，由不同技术与业务负责人填写九项 P0 检查及对应归档证据，再运行该脚本生成 `final-launch-decision.json`。最终脚本会校验路径越界、符号链接、证据新鲜度、双人同名、缺项和逐文件 SHA-256；任何一项异常均输出 `BLOCK`。机器 `GO` 不能替代最终双签 `GO`。

`client-artifact-audit.json` 和固定包内的 `client-config-binding.json` 是客户端成品审计与指纹基线，必须与五端成品和变更单一并归档；主机预检生成的 `host-preflight-readiness.json`、数据库最终核验生成的 `database-migration-verification.json`、服务器从权限受控原始接入清单生成的 `infrastructure-intake-readiness.json` 与重新生成的 `client-config-binding-verification.json` 均属于上述九份“现场运行可信链”。

## 明确禁止

- 禁止从服务器临时 `git pull` 不确定分支替代固定包。
- 禁止使用 `--allow-dirty` 生成生产包。
- 禁止自动删除旧发布版本、固定包、数据库快照或证据。
- 禁止在切流后用旧库覆盖新库。
- 禁止把降级预览、旧版本健康或单一 `/health` 成功当成正式上线。
- 禁止在新环境全部通过前关闭旧系统或取消回滚权限。
