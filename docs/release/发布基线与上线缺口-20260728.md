# 发布基线与上线缺口（2026-07-28）

## 当前结论

- 分支：`deploy/20260724-content-ui-batch`
- 2026-07-29 增量已完成 H5 沉浸式内容层返回能力、古籍阅读弹层滚动隔离、交互安全发布门禁，以及商家移动端/PC 端库存档案、供应商档案、采购到货质检批次、订单占用台账与单据追踪闭环；当前仍未部署或迁移生产环境。
- 最新已验证功能提交：
  - `b298b5a4` 直播回放进度与断点续播
  - `f59053d5` 空评论区唤起输入框
  - `a20c36f7` 圈子双年续费与失效审核页清理
  - `776eec75` 小程序不支持选择器清理
  - `d59723e3` 依赖安全与发布门禁
  - `cf884ebc` 新生产环境迁移门禁
  - `5bcd87a6` 多端新域名构建契约
  - `62c6934f` 商家库存履约容错与宽屏适配
  - `1dc095ff` 后台图表按需加载
  - `a492dea7` 移动端跨端静态错误清理
  - `30f65b5e` 移动端跨端验证结果归档
  - `ad413e6c` 后台与服务端静态错误清理
  - `330bb0ec` 移动端脚本静态门禁补齐
  - `52a65ccc` 共享算法包静态错误清理
  - `700ce8ee` 全栈静态检查基线更新
  - `97fcbd28` 完整本地发布门禁建立并跑通
  - `e425e3ad` 圈子治理规则生效状态校准
  - `a1b47090` 圈子年度报告直播观看统计补齐
- `artifacts/` 是用户本地验收材料，明确排除在提交和部署之外。
- 当前没有部署、迁移数据库或修改生产域名。

## 自动审计等级

### P0：0

没有发现会直接阻断发布的 mock、资金模拟、失效导航或生产默认放行。

### P1：0

此前四项已收口：

- 圈子发布授权由后端真实权限判断。
- 圈子两年续费已接通档位、金额、下单与支付确认。
- 直播回放支持账号级进度、断点续播和幂等上报。
- 失效的本地假用户审核页已移除。

### P2：11

均为显式“研发预告/即将开放”的延期能力，不伪装成已上线功能，包括同城空间、部分排盘预告、第三方绑定、赛事后续能力等。

## 本轮验证矩阵

截至 2026-07-29，以下本地验证均已通过：

- H5 生产构建。
- 微信小程序生产构建；主包 `1.89 MB`，最大分包 `1.90 MB`。
- App 生产构建。
- Server 生产构建。
- Admin 生产构建。
- Mobile TypeScript 类型检查。
- Mobile ESLint 错误由 `120` 个清理至 `0`；保留的提示均为非阻断型历史规范债务。
- Admin TypeScript 类型检查；Admin ESLint 错误由 `352` 个清理至 `0`。
- Server TypeScript 类型检查；Server ESLint 错误清理至 `0`。
- Apps 与 Packages 全仓 ESLint 错误清理至 `0`。
- 八字与紫微共享算法包测试：`13` 个测试套件、`236` 项测试全部通过。
- Mobile `16` 组排盘与历法算法回归套件全部通过。
- 商家库存聚合与退款定向测试：`55` 项全部通过。
- 商家订单、发货、物流回调与售后边界定向测试：`63` 项全部通过。
- Server 当前全量单元测试：`426` 个测试套件、`5729` 项测试全部通过；异常服务日志均来自故障降级测试用例，另有一个 Jest 工作进程未优雅退出的历史告警待定位。
- 导航静态审计：`438` 个页面、`288` 个字面跳转全部通过。
- `pnpm release:verify:local` 已完整跑通，覆盖上述检查及 H5、微信小程序、App、Admin、Server 生产构建；总耗时约 `8 分 10 秒`。
- 生产环境门禁仍需等待新服务器提供真实 `docker/.env.production`，缺少环境文件时会按预期阻断，不会误发生产。

后台图表已改为 ECharts 按需注册，图表公共分包由约 `1113 KB` 降至 `642.48 KB`，下降约 `42%`。直播后台原本未安装插件却使用的 `wordCloud` 系列已改为可用的横向打赏热度排行。

## 真实视觉与交互验收

- H5 文章专区已使用生产只读数据在本地最新代码中实际打开并截图检查：大图、三图、右侧单图三种文章卡片均正常展示，无横向溢出。
- 已实际点击文章卡片验证“原位放大—沉浸式详情—缩回原卡片”链路；详情层打开时底层滚动和全平台底部导航被锁定，侧边收起和 `Esc` 返回后原列表、滚动能力与底部导航均恢复。
- H5 沉浸式详情层新增左缘右滑收起能力，包含方向、距离和速度阈值；同时保留侧边按钮、浏览器历史返回、`Esc` 和 iframe 子页消息返回四条可达路径。
- 听书目录与古籍阅读目录已在真实长正文中实际打开并滚动检查：弹层手势不会再传递到底层正文。
- 古籍空评论区已实际点击验证：点击气泡或整块“还没有评论，来抢沙发～”区域均会展开评论输入框并自动聚焦。
- 新增 `pnpm release:audit-interactions`，并纳入 `release:gate:code`；当前手势返回、历史返回、滚动隔离、固定导航、触控热区、减少动态、视频首帧、评论唤起、客服消息入口与高争议场景投诉入口等契约共 `16/16` 项通过。
- 商家“库存与履约”H5 已在 `390 × 844` 手机视口实际打开并截图检查：库存健康度、经营待办、采购—入库—发货—售后流程、库存列表均正常显示，无旧版空白页和横向溢出。
- 同一页面已在 `1280 × 900` 宽屏视口实际检查，按移动端产品壳居中显示，不会把手机布局粗暴拉伸；商家 PC 操作使用独立管理后台页面。
- 已实际点击“发起采购”并检查采购弹层：供应商、联系人、电话、数量、进价、预计到货、备注、合计金额及保存草稿入口完整可见。
- 商家 PC 管理后台已在 `1440 × 1000` 桌面视口以真实数据态逐项检查：经营待办、库存健康度、采购—验收—可售—发货—售后单据链路、库存表格均正常显示。
- 已实际切换 PC 端“库存／流水／采购单”并打开“分批到货验收”和“新建采购单”弹层；采购进度、逾期状态、分批数量、供应商与采购明细均完整可操作。该验收入口仅在开发环境注册，不进入生产路由表。
- 商家移动端库存已补齐 SKU 档案抽屉：可查看状态、当前库存、精确到该 SKU 的最近流水，并可直接发起预警、盘点、报损和采购；从档案进入“全部流水”会携带商品与 SKU 双重过滤，不会混入其他规格记录。
- 商家 PC 库存台账已补齐同一套 SKU 档案与精确流水联动，并在真实浏览器中逐项点击验证“档案—最近流水—完整流水—清除筛选”闭环；抽屉在窄屏时限制为 `94vw`，不产生横向溢出。
- 采购到货已升级为不可变质检批次：移动端与 PC 端分别登记合格数、拒收数、拒收原因、验收仓库和批次备注；只有合格品增加可售库存，拒收品仅留存质检记录。批次使用商家与 `requestId` 联合唯一键防止重复提交二次入库。
- 数据模型新增 `PurchaseReceipt`、`PurchaseReceiptItem`，明细固化商品名、规格、合格/拒收数量和拒收原因，避免后续商品资料变化污染历史凭证；移动端与 PC 端均可按采购单查看不可变验收批次，服务端定向测试当前 `14/14` 通过。
- 库存展示已拆分为“账面现货、待付款占用、待发货占用、当前可售”四个真实业务口径；服务端按 `PENDING` 与 `PAID` 实物订单实时聚合占用，移动端和 PC 后台均显示同一套数字，盘点与预警继续以可售库存为准。
- 库存盘点统一录入“仓库实物总数”，服务端自动扣除待付款与待发货订单占用后写回可售库存；实物数低于订单占用时直接拒绝，盘点流水同时保存实物、两类占用与计算后可售数，避免盘点把已售库存重新释放造成超卖。
- 已修复平台自营商品在未发货退款时未回补库存的问题：自营与商家商品都回补真实库存，但只有具备商家主体的订单才生成商家库存流水，避免伪造归属。
- 商家订单列表会一次批量补齐物流公司、运单号、物流状态和运单更新时间，PC 发货页无需逐行查询运单；已发货订单可直接查看真实运单，不再出现“后端已发货、列表看不到物流”的断层。
- PC 批量发货已改用服务端批量接口，一次提交整批订单并逐项返回成功、幂等重放或失败原因；部分失败时只保留失败订单供重试，避免逐单并发请求导致重复发货与结果不透明。
- PC 订单页已移除对已发货/已完成订单的直接退款与拒绝入口，统一进入售后管理；旧兼容端点也必须先命中真实的“仅退款”申请，退货退款必须经过退货地址、买家回寄和商家验收入库，不能绕过质检直接退钱。
- 快递回调已完整区分揽收、运输、派送、异常、签收、退回、转投与清关状态；非终态延迟回调不能把已签收/已退回运单倒退。实时快递接口未配置或暂时故障时，服务端、移动端和 PC 后台都会回退显示数据库最近一次已同步轨迹，不再把已有物流信息显示成空白。
- 当前应用内浏览器不能访问本地开发地址，因此这次质检弹层尚未伪装为“已真人视觉验收”；已完成静态审计与多端编译，待新服务器部署后用真实采购单分别在手机、平板和 PC 复验。
- 空评论区和空状态气泡已统一接通输入框唤起事件，古籍、文章和圈子帖子共用同一交互能力。
- 全站共享导航以及古籍、文章、课程、搜索、智能体、圈子、商品详情、直播管理等 24 个高频独立导航入口已统一到不小于 `44pt` 的触控热区；视觉图标尺寸保持克制，仅扩大可点击范围。该规则已纳入交互安全发布门禁，避免后续页面优化时退回难点击的小图标。
- 已在 `430 × 932` 手机视口逐页检查古籍馆、课程、文章专区、搜索和课程目录：返回热区实测约 `50 × 50px`，页面无横向溢出；文章专区另在 `1024 × 768` 宽屏检查，内容壳保持 `480px` 居中而不被粗暴拉伸。
- 全局循环动效、语音通话光环和声波、智能体流式光标、听书声波与盖章入场均支持 `prefers-reduced-motion`；正常用户仍保留平台动感，开启系统“减少动态”的用户自动切换为静态或瞬时完成状态。

本轮在增量修改完成后再次通过：

- `pnpm release:gate:code`：P0/P1 均为 `0`，能力审计 `12/12`、交互审计 `16/16`、商家履约审计 `32/32`、导航 `438` 页面/`288` 跳转全部通过。
- `pnpm lint:all` 与 `pnpm typecheck:all`。
- 八字/紫微共享引擎 `13` 个测试套件、`236` 项测试。
- Mobile `16` 套算法逐值回归。
- Server `426` 个测试套件、`5729` 项测试。
- Server、Admin、H5、微信小程序、App 五类生产构建。

## 已接受的构建提示

- `vod-js-sdk-v6@1.8.0` 内部 `js-sha1` 使用 `eval`。该代码来自腾讯云官方点播上传 SDK，当前仅位于懒加载的 `tc_vod` 分包；上传签名链路依赖它，暂不自行改写第三方 SDK。
- `ElementPlus`、富文本编辑器和 `tc_vod` 分包体积较大，但均为路由或能力懒加载资源，不属于首页首屏阻断项。后续可在真实网络环境中继续测量缓存命中率和首次进入时延。

## 发布前硬门禁

本地代码门禁：

```powershell
pnpm release:verify:local
```

新服务器环境门禁：

```bash
pnpm migration:check-env /opt/guoxue/shared/.env.production --full \
  --deploy-target "$DEPLOY_TARGET" \
  --node-role operations \
  --report release-evidence/environment-readiness-$(date +%Y%m%d-%H%M%S).json
docker compose \
  -f docker/docker-compose.yml \
  -f docker/docker-compose.prod.yml \
  --env-file /opt/guoxue/shared/.env.production config -q
```

`release:verify:local` 会串行执行上线审计、全仓 ESLint、三端类型检查、共享算法包构建与测试、16 套排盘/历法逐值回归、服务端全量测试，以及 Server、Admin、H5、微信小程序和 App 生产构建。`release:gate:full` 必须在 CI 或完整构建机上先校验真实生产环境再执行同一套完整验证；正式 `launch` 阶段必须显式传入 `--release-id "$RELEASE_ID"`，让客户端审计、独立验真和配置绑定证据与同一固定包批次关联。固定发布包不携带宿主机 `node_modules`，新服务器不重复安装项目依赖或执行全仓构建，只复核已归档报告、固定包、配置绑定、Compose、生产镜像、数据库和运行时证据。

其中 `release:gate:code` 已加入 `release:audit-capabilities`：语音通话样板、
三端麦克风权限、建房链路、古籍 TTS 与三端构建入口任一发生代码断链都会阻断；
DCloud appid、微信合法域名、原生 RTC SDK 与真机授权会明确列为外部待办。
同时加入 `release:audit-interactions` 与 `release:audit-merchant-fulfillment`：
沉浸式返回、弹层滚动隔离、固定导航，以及移动端/PC 端 SKU 档案、精确流水、
采购验收、物流和售后链路任一退化都会直接阻断发布。

数据库恢复后：

```bash
export TARGET_RELEASE_ID='release-20260731-001'
export EVIDENCE_DIR="/opt/guoxue/release-evidence/$TARGET_RELEASE_ID"
export SOURCE_COUNTS_FILE='/安全路径/<prefix>.table-counts.tsv'
export SOURCE_CHECKSUM_FILE='/安全路径/<prefix>.dump.sha256'
export SOURCE_MANIFEST_FILE='/安全路径/<prefix>.manifest.txt'
export MIGRATION_VERIFICATION_MODE='final'
export MIGRATION_VERIFICATION_REPORT="$EVIDENCE_DIR/database-migration-verification.json"
bash scripts/migration/verify-postgres.sh
```

核验脚本会通过实际生产 `server` 镜像把 `prisma migrate status` 纳入同一份数据库机器证据，不依赖固定包中不存在的宿主机 `node_modules`。任何 P0/P1、环境校验错误、Compose 配置错误、测试/构建失败、数据库计数差异或待执行/失败迁移都必须阻断发布。

## 当前仍依赖新资源的工作

- 真实生产密钥和新域名填充。
- Compose 展开、镜像构建及容器健康检查。
- PostgreSQL 恢复演练和逐表核对。
- 对象存储/CDN 同步及第三方控制台回调更新。
- H5、小程序、App 与手机/平板/PC 的新域名真机验收。
- DNS 切换、72 小时双环境观察和资金/库存/物流对账。

具体执行顺序见 `docs/operations/服务器数据库域名迁移手册-20260728.md`。

## 2026-07-29 短视频首帧与全平台卡片一致性增量

- 生产只读验收确认短视频列表已经采用“今日放映 + 灵感片场”的国学影卷式陈列；旧截图中的普通双列空卡片并非当前代码形态。
- 对“未上传首图”的短视频建立两阶段视觉链路：先显示与内容类型一致的国学渐变封面，浏览器或客户端真正解码出视频首帧后再渐显切换，避免原生 `<video>` 在首帧未就绪时暴露黑屏。
- 首页瀑布流、通用视频卡片、创作者中心榜单与作品列表统一传递 `videoUrl`，不再出现短视频广场可取首帧、其他入口只能显示占位图的差异。
- 创作者接口新增 `videoUrl` 返回，并兼容旧版 `SanitizePipe` 留下的 `&#x2F;`、`&#47;`、`&amp;` 媒体地址，避免历史视频在创作者页失效。
- `prefers-reduced-motion` 用户不执行首帧透明度动画，仍保留清晰的静态切换。
- `release:audit-interactions` 新增两项短视频防回归规则，当前由 `9/9` 扩展为 `11/11`。

本轮增量验证结果：

- Mobile TypeScript：通过。
- Server TypeScript：通过。
- 定向 ESLint：`0 error`；保留既有模板格式 warning。
- `git diff --check`：通过，仅有 Windows 行尾提示。
- H5 构建：通过。
- 微信小程序构建：通过；主包 `1.89 MB`，最大分包 `1.90 MB`。
- App 构建：通过。

当前应用内浏览器安全策略不允许访问本地开发地址，因此没有把“本地未部署改动”伪装成已完成真人视觉验收。生产环境只读检查已用于确认问题基线；新服务器部署后必须使用真实无首图视频，在手机、平板、PC H5 以及小程序/App 真机上复验“生成封面 → 首帧渐显”的最终效果。长期最佳方案仍是将移动端上传链路迁移到 VOD 直传并在服务端回调生成持久化截图，客户端首帧仅作为容错兜底。
