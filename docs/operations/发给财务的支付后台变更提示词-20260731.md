# 发给财务的支付后台变更提示词

请只处理“热卜星火”新系统支付配置，不要修改第三方旧“热卜国学”小程序。

正确身份：

- 热卜星火（新系统、此前支付已调通）：`wx06397e8ab26bed9e`
- 热卜国学（第三方旧小程序、已限制分享）：`wx07a95ead2bb66d04`

## 汇付后台

1. 找到已经调通的热卜星火 `wx06397e8ab26bed9e` 对应商户/产品配置，不要新建商户，不要重置公私钥。
2. 确认小程序支付产品 `T_MINIAPP` 绑定的 AppID 仍为 `wx06397e8ab26bed9e`。
3. 将本次变更单中经运维签字确认的新服务器/NAT 固定出口 IP 加入白名单（如该商户启用了 IP 白名单）；不得复制历史服务器 IP。
4. 将异步通知地址更新为本次正式配置的 `HUIFU_NOTIFY_URL`。它必须是 `PUBLIC_API_URL` 同源的 `/api/v1/huifu/notify`，不得保留历史预发布或旧生产地址。
5. 原商户号、系统号、产品号、公钥和私钥保持不变。完成后把“商户配置页、AppID、IP 白名单、通知地址”的非敏感截图发回；不要在聊天中发送私钥或完整密钥。

## 微信支付商户平台

1. 找到此前已经与热卜星火绑定并调通的商户号。
2. 在「APPID 账号管理」确认 `wx06397e8ab26bed9e` 状态正常；不重复绑定，不重置 APIv3 密钥，不重新申请支付商户。
3. 不修改热卜国学 `wx07a95ead2bb66d04` 的原支付配置。
4. 若该商户当前使用微信内网页 JSAPI 支付，在「开发配置」增加本次 `PUBLIC_H5_URL` 对应的真实支付页面授权目录，并移除已退役环境目录。
   未开通或暂不使用 JSAPI 时跳过此项。
5. 小程序支付通知地址由新系统下单请求携带，不需要在商户平台重复填写；只需确认商户号和热卜星火 AppID 的绑定正常。
6. 完成后只提供商户号、绑定状态和非敏感截图；不要发送 APIv3 密钥、私钥或证书正文。
