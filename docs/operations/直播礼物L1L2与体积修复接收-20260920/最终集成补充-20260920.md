# 直播礼物、微信体积与支付排盘线最终集成补充（2026-09-20）

## 1. 实际接收目标

本次没有再以过时的 `release/integration-20260801 @ 2e458597` 为目标。实际接收目标是已经包含三候选、支付、第三方排盘以及两节点正式发布记录的最新接收线：

- 接收前：`receive/integrated-three-20260919 @ c0035a8f4e5c9e9bb26a0c8f0e99060a714818f2`
- 最终隔离分支：`codex/integrated-live-gift-paipan-pay-20260920`
- 接收后：`d535a70b26aaa13ff195913e4ea8b89ccf6204a8`

礼物和体积批的五个代码提交均未在目标中出现，按原顺序等效挑入；评审文档随后一并进入。支付与排盘线原有提交没有重新挑选。

| 原接收提交 | 最终集成提交 | 内容 |
|---|---|---|
| `1850907fc5b3b923cdcd45c4e463af712c735f33` | `2c6e6ace0` | 直播礼物 L1/L2 主体 |
| `3d3c0082c8d72bf44fc9f1e6982e12e3bb344148` | `f7bc0d00a` | 房间终态残留修复 |
| `4fae2a3ad3bf8bfbf59d1f2193da74ff22ade9cb` | `9de344854` | 缓冲重试保留反馈 |
| `1e32f0c801c57d21c7b9e247479900c7392bfefa` | `1977c8578` | 微信主包体积修复 |
| `697f7bda0eae1da898b57b168d06aff76573966d` | `47b24617c` | 恢复前台时连击合并及残留节点修复 |

## 2. 最终集成验证

- 礼物编排：48 / 48 通过，包含恢复前台连击合并、同 key 不覆盖、车道占满继续合并。
- 排盘嵌入、返回与分享：45 / 45 通过。
- 微信小程序正式构建：成功；WXML 615 个文件通过，兼容性 2,682 个文件通过，33 个分包全部过门禁。
- 最终主包本地求和：1,703,194 B；加 288 KiB 历史平台开销后的估算为 1,998,106 B；按 2 MiB 上限再保留 80 KiB 后，当前余量为 **17,126 B**。

此前的 18,432 B 只对应礼物/体积独立候选。支付与排盘线合并后必须使用 17,126 B，后续任何源码变化仍须重算。

## 3. 构建配置

本次 `.env.production` 从已验证的礼物接收工作树复制到最终隔离工作树；该文件被忽略，不进入提交。四项均为公开配置：

- `VITE_RELEASE_CHANNEL=formal`
- `VITE_API_URL=https://api.rebugx.cn`
- `VITE_PUBLIC_H5_URL=https://api.rebugx.cn/h5/`
- `VITE_PUBLIC_ASSET_ORIGIN=https://static.rebugx.cn`

它们对应当前正式 API/H5/静态资源入口。`gx.yrydai.com` 是已部署的新 H5 入口，本次未把正式构建基线切到该域名。

## 4. 运行环境验证状态

### 小程序体积批

微信开发者工具已重新安装，`app.asar` 约 213 MiB，官方 CLI 能启动 IDE。CLI 身份探针一度返回已登录，但打开本候选时明确要求重新登录；已发起官方登录二维码。本轮不上传、不提审。

在登录完成前，以下页面的开发者工具实载仍是“尚未覆盖”：`pkg-live/watch`、`pkg-course/publish-course`、`pkg-creator/teacher-dashboard`、`pkg-creator/teacher-profile`、`pkg-profile/vip`。源码引用与构建产物完整性已通过，但不能替代页面实载。

### 礼物批

现有 Android 247 候选只包含支付与排盘，不包含本批礼物代码，不能冒充匹配版本的真机证据。当前没有可复用的匹配正式包；因此正式发布前仍需生成匹配候选包，并在受控测试视频或隔离测试直播间检查视频叠层、关键按钮、缓冲恢复和退出清理。模拟成功事件只能验证展示，不替代真实消息链路，且不得扣真实币。

### 溢出摘要

当前“另有 N 份心意已合并”的 N 是本房间会话内**累计发生过的队列溢出数量**，不是此刻仍在等待的数量。它只在进房、关房或清理时归零，可能持续显示，属于既有体验问题；没有发现无限内存累加或遮挡关键按钮的新增证据，本批不扩展修复。

## 5. 发布判断

- 礼物批：已进入最终隔离集成，逻辑与构建通过；匹配运行环境的真机验证尚未覆盖，暂不具备正式发布条件。
- 体积批：已进入最终隔离集成，构建与体积门禁通过；微信开发者工具页面实载和最终真实 upload 尚未覆盖，暂不提交审核。
- 支付与排盘：原有服务端/H5 已部署证据保留；本次没有重新部署。支付小程序版本仍须在开发者工具验证后再走上传审核。

回退时可从最终隔离分支按逆序撤回上表五个等效提交；无数据库迁移。支付与排盘既有提交保持不动。
