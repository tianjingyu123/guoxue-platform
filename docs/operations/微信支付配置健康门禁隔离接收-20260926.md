# 微信支付配置健康门禁隔离接收（2026-09-26）

基于四窗口组合 `1bd1ad30808461caa13546bbf2e9f11feac10eaa` 与首页卡片独立接收 `db6eaf2813585e62108ecbb20d4489a06c5e42f9`，在隔离工作树 `codex/release-safety-receive-20260926` 接回原候选 `9a71f031d` 的四个服务端文件。应用无冲突，未覆盖根工作区。此批只恢复微信支付配置健康检查/后台就绪度，不改变订单建单、收费、退款、商户配置和真实支付。

复核发现四窗口组合原来缺少生产运行时 DB 配置来源、商户白名单、公钥与公钥 ID 同时存在的检查；第三方配置后台也没有公钥模式配对/ID 格式就绪检查。恢复后，配置不完整会显示 `degraded` 或后台未就绪；**健康状态的改善不能代替真实支付验签和收款测试**。实际两节点所需变量与 DB 配置仍待目标环境只读核验。

隔离依赖通过本工作树 `pnpm install --offline --frozen-lockfile` 恢复；`pnpm build:prerequisites` 在本工作树生成 Prisma 客户端和三个共享包。定向 Jest 两套、35 项全部通过；服务端 `tsc --noEmit` 通过；`git diff --cached --check` 通过。未执行真实渠道请求或生产操作。

此批可作为发布前配置预检的一部分，但支付意图旧候选 `128a00fca` 与当前建单后落库代码的行为差异尚未接收评审；不得因本批测试通过就认定支付链路具备发布条件。首页卡片批仍需正式小程序预览/真机视觉验收。
