# 圈子付款直接入圈隔离评审（2026-09-26）

分支 `codex/four-window-circle-pay-return-20260926`，基于四窗口组合评审 `dac57412181e9188daffbc2278424f5937a88639`；业务修复独立提交 `98f4f00b992acb83d14a445e3030926677c34cfb`。未进入正式发布线或线上环境。

原流程在商城/微信小程序支付回调被服务端确认为已支付后，仍先展示通用成功页，圈子会员需自行寻找入口；App 汇付支付宝分支已可直接回圈子。现统一在 `completePaidOrder` 完成本人订单服务端查单和圈子权益确认尝试后，对 `CIRCLE_JOIN`、`CIRCLE_RENEW` 且有目标圈子 ID 的订单直接用根路由进入该圈子，携带订单号供圈子详情页再次核验本人已付订单并补做失败的权益确认。没有根据 SDK “拉起成功”或前端回跳标志判定到账；目标 ID 缺失时仍保留通用结果页。普通实物商品订单流程不变。

原支付回跳静态测试有一处只匹配旧变量名 `st`，与当前 App 支付回调参数 `order` 不符；本批修正断言，并增加执行真实函数片段的行为测试，覆盖圈子加入、续费自动跳转及普通商品仍去结果页。支付/地址/旧排盘相关定向测试 **30/30** 通过；移动端类型检查、使用正式公开配置的 H5/微信小程序/App 本地构建通过，App 原生脚本门禁通过。小程序 624 个 WXML、2717 个文本校验通过，主包 1.64 MB、保守上传估算 1.92 MB，最大分包 `pkg-paipan` 1.67 MB。测试和构建均未发起真实支付，也未上传、部署或替换用户 App。

待匹配正式包的真机验证：微信小程序与 App 各完成一次受控圈子付款或隔离订单回调，核查查单到账、权益已开通、自动落在对应圈子、返回栈不回收银台；模拟权益确认失败时核查圈子内可重试，不得重复扣费。实物商品、充值和退款仍分别核验。撤回该独立提交即可恢复通用结果页路径；已产生的订单与权益状态不应随代码回退篡改。
