# 四窗口 AI 追问试用额度补丁隔离接收评审

日期：2026-09-26。工作树 `D:/gx-deploy-91/.worktrees/four-window-ai-quota-review-20260926`，基线为四窗口组合评审 `66e395efdc579807bc40e07e43b5c103e12bf7fb`，原候选 `a358ac98dfc0e3dce76f9b436d9a402a1016415a` 以独立提交 `8db7bb5cbba187e31e4b37cd9ca493c5d2447ddf` 接入；自动应用，无冲突。它仍是隔离评审，不是正式发布线或线上版本。原候选工作树未改，根工作区未改。

代码将 `freeUsed` 的“先读再无条件加一”改为数据库条件递增：只有当前值低于配置试用上限才能抢到一次；未抢到时再按已有的 `paidRemaining > 0` 条件扣已购次数，否则返回购买/会员引导。没有改变价格、购买发包事务、模型调用或真实额度。组合分支对智能体列表的 `voiceEnabled` 扩展得以保留。

隔离依赖离线安装；Prisma Client 按**本工作树 schema** 在本工作树内生成，共享工作区未生成。服务测试 `bot.service.spec.ts` 与 `bot.controller.spec.ts` 为 55/55 通过，定向 ESLint 0 error。第一次运行因隔离树缺少生成客户端与共享包构建而无法编译；生成客户端、构建本树 shared/bazi/ziwei 包后重跑通过。

另用只监听本机的 PostgreSQL 16 和仓库 `full-baseline.sql`，通过本分支 `BotService.consumeQuota` + 真实 Prisma Client 对同一合成用户/智能体并发发起 20 次请求：初始 `freeUsed=2/3`、已购 0 时，1 次 trial、19 次拒绝，最终 `freeUsed=3`；初始已购 5 时，1 次 trial、5 次 paid、14 次拒绝，最终 `freeUsed=3`、`paidRemaining=0`。两组均未超发。临时测试代码已移除，数据库已停止，无真实账号或余额操作。

此验证覆盖数据库并发条件和服务分支；模型失败退额、流式断线、跨进程网络入口及真实付费仍未覆盖。本提交可独立撤回；与迁移顺序修复分支分开保留回退点，不自动部署。
