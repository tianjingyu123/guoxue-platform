# AI 阅读层级与古籍内容导览隔离交接（2026-09-22）

## 候选与范围

- 基线 `ca1df63da`；分支 `codex/ai-reading-cards-20260922`。此候选与 `codex/ai-quota-reliability-20260922` 并列隔离，主线需分别评审接收。
- AI 搜索与古籍 AI 助手接入同一阅读组件：短答全文直显；长答在自然断句处分成主句和可展开详情，找不到安全断句就保留全文。内容不由前端另行总结或改写。
- 古籍问答服务复用全平台按问题难度控制篇幅的提示规则，单句古文先给白话解释，出处不确定时不编造。
- 古籍 AI 助手只在学习类提问后异步补最多两张真实公开内容卡；默认限古籍和文章，用户明确找课程/圈子时才可显示对应入口，收费内容明示以详情页为准。导览失败不影响已生成的回答；清空页面或退出后忽略迟到结果。卡片跳转限制为平台公开详情页。
- 视觉沿用现有暖白、深墨与朱红；回答主字号 32rpx，正文 30rpx。没有给寒暄附加图像或大面积装饰。内容封面来自平台记录，无封面时使用类型印记。

## 本轮新运行的验证

1. `tsx apps/mobile/scripts/verify-ai-reading.ts`：短答/长答分层、无安全断句不截断、投诉抑制、公开详情页白名单、商业卡按明确意图过滤，检查通过。
2. `node apps/mobile/scripts/verify-ai-reading-sfc.cjs`：三个变更 Vue 页面的脚本和模板定向编译通过；不代表 uni-app 三端构建或真机表现。
3. `classic.service.spec.ts`：41 项通过，其中新增古籍提示词断言；本工作树复用的 Prisma Client 与 schema 不一致，普通 ts-jest 因旧有 `reviewStatus` 类型错误无法启动，故本次使用 `--globals '{"ts-jest":{"diagnostics":false}}'` 仅执行运行时单测，不将其计为类型检查通过。
4. 变更文件 ESLint 0 error；存在既有 Vue 格式 warning。
5. H5 整包构建未通过：首次缺生产公开域名配置；用 `example.invalid` 临时公开地址重新编译后，失败于 `packages/shared/src/paipan/index.ts/luopan-rings` 路径解析错误。该产物不用于发布，也不能据此判断本轮改动跨端可用。

## 主线验收与撤回

- 前置：修复共享排盘包路径/构建基线，提供匹配提交的 H5、微信小程序和 Android 测试构建及受控账号。不需要真实付费；课程/圈子卡可先用公开免费样本。
- 入口：AI 搜索弹层、古籍首页或古籍搜索进入的“古籍 AI 助手”。样本至少包含“你好”“仁是什么意思”“《论语》一句原文解释”“找一门课程”“我要退款”及长问题。
- 通过：短答不出现多余展开；长答主句准确且详情完整；字号可读；卡片有真实标题/封面/目标且可打开；投诉无卡片；清空或退出后无迟到卡片。需分别记录 H5、微信小程序、Android 真机的截图或录屏与构建 SHA，保存至 `artifacts/ai-service-acceptance/reading/`。目前均未覆盖。
- 未合入、未部署、未调用真实付费链路。主线未接收时不合入即撤回；接收后若有问题，按候选提交回退。不能把本轮模拟/组件编译写成正式环境验收。
