# AI 导览意图边界交接（2026-09-22）

状态：隔离候选，未合入、未部署。工作树 `.worktrees/ai-guide-intent-20260922`，分支 `codex/ai-guide-intent-20260922`，基于 `ca1df63da`。

## 本轮改动

- 独立寒暄直接给一句短答，不检索内容，也不消耗模型调用；带实际问题的问候仍走原回答链路。
- 退款、投诉、订单、登录故障等服务问题不附内容卡；原 AI 回答链路保留。
- 课程和圈子只在明确学习或寻找社群意图时进入候选。普通知识问题仍可显示经现有搜索权限筛选的古籍、文章、内容卡。
- 后续收紧为课程、圈子分别判定：只找课程不混入圈子，只找圈子不混入课程；同时提出两类需求时可以一起返回。
- AI 搜索弹层增大快捷提问、卡片标题/摘要/价格说明和底部说明字号，长标题不再强制截成一行，键盘聚焦时显示轮廓；沿用既有品牌色与封面/类型徽标，不新增动画或素材。
- 不改搜索的发布/权限筛选、价格判断、付费与交易逻辑；当前实现为关键词规则，不能替代真实内容相关性评估。

## 实际验证

首轮新运行 `content-guide.service.spec.ts`、`ai-search.controller.spec.ts` 与 `search.controller.spec.ts`：3 套件、28 项通过；随后在课程/圈子分流改动后重跑：3 套件、29 项通过。服务端变更文件 ESLint 0 error、29 条 `any` 类型警告（既有代码与测试写法）；移动端弹层 ESLint 0 error、64 条现有模板格式警告；弹层 SFC 解析及 scoped style 编译通过；`git diff --check` 通过。测试使用模拟搜索结果和模拟 AI 网关，属于单元级模拟验证；样式编译仅证明语法可用，不代表真实模型、真实库或端上视觉效果。既往分支的测试不计入本轮新运行。

## 主线接收与后续验收

先评审本候选与 `ca1df63da`、阅读层级候选、额度可靠性候选的依赖及合入顺序。合入前，在隔离测试库以“你好”“你好，论语是什么”“论语中的仁是什么意思”“八字如何入门”“推荐论语课程”“推荐论语圈子”“退款进度怎么查”分别调用 `/search/ai` 和 `/search/guide`；通过标准为短答不返回卡、服务问题无卡、知识问题无课程/圈子、课程与圈子分别按意图返回真实且可访问的资源。保存脱敏请求响应、构建 SHA 与卡片目标页核对至 `artifacts/ai-service-acceptance/evals/`。

Android、H5、微信小程序的字号、折叠、封面和点击效果仍须使用版本匹配的构建独立验收；本轮没有这类证据。真实模型回答准确性、真实内容相关性、用户指标亦未覆盖。若误杀有效推荐，可单独撤回本提交，恢复原导览规则；不得把此候选视为已具备发布条件。
