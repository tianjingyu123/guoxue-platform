# 预发布基础设施现场验收

更新时间：2026-08-01（腾讯云现场记录时间为北京时间）
适用环境：`pre-api.rebugx.cn` 及北京地域新购资源
执行仓库：`D:\gx-deploy-91`

> 本文只记录资源状态与验收结论，不记录密码、Secret、私钥、完整连接串或证书正文。旧生产域名 `api.rebugx.cn` 与旧系统不在本次变更范围内。

## 已通过

- 两台 CVM 均运行应用容器，业务容器健康且无重启记录。
- CLB 公网入口可用，`https://pre-api.rebugx.cn/h5/`、管理后台和 API 均返回 HTTP 200。
- `/api/v1/health` 完整健康检查返回 `status=ok`，PostgreSQL、Redis、COS 和 AI 依赖均为 `ok`。
- PostgreSQL 为北京地域高可用实例，主备位于不同可用区；自动数据备份和日志备份已启用。
- Redis 为私网访问实例，已启用自动备份与销毁保护。
- 生产 COS Bucket 为私有读写；CDN 域名已启用并完成 CNAME。
- Prometheus、Grafana、Alertmanager、Loki、Tempo 与各 exporter 运行正常。
- 企业微信告警已完成真实发送和接收闭环。
- 监控端口已全部收口到 `127.0.0.1`，安全组未向公网开放监控端口。
- 已纠正远端 Prometheus 与业务容器网络不一致、旧 `/metrics` 路径无效的问题；13 个采集/拨测目标现场复核全部为 `up`，业务指标与两张公网证书到期时序均已产生。
- 已将公网证书告警扩展为互斥的 60/30/14 天三级提醒；当前口径分别为续期与替换准备、实际执行、人工签发与部署兜底。部署脚本已修复 bind mount 原子替换后进程仍读取旧 inode 的缺陷，只重建 Prometheus 与 Blackbox 容器，现场确认三条规则均已加载。
- 监控配置更新前的回退副本保存在服务器 `/opt/guoxue/backups/monitoring-config-20260801T081820Z`；更新过程中未重启业务容器。
- 已按节点 A→节点 B 顺序滚动更新 CLB 回源 Nginx 配置；公网 H5 现具备 HSTS、`X-Frame-Options`、`X-Content-Type-Options`、`Referrer-Policy` 与 `Permissions-Policy`，且 HTML 继续保持 `no-cache`。两台 Nginx 与业务容器均为 `healthy`、重启计数为 0。
- 最终 Nginx 网络修正前的回退副本分别保存在两台服务器 `/opt/guoxue/backups/nginx-clb-config-20260801T083515Z` 与 `/opt/guoxue/backups/nginx-clb-config-20260801T083600Z`；更早的首次安全头更新副本仍保留。
- 两台 CVM 的腾讯云 `barad_agent`、`sgagent` 与自动化助手代理均在运行，主机级基础指标不依赖单机 Prometheus；腾讯云告警策略是否绑定两台实例仍需在云监控控制台确认。
- 已生成 PostgreSQL 18.4 自定义格式演练备份，清单、SHA-256 和 `pg_restore --list` 校验全部通过。
- 已在无网络、无端口映射的临时 PostgreSQL 18.4 容器中完成隔离恢复：344 张业务表、82 条已完成 Prisma 迁移、0 条失败迁移；演练容器已清理。
- 两台 CVM 均完成 20 并发、60 秒节点内只读混合容量测试：分别处理 3421/3391 次请求，失败率均为 0；P95 为 3.74/4.36ms，P99 为 5.66/6.61ms；应用容器 CPU 峰值为 36.83%/34.38%，内存峰值为 7.42%/7.20%。
- 已完成 CLB 双向单节点故障切换演练：节点 A、节点 B 分别停止 Nginx 90 秒，均由另一节点独立承接 `H5`、`/api/v1/health/ready` 和 Socket.IO 轮询；每轮完成 82 个周期、246 次公网请求，失败均为 0。每个节点恢复后又连续观察 45 秒、123 次请求，失败均为 0。
- 演练时为被摘除节点设置 4 分钟自动恢复保险，人工恢复成功后已取消定时器；演练结束两台 Nginx 与业务容器均为 `healthy`、重启计数为 0，关键错误日志计数为 0。Prometheus 13/13 个目标全部为 `up`，没有遗留平台不可达告警。
- CLB 连续探测脚本已固化为 `scripts/operations/probe-clb-failover.sh`，默认检查 H5、就绪 API 和 Socket.IO，可用于后续维护窗口复演。
- 本地严格代码门禁 `pnpm release:gate:code` 通过，P0/P1 均为 0。
- 2026-08-01 03:23（UTC-07:00）完整执行 `pnpm release:verify:local`，678.6 秒退出 `0`；全仓 Lint、类型检查、全部测试及 Server、Admin、H5、微信小程序、App、Harmony 六端生产构建均通过。
- 从当前外部网络复核两台 CVM 公网端口：仅受控 SSH 22 可达；80/443、应用 3000、PostgreSQL 5432、Redis 6379 及监控端口均不可达，未发现绕过 CLB 的公网服务暴露。

## 当前阻断

- 当前服务器运行的是预部署临时目录，不是 `/opt/guoxue/releases/<release-id>` 与 `/opt/guoxue/current` 标准固定发布结构。
- 存活响应尚未携带正式 `releaseId`，不能生成最终运行实例身份证据。
- 工作树存在大量未提交改动，不能按生产标准生成干净固定包。
- 支付、退款、回调与对账尚未形成至少一条真实闭环。
- 客户端正式签名包和应用商店提审按计划暂缓。
- 客户端 `manifest.json`、正式环境模板和 Compose 透传项已统一切换到受控商店基线“热卜星火”；2026-08-01 05:23（UTC-07:00）重新构建微信小程序成功，成品 `project.config.json` 的 AppID 与商店基线一致，且成品中未检出旧小程序 AppID。正式服务器仍须由管理员填入真实 AppSecret、商户号及支付密钥，并在微信公众平台、微信支付/汇付后台完成同一 AppID 的绑定闭环；代码门禁会阻断旧 AppID、别名冲突和支付错绑，但不能替代外部平台授权。
- 第三方旧系统真实数据、最终差量和停写方案尚未交接，不能执行正式数据迁移。

## 待演练或待确认

- PostgreSQL 数据备份和日志备份当前保留 7 天；隔离恢复能力已验证，仍建议上线前评审是否提升为 30 天。
- CLB 入口低负载正常；20 并发单出口压测触发应用“单 IP 每分钟 300 次”全局限流，节点容量本身已通过。当前 10 Mbps 上限仍需用真实外部多源流量或云监控指标核定，不能用同地域 CVM 回环公网地址的结果代替。
- 腾讯云控制台现场复核：CLB 状态正常、支持获取客户端 IP、删除保护已开启、配置修改保护未开启；当前为共享型、按流量计费、10 Mbps。WAF 与七层访问日志均未开启，后者会产生独立 CLS 费用，未擅自启用。
- 已完成共享 NAT 限流代码加固：全局匿名 IP 基础限额调整为 1200 次/分钟，Nginx 仍保留每 IP 60 次/秒和连接数保护；严格接口继续保持 10 次/分钟，但登录用户按可信用户 ID、匿名登录/注册按脱敏账号标识独立计数，无法识别账号时才回退真实客户端 IP。手机号、邮箱或用户名不会以明文进入 Redis key。该变更已纳入运行时门禁和单元测试，尚未随正式固定发布包部署到预发布节点。
- Redis 当前约使用 7.5 MiB/8 GiB、1522 个键中 1517 个带 TTL、驱逐速率为 0；5 分钟窗口约为每秒 2.25 次命中、3.50 次未命中，`RedisCacheHitRateLow` 为预发布低流量下的真实信号。暂不修改阈值或缓存代码，需在缓存预热和接近真实流量的观测窗口复核。
- CLB 与 CDN 公网实际证书均于 2026-10-28 到期；该日期只属于上一套预发布资源。续期/重新申请、DNS 验证和替换仍待证书管理员执行，详细规则见 `docs/operations/长期证书与CDN安全配置决策清单-20260801.md`。新正式域名必须重新签发并验收，不得继承本条结论。
- CDN 已确认未启用防盗链、IP 黑白名单、IP 限频、Token 鉴权、UA 规则或区域访问控制；因 APP/小程序无 Referer、共享 NAT 和签名 URL 兼容风险，未擅自开启。COS 生命周期、SSE-COS、CDN 费用告警及硬封顶阈值待业务确认。
- CLB 七层访问日志、WAF 等可能产生费用的功能待成本确认。
- APP/小程序备案、隐私清单、生成式 AI 应用登记和生成内容隐式标识待合规复核。

## 上线判定

当前环境可继续进行预发布联调和演练，但不能标记为正式上线。备份恢复、单节点故障切换和节点容量测试已通过；支付、正式数据迁移、固定发布包、运行版本身份、合规复核和最终双人签核全部通过后，方可生成最终 `GO` 结论。
