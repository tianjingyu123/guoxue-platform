# AI 追问试用额度并发门禁隔离补丁（2026-09-26）

## 状态

隔离工作树 `D:/gx-deploy-91/.worktrees/ai-guide-intent-20260922`、分支 `codex/circle-knowledge-universe-3d-20260923`。主线 `release/integration-20260801` 截至本轮核对尚未接收该分支上一批交接提交 `d277e57c9`；主线工作区有大量其他未提交改动，本轮未触碰。未连接平台数据库、未请求真实模型、未扣币、未合入或发布。

## 缺口与候选

`BotService.consumeQuota` 原先读取 `freeUsed` 后用无条件 `update` 递增。两个并发请求都读到“还剩最后一次试用”时，均可能消耗免费次数并越过 `freeUses`。候选改为 `updateMany` 条件更新：仅当数据库中的 `freeUsed < freeUses` 时原子递增；若未抢到试用，再按原有 `paidRemaining > 0` 条件尝试已购次数，仍无余量则保持原购买/会员引导。`purchaseUses` 扣币与发包事务未改。既有超额历史数据不自动修正。

## 本轮新运行证据

- `pnpm --filter @guoxue/server test -- --runInBand bot.service.spec.ts bot.controller.spec.ts`：实际匹配 2 套件、55 项通过。新增用例覆盖条件更新、并发抢先耗尽试用时转已购次数或明确拒绝；其余用例为本轮同命令重跑的既有测试。
- `pnpm --filter @guoxue/server exec eslint src/modules/bot/bot.service.ts src/modules/bot/bot.service.spec.ts --quiet`：0 error；`git diff --check` 通过。
- 测试使用模拟 Prisma，没有并发真实数据库压力、模型失败退额或流式中断验收；不能把单测写成计费闭环已完成。

## 主线验收与撤回

主线评审后，在隔离 PostgreSQL 和测试账号上对同一用户/智能体的最后一次试用发起并发请求。通过标准：`freeUsed` 不超过配置 `freeUses`；只有一个请求计为试用，其余仅在已购余量存在时原子扣减一次，否则明确拒绝；没有真实余额变化。保存脱敏用户/智能体 ID、请求号、事务前后额度与源码 SHA 至 `artifacts/ai-service-acceptance/reliability/`。进一步单独审计模型失败、客户端断开后的退额和请求幂等；本补丁没有解决这些问题。

若主线不接收，撤回 `apps/server/src/modules/bot/bot.service.ts` 与对应单测的本次差异即可；已上线回滚须按发布流程处理，不手工改真实额度。
