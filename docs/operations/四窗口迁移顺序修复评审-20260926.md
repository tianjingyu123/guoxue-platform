# 四窗口候选迁移顺序修复评审（2026-09-26）

状态：**隔离修复候选；禁止直接生产迁移或部署**。工作树 `D:/gx-deploy-91/.worktrees/four-window-migration-order-20260926`，起点为四窗口组合评审提交 `66e395efdc579807bc40e07e43b5c103e12bf7fb`。根工作区、原四候选和正式接收线未改动。

## 现状和失败证据

两台北京新节点通过已审核 TAT 只读脚本查询，均指向 `172.21.0.16:5432` 同一目标库。`ins-0sen8yvf` 完整账本查询 `inv-d95h3g0pm5` / `invt-d95h3g0pm6`：组合源码有 120 个迁移目录，目标库已完成 103 个，待执行 17 个，已执行但源码缺失 0 个。另一节点的目标库身份及抽查结构在 `inv-e95h2901te` / `invt-e95h2901tf` 一致。原始输出留在本机受限 TAT 目录，不存仓库、不输出凭据。

增强后的同一只读脚本再次查询 `inv-e95h9mgevh` / `invt-e95h9mgevi`：目标库 `PaipanReportKnowledge`、`VoiceAgentProfile`、`VoiceSession`、`VoiceDevice` 四张依赖基础表**均不存在**。迁移账本总行数为 104，其中 103 条已完成，另有一条旧修复迁移标记为已回滚；前述 17 条待执行计数按“已完成且未回滚”计算。由此不再依赖本地基线推断目标缺表，原目录排序在目标环境存在相同建表前置缺口。

按原目录名排序，在 `full-baseline.sql` 构建的独立 PostgreSQL 16 库执行待迁移 SQL，前三条通过；第四条 `manual_add_knowledge_debate` 因 `PaipanReportKnowledge` 不存在失败。该表由排序更靠后的 `manual_add_xiaobu_ai_assets` 创建。继续测试还发现 `manual_add_owner_services` 在 `VoiceAgentProfile` 创建前失败；设备终端与固件迁移也必须排在创建 `VoiceDevice` 的 provider boundary 迁移之后。**这意味着当前组合源码的默认 `prisma migrate deploy` 目录顺序不能用于目标库。**

## 隔离修复

将八个在目标库账本中尚未执行的迁移目录重命名为 `manual_z_20260926_01` 至 `08`，依次为 knowledge debate、knowledge source、owner services、voice provider boundary、voice device terminal identity、voice firmware release、voice session idle、voice trial usage。每个 `migration.sql` 与 `rollback.sql` 均为 Git 100% 内容相同的移动；`manual_add_xiaobu_ai_assets` 及其他九条迁移未改。排序保障先建 `PaipanReportKnowledge`、`VoiceAgentProfile`、`VoiceSession`，再扩展这些表；先由 provider boundary 建 `VoiceDevice`，再扩展设备与固件结构。

本地另建干净隔离库，从仓库 `full-baseline.sql` 起步，按**重命名后目录字典序**逐条执行目标库账本缺失的 17 条 SQL，17/17 通过。`node --test tests/release/four-window-migration-order.test.mjs` 为 2/2 通过。日志在 `%TEMP%/rebu-four-ordered-20260926-*`；隔离库不含目标真实数据。这验证的是 SQL 结构和顺序，**不是 Prisma 迁移引擎在目标库的正式预演**，不涵盖大表建索引锁时间、历史数据约束或两节点切换。

## 发布前门禁和回退

1. 此修复仅对**原八个迁移名均未执行**的数据库安全。当前目标库已验证如此；其他开发、预发布或共享环境账本尚未逐一核查。任一环境若已记录旧名，**不得直接部署这些新目录**，需单独设计迁移账本兼容方案，不能删除或改写已执行迁移。
2. 以目标库一致性快照做完整 17 条迁移预演，核对待执行清单、数据量、锁时间、唯一约束和回退可行性。`Order` 唯一索引使用非并发建索引；生产窗口需要明确写入影响。备份及两节点固定包哈希先留证，再安排迁移与应用版本匹配发布。普通容器启动默认跳过迁移；显式 `migrate deploy` 会处理全部待执行项，不是只执行建单迁移。
3. 迁移一旦写入生产账本，回退应用优先保留新增可空列/表并切回旧兼容代码；不得通过 Git 反向提交假装数据库已恢复。涉及 `ALTER TYPE ADD VALUE` 的枚举变更尤其不以简单降级 SQL 处理。

本轮未执行生产迁移、未部署、未修改订单/余额、未触发通知。该分支只是对四窗口组合的**迁移顺序修复候选**，不能代替真机、支付、硬件与内容权限的上线验收。
