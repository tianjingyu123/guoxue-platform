# 四窗口上线接续总账（2026-09-23）

本记录是接收协调索引，不能代替各候选的原始交付文档、代码评审、真实环境验收或发布授权。用户指定本任务接续“项目功能优化推进1”“项目功能优化2”“接续小卜机器人硬件与语音联调”“接续 Claude 排盘算法与诗词出处任务”。先逐项保全与评审，不把四个分支或根工作区整体合并。

## 本次只读基线

| 来源任务 | 工作树 / 分支 | 2026-09-23 核对 HEAD | 相对 `receive/integrated-three-20260919` 的共同祖先及后续提交 | 本次工作树状态 |
| --- | --- | --- | --- | --- |
| 项目功能优化推进1：商城与课堂 | `D:/gx-deploy-91/.worktrees/home-feed-card-refresh-20260922` / `codex/home-feed-card-refresh-20260922` | `84c082ef4c53e70b3b8953176af6b2109723108b` | `45baf0256a08fa1107d0c14b1a026ad4c762b041` / 58 | 干净 |
| 项目功能优化2：AI 推荐与圈子知识星图 | `D:/gx-deploy-91/.worktrees/ai-guide-intent-20260922` / `codex/circle-knowledge-universe-3d-20260923` | `d277e57c95b45a9ff7eb496978bf484a8976c333` | 同上 / 70 | 干净 |
| 小卜硬件与语音 | `C:/Users/Administrator/.codex/worktrees/11f4/gx-deploy-91` / `codex/xiaobu-hardware-handoff-20260922` | `76f40eaf95a632ad885ef64c5f0e78013f4d8544` | 同上 / 64 | 存在未跟踪 `apps/server/backups/`，保留原现场 |
| 排盘算法、诗词出处及报告链路 | `C:/Users/Administrator/.codex/worktrees/b147/gx-deploy-91` / `codex/paipan-poetry-continuation-20260922` | `fce9e70efc6639337e4b21c634186e0995076ca7` | 同上 / 91 | 干净 |

本窗口原隔离分支为 `codex/integrated-live-gift-paipan-pay-20260920`，已知接收基点为 `c0035a8f4e5c9e9bb26a0c8f0e99060a714818f2`；根工作区 `D:/gx-deploy-91` 仍为旧 `release/integration-20260801 @ 2e458597a80076ba2728e9ab2a3156bf78a3d0e3`，有数百项其他任务未提交内容。当前任务本身是主线协调任务，不再等待另一个“主线窗口”答复，但最终正式发布分支/包仍须重新确认。原云端写入任务“修复微信点击支付白屏”已明确回复：无未完成或状态不明的 TAT 命令，已交接北京两节点后续操作权；本任务仍须在首次执行前核对目标和既有记录，维持唯一写入者。

四候选与本窗口分支相对各自共同祖先的改动文件交集：商城 11、小卜 12、AI 17、排盘 17；小卜与 AI 交集 490、AI 与排盘交集 661。大量交集来自共享前置提交，不等于每个文件都发生真实内容冲突，但足以排除整支覆盖式合并。逐提交核对等效改动及依赖链，复用已验证证据，新增影响才重跑对应测试。

商城候选的商品建单新结构位于 `apps/server/prisma/migrations/manual_add_order_client_request_id/migration.sql`，向 `Order` 新增 `clientRequestId`、`requestFingerprint` 和 `(userId, clientRequestId)` 唯一索引；本窗口已知接收基点的 `Order` 尚无这两列。必须先查询目标库迁移记录与实际列/索引，不能仅据候选源码或本地建库判定线上具备结构，也不能在此任务自动执行生产迁移。服务端依赖提交 `0e3de14a`、事务锁修复 `8d28d1b85`，客户端请求键依赖 `2471428d`、`deadd58d`，必须作为有序接收单元评审。

## 任务和发布边界

| 批次 | 当前已有证据 | 本任务接续动作 | 发布判定与负责人 |
| --- | --- | --- | --- |
| 商城/课堂 | 候选 `84c082ef4`，本地类型检查、构建、合成和独立数据库 HTTP 测试；交接见 `D:/gx-deploy-91/.worktrees/home-feed-card-refresh-20260922/docs/progress/商城与课堂全链路体验审计及候选优化-20260922.md`。 | 优先评审建单迁移与服务端事务锁，再评审客户端幂等键；售后权限、退款缓存与凭证图片按依赖接收。按受控订单验证支付后课程权益、退款和上传访问权限。 | 真实支付/退款渠道、图片权限、目标设备均未覆盖；发布接收任务及商城 QA。不得把构建通过当在线验收。 |
| 排盘/报告 | 候选 `fce9e70ef`，本地全量类型检查、服务端/管理端及 H5/微信/App/Harmony 构建，排盘/报告定向回归；交接见 `C:/Users/Administrator/.codex/worktrees/b147/gx-deploy-91/docs/progress/排盘链路收尾验收矩阵-20260923.md`。 | 核对与商城支付页面、AI 助理的重叠文件；在隔离 PostgreSQL 与受控账号验证首购、续费、回调延迟、报告交付/撤回并发和旧链接恢复。 | 真实支付、真机多端、模型结果尚未覆盖；排盘 QA/发布接收任务。语料只开放已逐句核过的 8 条，不将 1818 条归一作品直接上线。 |
| 小卜 | 候选 `76f40eaf9`，实体设备 Mock、专用 PostgreSQL 与 Redis DB 15 回归 23 套 193 项，服务端/后台类型检查；交接见 `C:/Users/Administrator/.codex/worktrees/11f4/gx-deploy-91/docs/progress/小卜主线窗口交接提示词-20260923.md`。 | 先审 32 项前置提交与旧版未跟踪文件冲突；隔离接收后验 Redis 双节点重连、OTA `wss`/固件 `https`、告警回执及回退，安全条件下验真机余额不足提示。 | 3989 局域网 Mock 服务及 `apps/server/backups/` 保持原状；商业 API 年约、真实供应商语音及费用未验。硬件/语音负责人和发布接收任务。 |
| AI 推荐/知识星图 | 候选 `d277e57c9`，多项模拟/隔离测试及本地图谱浏览器演示；交接见 `D:/gx-deploy-91/.worktrees/ai-guide-intent-20260922/docs/operations/本窗口AI推荐与知识星图隔离交接-20260923.md`。 | 先审目录空表迁移、公开权限、撤权缓存、审核/撤回；只读核对受控内容库后评估覆盖率、推荐准确率、负载，获授权测试知识才做多端星图验收。 | 真实库、正式包、Android/微信/H5 图谱与真实模型未覆盖；不自动迁移/回填/公开圈内知识。AI/内容负责人及发布接收任务。 |

## 执行顺序和回退点

1. 确认主线实际发布目标、单一云端写入者、根工作区未提交内容归属。四候选保持原工作树；接收一律另建隔离分支，不在根工作区覆盖、stash 或清理。
2. 先审资金与权限：商城建单/退款/课程权益、排盘付费报告。数据库迁移先核对目标 schema；生产迁移与真实订单操作不由本清单自动授权。
3. 将小卜独立为可关闭的硬件/语音批，将 AI 目录与星图独立为默认不公开的内容批。它们不能因商城或排盘验收而被顺带启用。
4. 每个最小批次单独提交、记录源码 SHA、schema/Prisma 客户端、构建包哈希、定向测试与回退提交；冲突逐文件人工合并。发布后按相反顺序撤回应用代码，迁移如不可逆须先有兼容回退方案。
5. 环境验收按“源码候选 → 隔离 HTTP/数据库 → 预发布两节点 → 匹配构建多端 → 受控资金/设备 → 放量”留证据。未经过的层级保持“尚未覆盖”，不能由较低层级推断通过。

截至本记录，四批都是**隔离候选**；未取得任何一批已进入当前公开入口的证据，线上状态需按实际发布包核对。本任务不自动部署、生产迁移、上传审核、真实支付/退款、商业 API 开通或真实圈子知识公开。

## 已开始的隔离接收：商品建单重试键

新建 `D:/gx-deploy-91/.worktrees/four-window-integration-20260923` / `codex/four-window-integration-20260923`，从本窗口圈子集成版本 `35a69392d05b0f7ad67bd00c6a5de58bff95e49c` 起步，不改根工作区与四候选工作树。逐项接收并保留撤回点：

| 原候选 | 隔离接收提交 | 处理 |
| --- | --- | --- |
| `0e3de14a2` | `b7697b82e` | 源码自动合并；候选交付文档在此基线缺失，保留对应候选文档版本。 |
| `8d28d1b85` | `9abd2179a` | `shop-coupon` 锁查询冲突按候选可反序列化整数结果合并；交付文档维持此前版本，避免把未接收功能误写成已接收。 |
| `2471428df` | `dfe7698aa` | 结算页人工合并：保留既有 APP/H5 支付选路，同时加入持久重试键与查看订单入口；同商品重复行改按行号缓存。 |
| `deadd58d5` | `45c4c5e1e` | 保留用户资料晚到时的重试键；交付文档冲突继续保持实际接收口径。 |
| 接收线适配 | `37489a704` | 新增用例使用已存在的实物收货地址校验，并在隔离库并发测试中建立/删除合成地址。 |

本轮新验证：隔离工作树本地安装依赖，Prisma 客户端生成于该工作树 `node_modules`；服务端 `tsc --noEmit`、移动端 `vue-tsc --noEmit`、H5 与微信小程序构建通过，微信产物 615 个 WXML 与 2684 个文本检查通过。重试键脚本 7 项通过；独立 PostgreSQL 16、仅监听 `127.0.0.1:55440`、独立新库 `rebu_candidate_test`，8 项真实数据库并发场景通过，测试进程结束后服务器已停止。服务端 `shop-order.service.spec.ts` + `shop-coupon.service.spec.ts` 共 120 项中 119 通过，唯一失败的白标贺卡期望在接收前基线也可复现，属既有问题，不归因本批。微信开发者工具 CLI 对此新包的预览调用停在 `Preview` 阶段超过两分钟，已终止本次命令，不报预览成功；最终包量仍需在匹配工具环境复核。构建用的是无效占位公开域名，不可作为部署配置。

上述仅证明**隔离接收及本地技术验证**。目标两节点数据库是否已有新列/索引、迁移升级路径在目标库是否可行、实际支付/退款/库存与移动端重进仍未验收；此批不可单独发布。撤回按 `37489a704` → `45c4c5e1e` → `dfe7698aa` → `9abd2179a` → `b7697b82e` 逆序评审，若迁移已执行则先保证旧版对新增可空列兼容，不用 Git 回退代替数据回退。

## 目标两节点只读复核与第二批隔离接收（2026-09-23）

外部系统原写入任务已书面交接后，本任务通过已验证的官方 TAT 脚本逐节点执行只读命令。`ins-0sen8yvf`、`ins-n0sqc627` 的 `guoxue-server` 均为 running/healthy、重启 0 次，`/api/v1/health/ready` 的数据库与 Redis 均为 ok；两节点 H5 `index.html` SHA256 同为 `b765d4b75e3b46dc5d89be91d29b0a094737d2c2371d44f8208da4b0a2a23dda`。节点镜像 SHA256 分别为 `5258150c8878fc5fc95afb68bb2b4f6e68c400670b6de96556ed2826838bf484` 与 `e47d5a8c92151d28cd493ea269a066e32038ea3560855a67b15afc26d43d0eb7`，**并不相同**；服务端入口 `apps/server/dist/main.js` SHA256 均为 `d1cb931eec5e2e51358eba00a954aac01355981d6c39b766e6d1a29533c821be`。入口文件一致不证明整个运行包或 Prisma 客户端一致，正式发布前需补全包内容清单、版本来源和固定包发布记录。

两个运行容器均经自身 `DATABASE_URL`（不输出凭据）连接至 `172.21.0.16:5432/guoxue`、`public`；只读查询实际 `Order` 表显示 `clientRequestId`、`requestFingerprint` **均不存在**，相关唯一索引也不存在。故本批建单重试键在目标环境缺少必要结构，不能直接部署或试单。需要先审阅 `manual_add_order_client_request_id` 增量迁移、备份与旧版兼容性，按固定包流程安排迁移和两节点一致发布；本轮未执行任何生产迁移。TAT 证据：健康 `inv-c91t7r0j0w` / `inv-e91t95gdad`，数据库 `inv-e91tcv0rqx` / `inv-e91tdb0jgi`，服务端入口 `inv-e91te00qhf` / `inv-e91teh0cs1`；原始输出保存在本机受限 TAT 结果目录，不存入仓库。审计脚本在 `scripts/ops/target-*-readonly-20260923.sh`，执行时逐个校验 SHA256。

两个镜像 SHA 与 `docs/progress/支付与第三方排盘收口-20260920.md` 记录的 `4e11bd062bdad480b9eb9db0710caf67d53938dd` 发布后镜像**逐字一致**，可将它作为当前服务端已知发布来源；本次只读复查未发现服务端镜像换版。该记录解释了两节点镜像 SHA 不同是当时固定包流程的实际结果，但不能单凭镜像/入口文件证明整个产物一致或本隔离接收分支已上线。该批当时未迁移数据库，和今日缺列观测一致。

商城/课堂后续按功能独立接收：`7cf39f3d4` → `6d19812f3`（课程续购取最新已付款订单），`d946bf78d` → `5ad87db66`（售后取消 CAS 和本人权限 HTTP），`c4d00108a` → `db656e0bc`（退款真实状态与按订单筛选），`570dc6b6e` → `8976ec07e`（订单详情跨用户缓存权限测试和退款后列表缓存失效）。`c4d00108a` 的浏览器测试服务在原接收线不存在，作为新测试文件接入；两份候选交付文档冲突时维持本接收线原文，避免将整个候选误记为已接收。`570dc6b6e` 的订单 HTTP 测试原文件在本接收线不存在，按新文件接入。两项 HTTP 测试均按本接收线 `ShopController` 的 `WechatService` 注入和实物收货地址要求做**仅测试适配**，没有改变生产逻辑。

本轮新增验证：同一隔离 PostgreSQL 16 库在 `127.0.0.1:55439` 下，课程续购 HTTP 1/1、售后本人权限 HTTP 2/2、商品建单与跨用户缓存 HTTP 3/3 通过；`shop-refund.service.spec.ts` 41/41、`shop-coupon.service.spec.ts` 52/52、退款状态合成脚本 7/7 通过；服务端和移动端 TypeScript 检查通过。独立 PostgreSQL 已停止。未进行真实支付、退款回调、Redis 缓存服务联动或真机验收；旧版 `ShopController` 依赖差异暴露的测试适配不得宣称候选直接无冲突。上述四批源码均仍只在隔离接收分支，回退以接收提交的**逆序**逐批进行，数据库迁移单独按兼容方案处理。

课程浏览与权限随后按依赖继续接收：`56526da8d` → `40e87128a`（续购课程列表去重），`1adcba1ad` → `1bb16a1ac`（课程详情附加内容独立加载、首页新课使用真实 newest 接口），`7e68e3ab5` → `8138d066b`（权限查询三态与支付后回源核验），`9a741e29b` → `4e62a7b8f`（免费课可学习与已收录状态分离）。`1adcba1ad` 的课程首页冲突按真实 newest 与分类各自降级合并；交付文档冲突沿用接收线原文，实际批次以本总账为准。隔离库课程续购 HTTP 随列表修复扩展为 2/2；课程服务/控制器 Jest 93/93、移动端权限与收录合成脚本 9/9、续购列表合成脚本 3/3，服务端与移动端 TypeScript 检查通过。隔离 PostgreSQL 已再次停止。未实测课程付款回跳、审核数据、真机播放器与跨端恢复；不能因此宣称课程全链上线。

早期静态评审曾把 `26ea5751b` 的支付异常页误判为本线已有等效回源核对。随后运行旧失败深链的浏览器测试时发现本线页面仍显示静态“支付超时”与 URL 金额，**该等效结论已撤销**；实际修复及证据见下文 `6246f1812`、`644f74623`。排盘候选的报告问答服务入口来自共享前置提交 `a285ad325`，现只在独立评审分支合并；本接收线未因此自动获得报告链路。`git merge-tree` 预检显示排盘候选与本隔离线在支付、圈子、AI 网关、课程等多个文件存在真实冲突，不能采用整支静默合并。

同一目标库的第二次只读探测（TAT `inv-d91twngtj4` / `invt-d91twngtj5`）核对了四窗口代表性新增结构：`ClassicSegment`、`TextDerivedAsset`、`AudioAsset`、`PaipanCaseFeedback`、`ContentCatalog`、`CircleKnowledgeShowcaseNode`、`CircleKnowledgeShowcaseEdge` 七张表在 `public` 均**不存在**，对应五条候选迁移在 `_prisma_migrations` 中也无记录。这只覆盖列出的结构，不能概括为候选所需全部结构已核对；结论是小卜/报告/公开目录/星图均尚无直接部署的目标库前提。本次无写入、无回填、无公开内容变更。小卜商业语音仍未签约，AI 公开目录默认关闭，故两批须独立于商城发布门禁处理。

新增 `17aa8d4e5` → `3073a5a36` 的课程退款后学习进度/完课 HTTP 验证。首次在本接收线上运行发现**真实源码缺陷**：未购用户写进度返回 200；`CourseLearningService.updateProgress` 原先只检查章节存在，不检查当前课程权益。独立修复提交 `d27346b` 在写进度与学习行为前调用 `checkAccess`，无权返回 403。此后同一隔离库 HTTP 3/3、课程购买单测 3/3、服务端类型检查通过；退款后进度仍保留原值且不能再写。此修复依赖本批课程权限代码，应随课程服务端批次评审，不能只取测试。

新增课程详情合成窄屏用例 `536b9ceec` → `e63283ee4`；候选测试 API 脚本已在接收线由退款批次引入，冲突中沿用已有更完整的脚本，仅接收新浏览器测试。独立 `127.0.0.1:3989` 合成 API 与 `127.0.0.1:5178` H5，Chrome headless 拦截全部非本地请求，测试通过：评价 503 时主课程仍可看、免费课收录按钮与学习入口并存、320/375/430px 无横向溢出。320px 截图已目检，脚本进程已停止。新版 H5 与微信小程序构建通过（无效占位公开域名）；微信构建检查 615 个 WXML、2684 个文本文件，主包估算 1.64 MB、最大分包 `pkg-paipan2` 1.94 MB。未上传微信平台，也未用真实 API/真机操作；包量仅为本地工具口径。

## 四候选共享前置与小卜、排盘、AI 的独立合并评审

为避免把跨窗口共用的大型前置提交直接并入上述按批次接收线，从 `245276ce8` 另建 `D:/gx-deploy-91/.worktrees/four-window-precursor-review-20260923` / `codex/four-window-precursor-review-20260923`。此分支用于**交叉冲突评审**，不是正式发布分支；原四工作树及根工作区均未覆盖、stash 或清理。

| 评审单元 | 隔离合并提交 | 冲突处理与状态 |
| --- | --- | --- |
| 共享前置 `a285ad325` | `a19b089e2` | 9 处人工处理；保留原 AI 额度与圈子推荐权限保护。三端类型检查、5 组定向测试 81/81 通过。大提交内含多个功能和迁移，未按发布单元拆分。 |
| 小卜候选 `76f40eaf95a632ad885ef64c5f0e78013f4d8544` | `29567e1ff` | 5 处人工处理；保留语音入口回跳、古籍翻译严格解析、额度失败回退。三端类型检查；古籍/圈子 91/91，小卜协议/固件/运维/上下文等 60 通过、5 跳过。未测试商业语音及真硬件。 |
| 排盘候选 `fce9e70efc6639337e4b21c634186e0995076ca7` | `95559f8ad423b8b24c1cc8c3a51f30f14f2ef5e7` | 8 处人工处理；保留支付前回源查原订单、商城重试键/PG 锁、公众号与 App 已有支付路线、圈子咨询分页。三端类型检查；排盘报告与 Redis 75/75。商品订单与上述测试合跑 153/154，唯一失败为接收前已复现的白标贺卡 `giftCardMeta` 断言；新增 PG 锁断言已按实际安全查询更新。排盘黄金测试耗时过长，本轮中止，不写为通过。 |
| AI 推荐/知识星图候选 `d277e57c95b45a9ff7eb496978bf484a8976c333` | `de4b06607c59e9d8229a917b6554c8f398aec036` | 8 处人工处理；公开内容详情改为实时按已发布/未删除/平台目录筛选，避免旧缓存泄漏撤回内容；保留原圈子分页、会员状态和 AI 消息知识提示。三端类型检查；内容权限、公开目录、星图、推荐/RAG 等 10 组定向测试 100/100 通过。 |

这些合并只证明隔离源码能共同编译与通过列出的定向测试，**不等于四批功能已经正式接收、部署或真机验收**。`Order` 重试列/索引与 `ClassicSegment`、`TextDerivedAsset`、`AudioAsset`、`PaipanCaseFeedback`、`ContentCatalog`、`CircleKnowledgeShowcaseNode/Edge` 在目标库缺失；相关迁移需分别完成升级路径、备份和回退评审后才可安排生产操作。知识目录默认不公开，圈内内容撤权、可见性和审核链路要在匹配目标数据的隔离环境继续验证；小卜商业 API 与硬件真机仍待条件。大型共享前置与 AI/排盘合并提交不能作为一个不可拆分的正式发布回退点；正式发布线还需按功能拆批、固定源码及产物哈希，逐批验证。此评审分支不触发部署、生产迁移、审核提交、真实支付或商业调用。

## 商城/课堂继续分批接收（2026-09-23）

按业务边界在 `codex/four-window-integration-20260923` 继续从“项目功能优化推进1”挑选可独立验证的改动，不整体合并其分支：

| 原候选 | 本线提交 | 隔离验证与剩余缺口 |
| --- | --- | --- |
| `b0cf28492` | `0ec59ec0a` | 售后申诉凭证和分页；一处列表刷新冲突保留旧页面回前台静默刷新，同时接入分页。合成检查 9/9、售后服务单测 53/53、移动端类型检查通过。真实图片访问授权、后台处理与真机分页仍待验。 |
| `db4e2620b` | `d00c7fe48` | 课程进度/证书资格；进度冲突保留本线已在独立库 HTTP 验证的 403 权限检查，接收证书资格收紧与测试。服务端类型检查、课程定向单测 70/70 通过。真实课程章节和退款后的证书撤权仍待端到端核验。 |
| `90c40aeeb` | `37564f889` | 课程评价全站评分与分页；移动端类型检查通过，隔离 H5+合成 API 浏览器测试覆盖第二页、评分失败降级、列表失败重试、320px 无横向溢出，截图 `C:/Users/Administrator/AppData/Local/Temp/rebu-course-reviews-320.png` 已目检。真实评价库、用户权限和真机仍待验。 |
| `6d33c56b4` | `a829c64f6` | 本人评价状态使重复提交入口收起；服务端/移动端类型检查、课程服务端 31/31、同一隔离浏览器测试再次通过（包含已评价态）。真实并发提交与多端状态仍待验。 |

上述 H5 测试只使用本地 `127.0.0.1:3989/5178`，非本地请求均由浏览器脚本阻断；服务已停止。冲突中的候选交付文档沿用本接收线原文，实际接收以本总账和 Git 提交为准。`vite.config.ts` 拒绝生产构建使用 HTTP 本地域名，本批浏览器验证采用开发模式；它不能代替按目标 HTTPS 配置生成的正式包或真机验收。下一阶段还需按独立提交评审课堂播放、问答、讲师发布等剩余增量，不把“本轮可验证批次完成”写为商城课堂全功能清零。

继续接收 `40bc28611` → `1637ffae8`：课堂播放器将权限查询失败与明确无权分开，未知态可重试并以服务端最新结果进入播放。合成 API 脚本冲突保留本线已有售后、评价模拟状态，加入播放器场景；两份旧交付文档冲突仍沿用本线版本。移动端类型检查、`test-course-player-access-browser.cjs` 的隔离 H5 浏览器验证通过（权限未知不误判拒绝，重试后获权）。本地 API/H5 进程已停止；真机视频、有效期边界、支付回跳和正式发布包尚未覆盖。撤回此批先撤 `1637ffae8`，再按上表逆序撤回课程评价及售后/证书批次；数据库结构另行处理。

截至 `3f68aad154213f9d4bda3a7feb5dcee62e6f6fde` 的最新本线，使用明确无效的 HTTPS 占位公开域名完成 H5 与微信小程序构建；小程序完整构建脚本（含共享组件搬移、资源裁剪、WXML 和文本校验）通过：615 个 WXML、2685 个文本文件，主包 1.64 MB、保守上传口径 1.92 MB、最大分包 `pkg-paipan2` 1.94 MB。保守主包余量仅 0.08 MB，下一次源码/资源变化后必须重测；这不是微信开发者工具上传或线上包量验证。构建用的域名不可用于部署，实际目标环境公开配置须另行核对。构建日志在本机临时目录 `rebu-mp-build-20260923.log`，不含凭据。

## 课堂后续页面与支付异常链路接收

在同一隔离接收分支上继续按可撤回单元接收：

| 原候选 → 接收提交 | 本轮新验证 | 发布边界 |
| --- | --- | --- |
| `b97ee537c` → `7367ac072`：讲师公开课程列表 | 服务端/移动端类型检查，课程单测 67/67，隔离 H5 浏览器验证公开课程、空态与错误区分及 320px 布局通过。 | 真实讲师数据与权限未验。 |
| `5d6cbfff7` → `0f25d9d8b`、`09ef43d33` → `652e90ee8`：拼团失败/成功页 | 移动端类型检查；隔离浏览器验证失败页退款时间语义、WAITING/SUCCESS 不误入失败，成功页仅 SUCCESS 展示、WAITING/REFUNDED 拒绝误报；320px 通过。 | 真实拼团支付、渠道退款回调未验。 |
| `f67e4c946` → `3e02b37ee`：证书页 | 移动端类型检查；隔离浏览器验证资格不可用、重试恢复、再失效隐藏旧证书、日期字符串不偏移及 320px。 | 真实结课与退款撤权未验。 |
| `5b5cc46da` → `0886b1b40`、`f43ac3dfa` → `3f152036f`：课程问答、作业批改 | 移动端类型检查；同一隔离 H5 浏览器测试分别验证失败/空态、真实问答字段/分页/关闭态，以及已批改/待批改字段文案；320px 通过。 | 真实提问、批改写入和权限未验。 |
| `94f07403b` → `1061d24eb`：讲师课程发布 | 服务端/移动端类型检查，价格 DTO 6/6；隔离浏览器验证资格接口失败与无资格分离、价格校验、合成创建和 320px。 | 真实讲师资格、审核与发布未验。 |
| `26ea5751b` → `6246f1812`，随后 `86e0bac8a` → `644f74623`：支付结果回源与旧失败深链 | 首次旧深链测试超时暴露缺失前置，已中止未完成挑选并按依赖顺序重新接收。移动端类型检查；隔离浏览器验证服务端金额、查单失败保持未知、服务端确认付款才跳成功页，旧深链进入同一查单页，320px 通过。 | 真实支付渠道、迟到回调、多端返回栈未验；不能因合成查单通过宣称资金闭环。 |

所有浏览器测试使用 `127.0.0.1:3989/5178` 合成数据，非本地请求被脚本拦截；进程已停止。页面源码改动后，上节 `3f68aad15` 的包量数字**不再对应当前 HEAD**，必须在下一轮完整构建重新测量。旧交付文档冲突保持接收线原文；本表与各接收提交是实际版本记录。以上均未部署或触发真实资金、课程发布、生产通知。

随后在 `8fad1a3dd52e81d62754241f19e95c8804759ba7` 重新使用无效 HTTPS 占位公开域名执行正式构建命令：H5 通过；小程序完整构建、共享组件搬移、资源裁剪、615 个 WXML 与 2685 个文本校验均通过。当前主包 1.64 MB，保守上传估算 1.92 MB、余量 0.08 MB；最大分包 `pkg-paipan2` 1.94 MB，分包 33 个。此数字**对应 `8fad1a3dd` 源码和本地工具产物**，仍非微信平台上传口径，不能用占位域名部署。构建日志为本机临时目录 `rebu-h5-build-four-window-final.log`、`rebu-mp-build-four-window-final.log`。截至核对时四来源候选 HEAD 未变化，原小卜工作树的未跟踪 `apps/server/backups/` 仍原样保留；两条隔离工作树均干净。

此前商品订单单测唯一失败的白标贺卡用例已查明为**测试桩缺口**：生产代码从 `BrandConfig` 读取 H5 入口，原 `makeMockPrisma()` 没有 `brandConfig.findUnique`，该调用抛错后按设计使贺卡组装返回 null；并非已证实的真实下单缺陷。`35272860a` 只补本地测试桩的缺省品牌配置，不改生产逻辑；`shop-order.service.spec.ts` 现为 70/70 通过。真实品牌配置与扫码归因仍需匹配目标环境验收。此前本总账将失败记作“基线既有问题”是当时的测试现象，不再将它作为未解释的发布缺陷。
