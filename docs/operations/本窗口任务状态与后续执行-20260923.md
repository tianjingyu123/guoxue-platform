# 本窗口任务状态与后续执行（2026-09-23）

口径：这里的“完成”仅指隔离候选或咨询交付，不等于已合入、部署、正式包验收或具备发布条件。本工作树为 `codex/circle-knowledge-universe-3d-20260923`，截至本记录的候选基线 `187da520b`；本轮新增的知识库防护见其后续提交。以下历史测试结果来自对应交接文档，不算本轮重跑。

| 用户交代的工作 | 当前实际状态 | 未完成及接手方式 |
| --- | --- | --- |
| 全平台 AI 短答、字号、分层与图文展示 | 隔离候选 `d342431c8`；搜索/智玄等导览候选 `ca1df63da`、`7b729d53a`、`bdc83c05b`；详见《全平台AI回答体验优化》《全平台AI服务与内容分发实施方案》《AI导览意图边界交接》 | 真实模型答复质量、真实内容卡片、Android/H5/微信小程序视觉及交互未验收。我继续负责候选代码和受控验证；主线负责评审合入与发布安排。 |
| 全平台内容推荐中心 | `eb88e8a41` 做了准入收紧及规划；本轮修补全局视频搜索、语义候选的公开门禁与向量空间，AI 导览改用新鲜检索，并按明确意图返回短视频或商品卡；早期内容可按 ID 更新向量；新增七类公开内容的只读目录预览、默认未启用的独立目录空表迁移候选与分站安全的增量对账纯函数，但未连真实库、未执行迁移或回填。平台 Content 补了独立阅读页、列表/详情及诗词入口的公开权限底线后在隔离候选恢复搜索与导览；直播/圈帖/工具等类型尚未落地 | 先用经核对的只读数据库运行覆盖率与抽样预览，验证 Content 真正可打开、权限和多端展示，再评审目录迁移/回填并做同样本推荐评估；其他类型普通搜索旧缓存的撤权即时失效也未完成。不能宣称“全平台内容都已进入推荐”。详见《全平台推荐内容目录与圈子知识采集方案》《AI公开召回与语义排序隔离修补》《AI公开内容目录只读预览》。 |
| 圈主助理知识采集 | 已有候选、人工确认、精确哈希及向量相似度机制。本轮补充确认前重新核对精华帖原文、公开问答状态，阻止旧候选重复确认；增加同来源同内容待审核候选去重；单元测试新运行 1 套件 34 项通过 | 尚需来源版本统一记录、语义近重复的人工决策闭环、内容质量抽检、真实数据库验证。尤其旧的已入库内容需单独做权限/来源对账。 |
| 具体知识点 3D 星图 | `f3e83b3d5` 至 `187da520b` 完成独立体验样例、公开审核快照和多端候选入口；详见《圈子知识宇宙3D选型与样例》 | 演示数据不是真实圈子知识；正式包、受控真实内容、Android 真机视觉/性能和微信/H5 验收未完成。主线评审后再决定合入，不自动上线加入页。 |
| 旧平台课程复用旧主体腾讯云点播 | 本轮已只读核实新平台课程/章节以 `mediaUrl` 存播放地址、当前 VOD 播放签名只配置一个子应用；尚无旧账号双来源签名或批量映射。未取得两个账号的已验证访问、课程与媒资对应清单，也未执行上架或跨账号迁移 | 先核对一门受控旧课的媒资与播放规则、虚拟圈主/圈子和章节清单，再做隔离导入。网页登录不等于 API 鉴权，不授权真实上架。详见《旧课程跨主体点播复用代码核查》。 |
| 直播礼物 L1/L2 与 L3 | 按用户此前指令保留 `792c4a339`，隔离交接在 `504a5e3a9`；状态固定为“隔离评估完成，正式运行环境验收未完成” | 正式视频播放与礼物叠层、真实消息链路未验收；L3 暂缓。此项依此前明确停工指令，不自行重启测试、合入、部署或接入 L3。 |

## 本轮新运行与限制

- 新运行：`pnpm --filter @guoxue/server test -- --runInBand circle-knowledge.service.spec.ts`，1 套件 34 项通过，源码为本工作树的未发布候选。`git diff --check` 无差异错误；相关 ESLint 0 error、3 条既有 `any` 警告。测试使用模拟 Prisma/向量/审核服务，非真实内容库、非正式运行环境。
- 此后新运行：搜索、语义搜索、导览 3 套件 34 项通过；具体命令、变更和限制见《AI公开召回与语义排序隔离修补》。该结果同样只是模拟验证。
- 再后新运行：只读覆盖率工具和推荐向量服务 2 套件 5 项通过；无 `--read-only` 参数时脚本按设计拒绝运行，未连正式库或产生真实内容覆盖率结论。
- 此前新运行：视频导览相关服务端 4 套件 47 项通过；移动端 ESLint 0 error、64 条现有模板 warning。当时本地图谱仅 HTTP 200、浏览器控制通道故障；此限制已由下述本轮浏览器实测部分补足，但仍未完成真机验收。
- 合并范围回归又新运行 8 套件 83 项通过；移动端 SFC 编译检查因当前工作树缺 `@vue/compiler-sfc` 未运行成功，不计通过。
- 商品卡加入后新运行相关服务端 4 套件 48 项通过；这是模拟验证，不替代真实商品与移动端检查。
- 公开内容目录预览与覆盖率口径调整后新运行 3 套件 6 项通过；仅 mock 数据。未连接真实库、未建目录表或切流，详见《AI公开内容目录只读预览》。
- 随后公开内容目录空表与分站安全对账候选新运行 12 套件 94 项通过，`prisma validate` 使用占位 URL 通过；只读翻页盘点入口新运行 4 套件 14 项通过，缺少 `--read-only` 时脚本拒绝运行。分别对应候选提交 `eadd0fffb`、`41b2880c1`；均未连接真实数据库、执行迁移或回填。
- 本轮新运行本地图谱浏览器点击、拖动及控制台检查：24 个演示节点，点选具体知识卡成功、视角可拖动，控制台无新 error、仍有重复加载 Three.js warning。仅本地浏览器，不是 Android 真机/微信小程序/正式包；细节见《圈子知识宇宙3D选型与样例》。
- 历史证据：AI 答复体验 45 项、AI 服务与分发 83 项、AI 导览 29 项等见各自文档；本轮未重跑，不能写成本轮通过。
- 圈子问答采集现限制为公开已回答记录，确认时再次核对公开状态；帖子确认时校验当前正文、发布状态和精华状态。同一来源同一内容的待审核候选不重复入队。此防护尚未覆盖所有旧候选类型，也不能替代来源版本化、数据库唯一约束及并发事务控制。

## 后续执行顺序

1. 我继续在隔离工作树完善 AI 内容目录、知识来源与推荐质量门禁；每项附新运行测试和候选提交。未经评审不合入主线。
2. 主线统一安排受控测试内容、匹配构建、Android/H5/微信端与真实模型链路验收；入口和通过标准见上述三份 AI 交接文档。证据放 `artifacts/ai-service-acceptance/`，按模拟、浏览器、真机、微信和正式环境分别标注。
3. 旧 VOD 上架另设只读账号与课程清单核验，再经用户确认上架范围、虚拟圈主与操作窗口。礼物项目维持已约定停工状态，除非用户明确重新启动该项。

这份清单用于防遗漏，不把候选代码、演示图谱或历史测试写成线上完成。
