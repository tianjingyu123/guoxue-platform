# 四窗口之外优化候选遗漏审计（2026-09-26）

## 结论与口径

原《四窗口上线接续总账》只接收用户指定的四个任务来源，**不是全项目已完成优化的总账**。本次在独立工作树 `codex/release-gap-audit-20260926`，以四窗口最新隔离候选 `1bd1ad30808461caa13546bbf2e9f11feac10eaa` 为比较对象，检查仓库分支、代码标志及既有交付记录；未改根工作区、生产环境、订单或资金。`git` 祖先关系只能作为线索：平行分支可能存在 cherry-pick 或等效改动，最终接收前必须逐文件核对。

“代码完成”“隔离组合包含”“正式发布”“用户当前端可见”是四个不同状态。四窗口组合尚未进入正式发布线；本机 Android 247 测试包只覆盖一台设备，微信小程序候选仅预览而未上传审核/发布。2026-09-26 两节点只读核验显示仍运行旧包、连接同一数据库，四窗口所需迁移未执行。故当前软件看不到这些新优化是预期现象，不能以分支提交或本地构建声称上线。

## 已确认遗漏的独立批次

| 批次 | 候选与证据 | 当前四窗口隔离组合核对 | 线上/接收状态与下一步 |
| --- | --- | --- | --- |
| 关键业务通知闭环 | `codex/notification-audit-integration-20260921 @ 03aa7146b`；其中验证收口 `1d26ab18f`，候选本地指定测试 121/121；原记录 `docs/progress/通知闭环审计与候选补齐-20260921.md` | `1d26ab18f` 不在组合祖先链；组合的 `notification` 目录无 `sendOnce`/`ORDER_PAID:<orderId>`/`ORDER_REFUNDED:<orderId>` 标志。**未发现这批核心实现已被等效纳入**，仍需逐文件接收评审 | 代码候选和隔离验证完成，线上未知。按 notification → shop-payment → shop-refund → shop.module 分批评审；事务失败端到端、Redis 跨进程、跨用户详情等缺口仍在。不自动发真实通知 |
| 圈子履约 A / 退款收益自动冲正 B | A `6fbadf16db0130657858d2507b74811288e2d980`；B `codex/circle-fulfillment-manual-resolution-review-20260920 @ c71abd06af2d94fa07568e4f9bc3b02e91518d87`；原交接 `docs/operations/圈子履约B接收交接-20260920/主线交接-20260920.md` | B 非组合祖先；组合中无 `shop/circle-fulfillment.ts`，当前 `circle-refund.service.ts` 仍使用旧收益查询/冲正路径，不能视为 B 已接收 | A、B 应独立评审，重点核对收益归属、部分退款比例、累计上限、并发幂等、迁移升级路径。用户已决定正常退款按比例自动冲正；这不是生产资金操作授权。目标库与真实资金链路未验收 |
| 直播礼物 L1/L2 与小程序体积修复 | 接收线 `receive/live-gift-l1l2-20260920 @ be1edd7a9`，L3 评估记录 `792c4a339`、`504a5e3`；`docs/operations/直播礼物L1L2与体积修复接收-20260920/最终集成补充-20260920.md` | 组合存在礼物和主包修复的等效独立提交 `2c6e6ace0`、`f7bc0d00a`、`9de344854`、`1977c8578`、`47b24617c`；不能重复合并原分支 | 隔离代码/小程序预览有证据，正式视频叠层、真 IM、匹配包真机仍未完成。L3 暂缓；礼物和体积修复应保留两个回退单元。未发布 |
| 圈子整体体验重构 | `docs/design/圈子苹果级体验重构规范-20260922.md`、`docs/progress/圈子上线前统一验收-20260923.md`；入口/写作/加入/管理等提交 `88ecfbca3`、`5d61fb0a2`、`2cf869bc2`、`366e26560` | 上述体验批在组合历史，另有四窗口圈子付款回跳、读屏修复；不是“没有做代码”。此前圈子专项只证明隔离脚本与构建，不能把全域审计或真机验收写为已完成 | 当前端未发布这版。先按同一源码核对圈子入口、阅读、支付、退款、管理权限，再做 App/小程序真实返回周期和受控订单验收；独立于圈子资金 B 接收 |
| 智能体对话视觉、语音入口、推荐与额度 | 苹果级交互 `335ede766`、`36f49df2e`，体验 `7046f9f48`；当前隔离组合另有额度预留、购包幂等及真实本机 PostgreSQL 测试，详见 `docs/operations/四窗口上线发布门禁-20260926.md` | 交互提交在组合历史；额度新增迁移 `_09`/`_10` 在组合，但两节点目标库不存在对应表 | 代码和本地测试不等于已向用户呈现；收费能力发布阻塞，须审迁移、两节点同包、受控余额对账。语音硬件真链路、外部 bridge 仍未验收 |
| 微信小程序手机号快捷登录与支付直达 | 手机登录候选 `cb8b1afd9`；支付直达修复 `947c3b4bf4a2a020aabcf0ed9ec673c9802a5762` | `947c3b4bf` 在组合祖先；登录原提交不在祖先，但当前 `pkg-auth/login/index.vue` 已有 `getPhoneNumber` 快捷登录实现，按等效代码核对而非重复合并 | 小程序候选未发布，手机用户仍看到旧体验。微信 Scheme 直达付款页须用已发布匹配小程序版本及 App 真机支付测试，不能凭源码认定可用 |
| 支付、旧排盘与三候选历史发布 | `receive/integrated-three-20260919` 的统一记录显示 `1bce152f` 对应三候选曾在两节点发布；H5 新域名另有发布记录。Android 支付/旧排盘的后续修复及本地 247 包另见四窗口门禁 | 四窗口组合继承上述历史接收基点，并继续修改付款回跳与排盘分享；不可把过去已发布的 `1bce152f` 说成全部最新功能在线 | 区分历史发布版本与当前旧运行镜像。旧排盘 App 链接分享的线上 H5 承接仍有白名单不匹配；支付宝/微信 App 真实支付闭环尚未取得与最终候选匹配的验收 |

## 尚需扩大清查的来源

仓库还存在后台管理端、反馈客服、结算/商家、赛事、驿站、古籍、小程序视觉与跨端交互等大量历史优化提交；部分位于四窗口共同前置历史，部分是独立平行分支，部分只有文档或未提交现场。本表**不宣称已经穷尽所有窗口**。后续总账应以“用户需求 → 任务/工作树 → 完整提交 → 最终源码是否在位 → 目标数据库/服务端版本 → App/H5/小程序产物版本 → 真机/业务验收 → 上线记录”逐项登记；只看到提交或截图不得标记上线。尤其先复核管理后台两项独立任务及 `codex/admin-experience-polish-20260903`、`codex/admin-unified-20260903` 与当前管理端/线上版本的关系。

## 用户点名事项的逐项复核（2026-09-26 补充）

| 用户要求 | 候选证据 | 当前四窗口源码实况 | 应补动作 |
| --- | --- | --- | --- |
| 圈子整体重构 | `88ecfbca3` 入口分区、`5d61fb0a2` 加入续费、`2cf869bc2` 写作阅读、`366e26560` 管理待办均为当前组合祖先；另有设计规范和 `docs/progress/圈子上线前统一验收-20260923.md` | **代码在隔离组合，但只是多批重构候选**。首轮交付文档明言不是完整圈子终验；后续本地浏览器、构建及部分回归已做，三端真机、资金/权限闭环仍缺。终端未发布这版 | 在正式接收清单中按广场、详情、创作阅读、加入续费、圈主事务、付款退款逐项登记，分别做真机和真实权限/受控交易验收；不可再用“圈子已完成”统称 |
| 课堂卡片整体性及首页卡片间距 | `codex/home-feed-card-refresh-20260922` 含 `764f96556`，改首页课程/商品卡和瀑布流间距；商城课堂审计文档亦明确两端卡间距偏大 | **明确漏接收**：`764f96556` 不是当前组合祖先；首页 `.flow/.col` 当前仍 `18rpx`，该候选为 `12rpx`；课程卡、商品卡、通用卡文件内容均非该候选版本。四窗口只接入商城课堂后续若干服务/页面批，不能宣称这一视觉批已在位 | 以 `764f96556` 为独立视觉批，在当前源码上逐文件人工接收；保留现有商品/课程状态逻辑，做 320/375/430px 和真机截图对比，再按最终源码重做小程序包量与 H5/App 构建 |
| 智能对话机器人 UI 与使用闭环 | `335ede766` 规范、`36f49df2e` 界面、`c8ff530f2` 配套智能体页面及后续 AI 体验提交 | 这些提交在组合历史；当前又有额度预留、购包幂等、公开密钥字段修复等隔离增量。**终端未发布，语音硬件、真实模型、购包对账未完成** | 分“文字/富消息 UI”“语音入口”“推荐导览”“额度耗尽与续费”“真实计费”验收，不以同一构建通过代表全部在线 |
| 排盘算法前后端合并、后移与校正 | `003f404ac` 共享单一算法真源、`e4ab4931d` 服务端计算入口、`c02cf2cf8` 等后续算法修正均在组合历史 | 多个排盘结果页确实调用 `apps/mobile/src/lib/paipan/engine-client.ts` 的 `/paipan/engine/:tool`；服务端控制器存在。但移动端仍导入部分 `@guoxue/shared/paipan/*` 子模块用于节气/干支/罗盘等，**不能未经最终产物检查就宣称所有算法都已从客户端移除**。最终服务端/客户端未发布 | 按工具列算法真源与是否仍打包，在最终小程序/App 产物中查实际模块闭包；用黄金样例比对新旧服务端结果，核对目标 API 版本和限流。新算法与旧排盘过渡开关保持现有发布门禁 |
| 报告生成、购买、阅读、问答与工作台 UI | `3999644e5` 报告权益门禁、`fea4e2255` 问答历史按报告版本隔离等在组合历史；`docs/progress/排盘链路收尾验收矩阵-20260923.md` 记录本地全量构建、关联测试和未覆盖 | 服务端 `generateReport` 与前端 `ai-report.vue` 入口在源码；**只证明候选代码和本地验证，不证明终端可见、真实模型生成成功或跨端付费交付**。矩阵将付款权益、报告并发、多端动线、真实内容样本仍列待验 | 在隔离测试数据上贯通同一排盘记录的首购/续费→生成/失败恢复→阅读提问→工作台交付/撤回；服务端与多端同版发布后再做受控真机验收 |

特别纠正：之前四窗口总账中“商城/课堂代码整合”不能覆盖首页课程卡和缩间距批；后续接收表必须把 `764f96556` 单列，直到代码与视觉验证都完成才改状态。

该视觉批已进一步在独立分支 `codex/home-card-receive-20260926 @ db6eaf2813585e62108ecbb20d4489a06c5e42f9` 无冲突接收；工作树内 Prisma/依赖隔离恢复后，移动端类型检查、H5 与小程序源码构建通过。商品卡首图 1:1→4:3、课程卡统一内容面和首页 18→12rpx 间距均在此**隔离候选**，尚未进入正式发布线，亦无真实商品/课程视觉截图。详见 `docs/operations/首页卡片视觉批隔离接收-20260926.md`（该独立分支）。

对“每个排盘都能生成报告”须使用更窄口径：当前 `GenerateReportButton` 在八字、紫微、七政、合盘结果页有明确工作台入口；大六壬、梅花、奇门、六爻有保存排盘记录后进入统一 AI 报告页的路径，八字亦有小卜 AI 报告入口。`TOOL_REPORT_TYPE` 映射列出更多工具，不等于对应结果页都有可点击报告入口；起名结果页甚至明确注释移除了 AI 报告按钮。因此全工具入口矩阵和每种盘式报告内容质量仍须逐项核对，不可对用户宣称“全部排盘都已完成报告生成”。报告问答服务与前端 `report_dialogue` 语音续接在候选源码，但真实模型、购买与多端终端验收仍缺。

## 接收顺序与门禁

1. 冻结真实发布基线和各端线上版本，保全根工作区及其他窗口未提交现场；以上候选逐批做等效差异核对，避免整支覆盖或重复合并。
2. 优先处理支付/退款资金与权限、通知成功语义、圈子收益归属；每批独立提交与撤回点。后台/交互类在不改变资金规则前提下独立评审。
3. 在最终源码上重新核对 Prisma 客户端、目标库迁移顺序、固定构建包、小程序包量与签名 App；不以旧候选测试替代新组合验证。
4. 取得必要发布决策后按两节点固定包流程部署并核验入口，随后验证真实客户端版本、受控订单/权益、圈子/排盘/通知/直播/语音链路。小程序需要上传审核及正式发布后，普通用户才会看到新版本。

本轮仅做遗漏审计与接收计划修正；未部署、迁移、发通知、提交小程序审核、改真实订单/余额或 DNS。
