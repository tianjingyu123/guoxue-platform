# 四窗口上线发布门禁（2026-09-26）

本表是接续总账的执行视图。根工作区 `release/integration-20260801 @ 2e458597a80076ba2728e9ab2a3156bf78a3d0e3` 仍含其他窗口未提交改动，**不是**本表的功能组合；隔离组合原评审版本为 `5b9dc8739307f511b5ad217bc1daa0d0a2b6b3a6`，其上依次有圈子交互门禁 `f5ec591ba`、智能体会话归属 `d9e3af532`、审计失败退额 `7db928ad0` 与持久额度预留候选 `f0e8407e4`，各有独立回退点。最新隔离分支为 `codex/bot-quota-ledger-20260926 @ f0e8407e4fdb160f76d3d2834b0385cebe16b830`；这些均**不是**正式发布线或线上版本。未经逐批接收、迁移和发布决策，不得用“本地编译通过”代替上线结论。

| 门槛 | 状态 | 当前证据与对应版本 | 剩余动作 / 负责人 |
| --- | --- | --- | --- |
| 四窗口代码整合与回退点 | 已完成（隔离评审） | 组合 `5b9dc8739`；迁移 `83d97cdc5`、AI 并发 `c5fa5d14e`、圈子付款回跳 `c5815a9c1`、旧排盘门禁 `274556b63`、研究院兼容 `9807a2fd6`、通用小程序查询兼容 `5b81f3f9e`、赛事胶囊避让 `ecd27030e`、课程/古籍胶囊避让 `279bc83a4`、语音充值未开放状态 `8f3a274ee`、AI 失败退额 `282367904`、断线验证 `8119c163f` 与排盘分享 `5b9dc8739` 各有独立提交；后续圈子读屏/审计修复 `f5ec591ba` 也为独立回退点 | 主线接收窗口逐批核对当前正式发布线与等效改动，再形成正式发布候选；不得整体覆盖根工作区 |
| 服务端 Prisma 与本地验证 | 已完成（隔离环境） | 组合按自身 schema 生成 Prisma Client 6.19.3，类型检查/Nest 构建及指定测试通过；迁移分支本地 baseline 模拟目标账本 17/17 | 发布负责人在最终固定包源码上复核 Prisma 版本；本地 baseline 不代表目标数据 |
| 北京两节点目标库 | 发布阻塞 | 两节点只读指向同一库；原组合核验时 103 条迁移完成、17 条候选待执行，四张依赖表缺失；`Order` 当时精确 81 行，见总账 TAT 记录。新收费候选又新增 1 条未执行迁移，不能把旧核验当成当前实时值 | 数据库负责人重新只读核对实际迁移状态并取得目标库一致性快照，审查原待执行迁移和新预留表的历史数据/锁影响，单独批准后执行及核验；本任务未写生产库 |
| H5/微信小程序/App/Harmony 源码构建 | 部分完成（隔离源码构建、签名 Android 本地候选与小程序平台预览）；真机业务链路未覆盖 | `5b9dc8739` 的 App/H5 源码构建、支付/排盘 64/64 定向测试通过；同源本地签名 Android 包已装 OnePlus 9R。其上 `f5ec591ba` 仅改圈子入口读屏属性与审计规则，小程序重建和平台预览通过：624 WXML/2718 文本，主包 1,911,851 B、最大排盘分包 1,928,002 B、总包 12,381,528 B。鸿蒙两端上次构建仍对应 `279bc83a4`；本批均未上传审核或发布 | 构建/设备验收负责人在最终接收源码上重做正式固定包与跨端核对；小程序主包距 2 MiB 上限 185,301 B。当前手机包不是云端正式固定包，不能凭编译或验签代替支付、排盘的在线链路通过 |
| 圈子付款后直接入圈 | 已完成（代码候选） | 独立 `c5815a9c1`，组合 `274556b63`，支付定向 30/30；只在服务端确认本人订单已付后跳对应圈子 | 支付验收负责人用匹配安装包/测试订单验证渠道回调、延迟到账、权益确认与返回栈；不得以旧 App 包代表新源码 |
| AI 试用并发与失败退额 | 发布阻塞（收费链路） | 并发 20 请求本地 Prisma 验证只发 1 次试用；`d9e3af532`、`7db928ad0` 补会话归属与非流式审计失败退额。新隔离候选 `f0e8407e4` 新增预留流水及超时释放，Bot 71/71、服务端类型检查/构建/Prisma schema 校验通过；迁移未执行，真实数据库事务与跨进程恢复未验证 | AI/计费负责人在隔离 PostgreSQL 上验证迁移、并发预留/释放、进程中断与双节点扫尾；目标库迁移须单独审查批准，收费智能体暂不发布 |
| 旧排盘与新排盘入口 | 部分完成（本地 App 分享入口真机通过；线上承接页旧版拒绝链接） | 虚构八字结果地址仅核得路径 `app_p1.php`、查询键 `mod,act,id,ruid`，未留存取值。手机 App 环境无全局 `URL`；隔离修复 `c5ca0fcb9` 无冲突进入组合 `5b9dc8739`。25/25 定向测试、移动端类型检查及 App/H5 构建通过；OnePlus 9R 同签名本地包 `CB6EE487...` 出现热卜链接分享选项。线上 H5 仍是旧白名单，浏览器接收实际提示“分享链接无效”，见本轮附记与截图。新主动入口 `789074857` 未接收 | 发布负责人须将 App 与 H5 同版接收、发布并用受控有效结果链接验证接收页、盘面一致及登录回跳；不能只发 App。小程序正式继续显示升级中；新算法只做隔离 QA |
| 语音硬件与外部 bridge | 尚未覆盖 | 小卜候选与本地协议/上下文测试有证据；真实硬件与外部桥接不在本地构建范围。formal 与 pre-api 的语音充值档位接口只读探测均返回 404，候选端已在 `8f3a274ee` 将其显示为未开放 | 硬件/语音负责人在受控设备验证连接、语音往返、断线和费用；部署后核验充值档位、关闭计费时不得下单；不默认开启商业配置 |
| 实际设备与微信工具 | 部分完成（排盘分享本地修复包已装、线上承接页仍旧版） | 用户允许保留数据覆盖安装；OnePlus 9R（Android 14）现装隔离修复 `c5ca0fcb9` 本地同签名 `1.1.0(247)` 测试包，SHA-256 `CB6EE487...`，旧排盘结果分享菜单出现公开链接；本机浏览器接收被线上旧版 H5 拒绝。早前 `8f3a274ee` 包已留证，不能仅凭同版号区分。微信开发者工具对当前圈子修复候选 `f5ec591ba` 完成平台预览，但未取得本批微信真机登录态页面证据 | 设备验收负责人须在同版 H5 发布后复测接收端，还需受控测试订单验证原生支付与微信小程序 Scheme 直达；小程序需已发布对应付款页或具备体验版入口，真机证据仍缺；不自动提交审核或发布 |
| 线上两节点与入口版本 | 尚未覆盖候选 | 两节点目前仍是旧构建，镜像 SHA 不同而入口文件 SHA 相同；公开域名走 CDN，DNS A 记录不能证明回源版本 | 发布负责人在迁移/固定包批准后核对 CLB/CDN 回源、两节点同一包、健康和业务链路；本任务未部署/DNS 切换 |

发布顺序：先锁定正式接收提交和公开构建配置；对目标库快照审查并单独安排迁移；制作/校验同一固定包，按两节点流程发布；核对真实入口到节点与版本；最后分别验收支付、旧排盘、语音、微信小程序和收费 AI。任何一步失败先停止后续外部写入并保存执行记录。未执行的迁移可保持旧服务；迁移一旦应用，不靠反向 Git 或删除列回退，应优先回到兼容旧应用、保留可空新增结构，再按已审阅方案恢复。两节点应用回退必须用之前留存的固定包，核对数据库版本兼容。

本轮设备补验（2026-09-26）：电脑已通过 `C:/Program Files/HBuilderX/plugins/launcher-tools/tools/adbs/adb.exe` 识别 OnePlus 9R；HBuilderX CLI 可编译、同步并在其标准基座启动隔离候选。调试构建只配置公开的预发布 API/H5/静态资源地址，未读取或保存登录凭据。`pre-api.rebugx.cn` 的只读运行模式接口返回 `legacy`；手机调试记录显示运行模式与入口响应均为 `legacy`，旧版工具首页、八字排盘输入页均已打开；手机返回键先回旧工具首页、再回 App 首页。因此先前将该工具格误判成新排盘的记录作废。未提交排盘、未验证登录回跳与分享、未做真实支付回调；当前标准基座的 SDK 版本不匹配，不能把这次调试当成最终安装包验收。新增门禁修复先在独立分支 `codex/four-window-paipan-mode-gate-20260926 @ db1dfde9d0e70ccdd73363f9cee446e0d17cdcd9` 验证，再无冲突进入组合 `274556b63e53532bd68446ed2c2f7033e5ebf423`；两边定向测试及移动端类型检查均通过。类型检查前先在各自隔离工作树构建 `@guoxue/shared`，没有借用根工作区产物。

手机页面截图保存在 `docs/operations/四窗口设备验收证据-20260926/`，依次为旧排盘工具首页、八字排盘输入页、返回 App 首页；截图均来自隔离开发基座，不是已安装的正式 App。未保存签名入口 URL、账号或个人资料。

小程序平台预览补验（2026-09-26）：在隔离组合 `274556b63e53532bd68446ed2c2f7033e5ebf423` 的 `apps/mobile/.env.production` 四项公开 formal 配置下重建 `mp-weixin`，后处理与语法检查均退出码 0；开发者工具 CLI 已登录并打开该构建，`preview` 退出码 0。平台统计主包 1,987,375 B、`pkg-paipan` 1,942,107 B、`pkg-circle` 1,281,553 B、总包 12,609,205 B，主包距 2 MiB 上限 109,777 B。产物 `pages/paipan/index.js` SHA-256 为 `143bd1e31e1f2d3a9ccbd4da51d14ea98be3a0cc12e7924fc347cb1d0167d6ac`。预览二维码与临时信息仅留本机临时目录，不入仓库；本次没有提交审核、发布或获得手机页面验收结果。

小程序模拟器及最终组合补验（2026-09-26）：开发者工具自动化连接后逐页打开首页、发现、赛事、研究院、视频和排盘入口。`274556b63` 的研究院首屏报 `URLSearchParams is not defined`；修复独立提交 `618b5c46b` 无冲突接收到组合为 `9807a2fd6`。同类风险检查发现支付成功回跳、语音续费、驿站、商家等路径；独立提交 `97702c368` 无冲突接收到组合为 `5b81f3f9e`，无该全局对象的定向运行测试 8/8，移动端类型检查通过。赛事截图再次显示右上“人才榜”被微信胶囊遮挡，独立提交 `2fa2145fd` 无冲突接收到组合为 `ecd27030e`，将入口移到搜索框右侧，最终模拟器截图确认完整可见。证据见本目录 `四窗口设备验收证据-20260926/小程序最终候选-赛事入口.png` 和 `小程序最终候选-研究院入口.png`；模拟器能打开不等于手机真机、登录态或真实支付链路通过。

最终组合 `ecd27030e1b4cb7b9bde291c7f15cb5b53f170e9` 用相同公开 formal 配置重新构建 `mp-weixin`，624 个 WXML、2718 个文本产物检查通过；微信开发者工具 CLI `preview` 退出码 0，平台统计主包 1,911,870 B、`pkg-paipan` 1,928,002 B、总包 12,380,822 B。此前一次预览在构建刚结束且工具仍打开旧工程时找不到已迁入分包的课程组件；关闭并重开该工程后同一最终产物预览成功，未改源码。最终六页模拟器复核均能打开，但排盘入口在游客态只呈现登录提示，未据此宣称排盘完成；正式小程序仍维持“升级中”对外门禁。本轮没有微信审核、发布、线上部署、生产迁移或真实交易。

同版本跨端源码编译补验：`ecd27030e` 的 `build:h5`、`build:app`、`build:mp-harmony`、`build:app-harmony` 均退出码 0。使用的 `apps/mobile/.env.production` 只含公开值：`VITE_RELEASE_CHANNEL=formal`、`VITE_API_URL=https://api.rebugx.cn`、`VITE_PUBLIC_H5_URL=https://api.rebugx.cn/h5/`、`VITE_PUBLIC_ASSET_ORIGIN=https://static.rebugx.cn`。这些地址是目标 formal 配置，**不是**已部署候选的证据；`api.rebugx.cn` 实际 CDN/CLB 回源及两节点版本仍待部署后核验。没有生成可安装签名包，也没有在匹配版本手机上验收 App 原生支付。

课程与古籍馆扩展抽检：在 `ecd27030e` 模拟器中，课程搜索/我的学习图标及古籍馆书架按钮仍受胶囊遮挡。独立提交 `5f91f52ff4ca33419ded377aa594afd665d5adfd` 无冲突接收到组合为 `279bc83a48f26ed3a85ec216d36ec81b755225fa`；课程按钮移至第二行，古籍馆按实际胶囊宽度预留右侧空间。该版本移动端类型检查与微信小程序重建通过，开发者工具预览平台主包 1,911,870 B、`pkg-paipan` 1,928,002 B、总包 12,381,301 B；模拟器重开课程、古籍馆截图确认按钮不重叠。截图见 `四窗口设备验收证据-20260926/小程序最终候选-课程入口.png`、`小程序最终候选-古籍馆入口.png`。扩展页还打开了驿站发现；商家后台在游客态重定向登录，未验收登录后页面。语音时长充值页向当前 formal API 只读请求 `GET /api/v1/voice/topup/packs` 返回接口不存在，因此该候选的语音充值**线上未部署/未覆盖**，不能标为在线可用。本轮没有发起充值订单或真实支付。

`279bc83a48f26ed3a85ec216d36ec81b755225fa` 的 H5、App、鸿蒙小程序与鸿蒙 App 源码也已分别重新构建，四项退出码均为 0，补齐上段跨端构建与最终组合的版本对应关系；仍无可安装签名包或匹配版本的原生支付验收。

Android 本地签名候选补验（2026-09-26）：在隔离组合 `8f3a274eefa4e5a1f7305f26326d145520179f6e` 下使用相同的 formal 公开配置重建 `build:app`，退出码 0；配置文件 SHA-256 `FBEC17022BE7484AC8EF38B0D2192E5FA3F3A1D9361ED24F738D0B2345C6A200`。以已留存的正式云包 `1.1.0(247)` 为原生壳，在独立 `artifacts/android-candidate-20260926/` 解包并暂存网页资源；保留原壳 `manifest.json` 的云打包元数据，运行主包来自本次编译，SHA-256 `C16EFD282C30A9FBF84BB14EF66C9C0FABE1C1C24B849DA7950911B65856AF79`。制作的**本地测试候选**为 `artifacts/android-candidate-20260926/signed-8f3a274e-v247/rebu-1.1.0-247-preprod.apk`，30,723,248 B，SHA-256 `9B06BD8652644956CB5E1C7CFC9FF018DF6C1A4976552456054335A138CB5C2E`。签名验证 v1/v2 均通过，证书 SHA-256 与原云包一致（`737c54a45e710bce7c6875a41a5a5c0c228c09bd31fc08df8e016364b61b9d67`）；包名 `com.rebu.apprebu`、版本 `1.1.0(247)`，28 个 DEX/原生库条目与原云包逐项一致，包内 `app-service.js` 哈希与本次编译一致。此包沿用正式壳与正式 API/静态资源配置，但**不是云端正式固定包，不能代替支付或完整排盘真机验收**。构建脚本从本机私有文件读取签名材料，记录未写入口令。

用户本轮明确允许后，已在 OnePlus 9R 以 `adb install -r` 覆盖安装上述包，未卸载或清除 App 数据；设备回读 `base.apk` SHA-256 **完全等于** `9B06BD8652644956CB5E1C7CFC9FF018DF6C1A4976552456054335A138CB5C2E`。启动后点击首页排盘，真实手机显示旧工具首页及八字排盘输入页；Android 返回两次回到 App 首页。输入页“分享”只出现微信好友/朋友圈/保存当前页截图选项，截图为 `四窗口设备验收证据-20260926/Android签名候选-旧排盘首页.png`、`Android签名候选-八字排盘输入页.png`、`Android签名候选-排盘输入页分享选项.png`。**此处并非完成排盘后的分享页**，不能据此判断最终分享链接是否正确。本次没有填写真实个人生辰、提交排盘、发送分享或付款。原正式云包仍保存在 `artifacts/releases/android-1.1.0-247-wechat-scheme-cloud/rebu-1.1.0-247-wechat-scheme-cloud-traditional.apk`，SHA-256 `7887D7694AAE3D7B4C250F419471C3428CD8A3F1FFB3CDB26AE9FC6C476AEE7F`，可按同签名保留数据方式回退；回退前还须核对当前设备状态。

App 微信支付源码链路复核：当前候选 `ShopController.miniProgramPayLink` 对本人待付订单生成 `pkg-shop/paying/index?orderId=…&method=wechat&fromApp=1` 的微信 Scheme，App `pkg-shop/paying/index.vue` 获取 Scheme 后用 `plus.runtime.openURL` 调微信；小程序付款页重新从服务端读取订单归属与金额，首页旧 Scheme 参数仅保留兼容跳转。此处是**源码结论**，不是微信已发布版本或真机支付结果。本轮未创建、修改或支付真实订单，也未取得受控待付订单。对正式及预发布 API 的该路由，以无凭据、无真实订单的匿名请求探测均得 HTTP 401，而相邻不存在路径均得 404，可判定两处路由在当前环境已注册；这**不证明**登录后生成的 Scheme、代码版本或微信付款结果。微信小程序已发布版本是否包含直达付款页仍待核验；若尚未发布，现网旧小程序到首页的用户反馈仍可能复现。

已尝试只读打开微信公众平台查看已发布小程序版本；当前浏览器会话停在扫码登录页，未取得账号内的版本信息，没有扫码、登录、上传或发布。因此不能把开发者工具的本地预览视为线上已发布版本。

公开 H5 承接页的无登录 HEAD 探测：`gx.yrydai.com/h5/pkg-common/legacy-paipan-share/index?...` 与示例第三方结果地址 `www.yrydai.cn/p1.php?id=1` 均返回 HTTP 200；第三方结果响应未见 `X-Frame-Options` 或 `Content-Security-Policy` 响应头。此项只说明当前可达及未从这两个响应头发现显式禁嵌；**尚未证明浏览器实际 iframe 加载、登录回跳、分享落地或备案完成**。没有访问其他用户的结果内容。

浏览器实际渲染补验：承接页显示“热卜国学 / 排盘结果”页头和第三方 iframe，但示例内容区域空白。进一步 GET 对照发现 `https://www.yrydai.cn/p1.php?id=1` 自身响应虽为 200，**正文为 0 字节**，所以这个 `id=1` 是无效的验收样本，不能把空白归因于 iframe 阻止，也不能用它宣称真实结果已嵌入。需用受控生成且公开可访问的有效结果地址复验；本轮没有创建第三方排盘记录。

同一签名候选的 App 公开入口抽检：OnePlus 9R 上首页短视频入口进入列表，点开官方短视频显示播放画面，返回首页正常；圈子底部入口进入圈子列表，进入官方圈详情及圈主助理对话页，页首语音入口可见，逐级返回正常。没有发送对话、开启麦克风、消耗 AI 额度、发布圈内内容或产生订单。抽检仅证明这些页面可打开，不代表圈子履约、通知或语音真实链路验收完成；页面临时截图未归档，避免记录当前账号的圈子成员信息。

支付产物静态补验：当前 `8f3a274ee` 微信小程序构建的 `app.json` 已注册 `pkg-shop/paying/index`，该页 JS/JSON/WXML/WXSS 四件产物均存在，页面 JS SHA-256 `A9FF0C75A9B572A6480CB224DFEE5D5C7E518E8E492D48B50EB4A024CB3D77C2`，含 `requestPayment` 调用；但**仅属于本地构建，未证明线上微信小程序已发布该页**。已安装 Android 候选的 DEX 中可找到支付宝 `PayTask`、`H5PayCallback` 与 `payInterceptorWithUrl`；源码 `openHuifuAlipayWithSdk` 优先尝试 SDK，失败才走 Scheme。原生类存在不能代替真实汇付 A_NATIVE 返回码、支付宝拉起、异步回调和订单权益验收。

语音充值未部署状态补验：当前手机通过 ADB 仍识别为 OnePlus 9R，正式包 `com.rebu.apprebu` 为 `1.1.0(247)`，不能代替 `8f3a274ee`。`api.rebugx.cn` 与 `pre-api.rebugx.cn` 的 `GET /api/v1/voice/topup/packs` 无凭据只读探测均返回 HTTP 404；候选服务端 `VoiceUserController` 已定义该只读路由，但没有在线部署证据。原小程序页面把“请求的接口不存在”及 API 路径直接显示给用户；独立 `a97e11b58` 无冲突接入组合 `8f3a274ee` 后改为“语音时长充值暂未开放，请稍后再来”及返回操作，不产生订单。该版本移动端类型检查、小程序构建及平台预览通过，模拟器截图 `四窗口设备验收证据-20260926/小程序最终候选-语音充值未开放.png` 确认文案与按钮；这仅验证降级态，不代表充值可用。最终小程序主包 1,911,870 B，最大排盘分包 1,928,002 B，总包 12,381,547 B。

最终 `279bc83a4` 定向运行测试 8/8 通过。相较已验证的 `274556b63`，其后提交仅改 `apps/mobile` 与对应移动端测试，未改服务端源码、Prisma schema 或迁移，因此沿用该基线的服务端本地验证；目标库状态、两节点运行版本仍须独立核验，不能由源码差异推断在线通过。

截至本记录，**不具备对外发布这批功能的条件**；其中收费 AI 需先关闭发布或补齐持久额度闭环，其他外部验收须使用最终候选，不将模拟器、静态审计、本地 baseline 预演写成真机或线上通过。本轮未部署、未做生产迁移、未发送真实通知，也未改真实订单或余额。

2026-09-26 继续核验（同一签名 App 候选）：OnePlus 9R 在旧版八字工具中仅使用虚构样本生成了盘面，传统模式结果正常显示，返回仍可逐级回到热卜。结果页点击 App 注入的“分享”后，原生菜单只有“微信好友（当前页截图）”“朋友圈（当前页截图）”“保存当前页截图”，**没有热卜公开链接或系统分享链接**；没有实际发送、保存图片或提交真实个人资料。受控页面截图见 `四窗口设备验收证据-20260926/Android签名候选-虚构八字结果.png` 和 `四窗口设备验收证据-20260926/Android签名候选-结果分享仅截图.png`。这补齐“生成结果能打开”的真机证据，同时明确**分享链接闭环仍未通过**。源码 `pkg-common/legacy-paipan/index.vue` 在分享时读取子 WebView 当前地址，再经 `lib/legacy-paipan-share.ts` 白名单转为热卜承接链接；菜单缺少“系统分享公开链接”说明本次请求到菜单前未得到可用的公开地址，具体是结果页地址形态还是白名单拒绝，尚未取到脱敏后的实际地址，不能擅自放宽签名和个人信息过滤。须在隔离诊断包中只记录域名、路径和查询键名，确认目标确为可公开结果后作最小适配，再用微信接收端验收链接内容与盘面一致；截图分享可作降级，不可把它写成网页链接分享已完成。

收费 AI 候选 `codex/four-window-ai-failure-release-20260926 @ 122b2faa2adc39dace9bcb274dc8a0518ad7b014` 本轮复核：`consumeQuota` 先原子增减 `UserBotQuota`，随后在请求内调用模型；首字前失败由同进程 `releaseFailedQuotaSafely` 反向更新。`UserBotQuota` 只有聚合计数，没有请求唯一标识、预留状态或成功/释放流水；退额写库失败仅记日志，进程中断无法从现有表判断哪次请求未交付。故现有 62/62 测试与类型检查是正确的候选局部证据，却不解除“已购次数可能白扣”的发布阻塞。本次没有改动收费参数、目标库或真实额度。下一版须把扣额与持久请求记录置于同一事务，建立成功确认/失败释放及幂等重试；对于“已发给客户端但确认写库前进程中断”的不确定状态还需明确用户优先的恢复策略，并以重启、写入失败和并发注入测试验收。未有这些证据前，收费 AI 保持关闭。

隔离组合接收记录：从候选顺序 `cherry-pick` `de2d4154f7c3d85e6bf2c3e3d869dd92cf72679e`、`122b2faa2adc39dace9bcb274dc8a0518ad7b014`，均无冲突，形成独立回退点 `282367904fa5592a6fde5eaccbafbd4265627ebc`、`8119c163f9985c88e843ada50c95819106880565`。在最终隔离 HEAD 上重新运行 BotService/BotController 定向测试 **62/62 通过**、服务端 `tsc --noEmit` 通过；相对 `8f3a274e` 未改 Prisma schema 或移动端源码，故前述 App/微信小程序构建仍对应 `8f3a274e`，没有伪称新 HEAD 的全端构建。撤回顺序为先 `8119c163f` 后 `282367904`，不触碰根工作区或线上。此接收只保留代码候选，不改变收费 AI 发布阻塞状态。

旧排盘分享补验（2026-09-26）：在单独诊断包上使用同一条**虚构**八字记录，手机返回的结果页路径为 `app_p1.php`，查询键仅记录 `mod,act,id,ruid`；App 父层的全局 `URL` 为 `undefined`。脱敏诊断截图为 `四窗口设备验收证据-20260926/Android诊断-仅路径和查询键.png`。原 `publicLegacyResultUrl` 因此直接返回空，且原双层白名单也拒绝 `act`、`ruid`。隔离修复 `codex/paipan-share-url-runtime-fix-20260926 @ c5ca0fcb9` 以受控字符串路径替换代替 App 全局 `URL`，双层白名单只新增 `act` 与**数字格式**的 `ruid`，仍拒绝签名、登录、重复和未知参数；无冲突进入组合 `5b9dc8739307f511b5ad217bc1daa0d0a2b6b3a6`。诊断弹窗仅存在诊断工作树 `6694899f0`，未进入修复提交或安装的修复包。定向测试 **25/25**、移动端类型检查、App 和 H5 构建均通过；正式公开 `.env.production` 配置哈希 `FBEC17022BE7484AC8EF38B0D2192E5FA3F3A1D9361ED24F738D0B2345C6A200`。本地新 H5 产物的承接组件哈希 `E15A4DA0DFC513E9495FDBCD66910E1F8AD2C08415BC3E910282FA739B1DABD2`，浏览器用合成参数测试显示 1 个结果 iframe、无无效提示；这只证明本地承接逻辑通过，不证明第三方真实盘面已在接收端显示。

OnePlus 9R 已在用户原有覆盖安装授权下安装相同源码的本地签名 v247 测试包 `artifacts/android-candidate-20260926/paipan-share-fix-v247-20260926/rebu-1.1.0-247-preprod.apk`，SHA-256 `CB6EE487E9782A5EE685A8B3F8E041F7A82F6CC711FC1010E096101D123B734D`，签名证书与原 247 一致；设备回读 `base.apk` 的 SHA-256 与此包一致，未卸载、未清数据。虚构八字结果页原生菜单现有“微信好友/朋友圈（热卜链接）”和“系统分享公开链接”，截图见 `四窗口设备验收证据-20260926/Android修复包-结果链接分享菜单.png`。通过系统分享中的**本机浏览器**打开链接后，线上热卜承接页提示“分享链接无效”，截图见 `四窗口设备验收证据-20260926/Android修复包-线上承接页拒绝旧白名单.png`。只读查询线上 `/h5/` 入口实际加载的 `pkg-common-legacy-paipan-share-index.qLdNCHyM.js` 仍无 `act`、`ruid` 白名单，解释了本次拒绝；未发送给联系人。测试过程中仅有一张虚构盘面截图被保存到测试手机相册，没有真实用户资料或交易操作。手机当前保留本地修复测试包，**不是线上发布版本**。

发布条件与顺序：先让正式接收窗口审查并接收 `5b9dc8739`（独立回退点；回退此修复不影响前序 AI 提交），在最终源码上构建 App 与 H5；按发布流程先更新热卜 H5 承接页，确认目标环境组件确已包含 `act` 与数字 `ruid` 白名单，再发布同版 App，最后用受控有效结果在微信/浏览器接收端核对盘面一致、返回和登录回跳。线上 H5 未更新前，不能向用户宣传“公开结果链接分享已修复”；如需保守发布，应暂时仅保留截图分享。本轮没有部署 H5/App、改 DNS、生产迁移或提交小程序审核。

最终隔离组合 `5b9dc8739` 的支付/排盘定向回归：Android 支付宝 SDK 与降级、微信 App 与小程序 Scheme、支付完成返回栈、旧排盘分享五组测试合计 **64/64 通过**。这是本轮新运行的本地测试，只验证代码分支及调用约束；没有新建真实订单、扣款、微信支付回调或小程序正式版付款页验收，因此不能据此解除真实支付发布门禁。

公开结果独立诊断（2026-09-26）：为区分“热卜 H5 承接页旧版白名单拒绝”和“第三方公开结果自身不可读”，在**仅供诊断、不得合入**的 `codex/paipan-public-result-probe-20260926 @ 50ac74d09594b979ef19068424fee066e844bb11` 上，用同一条虚构八字记录从 `app_p1.php` 推导 `p1.php`，只发起不带凭据的只读请求；弹窗仅显示 HTTP 状态、正文长度和响应类型，不显示目标 URL、查询值或正文。Android WebView 的 `redirect: manual` 结果为 `HTTP 0 / 正文长度 0 / opaqueredirect`，证据见 `四窗口设备验收证据-20260926/Android诊断-公开结果无凭据重定向.png`（SHA-256 `03EDDC773B47239BBA8DA221AE0EFB6395AC0EADA762598B43F05B7776E3A095`）。这**只能证明发生了跳转**，不能推出最终落地页内容、是否要求微信授权或实际 iframe 能否显示。诊断包曾经覆盖安装在 OnePlus 9R，验证后已用 `adb install -r` 恢复非诊断的本地修复包，设备回读 `base.apk` 与该包 SHA-256 同为 `CB6EE487E9782A5EE685A8B3F8E041F7A82F6CC711FC1010E096101D123B734D`，未卸载或清除 App 数据。公开结果接收端仍须用受控有效记录验证跳转后的实际内容；本轮不能据 `HTTP 0` 直接认定第三方不可用。

尝试为同一虚构盘面建立不上传链接的手机本地接收测试页时，自动审批拦截了经 ADB 启动手机浏览器访问本地测试页的命令，返回 `blocked by policy`，未给出更细原因。按拒绝停止该路径，已关闭临时 HTTP 服务并移除 ADB 端口反向映射；未将结果地址、参数值或凭据写入日志和仓库。由于浏览器接收端未完成实际盘面验证，线上分享仍记为**未覆盖/不可宣称完成**。测试时系统分享中的“保存当前页截图”被误点，额外一张虚构盘面保留在测试手机相册，没有真实用户资料。

小程序包量补验（2026-09-26）：最终隔离组合 `5b9dc8739307f511b5ad217bc1daa0d0a2b6b3a6` 在隔离工作树中使用同一份 formal 公开 `.env.production` 重建 `mp-weixin`，退出码 0；624 个 WXML 和 2718 个微信文本产物检查通过。仓库脚本的保守估算为主包 1.92 MB（强制余量约 0.08 MB）、最大 `pkg-paipan` 分包 1.67 MB、`pkg-circle` 1.05 MB、33 个分包均达标。随后对**同一最终产物**运行已登录微信开发者工具官方 CLI `preview`，退出码 0；平台实测总包 **12,381,502 B**、主包 **1,911,825 B**（距 2 MiB 余 **185,327 B**）、`pkg-paipan` **1,928,002 B**（距 2 MiB 余 **169,150 B**）、`pkg-circle` **1,257,587 B**。预览二维码与信息仅在本机临时目录，不入库；预览成功不等于手机页面验收或正式版已发布。本轮没有提交审核或发布。若后续正式发布源码再变化，需重新核对包量与页面闭包。

交互门禁后续修复（2026-09-26）：对 `5b9dc8739` 新运行 `release:check-nav`，451 个页面、315 个字面量跳转均通过；`release:audit-interactions` 起初 **38/39**，失败项为圈子广场。核对页面后发现一项真实遗漏：改版后的“创建圈子”入口虽有键盘操作但缺读屏名称；审计同时仍要求旧版顶部快捷栏及固定“换一批”文案。隔离分支 `codex/circle-a11y-gate-20260926 @ f5ec591ba3707748d134aeda12dc97909de1d0b8` 增加该入口读屏名称，并让审计对应现有“我的圈子/发现/动态”结构与失败时切换的换批文案。修改仅涉及一处页面属性和一项静态审计规则，可独立回退。新运行交互审计 **39/39**、移动端 `vue-tsc --noEmit`、微信小程序本地构建、624 个 WXML/2718 个文本产物及仓库包量检查均通过。该新 HEAD 第一次平台 `preview` 报旧工程缓存引用已迁走的课程组件；关闭并重新打开隔离工程后仍长时间停在编译阶段，主动停止。最后经官方 CLI 退出并重新启动开发者工具，对**未改动的同一产物**运行 `preview` 成功：总包 **12,381,528 B**、主包 **1,911,851 B**（距 2 MiB 余 **185,301 B**）、`pkg-paipan` **1,928,002 B**、`pkg-circle` **1,257,587 B**。前两次工具状态不能写成源码失败，最终成功也不等于手机登录态/真实业务验收；正式接收后如源码再变化，仍须复核。未上传或发布小程序。

智能体会话归属修复（2026-09-26）：审计 `5b9dc8739` 时发现历史详情查询仅按智能体和会话号过滤，且续聊会复用这条查询；已知他人会话号可能读取或带入其对话。基于 `f5ec591ba` 的独立提交 `codex/bot-history-owner-20260926 @ d9e3af532d52d6820a064285f0dbdc55eb34d9ec` 在历史查询中增加当前用户条件，并在非流式及流式续聊扣额度之前校验会话归属；无归属返回“对话不存在”，不泄露他人会话内容。BotService/BotController 定向测试 **65/65**、服务端 `tsc --noEmit` 均通过；没有改 schema、迁移、移动端源码或收费参数。它是新的隔离代码候选，未进入正式发布线或线上；相对前述小程序预览源码只变了服务端文件，因此小程序包量仍对应 `f5ec591ba`。撤回此修复应先于撤回 `f5ec591ba`，但不建议在仍提供对话历史服务时单独撤回安全校验。流式首轮若在审计记录落库前断开，客户端即使拿到会话号也不能续聊；这是宁可要求重开、也不以未知归属会话号访问上游的安全边界，后续若要无缝续聊需先持久建立会话归属。**收费额度的持久流水及进程中断恢复仍缺，收费智能体发布阻塞不变。**

智能体失败退额补验（2026-09-26）：上述分支继续以独立提交 `7db928ad0a6f2496cc7a49368810174772b4acf0` 修复非流式回答生成成功、但对话审计记录保存失败时额度未归还的路径；客户端尚未收到回答，故沿用已有单次失败退额逻辑并保留原数据库错误。新增定向回归后 BotService/BotController **66/66**、服务端 `tsc --noEmit` 通过，分支干净。该修复不改 schema 与收费规则，可先撤回 `7db928ad0` 再撤回 `d9e3af532`；仍不能解决退额写库失败或进程中断，持久流水发布阻塞不变。

收费智能体持久额度候选（2026-09-26）：在上述两次修复之后的独立 `codex/bot-quota-ledger-20260926 @ f0e8407e4fdb160f76d3d2834b0385cebe16b830` 新增 `BotQuotaReservation` 表与单独增量迁移 `manual_z_20260926_09_bot_quota_reservation`。试用/已购次数的条件扣减与预留记录在同一 Prisma 事务；只有 `RESERVED` 能被一次性释放，状态转换与退额同事务，双节点定时回收超过 30 分钟未确认的预留。非流式回答以 HTTP 响应 `finish`（服务端写完，**不等于客户端确已读到**）确认，提前 `close` 释放；流式回答在首个非空可见片段写出后确认。数据库暂不可用时保留预留并由后续扫尾按用户优先原则返还。免费智能体/会员不建收费预留。此改动尚**未进入正式发布线**，新迁移**未在目标库执行**，不得与仍按旧 schema 生成的客户端混装。

本地验证：仅在隔离工作树的独立 `node_modules` 中以最终 schema 生成 Prisma Client **6.19.3**；BotService/BotController 定向单测 **71/71**、服务端 `tsc --noEmit`、`nest build` 和使用无效占位连接串的 `prisma validate` 均通过。首次 `prisma validate` 因隔离树没有 `DATABASE_URL` 报缺少环境变量，设置本地占位值后 schema 校验通过，**没有连接数据库**。本机无 Docker/PostgreSQL 测试服务，本轮未能证明真实 PostgreSQL 上迁移、事务回滚、双进程并发或重启后扫尾；单元测试透传事务 mock 不能替代这些验证。按安全边界，收费智能体仍为**发布阻塞**。正式部署顺序必须是目标库快照与迁移审查 → 单独批准增量迁移 → 用该 schema 原地生成且固定 Prisma 客户端与服务端包 → 两节点同包发布 → 受控计费回归；不能先发新服务端再补表。若迁移未执行，旧服务可原样运行；迁移执行后应用回退到兼容旧包时保留新增表，不直接删表或反向改账。
