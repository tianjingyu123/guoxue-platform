# 四窗口上线发布门禁（2026-09-26）

本表是接续总账的执行视图。根工作区 `release/integration-20260801 @ 2e458597a80076ba2728e9ab2a3156bf78a3d0e3` 仍含其他窗口未提交改动，**不是**本表的功能组合；隔离组合评审 `codex/four-window-release-review-20260926 @ 274556b63e53532bd68446ed2c2f7033e5ebf423` 也**不是**正式发布线或线上版本。未经逐批接收、迁移和发布决策，不得用“本地编译通过”代替上线结论。

| 门槛 | 状态 | 当前证据与对应版本 | 剩余动作 / 负责人 |
| --- | --- | --- | --- |
| 四窗口代码整合与回退点 | 已完成（隔离评审） | 组合 `274556b63`；迁移 `83d97cdc5`、AI 并发 `c5fa5d14e`、圈子付款回跳 `c5815a9c1`、旧排盘门禁 `274556b63` 各有独立提交，详见本目录总账及组合评审 | 主线接收窗口逐批核对当前正式发布线与等效改动，再形成正式发布候选；不得整体覆盖根工作区 |
| 服务端 Prisma 与本地验证 | 已完成（隔离环境） | 组合按自身 schema 生成 Prisma Client 6.19.3，类型检查/Nest 构建及指定测试通过；迁移分支本地 baseline 模拟目标账本 17/17 | 发布负责人在最终固定包源码上复核 Prisma 版本；本地 baseline 不代表目标数据 |
| 北京两节点目标库 | 发布阻塞 | 两节点只读指向同一库；103 条迁移完成、17 条候选待执行，四张依赖表缺失；`Order` 当前精确 81 行，见总账 TAT 记录 | 数据库负责人取得目标库一致性快照、检查 17 条迁移与历史数据/锁影响，单独批准并执行迁移及核验；本任务未写生产库 |
| H5/微信小程序/App/Harmony 源码构建 | 已完成（前一隔离版本产物）；最新组合待固定包 | formal 公开配置下四端编译通过；付款修复后 H5/小程序/App 再构建，小程序保守主包估算 1.92 MB、余量约 0.08 MB；Harmony 只对应付款修复前版本。`274556b63` 又增加排盘门禁，已通过定向测试与类型检查，尚未重打四端最终产物 | 构建负责人以最终接收版本和正式配置重新打固定包，微信开发者工具核对平台口径；小余量需警惕后续增量 |
| 圈子付款后直接入圈 | 已完成（代码候选） | 独立 `c5815a9c1`，组合 `274556b63`，支付定向 30/30；只在服务端确认本人订单已付后跳对应圈子 | 支付验收负责人用匹配安装包/测试订单验证渠道回调、延迟到账、权益确认与返回栈；不得以旧 App 包代表新源码 |
| AI 试用并发与失败退额 | 尚未覆盖完整资金安全 | 并发 20 请求本地 Prisma 验证只发 1 次试用；失败退额另有隔离候选 `122b2faa2`，Bot 62/62、服务端类型检查通过，尚未并入组合 | AI/计费负责人补持久化预留、成功确认、失败释放及可审计重试；现有补偿遇进程崩溃或 DB 写失败不能自动精确恢复，收费智能体暂不发布 |
| 旧排盘与新排盘入口 | 已完成（隔离开发基座入口验证）；完整真机链路尚未覆盖 | OnePlus 9R 上运行组合基线 `0349f2c0f` 及门禁隔离分支的开发构建，服务模式与入口均记录为 `legacy`；已打开旧版工具首页、八字排盘输入页并逐级返回。门禁修复从独立 `db1dfde9d` 以无冲突 cherry-pick 进入组合 `274556b63`，该组合隔离测试 6/6、移动端类型检查通过。新主动入口 `789074857` 未接收 | 排盘验收负责人用匹配正式基座/签名包验证排盘提交、登录回跳、分享及公众号；小程序正式继续显示升级中；新算法只做隔离 QA |
| 语音硬件与外部 bridge | 尚未覆盖 | 小卜候选与本地协议/上下文测试有证据；真实硬件与外部桥接不在本地构建范围 | 硬件/语音负责人在受控设备验证连接、语音往返、断线和费用；不默认开启商业配置 |
| 实际设备与微信工具 | 部分完成（连接与开发基座） | OnePlus 9R（Android 14）通过 HBuilderX 自带 ADB 识别并运行开发构建；手机已装正式 App `1.1.0(247)`，其更新时间早于本批候选，不能代表本批。HBuilderX 编译器 5.23 与手机标准调试基座 5.14 不匹配，原生支付等能力不能据此验收；微信小程序仍只有本地构建证据 | 设备验收负责人取得与最终源码匹配的测试基座/签名包再做 App 支付；微信开发者工具核对包量与页面，取得体验权限后真机验证；不自动上传或提交审核 |
| 线上两节点与入口版本 | 尚未覆盖候选 | 两节点目前仍是旧构建，镜像 SHA 不同而入口文件 SHA 相同；公开域名走 CDN，DNS A 记录不能证明回源版本 | 发布负责人在迁移/固定包批准后核对 CLB/CDN 回源、两节点同一包、健康和业务链路；本任务未部署/DNS 切换 |

发布顺序：先锁定正式接收提交和公开构建配置；对目标库快照审查并单独安排迁移；制作/校验同一固定包，按两节点流程发布；核对真实入口到节点与版本；最后分别验收支付、旧排盘、语音、微信小程序和收费 AI。任何一步失败先停止后续外部写入并保存执行记录。未执行的迁移可保持旧服务；迁移一旦应用，不靠反向 Git 或删除列回退，应优先回到兼容旧应用、保留可空新增结构，再按已审阅方案恢复。两节点应用回退必须用之前留存的固定包，核对数据库版本兼容。

本轮设备补验（2026-09-26）：电脑已通过 `C:/Program Files/HBuilderX/plugins/launcher-tools/tools/adbs/adb.exe` 识别 OnePlus 9R；HBuilderX CLI 可编译、同步并在其标准基座启动隔离候选。调试构建只配置公开的预发布 API/H5/静态资源地址，未读取或保存登录凭据。`pre-api.rebugx.cn` 的只读运行模式接口返回 `legacy`；手机调试记录显示运行模式与入口响应均为 `legacy`，旧版工具首页、八字排盘输入页均已打开；手机返回键先回旧工具首页、再回 App 首页。因此先前将该工具格误判成新排盘的记录作废。未提交排盘、未验证登录回跳与分享、未做真实支付回调；当前标准基座的 SDK 版本不匹配，不能把这次调试当成最终安装包验收。新增门禁修复先在独立分支 `codex/four-window-paipan-mode-gate-20260926 @ db1dfde9d0e70ccdd73363f9cee446e0d17cdcd9` 验证，再无冲突进入组合 `274556b63e53532bd68446ed2c2f7033e5ebf423`；两边定向测试及移动端类型检查均通过。类型检查前先在各自隔离工作树构建 `@guoxue/shared`，没有借用根工作区产物。

截至本记录，**不具备对外发布这批功能的条件**；其中收费 AI 需先关闭发布或补齐持久额度闭环，其他外部验收须使用最终候选，不将模拟器、静态审计、本地 baseline 预演写成真机或线上通过。本轮未部署、未做生产迁移、未发送真实通知，也未改真实订单或余额。
