# 圈子履约与收益退款 B · 主线交接（2026-09-20）

> 本文由接收核查窗口编写。全部结论来自**本机只读核查**与会话记录取证。
> 文中所有测试数字均为**本机隔离环境**结果，**不构成线上验收**，不代表真实支付渠道或实际资金到账已验证。
>
> 本轮未部署、未执行生产迁移、未操作真实资金、未 force push、未推送远端、未修改根工作区、
> 未提交／回滚／stash／覆盖归属会话工作树中的任何未提交文件。

---

## A. 已提交 14 个提交：可独立审查

### A.1 审查入口

| 项 | 值 |
|---|---|
| 审查工作树 | `D:/gx-deploy-91/.worktrees/circle-fulfillment-review-20260920` |
| 检出 | detached HEAD `6fbadf16db0130657858d2507b74811288e2d980` |
| 建树时工作树状态 | 干净（0 项改动） |
| 来源 | 直接从该提交检出，**未复制**任何脏工作树内容 |
| Prisma 客户端 | **无**。未复用已被新 schema 覆盖的 `.prisma-candidate`；需验证时按该提交自己的 schema 独立生成 |
| 原候选工作树 | `.worktrees/circle-fulfillment-receive-20260919` —— **原样保留**，本轮未触碰 |

### A.2 提交链与基线

- 基线 `181fd516`（= `receive/integrated-three-20260919` 当前 HEAD，本轮复核未前进）；
- 本任务 **14 个提交**，可快进，无合并提交；
- 主线**尚未接收**：`receive/integrated-three-20260919`、`receive/live-gift-l1l2-20260920`、
  `receive/feedback-admin-c-20260918` 均不含 `6fbadf16`；无后续修复提交。

### A.3 实际改动范围（`181fd516..6fbadf16`）

共 **26 个文件，+4548 / −73**：

| 类别 | 文件 | 增量 |
|---|---|---|
| 服务端生产代码 | `shop/shop-payment.service.ts` | +331 |
| | `circle-refund/circle-refund.service.ts` | +310 |
| | `shop/circle-fulfillment.ts`（新增文件） | +295 |
| | `circle/services/circle-membership.service.ts` | +241 |
| | `commission/commission.service.ts` | +63 |
| | `shop/shop-order-lifecycle.service.ts` | +18 |
| | `circle-refund/circle-refund.controller.ts` | +16 |
| schema / 迁移 | `prisma/schema.prisma` | +21 |
| | `migrations/20260918120000_add_circle_revenue_order_idempotency/` | +21 |
| | `migrations/20260919090000_add_commission_recall_reason/` | +25 |
| | `migrations-deploy/full-baseline.sql` | +12 |
| 单测 | `circle/services/circle-renew-discount.spec.ts` | +19 |
| 验证基础设施 | `scripts/ops/circle-fulfillment-repro/*`（5 个） | +2199 |
| | `scripts/ops/prisma-candidate/*`（6 个） | +486 |
| 文档 | `候选交付说明-20260919.md` | +468 |
| | `待人工追回-只读查询.sql` | +94 |

**范围核对结论**：与交付说明 §1.4 / §七 的声明一致 ——
无 `apps/admin`、无 `apps/mobile` 改动（管理端页面未改、未构建）；未引入 A / D 或其他专项。

### A.4 schema 变更性质（已核实）

相对基线 `181fd516`，候选 schema **只有新增，无字段删除或改名**：

- `CircleRevenueRecord`：`orderId String?` + `@@unique([type, orderId])` + `@@index([orderId])`；
- `CommissionRecall`：`reason String?`、`sourceId String?` + `@@index([status, createdAt])`；
- 两处 `-` 行仅为同行注释变化（`sourceId` 注释补充、`status` 注释增列 `pending_manual`），类型未变。

### A.5 验证证据（本机隔离，2026-09-19，对应 `6fbadf16`）

| 套件 | 结果 |
|---|---|
| `prisma-candidate/assert-isolation.mjs` | 11 / 11 |
| `circle-fulfillment-repro/repro.mjs --mode=after` | 10 / 10 |
| 同上 `--mode=before`（旧实现反证） | 10 / 10 |
| `circle-fulfillment-repro/integration.mjs` | 40 / 40 |
| `circle-fulfillment-repro/revenue.mjs` | **81 / 81** |
| `circle-fulfillment-repro/concurrency.mjs` | `fixed` 0 失败；`legacy` 3 失败（预期反证） |
| `tsc -p tsconfig.candidate.json` | 0 错误 |
| `jest -c jest.candidate.config.cjs`（circle / circle-refund / commission / shop / coin） | **42 / 43 套件，904 / 905 项** |
| `scripts/release/audit-database-baseline.mjs` | 通过 |

**测试数字口径（校准）**：

- 按具体提交与证据记录为 **904 / 905，其中 1 项失败**（`shop-order.service.spec.ts` 白标贺卡
  `giftCardMeta`，基线既有）。**不写全绿。**
- **「866 / 867」不采用**：查证该数字仅出现在 2026-09-20T07:25:57Z 的指令文本中，
  在候选交付说明、验证脚本输出与任何运行记录里都没有对应，无法归属到某次具体运行。

**基线对照所用 Prisma 客户端是否匹配 —— 已核实，匹配，无需补跑**：

- `shop-order.service.spec.ts` 不连真实数据库（文件内无 `PrismaClient` / `DATABASE_URL` / 真实连接引用）；
- `jest.candidate.config.cjs` 通过 `moduleNameMapper` 将 `@prisma/client` 与 `.prisma/client`
  统一映射到候选客户端；
- 候选 schema 相对基线**只有新增**（见 §A.4），对基线代码是**类型超集**。

因此「在 `181fd516` 上用同一套候选客户端复跑、同样失败」这一对照**成立**，该失败可归因基线，
本轮不补跑。

### A.6 未覆盖（缺环境验证，维持原结论）

- **真实支付渠道：零覆盖**。全部为本机合成数据，未调用微信 / 支付宝 / 汇付任何真实接口。
- **实际资金到账：未验证**。`CircleRevenueRecord.settled` 源码中无任何写入方，也无代码读该表发起打款。
  已验证的是**台账入账与接口可见**，不是打款。
- 正式不可变镜像构建未执行（本机无 Docker）；全仓测试未重跑；真实生产数据分布未取得
  （N9 决策所需的各原因码发生量，必须在目标环境用只读 SQL 实测）。

### A.7 远端状态（本轮不处理）

`github` / `gitee` / `cnb` / `backup` 四镜像均停在 `ae2ddfa5`（2026-09-19 12:50），
与本地 `codex/circle-fulfillment-receive-20260919` **已分叉**（本地对应提交被重写为 `b757bad1`，
其后另有 5 个提交）。

**本轮不 force push、不自动推远端。** 后续需要交付时**优先使用新分支名**，保留已有历史。

---

## B. 6 个未提交文件：归属、差异与独立风险

### B.1 归属 —— **已确认**（非推测，非按修改时间认定）

| 项 | 证据 |
|---|---|
| 来源会话 | `C:\Users\Administrator\.claude\projects\C--Users-Administrator-Desktop\544e4936-7abb-4a44-b239-16ed87f0689e.jsonl` |
| 会话跨度 | 2026-09-19T18:50:08Z → 2026-09-20T07:33:25Z（UTC） |
| 任务线 | 同一条：首条指令即「圈子支付履约与收益退款候选收口」，含「按你建议执行」「负责人田崇民，7天内」两条追加指令 |

**直接触发指令**（2026-09-20T07:25:57Z，原文摘录）：

> 请确认待办可由负责人实际访问，并**明确人工处理后的记录、凭据与结案方式**。
> 不能仅凭"能查看超期"就认定整个处理流程已闭环，也不要求本轮建设大后台。
> **冲正只适用于核实后确需调整的记录。**

**执行记录**（同会话，UTC）：

| 时间 | 动作 |
|---|---|
| 07:27:53 | 声明「补最关键的缺口：待办目前只能看、不能结案。先实现记录/凭据/结案路径」 |
| 07:27:56 | 启动隔离 PG 集群（55432） |
| 07:28:32 – 07:32:13 | 依次编辑 6 个文件（q1–q6） |
| 07:32:24 | `revenue.mjs` → **98 通过 / 0 失败**（含 R20 全部 17 项） |
| 07:32:50 | `assert-isolation` 11/0；`repro` 10/0；`integration` 40/0；`concurrency` fixed 0 失败 / legacy 3 失败（预期）；空库基线审计通过 |
| 07:33:21 | 迁移幂等与存量兼容：两迁移连续两次均 OK；新 4 列全 NULL 1/1；`status` 未被改动 |
| 07:33:25 | 启动 jest 受影响模块单测 —— **会话记录到此终止** |

**授权范围（同会话首条指令）**：允许「在独立工作树、`codex/` 前缀分支中进行候选适配、
必要技术修复、本地合成数据验证及提交」；禁止连生产库处理资金、部署、执行生产迁移、
向共享 `node_modules` 生成客户端、顺带引入其他专项。

→ **写这批代码在授权范围内**。但「明确人工处理后的记录、凭据与结案方式」是否等同于
「实现一个可动资金的写接口」，存在解释空间，**需主线显式确认**，本窗口不代为认定。

### B.2 差异清单（6 文件，+372 / −12）

| # | 文件 | 变更 |
|---|---|---|
| 1 | `prisma/schema.prisma` | `CommissionRecall` 新增 4 个可空列：`resolvedAt`、`resolvedBy`、`resolutionNote`、`resolvedRevenueId` |
| 2 | `migrations/20260919090000_add_commission_recall_reason/migration.sql` | **就地追加** 4 条 `ADD COLUMN IF NOT EXISTS` |
| 3 | `migrations-deploy/full-baseline.sql` | 同步上述 4 列 |
| 4 | `circle-refund.controller.ts` | `adminManualRecalls` 增 `state` 查询参数；**新增写接口** `POST /circle-refund/manual-recalls/:id/resolve`，带 `@RedLineGate(RedLine.MONEY)`、`@Auditable`、`@Roles("SUPER_ADMIN","OPERATION_ADMIN")` |
| 5 | `circle-refund.service.ts` | `MANUAL_RESOLUTIONS` / `RESOLVED_STATUS` / `MIN_RESOLUTION_NOTE_LENGTH=10`；`getManualRecalls` 支持 `state=resolved`；新增 `resolveManualRecall`（`$transaction` + 台账行级锁 + 幂等 + 归属校验 + 按认定行金额冲正） |
| 6 | `scripts/ops/circle-fulfillment-repro/revenue.mjs` | 新增 R20 系列 17 条用例 |

### B.3 独立风险 —— 不默认批准接入

1. **能力性质发生变化**：从「只读待办」变为**可写、可动资金**的接口。
   旧候选交付说明 §4.2 明确写着「**刻意不做**：不提供一键冲正」，§3.2 结果表不含 R20，
   同目录只读 SQL 也不含结案字段 → **旧文档与这 6 个文件不一致**。
   **旧候选的通过结论不覆盖这部分，不得混用。**

2. **迁移文件被就地修改**：`20260919090000_add_commission_recall_reason` 已存在于 14 个提交中，
   并已在隔离库按旧内容验证过；现在同一文件又追加了 4 条语句。
   虽然全部 `IF NOT EXISTS`、可重入，但**同名迁移内容变化**本身需要显式决定：
   是修改原迁移，还是另起一个新迁移文件。这直接影响 D8 的执行顺序与目标库预检口径。

3. **候选客户端已被新 schema 覆盖**：`.prisma-candidate/meta.json` 的 `schemaSha256`
   由交付说明 §3.1 记录的 `12467ca6…` 变为 `3e8dd238…`（2026-09-20 00:30 生成）。
   → 旧交付说明记录的验证环境对**当前脏工作树**已失效；
   审查旧候选必须另行生成客户端（本交接的审查工作树已按此处理）。

4. **「有人工指定」不等于资金操作天然安全**。以下为静态阅读 + 合成数据用例所见，
   **未经本窗口独立复验**，逐项仍需单独评审：

| 评审项 | 现状（静态所见） | 待确认 |
|---|---|---|
| 权限与资金操作门禁 | `RedLineGate(MONEY)` + `Auditable` + 沿用既有退款审核角色 | 门禁是否足够；是否需要双人复核 |
| 指定收益行的归属校验 | 用例断言「需同圈子、同成员、类型 `circle_join` 且金额为正」 | **订单维度是否校验未见明确**；跨圈子 / 跨用户边界需复验 |
| 金额上限 | 按人工认定行自身 `ownerShare` 冲正，**无独立上限** | 是否需要绝对上限与单日累计上限 |
| 重复冲正保护 | 台账行级锁 + 已结案拒绝 + 用例断言「重复提交不产生第二条冲正行」 | 并发复验 |
| 事务、并发、审计、凭据 | `$transaction`；`resolutionNote` 最少 10 字；`resolvedBy` / `resolvedAt` 落库；`Auditable` | 审计与业务结论是否互为佐证、可追溯 |
| `no_change` 不动资金 | 用例断言冲正行 = 0 | 复验 |
| 迁移兼容与历史数据 | 4 列均可空、`IF NOT EXISTS`、历史行保持 NULL；存量兼容已验证 | 见风险 2（迁移文件就地修改） |

### B.4 验证缺口

**已跑并通过**（2026-09-20 00:32–00:33 本地，隔离库，含这 6 个文件的改动）：
`revenue.mjs` 98/0（含 R20 全部 17 项）、`assert-isolation` 11/0、`repro` 10/0、
`integration` 40/0、`concurrency` fixed 0 失败 / legacy 3 失败（预期）、
空库基线审计通过、迁移幂等与存量兼容通过。

**未完成**：

- 07:33:25Z 启动的 **jest 受影响模块单测无结果**，会话记录在此终止；
- 候选 **`tsc` 全量类型检查本轮未见重跑**（新增接口与列后未复验类型）。

> 以上结果**单独成包**，不并入 §A.5 中 14 个提交的通过结论。

### B.5 现场状态

6 个未提交文件**原样保留**：未提交、未回滚、未 stash、未续写、未覆盖。

---

## C. 可继续推进 / 需确认

### C.1 可继续（不依赖业务决策）

1. 主线按 §A.1 的审查工作树审查 14 个提交 —— 代码已处于可审查状态；
2. 主线如需独立复核，可补跑 14 提交的 jest 与 `tsc`：
   **建议新建独立合成测试库**，不重启已崩溃的临时集群，不影响本机现有 PostgreSQL 服务；
3. 编写 D8 迁移顺序、目标库**只读**预检、备份与回退方案（不执行）。
   硬约束不变：代码与迁移同批上线；**回退时不得删除 `CircleRevenueRecord.orderId` 列**（不可逆）；
4. 编写真实支付渠道验收方案（环境、账号、金额、退款清理方式），**未获授权前不发起真实扣款**；
5. 明确历史异常订单的**发现口径与交接对象** —— D6 继续不自动补发，
   但「不自动补发」不等于「不需要处理」。

### C.2 需确认

1. **§B 的 6 个文件是否接入本候选**，还是单独列包评审。
   若接入：迁移是改原文件还是另起新迁移（见 §B.3 风险 2）；
2. N9（推断匹配是否放开 `single`）、N10（`exact` 金额不符口径）、N11（分成基数）—— 财务 + 产品；
3. D3 下单前新增拦截的知情确认 —— 内容治理 + 产品；
4. D8 迁移执行窗口 —— 技术负责人 + DBA；
5. N2 历史双计口径（止血已落地，历史记录一条未动）—— 财务；
6. D6 补偿入口是否接调度 —— 产品 + 财务；
7. 远端四镜像分叉如何处理（本轮未推）；
8. 崩溃的临时集群数据目录是否继续保留。

---

## D. 措辞校准（本轮更正，记录于此，不直接改归属会话工作树中的文档）

1. **不写全绿**。按具体提交与证据记录为 **904 / 905，1 项基线既有失败**。
   「866 / 867」在任何文档与运行记录中均无对应，**不采用**（见 §A.5）。
2. **7 个自然日与 5 个工作日不直接比较宽严**。
   候选交付说明 §4.4 现有表述「这比《集中接收清单》N3 的建议值（5 个工作日）宽松」**不成立**：
   5 个工作日跨周末通常已是 7 天日历跨度，遇节假日更长。两者口径不同，不可直接比较。
   → 该句仍在归属会话的工作树中，**本轮未改**，待该会话或主线修订。
3. **「完成核对」与「必要时冲正」是两件事**。人工核对的结论有两种：
   核实后**无需调整**（不动资金）与**确需调整**（按人工认定的那一条收益行冲正）。
   「刻意不做一键冲正」指的是系统不自行挑选收益行，**不是要求每条待办都必须冲正**。
4. **本机隔离验证 ≠ 线上验收**。文中全部测试数字均为本机合成数据结果，
   不得表述为真实支付渠道验收或实际资金到账。

---

## E. 临时环境与进程（仅识别，未干预）

**隔离 PG 集群** `D:/gx-deploy-91/.codex-tmp/pg-circle-candidate-20260919`：

- 2026-09-20 00:34:48 PDT **异常终止**：`server process ... terminated by exception 0xC0000142`
  → `CreateProcess() call failed ... (error code 1455)` → `aborting startup due to startup process failure`
  → `database system is shut down`。**不是正常 `pg_ctl stop`。**
- **崩溃原因（推断，未独立验证）**：崩溃发生在 07:33:25Z 启动 jest 后约 83 秒；
  `0xC0000142`（DLL 初始化失败）与 `1455`（提交限制不足）均指向**系统资源／提交限制耗尽**，
  而非数据损坏。当时本机另有多个 node 进程在运行。
- 当前状态：无 `postmaster.pid`，55432 无监听，**无残留本任务进程**。
- **本轮未重启，未重复已失败的启动命令。** 数据目录原样保留（含 `cand_full` / `cand_baseline`
  两库，为交付说明 §3.1 记录的复现环境）。确需验证时应新建独立合成测试库。

**其他进程（身份不明，未结束、未干预）**：
`postgres.exe` 7 个，均为 Services 会话 —— 为本机 PostgreSQL 服务（默认端口），
**不是**本任务的 55432 隔离集群；`node.exe` 6 个，Console 会话。
