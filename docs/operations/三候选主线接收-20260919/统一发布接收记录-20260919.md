# 三候选统一发布接收记录（2026-09-19）

## 接收身份

- 污染主工作区实际状态：`release/integration-20260801`，HEAD `2e458597a80076ba2728e9ab2a3156bf78a3d0e3`，核验时有 378 个受跟踪改动和 237 个未跟踪项。未清理、stash、暂存或提交其中任何内容。
- 隔离接收工作树：`D:/gx-deploy-91/.worktrees/receive-integrated-20260919`。
- 接收分支：`receive/integrated-three-20260919`。
- 接收基线：`d65e39f493defb086bd7dd6a934992adad2dbed3`。
- Claude Code 完整候选：`420eeeb348f56941b60aab8e194601ff76b7ffad`，相对基线 12 个提交。
- 环境适配提交：`5d2130ae7cd58d116d2e44395a99ca26e1dfa2c4`。
- 业务边界收口提交：`d874987444f382177c3b402c525f9a8defd26dcf`。
- 正式域名构建门禁修复及最终代码提交：`1bce152fe044982fb984db7427aa9422ea78b6a1`。
- H5 新域名构建门禁适配及发布记录提交：`885b81d16622572e0a9ff4d999c7d033511400a8`。

## 实际接收范围和保护结论

已接收：圈子与直播体验优化、用户反馈管理后台 C、通用 AI 场景准入和参数边界。B/D 继续冻结，A 未启用，未接入新开源平台。

逐提交接收保持了 `ai-gateway.module.ts` 的既有注册、管理端 API/路由和移动端详情页的同线历史，没有整体覆盖这些文件。OBS 在活跃线已有等效提交 `fb027858`，未重复接收旧提交 `2e458597`。

## 本轮新增适配

环境核验确认通用限流守卫的基础设施 IP 和用户白名单会先于 userId 计数放行。为避免反馈明文接口因部署拓扑或内部来源绕过附加限流，本轮新增专用 `SensitiveRedisThrottleGuard`：

- 联系方式、正文原文、截图三个明文接口强制按已认证用户计数；
- 基础设施 IP 白名单和用户白名单均不能跳过该计数；
- 其他登录、验证码、AI 等原有严格限流行为不变；
- 新增两项单元验证，覆盖内网 IP 和用户白名单场景。

同时新增可复用的两节点只读审计脚本；脚本不读取业务行数据和密钥，不执行写入。

用户继续推进后，按报告中的推荐方案完成业务边界收口：

- 客服可按需查看联系方式和正文原文，逐次审计；截图仅 `SUPER_ADMIN`、`OPERATION_ADMIN` 可看，管理端同步隐藏客服截图入口；
- `general_chat` 改为仅 `SUPER_ADMIN`、`OPERATION_ADMIN` 可调用，普通用户请求在供应商调用前返回 403；专用 AI 场景不受影响；
- 本次发布继续使用现有正式域名，`gx.yrydai.com` 不纳入本批切换。

## 构建与 Prisma 客户端来源

- schema SHA-256：`38f5f5a95010dc4ef924072099e7d18922952d983fd0e50945783943c270109c`。
- 在隔离工作树内执行 `pnpm install --frozen-lockfile`，未向主工作区或多窗口共用 `node_modules` 生成客户端。
- 执行 `pnpm db:generate`，Prisma Client 6.19.3 解析到隔离工作树自己的 pnpm 目录。
- 依次完成 shared、bazi-engine、ziwei-engine、server 构建；本轮适配后重新完成 server TypeScript 完整检查和 `nest build`。
- 正式 `docker/Dockerfile` 在同一 builder 中先执行 `pnpm --dir apps/server exec prisma generate`，再构建 shared、两个引擎、server、admin 和 H5；编译和运行产物使用同一工作目录中的 schema/client。Dockerfile SHA-256：`661fa2940c49d019ae85f4c58e3581f37f16e7bcde05f6662577318f23309e34`。
- 本机没有 Docker，因此最终不可变镜像由国学2受控构建器按同一 `docker/Dockerfile` 生成；没有使用本机类型检查替代目标构建。
- 继续使用现有正式域名时，原客户端门禁会把“本次预期地址”同时误判为旧地址，导致 Docker 构建必然失败；现已修为：显式预期地址及其子路径优先，只有真正切换到其他域名时才禁止旧地址。回归测试 3/3 通过，切换新域名时旧地址残留仍会被阻断。
- 已按两节点核实的公开参数重新构建 admin 与 H5，并通过 Dockerfile 同款地址门禁。产物指纹：server `07d2e5feab575fbbcf7f621c4c7468fab05bc09d4bf3ebb548cdc5aad9ca8999`；admin `2693f54bfa4784c5d0a24a6889d4416ffca119377c98c346bf2ecf7f67f4349c`；H5 `a9ad0447bde9102bc4ffaaaad32a5a20336324472d616f92312469eb08c41896`。
- 最终代码源码包：`artifacts/releases/gx-source-1bce152fe044982fb984db7427aa9422ea78b6a1.tar.gz`，133,268,982 字节，SHA-256 `a5156750c9a91d8f18727206834db56b0ee7346f7181173c9b67e7d2703e5c62`。源码包经临时 COS 对象传入国学2，服务器构建前再次核对大小与 SHA-256，完全一致后才解包。
- 国学2隔离候选目录：`/opt/guoxue/release-candidates/three-1bce152f.TBB9zHWL`；镜像标签 `guoxue-server:three-1bce152f-three-1bce152f.TBB9zHWL`；不可变镜像 ID `sha256:b3486c3892ba584b31eed3cceb8bd595d564b1db5a910334c46ee4eeb7339ebd`。
- 候选运行产物树哈希：server `ba458801d85594bfbb25bbd6ade97fc51b20b428566cc2457990151a0b56033b`；admin `9b576ca9bbd504a9a796e2902375fce3253a085925090737cfc952e148310d8a`；H5 `46dd007164e4a8eb00510f238d0b164078c5e5cc870f8c6fa142b40a8c97461e`。
- 构建证据：TAT invocation `inv-a8ujpv0drc`、task `invt-a8ujpv0drd`，状态 `SUCCESS`、ExitCode 0、Dropped 0。构建仅生成候选目录和镜像，未切换服务、未重启、未迁移数据库、未修改 DNS。

## 状态分类

### 已通过

- 候选拓扑、基线祖先和 12 个提交范围核对；候选初始工作树干净。
- 原候选既有证据：反馈服务层 40/40、真实 HTTP 44/44、后台浏览器 25/25；AI 网关 71/71；圈子直播浏览器 48/48；移动端、管理端类型检查和相关构建通过。
- 本轮：隔离 Prisma 生成成功；server 完整类型检查和构建通过；敏感限流单元测试 17/17 通过。
- 业务边界收口后：AI HTTP/控制器定向测试 44/44、反馈权限元数据测试 3/3；server 与 admin 类型检查通过，server 与 admin 正式构建通过。
- 正式域名构建门禁回归 3/3；使用正式公开参数的 admin、H5 构建及两类客户端地址门禁通过。
- 两节点受控构建能力和公开构建参数只读核验通过，Docker 版本、架构、磁盘和内存满足候选构建要求。
- 国学2目标构建器已完成最终源码校验和不可变镜像构建；TAT 成功、退出码 0、输出未截断，镜像 ID 和三类产物树哈希已固定。
- 两节点健康、零重启；运行 schema 一致；候选需要的数据库列和审计表完整；迁移账本无活动未完成记录。
- `/admin/`、`/api/`、生产 base、API base、`trust proxy`、ValidationPipe 和代理头配置核验完成。
- 两节点服务端没有主机端口映射，公网链路必须经过 Nginx；三个 Host 在节点本地均为 200。

### 既有失败

- 移动端自定义导航安全区仍有 2 项旧失败；原因是审计脚本不理解 H5 条件编译分支，不是本候选引入。按原报告保留，未借本轮修改测试断言。
- 迁移账本保留 1 条历史 rolled-back 记录；当前 active unfinished 为 0，本轮不删除历史记录。

### 未覆盖

- 本轮未重复全仓测试；基线未变化，按要求只复验反馈权限/限流、AI 准入、类型检查及受影响的正式构建。
- 未重新执行完整原生 11 项、真机无障碍/弱网、AI 真实供应商质量、全仓全部套件。
- 未使用真实客服账号验证 nl2sql 角色归属；保持现有拒绝结果，不扩大权限。
- 未执行真实明文查看请求，因为这会产生审计行并属于生产数据访问；代码、HTTP 既有证据和限流单元证据已保留。

### 发布阻塞

无。本批两节点已经按顺序发布并通过观察。`gx.yrydai.com` 的证书/CORS 仍是后续域名切换条件，但现有正式入口 `api.rebugx.cn` 正常，因此不阻塞本批上线。

`gx.yrydai.com` 的证书和 CORS 仍未就绪，但本批明确继续使用现有正式域名，因此不构成本批代码发布阻塞；该域名后续单独验收。

## 最少用户决策

前三项业务选择已按推荐方案落地并通过定向复验；生产发布授权已取得并执行完成。本批没有仍需用户决定的发布事项。后续若启用 `gx.yrydai.com`，需单独完成证书、CORS、微信域名登记和真机验收。

`nl2sql` 无需扩大权限：先核对实际后台账号角色；保持 403 是安全默认值。若保留 `general_chat`，额度门禁作为后续单独事项，不在本轮擅自增加扣费、扣额度或失败回退。

## 发布步骤

1. 锁定本分支最终提交，记录源码、schema、Dockerfile 哈希；确认主线没有新的冲突改动。
2. 在隔离构建器按 `docker/Dockerfile` 生成 Prisma 客户端并构建不可变镜像；记录镜像 digest、server dist 树哈希、admin/H5 index 哈希。
3. 对镜像执行反馈明文限流、AI 场景准入、圈子/直播关键冒烟；不调用真实支付或真实权益。
4. 发布前备份两节点当前 compose、镜像引用和静态目录；数据库本候选无需迁移，仍保留常规数据库备份点。
5. 先更新国学2，核验健康、数据库连接、Redis、`/admin/`、H5、反馈权限/限流、AI 拒绝路径和日志；观察通过后再更新国学1。
6. 两节点哈希一致后验证 CLB 正式域名；若仍使用现有域名，不切换 DNS。`gx.yrydai.com` 只有在证书、CORS、微信域名登记和真机门禁全部通过后才启用。
7. 发布后保留旧镜像和静态备份，记录切换时间、digest 和验收结果。

## 回退方案

- 应用回退：按节点恢复发布前 compose/镜像引用，先回退异常节点并做健康检查，再决定是否回退另一节点。
- 静态资源回退：恢复发布前 admin/H5 备份目录，并校验 index 与入口资源哈希。
- 数据库：本候选不执行迁移，正常情况下无需数据库回退；若发布过程发生任何未计划写入，立即停止并按发布前备份处理，不自动反向迁移。
- 域名：本轮不改 DNS；若后续单独切换 `gx.yrydai.com`，保留现有正式域名入口作为回退路径。

## 生产发布执行结果（2026-09-19）

- 国学2最终镜像：`sha256:b3486c3892ba584b31eed3cceb8bd595d564b1db5a910334c46ee4eeb7339ebd`；备份目录 `/opt/guoxue/backups/three-1bce152f.LvfUmYee`；发布 TAT `inv-c8ukb0g5i6` / `invt-c8ukb0g5i7`，SUCCESS / exit 0 / Dropped 0。
- 国学2发布后观察：TAT `inv-d8ukcugdgv` / `invt-d8ukcugdgw`；镜像正确、healthy、restart 0、三分钟严重日志计数 0，H5/Admin 本机路由 200，数据库与 Redis ready。
- 国学1最终镜像：`sha256:ddc25deaf932e9c1abca442739e0336be44a0f3dc1c3fe72d32830afe6b00be6`；备份目录 `/opt/guoxue/backups/three-1bce152f.60IWokT9`；发布 TAT `inv-a8ukp8g52n` / `invt-a8ukp8g52p`，SUCCESS / exit 0 / Dropped 0。
- 国学1发布后观察：TAT `inv-d8ukqs0mpj` / `invt-d8ukqs0mpk`；镜像正确、healthy、restart 0、三分钟严重日志计数 0，H5/Admin 本机路由 200，数据库与 Redis ready。
- 两节点镜像 ID 因独立 Docker 构建元数据不同，但运行产物一致：server 树 `ba458801d85594bfbb25bbd6ade97fc51b20b428566cc2457990151a0b56033b`、admin 树 `9b576ca9bbd504a9a796e2902375fce3253a085925090737cfc952e148310d8a`、H5 树 `46dd007164e4a8eb00510f238d0b164078c5e5cc870f8c6fa142b40a8c97461e`。
- 两节点静态入口一致：admin index `ed3ed2e7e80b6168f4b6613a85be4e89266d161dc253a245a2f2146ae4232b6e`、H5 index `c92f6181e3ac3c3a3a7eb62221ce3cc4e9b37199fdbb8a985900949c1fe725af`。
- 正式域名经 CLB 连续 6 次 `/api/v1/health/ready` 均返回 200、数据库和 Redis 正常；`/h5/` 与 `/admin/` 均返回 200。
- 发布过程中未执行数据库迁移，未修改 DNS、支付配置或真实订单/权益。

国学2前两次切换因发布校验脚本错误比较“离线相对路径树哈希”和“运行态绝对路径树哈希”而自动回退；两次回退后均只读确认原镜像、Compose、静态入口和健康状态恢复。修正为精确镜像 ID、构建期整树证据、静态入口哈希和运行就绪检查后，第三次切换成功。该过程没有遗留半发布状态。

## 结论

代码接收、业务边界收口、正式构建、目标数据库与代理核验、两节点顺序发布、单节点观察和正式域名 CLB 验收已经完成。本批三候选版本现已上线；现有正式入口保持不变，回退所需的旧 Compose、镜像信息和静态备份均已保留。

## `gx.yrydai.com` 上线准备（2026-09-19）

用户后续明确要求上线前将 H5 切换到 `https://gx.yrydai.com/h5/`。本阶段先完成无中断准备，尚未切换线上入口。

- DNS 已指向现有北京 CLB：`gx.yrydai.com` CNAME 到 `lb-kifcf99d-5v1927jc0wm4nudq.clb.bj-tencentclb.com`，当前 A 记录为 `43.180.212.44`。
- 忽略证书校验时 `/h5/` 可经 CLB 返回 200，说明解析和后端路由已到达；严格 HTTPS 校验失败，当前监听器实际送出的证书只覆盖 `pre-api.rebugx.cn`，不能用于新域名。
- 从 `https://gx.yrydai.com` 向正式 API 发起 CORS 预检时没有返回 `Access-Control-Allow-Origin`。两节点运行配置也一致确认：`PUBLIC_H5_URL` 仍为 `https://api.rebugx.cn/h5/`，`CORS_ORIGIN`、`WS_CORS_ORIGIN` 均未包含新域名，`WECHAT_OAUTH_ALLOWED_ORIGINS` 为空。
- 发现并修复正式构建门禁的适配错误：管理端产物并不保证引用 H5 公网地址，原 Dockerfile 却要求其中出现 `VITE_PUBLIC_H5_URL`，导致新域名构建被错误拦截。管理端现在校验其实际依赖的 `VITE_API_URL`；H5 新域名另由候选构建脚本做精确存在/旧地址缺失校验。
- 客户端产物门禁定向回归 14/14 通过；同时覆盖继续使用原域名、子路径、新域名残留旧地址阻断及产物验真。
- 两节点均已用 `VITE_API_URL=https://api.rebugx.cn`、`VITE_PUBLIC_H5_URL=https://gx.yrydai.com/h5/`、`VITE_PUBLIC_ASSET_ORIGIN=https://static.rebugx.cn` 完成隔离候选构建。运行产物完全一致：server 树 `ba458801d85594bfbb25bbd6ade97fc51b20b428566cc2457990151a0b56033b`、admin 树 `51d2c773433d50924d31b0cd882591727c00a93fbf174f897d37464c8810a43e`、H5 树 `559c538fce402de6bef5655cd6c6624aab41f9fe353561d0ff8518c96211efc0`。
- 国学2候选：`/opt/guoxue/release-candidates/h5-gx-domain-1bce152f.lBBDOA50`，镜像 `sha256:1b5d8bbbbcfb49e16e98d8bba52886fa25f3bbd8fc7205a2b674fb8284211bd6`，TAT `inv-c8umwdg6r8` / `invt-c8umwdg6r9`，SUCCESS / exit 0。
- 国学1候选：`/opt/guoxue/release-candidates/h5-gx-domain-1bce152f.bDhBYnDa`，镜像 `sha256:5f9f6a5a7a94d3a87ed284e32f8d556fc04582dac7ea58a1e4e0a43120fda0d0`，TAT `inv-e8un0j0uqg` / `invt-e8un0j0uqh`，SUCCESS / exit 0。
- 两次构建均未切换服务、未替换静态文件、未重启、未迁移数据库、未修改 DNS。

切换顺序固定为：确认并绑定仅覆盖新域名的有效证书（不替换现有默认监听器证书）→ 更新两节点 `PUBLIC_H5_URL`、CORS、WebSocket CORS 和微信 OAuth 来源 → 先国学2发布并验证严格 HTTPS/API/授权回跳/支付/排盘返回 → 再国学1 → 最后切换数据库 H5 入口并保留 `api.rebugx.cn/h5/` 回退。

第三方排盘继续采用已真机通过的 H5 顶层直达作为上线兜底。完整 iframe 内嵌在安卓微信中仍为空白，尚无证据证明仅靠我方跨域页面可稳定实现，因此不发布未验收方案，也不把它作为新域名基础上线的阻塞项；需要第三方时只提出允许内嵌及 `postMessage` 返回/分享协议这一最小配合范围。

## `gx.yrydai.com` 实际切换结果（2026-09-19）

- 腾讯云免费证书 `aicDhQjd`（TrustAsia C1 DV Free，域名 `gx.yrydai.com`，有效期至 2026-12-13）已作为独立 SNI 域名绑定到北京 CLB `lb-kifcf99d` 的 HTTPS 监听器 `lbl-6gyvkdnt`；原有 `api.rebugx.cn`、`pre-api.rebugx.cn` 规则和证书未替换。
- `gx.yrydai.com` 的 `/` 转发规则已绑定上线目标两节点端口 80，权重各 50：`ins-0sen8yvf`、`ins-n0sqc627`。
- 国学2新域名镜像 `sha256:1b5d8bbbbcfb49e16e98d8bba52886fa25f3bbd8fc7205a2b674fb8284211bd6`；备份 `/opt/guoxue/backups/h5-gx-domain-1bce152f.gPvHC3XR`；发布 TAT `inv-c8uph9gdt3` / `invt-c8uph9gdt4`；观察 TAT `inv-e8upj7g0p6` / `invt-e8upj7g0p7`。
- 国学1新域名镜像 `sha256:5f9f6a5a7a94d3a87ed284e32f8d556fc04582dac7ea58a1e4e0a43120fda0d0`；备份 `/opt/guoxue/backups/h5-gx-domain-1bce152f.7yGJdnSX`；发布 TAT `inv-d8upjvgrxt` / `invt-d8upjvgrxu`；观察 TAT `inv-e8upm1g165` / `invt-e8upm1g166`。
- 两节点均为 healthy、restart 0、数据库和 Redis ready、观察窗口致命日志 0；新域名 CORS 在节点内返回 `204 https://gx.yrydai.com`。H5 index 均为 `f3a9871b26a244c2c987ba041eb51a1d586e5fbf883da84b0fdc5ba94647bc67`，管理端 index 均为 `aba20b898eb45aef89b410f2980e6da1e232bfb6444c3c1ee471a4a1497dad84`。
- 公网严格 TLS 检查通过，`ssl_verify_result=0`；`https://gx.yrydai.com/h5/` 和健康接口均返回 200；从新域名到正式 API 的 CORS 连续 6 次均返回精确允许来源。
- 证书实测 Subject 为 `CN=gx.yrydai.com`，到期时间 2026-12-13（北京时间）；HTTPS 返回 HSTS。CLB 已建立 `gx.yrydai.com` 的 HTTP:80→HTTPS:443、`/`→`/`、301 自动重定向规则，但公网 HTTP 仍在规则前进入腾讯云 WebBlock。腾讯云备案状态接口实测 `GovStatus=true`、`LandedStatus=false`、`AuditTicket=false`、`Ban=false`：工信部备案存在，但腾讯云接入状态未落地。应用生成及分享入口均为 HTTPS，不影响已生成链接；需要完成腾讯云接入备案后再复验 HTTP 301。
- 数据库 `BrandConfig.default` 的 `h5Url` 与旧值一致性校验通过后切换为 `https://gx.yrydai.com/h5/`，原 `websiteUrl` 为旧预发布入口时同步切换；备份 `/opt/guoxue/backups/h5-entry-gx-domain.p1qTKbD6/brand-config-h5-before.json`。Redis 品牌缓存清除后，公开接口已实测两项均返回新地址。
- 两次数据库切换预执行分别因容器标准输入参数和 Prisma 模块工作目录错误在写入前失败；旧值前置校验保证未产生部分写入。第三次执行成功。
- 本次没有执行数据库迁移，没有变更支付商户、订单或权益。旧入口 `https://api.rebugx.cn/h5/`、两节点旧镜像引用和静态备份均保留，可独立回退。

新域名基础链路已经切换完成。微信内网页授权和 JSAPI 支付是否能在新域名完整闭环，仍取决于微信公众平台把 `gx.yrydai.com` 登记为网页授权域名、JS 接口安全域名，并把 `https://gx.yrydai.com/h5/` 纳入 JSAPI 支付授权目录；该外部平台状态尚未取得可核验证据，必须完成真机支付前门禁。腾讯云 HTTP 重定向规则已经配置，待该域名完成腾讯云接入备案后复验。第三方排盘继续使用已验证的顶层直达兜底，完整 iframe 内嵌仍未通过稳定性验收。

## Android 支付与发布准备（2026-09-19）

- Android `1.1.0 (245)` 的支付实现提交 `1320f7f38ffd6de65d863a0a500574cd70f4ab03` 已包含在当前接收分支及已部署服务端/H5 对应源码中。
- 签名 APK 已复核：包名 `com.rebu.apprebu`，构建号 245，大小 30,988,367 字节，SHA-256 `53f5e215b4a0f61a1bb05c15ab5a047c4e04406e6450e0e2fd447c09a0d2f302`，签名证书与 244 一致。
- 真实 Android 设备已通过支付宝拉起并完成 0.01 元付款；平台订单 `3d100e2d-44e8-4a49-aa65-f3bd38ce9313` 为 `PAID`，实付、汇付成功流水和平台入账一致。该证据只证明支付宝支付与到账，不等于支付宝退款、微信 APP 支付或银联已通过。
- 正式发布副本及元数据位于 `D:/gx-deploy-91/artifacts/releases/android-1.1.0-245/`。当前公开版本检查接口对 1.0.8/108、1.1.0/244、1.1.0/245 均返回无更新，说明生产库尚无 ACTIVE Android 版本记录；在上传稳定下载地址并经后台草稿→发布前，现有用户不会收到 245。
- 建议 245 作为可选更新发布（`forceUpdate=false`），更新说明为“新增安卓支付宝支付能力，优化订单支付状态确认与返回体验。”；微信 APP 原生支付和银联继续隐藏，不向用户展示未开通入口。
- APK 已上传到 COS，并通过稳定 CDN 地址 `https://static.rebugx.cn/rebu-1.1.0-245.apk` 公网下载复验：HTTP 200、MIME `application/vnd.android.package-archive`、长度 30,988,367 字节、下载后 SHA-256 与本地冻结包完全一致。COS 临时签名地址不写入版本记录。

### Android 245 发布草稿与管理员引导

- 只读核验确认生产库不存在项目约定的初始管理员 `13800000000`；未运行会写入大量演示数据的完整 seed，仅按当前 `phoneHash`、`phoneEnc`、`PASSWORD/password` 身份规则创建一个 `ACTIVE` 的 `SUPER_ADMIN`。随机强密码只保存于本机受限凭据文件，未写入仓库或聊天记录。TAT：`inv-e8uru0gwh9` / `invt-e8uru0gwha`。
- 已通过正式登录和版本管理 API 创建 Android `1.1.0 (245)` 草稿，记录 ID `0bd1a095-4d7a-4d94-aaf9-8268fbd0d612`，`forceUpdate=false`；公开检查接口仍返回 `hasUpdate=false`，尚未对客户端生效。
- 创建草稿时发现全局 `SanitizePipe` 漏掉 `downloadUrl`，将 `/` 转成 HTML 实体。已按精确旧值前置条件把本草稿修正为 `https://static.rebugx.cn/rebu-1.1.0-245.apk`，并在源码中补充 `downloadUrl` 跳过规则和回归测试（7/7 通过）。修正 TAT：`inv-k8us02guk7` / `invt-k8us02guk8`。
- 发布接口标注 `RedLine.EXTERNAL_PUBLISH`，项目治理要求最终发布必须由真人执行；自动化已如实标记执行者类型，没有绕过红线。剩余动作仅为超级管理员在版本发布台核对草稿后点击“确认发布”，随后复验公开检查接口与下载包。
