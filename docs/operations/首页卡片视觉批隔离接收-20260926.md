# 首页卡片视觉批隔离接收（2026-09-26）

- 接收基线：`1bd1ad30808461caa13546bbf2e9f11feac10eaa`，独立分支 `codex/home-card-receive-20260926`；根工作区及四窗口组合未修改。
- 来源：`codex/home-feed-card-refresh-20260922` 的独立提交 `764f96556`。该提交原先不在四窗口组合祖先链，且组合首页仍为 18rpx 双列间距，课程/商品卡仍为旧内容。逐文件核对后无冲突接收 5 个文件；不整支合并商城课堂候选。
- 结果：首页瀑布流横向/纵向间距 18→12rpx、页面边距 24→16rpx；课程卡把封面、简介、讲师、价格收在同一内容面；商品卡首图 1:1→4:3，去掉无来源的固定销售钩子，缺价不显示假 ¥0；通用卡补键盘焦点可见状态。保留当前候选的其他页面与业务逻辑。
- 构建环境：依赖仅在本独立工作树 `pnpm install --offline --frozen-lockfile`，Prisma 6.19.3 只生成于本树 `node_modules`。公开构建参数为 `formal`、API `https://api.rebugx.cn`、H5 `https://api.rebugx.cn/h5/`、静态资源 `https://static.rebugx.cn`；这些仅是编译目标，不证明入口版本。
- 新运行验证：`pnpm build:prerequisites`、移动端 `vue-tsc --noEmit`、H5 构建通过；微信小程序源码构建通过，624 个 WXML、2719 个文本产物检查通过，保守上传估算主包 1.92 MB、分包最大 `pkg-paipan` 1.67 MB。`git diff --cached --check` 通过。第一次直接运行移动端类型检查因本树尚未构建 shared 声明而失败，补建前置后通过；第一次小程序编译因本树缺公开配置而停止，显式传入上述参数后通过。失败均非本批源码错误。
- 仍未覆盖：微信官方 preview 的实际包量、320/375/430px 视觉截图、长标题/缺图/真实商品与课程数据、系统大字/深色模式、App/微信真机触控。这批仅为**隔离接收候选**，未合入正式发布线、未上传小程序、未部署。正式总组合若再新增 UI 代码，须重做包量与视觉复核。
- 撤回：本批独立提交可单独撤回；无数据库或资金变更。
