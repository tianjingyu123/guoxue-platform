# 古籍导入质量门槛隔离接收（2026-09-26）

在 `codex/release-safety-receive-20260926` 隔离工作树，接收原候选 `b5403aeba` 中被四窗口组合遗漏的古籍导入质量部分。当前组合的 TTS `speech-text.ts` 与相关朗读调用已经等效在位，因此只补 `classic-content-quality.ts`、单测和代治阁导入器调用；`tts.service.ts` 冲突时保留现有源码，不重复覆盖。根工作区未修改。

导入时对无章节、短文待核底本、空章节设置 P0，先保存为草稿待审；控制符/私用区字符与单章长篇列 P1 提示。已有古籍记录不会因本代码自动回写状态，原文也不会被修改。该门槛只在导入器运行时生效，不能误称全部存量古籍已完成质量审校。

隔离验证：`classic-content-quality.spec.ts` 与既有 `speech-text.spec.ts` 两套 6 项通过；服务端 `tsc --noEmit` 通过。未执行批量导入、外部 TTS 调用或生产内容变更。发布前仍须用受控种子数据核对草稿数量、版权/出处以及管理端审核路径，避免把短篇合法古籍误拦后无处处理。
