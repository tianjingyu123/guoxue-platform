# 本窗口 AI 推荐与知识星图隔离交接（2026-09-23）

## 状态与边界

- 隔离工作树：`D:/gx-deploy-91/.worktrees/ai-guide-intent-20260922`；分支：`codex/circle-knowledge-universe-3d-20260923`；本窗口最后功能候选：`cf79b5d6d`。未合入主线、未部署、未执行平台数据库迁移、未回填真实内容。
- 本窗口可独立完成的候选代码、模拟测试和交接已收口。**不等于全平台 AI 已完整上线或具备发布条件**。旧课程清单尚未取得，不编造映射或执行上架；直播礼物项目按用户此前指令保持停工。
- 本轮新运行与复用历史证据分开记：`cf79b5d6d` 的跨页关系选点定向 Jest 2 套件 13 项、后台相关 Vue 文件 ESLint `--quiet` 0 error；普通搜索缓存边界相关 Jest 实际匹配 5 套件 60 项、服务端变更文件 ESLint `--quiet` 0 error；本地浏览器点选/拖动演示星图及控制台检查记录于《圈子知识宇宙3D选型与样例》。前期测试结果见各自交接，不算本次重跑。
- 本轮服务端 `tsc --noEmit` **未通过**，仍有共享排盘包模块解析等全局错误；按路径过滤未见 `src/modules/search/` 或 `src/modules/circle/` 错误。后台 `vue-tsc` 在此工作树无可执行文件，未取得完整构建证据。真实库、真实模型、Android 真机、微信小程序及正式 H5 均未因本轮测试而获得验收结论。

## 主线待安排的验收

| 项目 | 执行入口与前置条件 | 通过标准 | 证据保存位置 |
| --- | --- | --- | --- |
| AI 短答、字号、图文与导览 | 评审本分支及《全平台AI回答体验优化》《AI导览意图边界交接》；准备固定匿名问题集、受控测试账号、匹配构建和真实可打开内容 | “你好”等简单问题短答；古文解释先给白话结论、复杂问题有分层；文字在目标设备可读；内容卡来源准确且点击可达，不把圈内/私密内容推给圈外 | `artifacts/ai-service-acceptance/`，分别标注模型响应、H5、Android、微信及构建 SHA |
| 公开推荐目录与搜索撤权 | 先评审 `eadd0fffb`、`41b2880c1`、`cf79b5d6d`；用 `apps/server/scripts/audit-public-catalog-readonly.ts` 的 `--read-only` 入口，在核对数据库/账号身份后接入**只读**受控库；迁移/回填须另经主线评审 | 七类公开样本的准入、点击目标和分站隔离准确；私密、待审、删除、已撤内容不出现；撤权后普通搜索不复用旧 Redis 结果；同样本 Precision@4、空结果率、p95 搜索延迟与数据库负载有前后对比，不达标不能采用无缓存方案 | `artifacts/ai-content-catalog/eligibility/`、`artifacts/ai-content-catalog/evals/`；记录脱敏环境、查询集、SHA、耗时，不保存凭据或原文 |
| 圈主助理知识采集与公开星图 | 评审快照迁移、审核责任和内容授权；在隔离库准备测试圈子、公开/私密来源、管理员与普通账号；使用后台圈子详情“知识库 → 星域审核”和 `GET /circles/:circleId/knowledge-showcase` | 重复/闲聊内容不自动入知识；来源改动或撤回后快照不可公开；跨圈、未授权、草稿、失效来源不进入圈外接口；审核列表翻页、跨页选点和撤回可用。演示星图只按真实获授权的精选知识点接入，不把原文直接送出 | `artifacts/ai-service-acceptance/knowledge-showcase/`，留脱敏 ID、请求响应、审核/撤回记录和素材授权依据 |
| 图谱及 AI 多端性能 | 先解决完整构建阻断，准备与候选 SHA 匹配的 H5/Android 测试包和微信测试版本；使用受控数据与设备，不覆盖现装正式包、不清数据 | Android nvue 与微信端有可用静态/2D 降级，H5 3D 点选/拖动正常；首屏、掉帧、内存、切后台与退出清理有实测；重复 Three.js 警告在正式打包中消除或解释 | `artifacts/ai-service-acceptance/knowledge-showcase/`，记录设备、系统、基座、构建、网络、截图/录屏、指标 |
| 旧课程复用旧主体点播 | 用户取得旧课程与媒资对应清单、确认虚拟圈主/圈子；先以只读方式核对新旧账号身份、旧资源播放与签名规则，再明确授权导入范围 | 一门受控课程的章节、媒资、播放/权限和归属映射正确；播放仍在旧账号资源下，未误迁移或重复上传。未取得清单前停在方案/代码核查 | `artifacts/old-vod-course-pilot/`，只存脱敏映射和授权记录，不存密钥 |

## 主线接收和撤回

1. 先逐提交评审，再在受控环境验证；目录迁移与星图快照迁移均默认未执行，不得因合并源码自动推断数据已准备好。
2. `cf79b5d6d` 关闭普通搜索结果缓存，优先保护撤权正确性，但会增加数据库查询。主线若负载不达标，应评审可靠的按内容变更失效方案；不要直接恢复 120 秒旧结果缓存。未发布前撤回此提交或相关文件即可；若已发布，应走正常回滚流程并核查缓存/代理状态。
3. 不在此窗口部署、真实点播上架、生产圈子知识公开、合入礼物 L3 或操作真实付费。正式验收和发布由主线统一决定。
