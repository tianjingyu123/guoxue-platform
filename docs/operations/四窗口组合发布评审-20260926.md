# 四窗口组合发布评审（2026-09-26）

此分支 `codex/four-window-release-review-20260926` 是**隔离评审版本，不是正式发布线或线上版本**。从四窗口组合 `66e395efdc579807bc40e07e43b5c103e12bf7fb` 起，先接入独立迁移顺序修复 `9c0ce965e4ac6c7edbeceb88ee8bab6bc1092e91` 及两项静态审计规则修正，形成 `83d97cdc5796526889a0f2b6b4732cbfe9f261e8`；再以无冲突双亲合并提交 `03b531583a2f13bf59f31d0ceb8a9b633e130ed1` 接入 AI 试用额度独立评审 `c5fa5d14e2e75a96d0e47d51c90f20eb1f2692f0`。两项修复仍有独立提交与撤回点。原候选和根工作区未改。

本组合按最终源码 schema 在**本隔离工作树**生成 Prisma Client 6.19.3，并以同一工作树完成 shared、bazi、ziwei 包构建，服务端类型检查及 Nest 正式构建均通过。新运行的 Bot/旧排盘服务端测试 64/64，通过排盘承接与迁移顺序 Node 定向测试 24/24；旧系统迁移/排盘静态门禁 7/7，多端能力静态门禁 12/12。原组合的 H5/小程序完整构建与 204 项测试为 `66e395efd` 的历史证据；本批仅改服务端额度、迁移目录及审计文档/脚本，不把历史构建写成本批重跑。

迁移修复分支另用目标库**只读**账本提取的 103 个已完成名称，在本地仓库 baseline 上模拟账本，Prisma 迁移引擎执行余下 17/17 并报告本地 schema 最新；这是隔离预演，不是目标库快照。AI 分支另用本地 PostgreSQL 和真实 Prisma Client，对最后 1 次试用并发发起 20 请求：没有已购额度时 1 次试用、19 次拒绝；有 5 次已购额度时 1 次试用、5 次已购、14 次拒绝。详情见两个独立评审文档。

**目前不具备直接发布条件。** 北京两节点仍运行旧版本且镜像 SHA 不同；目标库 17 条候选迁移尚未执行，须取得目标库一致性快照，检查实际历史数据、`Order` 非并发唯一索引的锁时长与回退，再单独批准迁移和两节点固定包发布。外部语音 bridge、真实硬件、支付/退款渠道、App/微信 H5/小程序真机、新旧排盘授权及分享、正式公开域名配置仍待相应环境验收。不可把静态门禁和本地预演写成线上通过。

来源排盘分支新提交 `9d461be3f`/`789074857` 的“新旧并存主动入口”未纳入本组合：它基于旧版 `PAIPAN_LEGACY_MODE` 语义，与本组合正式 `PAIPAN_MODE=legacy`、新排盘仅预发布 QA、小程序暂缓开放的既定门禁不一致，须单独适配和决策。退回本组合新增 AI 额度补丁可先撤合并 `03b531583`；迁移目录已部署后不可仅靠 Git 反向提交回退账本，应保留新增可空结构并回到兼容应用，具体回退见迁移评审。全程未部署、未迁移目标库、未动真实权益或付费。
