# 四窗口组合发布评审（2026-09-26）

此分支 `codex/four-window-release-review-20260926` 是**隔离评审版本，不是正式发布线或线上版本**。从四窗口组合 `66e395efdc579807bc40e07e43b5c103e12bf7fb` 起，先接入独立迁移顺序修复 `9c0ce965e4ac6c7edbeceb88ee8bab6bc1092e91` 及两项静态审计规则修正，形成 `83d97cdc5796526889a0f2b6b4732cbfe9f261e8`；再以无冲突双亲合并提交 `03b531583a2f13bf59f31d0ceb8a9b633e130ed1` 接入 AI 试用额度独立评审 `c5fa5d14e2e75a96d0e47d51c90f20eb1f2692f0`。两项修复仍有独立提交与撤回点。原候选和根工作区未改。

本组合按最终源码 schema 在**本隔离工作树**生成 Prisma Client 6.19.3，并以同一工作树完成 shared、bazi、ziwei 包构建，服务端类型检查及 Nest 正式构建均通过。新运行的 Bot/旧排盘服务端测试 64/64，通过排盘承接与迁移顺序 Node 定向测试 24/24；旧系统迁移/排盘静态门禁 7/7，多端能力静态门禁 12/12。原组合的 H5/小程序完整构建与 204 项测试为 `66e395efd` 的历史证据；本批仅改服务端额度、迁移目录及审计文档/脚本，不把历史构建写成本批重跑。

迁移修复分支另用目标库**只读**账本提取的 103 个已完成名称，在本地仓库 baseline 上模拟账本，Prisma 迁移引擎执行余下 17/17 并报告本地 schema 最新；这是隔离预演，不是目标库快照。AI 分支另用本地 PostgreSQL 和真实 Prisma Client，对最后 1 次试用并发发起 20 请求：没有已购额度时 1 次试用、19 次拒绝；有 5 次已购额度时 1 次试用、5 次已购、14 次拒绝。详情见两个独立评审文档。

**目前不具备直接发布条件。** 北京两节点仍运行旧版本且镜像 SHA 不同；目标库 17 条候选迁移尚未执行，须取得目标库一致性快照，检查实际历史数据、`Order` 非并发唯一索引的锁时长与回退，再单独批准迁移和两节点固定包发布。外部语音 bridge、真实硬件、支付/退款渠道、App/微信 H5/小程序真机、新旧排盘授权及分享、正式公开域名配置仍待相应环境验收。不可把静态门禁和本地预演写成线上通过。

来源排盘分支新提交 `9d461be3f`/`789074857` 的“新旧并存主动入口”未纳入本组合：它基于旧版 `PAIPAN_LEGACY_MODE` 语义，与本组合正式 `PAIPAN_MODE=legacy`、新排盘仅预发布 QA、小程序暂缓开放的既定门禁不一致，须单独适配和决策。退回本组合新增 AI 额度补丁可先撤合并 `03b531583`；迁移目录已部署后不可仅靠 Git 反向提交回退账本，应保留新增可空结构并回到兼容应用，具体回退见迁移评审。全程未部署、未迁移目标库、未动真实权益或付费。

额度正确性补查发现一个**既有但仍未解决的付费 AI 发布阻塞**：`BotService.chat` 在调用模型前先 `consumeQuota`，模型抛错时没有额度释放；`precheckChat` 同样先扣，`chatStreamRich` 的 source error/客户端取消也没有释放路径。当前并发补丁只防超发，不解决“未得到回复却消耗试用或已购次数”。本轮不以无审计记录的补偿更新临时改真实计费语义；发布含收费智能体前，须单独实现并发安全、可幂等的预留/成功确认/失败释放，并覆盖模型报错、空回复、流式首字前断线与重试。免费智能体、会员免扣路径不受此额度扣减缺口影响。该结论来自源码入口/异常路径审查，尚无真实模型故障注入测试。

## 正式公开配置下的本地构建

本隔离工作树只从主工作区 `apps/mobile/.env.production` 复制四项**公开**配置：`VITE_RELEASE_CHANNEL=formal`、`VITE_API_URL=https://api.rebugx.cn`、`VITE_PUBLIC_H5_URL=https://api.rebugx.cn/h5/`、`VITE_PUBLIC_ASSET_ORIGIN=https://static.rebugx.cn`。这些值符合当前构建器的 formal 域名校验；没有复制 `docker/.env.production` 或任何服务端密钥。它们是构建目标值，**不证明 DNS/CDN 最终回源到新两节点**；本机 DNS 当前对 `api.rebugx.cn` 与 `gx.yrydai.com` 均解析到同一个 CDN 地址，无法仅凭 A 记录判断源站版本。

在最终组合源码 `1b0dcc37462e63b4c3b96fb64eb8ead52a3938ed` 上重新执行 H5 与微信小程序完整本地生产构建，均退出码 0；小程序后处理搬移 71 个共享组件、改写 121 个页面配置，624 个 WXML 和 2717 个文本文件校验通过。主包 1.64 MB，保守上传估算 1.92 MB、余量 0.08 MB；33 个分包，最大 `pkg-paipan` 1.67 MB。产物扫描未发现 `api/v1/api/v1` 或先前占位域名。H5 `index.html` SHA-256 `b6c4f4562baa5d81522a00d6122d9f16a35ddf65165823191f28c289520b6fe`；小程序 `app.js` 为 `0b955b289011e107c579c4888d4e824d892c075bc6f9c5b2c7aced103a225d62`，`app.json` 为 `3ef9bc7fff6251e22b37fb6f88e6664e6330d48d1e9a42c81bdd498faa972362`。小程序构建日志留在本机 `%TEMP%/rebu-mp-four-window-formal-20260926.log`，H5 构建输出未发布。微信开发者工具上传口径、审核、真机和实际 CDN 源站仍未覆盖。

同一源码与公开配置继续执行 `build:mobile:app`：App 构建退出码 0，内置 `audit-mobile-native-bundle.mjs` 对 `app-service.js` 门禁通过；`build:mobile:app-harmony` 也退出码 0。日志分别在本机 `%TEMP%/rebu-app-four-window-formal-20260926.log`、`rebu-app-harmony-four-window-formal-20260926.log`。因此四端**源码产物编译**均通过；App 原生包尚未云打包、签名、安装到匹配设备，也未进行支付/排盘/语音真机验收，鸿蒙亦未安装。构建成功不等于这些运行时能力通过。

## 圈子付款回跳独立修复

支付回跳定向重跑暴露历史测试旧变量名误报，并确认商城/微信支付的圈子订单在服务端确认已付后仍先去通用成功页，用户需再自行进入圈子。独立分支 `codex/four-window-circle-pay-return-20260926` / `c5815a9c13c3daf9fa5cdb1cd58aba5c3742ad5e` 已只对 `CIRCLE_JOIN`/`CIRCLE_RENEW` 且有目标 ID 的本人已付订单直接进入对应圈子，圈子页继续复核订单归属和权益；普通订单仍去结果页。新行为测试含加入、续费、普通商品，支付相关 30/30，移动端类型检查与正式公开配置 H5/小程序/App 构建通过；小程序主包仍 1.64 MB、保守上传估算 1.92 MB。本组合以无冲突合并提交 `fbb55a04f698304ad35b1d6fa750277d4b67d38b` 接收该批，支付源码和测试文件与独立分支字节一致；本组合再跑 30/30。原先四端构建数字对应合并前源码，此次 H5/小程序/App 数字对应付款修复源码，Harmony 仅有合并前构建证据。真机支付、回调延迟与权益确认失败恢复仍未覆盖；修复详情见 `docs/operations/圈子付款直接入圈隔离评审-20260926.md`。
## 追问包购买 HTTP 入口补验（2026-09-26）

在隔离候选 `8ce95f8d2d2365c6c5f7a5c7a461ec6ebdc831b4` 上，补了实际 Nest `POST /bots/:id/purchase-uses` 入口测试。测试使用本机专用 PostgreSQL 库、真实 `CoinService.spend`、全局 `ResponseInterceptor` 与 `ValidationPipe`；只替换登录守卫以注入合成用户，不连接真实支付渠道。带同一个 `requestId` 连续请求两次，均返回首次购得的 10 次权益，数据库仅生成一笔购包流水、一笔扣币流水，余额只减一次；两个不同合成用户使用同一请求号时，各自独立扣币和发放，未串户。与原有同号 6 并发和失败回滚测试合计该文件 4/4 通过，服务端 `tsc --noEmit` 通过。验证不涉及真实用户、生产数据库或线上两节点；老客户端不带请求号的重试边界依旧存在。目标库迁移、正式部署和受控账号验收仍是发布门槛。

## 最终候选完整迁移顺序补验（2026-09-26）

在本机 `127.0.0.1:5433` 新建专用库 `bot_quota_qa_20260926_full_19`，导入仓库 `full-baseline.sql`；仅从目标库既有只读 TAT 结果提取 103 条“已完成且未回滚”的迁移名称，并用本候选对应 `migration.sql` 的 SHA-256 在**本地**模拟账本。最终源码共 122 个目录，103 个模拟已完成、19 个待执行，已完成但本地缺目录为 0。以本隔离工作树的 Prisma 迁移引擎运行 `migrate deploy`，19 条按目录顺序全部成功；`migrate status` 显示 122/122、schema 最新。两张收费新表与订单重试键两列均在。迁移日志留在本机 `%TEMP%/rebu-bot-full19-migrate-deploy-20260926.log`，SHA-256 `134bc17a17d484bb6448e62ed803b19d72a6f6a2c5e074d6930ee50d1c940a55`。随后在这一完整升级库复跑额度 PostgreSQL/HTTP 与购包 PostgreSQL/HTTP 测试，共 **14/14** 通过，合成测试用户与币账由用例清理。该库不含目标库真实数据或一致性快照；不能据此证明生产索引锁、历史约束、双节点发布或真实扣费安全，仍须单独审查与授权正式迁移。
