# 排盘分端维护与超级管理员预览

## 最新用户澄清：整套模式切换（优先于下方历史阶段记录）

- 后台只管理整套第三方/自研模式，不建立单工具权限、工具白名单或逐工具开关。
- 第三方模式下普通用户所有入口统一进入第三方体系；有效超级管理员可通过一个独立预览开关测试整套自研。
- 显式选择自研模式后，整套自研面向普通用户按原有登录/业务权限使用，不再要求超级管理员身份。模式不自动切换，第三方异常不回退自研。
- 小程序维护为独立平台限制；不得全局关闭其他端。原生罗盘等第三方入口依赖的共用能力不能被误归为自研专属入口。
- 页面已有账号数据清理、异步响应隔离不得机械删除；改为消费同一套策略，不再以逐个页面接入数量作为权限功能完成标准。
- 当前核验：后台仅有开发预览开关，正式模式仍取环境变量；因此一键切换尚未落地。新增统一策略与单测只建立明确行为，尚未将公开自研授权接入运行守卫，不代表现在已开放。
- 后续需同一配置读源贯通后台事务保存/审计、入口探针、整套路由与服务端守卫，并用普通用户验证 legacy→native→legacy；没有端到端证据不得宣称完成。

以下为此前阶段记录；与上述最新澄清冲突时以上述为准，当前线上状态不因文档或本地策略发生改变。

### 整套配置事务接线进度（本地，未部署）

后续接线：后台现有页面已增加整套模式选择、明确影响说明、确认期间目标冻结；只修改模式也会提交。运行入口从主库读取模式，只有配置尚未建立时兼容 PAIPAN_MODE；主库查询失败或损坏值返回503。统一 NativePaipanGuard 使用单条主库语句同时核对模式与账号：native允许当前有效账号，legacy仅允许环境合格且主库预览开启的有效超级管理员。兼容旧路径的客户端资格探针已改用同一个总守卫，Station模块依赖已补齐；没有增加单工具权限。

新增验证：运行时/分站入口/真实HTTP合盘守卫（数据库替身）30/30；后台脚本交互6/6；server与admin类型检查通过。真实PostgreSQL7/7已扩展验证普通用户native放行、停用拒绝、切回legacy拒绝、管理员预览、错误配置拒绝；本轮隔离库 `circle-workflow-pg-H7fZFB/data`、端口55957已停止。没有上线写配置，没有设备安装。

仍待：后台真实浏览器交互、整套路由与直链统一边界收口（停止逐工具授权扩展）、第三方原生罗盘共用入口解耦、最新多端构建及真机。已有逐页钩子只是同一策略消费者，不能据此宣称所有直链或本地工具已收口。

专用设置服务已增加 `paipan.native-preview.mode`，与预览 enabled 同事务保存、同版本指纹、同私有审计。沿用私有前缀，不允许通用配置接口绕过；旧客户端不传 mode 时保留当前模式。DTO 拒绝工具编号等非 legacy/native 值。后台页面及运行入口尚未接完，不得单独发布此中间状态。

验证：服务/策略/通用配置隔离 15/15；server TypeScript 通过；真实本地 PostgreSQL 16.13、356 表 7/7，覆盖两个开关审计失败一起回滚、并发仅一个成功、撤权拒绝、整套模式往返与独立连接读取。HTTP 身份入口仍为合成替身，不替代真实登录测试。隔离库 `artifacts/local-qa/circle-workflow-pg-7xrGYa/data`、端口54890，已停止，保留合成数据；没有连接预发布或生产。

第三方排盘依赖的原生罗盘已补本地监听注册异常兜底与停止后迟到读数拦截，回归修复前2项失败、修复后2/2通过；第三方分享/支付/返回专项55/55通过。以上不是手机实测，苹果投屏恢复一次后仍报最小化，未重复操作、未发新包，罗盘真实方位/分享卡片/购买栏仍待真机验收。

## 必须完成

- 仅微信小程序暂停排盘；App、公众号、H5 既有入口不全局关闭。维护文案：排盘工具维护升级中，当前暂仅向 App、微信公众号及 H5 用户开放。如有疑问，请联系智能客服。接现有智能客服。
- 新工具仅有效超级管理员且后台私有预览开关开启时可用，默认关闭。关闭仍能管理开关，但不能使用工具。
- 后台菜单、配置、进度及接口、通用配置列表/历史/预览均需服务端隔离；不得下发公共配置。
- 客户端标识不是权限凭据；验证退出、撤权、关闭、缓存，不允许残留授权。
- 未来正式开放仍保留既有工具入口，历史互通未实现则不得宣称。

## 当前第一步

PaipanRuntimeService 已强制 SUPER_ADMIN，旧用户/普通管理员白名单不再提升权限；NativePaipanGuard 移除 native 模式免鉴权放行。专项7/7通过；首次测试类型错误已修正并重跑通过。仅本地代码，未改线上。

仍未完成：后台数据库私有开关管理/实时鉴权、所有配置通道防泄露、前后台页面/路由、MP维护页面、跨端回归。当前仍使用原环境 QA 开关及域名白名单作为额外限制，不能称后台双条件已全部实现。不得单独发布此中间状态。

## 第二步：主库双条件已接入

两个新工具守卫均在登录与原 QA 环境限制后调用单条主库查询：账号 ACTIVE/未删除、UserRole=SUPER_ADMIN 且 bindId=null、ConfigSystem `paipan.native-preview.enabled` 值严格 `true`。缺失、false、非规范值、数据库异常、无登录均拒绝，不缓存。参数绑定，不采信客户端角色或平台字符串作为数据库授权。

真实本地 PostgreSQL16.13/356表双条件贯通用例通过，覆盖角色降级/范围绑定、开关关闭及损坏、账号停用；server tsc通过。隔离目录 circle-workflow-pg-rwi75H/data、localhost:52698 已停止。

后台管理端点/页面、通用配置泄露隔离、MP维护、客户端缓存仍未完成；当前禁止将这枚配置写入线上或单独部署。未来私有管理入口需允许超级管理员在开关关闭时管理，而使用端继续404。

## 第三步：私有管理服务与配置隔离（本地）

- 通用配置列表（含已有缓存）、公开批量读取过滤 `paipan.native-preview.`；单键读写/删除、历史按键/按ID、差异及回滚拒绝该前缀。专项4/4。专用管理不调用这些通用方法。
- 新专用 `GET/PUT system/native-paipan-preview`，不列入公开API文档，响应 private/no-store。身份由现有JWT入口验证，服务在主库再次核对账号 ACTIVE/未删除和无业务绑定的 SUPER_ADMIN；开关关闭不影响管理。PUT保留 COMPLIANCE 真人红线。
- 保存使用客户端必填64位版本指纹、Serializable事务及专用事务锁。当前身份行共享锁与开关/私有ConfigVersion记录同事务；撤权、并发冲突或审计写入失败不返回成功。历史版本计入指纹，避免关闭再开启导致旧指纹复用。
- 私有变更记录保存在ConfigVersion（通用历史不可见），不写入通用操作日志中的配置快照。尚未实现私有历史展示界面。

真实 PostgreSQL16.13/356表验收6/6：默认关闭和实时资格、开关及台账、旧指纹409、撤权/停用、两管理员并发只成功一次、台账失败事务回滚；其中一个贯通用例使用真实Nest HTTP/DTO/RedLineGuard/服务/数据库，验证400/403/404/409/no-store。其JWT入口是合成身份替身，不能作为真实登录或Token撤销的最终证据。

隔离库 `circle-workflow-pg-cpR40V/data`（52300，首轮5项）与 `circle-workflow-pg-DNY8W3/data`（52770，HTTP扩展6项）均已停止，保留合成数据。未读取线上数据库、未部署、未线上迁移或写开关。

仍未完成：专用后台管理页面/菜单及进度数据隔离、小程序维护与客服入口、客户端缓存及跨端真实登录/退出测试；原QA环境/域名额外限制仍在。不得把本阶段当成完整上线验收通过。

## 第四步：后台专用页面与导航接线（本地）

新增“排盘开发预览设置”：显示主库确认状态、明确仅超级管理员与非正式发布范围、二次确认保存、并发/权限错误清空旧状态并要求重新加载。沿用后台朱砂主色、白色卡片、既有字阶和窄屏折行按钮，不引入新视觉体系。页面尚未做真实浏览器视觉验收。

后台新工具导航不再读取公开运行模式，而调用既有 `legacy-paipan/native-qa/access` 主库资格探针；专用管理路由独立核验管理资格，关闭不阻断管理。菜单常规和兜底路径均过滤新工具，普通管理员不发起使用资格探针；专用设置菜单也实时核对服务端管理资格。菜单请求换账号后不回填，预览响应换账号/退出清理后不回填。

真实SFC脚本执行测试4/4（依赖使用替身）：确认保存及菜单刷新、取消/失败、确认期间防重复与目标锁定、换账号和退出丢弃旧资格。后台 vue-tsc通过。未替代浏览器、真实JWT撤权、各端设备测试；小程序维护/客服、进度数据全入口隔离仍需继续。

## 第五步：微信小程序维护构建闭环（本地）

- 微信排盘首页仅编译维护文案、智能客服和返回首页/底部导航，不执行原排盘入口请求。
- 微信构建不登记 pkg-paipan/pkg-paipan2/pkg-paipan3、嵌入排盘页及排盘案例“我的投稿”页；其余端仍登记。统一路由、历史直接调用拦截及页面不存在深链处理都归到维护页，商城/圈子/内容发布不变。
- 首轮产物发现“我的投稿”跨包引用仍带入 case-data，已将该排盘专属页面纳入裁剪，未删除普通内容投稿。产物门禁增加维护模式不得声明或残留排盘分包/嵌入页，不降低体积或文本标准。
- 本地微信完整构建 Exit0：WXML461文件，文本1980文件，主包1.50MB，保守上传估算1.78MB，30个分包；维护分端/路由测试3/3，mobile vue-tsc通过。最初沙箱spawn EPERM后使用获准的本地沙箱外构建；类型检查发现BottomNav缺active，已补齐再通过。

这只证明本地编译与路由策略，尚未上传体验版、未做微信开发者工具和真机维护页/客服点击验收；App/H5仅做页面声明保留测试，尚未重跑本轮完整构建。客户端平台标识不是服务端权限依据；新工具仍由服务端主库双条件守卫保护。

## 第六步：非微信入口不信任公开模式授权（本地）

排盘主页三个新工具展示分支（显式nativeQa、运行模式native、入口响应native）统一调用本次私有资格探针；无登录、网络失败、allowed非严格true均不展示。公开运行模式及十分钟快照只决定入口走向，不授予预览权。页面隐藏时使在途请求失效并清除展示资格，返回时重查；换账号后的迟到响应不回填。

执行真实入口函数的替身测试5/5，配套既有隔离测试6/6（合计11/11），mobile vue-tsc通过。首次测试的异步等待方式错误，改为明确等待探针进入后注入迟到响应，重跑通过。测试身份/API依然是合成替身，不代替真机或真实JWT。

尚需检查工具页自身的直达/页面栈恢复、本地计算引擎页面权限、前后台进度数据及相关列表入口；不能只以主页修复宣称开发预览全链路隔离完成。

本轮本地 App 资源编译 Exit0/DONE（预发布公开配置，仅本地资源，未云打包或安装）。检查还发现 `pkg-paipan2/liuyao/index.vue` 在 onShow 直接读本地排盘历史；`bazi-case.controller.ts` 前台控制器未挂新工具总门禁，后台仅JWT/普通角色门禁。这两处需按开发预览范围继续核对处理，暂不可发布当前中间状态。

## 第七步：案例库API隔离（本地）

前后台案例库两个控制器统一接入 NativePaipanGuard，BaziCaseModule显式注册守卫及Runtime依赖，并从公开Swagger文档排除；原先“公开详情不含答案”只作为资格通过后的第二层保护，不再表示开发阶段公开开放。两个私有预览守卫在成功和拒绝响应上设置 private/no-store及noindex标记。

新增真实Nest HTTP+真实测试JWT签名/校验用例16/16：14条前后台列表/详情/相似/奖励/投稿/断语/揭晓/评分/审核入口，在主库不同意时业务调用0次；未登录、过期JWT、普通管理员失败关闭；主库同意的超级管理员可浏览；主库异常404。数据库与业务为替身，此轮不是实际JWT策略的撤销库验证，也未执行真实案例发奖。

本地工具页直达和本地历史读取尚未处理，继续作为阻断保留；未部署、未触碰线上数据。

## 第八步：本地六爻页面门禁接线（本地）

新增 useNativePreviewPage：页面展示/重新核验/本地修改均要求本次在线私有资格；隐藏和卸载撤销在途响应并清空内存展示，不持久化授权。回调约定为同步本地操作，异步业务不能直接复用它作为事务授权。

接入六爻入口、结果、历史三页：不合格不渲染工具；历史只在核验后读取；置顶/删除/清空前重查；结果页onLoad只接收参数，计算和记录在核验后执行，同一次页面实例恢复不重复保存记录。

门禁实际TS逻辑替身测试4/4：未获权不读写，隐藏/卸载/换账号后迟到响应无效，既有资格不能跳过下一次操作核验；mobile vue-tsc通过。未做三页完整真机操作；其他本地工具页面、旧本地历史的账号归属隔离仍需覆盖，不得据此宣称全部新排盘已隔离或可上线。

## 第九步：六爻历史账号作用域（本地）

私有资格探针在已有主库门禁通过后返回当前JWT主体 subjectId。页面门禁仅在该响应与当前令牌一致时建立同步本地历史作用域，不把账号或授权作为公共配置、页面参数或持久授权缓存。

六爻记录键改为按该账号ID隔离，作用域外读取返回空、修改拒绝；旧共享键 `rebu:liuyao-history` 与 `rebu:liuyao-records` 均不读取/迁入/删除。它们无可靠归属，不能把旧数据自动分配给当前账号。此为新开发工具本地记录，不改变第三方既有系统的历史或支付。

真实历史底座+作用域模块配内存storage验收3/3，配页面门禁4/4共7/7；验证A/B读写清空隔离、旧记录原样保留、作用域结束/异常清理、旧令牌与非法ID拒绝。服务端tsc通过。客户端存储命名空间不是对恶意修改客户端的安全边界，服务端权限仍独立强制。其他工具历史未统一，仍需继续。

## 第十步：六爻实际页面脚本贯通

测试执行实际 result.vue/history/index.vue 脚本，复用实际页面门禁、历史作用域和存储底座；Vue生命周期/网络/存储与计算引擎为合成替身，不是真机。验证onLoad无提前计算和落盘、通过资格后才保存、页面恢复不重复保存、撤权清空展示而非删除数据。

同时修复页面实例跨账号重放：门禁绑定首次通过核验的主体，同一实例更换账号后不再执行旧参数回调，需重新进入页面。结果初始化异常清空params，避免错误数据继续触发计算。页面贯通3/3与门禁4/4合计7/7通过。

仍不能据此判定所有本地排盘页面已覆盖；后续必须盘点全部已登记页面及历史存储调用，补齐遗漏后统一构建和真机验收。

本轮已按 pages.json 三个工具分包的实际声明扫描：83页，当前显式页面门禁仅3页；另36页命中本地历史/直接storage读写特征且未接该门禁。该正则只是定位线索，不意味着其余44页安全，也不能用服务器API守卫代替本地页面保护。mobile vue-tsc本轮通过。下一阶段需以这83页为完整覆盖清单，避免继续仅凭单一工具通过就升级测试包。
