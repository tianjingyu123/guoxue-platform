# 圈子板块全域审计与发布边界（2026-09-22）

本次以当前隔离工作树的 `apps/mobile/src/pages.json` 为入口核对圈子模块：`pkg-circle/circles` 共 48 个 Vue 文件，其中 45 个注册为页面。逐项检索读取失败、空态、固定首批截断、登录态与权限、右上角微信胶囊及主要触达区；针对可复现的断点修复，并用隔离 H5 数据、本地类型检查和微信构建验证。这个范围不等于 45 页都完成真机交互验收。

| 页面组 | 注册路由（省略统一 `pkg-circle/circles/` 前缀） | 本轮收口与剩余覆盖 |
| --- | --- | --- |
| 入口与内容 | `me`、`detail`、`preview`、`search`、`post`、`editor`、`publish`、`topic-tag`、`activities` | 入口/发现服务端续页、搜索、圈内动态与精华、草稿与帖子权限状态有本地回归；真实发布审核、附件、键盘和分享回跳需三端实测 |
| 入圈与个人事务 | `create`、`renew`、`exit`、`exit-requests`、`join-requests`、`my-join-requests`、`my-refunds`、`notifications`、`my-audits` | 入圈付费恢复、售后失败态、公告、通知和审核台账分页有本地证据；免费圈邀请码直达与圈规确认已补齐，付费圈邀请码绕过付款已封堵；真实付款、退款、审核与弱网恢复未覆盖 |
| 圈主管理 | `manage`、`dashboard`、`members`、`members-insight`、`knowledge`、`rules`、`roles`、`sanction-notice`、`announcements`、`guests`、`invite-codes`、`earnings` | 成员/知识/治理/公告分页和失败态、顶部动作避让完成候选；真实授权、资金、治理动作与审计仍待独立验收 |
| 咨询与成长 | `assistant`、`bots`、`consult-ask`、`consult-experts`、`consult-orders`、`my-questions`、`question-detail`、`booking`、`my-calls`、`call-end`、`expert-config`、`level`、`ranking`、`badges`、`checkin` | 咨询双来源分页、本人问答权限、公开答案保护和达人列表容错完成；真实 AI 回答、语音通话、金币结算、预约及等级服务需目标环境验收 |

未注册的 3 个遗留文件为 `distribution.vue`、`calendar.vue`、`analytics.vue`；它们没有在页面清单中，也未找到从现有圈子页面到这些路径的导航。`calendar.vue` 含 2026 年 6 月硬编码示例活动，`analytics.vue` 是全零静态展示；不能将其描述为在线功能，更不能仅因源码存在就对用户开放。后续若要恢复入口，必须先替换为真实数据和完整失败/权限状态。

另发现“我的圈子”中的“我的活动”快捷入口通向已停用、固定返回空列表的兼容接口，造成可点击但没有真实内容的死路。现已将此入口改为“圈内动态”，直接打开圈子广场中已接入的成员近期动态分区；历史 `/circles/activities` 路由仅保留旧链接兼容，不再作为当前活动能力宣传。

个人概览复核又发现三个容易误导的断点：圈子事务页“帖子”数字原来会跳到圈子列表；“发布审核与草稿”实际只有审核记录；我的圈子列表把统计接口失败默认为零。现分别改成纯“发帖数”、如实标“发布审核”，并独立加载圈子列表与内容统计；统计失败显示未确认并可重试，列表失败也不显示虚假的零个圈子。搜索/角色筛选无匹配时可一键清除，原生输入框文本与筛选状态同步；列表使用真实圈子封面，无图才用生成封面。通用封面在图片加载前以主题底色衔接，避免透明空框。隔离 H5 回归 `tests/release/circle-my-overview-browser.mjs` 覆盖上述状态、封面显示和错误恢复；截图保存在 `artifacts/circle-my-overview-20260922/`。该回归只证明本地浏览器行为，仍需小程序真机复核。

列表完全为空或读取失败时，搜索与角色筛选不再露出无效的“0”选项；角色筛选保留键盘可操作状态。隔离回归覆盖真实空列表和读取失败两种情况。

上述改动在隔离工作树重跑移动端类型检查、圈子概览交互回归及既有圈子体验回归（11 项）均通过。最终源码的小程序正式构建通过 WXML 615 文件、文本兼容 2683 文件校验；主包 1.63 MB、保守上传口径 1.92 MB（余量 0.08 MB），`pkg-circle` 1.00 MB，最大分包 `pkg-paipan2` 1.94 MB。包量只是本次候选结果，后续整合必须重算。

排行榜专项复核发现：排行榜 API 失败原被吞成空列表，页面的重试态无法出现；不足三条时仍绘制空的第二、三名；“最活跃”按成员数取数却用帖子数展示。现使独立排行榜页区分读取失败与真空，领奖台只画实际记录，前三名可点击进入圈子，触控区和键盘操作补齐。原“最活跃”明确改为“综合”，口径为成员数＋帖子数，服务端先按同一口径排序再分页，界面显示同一数字。这个综合分只是现有成员与内容数量之和，不代表真实日活或发帖频率。`circle-post.service.spec.ts` 验证服务端排序和分页（3/3 通过）；`tests/release/circle-ranking-browser.mjs` 用隔离数据覆盖失败恢复、1/2/3 条、入口跳转与 320/390px 截图。服务端构建、移动端类型检查及小程序正式构建均通过；真实环境排行数据和小程序真机仍未覆盖。

排行榜右上角装饰图标也接入微信胶囊真实边界避让；这项仅有代码与构建证据，胶囊实际位置需开发者工具和真机复核。

排行榜的旧版大面积红色渐变与圈子页面的内容优先风格不一致，现改为白色标题区、清晰的指标切换和克制的前三名层级；没有改变排行数据和跳转逻辑。隔离浏览器在 320/390px 宽度重跑失败恢复、1/2/3 名、指标切换和第一名入口，截图见 `artifacts/circle-ranking-20260922/`。本次改动后的移动端类型检查、小程序正式构建、WXML 及文本产物校验通过；主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB），`pkg-circle` 1.00 MB。小程序真实设备的视觉和触控仍待核验。

包体取舍：本候选上次正式构建的圈子分包约 1.00 MB，圈子页面并未因分包大小受限；主包按保守上传口径仅余约 0.08 MB。故本轮不剥离排盘、直播、驿站或研究院：删除其他分包不能直接证明可为主包扩容，也会破坏既有路径。排盘继续对小程序普通用户保持升级提示，保留以后与智能体场景衔接的能力。每次整合都重新构建并测算；若实际上传超限，再根据构建产物定位主包重复依赖和可延后能力，不以预设裁剪代替测量。

继续从个人入口检查发现，“我的圈子”快捷区的“我的勋章”未传圈子 ID，而徽章接口必须指定圈子，点击必定报错。快捷区改为可直接打开的“圈子排行”；历史无 ID 徽章链接保留兼容，但会提示先选圈子并提供返回选择入口。嘉宾管理的右上角邀请按钮和徽章页计数按微信胶囊真实边界避让，返回/邀请控件扩大触控区。隔离浏览器覆盖了快捷入口和徽章缺参回退；胶囊位置仍需小程序运行环境确认。

共读签到页复核：服务端签到基础经验为 10，但连续第 7、30 天另有奖励，原按钮的“立即签到 (+10经验)”容易被读成实际到账固定为 10。按钮改为“立即签到”，成功弹窗继续显示接口返回的实际 `expGained`；顶部返回动作按微信胶囊边界避让并补键盘操作。改动后的移动端类型检查、小程序正式构建、WXML 和文本产物校验通过，包体仍为主包 1.63 MB、保守上传估算 1.92 MB、圈子分包 1.00 MB。尚未做真机签到，也未触发真实用户经验变更。

签到服务另有原子性缺口：原先先插入每日签到记录，再独立写成长经验；第二步失败会使重试被唯一索引识别为“已签到”，留下未到账经验。现将两步放入同一数据库事务；唯一记录已存在时不再写经验。`growth.service.spec.ts` 新增成功、重复和第二步失败的定向检查，整份 7/7 通过，服务端构建通过。单测核对了事务边界，未使用隔离 PostgreSQL 进行真实并发/回滚验证；历史若已有半完成记录，本次不自动修复或补发。

成长页把“签到经验 ÷ 10”估为累计签到次数，遇到连续第 7/30 天奖励便虚增次数。现直接展示日历接口返回的 `totalCheckins`，按钮只写“立即签到”，实际经验仍由服务端结果决定；顶部“排行”明确为“签到榜”，避免与等级榜混淆。高饱和红色签到整卡改为白色内容卡，红色仅保留为可操作的签到按钮；标签最小触控高度 44px 并支持键盘选择。隔离 H5 使用 7 次签到、90 经验的奖励夹具验证两个数字不混淆，320px 页面无横向溢出；截图见 `artifacts/circle-level-20260922/experience-320.png`。移动端类型检查、小程序正式构建、WXML 和文本产物校验通过，主包 1.63 MB、保守上传估算 1.92 MB、圈子分包 1.00 MB。真实小程序胶囊、签到和到账仍待设备及目标环境验收。

签到榜原从“成长总经验前 20 名”中二次排序，可能漏掉真正连签靠前的成员；个人“圈内排名”也错用成长排名。服务端成长响应现增加独立的 `checkinLeaderboard` 和 `myCheckinRank`，只对今天或昨天有签到的成员计算有效连签，按连签天数、签到经验及稳定 ID 排序；成长榜保持原口径。小程序页面只使用新签到榜，旧响应缺字段时显式重试，不显示貌似正确的假排名。定向服务端测试 9/9 通过，覆盖成长榜前 20 名之外的连签第一名与断签归零；隔离 H5 验证旧接口失败恢复、新名次和 320px 布局。服务端构建、移动端类型检查、小程序正式构建及 WXML/文本产物校验通过；主包 1.63 MB、保守上传估算 1.92 MB、圈子分包 1.00 MB。服务端与移动端必须同批发布；真实数据库的排序性能及跨日表现未覆盖。

圈主管理页的成员/内容筛选曾在“已加载数据无匹配”时只剩说明文字，用户只能手动逐个恢复筛选。现提供“清除筛选/查看全部内容”直达出口；筛选、成员行及内容治理动作补键盘操作，筛选芯片与行内操作按至少 44px 触控区调整，窄屏操作行可换行。成员搜索与角色筛选仍只覆盖已加载页，页面继续明确提示可加载更多，不冒充全库搜索。`tests/release/circle-management-pagination-browser.mjs` 在当前工作树专用 H5 端口 53683 上用隔离数据验证分页、筛选恢复、触控高度和 320px 无横向溢出（8 项断言通过、0 写请求），截图见 `artifacts/circle-management-20260922/content-actions-320.png`。移动端类型检查、小程序正式构建、WXML 和文本产物校验通过；主包 1.63 MB、保守上传估算 1.92 MB、圈子分包 1.00 MB。未执行成员角色、删帖或嘉宾收益写操作。初次预设端口 5197 已被其他本地服务占用，不作为本次验收证据。

目前发现的已知边界：圈内动态聚合只取前 6 个已加入圈子各 2 条，不是全量消息中心；圈内课程和商品当前展示接口首批近期资源，不是完整目录；小程序正式胶囊避让和所有页面触控、真实 AI/语音、支付/退款/审核及弱网链路均缺真机与目标环境证据。咨询新分页端点、本人问答新端点与移动端必须同批发布。小程序包量接近限制，后续合并每次均需重新测算。

邀请入口专项见 `docs/progress/圈子邀请链接与入圈边界-20260922.md`。邀请码服务端限制和预览页新接口也必须与移动端同批发布；原有付费圈历史邀请码不能用于跳过付款。

圈主管理看板复核发现授权与缓存顺序问题：`getOverview`、`getTrends` 原按圈子 ID 缓存完整经营数据，命中缓存会跳过方法内的成员角色校验，普通成员可能读取到已缓存的收入等管理数据。现移除这两个含授权逻辑的方法缓存，每次请求均先查当前圈内角色；圈主管理页直达链接也先请求受保护的经营概览，确认权限后才加载成员和内容，失败则提供重试入口。其余圈子缓存点为公开帖子、排行榜和公告，没有在被缓存的方法内依赖调用者身份的管理权限校验。服务端新增“已有圈主缓存时普通成员访问仍拒绝”的两项回归，`circle-dashboard.service.spec.ts` 22/22 通过；隔离 H5 管理页 11 项断言通过（包含 403 后不继续请求成员/帖子、0 业务写请求），服务端构建、移动端类型检查和小程序正式构建通过。包体仍为主包 1.63 MB、保守上传估算 1.92 MB、圈子分包 1.00 MB。该修复仅在隔离候选，线上旧版本若存在相同缓存逻辑，在同批部署前应按权限泄露风险处理；尚未以真实账号和目标 Redis 验证。

圈主管理待办继续复核：付费提问原在看板上只把第一条作为唯一跳转，其他待回复问题没有逐条入口；服务端按最新时间截取 20 条，久候问题可能长期不显示，且混入分配给别人的问题会让圈主进入无法回答的详情。现只读取当前管理者本人被指定回答的 `PENDING` 问题，按最早提交顺序稳定排序，最多取 20 条；看板可展开逐条进入对应详情。达到 20 条时明确提示这是当前可见上限，处理后刷新会补入后续提问，不把上限当作总数。服务端定向测试 23/23 通过；隔离 H5 夹具验证两条问题都可见且第二条可直达详情（6 项断言、0 业务写请求），未触发真实回答或金币。服务端构建、移动端类型检查、小程序正式构建和包体校验通过：主包 1.63 MB、保守上传估算 1.92 MB、圈子分包 1.00 MB。此修复需服务端与移动端同批发布，真实问答权限和目标环境待验收。

咨询记录返回链路复核：`consult-orders` 与提问者的 `my-questions` 原只在首次挂载时读取，用户进入详情完成回答/拒答或等待状态变化后返回，列表仍保留旧状态和“待我回答”提示。现两页在重新显示时按本人身份刷新，保留当前筛选；筛选无匹配时明确说明并提供“查看全部”。筛选标签统一至少 44px 触控高度，补键盘操作和选中语义。隔离 H5 使用状态从 `PENDING` 到 `ANSWERED` 的只读夹具，验证达人订单、提问者列表返回刷新及两处空筛选恢复（11 项断言、0 业务写请求）。移动端类型检查与小程序正式构建通过，主包 1.63 MB、保守上传估算 1.92 MB、圈子分包 1.00 MB。此为本地页面行为证据，真机返回手势、真实问答状态与金币变更仍待验收。

付费问答资金一致性专项：审计 `QuestionService` 发现原超时任务先批量把 `PENDING` 改为 `CLOSED`，再逐笔独立退币；退币失败会留下已关闭但未到账的记录，旧移动端又把 `CLOSED` 一律写成“金币已退回钱包”。回答与拒答也先读后写，和超时处理并发时可能重复或覆盖。候选现将每笔超时退币和状态流转放入同一数据库事务，以 `PENDING` 条件更新认领，失败保持待处理由下次任务重试；新成功记录标为 `REFUNDED`，每批最多 200、每组最多 10 笔并发并记失败数量。拒答也在同一事务内认领并退币；回答在审核后再原子认领，并把旧口径收益记录写入同一事务，收益写失败则回答不成立。统一总账仍按现有影子双写机制在提交后处理，失败需运维核对。移动端只有 `REFUNDED` 才显示已退币；历史 `CLOSED` 等非确定状态显示“待核实，请查看钱包记录”，并将通话达人侧“预扣/已入账”改为尚无收入或以收益账户为准。**不自动修复历史 `CLOSED` 记录，不执行真实余额操作。**定向服务端测试 36/36 通过，覆盖重复/并发认领和收益失败，隔离 H5 状态回归覆盖历史 `CLOSED` 不冒称退款及通话分账文案（20 项页面检查、0 业务写请求）。最终源码服务端正式构建、移动端类型检查、小程序正式构建以及 615 个 WXML、2683 个文本产物兼容检查通过；主包 1.63 MB，保守上传估算 1.92 MB（余量 0.08 MB），圈子分包 1.01 MB。测试中的事务客户端为 mock，目标数据库真实回滚和现存历史账目仍需独立核验。

通话记录与结算单继续核对：此前服务端 `ConsultCallService.end()` 将用户预扣退差额与 `ENDED` 状态置于同一事务，达人分成则通过异步 `RevenueService.record()` 写收益账户，并不向达人金币钱包直接增币。此前“我的通话”和结算单把估算 50% 写成“已入账金币”，接听方的等待订单还把发起方预扣写成自己的支出。候选现仅展示订单金币金额、发起方实际支出及退回，并明确达人分成以收益账户记录为准；未接/取消的退款说明也区分发起方与接听方。隔离 H5 以两个身份验证列表和结算单 10 项界面断言、0 业务写请求；移动端类型检查、小程序正式构建及 WXML/文本产物兼容检查通过，主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.01 MB。**本段只修正展示；服务端事务修复与剩余验证边界见下。**

通话结算资金边界后续修复：审计发现 `end()` 原在订单状态及预扣差额退款事务提交后，异步写达人旧口径收益；失败会留下 `ENDED` 但未记录收益的通话。现把收益记录纳入同一事务，仅抢到 `ONGOING → ENDED` 的请求才能退差额并记收益，收益写失败则不返回结算成功、由数据库回滚后可重试。`accept()` 原先读取 `WAITING` 后无条件更新，可能在超时退款或取消已认领之后将已退款记录重新改为通话中；现按 `WAITING` 条件更新并检查认领结果。超时任务只统计实际认领且退款成功的条目。`consult-call.service.spec.ts` 定向测试 24/24 通过，覆盖接听竞态、正常事务、收益失败和并发结束；服务端正式构建通过。**统一总账仍为提交后影子双写，测试使用事务 mock，真实数据库回滚、目标环境双节点竞争及历史未入账通话仍需只读核验；本轮未处理真实余额。**

后续隔离复核：本机 PostgreSQL 服务在 `127.0.0.1:5433` 可达，但该工作树没有适配的测试连接身份；未借用共享或目标数据库试写，故问答、签到、通话的真实事务回滚与并发仍标为未覆盖。圈主管理 11 个经营/成员洞察服务入口增加逐项越权回归，确认普通成员在读取订单聚合或成员画像前被拒绝；`circle-dashboard.service.spec.ts` 34/34 通过。这是源码和 mock 权限证据，线上 JWT、Redis、角色变更及实际数据隔离仍待目标环境核验。

通话记录交互再收口：`my-calls` 返回显示时重新读取状态，避免结束通话后仍显示预扣中；结算单显示时也刷新，顶部标题统一为“通话记录”，未接通/取消时由真实状态标题说明，不再写成“通话已结束”。隔离 H5 双身份 14 项交互/文案检查通过，列表返回后读取次数可见、0 业务写请求；移动端类型检查与小程序正式构建通过，WXML 615 文件、文本产物 2683 文件兼容检查通过，主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.01 MB。真机返回手势与真实通话状态切换仍未覆盖。

售后页状态复核：`refundStatus=failed` 且圈主/平台审核字段仍为通过时，旧页面会落入“平台审核中”分支，掩盖未到账事实。现对失败状态显示“退款未完成”，不凭审核字段冒称到账，提供申请编号供客服核查；从退款申请或钱包返回时重新读取退款记录与余额。隔离 H5 用状态从审核中变为失败的夹具验证返回刷新及失败文案（7 项断言、0 业务写请求）。移动端类型检查、小程序正式构建、WXML 615 文件及文本产物 2683 文件兼容检查通过；主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.01 MB。这不改变真实退款流程、审核规则或余额，仅修正状态展示；失败账目的实际处理仍需平台核对。

状态：代码候选与隔离本地验证。未部署、未提交小程序审核、未触发真实资金或通知。线上状态未知，不能据此认定整个圈子板块已达到正式发布条件。

2026-09-23 审核页回访补强：发布审核和圈主管理的加入/退款审核页在返回显示时重新读取服务器状态，避免其他页面或管理员处理后仍显示旧待办。加入与退款两类待办独立判错：一类接口失败不掩盖另一类成功结果；失败项清掉旧数据并显示未确认，避免把上次结果冒充本次结果。发布审核的状态筛选加大到 44px 触控高度，筛选为空时说明当前状态无记录。圈主退出审核后端目前只有待审列表，前端“已处理”明确改称“本次已处理”；本页会话内保留已审核条目并在返回刷新时排除过期待办，并提示后续平台处理和退款到账状态不会在本页更新。关闭页面后的历史审核记录仍无圈主端查询入口，此项为后续服务能力缺口，不能把本次会话记录当作完整台账。隔离 H5 回归 `tests/release/circle-review-return-browser.mjs` 覆盖审核状态回访、空筛选、退出审核本地模拟成功及返回后状态、加入接口失败但退款仍可查看及重试恢复（16 项检查，1 次仅本地夹具拦截的审核 POST，未触发真实退款）；最终源码的移动端类型检查及小程序正式构建通过，615 个 WXML、2683 个文本产物校验通过；主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.01 MB。真机审核操作、目标环境权限与退款状态仍未覆盖。

2026-09-23 圈内通知回访补强：从帖子等目标页面返回时重新读取通知和未读计数；分类切换或返回刷新时用请求序号丢弃过期响应，避免慢请求把新分类覆盖。续页失败提供明确点击重试；分类为空时区分“此分类暂无通知”与整个通知中心为空，筛选触控高度增至 44px。隔离 H5 回归 `tests/release/circle-notifications-return-browser.mjs` 用只读夹具验证返回后的已读点、未读状态及空分类（7 项检查，0 写请求）；移动端类型检查与小程序正式构建通过，615 个 WXML、2683 个文本产物校验通过；主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.01 MB。当前修改不改变后端通知触发或真实推送渠道；目标账号、小程序真机及线上通知完整触发链仍待验收。

2026-09-23 通知与邀请码可靠性补强：单条通知的“标记已读”写入失败时改为重新读取服务器状态，避免本地长期显示错误的已读结果。邀请码页从海报等页面返回时刷新可用码与邀请统计；统计读取失败时仍可正常复制、分享有效邀请码，统计单独标为暂不可用，不显示虚假零值，刷新中也保留故障提示。复制与分享前再次检查本地邀请码的到期时间和已知使用次数，发现失效后提示并刷新；过期码不会被小程序分享回调带出。并发在别处用尽的号码仍需服务端校验，不能仅凭页面状态保证分享链接有效。隔离 H5 新回归 `tests/release/circle-invite-refresh-browser.mjs`（8 项，只读）及扩展后的圈内通知回归（9 项，1 次仅本地夹具拦截的失败 PUT）通过；既有免费圈邀请码直达、圈规确认和付费圈不带码回归也通过。最终源码移动端类型检查和小程序正式构建通过，615 个 WXML、2683 个文本产物校验通过；主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.01 MB。真实邀请码失效时序、微信分享与通知服务故障仍需目标环境/真机验收。

2026-09-23 圈子海报分享闭环：原“生成海报”从免费圈邀请码页进入通用圈子海报，二维码与分享链接只指向圈子详情、没有邀请码，和页面“邀请好友加入”的承诺不符。现从邀请码页传入当前有效码，海报数据层再次向圈主邀请码列表核验归属与可用状态；有效时二维码、分享面板 URL 和小程序分享路径统一指向带码圈子预览页，普通圈子海报仍指向详情页。失效码或读取失败时显示明确错误及返回/重试，不再永久转圈，也不生成无效海报；图片尚未绘制完成时不再提示“已保存”。视觉复核缩短二维码说明并同时画入导出图，H5 截图在 `artifacts/circle-invite-poster-20260923/invite-poster-h5.png`。`tests/release/circle-invite-poster-browser.mjs` 用隔离接口验证带码链接、失效错误、普通海报不带码（11 项，0 业务写入），既有邀请入圈与过期刷新回归通过；最终源码类型检查和小程序正式构建通过，615 个 WXML、2683 个文本产物校验通过；主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.01 MB。H5 截图不等于导出图片可扫码，仍需在小程序和 APP 真机保存、扫码与分享回跳验收；码发出后若被别人用尽，服务端兑换校验仍是最终准入控制。

2026-09-23 圈子邀请海报视觉重构：针对用户指出的模板感和 AI 味，只重做 `type=circle` 的邀请海报，其他内容海报保留现有设计。采用古籍扉页的信息层级：冷色纸面、深松绿墨色、大字号圈名、克制的朱色短线；底部深色区直接承载「扫码入圈」、邀请人和真实二维码。移除圈子海报的通用金框、印章、四套主题与套话式文案，分享文案改为真实圈名加可复制的落地链接。预览和 canvas 导出分别按同一布局绘制；导出提升到 600×840，H5 实际下载图见 `artifacts/circle-invite-poster-20260923/invite-poster-export.png`，页面预览见同目录 `invite-poster-h5.png`。隔离浏览器回归验证有效邀请码链接、失效错误、普通海报无邀请码、真实图片下载（12 项，0 业务写入）。二维码来自真实落地链接，尚需小程序与 APP 真机保存后扫码、入圈和分享回跳验收；本批不发布。

2026-09-23 圈子介绍与问答失败态：圈子介绍页原把详情和帖子预览一起读，帖子接口内部失败又被转换为空列表，容易把接口故障表现为圈内没有内容；现详情先呈现，帖子和成员资格分别后台读取，核实成员身份期间主按钮禁用。预览慢请求有独立进度，失败单独说明且可重试，不妨碍查看圈子和加入方式。圈子详情的问答 Tab、按圈及全平台达人列表也把接口故障误报成“暂无达人”，现调用严格错误模式，保留真实空态并增加重试。隔离 H5 `tests/release/circle-preview-partial-failure-browser.mjs` 验证访客在帖子首请求延迟 1.2 秒及随后返回 503 时仍能看到介绍和“免费加入”、重试后显示帖子；登录成员的资格接口延迟期间按钮禁用，核实后才恢复（9 项、0 写入）；截图见 `artifacts/circle-preview-partial-failure-20260923/preview-390.png`。扩展 `circle-detail-posts-browser.mjs` 验证圈内问答、按圈达人和全平台达人服务首请求失败后均可重试，既有邀请码入圈回归仍通过。最终源码类型检查和小程序正式构建通过，615 个 WXML、2683 个文本产物校验通过；主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.02 MB。浏览器测试不等于目标环境达人数据或小程序真机体验已验收。

2026-09-23 连麦咨询入口如实展示：达人列表原把尚无预约模型的页面标为“去预约”，App 说明页仍有点击后只弹“暂未开放”的发起按钮；现入口改为“了解连麦”，各端明确告知实时通话仍在联调，App 不展示无法履约的发起按钮。原页面读取达人接口失败时可能把 URL 中的价格直接显示为当前报价；现仅在服务端核实达人服务及价格后展示，失败给出重试，不变更金币或计费规则。从达人列表打开说明页后返回会退回原列表；直接打开说明页时则替换为达人列表，避免反复返回同一说明页。隔离浏览器在服务端首次 503、链接伪造 `price=1` 的情况下验证不展示旧价，重试后显示服务端价 5 金币/分钟，并覆盖两种返回路径；H5 视觉截图见 `artifacts/circle-consult-availability-20260923/booking-h5.png`。最终源码类型检查与小程序正式构建通过；主包 1.63 MB、保守上传估算 1.92 MB（余量 0.08 MB）、圈子分包 1.02 MB。没有发起真实通话或扣币。实时通话、预约模型和真机结算仍未完成，不能把该说明页当作已上线连麦能力。

本轮对“圈子整体优化完成”的口径：本地代码和浏览器夹具已覆盖上述修复及此前记录的主要入口、分页、空态、权限和回跳；最终源码类型检查、H5、App 和微信小程序正式构建通过，仍不能等同于三端上线验收。正式发布前需要用与候选匹配的安装包／小程序体验版按角色走完游客、普通成员、圈主、管理员的入圈、发帖、消息、管理和返回链路；付费、退款、金币咨询须用受控测试账号与可核销订单验证，不碰真实用户资金。实时语音／视频尚无可用前端组件，当前只展示说明。全平台达人发现接口当前最多返回 50 条且按提问价降序，页面中的推荐列表不能表述为全量达人目录；完整目录需另行设计分页和排序，本轮未扩展后端契约。任何正式上线结论还需目标环境版本、权限和真实渠道实测证据。
