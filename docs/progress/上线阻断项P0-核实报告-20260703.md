# 上线阻断项（P0）核实报告 — 2026-07-03

> 对 `上线就绪清单.md`（2026-06-25，部分已过时）中 P0 项做**生产库实测核实**。
> 方法：只读查证生产库结构/开关/配置 + 造测试角色实测后台页 + 密钥配置状态审计。
> **结论：技术性阻断项已基本清零；剩余 = 密钥配置（你的）+ 法律文书 + 开关决策。**

---

## 一、数据库结构 ✅ 全部就绪（原清单最大风险已排除）

| 核实项 | 原清单担忧 | 实测结果 |
|---|---|---|
| LiveReview / LiveTeamMember | ❌ 无 migration 无脚本→生产缺表 500 | ✅ **两表都在** |
| 其它硬缺口表 | 靠 db push 存在 | ✅ CircleRefundRequest/UserWallet/ConsultCall/CircleCheckin/CircleJoinRequest 全在 |
| deletedAt 列漂移 | 除 User 外可能缺→P2022 500 | ✅ **零漂移**：schema 10 个 model（Article/Circle/ClassicBook/ClassicChapter/Comment/Content/ContentAuditRecord/Course/Product/User）含 deletedAt，生产库正好这 10 张表全有列 |
| CircleKnowledge 复合唯一约束 | 无 migration 建立→RAG ON CONFLICT 失败 | ✅ `CircleKnowledge_contentHash_circleId_key` 存在 |

**→ 数据库层无上线阻断项。** 全新库若重新 migrate 需注意，但当前生产库结构完整。

---

## 二、功能开关（FeatureFlag）⚠️ 核心已开，商家待定

**已开启（enabled=true）8 个**：
`shop_checkout`（商城下单）/ `live_start`（开播）/ `member_purchase`（会员购买）/ `commission_withdrawal`（佣金提现）/ `content_publish`（圈子发布）/ `course_publish`（课程发布）/ `risk_fraud_scan`（风控）/ `recommend_algorithm`（推荐）

**未配置（守卫端点会 404）2 个**：
- `merchant_backend`（商家后台）、`merchant_onboarding`（商家入驻）
- → **上线商家功能前需 upsert enabled=true**（现有商家数=0，不影响其它模块）

---

## 三、密钥配置

**`.env` 基座（启动必配）✅**：DATABASE_URL / JWT_SECRET / ENCRYPTION_KEY / REDIS_URL / DEEPSEEK_API_KEY

**后台第三方配置页已配（DB ConfigSystem，启动注入 env）8 个服务**：
`tencent_cloud`（通用密钥）/ `tencent_cos`（存储）/ `tencent_im`（私信）/ `tencent_live`（直播）/ `tencent_trtc`（连麦）/ `tencent_ugsv`（短视频SDK）/ `tencent_vod`（点播）/ `wechat_pay`（微信支付）
> ⚠️ 有配置行 ≠ 值完整有效。需按功能实测验证（如短信登录仍需绕过=签名未通，说明 tencent_sms 未配全）。

**完全未配置（用到该功能才需）**：
- 其它支付：`alipay`（支付宝）/ `huifu`（汇付分账）/ `unionpay`（银联）
- `tencent_sms`（短信 SMS_APP_ID/SIGN_NAME/TEMPLATE_ID）— 影响真实短信验证码
- `coze`（智能体）/ `wechat_open`（微信登录）/ `kuaidi100`（物流）/ `tencent_map`（地图）
- `CRON_WEBHOOK_SECRET`（定时任务验签）— 缺则所有 cron webhook 被 403（这是自定义随机串，非外部密钥，可自行设）

---

## 四、数据 / 内容

| 项 | 状态 | 说明 |
|---|---|---|
| 商家入驻协议模版（merchantId=TEMPLATE）| ❌ 缺（count=0）| 商家签约页 `/merchant/agreement-preview` 400 的根因。**协议是法律文书，内容应由你/法务提供**，不宜占位 |

---

## 五、剩余上线动作清单（按需）

**必做（上线你要开放的功能）**：
1. 配齐要用功能的密钥（后台第三方配置页）：至少一种支付 + 短信（登录验证码）+ COS（上传）
2. 商家功能：开 `merchant_backend`/`merchant_onboarding` 开关 + 提供入驻协议模版
3. 设 `CRON_WEBHOOK_SECRET`（定时任务要用）

**密钥配齐后需再走一轮真机验证**：IM 私信 / 直播推流播放 / 微信支付充值下单 / 短视频上传 / 物流查询 / 地图选点 —— 这些密钥门控功能本轮保持空态/降级，配置后须端到端实测。

---

## 附：本轮已完成（非阻断，锦上添花）
- 设备级真机走查：消费端 419 路由 + 全后台角色（商家22/运营商+书院41/圈主）→ 2 个真实 bug 已修上线（书架 limit 400、优惠券 mock id）
- 后端安全/资金 7 维度审计 + 修复 + 部署（详见 `后端走查-写端点500转404系统性修复-20260702.md`）
- 后台补齐腾讯云语音识别(ASR)配置卡片
