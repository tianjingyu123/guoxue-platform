# 品牌硬编码收敛·第二批（租-T0 后续）— 记账

> 执行日期：2026-07-04 ·前置：租-T0 已建 BrandConfig（后端 `GET /system/public/brand-config`·mobile `lib/brand.ts` reactive BRAND + App.vue 水合·admin 配置页 `views/system/BrandConfig.vue`）
> 本批目标：把剩余"热卜"运行时硬编码全部接到 BrandConfig。铁律：默认值下替换前后渲染/输出逐字一致（未配置时零变化）。

## 一、本批完成

### 1. admin（13 文件·新增水合基建）

admin 此前无 BRAND 水合机制，本批补齐：

- **新建 `apps/admin/src/lib/brand.ts`**：与 mobile 版同构（`reactive BRAND` 内置"热卜"默认值 + `hydrateBrandConfig()` 幂等水合·走 `systemApi.getBrandConfig()` 公开端点·失败静默保留默认并允许重试·后端 siteName* → 前端 name* 字段映射）
- **`src/main.ts`**：`app.mount` 前 fire-and-forget 调用 `hydrateBrandConfig()`
- 逐文件替换：
  | 文件 | 替换 |
  |---|---|
  | `components/BrandLogo.vue` | 印章两字由 `BRAND.nameShort.split('').slice(0,2)` 驱动（v-for）；title 默认值改 `computed(props.title \|\| BRAND.name)` |
  | `views/Layout.vue`、`views/Login.vue` | `title="热卜国学"` → `:title="BRAND.name"` |
  | `views/Dashboard.vue` | 欢迎标题/文案 2 处 → `{{ BRAND.name }}` |
  | `views/dashboard/{Ai,Content,Platform,Offline,Transaction}Bigscreen.vue` | 大屏水印 5 处 → `{{ BRAND.name }} · XX大屏` |
  | `views/ai/CustomerServiceAdmin.vue` | 预置 FAQ 2 处 → `` `${BRAND.name}` `` 模板字符串 |
  | `views/content/ContentAudit.vue` | 审核启发式 `includes('如何在热卜')` → `includes('如何在' + BRAND.nameShort)`（默认值下逐字等价·与 server content-generation 标题格式联动） |
  | `index.html` | **保持硬编码** + 注释（构建期取不到运行时配置） |

### 2. mobile（30 文件·全部接 BRAND）

各 .vue 补 `import { BRAND } from '@/lib/brand'`，"热卜国学"→`BRAND.name`、"热卜"→`BRAND.nameShort`，canvas 绘制/分享文案用模板字符串（运行时求值无水合时序问题）：

pkg-circle/rankings、pkg-classics/shared-reading/detail（canvas×2）、pkg-competition/{poster,home,detail,certificate}、pkg-course/complete、pkg-poetry/solar-term（×3）、pkg-merchant/join、components/home/daily-verse、pkg-paipan/{bazi,qimen}/{index,result}（hdrTitle 保持 let + MP-WEIXIN 覆写逻辑不变）、pkg-im/im/invite、pkg-bounty/detail、pkg-institute/index、components/common/ai-search-modal、pkg-mine/{mentorship×2,achievements×4}、pkg-shop/paying、pkg-operator/{station-poster,station-detail,join-operator,join-station}、pkg-agent/agents、pkg-profile/invite、pkg-notices/{upgrade,detail}、index.html（保持硬编码+注释）。

复扫：`src/**/*.vue` 中"热卜"仅剩 3 处，全为代码注释（pkg-auth/welcome、name-card-poster.vue 头注释、pkg-mine/about 注释），非用户露出，不改。

### 3. server 提示词（6 个 service·9 处）

统一范式：`@Optional()` 注入 `SystemService`（SystemModule 为 `@Global` 导出·单测缺 provider 时注入 undefined 自动兜底）+ 私有 `getBrandName()`（`getBrandConfig().siteName`·失败/未注入兜底"热卜国学"）。**只把字符串里的品牌名换成变量，prompt 语义一字未动**：

| 文件 | 处数 | 方式 |
|---|---|---|
| `course/course.service.ts` | 3（批改×2+课程问答 systemPrompt） | 每次调用时取（redis 已缓存） |
| `ai-gateway/customer-service.service.ts` | 1（模块级 const → `buildCustomerServicePrompt(brandName)` 函数·ask/askStream 两调用点） | 每次调用时取 |
| `bot/coze.service.ts` | 1（createBot 默认 prompt_info·新增构造函数） | 调用时取（短路：传了 prompt 不取） |
| `competition/competition.service.ts` | 2（证书 HTML 抬头+红章） | generateCertificate 内取一次 |
| `content-generation/content-generation.service.ts` | 1（tutorial 标题格式"如何在{品牌}平台学XX"·模板 prompt 签名加第三参） | 每轮生成取一次 |
| `notification/smart-push.service.ts` | 1（defaultTitle 兜底·同步调用点） | **onModuleInit 启动缓存**（性能敏感豁免·改品牌需重启该实例生效） |

## 二、有意不动（清单备注）

- **构建期配置**：mobile `src/pages.json`（2处 navigationBarTitleText）、`src/manifest.json`（2处 name/title）、mobile/admin `index.html` title——构建期取不到运行时配置，保持硬编码（index.html 已加注释说明）
- **seeds/scripts（约10处）**：`server/prisma/seeds/legal-documents.seed.ts`、`server/scripts/{enrich-ebook-demo,enrich-classics-commentary,seed-competition-demo,setup-test-*,seed-station-materials}` 等——种子/演示数据历史性，不动
- **mock 演示数据**：mobile `src/lib/shop-data.ts`（2处）、`src/lib/same-city-data.ts`（1处）
- **代码注释/设计溯源**：admin `App.vue`/`styles/tokens.css`/`global.css`/paipan·bazi 组件"对标热卜"注释、mobile `styles/tokens.scss`/`utils/router.ts` 等注释；admin `BrandConfig.vue` 的 placeholder（配置页默认口径提示）
- **schema/migration/spec 默认值断言**：`schema.prisma`、`manual_add_brand_config/migration.sql`、system.service/controller spec 中的"热卜"是默认值本身的定义与测试，属真源
- **测试/基准脚本**：`server/test/cross-platform.spec.ts`、`scripts/benchmark-recommend.ts`

## 三、验收（全绿）

- server：`tsc --noEmit` 0 错误；`jest`（course/bot/competition/notification/content-generation/ai-gateway/system 域）**55 套件 714 测试全过**（未 build 未动 pm2）
- mobile：`vue-tsc --noEmit` 0 错误；`scan-mock-imports.sh` ✅ 通过（EXIT=0）
- admin：`vue-tsc --noEmit` 0 错误
- 变更规模：49 文件改动 +217/-76 行，另新增 `apps/admin/src/lib/brand.ts`

## 四、剩余/后续

- 换品牌名后 smart-push 的 defaultTitle 兜底需重启后端实例生效（启动缓存·已注释说明）
- 构建期三处（pages.json/manifest.json/index.html）如将来做多租户分发，需走构建变量方案（见 docs/design/多租户与国际化架构蓝图-20260704.md T0）
- BrandLogo 印章仅取 nameShort 前两字，nameShort 超两字时截断展示（与原印章版式一致）
