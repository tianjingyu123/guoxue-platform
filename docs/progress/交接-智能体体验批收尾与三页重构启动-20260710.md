# 交接文档：智能体体验批收尾 → 聚合三页重构启动（2026-07-10）

> 新窗口首读。上窗口 = 智能体体验批（已完成并 commit），下窗口主线 = 聚合三页重构（设计已拍板待实施）。

## 一、下窗口主线：聚合三页重构（首页/发现页/个人中心）

- **设计方案已拍板落盘**：`docs/design/聚合三页重构-首页发现页个人中心-设计方案-20260710.md` ← 先读这个
- 总纲：首页=刷（双列瀑布流·统一卡骨架强融合）｜发现页=找逛（金刚区+主题策展+分板块原生橱窗）｜个人中心=管续（会员金卡条+「继续」区+资产矩阵+B端身份入口）
- 落地顺序：P1 首页流（`/feed/recommend` 聚合端点 + `components/feed/cards/` 8类卡组件库+注册表分发）→ P2 发现页橱窗化 → P3 个人中心「继续」聚合端点
- 建议每页先走 V0 协作工作流出视觉稿再动码（`docs/knowledge/板块UI优化-V0协作工作流.md`）
- X5 红线：禁 writing-mode / aspect-ratio / backdrop-filter 承载关键信息

## 二、上窗口成果（智能体体验批·已 commit 未部署生产）

- **commit `95b2002f` 智能体体验批**：广场重排（智玄大卡置顶/最近使用/热门TOP5真热度/分类分区）+ 全链路真流式（新 `/ai/zhixuan/chat(/stream)` 自建链路+八字意图识别；bots stream 补审计/门控/RECO过滤/meta；伴读 stream；前端 `utils/stream-chat.ts` H5 fetch SSE+401续期，非H5降级）+ 富消息框架（`rich-message.vue` 注册表分发·新卡=新组件+注册一行）+ 八字卡示范（`components/agent/cards/bazi-card.vue`·真盘注入prompt）
- **🔴 关键修复必随部署**：`main.ts` compression 曾把所有 SSE 缓冲成整段（生产流式一直是假流式），已加 filter 跳过 text/event-stream
- 其他顺手修复：Coze 流式解析双发+completed重放 bug；gateway.chatStream 补 skipCache（八字防语义缓存串盘）；古籍阅读器目录抽屉滚动穿透（用户报障）；H5 聊天滚底两处（直滚 .uni-scroll-view 内层+触摸判定上滑暂停）
- 验证记录：server tsc 0 / vue-tsc 0 / jest 54套件735绿 / 本地 pm2:3000+nginx 端到端全过；截图 `C:\Users\Administrator\guoxue-e2e\shot-agent-*.png`（广场/流式中态/八字卡）
- **commit `a0c3fc95` 商城收敛手术**（另一并行实例所做）：断流11处+砍11孤岛页+H5真实支付接线。砍页死链已扫描：别名兜底全对，唯一真死链（收藏夹→商品）已修——**`pkg-mine/favorites/index.vue` 一行修复仍在工作区未提交，下批带上**

## 三、待办清单（按优先级）

1. **生产部署**（低峰）：95b2002f + a0c3fc95 + favorites 修复；冒烟必验=流式打字机真机逐字可见（compression 修复生效标志）/八字示范全链路/商城砍页后主链路/**H5 真实支付真机验证**
2. nginx 不用改（后端带 X-Accel-Buffering:no）；确认 proxy_read_timeout≥60s；勿给 event-stream 配 gzip
3. 台账尾巴：A5 存量商品重存、A2 轮播上传位（见 guoxue-handoff-0709）
4. 商城结论已拍：**不重构，收敛式整改**，验完 a0c3fc95 + 清台账尾巴即到位

## 四、环境与事实备忘

- **直播配置已确认**：生产库 ConfigSystem `third_party.tencent_live`（推流/播放域名+双Key+AppName）07-01 落库、loader 已热加载生效；IM/TRTC/UGSV/COS/VOD 同在。直播联调可直接开工
- 本地：pm2 `guoxue-api`(:3000·fork·dist) + `guoxue-nginx`(:80·/h5 站点+ /api 反代)；测试号 39c2bd42…（`%TEMP%\tk-root.txt` token）已开 30 天会员便于 AI 联调
- **🔴 纪律：同一时间只允许一个 AI 实例改 D:\guoxue-platform**（本次曾与"商城收敛"实例并行同仓，侥幸未互毁；vue-tsc 中途冒他线错误即此因）
- 测流式端点别用 Git Bash curl 直发中文（GBK 乱码假象），用 `--data-binary @utf8文件`
