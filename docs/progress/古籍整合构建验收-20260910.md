# 古籍整合候选验收

- 整合基线包含 287e7f3f：微信支付顶层授权、退款事务修复、订单售后自动刷新。
- 23 项针对性回归通过；H5 构建通过。
- 本地共享库、八字引擎、紫微引擎、服务端源码类型检查及编译通过。
- 本地使用现存依赖目录，显式映射到当前工作树的引擎声明和服务端类型目录。该检查不等同于全新依赖安装或 Docker 镜像验收。
- 引擎检查仅编译运行源码，不包含其测试文件。未声称引擎测试通过。
- 尚未部署，尚未实测线上古籍模型耗时与失败率。
- 下一发布步骤：用锁文件构建候选镜像，在新两节点核验后发布；古籍解读必须真实生成并验证结果与当前章节一致。
- 不应使用旧的 H5 包覆盖本整合版本；不更改数据库、支付配置或域名。
