# 排盘工具 V0 还原·第一批交接（2026-07-10）

> V0 源：`D:\V0排盘工具\extracted`（排盘工具7月10日版.zip，Next.js 16，40+工具，5.6万行，HANDOVER.md 至 v0.7）
> 本批 commit：`f754594f`（57 文件 +24614/-907，本地未部署生产）
> 拍板范围：底座+高频工具批 ／ 存量5工具（八字/奇门/阳盘/六爻/合盘）不动 ／ 易经工作台付费功能延后

## 一、本批成果（全绿已验证）

### 底座
- **引擎**（V0 纯 TS 逐字搬运，import 路径适配）：
  - 主包 `src/lib/paipan/`：ganzhi.ts（四柱/十神/神煞/大运）、jieqi.ts（VSOP87 节气）、types.ts、tool-prefs.ts（收藏/排序/频次·uni.storage）、almanac-lite.ts（轻量黄历·本批新写，见下）
  - 分包 `src/pkg-paipan/lib/`：wannianli-engine（buildAlmanac 全量黄历）、almanac-data、ziwei-engine（三合派安星）、meihua-data、luopan-data、xuankong-data、各 types
  - **lunar-typescript 已源码化**到 `src/pkg-paipan/lib/lunar/index.js`（415KB，npm 依赖已卸载）——微信主包 2MB 硬限所迫，见"体积策略"
  - 正确性：日柱/四柱与 lunar-typescript 权威对拍 10/10；紫微黄金案例（V0 verify-ziwei 案例1）30/30；梅花 64 卦全量自检 0 缺漏。**V0 引擎注释"2020-07-15=庚子"是笔误（实为己未），引擎本身正确**
- **通用组件** `src/components/paipan/`：tool-header（V0 ToolHeader：返回/衬线标题/说明/历史/分享+actions slot）、paper-card（gold 描边档）、section-title、today-hero；免责声明复用现有 `compliance/disclaimer.vue`

### 排盘主页重构（pages/paipan/index.vue·V0 v0.5-v0.7 首页）
- 今日时刻 Hero + 为你推荐收藏夹（V0 拖拽降级为「管理」编辑模式：全部工具格点选加减、推荐卡 X 移除、频次记录保留）+ 全部工具收起 3 排/展开全部 + 保留 AI 解盘卡/双人合盘卡/中医区/智能体横滚 + R4 小程序合规过滤原样保留
- tools-data.ts：加 category 分类/defaultFavoriteToolIds；新工具 href 切真页；`toolsApi` 的 if(true) 假后端调用已诚实化（工具清单=前端配置）
- **TodayHero 数据源 = almanac-lite（主包轻量真算）**：建除十二神宜忌/黄黑道十二神时辰吉凶/日支冲煞/日干财喜方位，全部通书正统口径；农历月日 H5/App 动态加载 lunar 补显，微信端显示节气行。与完整引擎是"简/全"关系非真假关系（已截图交叉验证同日口径一致：乙酉日冲兔煞东/未月酉日=满）

### 新工具 6 个（pages.json + router.ts 已注册，全本地计算零后端）
| 工具 | 路由别名 | 要点 |
|---|---|---|
| 万年历 | /paipan/wannianli | 日(撕历全量黄历)/月/年三视图+古今纪年转换；V0 假数据 tab（择日/档案/养生/历史今天）已砍 |
| 梅花易数 | /paipan/meihua (+/result) | 5 种起卦（时间/手动/数字1/数字2/自动），本互变错综五卦+体用生克+8类断语；历史本地存（rebu:meihua:history） |
| 小六壬 | /paipan/xiaoliuren | 三流派（道家/江氏/江氏2）六宫盘+六神六亲九星+36 日时断；农历已统一 lunar-typescript |
| 孔明神卦 | /paipan/kongming | CSS 铜钱摇卦+64 卦金钱课签辞（修 V0 源 3 处乱码） |
| 电子罗盘 | /paipan/luopan | 四盘制（廿四山/三合/综合/大卦）、三端指南针（uni.onCompassChange/H5 deviceorientation+iOS 授权）、无传感器诚实降级手动 slider |
| 紫微斗数 | /paipan/ziwei (+/result) | 原死文件重写：13 时辰宫格（分早晚子）输入+4×4 十二宫盘（三方四正高亮/宫干飞化/点宫详情）；旧 lib/ziwei-result-data.ts（6处if(true)）已删 |

### 顺手修复
- `components/agent/rich-message.vue` 的 `<component :is>` 在小程序端编译失败（上窗口智能体批遗留，当时只验了 H5）→ 改显式 v-if 分发。**这个修复随下次部署也会带上**

### 验证记录
- vue-tsc 0 / H5 build 绿 / mp-weixin build 绿·主包 1.87MB 达标（改造中一度 2.21MB，处理见体积策略）
- Playwright 走查 9 页 0 pageerror（唯一 console 噪音=本地访问生产 API 的 brand-config CORS，与本批无关）；截图 `C:\Users\Administrator\guoxue-e2e\shots\paipan-*.png`，脚本 shot-paipan-batch.js / shot-paipan-results.js

## 二、🔴 体积策略（下一批必读）

- 微信主包硬上限 2MB。本批教训：**node_modules 依赖一律进主包 vendor**（manifest 已开 optimization.subPackages 但只管 src 内模块）→ 大依赖必须**源码化放进分包目录**（lunar-typescript 已如此处理）
- **pkg-paipan 分包现已 0.84MB（上限 2MB）**。剩余 30+ 工具的引擎里还有大头：zidian 的 xinhua-dict.json/kangxi-strokes.json（大 JSON）、qizheng 依赖 astronomy-engine（npm，大）、qiming/xingming 依赖 pinyin-pro（npm）、zhuge 依赖 cnchar（npm）、jinqianke-data 62KB、taiyi/daliuren/jinkoujue 引擎各 20KB+
- **下一批开工前先规划分包拆分**：建 pkg-paipan2（占卜类）/pkg-paipan3（姓名字典类）等，或按 V0 分类拆；npm 依赖（astronomy-engine/pinyin-pro/cnchar）逐个评估源码化或替代，否则又顶爆主包

## 三、剩余批次（V0 还原待做清单）

1. **第二批候选（占卜类）**：大六壬（引擎已有黄金案例）、金口诀、金钱课、小成图、诸葛神数（cnchar 依赖）、太乙
2. **第三批候选（奇门风水类）**：阴盘奇门、命理奇门、飞宫小奇门、奇门穿壬、山向奇门（复用已搬 qimen-engine？注意存量 qimen 页不动）、玄空飞星、八宅、立极尺
3. **第四批候选（姓名历法类）**：起名（pinyin-pro+bazi-engine）、姓名解析、数字能量、字典（大 JSON）、节气文化页（jieqi-culture 27KB）、七政四余（astronomy-engine）、五运六气（中医区唯一真工具）
4. **升级类（需拍板）**：八字合盘 hepan-engine（存量 couple 是后端算，V0 是前端引擎，口径谁为准）、六爻 V0 版（存量 liuyao 页面存在但未注册的历史问题）
5. **易经工作台**（排盘中心/AI 报告/客户管理/账本，V0 mock 定价 ¥98/698/1980）：**延后待拍板真实定价与计费**
6. AI 解读：V0 的 13 个 /api/*/ai 路由（gemini）全部砍掉未还原，后续统一接后端 ai-gateway（DeepSeek）——可做成一个横向批

## 四、环境与事实备忘

- 本地 pm2：guoxue-api(:3000) + guoxue-nginx(:80，/h5 → C:\nginx\html\h5，已部署本批产物可直接真机看)
- H5 走查法：build:h5 → 拷 C:\nginx\html\h5 → guoxue-e2e 下 node shot-paipan-batch.js
- V0 源 zip 已解压在 D:\V0排盘工具\extracted，勿删（后续批次的真源）
- 生产未部署。**待部署队列：95b2002f（智能体批·含 SSE compression 关键修复）+ a0c3fc95（商城收敛）+ e9d7bd3d（favorites 修复）+ 2fe803b0（留档）+ f754594f（本批）**；冒烟清单见交接-智能体体验批收尾 §三
- 小程序端注意：本批新工具大多在 MP_HIDDEN_TOOL_IDS（R4 合规隐藏），微信端仅万年历/罗盘/节气等历法类可见——提审前记得跑 build:mp-weixin 确认 1.87MB 基线未涨
