# 全局代码坏味道审查报告（T4）

> 2026-07-05 · Fable 审查 · 只报告不动手 · 按性价比排序（高价值低风险在前）
> 总评：**代码库整体健康**（5300+ 测试全绿、TODO 仅 27 个、公共层收敛良好），以下是值得投入的改进点，供董事长决定优先级。

---

## P1 高性价比（建议 1-2 周内做）

### 1. 两个资金核心文件缺单测保护 【价值高·工作量 0.5-1 天】
- `modules/settlement/ledger-balance.service.ts` **无 spec**——这是 P2-c 结算转正的核心（净结算额=可提现口径计算），真实提现上线前必须有单测钉死"冲正负项/PENDING/FROZEN 排除"的语义。**这是本次审查最重要的发现。**
- `modules/fund-approval/fund-approval.service.ts` 无 spec（executor 有·B1 的 4 单测在 executor.spec）——审批流 CRUD 层补基础用例即可。
- 任务包：照 settlement.service.spec 的 mock 风格补两份 spec，重点覆盖 ledger-balance 的边界（空台账/全冲正/混合状态）。

### 2. 依赖高危漏洞三项 【价值高·工作量小时级】
| 漏洞 | 位置 | 修法 |
|---|---|---|
| vue-i18n 9.1.9 原型污染（high×2） | apps/mobile 直接依赖 | 升 9.14.x（小版本内升级，兼容风险低，升后跑 vue-tsc+build 三端） |
| @grpc/grpc-js 恶意报文 DoS（high×2） | server ← @opentelemetry/sdk-node 的 otlp-grpc exporter | 生产并未用 gRPC 导出通道——**评估直接移除 exporter-*-otlp-grpc 依赖**（否则升级 otel 全家桶 2.7→2.9 顺带解决） |
| jpeg-js 无限循环（high） | 溯源未定位（疑 uni-app 编译链 dev 依赖） | `pnpm why jpeg-js -r` 溯源；若纯 dev/编译期，降级为低优先 |

### 3. shop.service.ts 2174 行四域混杂 【价值高·工作量 2-3 天·风险中】
支付回调 / 订单履约 / 商品 CRUD / 分销归因四个领域挤在一个 service（且是资金最敏感文件，每次改动 diff 噪音大、审查困难）。
- 拆分方案：`shop-payment.service`（四条回调+completePayment+processPaidOrder）/ `shop-order.service`（下单/取消/超时 cron）/ `shop-product.service` / 归因逻辑并入 commission 域。controller 不动（注入拆后的 service），spec 随迁。
- **注意**：拆分是纯搬家不改逻辑，必须一次一个域、每步全量 jest 回归；建议在真实支付通电**之前**做完（之后改支付文件的成本会陡增）。

## P2 中性价比（有窗口再做）

### 4. 分页逻辑 207 处手写 vs 公共函数仅 7 处采用 【工作量分批·纯机械】
`safePagination` 公共函数已存在但采用率极低，207 处手写 `skip/take` 中混有页码越界/pageSize 无上限的不一致实现。建议：新代码强制用公共函数（写进 CLAUDE.md），存量按模块分批收敛（适合派 agent 机械改造，每批附全量回归）。

### 5. 巨型 service 第二梯队 【工作量各 1-2 天】
circle.service(1528) / course.service(1446) / recommend.service(1347) / offline.service(1158)——同 shop 的病但症状轻。**优先级低于 shop**，触发时机：当某文件下一次要做大 feature 时"先拆再加"。
（注：tool-registry/calculators 系列 800-2100 行是卦辞/签文数据字面量密集，不是坏味道，勿拆。）

### 6. 类型逃逸集中区 【工作量 0.5-1 天】
`tool-ai.service`(65 处) / `export.service`(54 处) 两个文件占了逃逸大头。export 的 Excel 拼装场景 any 可容忍；tool-ai 建议给 AI 响应定 3-4 个窄接口收掉一半。全局已收敛过（lib 687→42 的历史战果），**重点是防回潮**：建议 lint 规则把 `no-explicit-any` 从 warning 提为局部 error（common/ 与资金模块目录先行）。

## P3 观察项（不建议现在投入）

- TODO/FIXME 27 个：健康水位，多为"密钥后接线"类，随外部依赖自然消化。
- otel 全家桶 2.7→2.9、ioredis 5.11.0→5.11.1 等小版本升级：无 CVE 驱动不必追新，季度统一升级即可。
- 前端 pkg-circle(0.69MB)/pkg-mine(0.56MB) 分包体积：距 2MB 分包上限远，不动。

## 给后续执行者的任务包索引

- 补两份资金 spec → 照 P1-1，验收=jest 全绿+覆盖边界用例 ≥8 个
- 依赖修复 → 照 P1-2 表，验收=pnpm audit 无 high+三端 build 过
- shop 拆分 → 照 P1-3，验收=行为零变化（全量 jest+支付链路 spec 逐一过）+每个新文件 <600 行
