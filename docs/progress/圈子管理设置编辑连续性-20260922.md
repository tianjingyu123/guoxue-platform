# 圈子管理设置编辑连续性

本批基于 `366e26560a89d2de54c1527123db7eac8232344a`，在原隔离分支接续。源码、测试与本记录同提交。未部署、未执行真实管理或资金操作。

## 已完成

- 首次设置请求去重；切换成员、内容、设置时保留未提交表单，不被后台重读覆盖。
- 设置加载错误不再依赖旧 overview 判断成功；读取失败隐藏表单，允许重试。
- 显式返回按钮提示未保存设置及公告，可继续编辑；上传、保存、发布过程中阻止该返回动作。
- 保存失败保留输入，保存期间重复提交拦截；保存成功只标记实际提交的快照，期间新输入仍属未保存。
- 上传封面完成前不允许保存旧封面；发布公告失败保留输入，发布成功也不清除期间新增的公告文本。
- 公告读取失败独立提示和重试，不再伪装没有公告。撤除未经本批验证的“全体成员将收到通知”占位承诺。
- 延续圈子中性背景，分区和返回至少44px触达；顶栏预留微信胶囊区域。

## 本轮验证

- `tests/release/circle-settings-browser.mjs`：6组通过。全部接口拦截为夹具，保存请求仅为模拟，不访问真实用户数据。
- 移动端类型检查通过。
- 微信构建通过：615个WXML、2683个文本产物；主包1.63MB，上传保守口径1.91MB，圈子分包0.96MB，最大分包1.94MB。
- 本地证据：`artifacts/circle-settings-20260922/results.json`、`settings.png`、`mp-build.log`。截图已检查。
- 公开环境目标沿用前批 formal/API `https://api.rebugx.cn`、H5 `/h5/`、资源 `https://static.rebugx.cn`，本批不修改配置；不构成线上版本核验。

## 基线风险及发布约束

1. **多圈嘉宾分账定位待独立修复。** `circleGuestsApi.list/setShareRate` 未传当前圈子；后端 `CircleBackendController.resolveCircle` 仅平台管理角色支持显式 circleId，普通圈主通过 `getMyCircle/findFirst` 定位。因此不能假定多圈管理页面展示和修改的分账必然属于当前圈。本批未修改资金接口；正式开放该操作前必须完成圈子定位与权限回归。
2. 显式返回按钮有未保存提醒；系统手势、浏览器后退、关闭小程序仍未覆盖。不是持久化设置草稿，退出进程不会恢复。
3. 成员角色变更、移除、删帖和实际公告发布未操作。小程序真机、APP安全区/键盘/返回未验证。本批不等于全部管理子页验收完成。

回退：撤回本批独立提交，保留前批管理概览及个人事务；没有数据库回退。状态：代码候选完成、本地隔离验证完成、线上状态未知、待发布决策。
