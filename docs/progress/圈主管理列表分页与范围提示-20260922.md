# 圈主管理列表分页与范围提示

本批接续隔离分支 `codex/integrated-live-gift-paipan-pay-20260920`。圈主管理原先只请求成员和已发布内容的第一页（各 50 条），且没有后续入口；列表、搜索和角色/置顶筛选容易被误解为覆盖全圈。

现在管理页显示已加载条数与服务端总数，提供“加载更多”，第二页失败时原列表仍可用并可重试。搜索和筛选仍在已加载范围内进行，页面明确提示这一限制，不把空筛选结果说成全圈没有匹配项。服务端为同时间的成员及内容补稳定排序的 ID 次序，避免分页边界顺序不确定。未改角色权限、内容治理动作或资金接口。

验证：本地 H5 拦截全部业务接口，使用 53 名成员和 53 条内容夹具，覆盖第二页失败重试、完整加载和请求页码；4 组断言通过，浏览器异常 0、业务写请求 0。移动端类型检查、服务端构建、圈帖定向测试 2/2 通过；最终源码小程序构建通过，615 个 WXML 与 2683 个文本产物校验通过，主包 1.63 MB、保守上传口径 1.91 MB、圈子分包 0.97 MB、最大分包 `pkg-paipan2` 1.94 MB。构建使用隔离树已有公开配置：`formal`，API `https://api.rebugx.cn`，H5 `https://api.rebugx.cn/h5/`，资源 `https://static.rebugx.cn`；这只记录编译目标，不证明域名当前节点或候选已上线。

限制：当前搜索和筛选是已加载页的本地处理；若要全圈精确搜索/筛选，需增加对应服务端查询，并单独验证权限和分页。小程序开发者工具实际加载、真机长列表触控和线上接口仍未覆盖。本批为本地代码候选，未部署、未执行真实管理写入。
