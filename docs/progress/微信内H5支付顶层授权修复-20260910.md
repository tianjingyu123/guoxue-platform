# 微信内 H5 支付顶层授权修复（2026-09-10）

## 范围与证据

- 只处理微信内 H5 公众号 JSAPI；APP 支付和微信外 H5 支付未开通，不属于本次验收。
- 安卓 OnePlus LE2100（a07f9db3）复现：支付页点击继续微信授权后空白，商城顶层地址未变，左侧内容详情浮层关闭柄仍在。
- 桌面 DOM 只用于承载结构核验：商城商品详情处于 `iframe`，源地址带 `__contentLayer=1`。原授权使用当前窗口跳转，因此留在 iframe 内。
- 线上静态回跳页面和 OAuth 地址接口正常。服务器本次失败时没有支付授权回跳记录。

## 修改与验证

- 基于当前线上提交 `751e9a94`，独立分支 `codex/wechat-pay-top-level-20260910`；不包含未上线的 openid 复用候选方案。
- 商品详情使用完整页面；旧内嵌支付入口自动提升到同源顶层并保留订单参数；支付和登录授权统一从顶层跳转，限定微信 OAuth 域名和路径。
- 4 项行为测试通过；正式 H5 构建通过；差异检查通过。
- 包 `h5-wechat-top-level-20260910.zip`，1142 文件，3859689 字节。
- 包 SHA256：`e432694a5583a67b08fa6a1000aeaba08054bc6f4c8bb99cb4344bbed492586f`。

## 两节点发布

腾讯云 TAT 命令 `cmd-css959ia`，只更新静态 H5，保留旧哈希资源，入口原子替换，不重启服务、不修改数据库和支付配置。

- 首次执行 `inv-a8gc7vg06j`：包校验通过，但脚本错误使用 startsWith 匹配回跳页实际 indexOf，发布前停止，线上未替换。修正匹配后再执行。
- cvm-a / ins-0sen8yvf / 62.234.115.193：`inv-j8gcd9gc56` 成功，退出码 0。
  备份：`/opt/guoxue/h5-release-backups/h5-wechat-top-level-20260910.8yaCww/h5-before-publish.tar.gz`。
- cvm-b / ins-n0sqc627 / 49.232.184.7：`inv-j8gcgvgqic` 成功，退出码 0。
  备份：`/opt/guoxue/h5-release-backups/h5-wechat-top-level-20260910.B0qSv5/h5-before-publish.tar.gz`。
- 两节点及公网入口 index SHA256 一致：`db105ed55879acf1898b4bd6c8bea2dd127adf6405b75568ab124f07c02d54a2`，入口脚本 `index-BPTzn28H.js`。
- 替换前严格校验原入口 SHA256 为 `b8321d41dba1928745cd639188aed2caf78ef995c65a3f77c57a6f4defc24e78`，防止覆盖并行发布。

## 真机验收状态：尚未完成

2026-09-10 北京时间约 16:09：微信刷新商城成功；点击 0.01 元预发布验收商品后，微信显示“将要访问……无法确认该网页的安全性，请谨慎访问”，目标为本站商品详情 URL。已停在安全提示页，要求用户自行处理“继续访问”，没有自动绕过。

必须继续核验：商品详情 → 新订单 → 微信支付 → 继续微信授权 → 静态回跳 → 微信收银台。尚不能声称白屏已彻底解决或支付成功。最终付款由用户操作。

真机证据在主工作区 `artifacts/wechat-white-screen-20260910`：`wxcurrent.png`、`oauth-after.png`、`refreshed.png`、`products.png`、`detail.png`。含个人信息的菜单图、原始日志与临时签名地址不纳入提交。
