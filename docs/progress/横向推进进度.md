# 前端横向推进进度（全域贯通与品质优化）

> 纵向各业务模块已完工，现按"全局维度"横向扫平全平台，目标=国际一流、无死角上线。
> 用户授权：自主连续推进，发现问题直接改，需拍板的集中到最后。延后模块(im14/live5/排盘17)统一诚实降级。
> 技术栈=uni-app 多端：pages.json 路由 + navigateTo；数据走 lib/*-data.ts 的 xxxApi；rpx+scss；CSS令牌 var(--xxx) 见 styles/tokens.scss。

## 批次总览

| 批 | 内容 | 状态 |
|---|---|---|
| 0 | 全局体检（4 agent 并行勘察） | ✅ 完成 |
| 1 | 地基：uni.scss + 全局组件 + 故宫红令牌化 | ✅ 完成·vue-tsc全绿 |
| 3 | 入口贯通·死链路由补全 | ✅ 完成（入口UI建设待续） |
| 2 | 容错铺设：app-error 覆盖 + 图片懒加载 + 骨架屏 | ⬜ 待续（统一组件已就绪） |
| 3b | 入口UI：孤岛聚合入口 + 身份切换补全 + 商品→商家 | ⬜ 待续（需拍板，见文末） |
| 4 | 脸面：搜索栏下放 + 客服FAB + 分享收编 | ⬜ 待续 |
| 5 | 推荐+消息：recommend组件 + 公告收编 + 全局角标 | ⬜ 待续 |
| 6 | 埋点：统一 track + 异步上报 | ⬜ 待续 |
| 7 | 总验收：vue-tsc/scan 全绿 + 真机端到端 | ⬜ 待续 |

## ✅ 已交付（本窗口，均经 vue-tsc 全量 EXIT=0 验证）

### 第1批·地基
- `src/uni.scss`：SCSS 层（$space/$radius/$font/$shadow/$z 令牌 + card-base/cover-3x4/line-clamp/ellipsis/hairline/press-feedback/safe-area-bottom/price-font/flex-center mixin）
- `src/composables/useToast.ts`：统一四色 Toast（解 559 处散乱 uni.showToast）
- `src/composables/useConfirm.ts`：统一确认弹窗（danger 红色，解 40 处散乱 uni.showModal）
- `src/composables/usePreviewImage.ts`：统一图片预览（替代孤儿 image-viewer 页）
- `src/components/common/app-load-more.vue`：统一加载更多页脚
- `src/components/common/app-network-bar.vue`：断网提示条（uni-app App.vue 无视图层→需枢纽页引入）
- `app-error.vue`/`app-empty.vue` 令牌化；`error-state.vue` 收编为 app-error 薄封装（不动 52 处调用页）
- 故宫红 codemod：`scripts/codemod-brand-color.mjs`，**2011 处/353 文件** `#c41e3a`→`var(--brand)`（仅 style 块，已写回，暗色自动跟随）
- `package.json` 补 `type-check`(vue-tsc) + `scan-mock` 脚本

### 第3批·死链路由补全（utils/router.ts + lib/profile-data.ts）
- 补 ROUTE_MAP：`/ebook` 全套（pkg-ebook 此前整包零映射）、`/operator` `/operator/dashboard` `/operator/quota` `/operator/station-master-panel`、`/join/operator` `/join/station`、`/creator/live/console`
- 补 DYNAMIC_ROUTES：`/ebook/:id` → detail、`/ebook/:id/reader` → reader
- profile-data 修死链入口：「我的电子书」`/downloads`→`/ebook/bookshelf`；station_owner roleHref `/mine/role-panels/...`→`/operator/station-master-panel`

### 第4/5/6批·全局基础设施组件预建（地基，接入待续）
> 这些是脸面/推荐/埋点批次的统一组件，先建好骨架（设计精良、令牌化、vue-tsc 全绿），续作只需"接入"而非"从零建"。
- `src/components/common/search-bar.vue`：统一搜索栏（AI 徽章 + defaultTab 智能选 Tab + keyword 回显）→ 替换 40+ 页手写搜索框
- `src/components/common/customer-service-fab.vue`：全局客服悬浮按钮（各枢纽/详情页引入）
- `src/components/common/recommend-section.vue`：统一「猜你喜欢」区块（标准化 RecommendItem，空则不渲染，3:4 封面）
- `src/composables/useTrack.ts`：统一埋点（pageView/click/purchase/search/share，批量异步上报，端点未就绪静默降级）
- `src/composables/useShare.ts`：统一分享（onShareAppMessage/Timeline 配置生成 + 统一海报跳转，内置埋点）

**全部 10 个新建文件经 vue-tsc 全量 EXIT=0 验证。**

---

## ⬜ 续作计划（建议新窗口按序执行；每件都给了精确落点）

### 第2批·容错铺设（统一组件已就绪，纯铺设）
- **图片懒加载**：列表/卡片封面 `<image>` 补 `lazy-load`（排除首屏 banner/头像防闪）。可写 codemod 但需排除首屏，建议按分包 agent 精做卡片组件（components/cards/*、components/home/feed-card 等）。
- **错误态覆盖**：app-error/error-state 当前仅 ~54 页接入。给数据驱动的 home/list/detail/feed 类页补 error ref + 模板 `<error-state v-else-if="error" @retry="loadXxx"/>`。按分包 3-4 agent 并行（文件隔离），模式见 CLAUDE.md 三态铁律。**不求 486 页全覆盖，优先高频数据页**。
- **骨架屏统一**：app-skeleton 已有，把各页裸 `.skeleton` 拼装收敛为列表/卡片级骨架组件。
- **统一组件迁移**（可随手/低优先）：559 处 uni.showToast→`toast.xxx`、40 处 uni.showModal→`useConfirm`、图片点击→`usePreviewImage`。入口已统一，迁移是机械活。

### 第3b批·入口UI建设（需先看 lib/discover-data.ts + 我的页身份逻辑）
- **孤岛聚合入口**：merchant/operator/institute/offline/bounty/qa/expert/ebook 路由虽已注册，但发现/我的页缺卡片入口。建议在发现页加"全部服务"矩阵页 or 我的页"更多身份/服务"区。**布局需拍板**。
- **身份切换补全**：我的页身份区补 merchant/operator 的"去开通"入口（applyRole 现仅 toast）。
- **商品→商家**：pkg-mall/product/detail 补"进店"入口 → 需 C 端店铺页（merchant 现有 shop-preview 是后台预览，可复用或新建）。**需拍板**。
- **发现·精选专栏**：4 个专栏 goColumn 现全 toastComingSoon，需接 `/topic/:tag` 或新建专题页。

### 第4批·脸面（依赖统一组件；部分依赖后端）
- **搜索栏下放**：抽统一 `SearchBar` 组件（含 AI 徽章），替换 40+ 业务页各自手写的搜索框，统一跳 `/search` 并按来源页智能选默认 Tab。
- **搜索结果真连**：`pkg-search/search/result.vue` 现 100% mock。**需拍板：后端补聚合搜索端点（真连）还是前端诚实降级**。结果 Tab 缺电子书/智能体/用户。
- **客服 FAB**：做全局 `CustomerServiceFab` 组件，在主要枢纽页引入（uni-app 无全局层）；simple-chat 改流式输出 + 转人工。
- **分享收编**：圈子/赛事/分站三套海报收编为一套 poster-data 体系；补 `onShareAppMessage`/`onShareTimeline`（全平台 0）；poster-data 真连 + 动态带参二维码；补课程/商品/古籍/证书/个人主页分享场景。

### 第5批·推荐+消息
- **统一 recommend-section 组件**，补 商品详情「经常一起购买」/课程详情「学了还学」/圈子详情「相关圈子」/我的页推荐（现返回 []）。`/discover/recommendations` 端点已存在可复用。
- **首页编排**：类型交替（复用发现页 bucket 轮询）、已购/已加入去重（前端用户态过滤）。
- **消息收编**：pkg-notices 公告页（mock+职能重复）并入 pkg-mine/notifications 消息中心；app-header 铃铛未读写死 true→接后端、跳转修正（现误跳私信）。

### 第6批·埋点
- 建 `composables/useTrack.ts` 统一行为埋点（页面浏览/点击/购买/搜索/分享），异步上报，字段含 userId/ts/path/action。后端需对应 `/track` 端点（或先本地队列）。

### 第7批·总验收
- `pnpm --filter @guoxue/mobile type-check` 全绿 + `scan-mock` 仅剩 live 延后块
- pkg-expert call/booking 5 处 Math.random 人工甄别（是否假数据）
- 真机端到端（需用户自己终端启后端，见记忆 backend-restart-gotcha）

---

## ✅ 用户已拍板决议（2026-06-28）
1. **搜索结果** → 后端补聚合搜索端点·真连（多类型并行检索+AI总结）
2. **孤岛入口** → 发现页「全部服务」矩阵 **✅ 已实现**（serviceGroups + 发现页渲染，覆盖 8 孤岛）
3. **商品→商家** → 新建 C 端店铺页
4. **全局组件铺设** → 主要页（枢纽 + 详情）
5. 主播中心入口 → 暂指 /pkg-live/console/index（我的默认，未否决前沿用）
6. 我的电子书 → 改指 /ebook/bookshelf（我的默认）

## 接入层续作

### ✅ 已交付（2026-06-28 本窗口·均经 vue-tsc 全量 EXIT=0 + scan-mock 仅剩 live 延后块）
- **客服FAB + 网络条**：5 tab 枢纽页(index/circles/paipan/discover/profile)同铺 `<app-network-bar/>`+`<customer-service-fab/>`；12 个详情页(course/mall-product/circle/classics/ebook/poetry/expert/video/competition/bounty/qa/activity)铺 `<customer-service-fab/>`，统一放根 view 最外层/多根页作无条件首根节点(覆盖所有状态)。reader 沉浸阅读页与 report(排盘延后)/notices(将收编)/order 未铺。`/customer-service`→`/pkg-agent/agent/customer-service` 路由已通。
- **recommend-section 接入**：新建 `lib/recommend-data.ts`(真连 `GET /recommend/:scene` + VO→标准卡映射 + type→href + metadata社交证明副标题 + 诚实降级)；商品详情「经常一起购买」(product_detail)、课程详情「学了这门课的人还在学」(course_detail)、圈子详情「相关圈子」(guess_like)、我的页「猜你喜欢」(guess_like)。**🔴受后端唯一正式开关 `recommend_algorithm` 门禁**——FeatureFlagGuard 未开则抛 404→前端空→区块不渲染(诚实降级)；开关由运营控制，开即生效。2026-07-23 已将控制器、种子和前端说明统一回生产长期使用的正式键，`recommend_engine` 仅为 7 月 18 日止血产生的生产冗余键，待新版上线验证后删除。我的页清理了旧死空 recList 区块 + `profileApi.recommendations()` 死方法(去重)。
- **search-bar 统一下放**（2026-06-28）：5 页手写导航搜索框→`<search-bar default-tab>`（discover=all / course-market+home=course / mall-home+shop-home=product，mall/shop 原 toast 占位顺带修复为真跳转）；清理失引用 handler。**古籍/视频垂直搜索页有意保留**（专搜书名/朝代体验优于全局 /search）。
- **搜索结果真连**（2026-06-28·三验证全绿）：后端补 Ebook 搜索 config（runFts+runLike 两处缺口·9 类型齐全·jest 48/48·tsc 绿）；新建 `lib/search-data.ts` 适配层（后端 9 数组→7 类展示结构·articles+videos+contents 合并为「内容」·缺字段降级）；`result.vue` 去 100% mock 真连（8 Tab 增古籍/电子书·AI 总结接 `POST /search/ai/summary` 失败静默隐藏·三态·关键词高亮保留）。原型大量后端无字段(rating/originalPrice/followers/postCount/comments)按铁律 v-if 删除。🔴端到端需用户启后端验证。

- **商品 C 端店铺页**（2026-06-28·三验证全绿）：🔍发现 DB `Product` 表**无 merchantId 列**（schema 有该字段+index 但从未落库）→改用 `Product.userId = Merchant.userId` 关联（实测 3 活跃商家/5 件商家商品）。后端 getProduct 补 merchant 公开信息（仅 ACTIVE 商家）+ 新增 `getStore` + `GET /shop/store/:merchantId`（tsc 0/jest 40）；前端 shop-data 补 Store 类型/getStore/ProductDetail.merchant；新建 `pkg-shop/store/index.vue`（店铺头+商品网格+三态）+ router 动态路由 + pages.json 注册；商品详情价格区下加「进店」入口（merchant=null 即自营商品→v-if 诚实降级）。

- **useShare 接入**（2026-06-28·vue-tsc 0）：8 个内容详情页（课程/商品/圈子/古籍/电子书/诗词/视频/赛事）补 `onShareAppMessage`+`onShareTimeline`（复用 useShare composable·title/path/cover 取页面真实数据 + 诚实兜底·无封面图的省略 cover·视频取当前播放项·competition coverImage 修 null）。⏳海报三套收编 poster-data + 证书/个人主页分享未做（次要场景，可后续）。
- **schema↔DB 一致性核查 + 修复**（2026-06-28）：脚本 `apps/server/scripts/schema-db-check.ts` 全库核查→267 model 仅 2 处未落库（Content/ContentAuditRecord.deletedAt）；已 `add-deletedat-columns.ts` 补列对齐，拆除「generate 后 content 裸查询 500」炸弹。详见记忆 [[guoxue-schema-db-consistency]]。

- **公告并入消息中心 + 铃铛角标真连**（2026-06-28·vue-tsc 0 + scan-mock 仅剩 live 延后块）：
  - 🔍 后端无独立公告(announcement)模型——**平台公告本质=SYSTEM 类 notification**；`/notices` 平台公告页是 mock 孤岛、**C 端无入口**、职能与消息中心重复。
  - **铃铛角标真连**：`app-header` 铃铛未读由写死 `true`→真连 `GET /notifications/unread-count`（新增 `mineApi.getUnreadNotifyCount`·未登录/异常降级 0）；小红点升级为**带数字角标**(99+)；跳转由误跳私信 `/im/conversations`→修正为消息中心 `/notifications`。app-header 是常驻子组件(页面 onShow 不触发)→用全局事件总线 `uni.$on('notify:refresh')`，消息中心标记已读(单条/全部)后 `uni.$emit` 同步角标。
  - **公告并入**：消息中心 `pkg-mine/notifications` 新增**分类 Tab**(全部/系统公告/互动/交易·service→系统、income→交易)，「系统公告」Tab 即承载平台公告；filteredNotifications 纯前端按 kind 过滤(不需后端改)，per-Tab 未读红点。pkg-notices mock 页保留供 `/station/notices` 驿站复用，C 端不再暴露入口(职能已被系统 Tab 覆盖)。

- **第2批容错：图片 lazy-load + error 态覆盖**（2026-06-29·vue-tsc 0 + scan-mock 仅剩 live 延后块）：
  - **图片懒加载**：7 个被各列表页复用的共享卡片组件列表封面(cover-img)补 `lazy-load`——cards/{video,live,product,course}-card、circle/{circle,post}-card、home/feed-card。**头像/首屏 banner(home-banner)/本地装饰 svg(paipan-guide) 有意排除**(防闪+本地无需懒加载)。卡片是复用最广的封面来源，收益最大。
  - **error 态覆盖**：🔍 关键发现——初始统计用 `error=ref` 模式**漏算了 `errMsg=ref('')` 形式**(早期实现)；实查 pkg-institute(8页)/pkg-offline(11页)/pkg-video creator(4页) **早已具备完整 error 态**(loading→errMsg分支[alert-circle+文案+重试]→empty→content)，3 个并行 agent 核实后正确未破坏。真正缺 error 态的高频数据展示页仅 4 个，已补：`pages/circles/index`(圈子主tab·手动)、`pkg-mine/notifications`(消息中心·手动·首屏失败整页 app-error+刷新失败保留旧列表)、`pkg-bounty/{index,detail,my}`+`pkg-qa/index`(agent·均用 easycom `<app-error>` 无 import·loading→error→empty→content)。bounty/qa 仍 setTimeout mock(无失败路径)→error 分支为三态脚手架，真连时接 catch 即生效。
  - **结论**：其余 53 页差集全是**表单/向导/操作/静态/延后(im/排盘/live)页**，loading 是「提交中」非「数据加载」，error 靠 toast 反馈即可，无需加载重试态。全平台 error 态覆盖已实质完整。

- **第6批埋点：全链路闭环**（2026-06-29·后端 tsc 0 + jest 6/6·前端 vue-tsc 0 + scan-mock 仅剩 live）：
  - **后端从零建 track 子系统**：Prisma 加 `TrackEvent` 模型(id cuid/userId可空/action/path/payload Json/occurredAt/createdAt·双索引·**不建外键**以免阻塞用户删除)；新建 `modules/track`(dto/service/controller/module)；`POST /track/batch` 用 `OptionalAuthGuard`(**可匿名上报**·有 JWT 关联 userId)；service 批量截断 50 条/action 截 64/path 截 256/**落库失败静默吞**(埋点永不阻塞主流程)；app.module 注册 TrackModule。jest 6 例(空/关联/匿名/截断/超长/异常)全过。
  - **建表踩坑**：`prisma generate` 遇 **EPERM**(运行中后端进程锁住 query_engine dll)→但**类型已写入 index.d.ts**(tsc 能识别)，仅运行时 dll 未换；`db push` 是全量同步会**误删无关的 `_quality_snapshot`(44行)**→改用 **`prisma db execute --file` 精准建表**(只加 TrackEvent·dangerouslyDisableSandbox 前台连 5433)，mcp postgres 只读验证表结构 OK。
  - **✅ 端到端实测通过**(2026-06-29·端口3000/前缀api/v1)：`POST /track/batch` 返回 `{ok:true,count:N}`、数据真落库、匿名 userId=null、ts→occurredAt 转换正确。**🔍 发现并修复 path 被污染**：全局 `SanitizePipe` 把 `/`→`&#x2F;` 转义(path 落库成 `&#x2F;mall&#x2F;home`)，因 `path` 漏在 `SKIP_FIELDS` 白名单外(与已豁免的 url/href/link/src/redirectUri 同属路径类)→补入白名单，复测 path 干净存为 `/mall/home`。**踩坑**：dist/main 占端口致 `pnpm start` 重启不生效(EADDRINUSE·PID 不变)→须先杀旧进程释放 3000 再启(见 [[guoxue-backend-restart-gotcha]])。
  - **前端接入**：`App.vue` onLaunch 用 `uni.addInterceptor` 拦截 navigateTo/redirectTo/reLaunch/switchTab 的 invoke **全局统一上报 page_view**(一处接入·全局覆盖·无需逐页改)，onHide flush 队列；搜索中间页 `doSearch` 接 `track.search(q)`(搜索词分析)；`useShare` 已内置 share 埋点。useTrack 批量异步上报(20条/3秒·端点失败静默)。

### ✅ 埋点深化·购买转化闭环（2026-06-29·vue-tsc 全量绿 + scan-mock 仅剩 live 延后块）
- **5 个购买转化点全接入 `track.purchase`**（均 `import { track } from '@/composables/useTrack'`）：
  - 课程 `pkg-course/detail/index.vue` `onPurchased()`→`{type:'course',id:course.value?.id,amount:course.value?.price}`
  - 圈子 `pkg-circle/circles/detail.vue` `onPurchased()`→`{type:'circle',id:circle.value?.id,amount:circle.value?.price}`
  - VIP `pkg-profile/vip/index.vue` `handlePurchase()` setTimeout 内→`{type:'vip',planId,level,amount}`
  - 商城 `pkg-shop/paying/index.vue` **轮询到 `st.paid`(后端真实确认)那一刻**→`{type:'shop_order',orderId,amount,method}`（🔍 比 pay-success/payment-result 落地页更准——后者 amount 是写死/query mock；paying 的 st.paid 是后端真状态）
  - 电子书 `pkg-ebook/checkout/success.vue` `fetchData` 成功后→`{type:'ebook',bookId,orderId:order.orderNo,amount:order.amount}`
- **埋点全链路现状**：page_view(App.vue 全局拦截)✅ + search(搜索中间页)✅ + share(useShare 内置)✅ + **purchase(本轮 5 点)✅**。剩 `track.click`(卡片/CTA·共享卡片组件颗粒·价值递减) + 运营侧查询/看板端点(需后端开发)未做。

### ✅ 埋点 `track.click`·共享卡片点击信号（2026-06-29·vue-tsc 全量 EXIT=0）
- **8 个共享卡片的点击跳转函数统一埋 `track.click('<type>_card', { id })`**——商品/课程/视频/直播/古籍/智能体/圈子/首页feed-card。
- **零破坏关键**：8 卡片全是**自包含跳转**模式（`open()`/`go()`/`openDetail()` 内 `navigateTo`，circle-card 另有 `emit('join')` 保持不动）→ 在跳转前加一行埋点，所有复用页（首页流/各列表/推荐区/榜单）自动覆盖，无需改任一调用方、无 props/emit 变更。
- feed-card 双分支（feed item 按 type 跳带 `{type,id}` / agent 跳对话带 `{type:'agent',id}`）；agent-card 带 `from` 上下文。target 命名统一 `${type}_card`，与 useTrack 注释风格一致。
- **🩹 工具污染插曲**：读 feed-card 时 Grep/Read 返回乱码(`const t\=2/g'` 等幻觉)，改用 PowerShell `Get-Content` 按行号读到真实源码(函数名实为 `go()` 非污染显示的 `onCardTap`)，Edit 字节匹配正常。见记忆 [[tool-context-hygiene]]。

### ⬜ 待续
- 统一组件机械迁移（低优先·可随手）：559 处 `uni.showToast`→`toast.xxx`、40 处 `uni.showModal`→`useConfirm`、图片点击→`usePreviewImage`（入口已统一，纯机械活）
- 埋点收尾（可选）：关键 CTA 按钮 click（如「立即购买」「加入」按钮本身，区别于卡片进详情）、运营侧埋点查询/看板端点需后端开发。
- 埋点全链路至此：page_view + search + share + **purchase(5点) + click(8卡片)** 全闭环；剩 CTA 细颗粒 + 运营看板。
- **pkg-expert Math.random 甄别 + 红线修复**（2026-06-29·vue-tsc 0）：甄别结论——`call/index:57` waveBars 是**音频波形 UI 装饰动画**(非业务数据·合规保留)；`booking/index` 3 处 `isOccupied=Math.random()` 是**伪造时段占用假数据**(违铁律)→已消除(改诚实实现:营业时段固定·不伪造占用·今天已过时段不可约·`isOccupied:false`)。**🔴深层发现**：pkg-expert 整模块(index/detail/booking/call)**无任何入口+无数据层=孤立死页**；且后端达人咨询是**圈子内场景**(`CircleMember.callPricePerMinuteCoin`+`ConsultCall`绑 circleId)，**平台级独立"专家"后端不存在**——pkg-expert 是原型虚构。用户曾选"后端补预约子系统(B)"，但前提(booking 是真实功能)不成立→**暂不为死页建后端**，仅拆假数据红线。**✅ 最终决议(2026-06-29 用户拍板)=搁置 pkg-expert·统一到圈子达人**：废弃死页不上线，专家咨询沿用已真连的圈子达人体系(图文+即时通话)，预约需求未来在圈子内加 ConsultBooking(绑 circleId)按需再做。**🔧 上线前清单项**：从 pages.json 摘除 `root:pkg-expert` 分包 + router.ts 四条映射(/experts·/expert/:id·/booking/:expertId·/call/:id)。代码暂保留供未来圈子达人预约 UI 参考(非 git 环境·删除不可逆)。
- 第7批总验收：真机端到端（需用户自己终端启后端·`/track/batch` 落库✅已实测）

## 🔴 原待拍板清单（备查）

1. **主播中心入口**：streamer 身份/申请此前死链，我暂指向 `/pkg-live/console/index`（直播控制台）。确认是否正确？（备选 manage/host-data）
2. **「我的电子书」语义**：我从 `/downloads`(通用下载) 改指 `/ebook/bookshelf`(电子书书架)。确认语义对？
3. **商品→商家店铺页**：新建 C 端店铺页，还是复用 merchant/shop-preview（现为后台预览页）？
4. **搜索结果真连**：后端补聚合搜索端点（多类型并行检索+AI总结，真连），还是前端诚实降级占位？你有后端最高权限授权，倾向哪个？
5. **孤岛聚合入口布局**：8 个已完工分包（商家/分站/书院/驿站/悬赏/问答/达人/电子书）入口放哪——发现页"全部服务"矩阵页？我的页"服务中心"区？
6. **全局组件铺设范围**：客服FAB、原生分享（onShareAppMessage）在 uni-app 需逐页挂载，全部页铺还是仅主要枢纽+详情页？
