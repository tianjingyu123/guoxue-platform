# H5支付设备区分与登录身份复用候选（2026-09-13）

当前仅本地候选，未发布；线上仍为1b312e41。

用户报告：手机外部浏览器点击现有“支付宝支付”按钮能够拉起支付宝并付款。本次未独立匹配这笔新交易订单，不能与此前已核对的0.01元订单混同。

修改：手机渠道名称为支付、电脑为扫码；手机默认折叠付款码并保留原订单显式支付宝拉起，付款码可手动展开作为备用。微信内不虚构支付宝直拉能力，保持外部浏览器引导。云闪付入驻22000005阻断未解决。

微信支付页新增查询本人同付款公众号的H5身份：后端JWT保护、精确账号/namespace/AppID匹配、禁止缓存响应；唯一身份优先复用，多个身份不猜测。缓存隔离账号和公众号，不读旧无归属缓存。前端身份查询期间切号则停止。缺少身份或旧服务无此接口，保留原必要授权和静态回跳路径。因此去掉重复授权需要前后端配套部署，不能只发布H5。

验证：27项支付测试通过；mobile vue-tsc通过；构建本地shared后server tsc --noEmit通过；使用现用api.rebugx.cn公开地址H5构建通过；git diff --check通过。尚未发布或进行此候选真机验收，无真实交易、数据库或商户配置操作。

证据：artifacts/h5-existing-order-cashier-20260913/device-auth-tests.log、device-auth-server-types.log、device-auth-build.log（主工作区）。