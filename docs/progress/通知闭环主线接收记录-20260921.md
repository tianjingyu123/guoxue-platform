# 关键业务通知闭环主线接收记录（2026-09-21）

## 接收结果

- 实际主线基线：`release/integration-20260801`，接收前 HEAD `2e458597a`。
- 接收分支：`codex/notification-audit-integration-20260921`，独立工作树：`D:\gx-deploy-91\.worktrees\notification-audit-integration-20260921`。
- 按 `notification` → `shop-payment` → `shop-refund` → `shop.module` 顺序核对候选。候选两个提交均未拆分出可安全跳过的等效改动，按完整提交链接收：`01bb95a91`、`b552fe942`；接收后 HEAD：`5a1ef4b32`。
- 四步接收均无冲突，未覆盖主工作区未提交文件；主工作区仍保持原状。
- 撤回顺序：先 `5a1ef4b32`，再 `dda24196d`，或按接收提交分别执行 `git revert`；不执行生产数据回滚。

## 隔离验证

- 在本工作树离线恢复依赖，未使用主工作区 `node_modules`；Prisma Client 仅由本工作树 schema 生成。
- `pnpm --filter @guoxue/shared build`：通过。
- `pnpm --filter @guoxue/server exec prisma generate --schema prisma/schema.prisma`：通过。
- 通知、支付、退款指定 Jest：3 个套件通过，121 个测试通过。日志中的资金对账错误为既有异常分支测试输出，不代表真实渠道调用。
- 候选说明中的通知幂等、落库失败释放后重试、支付/退款状态回归已纳入通过结果；跨进程 Redis 并发、真实 HTTP、事务失败端到端不发送成功通知、跨用户详情访问和点击目标二次授权，仍未取得本轮独立证据。

## 业务状态

- 支付成功：代码候选完成，最终支付成功事件后使用 `ORDER_PAID:<orderId>`；线上状态未知。
- 退款完成/失败：代码候选完成，退款 CAS 与权益冲正后发送完成，失败/关闭/异常不发送成功文案；线上状态未知。
- 权益到账、客服回复、课程、直播、圈子：通知矩阵中只有已有入口或分类的部分，不能视为统一闭环已全面实现；缺少统一事实源的仍列为未接入或待核验。
- 通知详情和目标页面需继续依赖资源自身权限；统一目标授权和跨用户攻击测试尚未补齐。

## 发布边界

状态准确标记为：**代码候选完成、隔离验证完成、线上状态未知、待发布决策**。

本轮未部署、未迁移生产数据库、未发送真实微信/短信/App 通知、未修改订单/退款/权益/余额。上线前仍需补齐事务失败不发成功通知的集成证据、跨用户详情和点击权限链路、Redis 跨进程并发，以及业务方对通知矩阵和偏好策略的决策。

## 2026-09-21 主线重新核对

- 重新读取真实主线 `release/integration-20260801`，当前 HEAD 仍为 `2e458597a80076ba2728e9ab2a3156bf78a3d0e3`；未沿用历史假设。
- 主线工作区存在其他窗口的 856 项未提交改动，因此未在主线工作区直接合并、暂存、stash 或清理。
- 候选最终版本 `1d26ab18f10d6a166fc7b66fe2950256c5d513ce` 不是当前主线祖先，差异仍为通知候选的 8 个文件；此前隔离接收分支已从该真实 HEAD 按顺序接收，最终隔离 HEAD 保持不变。
- 本次核对结论：**接受隔离接收结果，不直接改写脏主线工作区**。待主线维护窗口可安全合并时，将隔离分支的三个候选提交按记录顺序接入。
- 支付/退款通知失败不阻断主事务，幂等键与撤回顺序保持。Redis 跨进程并发、事务失败端到端、跨用户详情访问、客服/课程/直播/圈子完整触发链路仍未覆盖；微信、短信、App 推送及线上两节点状态未知。
- 状态保持：代码候选完成、隔离验证完成、线上状态未知、待发布决策。
