# 支付安全整合验收

基线360ac6d3，集成255723b7（合入苹果6dea48ab/218e2a0a/bbe5d633/96cd23c6和汇付微信ab822bef）。未发布、未实付、未更改商户权限或数据库结构。

## 本轮完成

1. 汇付调用前持久化订单付款意图，同订单唯一约束和事务条件更新；响应丢失、保存失败、重复点击不另开付款单。
2. 微信四入口与汇付共用订单数据库状态，阻止跨渠道并发覆盖。未知初始化保留原流水，严格查询确认未付款后才进入明确关单恢复。
3. 微信资金查询严格验证原始响应签名、时间、公钥/证书标识、商户、单号、币种和金额。未知状态不当作明确未付款。
4. Apple首次验票校验签名账号标记；退款状态条件更新防重复扣币；正式服务不接受沙盒免费交易；客户端恢复交易先验票后关闭。
5. iOS通用购买面板未接好IAP的数字业务在创建现金订单前提示暂未开放，H5/Android不启用该限制。

## 验证

- 集成服务端21套475项测试全部通过，日志payment-integrated-tests.log；完整服务端noEmit类型核验通过。
- 苹果候选16项服务测试、4项客户端执行测试通过；连同既有微信和旧排盘桥客户端检查23项通过。数字不要相加为不重复交易数。
- 苹果候选完整前端类型检查及H5构建通过；集成未混入未验收Android245支付资源。
- 国学2隔离PostgreSQL演练：12个并发付款占用请求仅1个成功；12个退款条件更新请求仅扣一次。无网络、无端口、512MB、仅临时模拟表，容器已清理。TAT inv-e8kjqf053q / invt-e8kjqf053r SUCCESS exit0。真实数据库语义验证不是完整Prisma业务端到端测试。

## 不能省略的后续

- 新旧初始化规则不能混跑接收新付款：发布需暂停新初始化、排空旧请求、两节点统一后恢复；不得普通滚动发布假定互斥成立。
- 候选构建、配置/启动方式核对、发布后端到端小额交易与退款仍未完成。
- 汇付未知且无凭据、确定拒绝的原单仍保留，需要对账核实；不能自动清空流水另付。
- Apple目标数据库没有交易通知记录，当前独立配置探测未就绪；需App Store商品/密钥/通知设置、隔离沙盒和苹果真机确认账号标记传递。
- 苹果通知乱序和全量数字商品路由、汇付账单对账、部分渠道商户权限仍为后续验收项；本轮不宣称全部完成。
- 当前已验证的线上通道未关闭，未发起用户扣款。
