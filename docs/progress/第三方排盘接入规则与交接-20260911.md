# 第三方排盘接入规则与交接

更新：2026-09-11。本文是其他任务接续第三方排盘工作的统一入口。依据为用户确认的业务规则、用户最新转发的第三方回复、源码与独立测试。**协议说明、本地实现与线上验收须分别判断；本文不表示完整接入已完成。**

## 接续者先读

- 本节与“第三方最新接口契约”取代历史记录中相冲突的推测，不重复向用户询问已确认参数。
- 公开可查的浏览器行为、跳转、公开脚本与现有实现由我方自行调查；只对第三方独有协议或必须由其修改的页面提出协作。
- 用户要求稳定性优先。未验收的内嵌和分享改造不得直接发布；不能靠关闭浏览器安全机制、代理旧站登录支付或反复全量试发解决。
- 第三方没有交付整套源码。先检查既有能力，不把“提供源码”当默认前提。
- 正式目标为 AGENTS.md 指定的北京两节点，旧开发服务器不作为替代上线目标。

## 第三方最新接口契约

来源：用户在当前任务最新转发的第三方原文。以下保留准确路径、参数和响应，示例均为占位符，不能直接执行；不包含真实手机号或可登录的签名。

### App 用户签名入口

第三方明确称其为 **App 用户入口**，不能直接假定等同于微信 H5 内嵌入口。

```text
排盘工具：https://www.yrydai.cn/guoxueApp.php?mobile={手机号}&key={签名}&go=tool
个人中心：https://www.yrydai.cn/guoxueApp.php?mobile={手机号}&key={签名}&go=my
签名计算：key = md5(手机号 + "@rebuguoxue" + 手机号)
工具显示版本：https://www.yrydai.cn/guoxueApp.php?mobile={手机号}&key={签名}&go=tool&v=1
```

- 第三方最早也给过 www.yrydai.com/guoxueApp.php 示例，后续工具/个人中心的明确说明使用 .cn。保留此来源差异，不擅自全局替换域名。
- 当前服务端使用 UTF-8 字符串计算 MD5，输出小写十六进制；真实签名仅用于约定入口，不写日志、分享链接或公开文档。
- v=1 是第三方已有显示版本参数，仅明确用于工具入口。个人中心不擅自追加。
- 第三方原文还提及由其控制审核期及审核后显示内容。此为对方背景说明，不作为我方隐藏功能、规避审核或修改功能开关的执行要求；接入和验收以真实功能为准。
- 未明确 App 签名入口接受 ruid、userid、recommendId、fromuid 等推荐参数，不能自行拼接。

### 用户查询与推荐人开通

| 用途 | 地址 | 已说明行为 |
| --- | --- | --- |
| 查手机号对应旧用户 | `https://www.yrydai.cn/recommend/mobileUser.php?mobile={手机号}` | 成功返回数字 userid；普通用户也会返回 userid |
| 已存在用户开通合伙人 | `https://www.yrydai.cn/recommend/partner.php?mobile={手机号}` | 手机号存在则开通；不存在则失败，不自动开户 |
| 微信授权开户及开通入口 | `https://www.yrydai.cn/my.php?mod=member&act=addPartner&mobile={手机号}` | 只能在微信或小程序 webview 打开；自动微信授权，不存在用户时自动创建 |

第三方提供的响应：

```json
{"status":"1","message":"操作成功","userid":"用户id"}
```

上行为查询成功。partner.php 的成功与失败分别为：

```json
{"status":"1","message":"操作成功"}
{"status":"0","message":"操作失败，未查到相关用户"}
```

**数字 userid 是用户标识，不是推荐人已开通证明。** partner.php 不存在用户时返回失败与 addPartner 授权页可自动开户并不矛盾，不能据“支持自动开户”删除待授权状态。

### H5 推荐入口

第三方明确建议推荐用户使用：

```text
https://www.yrydai.com/p1.php?ruid={站长旧系统userid}
```

- 参数名确定为 ruid，不再询问是否 userid/recommendId/fromuid。
- mobileUser.php 返回的数字 userid 可以作为此推荐入口的 ID，但站长开通状态仍需按上述流程确认。
- 使用的是推荐人的 userid，不是当前访问者的 userid。
- 未提供“App 签名登录同时携带推荐人”的组合入口，不自行创造。
- 无参数 p1.php 能触发网页授权，不等于已证明可完成无手机号开户、内嵌授权或全部工具操作。

## 用户确认的业务规则

### 普通用户与登录回跳

- 普通排盘只要求热卜已登录，不能因未绑定手机号阻止使用。
- 微信内优先微信快捷登录，保留账号登录选择；不代替用户确认平台要求的授权。
- 登录完成回原受阻页面，保留参数、分站来源及必要页面位置；所有功能适用，不一律回首页。
- 不自动重放付款、创建订单等操作。取消登录不循环弹出授权。
- 有手机号按旧系统支持的签名接入与账号核对规则处理；无手机号允许走旧系统自己的授权流程，不伪造手机号。
- 新系统微信登录不等于旧系统已有登录会话。追求一次连续体验，但不承诺两个公众号首次授权永远无需再次确认。
- 后续绑定手机号可查询并保存对应关系，不自动合并旧账号或迁移权益；冲突待核对。无手机号期间不要求权益互通。

### 分站与业务归属

- 开通分站、运营商要求绑定手机号，用来核对或开通旧站推荐人。
- 已有旧用户：开通推荐人后复查 userid，保存映射和推荐链接。
- 不存在旧用户：进入微信授权开户流程，完成后复查并保存映射；授权回跳与幂等仍需实测。
- 用户从哪个分站点击排盘，使用该站长的 ruid 入口；登录、刷新、重试不能丢失来源。
- 是否形成旧推荐关系由旧系统判断，不覆盖已有归属。新旧业务、推荐关系和佣金独立。
- 旧站支付、订单、佣金、客服保持旧系统处理，热卜不接管。

### 页面壳与分享

- 目标是 H5 和 App 有清楚的热卜返回入口，不让接收分享的人陷在旧站无法返回。
- 分享须对应准确的公开盘面，不能泄露签名、手机号、会话或订单凭据。
- 切换盘面后再次分享，不能继续分享第一次的地址。
- App 原生 WebView 能力与微信 H5 跨域 iframe 能力不同，不能将 App 桥直接视为 H5 可用。
- 有外层首页按钮不代表能控制旧页内部首页、支付、分享或微信系统返回。

## 已验证的事实与边界

1. 2026-09-11 无 Cookie 公开请求：www.yrydai.com/p1.php 返回 302 至 open.weixin.qq.com/connect/oauth2/authorize；首跳未见 X-Frame-Options/CSP。不能据此判断最终授权页或登录后内容允许内嵌。
2. 同日 www.yrydai.cn/p1.php 无参数返回 200、0 字节。共用数据库不代表入口、会话行为相同；不能互换域名或把 200 当成功。
3. 两节点独立验证页 paipan-embed-probe-20260911.html：安卓微信外框可见，子页持续空白。具体根因未定位，不能断言唯一是拒绝嵌入。
4. signed-entry 探针调用当前线上入口遇到绑定手机号的 400；手机设置确实显示未绑定。未到第三方加载阶段，不能当第三方失败证据。
5. 历史 App r8/r9 已核实原版 WXshare 从 WebView 当前 URL 取地址，并限定将 /app_p1.php 转为 /p1.php，保留允许的盘面参数。App 当前盘面抓取不是待第三方回答的问题，需核对实际发布版本是否包含已有实现。
6. 历史微信“先顶层访问原站再进外框”对照仍为空白。这不是完整授权、会话或内嵌原因的证明，不重复把该方法当已验证解决方案。
7. 构建、测试、HTTP 200 或 iframe load 都不等于实际盘面、授权和支付验收通过。

证据位置：

- `artifacts/qa-v243-freeze-20260906/bridge-protocol-followup.md`
- `artifacts/qa-v243-freeze-20260906/排盘融合方案验证结论-20260907.md`
- `artifacts/qa-v243-freeze-20260906/h5-frame-probe/HTTPS部署与微信验收-20260907.md`
- `docs/progress/第三方排盘内嵌调查结论-20260911.md`（主工作区）
- 浏览器规则：https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Same-origin_policy

## 当前发布及验收状态

- 两节点后端 f004ed1b 已发布并通过健康检查；H5 159cb870 已发布，首页摘要 bc92c2449f129ba24a95866e8a9f1ba64e882aac063c89bf6c2a9e3aefa13f65。
- 本地和服务器候选构建均通过。已实测安卓微信首页直接进入旧排盘、八字输入页可打开；返回遇微信域名提示，继续后可回热卜且不循环。
- 普通用户不因缺手机号被拒绝；H5工具使用网页入口。完整内嵌尚未实现，不能称为全壳内闭环。
- 首次微信登录、真实站长开户及推荐落库、本轮支付尚未完成真机验收。
- 2026-09-11 发现站长在旧站授权后再次进入仍可能反复显示待授权；本地增加再次进入前复核同步，13项相关检查通过，尚未发布。不得把此项视为线上已修复。

## 接下来如何推进

继续自行核对现有入口、公开页面/脚本、实际 App 版本和微信现场。仅在现有 H5 消息接口或授权回跳行为无法从我方观测和已给文档确定时，向第三方询问其私有契约；若确认必须修改它的页面，再提出最小改动要求。

协作边界见同目录《第三方排盘最小协作确认清单-20260911.md》。目前不要求用户转发泛泛的浏览器疑问，也不把“必须第三方重写授权”当已得出的结论。

最新上线收口详见同目录《排盘上线收口与发布交接-20260911.md》：H5 普通工具显式选择 client=h5，保留 App 原协议；完整内嵌和接收端分享融合不阻断整体上线。源码补丁与只读基线脚本位于 artifacts/paipan-launch-handoff-20260911，最终发布必须与当前域名及其他任务修复统一整合。

## 微信H5取消二次确认（本地待验收）
用户要求点击排盘后不再选择“打开工具/返回首页”。实物手机 a07f9db3 已复现现网中间页，手动打开旧站及八字工具成功，返回仍停留确认页。本地改为取得原上下文入口后同页自动进入；正常只显示连接状态，四秒仍未离开才提供重试。浏览历史项记录一次性返回标记，兼容重新加载及页面缓存返回，防止自动外跳循环。此方案仍会显示第三方域名，不等于完整iframe嵌入；不修改App、支付或后端。新版本需部署后真机验收。

## 159cb870 发布后真机结果
两节点发布成功，首页SHA bc92c2449f129ba24a95866e8a9f1ba64e882aac063c89bf6c2a9e3aefa13f65。已连接安卓微信验证：首页点击排盘直接进入旧站，无二次选择；八字按钮打开输入表单。返回遇微信对 api.rebugx.cn/h5 的安全提示，点击继续访问后回热卜首页，未自动再跳旧站。域名现象已同步“修复微信点击支付白屏”任务；不是完整无感返回验收通过。首次登录、分站推荐落库、支付未在本轮验证。证据见 direct-entry-phone-acceptance.json 与同目录截图。
