# 新功能走查报告（生产环境·真人视角·只报告不改码）

- 走查时间：2026-07-04（报告文件名按任务指定）
- 环境：生产 H5 `https://api.rebugx.cn/h5/`（后端 pm2 guoxue-server·真库）
- 手段：Playwright iPhone 模拟 + touch + 短信登录 storageState（测号 13912340099·登录态已按既定流程刷新）
- 工具留存：`C:\Users\Administrator\guoxue-e2e\walk-new.js`（主走查）、`walk-new2.js`（补充验证）、`walk-new-report.json` / `walk-new2-report.json`（原始记录）、`shots/walk-*.png`（截图证据）
- 判定口径：区分「新库无数据的诚实空态」（正常）与「报错/白屏/卡死」（bug）

---

## 一、问题清单

### P0（崩/不可用）

无页面级崩溃、无白屏、无卡死 loading。

### P1

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| P1-1 | **DeepSeek 账户欠费 → AI 润色生产不可用**。创作助手「润色」tab 点「AI 润色全文」→ `POST /api/v1/ai/publish/polish` 返回 **502**；页面透出的底层原因：`DeepSeek API返回 402: {"error":{"message":"Insufficient Balance",...}}` | walk-new-report.json `assist-polish`；shots/walk-assist-polish.png | 所有走 DeepSeek 的 AI 能力（润色、AI 兜底审核、会员 AI 权益等）生产不可用，**需充值 DeepSeek 账户**（非代码问题） |
| P1-2 | **生产库无任何驿站数据**：`StationOffline` 0 行（psql 直查 5433 生产库确认）；`GET /offline/stations/discover` 返回空 | curl discover→`{"stations":[],"total":0}`；psql `SELECT count(*) FROM "StationOffline"` → 0 | station-home 品牌主页**无正样本可验**（本次只能验错误态）；C 端所有驿站入口全空；manage-brand 无驿站主账号可验。需注入驿站种子数据后复验正样本 |
| P1-3 | **服务器 DB 配置漂移隐患**：`/opt/guoxue/.env` 的 `DATABASE_URL` 指向 `127.0.0.1:5432`（旧库·User 仅 11 条·最后建号 2026-06-07·却存有一条「终南山国学书院」驿站），而 pm2 运行时实际连 `127.0.0.1:5433`（真生产库·User 20 条·最新今天）。任何按 .env 起的新进程/脚本（含 deploy 流程、seeds、psql 手工操作）都会**连错库** | ssh rebu-server：`grep DATABASE_URL /opt/guoxue/.env`=5432；`pm2 jlist` env=5433；两库 User 计数/时间戳对比 | 本次差点用旧库驿站 id 当"真实数据"；后续数据注入/迁移若照 .env 执行会写错库。建议主控安排统一（改 .env 指 5433 或下线 5432 实例） |
| P1-4 | **我的页无「会员」入口（非会员不可见）**：任务预期"我的页新接入口…会员"，实测 profile DOM 无任何"会员"字样。源码核实：`pages/profile/index.vue` 仅有 `isVip && vipDaysLeft<=30` 的到期续费提醒条（L336-340）；`/pkg-mine/memberships/index` 的全站路由入口**仅此一处**。非会员用户从我的页无法进入会员开通链路（书院会员 C1 主推年 168 的转化断点；课程/电子书等板块有各自场景入口，但个人中心无常驻入口） | walk-new-report.json `profile-entry-会员`（tap 未找到）；grep 全库 `pkg-mine/memberships` 仅 profile 一处引用 | 会员转化主入口缺失，属功能未接完/待拍板，非崩溃 |

### P2（体验/待查证）

| # | 问题 | 证据 | 说明 |
|---|------|------|------|
| P2-1 | **钱包页+积分中心每次进入抛未捕获 JS 异常** `Cannot set properties of null (setting 'scrollTop')`。直访与从我的页转场均 100% 复现（wallet、points 两页各自复现）；页面内容渲染正常、API 全 200，属"带伤运行" | walk-new-report.json `profile-entry-国学币`/`profile-entry-积分`；walk-new2-report.json `wallet-direct`/`points-direct`/`wallet-via-profile-repro` | 疑 scroll 相关 ref 为 null 时直接赋值（两页共性组件/逻辑）。今日新接入口把流量导到这两页，建议排期修 |
| P2-2 | **润色失败时把底层原始报错 JSON 直接透给 C 端用户**：抽屉内展示 `DeepSeek API返回 402: {"error":{...}}` 全文 | shots/walk-assist-polish.png | 即使欠费解决，错误文案应友好化（"AI 服务暂忙，请稍后再试"类），不应暴露供应商与原始 JSON |
| P2-3 | **创作助手「引用」「案例」真调后端但合理输入返回空结果**：引用 tab（正文含《论语·学而》原句）→ `201 POST /ai/creation/classic-quotes` 空结果；案例 tab → `201 POST /ai/creation/similar-cases` 空结果。空态文案诚实（"未找到贴切的古籍引用…"/"未找到相似案例…"），不算页面 bug，但**命中率存疑**：疑与已知 embedding 命门（deepseek-embedding 404→字符哈希向量·语义检索无效）或 DeepSeek 欠费连带有关 | walk-new-report.json `assist-quotes`/`assist-cases` | 待欠费解决后复测；若仍空，需查 creation 检索实现 |
| P2-4 | **站长工作台新卡（CRM 待跟进+团队任务）本次无法验证**：测号在生产库非站长，`GET /station/my` 业务 404「你还没有开通分站」→ 页面落"您还未开通分站"开通引导（诚实、非报错）。新卡代码路径未被触达 | walk-new-report.json `panel-cards`（4 个 /station/* 业务 404·页面无崩） | 账号/数据缺口。需生产库开通一个站长测号（或注分站数据）后复验两张新卡 |
| P2-5 | **CRM 客户详情页（crm/detail）未验证**：生产无客户档案数据、列表空，拿不到 client id 深链 | — | 可在 crm/edit 建一条测试客户后复验（本次只走查不写数据） |

---

## 二、正常项一览（走查通过·含诚实空态）

| 路由/操作 | 结果 |
|---|---|
| `/pkg-mine/crm/index` 客户经营 | ✅ 4 个 API 全 200（suggested-clients/reminders/clients）；体验档配额文案「客户档案 0/20，成为站长/商家/驿站/研究院成员即无限量」；分层 tab（全部/高价值/活跃/流失预警/沉睡）渲染；空态诚实「还没有客户档案，点右下角添加」 |
| `/pkg-mine/crm/insights` 经营洞察 | ✅ `GET /crm/insights` 200；无数据时诚实引导「先从可入库的成交客户一键归档…」+「去建客户档案」按钮 |
| `/pkg-mine/crm/edit` 添加客户 | ✅ 表单完整渲染（称呼*/电话(加密提示)/性别/生辰+时辰/标签/备注）；隐私合规文案在（"生辰仅用于…加密存储、绝不外泄；请征得客户同意"） |
| `/pkg-institute/lectures/index` 大师讲座 | ✅ `GET /institute/lectures` 200；诚实空态「暂无讲座上架，敬请期待」（生产无 INSTITUTE_LECTURE 课程·数据缺口可注种子） |
| `/pkg-competition/talents/index` 人才榜 | ✅ 3 个 API 全 200（talents/talents/me）；「我的战绩」卡+积分规则文案+空榜诚实「虚位以待，参赛即可上榜」+「去看看赛事」引导 |
| `/pkg-offline/station-home/index?id=<不存在>` | ✅ 错误态诚实：「驿站不存在」+重试按钮（API 404 属预期·非白屏——sweep 的 WHITE 标记是字数阈值误报） |
| `/pkg-offline/station-home/index`（缺参） | ✅ 「缺少驿站参数」+重试，未卡 loading |
| `/pkg-offline/manage-brand/index`（非驿站主） | ✅ `GET /offline/stations/my` 200(null)；诚实提示「您还不是驿站运营者」 |
| `/pkg-mine/settings/index` 设置 | ✅ 全量渲染（安全分 82/修改密码/修改手机号/支付密码/注销/通知/隐私/通用/关于） |
| `/pkg-mine/change-password/index` | ✅ 页面正常；**「没有密码或忘记了？用验证码设置」链接可点**→ 跳 `/pkg-auth/forgot-password/index` 且找回密码页正常渲染（今日修复项复验通过） |
| `/pkg-order/list/index?tab=pending_pay` 深链 | ✅ 「待付款」tab 正确激活（DOM active 元素文本=["待付款"]）；`status=PENDING` 查询 200 |
| 我的页入口：订单四状态 | ✅ 待付款→`tab=pending_pay`(PENDING)、待发货→`tab=pending_ship`(PAID·有真实订单 00AD53F4688B)、待收货→`tab=pending_receive`(SHIPPED)、售后入口在（查看全部订单亦在） |
| 我的页入口：国学币/优惠券/积分/关注 | ✅ 均真跳真连：钱包(余额 2730/充值档位/交易流水)、优惠券(空态+去领券)、积分中心(1500 分/任务/兑换)、关注与粉丝(关注 1)。※wallet/points 有 P2-1 的 JS 异常但内容正常 |
| 我的页入口：设置（右上角齿轮 icon） | ✅ 可点→跳 `/pkg-mine/settings/index`（icon-only 无文字，首轮按文字找不到属工具方法限制，已用坐标复验通过） |
| 发现页「精选专栏」四板卡 | ✅ 四卡齐全且计数真实（精选好课 36/热门好物 128/古籍新上 24/推荐圈子 52）；逐卡点击全部真跳：→`/pkg-course/courses-list/index`（真课程列表）、→`/pkg-shop/categories/index`（分类树 200）、→`/pkg-classics/home/index`（古籍馆 48 部）、→`/pages/circles/index`（圈子 tab 全量加载） |
| 圈子发帖页「创作助手」抽屉 | ✅ 入口 chip 在；抽屉打开四 tab 齐（引用/命盘/案例/润色）；**引用检索真调后端**（`POST /ai/creation/classic-quotes` 201）；案例真调（`similar-cases` 201）；**命盘 tab 手动输入四柱表单+「我的排盘记录」入口齐**，脱敏承诺文案在（"仅含四柱干支…不含姓名与生辰"）。※结果命中率见 P2-3，润色见 P1-1 |
| `/pkg-operator/station-master-panel/index` | ✅ 页面本身无崩：非站长落「您还未开通分站」开通引导（新卡验证受账号限制→P2-4） |
| `/pkg-mine/memberships/index`（直访补验） | ✅ 「我的权益」页正常渲染（权益概览/订单记录/空态+「开通会员」按钮）——页面本身健康，缺的是我的页入口（P1-4） |

---

## 三、复验建议（供主控排期·本次未动任何代码/数据）

1. **先充 DeepSeek**（P1-1），随后复测：润色、引用/案例命中率（P2-3）、AI 审核兜底。
2. **统一服务器 DATABASE_URL**（P1-3），再做驿站种子注入（P1-2），之后复验 station-home 正样本 + manage-brand 驿站主视角。
3. 生产库给测号（或新测号）开通分站站长身份，复验站长工作台 CRM 待跟进+团队任务两张新卡（P2-4）。
4. 修 wallet/points 的 scrollTop 空引用（P2-1）与润色错误文案友好化（P2-2）。
5. 拍板/补接我的页会员入口（P1-4）。
6. CRM 建一条测试客户后走查 crm/detail（P2-5）。
