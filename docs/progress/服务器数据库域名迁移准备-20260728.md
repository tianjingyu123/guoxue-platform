# 服务器、数据库、域名迁移准备进度（2026-07-28）

## 已完成（不依赖新资源）

- 公网 API、H5、静态资源与 Cookie 域名环境变量化。
- 服务端品牌配置增加环境覆盖，防止 Redis/数据库旧域名缓存污染分享链接。
- 讲师名片、报告交付、赛事邀请、协议内容、古籍共读等关键分享深链统一生成。
- 支付、退款、汇付、物流回调提示去除固定域名。
- Docker 生产覆盖文件补齐公网入口与关键回调变量透传。
- 增加旧域名审计、环境检查、数据库导出/恢复/表计数校验、API/H5/CORS 冒烟脚本。
- 增加完整迁移、双域并行、回滚和旧资源下线手册。

## 本地验证结果

- 旧域名审计通过：运行时硬编码 0 处；兼容兜底、演示资源和历史测试数据已分类列出，不作为切换阻塞项。
- 新生产环境变量模板检查通过，共读取 46 个键，未输出任何敏感值。
- PostgreSQL 导出、恢复、表计数核验与冒烟脚本均通过 Bash 语法检查。
- 服务端定向测试通过：2 个测试套件、36 个测试全部通过。
- 服务端、管理端、H5、微信小程序、App 构建全部通过。
- 移动端 `vue-tsc --noEmit` 类型检查通过。
- 微信小程序主包 1.88 MB，低于 2 MB 上限；最大分包 1.90 MB。
- `git diff --check` 通过。

说明：演示图片、历史种子和当前生产兼容默认值仍可能引用旧域名。新域名切流前必须完成对象存储/CDN 同步或保持旧域名资源回源，确认无访问后再按手册下线旧域名。

## 等待新资源后执行

- 填写新服务器、新数据库、Redis、域名、DNS/CDN 和对象存储信息。
- 在新数据库启用扩展，执行最终备份、空库恢复和精确表计数核对。
- 配置 Nginx、SSL、WAF、DNS、COS/CDN CORS。
- 更新微信、支付、物流、IM、直播、VOD 等第三方控制台域名与回调。
- 使用 hosts/预览域名完成 H5、小程序、App、手机、平板、PC 真机验收。
- 安排短暂停写窗口、最终切流、72 小时双环境观察和对账。

## 明日需要用户提供

- 新服务器云厂商、地域、系统、IP、SSH 用户（密码/私钥不要发到项目文件）。
- 新数据库连接方式、PostgreSQL 版本、TLS 要求、是否为全新空库。
- 新 Redis、对象存储/CDN 是否沿用。
- 新 API/H5/管理端/静态资源域名及 DNS 服务商。
- 是否接受切换时 10–30 分钟只读维护窗口。
- 是否要求保留现有登录态；若要求，JWT/Redis 迁移策略需按手册执行。
