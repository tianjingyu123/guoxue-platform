# 小卜主线窗口交接（2026-09-23）

## 工作总结与证据边界

- 交付分支：`codex/xiaobu-hardware-handoff-20260922`，编写本交接前最新提交为 `3034fe238`，隔离工作树 `C:/Users/Administrator/.codex/worktrees/11f4/gx-deploy-91`。主工作树 `D:/gx-deploy-91` 当时仍在 `release/integration-20260801`、`2e458597a`，`git status --porcelain=v1` 约 876 条（展开未跟踪文件约 1.43 万条）；它还有其他任务的并行未提交工作。接收前必须重新核对当时最新状态，这些数字只是 09-23 快照。
- 主线提交 `2e458597a` 是交付分支的祖先。到硬件基线 `4ca9ade61` 之间有 32 个前置提交、约 501 个路径；交付分支相对主线提交约 516 个路径，与当时主树未提交内容约 411 个路径重叠。不能把整个交付分支直接快进或覆盖主树，也不能只取硬件基线之后的提交而漏掉前置代码。截至 `a22ac62a9` 的基线后接续涉及 27 个路径，当时与主树直接重叠的是两个未跟踪旧版文件：`apps/admin/src/api/xiaobu-ops.ts`、`apps/server/src/modules/voice/voice.module.ts`；后续又增加了文档，接收时应重新计算最新冲突。
- 已完成：本地实体设备 OTA、激活/绑定、WebSocket、Mock 双向语音与 30 秒提醒；候选版真机两次约 15 轮会话，用户确认可回放、通话结束后不重启可再次唤醒。曾有久置后需重启及主观回复变慢的反馈；短时间待命复测未复现故障，不能归因于固件或服务。断线签发竞态已修复并在本地 3989 Mock 服务装载；其余硬件播放调校由设备方在可复现故障时配合。
- 已完成：专用 PostgreSQL 库与模拟设备验证零额度充值提示、90 秒预留、25 秒最终模拟用量、重复回调不重复扣，另一合成账号余额为 0；没有改动正常用户余额或真实支付。真机屏显/播报充值提示仍未在隔离账号下验收。
- 已验证：服务端 `tsc --noEmit`、后台 `vue-tsc --noEmit` 通过；隔离库及独立 Redis DB 15 的语音全量回归 23 套、193 项通过，Jest 自然退出。跨实例重连、告警失败补发已有真实 Redis + 隔离库/本机 HTTP 接收器的集成证据，但这不等于云端多节点或真实企微群送达。
- 当前局域网 Mock 服务在 `192.168.110.155:3989`（本交接时进程 29476）存活检查 HTTP 200；使用隔离库与稳定设备身份密钥。运行中的测试服务、旧编译包、设备 OTA 地址和测试数据库不要随主线文件合并而顺手替换或清理；如需切换，在设备空闲时先做并行端口健康检查并保留回退来源。
- 本地运行概况缺 Redis、正式 `wss` 设备地址、固件 `https` 根地址和运维告警 Webhook；供应商仍为 Mock。这只代表局域网联调服务，不代表云端预发布。商业小智 API/按年授权尚未开通，真实 ASR/模型/TTS、停费和费用不能宣称已验收。

## 主线接收顺序

1. **先完成主线窗口现有工作**，固定当时主工作树状态，确认单一写入者；重新读取 `AGENTS.md` 与本交接，核对主线 HEAD、交付分支 HEAD、未提交内容和路径重叠。不要对脏主树直接执行快进、重置或覆盖复制。
2. 识别主线现有代码已包含哪些前置功能，对照 `2e458597a..4ca9ade61` 的 32 个提交补齐依赖，再审阅 `4ca9ade61..交付分支 HEAD` 的硬件接续提交。两个旧版未跟踪文件按实际内容逐项合并，保留主线后续工作，不直接以候选文件覆盖。
3. 在目标环境运行服务端和后台类型检查、语音全量回归。数据库必须是已迁移的专用隔离库，Redis 使用专用空 DB；只给需要共享 Redis 的集成套件设置 `XIAOBU_IT_REDIS_URL`，不要把全局 `REDIS_URL` 指向回归 Redis。根据目标分支的新代码重新确定测试数，不机械要求恰好 193 项。
4. 先独立预发布，再考虑正式环境：实测 Redis 连接和订阅、两个实例的设备重连、告警 Webhook 接收回执、设备 OTA 返回的 `wss://`/固件 `https://` 地址及证书、失败回退。记录时间、设备尾号、目标实例、服务日志与接收端回执。供应商仍可设“暂未开放”；Mock 不能作为真实语音验收。
5. 仅在隔离账号和可回滚额度准备好、且不影响当前设备绑定/正常用户时，用实体设备补验额度不足、充值提示的屏显、播报和待命。不要为此修改真实支付或正常账号余额。
6. 商业 API 年约由决策人最后开通。正式文档与测试权限到位后，按 `docs/design/小智商业API适配边界与字段映射-20260921.md` 第 6 节核对设备音频中继、停费确认、用量与账单，再实现真实适配器；未知能力保持 `unknown`。

## 必读文件

- `docs/progress/小卜硬件无商业API验收清单-20260922.md`：当前完成项、剩余门槛、主线冲突。
- `docs/progress/小卜硬件窗口接续与验证-20260922.md`：真机/测试/切换逐次证据与回退来源。
- `docs/design/小智协议终端接入-20260921.md`：OTA、激活、设备身份与 WebSocket 设计。
- `docs/design/小智商业API适配边界与字段映射-20260921.md`：未签约能力边界及供应商资料清单。
- `D:/gx-deploy-91/docs/progress/外部系统高效控制与腾讯云通道-20260913.md`：官方 TAT 通道、云端单一写入任务和操作限制。云端写入者或通道状态可能变化，执行前重新核对。

## 可转发提示词

> 等你完成主线窗口现有工作后，请接收小卜硬件与语音的签约前剩余工作。交付分支是 `codex/xiaobu-hardware-handoff-20260922`，隔离工作树在 `C:/Users/Administrator/.codex/worktrees/11f4/gx-deploy-91`。先读 `docs/progress/小卜主线窗口交接提示词-20260923.md`、`小卜硬件无商业API验收清单-20260922.md` 和 `小卜硬件窗口接续与验证-20260922.md`，并重新核对当前 Git/云端状态。主线先前有大量未提交改动，交付分支包含主线尚未接收的前置功能；请在固定主线工作后逐项评审和合并，尤其处理 `apps/admin/src/api/xiaobu-ops.ts` 与 `apps/server/src/modules/voice/voice.module.ts` 的未跟踪旧版冲突，不直接覆盖主树。随后用专用库和 Redis DB 跑服务端/后台类型检查及语音回归，在独立预发布验收 Redis 多实例重连、告警真实送达、OTA `wss`/固件 `https`、证书和回退；安全条件具备时再用隔离账号补真机余额/充值提示。保留当前 3989 Mock 真机服务及其回退来源，不动正常用户余额、真实支付和主线其他任务成果。商业小智 API 年约现在不要开通；真实适配器、停费和费用在取得正式文档与授权后再做。请把每项实测证据和未完成项写回交接文档，不把 Mock、配置存在或本机集成测试说成生产验收。
