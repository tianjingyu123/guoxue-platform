# 圈主助理知识依据与隐私收口

基线 `16c149d677ecf927ee4eb2761ed6aae0d42d9e7c`。本批不改 RAG 检索、回答、权限或收费。

现有检索以已入库的本圈知识为优先，通用知识补充；接口没有证明会逐次读取圈规或课程。对话页能力提示已改成实际检索范围，避免用户以为课程和圈规一定被引用。

服务端原先把用户问题前 100 字写入应用日志。现仅记录圈子标识和调用类型，不记录问题原文；没有改变问答请求和历史上下文的处理。

非流式接口返回 `sources`；H5 流式接口原先只发送文本块与完成/错误事件。后续隔离提交已让流式接口从**同一次检索**发送本圈和通用知识的命中数量，非流式端从已有 `sources` 计算相同口径。对话页明确写“检索到”，并提示命中不等于回答已逐条引用；检索失败或无命中时如实显示零命中。两端都不把回答标成“已核实出处”，也不向客户端额外发送知识原文。

如需可点击的具体出处，应先给知识记录建立可读标题、来源范围与安全的访问路径，再验证成员权限、跨圈隔离及模型引用与真实记录的一致性。现有摘要只是透明度提升，不替代这些工作。

隔离验证：RAG、圈主助理服务与控制器定向测试共 26/26；H5 本地交互共 9 组，新增流式命中、零命中和失败时不展示摘要；服务端构建与移动端类型检查通过。小程序构建及产物检查通过：主包 1.63 MB，保守上传口径 1.91 MB；33 个分包中最大 `pkg-paipan2` 1.94 MB，`pkg-circle` 0.96 MB。测试全部使用本地模拟数据，没有向真实用户提问或产生业务写入。

状态：本地代码候选；线上状态未知。真实模型回答和来源准确性、日志采集链及三端实际显示仍待受控环境验收。
