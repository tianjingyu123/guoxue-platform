# 圈主助理知识依据与隐私收口

基线 `16c149d677ecf927ee4eb2761ed6aae0d42d9e7c`。本批不改 RAG 检索、回答、权限或收费。

现有检索以已入库的本圈知识为优先，通用知识补充；接口没有证明会逐次读取圈规或课程。对话页能力提示已改成实际检索范围，避免用户以为课程和圈规一定被引用。

服务端原先把用户问题前 100 字写入应用日志。现仅记录圈子标识和调用类型，不记录问题原文；没有改变问答请求和历史上下文的处理。

非流式接口返回 `sources`，但当前客户端没有展示；H5 流式接口仅发送文本块与完成/错误事件，不发送来源。故本轮不能把回答标成“已核实出处”。后续如需可点击来源，应先给知识记录建立可读标题、范围与安全的访问路径，再由两种接口返回一致的来源元信息，并验证成员权限及跨圈隔离。

状态：本地代码候选；线上状态未知。真实模型回答和来源准确性、日志采集链及三端实际显示仍待受控环境验收。
