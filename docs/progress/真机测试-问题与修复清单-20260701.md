# 真机测试 · 问题与修复清单（2026-07-01 起）

> 配套 [真机测试走查-新窗口自主执行手册-20260701.md]。每发现一个问题记一行，修完更新状态。
> **本轮走查方法**：自建 Playwright harness（iPhone 13 设备模拟 + touch + 真实 UI 短信登录拿 storageState），
> 广度扫描全部 **419 条 H5 路由 + 206 条 admin 路由**，捕获 pageerror/console error/失败请求(4xx/5xx)/白屏，
> 再对标记页深挖复现。工作区在 `C:\Users\Administrator\guoxue-e2e\`（login.js/sweep.js/admin-sweep.js/routes.json）。
> **短信登录法**（生产短信签名失败收不到码）：`ssh rebu-server "redis-cli set 'sms:code:LOGIN:<phone>' '123456' EX 600"` 再调 `/auth/login/sms`。

## 记录格式
`| # | 模块/页面 | 走查线 | 现象 | 根因 | 修法 | 严重度 | 状态 | commit |`

严重度：P0(崩溃/资金错/数据错) / P1(核心功能不可用) / P2(体验/视觉/文案) / P3(优化建议)
状态：待修 / 修复中 / 已修待复验 / 已复验通过

## 移动端前端

| # | 模块/页面 | 走查线 | 现象 | 根因 | 修法 | 严重度 | 状态 | commit |
|---|---|---|---|---|---|---|---|---|
| 1 | 电子书 `/ebook/purchases`·`/ebook/bookmarks`·`/ebook/notes` | 页面状态 | 三端点 **500**（书架/书签/笔记页 API 失败） | 全局 `ValidationPipe(transform:true)` 把缺省的 `@Query() page?: number` 转成 **NaN**（NaN!==undefined 绕过 service 解构默认值）→ Prisma `skip:NaN`/`take` 缺失 | service 三方法防御性归一化（`+`转数+`Number.isFinite`+正数兜底），对 undefined/NaN/字符串/负数均安全 | P1 | ✅已复验通过（线上 200） | afce0328 |
| 2 | 商城 `/pkg-shop/product/index` | 页面状态 | 缺 id 参数进入 → **全白屏**（卡在骨架屏） | `fetchProductData` 对无/非法 id 早 return 未复位 loading；onLoad 无 id 分支只设 loading=false 未设 error | onLoad/fetch 无有效 id 时显式 `loading=false`+`error='商品不存在'` | P2 | ✅已复验通过（显示"商品不存在 重试"） | ca7c0f4d |
| 3 | 古籍 `/pkg-classics/collection/index` | 页面状态 | 缺 id 参数进入 → **全白屏** | `collectionId` 默认值 `ref('1')`（mock 残留·truthy）→ 缺参守卫失效 → 拉到 null 后渲染空内容 | onLoad 无有效 id（或 id==='1'）时落错误态 | P2 | ✅已复验通过（显示"书单不存在 重试"） | ca7c0f4d |
| 4 | 个人中心 `/pkg-mine/wallet`·`/pkg-mine/points` | 页面状态 | 控制台 `TypeError: Cannot set properties of null (setting 'scrollTop')` | uni-app H5`<scroll-view v-else>` 在 loading→content 切换时框架内部对已卸载元素设 scrollTop（压缩代码 index-*.js） | 非致命·页面功能完好渲染正常；框架级 quirk，改造 loading/content 结构风险>收益 | P3 | 记录·暂不改 | — |

## 数据缺口（生产库未跑种子·按用户授权直接注入·均幂等脚本）

| # | 模块 | 现象 | 处置 | 状态 |
|---|---|---|---|---|
| D1 | 法律文档 | 用户协议/隐私政策/社区公约页显示"协议内容暂未发布"（`LegalDocument` 表空） | 注入 3 份真实 markdown 文档（agreement/privacy/community·PUBLISHED） | ✅ 线上 API 200 返回内容 |
| D2 | 电子书 | 电子书城全空（`Ebook` 表 0 条） | 服务器 ts-node 跑 `enrich-ebook-demo.ts` → 10 本精品书+5分类+章节 | ✅ store 页渲染"菜根谭·限时免费"等 |
| D3 | 诗词雅集 | 诗词分类/列表空（`Poetry` 表 0 条） | 跑 `enrich-poetry-demo.ts` → 59 首公版经典+8分类+7诗单 | ✅ 首页渲染《将进酒》等 |
| D4 | 赛事 | 赛事中心空（`Competition` 表 0 条） | 跑 `seed-competition-demo.cjs` → 2 赛事（八字大赛可答题+诗词大赛往届top3）·测试号已报名带证书 | ✅ 首页渲染"八字命理大赛·10人报名" |
| D5 | 积分商城 | 积分兑换页"暂无兑换商品"（`PointsProduct` 表空） | 跑 `seed-points-mall.cjs` → 6 兑换商品 | ✅ 已上架6件 |
| D6 | 短视频 | `/videos` 全空（`Video` 表 0 条） | 短视频需真实可播 VOD 资产·伪造行会致播放器报错；保持诚实空态（"暂无视频"）·待接真实 VOD 后注入 | ⏸ 门控（缺媒资） |

## 后台管理端

| # | 模块/页面 | 走查线 | 现象 | 根因 | 严重度 | 状态 |
|---|---|---|---|---|---|---|
| A1 | 全域（206 路由） | 会话/页面状态 | **0 崩溃 · 0 白屏**（3 处 sweep"白屏"经 networkidle 复测均正常渲染后台框架·系扫描时序假阳性） | — | — | ✅ 健康 |
| A2 | operator/institute/merchant-backend 等角色域 | 权限 | 超管访问返 403/404（`/station/operator-dashboard/*`·`/institute/manage/*`·`/merchant-backend/*`） | 这些是**角色上下文视图**（需绑定为运营商/院长/商家 bindId）·超管未绑定属预期·页面正常降级 | P3 | 非 bug·记录 |
| A3 | marketing/social/pricing 若干页 | 页面状态 | 调用 `/users/capabilities`·`/marketing/invite-rewards`·`/marketing/spread-power`·`/pricing/demand`·`/social/achievements`·`/social/notes-audit` 等返 404 | 后端对应端点未实现·admin 页为空壳/待接后端 | P3 | 记录·低优先（不崩·空态） |

## 已知门控项（非 bug·测你能测的·如实记录）
- **IM/TIM**：`/im/user-sig` 返 400（腾讯 IM user-sig 未就绪）→ IM 单聊/群聊/弹幕依赖·前端已集成 SDK·待真 IM 配置。
- **微信支付**：openid 仅微信端可得·H5 降级演示。
- **短信**：生产腾讯云短信签名校验失败（`credentials could not be validated`）→ 收不到验证码·走 redis 写码登录。
- **短视频 VOD / 直播转码**：需真实腾讯云媒资·相关消费页保持空态。

## 走查进度（模块打勾同步手册 §4）
- 移动端：**22 / 22 模块**（Playwright 广度扫描全 419 路由 + 重点页深挖复现）
- 后台管理端：**全域**（206 路由广度扫描）

## 汇总统计
- 发现总数：11（P1:1 P2:2 P3:2 · 数据缺口 D1-D6 · admin A2/A3）
- 已修复上线：**5**（ebook 500·product 白屏·collection 白屏 + 5 项数据注入 D1-D5）
- 已复验通过：全部已修项均线上复验
- 生产 commit：后端 `afce0328`（ebook 修复·已部署）· 前端 `ca7c0f4d`（两页白屏·H5 已部署）
