# 小卜 AI / 小智语音：非商业 API 工作收口交付

日期：2026-09-21。状态：**非商业 API 工作已收口，待主线评审与商业 API 联调。**

分支：`codex/xiaobu-noncommercial-closure-20260921`（工作树 `D:/gx-deploy-91/.worktrees/xiaobu-closure-20260921`，基线 `2e458597a`）。
未部署生产、未执行生产迁移、未购买服务、未真实收费、未接入任何假冒的供应商接口、未改动其他窗口文件。

本文是本轮交付的唯一汇总；换窗口续做仍以 `小卜AI-接续记录-20260917.md` 为入口（其末尾已指向本文）。

---

## 0. 开工核对（先恢复现状）

- 主工作区 `D:/gx-deploy-91`：分支 `release/integration-20260801`，HEAD `2e458597a`，约 800 项未提交改动分属多个窗口。
- **关键发现：此前小卜全部代码从未进入任何分支**，只以未提交改动混在主工作区。旧报告里的“已完成”全都没有可评审、可撤回的载体。
- 处理：按修改时间与内容逐文件、逐 hunk 判定归属，把本专项剥离进独立工作树（接收顺序见 §11）。混合文件只取小卜 hunk：
  `schema.prisma`（其他窗口的商品运费、直播预约、AI 事件、悬赏、版本发布等不带）、admin `router`（其他窗口的鉴权守卫改造、商家角色、QA 页不带）、
  `redis.service`（AI 运营窗口的 Pub/Sub 不带）、`third-party-services`（支付商户项不带）、`ai-gateway.module`（AiOpsBridge 不带）。
- 依赖外部窗口的唯一文件 `tts/speech-text.ts`：与古籍质量窗口已提交的 `b5403aeba` 逐字一致，单独作为“依赖快照”提交。
- 没有 stash、没有清理、没有暂存或提交主工作区任何文件。工作树有自己的 `node_modules`，Prisma 客户端只生成在工作树内。

---

## 1. S01—S10 当前状态（2026-09-21，以本分支代码与实测为准）

状态口径：已实现并验证 / 已实现未验收 / 可继续开发 / 商业 API 阻塞 / 硬件或外部条件阻塞 / 尚未实现。
“验证”= 单测 + 真实库集成（隔离库 `xiaobu_closure_20260921`）+ 本机 H5 实点；**均不是真机、不是真实语音接通**。

| 场景 | 状态 | 本轮落地与证据 | 仍阻塞/未覆盖 |
| --- | --- | --- | --- |
| S01 智能体广场 | 已实现并验证（非商业部分）；实时语音 **商业 API 阻塞** | 文字与语音入口分开（广场「和小卜语音聊」「我的小卜硬件」）；统一语音页 `pkg-agent/agent/xiaobu-voice` 含未开放/待开始/接通/挂断/失败/评价态；会话模型含 provider/agent/user/scene/status/requestId/startedAt/endedAt/usageState；本人历史页；越权 404 | 真实收音播放（需商业端上 SDK）；真机 |
| S02 圈主知识助理 | 已实现并验证（非商业部分）；下发到小智 **商业 API 阻塞** | 申请/审核/驳回/重审/版本快照/回滚/停用（存量）；语音入口校验有效成员+圈主已开通；圈子私有知识不随上下文下发；MCP 身份绑定工具边界（未验证身份不列出、不执行、不认参数自报） | 角色下发到小智、MCP 传用户身份；腾讯内容审核真实调用未验 |
| S03 古籍按需整理 | 已实现并验证；真实模型断句质量 **未验收** | 简体阅读版（OpenCC 1.1.9 数据入库，不改锁文件；词组优先、乾/夥保留待复核、餘→馀；**等长映射**，划线/注释/段落可原样回到原文）；真实数据只读预览 300 章 135 万字等长失败 0；段落/断句/伴读统一公开口径（补漏：此前未发布/版权未审章节可经段落接口读出） | 异体字（如「増」）不归一；AI 断句质量需真实模型抽检 |
| S04 白话译文 | 已实现并验证（桩模型）；真实译文质量 **未验收**（计次口径已拍板，见 §13） | 段落级译文绑定段落 ID + 原文哈希；状态 none/generating/success/failed/expired + 人工复核字段；原文修订后判 expired 不返回旧正文；空上下文记 none 不通配；命中不调模型、**不计 AI 次数**（拍板前不擅自扣） | 命中是否计次待产品拍板；人工复核后台页面未做（字段已备） |
| S05 有声读书 | 已实现并验证（内存存储替身）；COS 真实桶 **外部条件阻塞** | 段落音频原文/白话、同内容同版本零合成、原子锁；倍速不进资产身份；10 分钟 HMAC 临时地址（签发前重新鉴权）；断点续听；**修复超 3000 字静默截断**（原用例把截断固化成规格，已改） | COS 真实桶、多音字/人名/干支读音真实听测、真机播放 |
| S06 古籍语音伴读 | 已实现并验证（非商业部分）；实时语音 **商业 API 阻塞** | 段落/选中句上下文（选中句必须是原文子串，防注入）；阅读页「小卜语音讲讲」入口；未开放时回到文字伴读；听书提问暂停/继续（存量）；危机转介（存量）；**不录制、不保存、不复播**实时音频 | 听书与实时语音的音频焦点需真机+SDK 验 |
| S07 排盘报告对话 | 已实现并验证（非商业部分）；语音 **商业 API 阻塞** | 语音上下文只含报告提纲与要点，**抹去出生日期/时刻/手机号/证件号与命主姓名，不下发标题、四柱与起盘校验**；只能本人报告、小节须存在；文字问答历史/清空/限频/危机转介（存量）；报告页语音按钮接入统一语音页，文案「开放中」改为「尚未开放」 | **报告生成无支付/权益门禁且每份报告送 30 分钟语音**（见 §7 上线阻塞） |
| S08 内容导览 | 已实现并验证 | 卡片目标均为 pages.json 真实页，H5 实点打开；**修复：搜索与导览对古籍只看“已发布”，版权未审的书也被推荐、点开 404**；分享报告令牌仅限单份、可撤回、按报告每日限次 | 分享令牌无有效期（待定）；公开分享页返回客户出生信息（产品确认） |
| S09 圈主硬件 | 已实现并验证（模拟设备契约）；**硬件与商业固件阻塞** | 设备台账、一次性绑定码、解绑、转赠（转赠码只显示一次）、停用/恢复；序列号只存 HMAC 与末四位；按绑定代次隔离历史（新主人看不到旧历史）；未激活显示「待开通」；后台台账页（客服只读） | 样机、商业固件注册、设备映射、真实激活 |
| S10 费用与后台 | 已实现并验证；真实用量回调 **商业 API 阻塞** | 每次供应商调用记一条尝试；技术结果/回答完整/用户满意/用量状态分开；开始/结束/取消/回调/超时幂等；预留-结算-释放；用量未知不填 0、不扣；模拟用量不计成本；后台供应商状态、异常清单、会话明细（脱敏）；发放限运营与超管并审计，财务只读；**不提供导出** | 真实计费、分账（规则未定不实现）、`chargeUsers` 仍默认 false |

---

## 2. 本轮实现文件（相对 `a285ad325` 存量提交之后）

提交（新→旧）：

| 提交 | 内容 |
| --- | --- |
| `10cc2de0b` | H5 实点发现的三处修复：搜索/导览古籍公开口径、挂断即可评价、移动端分页读取 |
| `e048a0803` | 移动端统一语音入口/记录/我的硬件、阅读页简体/原貌与语音讲讲、后台供应商/会话/异常/设备台账 |
| `a0d0e7f87` | MCP 身份绑定工具统一边界 |
| `aeca1dca3` | 有声读书段落音频、临时地址、断点；去除静默截断 |
| `d7fb451ee` | 古籍简体阅读版、段落级译文、公开口径补漏 |
| `adef3577f` | 语音供应商适配边界、会话编排、设备台账、迁移 |
| `a285ad325` | 小卜存量（09-14~09-21，此前仅在主工作区） |
| `59796ab21` | 排盘算法核验线存量（服务端与共享包，不含移动端引擎） |
| `ccc850af1` | 依赖快照 speech-text（= b5403aeba） |

主要新文件：
- 服务端 `modules/voice/`：`provider/*`、`voice-context.builder.ts`、`voice-session.service.ts|controller.ts|sweeper.task.ts`、`voice-device.service.ts|controller.ts`
- 服务端 `modules/classic/`：`simplified/`（转换器、OpenCC 数据与许可证）、`classic-simplified.service.ts`
- 服务端 `modules/tts/`：`audiobook.service.ts|controller.ts`、`audio-response.ts`
- 迁移 `prisma/migrations/manual_add_voice_provider_boundary/{migration,rollback}.sql`
- 移动端 `lib/xiaobu-voice-data.ts`、`pkg-agent/agent/xiaobu-voice*.vue`、`xiaobu-devices.vue`
- 后台 `views/ai/XiaobuDevices.vue`，`XiaobuOps.vue` 扩展
- 文档 `docs/design/小智商业API适配边界与字段映射-20260921.md`、本文
- 脚本 `apps/server/scripts/preview-simplified.ts`（只读预览）

---

## 3. 商业 API 阻塞清单（逐条等商业文档/账号/测试权限）

1. 会话签发与端上临时凭据（`sessionIssue`）
2. 服务端结束会话并确认停费（`sessionEnd`）——未确认前不能开收费
3. 真实用量回调与取整口径（静音/待机/MCP 等待/断线是否计费）
4. 回调签名与密钥轮换
5. MCP 传递已验证用户身份——决定圈子私有知识能否开放
6. 按智能体下发角色/提示词/音色与生效时间
7. 商业固件设备映射、激活、解绑后记忆清除
8. 长期记忆加载/删除/重置
9. 回答完整性信号
10. 实时音频是否录制/保存（热卜侧默认不录不存）
11. Lite 与标准版对比验收（交接书要求的 30 题）、热词定义与计费范围

## 4. 外部依赖清单

| 依赖 | 状态 | 需要谁 |
| --- | --- | --- |
| 小智商业 API 文档、测试账号、回调规范 | 未提供 | 用户与供应商 |
| 小智控制台 MCP 接入点地址 `XIAOZHI_MCP_ENDPOINT` | 未配置 | 用户在控制台复制后配置（属密钥） |
| 样机、商业固件注册、烧录/恢复方法 | 未到位 | 用户与厂商 |
| 腾讯云 COS 上传权限证据 | 未验证（TAT 权限不代表 COS） | 运维 |
| 真实模型额度（本机未配 `DEEPSEEK_API_KEY`，翻译返回“AI服务未配置”） | 本机未配置 | 运维 |
| Android 测试机 | 已连接，但本轮观察到另一窗口正在使用（浏览器前台、已有 5173/3000 反向代理）**未抢占** | 协调使用时段 |
| iPhone | 未盘点到可用设备 | 用户 |
| 生产密钥：`XIAOBU_USER_REF_SECRET`、`XIAOBU_MEDIA_URL_SECRET`、`XIAOBU_DEVICE_PEPPER`（均 ≥32 字符） | 未配置 | 运维（不进仓库） |

## 5. 模拟与真实能力边界

- `MockXiaozhiProvider` 只在非生产环境、且显式 `XIAOBU_VOICE_PROVIDER=mock` 时启用；生产环境设置会被拒绝并回退为暂未开放。
- 模拟会话在库里 `providerIsMock=true`、`usageState=mock`、`usageSource=mock`，不计供应商成本，流水注明“模拟供应商，非真实计费”；后台列表单独标注，异常统计排除模拟。
- 页面：模拟环境顶部常驻“测试环境 · 模拟会话，非真实语音，不产生费用”；挂断后说明“模拟会话，非真实用量”。
- 模拟器不下发任何凭据；回调用进程内随机密钥签名，格式与小智无任何对应关系。
- 模拟器同样不宣称支持用户身份、角色下发、设备、记忆、完整性——这些正是商业接口要回答的问题。
- **本轮所有“通过”都来自模拟供应商或暂未开放实例；没有任何一项代表小智已接通。**

## 6. 数据迁移与回滚

新增迁移 `manual_add_voice_provider_boundary`（纯新增列与表；`requestId` 先加可空列→回填→再设非空；全部 `IF NOT EXISTS`）。

执行顺序（部署前在目标库依次执行，均未在任何生产库执行过）：
`manual_add_xiaobu_ai_assets` → `manual_add_knowledge_source` → `manual_add_owner_services` → `manual_add_knowledge_debate` →
`manual_add_voice_trial_usage` → `manual_add_user_preferred_name` → `manual_add_couple_scene` → `manual_add_paipan_case_feedback` →
`manual_add_voice_provider_boundary`

隔离库验证（本机 PostgreSQL 16 @5433，`xiaobu_closure_20260921`）：HEAD 基线 schema 建空库 → 依序执行 9 个迁移 → `prisma migrate diff` **无差异**；
回滚最新迁移后重放两次（幂等）→ **仍无差异**。脚本见会话临时目录 `migcheck.sh`（不入库）。

核对中发现并修正 3 处**早先迁移与 schema 的漂移**（非本轮引入）：CoupleChart.scene 索引、PaipanReportKnowledge.sourceKind 索引、
UserPreferredName.updatedAt 默认值。按“不改已写迁移”原则，改 schema 对齐迁移。

回滚：`rollback.sql` 删除本轮新表与新列（会丢失这些字段上的数据，执行前先备份）。代码回滚：撤回对应提交即可（见 §11）。

## 7. 计费与额度口径（未拍板项保持保守默认）

- `chargeUsers` 默认 **false**：不向用户扣额度；免费单次上限 180 秒。价格已拍板但链路未通前不打开。
- 预留：收费模式下开始会话原子预留 `min(可用, 单次上限)`；并发同一账户只有一个能预留成功（真实库并发测）。
- 结算：以供应商用量为准；供应商未给时长 → `unknown`，**只释放预留、不扣、不填 0**，进异常清单人工对账；有客户端估算 → `estimated` 待对账。
- 幂等：开始按 `userId+clientRequestId`；签发/结束按会话幂等键；回调按 `(provider, eventId)`；发放按请求号；结算条件更新只生效一次。
- 预算统计不是并发硬上限；硬约束只有额度预留与单次上限。
- 供应商成本、用户收费、圈主分成**分开记账**；分账规则未确认，**未实现自动分账**。
- **上线阻塞（S07）**：报告生成接口没有支付/权益门禁，且每份报告按报告 ID 幂等发放 30 分钟语音。`chargeUsers` 打开前必须先接报告支付门禁，否则免费生成报告即可领取语音时长。本轮按要求只记录，未改收费规则。
- 复用已有 AI 结果（翻译/译文/断句）也扣 AI 次数（决策人 09-21 拍板）。
- 报告赠送时长用完可充值（`VOICE_MINUTES` 订单，2 元/分钟），语音开始计费前不开放下单。

## 8. 权限矩阵

| 能力 | 普通用户 | 圈主 | 客服 | 运营 | 财务 | 超管 |
| --- | --- | --- | --- | --- | --- | --- |
| 开始/结束/评价本人语音会话、看本人记录 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 看他人会话（用户侧接口） | ✗（404） | ✗ | ✗ | ✗ | ✗ | ✗ |
| 圈子语音助理 | 有效成员 | ✓ | — | — | — | — |
| 提交圈子语音角色 | ✗ | 仅本圈 OWNER | ✗ | 审核 | ✗ | 审核 |
| 绑定/解绑/转赠本人设备 | ✓ | ✓ | — | — | — | — |
| 后台设备台账：查看 | ✗ | ✗ | ✓（绑定人脱敏） | ✓ | ✗ | ✓ |
| 后台设备：登记/发码/停用/恢复 | ✗ | ✗ | ✗ | ✓ | ✗ | ✓ |
| 后台会话明细与异常 | ✗ | ✗ | ✓（接口，脱敏；后台页面暂只对运营/财务/超管开放） | ✓ | ✓ | ✓ |
| 后台用量/成本/供应商状态 | ✗ | ✗ | ✗ | ✓ | ✓ | ✓ |
| 手工发放语音额度（审计） | ✗ | ✗ | ✗ | ✓ | ✗（只读） | ✓ |
| 导出会话/用量明细 | 不提供接口，需走数据导出审批 | | | | | |

脱敏与日志：后台用户标识只显示末 6 位；设备序列号只存 HMAC 与末四位；绑定码/转赠码只存哈希；
上下文只存摘要哈希；回调原文只存哈希；端上凭据不落库不写日志；日志只记 requestId、工具名与耗时。

## 9. 测试结果与未覆盖项

| 类别 | 结果 | 说明 |
| --- | --- | --- |
| 服务端相关范围单测+集成 | 204 组 / 2615 项通过；需真实 Redis 的 1 组单独跑 4/4 | voice/dialogue/tts/classic/search/circle/paipan/practitioner/user/upload/ai-gateway/redis/tool-registry/test |
| 真实库集成 · 语音会话与设备 | 14 项 | 幂等并发 5→1、同账户并发预留 4→1、并发结束只调一次、回调重复只扣一次、迟到回调待对账、无回调 unknown 不填 0、签发拒绝/超时释放、他人 404、超时清理、切换供应商遗留会话不外发、免费模式、设备绑定码并发、转赠后历史隔离、停用 |
| 真实库集成 · 额度 | 4 项 | 存量用例在新结算口径下仍通过 |
| HTTP 层契约与越权 | 7 项 | 真实路由+DTO 校验+角色守卫+真实库；客服/财务/普通用户的越权全部被拒 |
| 古籍派生资产真实库 | 4 项 | 公开口径、底本不变、并发只生成一份、原文修订后译文 expired |
| 有声读书真实库 | 5 项 | 并发只合成一次、签名篡改/过期/换资产失效、白话需当前版本译文、未公开段落拒绝、断点 |
| 简体转换 | 9 项 + 真实数据预览 | 全字表枚举等长（不靠抽样）；本机库 300 章等长失败 0 |
| 反证（注入缺陷看测试是否变红） | 通过 | 去掉脱敏/子串校验 → 3 条红；去掉会话归属校验 → 2 条红 |
| 迁移 | 执行后/回滚重放后均无差异 | |
| 真实库 + 真实 Redis · 资产并发 | 4 项 | 两实例并发同一音频只合成 1 次；assetKey 唯一；文本资产并发模型 1 次；报告知识库检索（该用例写于 09-17、按旧口径断言，09-18 口径改为“各派都返回、指定门派排第一”后因环境门控一直未重跑，本轮更新） |
| 移动端 / 后台 `vue-tsc` | 0 错误 / 0 错误 | |
| 八字引擎 | 10 组 490 项 | |
| H5 实点（本机 Chrome 390×844） | 8/8 | 未开放态（真实 unavailable 实例响应）、模拟接通-挂断-评价、报告语音不含出生信息、他人报告被拒、记录页本人/他人、设备绑定-待开通-转赠、阅读页简体/原貌与语音讲讲跳转、导览卡片目标页 |

**明确未覆盖**：小智商业 API、真实语音收音播放与打断、真实供应商回调、真实成本与账单对账、真实收费与分账、
真实硬件与商业固件、COS 真实桶、真实模型输出质量（断句/译文/伴读）、多音字真实读音、Android/iPhone 真机、小程序。
第三方测试客户端（xiaozhi-android-client）的真机结果只证明官方云协议可行，**不代表热卜 App 已接入**。

## 10. 商业 API 到位后的最短上线步骤

1. 取得文档、测试账号、回调规范；按 `docs/design/小智商业API适配边界与字段映射-20260921.md` 填映射表（附出处）。
2. 实现 `xiaozhi-commercial.provider.ts` 并在注册表接入；`probe()` 只把联调证实的能力写成 supported。
3. 配置密钥（`XIAOBU_USER_REF_SECRET`、`XIAOBU_MEDIA_URL_SECRET`、`XIAOBU_DEVICE_PEPPER`、供应商凭据），不进仓库。
4. 在隔离环境、授权测试预算内：签发/结束/回调/超时/取消/重复回调各一轮；核对供应商账单与 `VoiceSession` 用量。
5. 移动端接入商业端上 SDK（收音、播放、打断、静音），补齐聆听/识别/播放/静音/重连状态；真机逐端验收（Android、iPhone、小程序、样机）。
6. 若供应商能传已验证身份：MCP 桥把身份传给 `handle()`，圈子私有知识工具才会出现。
7. 先接报告支付门禁（§7），再由运营后台打开 `chargeUsers`；分账规则拍板后另行实现。
8. 生产执行 §6 迁移（单一写入者、先备份）→ 灰度 → 观察异常清单（unknown/estimated/未匹配回调/卡住会话）。

## 11. 主线接收顺序与撤回方式

接收顺序（在 `2e458597a` 之上依次）：

1. `ccc850af1` 依赖快照——主线若已接收 `b5403aeba` 可跳过
2. `59796ab21` 排盘算法核验线存量——须与第 3 条一起接收
3. `a285ad325` 小卜存量
4. `adef3577f` → `d7fb451ee` → `aeca1dca3` → `a0d0e7f87` → `e048a0803` → `10cc2de0b`（本轮）

注意：
- 第 2 条不含 `apps/mobile` 排盘引擎改动（与前端窗口共改，单独接收），读取移动端源码的 27 个跨端比对测试也未收入。
- 主工作区的同名文件仍有其他窗口未提交的改动；合并时以分支内容为准逐文件核对，**不要用主工作区文件整体覆盖**。
- 与其他分支的已知交叠文件：`schema.prisma`、admin `router/index.ts`、`menu-structure.ts`、`pages.json`、`redis.service.ts`、`third-party-services.ts`、`ai-gateway.module.ts`、`search.service.ts`、`classic.controller.ts`。

撤回：
- 代码：`git revert` 对应提交（本轮 6 个提交彼此独立性较好；撤回 `adef3577f` 前先撤回依赖它的前端与修复提交）。
- 数据：执行 `manual_add_voice_provider_boundary/rollback.sql`（先备份）。
- 运行：`XIAOBU_VOICE_PROVIDER` 不设置即为暂未开放，页面自动显示未开放态，无需改代码。

## 12. 本轮发现并修复的问题（均附测试）

1. 小卜代码从未进分支（已剥离入分支）
2. 3 处早先迁移与 schema 漂移
3. 段落接口、断句、语音伴读上下文未校验“已发布+版权已审计”，未公开正文可读出
4. 搜索与内容导览推荐版权未审的古籍（点开 404）
5. TTS 超 3000 字静默截断（旧用例把缺陷固化成规格）
6. 播放倍速进入音频资产身份，每档倍速重复合成（段落音频已固定 rate）
7. 报告页文案“语音功能开放中”与实际不符
8. 语音会话清理会把旧供应商的会话发给当前供应商
9. 挂断后等回调期间无法评价
10. 简体转换：OpenCC 字表把单字「乾」首选为「干」（卦名改义）；「餘→余」与古文第一人称混淆（真实数据 616 处）
11. 语音页“正在讨论”把给模型的指令展示给了用户；报告场景给供应商的上下文带报告标题与摘要中的命主姓名——改为用户话题与模型指令分离、标题不下发、姓名替换为「命主」

已知限制（未改）：TTS 降级到备用供应商期间，每次请求都会重试首选供应商并重新合成（设计上优先回到首选音色，降级期间有重复合成成本）。

## 13. 决策人 2026-09-21 拍板与落地

| 事项 | 决定 | 落地 |
| --- | --- | --- |
| 复用已有 AI 结果是否计次 | **也要扣 AI 次数** | 白话翻译、段落译文、AI 断句命中已有结果先扣次数再返回（不再调模型）；原文本身已有足够标点、直接返回原文的不扣；公开的译文状态接口不再返回正文（防绕过计次）。提交 `98032d4ad` |
| 报告分享链接有效期 | 暂不设置 | 维持现状（仅限单份报告、可撤回、按报告每日限次） |
| 报告赠送 30 分钟用完后 | **支持充值继续使用** | 新订单类型 `VOICE_MINUTES`（迁移 `manual_add_voice_minutes_order_type`）；档位默认 10/30/60 分钟、2 元/分钟服务端计价；支付后同一事务加时长（按订单幂等）；退款冲正最多扣回可用余额，已用部分记流水待人工核对；语音未开始计费时拒绝下单；移动端充值页与两处入口。真实库集成 3 项通过 |
| 分享报告是否保留出生日期 | 保留 | 维持现状 |

仍待处理：报告生成本身仍无支付/权益门禁（与充值无关，打开 `chargeUsers` 前须接上）。
真机：09-21 晚手机可用后尝试真机 H5，但手机自带浏览器被系统「数字健康」停用（“停用中，无法使用”），手机上无其他浏览器，未绕过该限制，真机 H5 仍未验。
