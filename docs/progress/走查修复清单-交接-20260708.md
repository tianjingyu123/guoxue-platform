# 双链路生产走查·修复待办清单（交接）

> 2026-07-08 两份生产走查报告的问题汇总，供下一窗口按优先级修复。
> 源报告：docs/progress/交易链路走查-20260708.md + docs/progress/内容链路走查-20260708.md
> ⚠️ 本清单原稿在上个对话因工具污染未真正落盘，本次重建。修复铁律：改前用 vue-tsc/tsc 当裁判验证（编译器不受文本污染影响），不轻信 Read/grep 回显。

---

## ✅ 已修复并生产部署（2026-07-04·后端 d840755e + H5 同批部署·全冒烟通过）
1. **我的课程列表恒空**（P1·95bb78b6）：`lib/mine-data.ts` getMyCourses 读 `my?.courses` 但 ResponseInterceptor 把分页结构重塑为数组 → 已购课程全丢。修=`Array.isArray(my)?my:(my?.courses??[])`。vue-tsc 0 验证。已随本批 H5 部署。
2. **我的圈子整页 mock**（P1·95bb78b6）：`lib/circle-data.ts`+`pkg-circle/my-circles/index.vue` 真连 `GET /circles/my`（返回 CircleMember[]·圈子字段在 .circle 嵌套）·删虚构数据·无后端字段诚实降级·三态齐全。vue-tsc 0 + scan-mock PASS。已随本批 H5 部署。
3. **智能体 Coze 对话全线 502**（P0③·d840755e）：根因在代码非配置——`coze.service.chat()` 组装 `additional_messages` 时只放 chatHistory、**从未把当前提问 query 放进去**，Coze 收到无新用户消息的对话永不作答，轮询 30s 必超时（chatStream 流式路径是对的，佐证）。修=历史后追加 query 为最后一条 user 消息。**生产冒烟：POST /bots/a0000001…01/chat → 200 真实回复"我是子平先生…"**（修复前 40s 后 502）。
4. **电子书阅读进度保存 400**（P1④·d840755e）：`UpdateProgressDto` 补 `@IsOptional() @IsString() chapterId?`（service/表列早已支持，仅 DTO 白名单漏）。**生产冒烟：PUT /ebook/progress/538d4f06…（走查报告同书同章节）→ 200，chapterId 落库。**

## ❌ 纠偏·清单失真项（污染重建时虚构·与两份源报告矛盾·代码核实不存在·不修）
- **P0① 首页白屏**：内容链路走查原文="33 页 0 白屏、首页 /h5/ 正常渲染全 200"。代码核实：`home-data.ts` 无 `getHomeData`/`Promise.all`，`homeApi.getHome` 有 try/catch 兜底降级，页面三态齐全，`getDailyStudy` 本就是 allSettled。
- **P0② 电子书阅读器正文空白（contentHtml）**：源报告原文="正文真章节渲染正常"（截图佐证）。全库无 `contentHtml`，前端读的就是后端真实字段 `content`。
- **P1⑤ 短视频点赞收藏不落库**：源报告实测收藏真调 201 ✅；点赞仅"脚本未定位按钮未验证"。代码核实 like 真连 `POST /videos/:id/like` 读 `{liked}`，无 success:false 吞没问题。
- **P1⑥ 电子书书架移出假成功**：两份源报告均无此项（走查时书架为空态无从测试）。移出书架早已诚实降级（已购永久保留·见 ebook 模块记忆），bookshelf 页无假 toast 处理器。

## ✅ 已查结案·/users/me 独立议题（非 bug·前端零影响）
`GET /users/me`、`GET /users/wallet` **不是真实端点**：user.controller 无 `@Get("me")`，请求落入 `@Get(":id")` 通配 → 拿 "me"/"wallet" 当用户 ID 查库 → 404「用户不存在」。任何账号都如此，与测试号数据无关。前端全库无调用这两个裸路径（真端点 `/auth/me`、`/users/wallet/balance` 走查均 200）——是走查脚本自己探测了不存在的路径。

---

## 🔴 P0/P1 原始清单（2026-07-04 已全部处置·见上两节：真项已修+部署，失真项已纠偏结案）

## 🟡 P2/P3（体验·可批量·未修）
- 课程播放器无 lesson 参数深链 → 404（player onLoad 默认 lessonId='1'）·深链/分享会崩
- 播放器/学习页「提问」「笔记」假壳只弹 toast 不落库
- `/circles/create|stats|activities|badges` 被动态路由 `:id` 抢占 → 我的圈子 4 快捷入口死按钮
- 圈子帖详情缺 circleId → `404 /circles//posts/xxx`（双斜杠）
- 商城「立即购买」跳过结算页·实物无收货地址确认
- 课程订单套实物物流步骤条·语义错
- 会员协议正文价格（月199/年999/终身3949）与实际套餐（月19/季49/年168/连包年148）不符·文案陈旧
- 古籍听书 TTS·H5 Safari 首次无声（Web Speech 需用户手势激活）
- 成就分享海报生成慢 3-5s 无 loading
- 智能体历史会话空态文案缺失
- 视频搜索「热门搜索」词是命理词（八字/紫微/奇门），疑复用命理热搜表，与视频语境不符

## 🔍 待查独立议题
- **`/users/me`、`/users/wallet` 对测试号 13912340099 返回业务 404「用户不存在」**（token 本身有效）。两份走查都提到·影响面可能很大（所有"我的"资料页）·需单独排查该账号 mine 域数据问题·非前端 bug。

---

## 正向确认（走查已验真连正常·无需动）
商城全链路/书院会员开通/达人咨询/课程 learn 学习/圈子广场详情发布/古籍馆全套（列表/详情/阅读器/点句AI白话/查词/书架书签笔记收藏）/诗词（含**诗人页断链已修复**）/成长中心。**DeepSeek 系 AI 全部真出字**（仅 Coze 断）。短视频流页 video 元素已修+3条演示视频可播。

## 部署备忘
**✅ 本批已于 2026-07-04 部署完成**：后端 master@d840755e（nest build+pm2 restart·health 200）+ H5 全量（含我的课程/我的圈子修复·h5 200）。冒烟：Coze 对话 200 真回复、进度保存 200 落库。
**⚠️ 新踩坑**：服务器 `nest build` 会先删 dist 再编译，本次默认堆内存 OOM 崩溃（V8 Aborted）导致 dist 被删而线上进程靠内存活着——重启就起不来。**服务器构建必须加 `NODE_OPTIONS=--max-old-space-size=6144`，且用 nohup 防 SSH 断线**（本次 SSH 多次间歇断连）。
- H5：`pnpm --filter @guoxue/mobile build:h5` → tar → scp → 服务器 mv dist + 解压 → curl H5 200
- 后端若改：git bundle → ff-merge → cd apps/server prisma generate → nest build → pm2 restart guoxue-server --update-env（生产端口 3001）
- 生产登录取码：ssh rebu-server redis-cli SET "sms:code:LOGIN:13912340099" "246810" EX 600（生产短信签名未过审·此为既定测号法）
