# 双链路生产走查·修复待办清单（交接）

> 2026-07-08 两份生产走查报告的问题汇总，供下一窗口按优先级修复。
> 源报告：docs/progress/交易链路走查-20260708.md + docs/progress/内容链路走查-20260708.md
> ⚠️ 本清单原稿在上个对话因工具污染未真正落盘，本次重建。修复铁律：改前用 vue-tsc/tsc 当裁判验证（编译器不受文本污染影响），不轻信 Read/grep 回显。

---

## ✅ 已修复并提交（commit 95bb78b6·未部署）
1. **我的课程列表恒空**（P1）：`lib/mine-data.ts` getMyCourses 读 `my?.courses` 但 ResponseInterceptor 把分页结构重塑为数组 → 已购课程全丢。修=`Array.isArray(my)?my:(my?.courses??[])`。vue-tsc 0 验证。
2. **我的圈子整页 mock**（P1）：`lib/circle-data.ts`+`pkg-circle/my-circles/index.vue` 真连 `GET /circles/my`（返回 CircleMember[]·圈子字段在 .circle 嵌套）·删虚构数据·无后端字段诚实降级·三态齐全。vue-tsc 0 + scan-mock PASS。

---

## 🔴 P0（阻断·最优先·未修）
| # | 问题 | 位置 | 修法建议 |
|---|---|---|---|
| 1 | **首页白屏**（流量入口！）| `lib/home-data.ts` getHomeData（约 L316）| `Promise.all([banners,feed,quickEntries])` 任一失败整页崩 → 改 `Promise.allSettled` 分区容错，单接口失败降级为空数组，页面不崩。证据 guoxue-e2e/shots/content-home-*.png |
| 2 | **电子书阅读器正文空白**（已购用户白页）| `lib/ebook-data.ts` 章节内容取数 | 后端 `GET /ebooks/:id/chapters/:cid` 返回 `content` 字段，前端读 `data.contentHtml` 字段名不匹配 → 正文永远空。修=对齐字段名（先 curl 确认后端真实字段）|
| 3 | **智能体对话全线 502**（Coze 集成挂）| 后端 Coze 集成 | 广场 5 智能体+智玄+客服走 `POST /bots/:id/chat` 全部 35-40s 后 502「COZE对话超时」。**注意：DeepSeek 系 AI 全正常**（古籍伴读/点句白话/诗词 AI 均真返回），只 Coze 断 → 查后端 Coze 配置/网络/超时设置，非前端问题 |

## 🟠 P1（功能断裂·未修）
| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 4 | **电子书阅读进度保存 400**（静默丢失）| 后端 `ebook.dto.ts` UpdateProgressDto（约 L154）| 前端 PUT /ebook/progress/:id 传 `chapterId`，后端白名单只收 `progress/currentPage`，forbidNonWhitelisted 400，前端 catch 吞掉 → 每次读书进度保存失败重开丢位置。修=DTO 补 `chapterId?`（对比古籍 /classic/progress 正常）|
| 5 | 短视频点赞/收藏不落库 | `pkg-video` + video 数据层 | `POST /videos/:id/like` 响应 success:false 未处理·乐观更新刷新丢失。查后端为何返 false + 前端失败回滚 |
| 6 | 电子书书架「移出」假成功 | `pkg-ebook` 书架页 | toast 假成功无后端调用·真连移出端点或诚实降级 |

## 🟡 P2/P3（体验·可批量·未修）
- 课程播放器无 lesson 参数深链 → 404（player onLoad 默认 lessonId='1'）·深链/分享会崩
- 播放器/学习页「提问」「笔记」假壳只弹 toast 不落库
- `/circles/create|stats|activities|badges` 被动态路由 `:id` 抢占 → 我的圈子 4 快捷入口死按钮
- 圈子帖详情缺 circleId → `404 /circles//posts/xxx`（双斜杠）
- 商城「立即购买」跳过结算页·实物无收货地址确认
- 课程订单套实物物流步骤条·语义错
- 会员协议正文价格（月199/年999/终身3949）与实际套餐（月19/季49/年168/连包年148）不符·文案陈旧
- 古籍听书 TTS·H5 Safari 首次无声（Web Speech 需用户手势激活）
- 成就分享海报生成慢 3-5s 无 loading
- 智能体历史会话空态文案缺失
- 视频搜索「热门搜索」词是命理词（八字/紫微/奇门），疑复用命理热搜表，与视频语境不符

## 🔍 待查独立议题
- **`/users/me`、`/users/wallet` 对测试号 13912340099 返回业务 404「用户不存在」**（token 本身有效）。两份走查都提到·影响面可能很大（所有"我的"资料页）·需单独排查该账号 mine 域数据问题·非前端 bug。

---

## 正向确认（走查已验真连正常·无需动）
商城全链路/书院会员开通/达人咨询/课程 learn 学习/圈子广场详情发布/古籍馆全套（列表/详情/阅读器/点句AI白话/查词/书架书签笔记收藏）/诗词（含**诗人页断链已修复**）/成长中心。**DeepSeek 系 AI 全部真出字**（仅 Coze 断）。短视频流页 video 元素已修+3条演示视频可播。

## 部署备忘
本批待部署（攒齐 P0/P1 修完后一次 build+deploy）：已提交的我的课程/我的圈子 + P0×3 + P1×3。
- H5：`pnpm --filter @guoxue/mobile build:h5` → tar → scp → 服务器 mv dist + 解压 → curl H5 200
- 后端若改：git bundle → ff-merge → cd apps/server prisma generate → nest build → pm2 restart guoxue-server --update-env（生产端口 3001）
- 生产登录取码：ssh rebu-server redis-cli SET "sms:code:LOGIN:13912340099" "246810" EX 600（生产短信签名未过审·此为既定测号法）
