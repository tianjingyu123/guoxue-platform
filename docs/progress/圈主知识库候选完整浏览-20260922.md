# 圈主知识库与候选完整浏览（2026-09-22）

圈主助理知识库管理页此前每类只取前 50 条，接口错误被适配层吞掉后显示“知识库还是空的”或“暂无待确认候选”；管理概览候选数也最多只反映首 50 条。这里不能把“前 50 条”当作全部资源。

本批接通管理页已入库与待确认候选各自的分页、真实总数和失败重试。首批读取失败显示错误；续页失败保留已加载条目。概览待确认数使用服务端总数，不再用当前数组长度。服务端两类列表均加入 ID 次序，保证同时间记录分页稳定；顶栏避让微信胶囊，返回操作区至少 44px。没有改变知识确认、忽略、删除、AI 提炼的写入逻辑与权限。

本地验证：`tests/release/circle-knowledge-browser.mjs` 用拦截数据验证首批故障、已入库和候选各 23 条分页、续页失败重试；移动端类型检查、服务端构建、小程序构建通过；知识库控制器 13/13 测试通过。最终小程序主包 1.63 MB，`pkg-circle` 0.98 MB。未接触真实知识数据，也未做真机和线上管理权限验收。
