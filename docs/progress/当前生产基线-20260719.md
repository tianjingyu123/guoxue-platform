# 当前生产基线（2026-07-19）

> 本文件是项目进度的唯一当前真源。旧进度、旧交接和旧阻塞清单只用于追溯历史，不得据此判断现状。

## 一、代码基线

- 主仓：`D:\guoxue-platform`
- 当前分支：`master`
- 当前已验证代码基线：`6911e4b7`（商家发货物流闭环）
- 已验收生产代码基线：`6911e4b7`
- 当前生产 H5：`index-R00ZYbBO.js`
- 上一批上线前保护分支：`archive/prod-pre-inventory-20260719`（生产服务器）
- 上一生产基线保护标签：`prod-20260719-merchant-inventory`
- 本批上线前生产源码基线：`d8ae3c96`
- 当前生产基线保护标签：`prod-20260719-merchant-shipping`
- 商城缓存原始恢复分支：`recovery/shop-cache-invalidation-20260719`

原先停留在 `D:\gx-deploy-91` 游离工作树的 39 个提交已于 2026-07-19 全部快进合回 `master`。后续开发一律以主仓 `master` 为准，不再直接在该游离工作树继续开发。

## 二、已上线并经用户确认

1. 后台管理端 Z1-Z7 七区整改完成，人工工作区与 AI 自动化工作区已分离。
2. 短信验证码 scene 大小写问题、Token 续期把 HTTP 201 判失败的问题已修复。
3. 智能体广场与古籍伴读已切换真实 SSE 流式输出。
4. 退出智能体后再次进入可自动接续上次会话。
5. AI 一键排版已上线：后端 `typeset` 接口有“不改文字”硬校验，前端编辑器有排版入口。
6. 智能体最近使用卡片溢出已修复；当前 H5 构建标识为 `index-HMnOp98z`。
7. `BotConfig` 已增加 `runtime`、`systemPrompt`，生产已执行 DDL 并重新生成 Prisma Client。
8. 八字命理精批师、紫微斗数分析师、合婚匹配顾问、六爻占卜师、择日通书师已切换 `runtime=local`，通过自建 AI Gateway 调用 DeepSeek 流式回答；用户真机测试确认正常。
9. 快递100 `third_party.kuaidi100` 已配置，密钥加载、签名、实时轨迹查询、移动端物流时间线和商家后台查询均已接通。
10. 商家进销存已上线：库存台账与流水、采购到货、盘点调整、低库存预警，以及订单扣减、取消回补、退款/退货入库全部接通。
11. 商家发货物流闭环已上线：单笔事务发货、批量发货、快递100真实轨迹、运单修改、缓存失效与操作审计全部接通。

## 三、2026-07-19 本批已部署增量

### 商城支付后订单缓存修复（`babfc78e`）

- 支付入账后立即清理订单详情缓存。
- 清理该用户所有分页、所有状态组合的订单列表缓存。
- 发货、完成、取消和人工确认付款统一走 `invalidateOrderCache()`。
- 修复支付成功后页面可能继续显示“待付款”最长 300 秒的问题。

### 测试与接口文档同步（`713d5180`）

- 支付回调 E2E 与 HTTP 200 正确契约对齐。
- 通用 `/coin/spend` 按资金安全要求保持 fail-closed，测试不再假设客户端可直接扣币。
- 补齐新增依赖的测试 mock、排盘 AI 风险提示断言和支付回调 Swagger 200 声明。

### 商家进销存（`00f33a58`）

- 新增 `InventoryMovement`、`InventoryAlertSetting`、`PurchaseOrder`、`PurchaseOrderItem` 四表及三组枚举。
- 库存变更全部事务化、幂等并保留不可变流水；已发货退款不擅自自动回补，退货需验收入库。
- 商家端新增库存总览、流水、采购、补货、盘点和预警操作页。
- 生产 additive DDL 已获董事长授权并执行，确认连接 `guoxue@5433`；四张新表上线时均为 0 行，未写生产业务数据。

### 商家发货物流闭环（`6911e4b7`）

- 单笔发货改为数据库事务 + `PAID → SHIPPED` CAS，订单状态与运单同成同败；同一运单重复提交幂等。
- 新增最多 50 单的批量发货，逐单隔离失败并返回成功/失败明细；商家端新增“今日出库清单”页面。
- 商家订单详情接入快递100实时轨迹，支持刷新、复制运单和待收货阶段修改错单；查询失败 fail-open，不阻断订单详情。
- 发货和改运单均清理买家订单缓存并写操作审计；销售库存仍只在建单事务内扣减，发货不会二次扣库。
- 本批无 schema 变化、未执行生产 DDL、未写生产业务数据。

## 四、验证与生产验收

- 后端 `tsc --noEmit`：通过。
- 管理后台 `vue-tsc --noEmit`：通过。
- 移动端 `vue-tsc --noEmit`：通过。
- 后端构建：通过。
- 管理后台构建：通过。
- H5 构建：通过。
- 移动端 mock 导入扫描：通过。
- 后端 Jest：`431 suites / 5733 tests` 全部通过。
- 上一批相关回归：`14 suites / 214 tests` 全部通过。
- 本批采购入库、销售扣库、快递100、事务发货回归：`4 suites / 77 tests` 全部通过；控制器与发货专项另验 `2 suites / 15 tests` 全部通过。
- 生产 Prisma Client `6.19.3` 重新生成成功，后端四个 PM2 cluster worker 全部 `online`。
- 内网与外网 `/api/v1/health` 均为 HTTP 200，DB/Redis/支付/AI/存储等检查全部 `ok`。
- 外网库存与物流 API 未登录返回正确 HTTP 401；H5 主资源为 `index-R00ZYbBO.js`，批量发货与订单详情新 chunk 均为 HTTP 200。
- 回退点：上一批数据库备份继续保留；本批旧后端与旧 H5 分别保留为 `dist.bak-6911e4b7`，无需数据库回退。

## 五、下一阶段主线

### P0：商家侧生产验收与观测

由真实商家账号验收库存总览、采购到货、盘点、预警和退货验收入库，以及单笔/批量发货、真实物流轨迹和运单修改；部署窗口不代写生产业务数据。持续观察订单支付、取消、退款、发货后的库存流水、缓存与幂等性。

### P1：继续 B 端经营闭环

库存地基完成后，优先推进商家优惠券/客户复触达，或继续处理分站邀请归属、续费与运营商名额体系；具体优先级由董事长拍板。

## 六、工作纪律

1. 项目当前真源为本文件和 `master`，不要再把旧交接文档当作最新状态。
2. 生产是否完成必须同时具备：代码提交、构建/测试、部署记录、真实 API 或真机验证。
3. 生产数据库为宿主机端口 5433；不得把旧容器 5432 的废库结果当成生产事实。
4. `.claude/settings.local.json` 是用户本地配置，保持未提交，不纳入业务提交。
5. 每个功能独立提交，部署前保留可回退基线。
