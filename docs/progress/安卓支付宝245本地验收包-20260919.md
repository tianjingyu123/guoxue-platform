# 安卓支付宝 245 本地验收包

2026-09-19。仅交付本地 APK，未发布 H5、服务端或应用市场，未操作手机，未发起付款/退款。手机安装和真机支付由主任务负责。

## 代码与目标

- 工作树：`D:/gx-deploy-91/.worktrees/android-alipay-ready-20260919`。
- 分支：`codex/android-alipay-ready-20260919`。
- 基线：`a508d76c`，对应已发布 H5 支付源 `73d6a2af`。移植旧原生候选 `9bd229c8` 后为 `98218833`；本次完整修复提交 `1320f7f38ffd6de65d863a0a500574cd70f4ab03`。
- 只补安卓：结算页、原订单详情、购买弹层均显示支付宝支付，未开通的安卓微信/银联不能从这些入口建支付单；旧微信收银深链在发起请求前拦截。
- 保留 H5 的公众号登录身份复用、手机支付宝支付/电脑扫码、微信内仅开放微信的环境限制。iOS 原分支、小程序原分支不增加支付宝支持，不将安卓方案用于 iOS 虚拟商品。
- 金额和订单流水取本人服务端订单；初始化前后使用已发布 `/shop/orders/:id/current`，解决缓存仍无支付流水而无法打开支付宝的问题。
- 3 秒普通轮询中，每 10 秒最多一次直读，单页面每分钟最多 8 次；服务端已确认 PAID/SHIPPED/COMPLETED 才回原订单。通道 S、openURL 成功、URL 参数均不是支付成功证据。
- 账号和订单隔离的持久化标记先保存再请求；存储失败、请求超时、重新进入、重复点击均不自动重建不确定的付款。账号切换、页面离开后迟到响应不拉起支付宝、不回跳。
- 返回原订单携带 paymentReturn=1 只触发首次直读；同账号旧 PENDING 缓存不会覆盖已确认的支付状态，真实退款仍能刷新。

## APK

`D:/gx-deploy-91/artifacts/android-payment-acceptance-20260919/candidate-245/rebu-1.1.0-245-preprod.apk`

- SHA256：`53f5e215b4a0f61a1bb05c15ab5a047c4e04406e6450e0e2fd447c09a0d2f302`。
- 大小：30,988,367 字节；包名 `com.rebu.apprebu`；应用 ID `__UNI__277B108`。
- 源码、编译产物、APK 内外版本：`1.1.0 (245)`。
- app-service SHA256：`e023a2122865ee1b678c888f9140fd13f5ae065ca0724a401f38a2e2820d4eca`。
- 证书 SHA256：`737c54a45e710bce7c6875a41a5a5c0c228c09bd31fc08df8e016364b61b9d67`，与手机已安装 244 一致；apksigner v1/v2 验证通过，zipalign 检查通过。
- 文件名 preprod 是现有打包脚本历史命名，实际运行 API 为 `https://api.rebugx.cn`，静态资源 `https://static.rebugx.cn`。这是正式签名的本地验收候选，不代表已云打包或已上架。

## 打包可追溯性

原包由主任务从已安装设备只读获取：`artifacts/android-payment-acceptance-20260919/installed-244.apk`，SHA256 `6024ec57bb736ae5df970898aa9efc47667b67eeb66d8a881e4c5f92f27092b5`，与归档官方云包一致。

采用既有 `D:/gx-deploy-91/scripts/release/build-preprod-android-candidate.ps1`，从原 244 新解包，apktool 使用 `--no-src` 保留 DEX 字节；替换本次编译的 www 资源后使用原正式 keystore 签名。私钥/密码仅由现有脚本在受限文件及进程中读取，没有输出或提交。

已检查所有共有 plus 运行配置一致，编译包没有新增原生模块。暂存资源 `web-245-cloud-compatible` 保留 244 官方云包 manifest 的原生/云打包运行配置，仅更新 version；其余资源与本次编译逐字节一致。保留 Orientation、adid、channel 等云包字段，不把编译期 nativePlugins/distribute 元数据当成新增原生能力。详细记录 `manifest-compatibility.json`。

签名 APK 再解包核实外层版本/包名，压缩包逐文件检查：29 个 DEX/原生库文件与 244 完全一致；477 个 www 文件与暂存资源一致、无多余旧文件。运行兼容性仍需真机验收，字节核对不能代替启动和支付测试。

## 验证与证据

证据目录：`D:/gx-deploy-91/artifacts/android-payment-acceptance-20260919`。

- 完整 vue-tsc --noEmit 退出 0。
- APP 构建通过、原生脚本门禁通过；`build-245.log`。
- H5 回归构建通过，未发布；`h5-regression-build.log`。
- 74 项支付、订单刷新、顶层微信回跳测试通过；`payment-tests.log`。覆盖真实三个入口函数、组件账号隔离与持久化、旧缓存、有限直读、通道 S 非成功、原单回跳及 H5/iOS/小程序原行为。
- `package-245.log`、`decode-verify-245.log`、`verification-245.json` 记录签名、包内版本及文件匹配。
- Sass legacy-js-api 警告不影响构建；apksigner 对 META-INF 的 v1 范围警告存在，整包 v2 验证通过。

## 线上基线与未完成验收

本次只读核对两节点，仍为 9 月 14 日支付发布：H5 live/startup SHA256 均 `6ce011feebfcd9b9d757fc4fb88c739453025f45e099aaa69de71a197d186a38`，两节点健康、restartCount=0。未混入 9 月 18/19 日其他业务候选、未变更远端。

本轮 APK 尚未安装/真机付款。下一步由主任务核对文件哈希后安装 245，验证三个入口、取消返回/原单重试、支付后自动返回原订单及金额。不能提前宣称安卓支付宝已真机验收成功。支付宝完整退款链路仍缺独立验收记录，本轮未追加退款交易。微信 APP 申请及银联商户配置仍属外部依赖。
