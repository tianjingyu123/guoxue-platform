# 圈子上线前统一验收（2026-09-23）

本记录针对隔离分支 `codex/integrated-live-gift-paipan-pay-20260920`。业务代码与整套回归收口提交为 `b5feeaa2bfe2be5613615a07b086141e23c53faa`。本分支以 `receive/integrated-three-20260919 @ c0035a8f4e5c9e9bb26a0c8f0e99060a714818f2` 为已知接收基点，后有 135 项提交；不能将整条分支直接作为圈子补丁接收。通知、礼物后续评估等候选是独立分支，不能据本分支祖先关系认定已接收。`D:/gx-deploy-91` 根工作区仍在旧 `release/integration-20260801 @ 2e458597a80076ba2728e9ab2a3156bf78a3d0e3` 且有大量其他窗口未提交改动；本批没有触碰它，也不把它单独认定为当前正式发布线。

| 状态 | 事项与证据 | 对应版本 | 剩余动作 / 负责人 |
| --- | --- | --- | --- |
| 已完成 | 圈子退款申请确认金额、服务端重算拒绝过期报价、按成员行锁串行查重建单；定向测试 7/7。详见 `apps/server/src/modules/circle-refund/circle-refund-quote.spec.ts` 和《圈子板块全域审计与发布边界》。 | `1a1840e01` 起 | 发布时服务端先于移动端；发布负责人 |
| 已完成 | 圈子搜索错误恢复、探索主题真实口径、推荐重试；隔离截图 `artifacts/circle-search-20260923/search-home-390.png`。 | `a81f43f05` | 真机复核触控；移动端 QA |
| 已完成 | 当前候选 H5 隔离接口夹具下 `tests/release/circle-*-browser.mjs` 38/38 通过。旧脚本端口、公告状态和 H5 `window.uni` 假设已校正；H5 整页重进不冒充 App/小程序 `onShow`。 | `b5feeaa2b` | 在匹配包上做设备返回周期与真实入口验收；QA |
| 已完成 | 移动端类型检查、H5、App 与微信小程序正式构建通过；小程序 615 个 WXML、2683 个文本产物检查通过。2026-09-23 使用已登录的微信开发者工具 CLI 对本候选构建目录执行正式预览编译成功；平台实测总包 13,698,162 B、主包 1,908,006 B（距 2 MiB 余 189,146 B）、圈子分包 1,232,904 B、排盘分包 2,004,460 B、排盘 2 分包 2,014,580 B。尺寸摘录见 `artifacts/qa-circle-20260923/devtools-preview-size.json`。先前 1.64 MB / 1.92 MB 为本地估算，现以工具实测为准。 | `a0b4ef574` 源码；本次仅增加验收记录 | 最终整合后重新预览编译并复核包量；匹配页面加载与真机尚待验收；移动端发布负责人 |
| 尚未覆盖 | 目标两节点当前运行源码与数据库迁移。2026-09-23 12:44 UTC，`api.rebugx.cn`、`pre-api.rebugx.cn`、`gx.yrydai.com` 的公开 `/api/v1/health` 均返回 `preprod-cd055084-20260903-r1`；DNS 公网入口解析到 `43.180.212.44`。对目标两台 CVM 公网 IP 的直连 443 均超时，不能据公开健康接口认定两节点分别运行何版。 | 当前公开入口，不是本候选 | 由唯一云端操作窗口只读核对两个节点版本、固定包哈希及所连数据库和迁移表；云端运维负责人 |
| 发布阻塞 | 已知接收基点为 `c0035a8f4`，但其后有多个平行候选，最终正式发布线尚未确认；当前公开入口也未运行本候选。根工作区存在大量其他窗口改动，不可直接覆盖、stash 或整体提交。 | `a0b4ef574` | 发布负责人确定最新接收目标，对本分支 135 个后续提交按功能和等效改动核对，只接收所需差异并保留回退点；发布负责人 |
| 发布阻塞 | 付费入圈、退款执行/余额、审核、邀请回跳和权限跨账号链路缺匹配目标环境与受控账号端到端证据。38 个本地脚本均为夹具，不会证明生产资金正确性。 | 未部署候选 | 用受控测试订单及独立账号验收；不操作真实用户资金，不自动生产迁移；业务 QA / 云端运维负责人 |
| 尚未覆盖 | 微信小程序、App 真机的胶囊避让、支付后直达圈子、页面返回 `onShow`、弱网恢复和无障碍。App 构建不等于已安装签名包；小程序包量不等于已上传/发布。 | 未取得匹配设备包 | 取得匹配候选包与体验权限后设备测试并留截图/录屏；移动端 QA |
| 后续优化 | 未注册的圈子遗留页面 `distribution`、`calendar`、`analytics` 不作为在线功能；真实 AI/语音服务依各自能力门禁验收。 | 当前源码 | 需要恢复时另立真实数据、权限和多端验收；产品负责人 |

发布顺序应为：确认正式接收目标及差异 → 核对目标数据库与新接口兼容 → 构建一次固定包并记录源码、Prisma schema、客户端及包哈希 → 两节点顺序部署并逐个核对 `releaseId` → 保持既有入口做受控账号支付/退款与三端真机验收 → 达标后再决定放量。回退时按该固定包记录恢复上一版服务与静态资源；迁移若不可逆，必须先准备兼容回退方案，不能用 Git 回退代替数据回退。本文是发布准备清单，不是部署授权或上线完成证明。
