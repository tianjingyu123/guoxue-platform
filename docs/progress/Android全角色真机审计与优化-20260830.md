# Android 全角色真机审计与优化（2026-08-30）

## 1. 目标与结论边界

本轮不再以「页面能打开」作为验收，而以真实角色完成一条业务任务为最小单元。当前仍处于审计与修复阶段，未获得同版本服务端、数据库迁移、客户端和真机证据前，不宣称「全部通过」或「已达上线水平」。

## 2. 新的验收方法

每条证据必须同时绑定：

- 唯一完整 `sourceCommit`；
- 唯一 `releaseId`、服务端双节点版本与数据库迁移状态；
- 客户端版本号、APK SHA-256、包签名和运行域名；
- 设备型号、Android 版本、分辨率和验收时间；
- 账号角色、入口、操作序列、截图/XML/日志和最终状态。

审计按五层递进，任一层失败就不能以下一层的「静态测试通过」代替：

1. **导航与生命周期**：冷启动、热启动、前后台、左缘返回、系统返回、重复进入、异常恢复；
2. **真实任务链**：从入口到结果页，不仅点第一个按钮；
3. **交互完整性**：点击、竖滑、横滑、输入、弹层、键盘、焦点、空态、错误态、加载态；
4. **角色与权限**：普通用户不多权，业务角色能完成工作，未配置能力诚实禁用；
5. **弱网与失败恢复**：超时、401/403/404/5xx、重试、幂等、重复点击和进程重建。

本轮同时新增“版本边界”规则：若手机 `versionCode/sourceCommit`、公网 `releaseId` 与数据库迁移不属于同一冻结候选，测试结果只用于定位问题，不得记为上线通过。这样可以避免新客户端连旧服务端时出现套餐缺失、旧排盘回滚等伪缺陷或伪通过。

## 3. 全角色任务矩阵

| 视角 | 必过核心任务 | 关键交互/异常 | 最终证据 |
| --- | --- | --- | --- |
| 游客 | 首页、文章、古籍、课程、直播、短视频浏览 | 需登录操作精确拦截且可返回 | 公开路径不白屏、不死按钮 |
| 普通用户 | 登录、内容、收藏、评论、订单、地址 | 键盘/弹层/返回/前后台 | 普通用户端到结果页 |
| 会员/买家 | 套餐、下单、支付前确认、订单、售后 | 空套餐、库存、重复提交、支付取消 | 不扣款预发布链 + 既有小额证据 |
| 创作者 | 创作中心、作品、发布页、数据 | 权限、空作品、上传中断 | 发布前不产生真实内容 |
| 圈主 | 圈子详情、成员、待办、帖子、收入 | 空圈、审批、治理权限 | 圈主管理后台闭环 |
| 讲师 | 讲师工作台、课程、直播、收益 | 资质、上传、推流、下播恢复 | 讲师与学员双视角 |
| 直播主播 | 创建/编辑、OBS 预检、开播、评论、带货、下播 | 过期推流地址、断流、回调、前后台 | 媒体在线+回调+Redis+DB |
| 商家 | 商品、库存、订单、发货、退款 | 越权、伪库存、重复发货 | 商家与买家同单据闭环 |
| 站长/运营商/线下驿站 | 业务看板、归因、履约、分润 | 旧排盘映射失败不影响主业务 | 三种角色各自独立证据 |
| 研究院成员/院长 | 成员端、管理端、内容与课程 | 双角色切换与越权 | 成员/院长各一条闭环 |
| 内容/商品审核 | 待审、详情、通过/拒绝前确认 | 空队列、并发审核、审计日志 | 不执行真实审批的预发布路径 |
| 财务 | 对账、退款审核、流水 | 金额与币种、重复回调、权限 | 历史小额证据+无真扣款路径 |
| 客服 | 用户、工单、会话、售后协同 | 敏感信息脱敏、不越权处置 | 客服工作台闭环 |
| 超管/运营管理员 | 运行看板、开关、内容、用户、风险 | 变更前确认、审计记录、回滚 | 只读预发布证据，不擅自改配置 |

## 4. 当前 v232 重审计已得结论

| 项目 | 真机结果 | 归因/处理 |
| --- | --- | --- |
| 短视频列表→沉浸式播放器→上下切换→评论面板 | 通过 | 已纠正“列表滚动等于视频切换”的旧验收误判；评论面板只打开未发送 |
| 工具主入口→八字排盘 | 失败且已得决定性根因 | 真机同时存在底层原生页与上层旧排盘 WebView，旧页按钮无响应并遮蔽底部导航 |
| 排盘运行模式 | 用 v233 原生隔离包完成了根因诊断，但该语义不进入正式候选 | 后续产品指令明确正式 legacy 面向普通用户，新排盘只做隔离 QA；正式修复必须落在旧排盘容器/路由/返回链 |
| 商城商品→SKU→确认订单 | 通过，未提交订单 | 发现详情与 SKU 库存不一致，已统一使用实时 `currentStock` |
| 会员购买 | v232 真机被「会员套餐暂未配置」诚实阻断 | 不是前端按钮故障；新迁移已含幂等套餐初始化，当前预发布尚未同版部署/迁移 |
| 文章/帖子评论按钮 | 真机复现「看似无响应」 | 旧实现只滚到评论区；已改为定位并聚焦真输入框 |
| 标准左缘返回/系统 Back | 商城结算页通过 | 之前跨全屏过长模拟滑动是测试输入伪差，不计产品缺陷 |
| 课程列表→详情→文字课 | 通过 | 同版候选需再复测 |
| 古籍列表→详情→阅读器→滚动 | 通过 | 内容与工具栏正常 |
| 听书播放/暂停/后台继续/前台恢复 | 通过 | 进度从第 2 句前进到第 5 句，路由保持 |
| 智能体广场→助手详情→输入框/键盘/返回 | 通过，未发送消息 | 无真实对话副作用 |
| 圈子→圈子详情→圈主管理后台 | 通过 | 成员/收入/待办/起步任务正常展示 |
| 创作中心、我的直播、编辑直播、帮助中心、设置 | 通过 | 未发布、未开播、未改配置 |

## 5. 进入上线候选的硬条件

- 客户端、服务端、数据库迁移和公网 `releaseId` 必须对齐到同一冻结提交；
- 普通用户核心链与 15 类 QA 角色各至少一条核心任务真机通过；
- 所有高频可点元素均有可见反馈，不存在死按钮、假成功或无返回路径；
- 五个主导航连续切换、排盘工具连续切换、短视频竖滑、支付前确认、评论输入、前后台恢复均通过；
- 无白屏、无状态栏/挖孔遮挡、无未处理 JavaScript 异常；
- 支付、评论、开播、发布、审批等外部副作操作仅在明确的动作级授权下执行，不用伪数据代替结果。

## 6. v233 原生排盘隔离诊断包真机复验

本节只记录 `1.1.0(233)` 本地签名诊断 APK 的实机结果。该客户端仍连接旧预发布服务端，并临时隔离了旧排盘，用于判断故障边界；后续更晚的正式产品指令要求普通用户在 `PAIPAN_MODE=legacy` 时统一进入旧排盘，因此本节的原生排盘通过项只能证明平台原生工具本身可用，不能作为正式运营排盘口径或最终验收。

| 任务链 | v233 真机结果 | 证据/边界 |
| --- | --- | --- |
| 冷启动、后台恢复、强停后冷启动 | 通过 | 三种启动均回到完整首页，无白屏、无历史排盘页重放 |
| 五主导航连续两轮切换 | 通过 | 首页、圈子、工具、发现、我的十次连续切换均完整渲染 |
| 工具主入口 | 诊断通过 | 隔离旧排盘后只渲染平台原生工具页，证明重叠 WebView 是故障边界；正式 legacy 仍未通过 |
| 八字、大六壬、奇门、姓名、八宅 | 通过 | 五种不同工具/分包均进入真实原生表单；未提交排盘 |
| 工具页系统 Back 与左缘返回 | 通过 | 均返回同一原生工具网格并保留展开和滚动位置 |
| 全部排盘工具路由 | 静态全量通过 | 新门禁逐项解析工具配置，37 个工具均必须映射到真实 `.vue` 页面；开发中条目只能进入统一说明页 |
| 短视频列表→沉浸播放器→上滑下一条 | 通过 | 已验证的是播放器内视频切换，不再把列表滚动误判为通过 |
| 短视频评论 | 通过 | 评论图标打开评论抽屉；未输入、未发送、未触碰其他互动状态 |
| 文章输入胶囊与评论图标 | 通过 | 两个入口均定位评论区并聚焦真输入框、弹出键盘；未发送评论 |
| 商城→商品→SKU→确认订单 | 通过 | 实时库存、地址、金额和支付方式可见；停在“提交订单”前，未建单、未支付 |
| 古籍馆→详情→阅读器 | 通过 | 正文、目录/书签/笔记/设置工具栏和滚动正常 |
| 课程→详情→第一讲 | 通过 | 图文课正常渲染，返回链正常 |
| 智能体广场→助手详情 | 通过 | 助手介绍、剩余次数、建议问题和输入区正常；未发送消息 |
| 圈子→圈主后台 | 通过 | 成员、收入、待办和起步任务正常，未执行邀请/发布/审批 |
| 我的直播→主播房间列表 | 通过 | 待开播房间与编辑/开播入口正常；未点击开播 |
| 创作中心 | 通过 | 数据、收益、设置、发布入口及真实空态正常；未发布内容 |
| 运行日志 | 通过 | 本轮旅程无未处理 JavaScript、白屏、进程崩溃；仅见 OnePlus 系统 NFC 路由噪声，与应用无关 |
| 会员开通 | 阻断 | 旧预发布数据库未初始化套餐；须随统一候选执行幂等套餐迁移后再测支付前链路 |

## 7. 本轮对“旧排盘是否为根因”的结论与正式口径

结论不是猜测：当前旧排盘接入容器/路由是排盘工具点击无响应、底部导航消失和返回链异常的直接主因。v232 真机同时存在底层原生工具页和上层第三方 WebView；v233 临时隔离旧排盘后，相同设备、账号与服务端下五个代表工具及两种返回手势立即恢复。这一对照实验定位了故障，但不能用“切回原生”规避正式业务要求。

但不能把所有问题都归给旧排盘：

- 会员购买被阻断是预发布数据库套餐未迁移；
- 短视频此前“无法上下滑”包含旧验收方法错误，必须在沉浸播放器内验证；
- 评论“无反应”是缺少定位加输入聚焦的可见反馈问题；
- 最终上线仍需客户端、双节点服务端、数据库迁移和公网制品统一到同一冻结提交。

更晚的用户产品指令固定为：`PAIPAN_MODE=legacy` 时普通用户所有排盘入口统一进入旧排盘，新排盘只对隔离 QA 开放；`PAIPAN_MODE=native` 时才恢复平台原生排盘。因此正式整改必须修复旧排盘承接容器、同源/跨域导航、工具点击、返回链、底部导航承接和异常态，且第三方失败不能泄露或自动回退新排盘。此前“主导航工具强制原生”的未提交方案已撤回，不进入统一候选。

## 8. 审计方法纠偏与新增真机发现

本轮出现过一次测试输入误判：创作中心“收益/设置”最初被记录为点击无响应，复核后发现是截图缩放坐标被错误当作设备原始坐标。改用设备原始 1080×2400 坐标并在每一步读取新截图后，两页均正常打开。该项不是产品缺陷，旧结论已撤销。由此新增以下强制规则：

- 不允许用缩放后的截图坐标直接驱动真机；
- 每次关键点击后必须读取新的页面标题、截图或 UI 层级，不能只按等待时间推断成功；
- 一个按钮失败必须至少用“可访问元素定位/原始坐标/手工复核”中的两种独立方式确认；
- 所有测试按“初始状态—动作—可见反馈—最终状态—是否产生副作用”记录，不能只记录点击动作。

新方法随后发现一个此前遗漏的真实缺陷：个人中心向下滚动时，“我的订单”等正文会进入 Android 时间、挖孔、信号和电量区域。该问题与旧排盘无关，是主导航页面缺少稳定状态栏隔离层。已在 v234 源码增加固定状态栏遮罩，并对发现页搜索栏增加吸顶与安全区隔离；同时新增静态回归，防止滚动内容穿透顶部安全区。该修复已通过构建和自动门禁，但尚未生成并安装签名 v234 APK，因此暂不能记为真机通过。

继续沿买家订单旅程进入“智能客服”后，又发现共享聊天组件顶部导航与 Android 状态栏重叠。原有全局门禁把页面引用 `SimpleChat` 当成了安全区证据，却没有验证组件内部的 `--status-bar-height` 是否真的被运行时赋值，属于“门禁假绿”。现已让共享 `SimpleChat` 从真机 `statusBarHeight/safeAreaInsets/safeArea` 取最大值，并保留 H5 `env(safe-area-inset-top)` 兜底；门禁也改为直接审计组件实现。这一处修复会同时覆盖智能客服和复用该组件的智能体聊天页，仍须随签名 v234 APK 做真机像素复验。

## 9. 额外角色旅程与副作用边界

| 角色/任务 | 真机结果 | 副作用控制 |
| --- | --- | --- |
| 创作者→创作中心→收益/设置 | 通过 | 未提现、未保存设置；此前“无响应”为坐标误判 |
| 买家→我的订单→待收货订单详情 | 通过 | 订单状态、商品、价格、物流、客服、投诉和售后入口均可见 |
| 买家→订单智能客服 | 功能入口通过、顶部安全区失败且已修源码 | 客服欢迎语、快捷问题和输入框可见；未发送；v233 标题与状态栏重叠，待 v234 复验 |
| 买家→申请售后表单 | 通过到提交前 | 原因、金额、说明、凭证上传入口与提交按钮完整；未填写、未提交售后 |
| 买家→申请换货 | 待同版候选复测 | 本轮未执行会产生业务状态的提交动作 |
| 买家→确认收货 | 未执行 | 属于不可逆业务动作，必须动作级授权 |

本轮最后已用系统 Back 从售后表单安全返回订单详情，未创建退款/售后记录、未确认收货、未支付、未发送评论或消息。

## 10. v234 当前门禁状态

当前客户端源码版本为 `1.1.0(234)`，修复集合包括：个人中心滚动安全区、发现页吸顶安全区、SimpleChat 共享导航安全区，以及 37 项隔离 QA 原生工具路由校验和相应发布回归。正式排盘仍遵循服务端运行时 legacy/native 模式，不包含“普通用户强制原生”的语义变更。当前证据如下：

- `release:test-mobile-native-bundle`：113/113；
- 导航审计：441 个注册页面、299 个字面跳转，0 错误；
- mobile type-check：通过；
- migration audit：7/7；
- strict release audit：P0=0、P1=0，P2=11 均为明确标注的暂未开放能力；
- App 本地预发布构建：通过；
- 签名 v234 APK 与真机安装：尚未完成。

因此现阶段只能确认短视频、评论、商城、课程、古籍、智能体、圈子、直播入口和创作者核心交互在 v233 诊断包可用；正式 legacy 排盘仍未通过，不能列入恢复项。v234 的顶部安全区修复还需使用既有正式 Android 签名链生成候选、覆盖安装到同一 OnePlus 9R 后，复测个人中心/发现页/SimpleChat 长滚动、五主导航、前后台和强停冷启动；旧排盘则必须另做正式模式真机闭环。

## 11. 最终上线验收不再接受的替代证据

- 不能用“路由存在”替代按钮可点与任务完成；
- 不能用列表滚动替代沉浸视频切换；
- 不能用 H5/模拟器替代 App 真机状态栏、系统返回和生命周期；
- 不能用静态门禁替代签名候选覆盖安装；
- 不能用新客户端连接旧服务端的结果判定最终业务通过；
- 不能把未执行的支付、发布、开播、审批或退款写成通过；
- 不能以单一管理员账号替代游客、普通用户、买家、创作者、圈主、讲师、主播、商家、客服和运营角色。

下一次“可上线”结论必须绑定同一个客户端 `sourceCommit`、APK SHA-256、双节点 `releaseId`、数据库迁移状态和完整角色旅程证据；任何一项漂移即退回定位状态，而不是继续补口头说明。
