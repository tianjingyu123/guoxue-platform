# B88-B90 上线收口记录（2026-07-23·Codex）

> 工作树：`D:\gx-deploy-91`
> 冻结基线：`5ef8e3935037d025dd1c98eae1b28c386b36a675`（detached HEAD）
> 本轮未提交、未部署、未写生产业务数据、未触发真实短信/登录/支付/物流。

## 一、环境与验收边界

- 2026-07-23 重启加载最新配置后，普通权限 `git status`、应用内浏览器控制、本地截图读取与视觉分析均已通过。
- 本轮 PowerShell、浏览器控制和截图链路均未再出现 `SetTokenInformation(TokenDefaultDacl) failed: 1344`。
- 已使用应用内浏览器对本地 H5 真页面截图验收；未使用源码阅读、类型检查或构建结果冒充视觉验收。
- 课程页通过本机只读转发访问生产公开课程接口；未登录订单页使用同结构临时压力数据验收，入口在截图后已完整移除。
- 未执行生产写操作、真实短信、登录、支付、退款、发货或物流订阅。

## 二、B88 外部渠道验收

| 渠道     | 配置就绪                                                | 接口就绪                                           | 真实冒烟       | 待办                                            |
| -------- | ------------------------------------------------------- | -------------------------------------------------- | -------------- | ----------------------------------------------- |
| 微信登录 | 已补公众号/小程序历史环境变量兼容；后台热更新可即时读取 | 登录服务与健康检查通过定向测试                     | 未执行         | 需真实微信授权；若出现扫码/手机确认再由用户参与 |
| 微信支付 | 生产健康检查 `payment=ok`；现网支付由汇付子通道承接     | 沿用 B82-B87 已验支付链                            | 本轮未重复支付 | 不重复已完成的真实支付宝子通道付款/退款         |
| 短信     | 生产健康检查 `sms=ok`；别名兼容已补                     | 认证与健康检查测试通过                             | 未发送         | 需指定接收手机号后再做真实签名发送和验证码登录  |
| 快递100  | 查询、订阅 URL、回调 URL、salt 配置位已接通             | 查询、订阅、501 幂等、回调验签、轨迹持久化均有测试 | 未使用真实运单 | 需现有可追踪测试单号；不得伪造渠道成功          |

快递100专项修正：

- 发货/改物流单号后非阻断发起订阅。
- 回调按 `MD5(param + salt)` 大写验签，原始 `param` 不被全局清洗转义。
- 官方重复订阅返回码 `501` 视为已订阅。
- 状态 `3` 或 `ischeck=1` 持久化为 `SIGNED`；状态 `5`（派送中）保持 `IN_TRANSIT`，不再误写为已送达。
- 回调成功严格返回 `{ result: true, returnCode: "200", message: "成功" }`。

## 三、移动端 UI 调整

- 智能体首页：最近使用卡片固定 `144rpx`（浏览器实测约 72px），长回复压平为两行摘要，横向续聊带可滑动且不产生页面级横向溢出。
- 紫微斗数：恢复宫位关系阅读。H5/App 首次点宫显示 SVG 连线与关系明细、再次点同一宫进入详情；微信小程序通过条件编译排除 SVG，保留同一套宫位高亮与文字明细。
- 七政四余：命盘视口响应式适配，支持小屏滚动与居中缩放。
- 订单列表：调整为现代中式账簿视觉，增加状态印章、待办计数、统一标签与卡片层级。
- 课程卡片：收紧信息密度并改善价格、讲师区域的栅格关系。
- 发现页课程卡：保持双列宽度、3:4 内容容器和标题区纵向尺寸不变，以封面渐隐、宣纸上叠、连续纸色和装帧书脊线消除首图/信息/标题三段割裂。

应用内浏览器视觉验收：

| 页面 | 实测结果 | 截图 |
| ---- | -------- | ---- |
| 智能体最近使用 | 三条长历史回复下卡片等高 72px、摘要两行、横滑正常 | `C:\Users\Administrator\agent-recent-long-conversation-fixed.png` |
| 紫微斗数 | 真实盘面点选福德宫：三方四正显示 3 条朱红虚线；宫干飞化显示禄/权/科/忌 4 色实线；再次点宫正常打开详情 | `C:\Users\Administrator\ziwei-lines-sanhe.png`、`C:\Users\Administrator\ziwei-lines-feihua.png` |
| 七政四余 | 480px/375px 下 100% 圆心偏差均为 0px；150% 放大后画布仍以圆心为视点 | `C:\Users\Administrator\qizheng-centered-100.png`、`C:\Users\Administrator\qizheng-centered-150.png`、`C:\Users\Administrator\qizheng-centered-375.png` |
| 订单列表 | 长商品名、双商品、金额、状态与操作区均未溢出 | `C:\Users\Administrator\ui-qa-order-ledger.png` |
| 课程卡片 | 生产公开课程数据下排行、横滑卡与价格栅格正常 | `C:\Users\Administrator\ui-qa-course-cards.png` |
| 发现页课程卡 | 生产公开课程数据下，480px 单卡保持 224×338.45px、3:4 区为 224×298.66px；375px 双列无横向溢出，封面与两级信息区视觉连续 | `C:\Users\Administrator\course-card-unified-480.png`、`C:\Users\Administrator\course-card-unified-375.png` |

工程验证：相关文件 `git diff --check` 通过；定向 ESLint 0 error（仅项目既有格式 warning）；移动端类型检查通过；H5 和微信小程序正式构建通过。小程序产物 WXML 不含 SVG 关系层，主包 1.79 MB。浏览器控制台无页面运行错误，仅有项目既有 `vue-router` 弃用 warning。

2026-07-23 用户在生产页实测指出七政盘面偏左。生产仍是旧版：全宽 `scroll-view` 内的 340px canvas 从左起排。工作区修复版已将视口显式居中，并把放大后的滚动定位延后到 canvas 扩宽与绘制完成之后；上述三组浏览器测量与截图均基于用户同款 1992-08-16 参数。此修复尚未部署生产。

## 四、B89 发布包验收

| 产物       | 文件数 |   总字节 | 关键文件 SHA256                                                                     |
| ---------- | -----: | -------: | ----------------------------------------------------------------------------------- |
| Server     |   2947 | 22982719 | `main.js`：`DD6731F950CA79514B438442BB23B7F947E733A789D54B9789D47F13A930E4F0`       |
| Admin      |    483 |  6631649 | `index.html`：`71CE8804CC9CBBB27DD3D597B15E838359A038D4E46FB82F064873E6F70B3D6A`    |
| H5         |   1093 | 11522566 | `index.html`：`E1150401987D4A975B165626F15C3B9CEFF1684E028CF566421A4D873D26C695`    |
| 微信小程序 |   2468 | 11530869 | `app.json`：`3C94E97138B3426E4763AA41CF6A428A4D9A3FE7CF10DBF7E169304A495AF212`      |
| App        |    459 | 12706840 | `manifest.json`：`07C60C55A33D2826E1E3E25DCED500ACC5408C79AED5F4F40975D8C1AFD3A5A9` |

构建结论：

- Server、Admin、H5、微信小程序、App 五端正式构建均成功。
- App 已启用 DCloud 官方 `app-plus.optimization.codeSplitting`，解决代码分割与 IIFE 输出冲突。
- 微信小程序主包约 1.79 MB，最大分包约 1.90 MB，未超过 2 MB 单包限制。
- Admin 构建仍有 `js-sha1` 的 `eval` 与大 chunk 警告，不是本轮新增阻断。

提审前阻断：

- `manifest.json` 顶层 DCloud `appid` 仍为空。
- App 版本仍为初始 `0.1.0 / 100`，未找到董事长批准的正式版本号。
- 不得臆造 DCloud AppID 或发布版本；需正式值后再冻结提审包。

## 五、B90 只读生产巡检

2026-07-23 本轮复核：

- `https://api.rebugx.cn/h5/`：HTTP 200。
- `https://api.rebugx.cn/admin/`：HTTP 200。
- `/api/v1/health`：HTTP 200，`sms/live/vod/db/redis/cloud/im/payment/storage/ai` 全部 `ok`。
- `/api/v1/health/ready`：HTTP 200。
- `/api/v1/health/live`：HTTP 200。

本次仅 GET 探针，没有登录、写配置或写业务数据。

## 六、验证记录

- 后端全量 Jest：452 suites、6176 tests，四分片均以退出码 0 全部通过；定向回归 7 suites、135 tests 亦通过。
- 后端正式 build：通过。
- 移动端类型检查：通过。
- H5、Admin、微信小程序、App 正式 build：通过。
- `git diff --check`：通过。
- 2026-07-23 补验：五个移动端 UI 场景已在应用内浏览器完成真实截图验收，临时测试入口已移除。
- 2026-07-23 紫微补验：H5/App 宫位关系连线恢复；微信小程序无 SVG 兼容构建通过；真实盘面三合、飞化、二次点宫详情交互均通过。
- 未跟踪构建产物进入 Git 差异。

## 七、冻结、回滚与发布闸门

当前不是可直接发布的冻结提交：HEAD 仍在生产基线，工作树含本轮未提交改动。

本轮没有 Prisma schema、迁移或生产 DDL；回滚以代码/静态产物原子切回为主，不需要数据库回滚。正式发布时必须：

1. 补齐 UI 真截图验收、真实外部渠道待验项和 DCloud AppID/正式版本号。
2. 复核全部差异后建立独立提交与发布标签，记录提交 SHA 和五端最终产物哈希。
3. 部署前核对生产 HEAD、PM2 和现有产物，保留 `dist.prev-<sha>`。
4. 两段式原子交换 Server/H5/Admin；任一探针失败立即切回上一版。
5. 部署后复核 PM2 4/4、health/ready/live、H5/Admin 入口与线上产物哈希。

在用户明确授权提交和部署前，本轮停留在“本地实现与验收材料就绪”。

## 八、智能推荐开关收口

2026-07-23 已完成交接遗留的推荐开关错位修正：

- 唯一正式运行时契约恢复为生产长期使用的 `recommend_algorithm`。
- `RecommendController` 三个门禁端点改为引用同一常量，避免字符串再次漂移。
- Prisma 种子和移动端数据层说明同步为 `recommend_algorithm`。
- 控制器测试增加门禁元数据断言，定向 Jest 1 suite / 10 tests 全部通过。
- Server 定向 ESLint 0 error（仅测试文件既有 `any` warning）；Server 正式 build 通过。

生产当前同时存在 `recommend_algorithm` 与 7 月 18 日止血补建的 `recommend_engine`。本轮未写生产配置，也未删除冗余键。正式上线顺序必须为：

1. 先部署使用 `recommend_algorithm` 的新版 Server。
2. 只读验证三个推荐端点与推荐区块正常。
3. 经明确授权后再删除生产冗余键 `recommend_engine`；删除前后均保留开关快照。

## 九、圈帖不出圈与文章开放范围收口

2026-07-23 根据发现页生产实测完成本地修正：

- 发现页删除“圈内精华”公共分区；数据层拒绝历史 `post` 分类请求和异常返回的帖子卡。
- Smart Feed、旧首页 Feed、搜索、推荐场景、小程序首页和分站公共精选统一要求文章同时满足 `auditStatus=APPROVED` 与 `visibility=PLATFORM`。
- Smart Feed 统一出口额外过滤 `post`，可拦截个性化策略和旧缓存残留。
- `/circles/hot-posts` 与 `/circles/activities` 保留旧客户端接口兼容，但不再查询或返回跨圈帖子。
- 圈内帖子列表、圈内详情、发帖、互动和圈主治理链路未改动。

验证：

- 定向 Jest：2 suites / 17 tests 全部通过。
- Server 正式 build、移动端 `vue-tsc --noEmit` 均通过。
- 应用内浏览器接只读生产数据实测：公开卡片 10 张、`post` 卡 0、`圈内精华` 分区 0、横向溢出 0、控制台 error 0。
- 截图：`C:\Users\Administrator\discover-no-circle-posts-viewport-20260723.png`。

本修正尚未提交或部署生产；生产页面仍需待正式发布后复验。

2026-07-23 发现页入口进一步收口：

- 删除底部“事业与合作”折叠区及其脚本、模板、专属样式。
- 商家、分站、研究院、线下驿站等身份入口统一由个人中心“身份切换”区域承载。
- 移动端类型检查通过；应用内浏览器接只读生产数据验收，重复区块节点 0、圈帖卡 0、横向溢出 0、控制台 error 0。
- 截图：`C:\Users\Administrator\discover-without-business-section-20260723.png`。
