# 圈子咨询订单完整浏览（2026-09-22）

问题：原咨询订单页固定取前 100 条图文提问；通话接口固定返回前 50 条，读取失败会被当成没有通话。页面仍写“累计消费”“咨询总笔数”，容易把截断后的数据误认为全量。

本批为通话增加只读 `/consult-calls/my-page`，按登录用户作为主叫或达人过滤，稳定排序，限制页大小；保留旧 `/consult-calls/my` 响应以兼容其他调用。咨询订单页的图文和通话分别分页、分别显示失败与重试；单一来源失败仍可浏览另一来源。当前页未取全时只称“已显示的已付及托管/已显示笔数”，两种记录都取完才显示全量标签。圈内查询对通话按圈过滤，但分页进度使用服务端已取条数，避免本圈条数少时反复请求。这里展示的是咨询金币金额，不是已结算净收入，真实账务仍以后端为准。

验证：移动端类型检查、服务端构建通过；通话服务 20/20 单测通过，新增验证本人过滤、总数、稳定排序及页码限制；本地浏览器模拟 23 条图文和 22 条通话，通过读取失败恢复、完整分页及总数标签检查，无写请求。微信小程序构建、WXML 和包体积校验通过：主包 1.63 MB，保守上传口径 1.91 MB，圈子分包 0.98 MB。未调用真实付费接口、未部署。

发布依赖：移动端使用新 `/consult-calls/my-page`，须与服务端同批部署。真实登录态、网络中断及大量历史数据需目标环境再核验。
