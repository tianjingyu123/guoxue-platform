# 排盘上线收口与发布交接（2026-09-11）

## 本轮完成的本地修复

- 新系统登录是普通排盘准入条件，无手机号不再直接拒绝。
- 微信 H5 工具明确请求 client=h5，由服务端返回已确认的 .com/p1.php 网页入口；绑定手机号不再误选 App 专用接口。分站仍使用其站长 ruid，不给 App 地址发明推荐参数。
- 排盘触发的微信内登录页优先展示微信快捷登录，保留验证码/密码切换及协议确认。首次微信登录允许按现有协议创建新用户。
- 登录回到合法原路径并保留参数；交接过期、刷新、重新登录仍请求原分站或个人中心；不同分站不串用短期地址。
- 保留加载态，撤除入口页直接整页外跳的分支，交给原兼容页承载。不把加载期间误显示成不可用。

## 本地验证

50 项前端检查、8 项后端检查、H5 构建通过。后端构建最初被本机借用另一工作树 node_modules 的算法包路径及类型搜索路径阻断；构建本工作树算法包，并用 artifacts 内临时配置显式映射本地声明和已有类型目录后，Nest 编译退出0。未修改共享依赖目录或掩盖业务类型错误；服务器干净环境构建仍须在候选阶段验证。

## 发布边界

当前代码提交与补丁 SHA 见 artifacts/paipan-launch-handoff-20260911/manifest.json。补丁以 4ccccf5e 为基线，仅用于整合，尚未部署。对当前统一发布源码先做差异检查；冲突须逐项合并，不整文件覆盖 App 最新分享、支付、AI、退款等修复。

协助任务已确认用户另行选择 gx.yrydai.com 为未来新系统 H5 用户端域名；它不是第三方新排盘接口。DNS已生效，但证书、CLB规则和业务入口尚未启用。此状态来自协助任务，不代表本任务已发布。最终 H5 应在统一域名配置确认后重建，不发布这里用于本地验证的旧域名构建产物。

两节点实时镜像及首页摘要尚待一次 TAT 只读核验，脚本为 artifacts/paipan-launch-handoff-20260911/两节点发布前只读基线.sh，命令类型 SHELL，超时60秒，两台北京新节点执行。脚本不修改配置、不重启、不调用第三方账户或支付。公网单次响应不替代双节点核验；脚本无法证明不存在其他人在手动发布，需发布前协调。

## 仍需真机验收的基础流程

部署后同一轮验证：微信新系统游客登录回排盘；已登录无手机号进入；站长来源登录/重试后保留；八字/奇门工具可操作；返回热卜；旧支付取消返回。当前本地自动检查不等于这些流程已在候选版本真机通过。

## 不阻断整个平台上线的增强项

- 始终留在热卜域名的完整 H5 内嵌。
- 接收人切盘后再次分享当前结果、旧页内部首页/返回/分享全部由热卜接管。

现有 H5 保底承接页仍需用户打开第三方页面，并依赖浏览器返回，不能声称已经达成完整壳内闭环。此限制必须明确列入上线说明，不通过隐藏入口故障冒充通过。

## 必须第三方配合时的具体最小范围

先由我方完成公开页面、既有桥和实际版本检查。如确实没有网页接口，由第三方在其控制的H5公共页面允许加载我方适配脚本，并提供/保留来源标识；支持公开当前盘面变化、首页、返回、分享消息。若其授权无法在嵌入中完成，再由它确认安全授权回跳契约。无需整套源码；不让它回答MDN可查的浏览器基础问题。上述增强与整体上线解耦，不自行代理旧站登录或支付。
