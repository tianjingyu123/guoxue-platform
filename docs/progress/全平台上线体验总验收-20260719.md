# 热卜国学全平台上线体验总验收（2026-07-19）

> 目标：不扩张高级功能边界，优先把已存在功能做到可理解、可完成、可信、顺滑、统一，并以真人路径、正式构建和生产实测作为验收依据。

## 一、冻结基线

- 接管工作树：`D:\gx-deploy-91`（detached HEAD）
- 本轮起点：`52a8acdb`（`prod-20260719-selfhost-ai`）
- 当前生产基线：`42b61569`（`prod-20260719-guest-browse`；H5 `index--GAkVArJ.js`；服务端仍为 `b0fcbb85`；PM2 4/4 online；外网 health 200）
- 用户已有改动：工作树接管时为 clean；只精确提交本轮授权文件
- 生产数据铁律：不写生产业务数据；联调/演示内容通过代码级精确隔离，不做模糊误伤
- 资金/权限铁律：每处双查；本轮体验改造不改变资金口径、审核状态机和角色授权

## 二、规模与覆盖范围

- 移动端注册页面：471
- 移动端 Vue 页面/组件：630
- 管理后台 Vue 页面：262
- 服务端 Controller：168
- 移动端数据模块：96

角色路径覆盖：游客、注册用户、会员、内容创作者/圈主、从业者、商家、分站站长、运营商、研究院、线下驿站、平台运营与管理员。

## 三、统一验收标准

每条核心路径按以下九项打分，P0/P1 未闭合不得宣称“完美”或“可上线”：

1. 入口可发现：命名与图标能让普通用户理解下一步。
2. 任务可完成：无死链、假按钮、重复提交、无法返回和上下文丢失。
3. 状态可感知：加载、空、失败、成功、禁用、处理中均有明确反馈。
4. 数据可信：不展示联调数据、硬编码统计、错误口径或虚假承诺。
5. 视觉一致：层级、间距、字阶、圆角、颜色、卡片密度和底部安全区统一。
6. 触控友好：热区不小于 44px；点击有反馈；关键操作防误触。
7. 弱网可恢复：超时可重试，已有数据不因刷新失败被清空，AI/三方服务 fail-open。
8. 跨端可用：H5、小程序/App 降级契约明确；窄屏、刘海屏、键盘弹出不遮挡。
9. 发布可验证：`vue-tsc`、`tsc`、相关 Jest、正式 build、生产 hash/API/页面实测齐全。

## 四、执行顺序

### A. C 端首屏与高频路径

- 首页、发现、圈子、工具、我的五个主导航
- 登录/注册/找回、搜索、内容详情、评论收藏分享
- 排盘创建/结果/保存/AI 解读、古籍伴读、智能体会话
- 课程学习、直播、商城下单、支付、订单、物流、售后

### B. B 端经营路径

- 商家：入驻、商品、采购/库存、订单、发货物流、结算、客户
- 圈主/达人：建圈、内容、咨询、会员、数据、收益
- 分站/运营商：开通续费、选品展示、归属、客户、佣金
- 从业者：会员、报告、案例、日程、客户、记账
- 研究院/驿站：准入、上架、核销、经营数据、结算

### C. 管理后台

- 登录与权限、审核、内容、用户、交易、商家、运营配置
- 列表筛选/分页/批量操作、详情返回、表单校验、危险动作确认
- 空态/错误态、统计口径、导入导出、生产开关与审计日志

### D. 横向质量

- 全局滚动、导航、安全区、弹层、Toast、表单键盘、图片失败兜底
- 统一卡片/按钮/字阶/状态色；减少重复装饰和信息噪音
- 可访问性语义、减少动画偏好、焦点可见、文字对比度
- 首屏体积、重复请求、缓存陈旧、列表长任务与图片性能

## 五、问题台账

| 编号 | 级别 | 现象 | 证据/根因 | 处理状态 |
|---|---|---|---|---|
| UX-001 | P1 | H5 右侧原生粗滚动条挤占内容，首页/发现均明显 | `body` 与 uni scroll 容器未统一隐藏 scrollbar | 已修 `97fcd354` |
| DATA-001 | P0 | 发现页公开展示“演示视频（播放器联调）”与测试用户 | `seed-demo-video.js` 的 `demo-video-*` 被正式推荐查询返回 | 已精确隔离 `97fcd354` |
| DATA-002 | P0 | 公开列表混入后台联调商品/课程/视频/圈子 | 生产只读审计确认 4 商品+8 课程+3 视频+1 圈子为 admin 联调记录；用户自建低质内容不得擅自清理 | admin 联调记录已按精确 ID 全链隔离 `7d13f655`；用户内容留后台审核治理 |
| UX-002 | P1 | 首页顶部首要任务不明确，孤立搜索图标削弱层级 | 品牌大字按既有拍板不回加，改用全宽统一搜索承接首要任务 | 已修（本批），真数据截图复核通过 |
| UX-003 | P1 | 发现页信息密度过高、首屏缺少主次 | 每类首屏 6→4，保留“查看更多”渐进展开；宫格/分区节奏收敛 | 已修（本批），真数据截图复核通过 |
| BUILD-001 | P1 | H5 build 报多处 ASI/unreachable 警告 | `return` 后遗留 H5/非 H5 分支代码，编译器明确提示不可达 | 已修 `4b570bec`，8 处告警归零 |
| BUILD-002 | P0 | 微信小程序主包约 2.00MB，超过项目 1.9MB 发布警戒线 | 大量仅单分包使用的数据模块/组件被打进主包，Logo 原图尺寸过大 | 已修 `d62df55d`，主包降至 1.83MB，门禁 EXIT 0 |
| BUILD-003 | P1 | 小程序构建存在埋点循环依赖及 H5 标签样式跨端告警 | `useTrack -> request -> useTrack`；H5 rich-text 的 `p` 选择器被小程序编译 | 已修 `c20f3acc`，双端正式构建告警归零 |
| API-001 | P1 | 首页/发现无运营楼层时每次请求 `pages/by-route` 均返回 404 | 前端契约声明“无页面返回空”，服务端却抛 NOT_FOUND，污染 api_error | 已修 `b0fcbb85`，3 条专项回归通过 |
| AUTH-001 | P0 | 游客进入圈子、搜索、商城、智能体广场会被私有增强接口 401 劫持到登录页 | 公共页面无条件拉“我的圈子/历史/购物车/最近会话”；catch 晚于统一 401 跳转 | 已修 `42b61569`，4 页无 token 黑盒通过 |
| AUTH-002 | P0 | 游客可在商城公开接口传 `status=ALL/PENDING` 查看非公开商品状态 | `GET /shop/products` 未做身份解析，服务层把显式状态当管理工作队列 | 已修 `7d13f655`；游客参数强制清空，管理员 ALL 保留 |
| TRUST-001 | P0 | 研究院公开宣称“交钱即进、可退押金”，实际无订单、收款、收入写入或退款出款 | 前端模拟支付；`InstituteMember.deposit` 名义金额被误当已缴金额与可退依据 | 已修 `9caa773c`；统一为免费提交申请，当前不扣款 |
| FUND-001 | P0 | 任务完成即可把会籍写成“已退款”，且分红审批可绕过真实收入余额 | 退款接口只改状态；分红请求未核对 `InstituteRevenue` 与已发放额 | 已修 `9caa773c`；无真实订单拒绝退款，分红仅限真实入账留存 |
| AUTH-003 | P1 | 游客打开公开“讲师遴选”机制页会因私有会籍接口 401 被跳到登录页 | 页面无条件调用 `/institute/my` | 已修 `9caa773c`；游客不请求私有会籍数据，选席位时再友好提示登录 |
| TRUST-002 | P0 | 平台公告列表与详情整页硬编码，时间按当前时刻伪造，浏览量/附件/分享均为假 | C 端未接后台 `SiteNotice`；任意 ID 详情固定展示同一篇 2025 公告 | 已修 `95dbee7c` + `d8063c43`；只读真源、时间窗、真实详情与诚实空态 |
| API-002 | P1 | 首次生产验收发现公告分页被统一响应器拆成 `data + pagination`，前端误按对象读取 | 新数据层误用 `apiGet`，未沿用项目 `apiGetPaged` 契约 | 已修 `d8063c43`，外网真实空数组黑盒通过 |
| TRUST-003 | P0 | 生产赛事中心把固定演示赛事、测试参赛者、虚构奖金与过期赛程当成真实赛事公开，2024 大赛在 2026 年仍显示“报名中” | seed-competition-demo.cjs 固定写入 comp-demo-*；公开控制器未真正限制状态，详情/榜单/答题可按 ID 旁路 | 已修 8254cd6d；公开全链精确隔离、后台保留核验 |
| UX-004 | P1 | 赛事首页 6 个快捷入口中 5 个点击后只提示“即将开放/请进入赛事”，制造可用假象 | 页面先展示未交付能力，再以 toast 降级 | 已修 8254cd6d；删除死入口，真实无赛事显示完整筹备空态 |
| A11Y-001 | P2 | 核心交互 DOM 大量为 generic，语义和键盘可达性弱 | uni view 为主，缺少 role/aria/可聚焦契约 | 横向治理 |

| TRUST-004 | P0 | 已注册的同城公开路由完整展示 6 条北京演示活动/群组/店铺/地点/视频，并伪造距离、人数、浏览量与可操作入口 | `same-city-data.ts` 直接返回本地 mock 数组，页面刷新、换城、详情和导航均不连接真实能力 | 已修 `47670af7`；删除运行时 mock 数据源，改为可信筹备态与真实驿站入口 |
| TRUST-005 | P0 | 关于我们公开展示不可审计的固定规模数字与占位邮箱/微信，品牌后台清空配置后客户端仍回退到假联系方式 | `brand.ts` 把 `support@rebu.com`、`rebu_guoxue` 当默认值且忽略远端空字符串；前后端关于页重复固化 `100+ / 500+ / 50万+` | 已修 `00728e32`；规模指标保持空，联系方式只显示真实配置或平台客服 |
| TRUST-006 | P0 | 第三方信息共享清单宣称集成 10 个支付、地图、统计、推送、社交 SDK，但客户端实际只核实到腾讯云 IM，且隐私/官网入口不可点击 | 页面使用本地 mock 清单与假加载；未按依赖、manifest 和实际运行入口核验 | 已修 `00728e32`；仅披露腾讯云 IM 3.6.6，补官方规则/官网真外链与真实客服 |
| SAFETY-001 | P0 | “青少年模式”公开宣称已限制时长、夜间、内容、消费和私信，实际只保存布尔开关；任意 4 位数字可关闭，身份证只校验长度，后端还允许明文保存并返回监护密码 | 全代码无全局执行闸门；前端密码/重置均为本地假验证，服务端仅写 `teenModeEnabled + JSON` | 已修 `c9ffa719`；统一为真实筹备态，后端拒绝开启、不读取旧敏感 JSON，仅允许用户主动关闭并清除遗留设置 |

| FUND-002 | P0 | 商家保证金页面伪造 `SIMULATED-*` 成功流水，审核可设置正额，签约在资金接口失败时仍默认放行；已营业/暂停状态还被误导去重复签约或重提 | 真实支付与原路退款均未接入，但前后端把账面状态包装为已到账/已退款，构成资金与经营状态双重失真 | 已修 `9fbdae1c`；当前免缴直接签约，正额必须真实流水核验，收款/退款未接入时零写入拒绝 |

## 六、已完成批次

### 批次 1：首屏观感与内容卫生

提交：`97fcd354 fix(experience): 隐藏H5粗滚动条并隔离联调视频`

- H5 全局隐藏 `html/body/uni-page-body/uni-scroll-view` 原生滚动条，保留触摸、滚轮、键盘滚动。
- 推荐服务精确排除 `demo-video-*` 播放器联调种子，不按标题/作者模糊过滤。
- 新增“生产内容卫生”回归断言，防止联调视频再次进入首页/发现。
- `vue-tsc --noEmit`：EXIT 0。
- `tsc --noEmit`：EXIT 0。
- `smart-feed.service.spec.ts`：11/11 通过。
- H5 正式构建：EXIT 0。
- Server 正式构建：EXIT 0。

### 批次 2：跨端代码分支清理

提交：`4b570bec fix(mobile): 清理跨端分支不可达代码`

- 修复文章编辑、发帖及 6 个排盘分享页面中 H5 `return` 后的非 H5 不可达分支。
- 用条件编译明确隔离 H5 与小程序/App 路径，行为不变。
- `vue-tsc --noEmit`：EXIT 0；H5 正式构建：EXIT 0；ASI/unreachable：0 条。

### 批次 3：微信小程序主包瘦身

提交：`d62df55d perf(mp): 业务模块归位分包并压缩主包资源`

- 将 18 个严格单业务使用的数据模块/组件归位到各自分包，更新 122 处引用且保留 Git rename 证据。
- Logo 从 640×640 JPEG（39,503B）等比转为 320×320 WebP（6,916B）。
- 微信小程序主包从约 2.00MB 降至 1.83MB；精确体积 1,921,730B，距 1.9MB 警戒线保留 70,564B。
- `vue-tsc --noEmit`、H5 正式构建、小程序正式构建、包体积门禁：全部 EXIT 0。

### 批次 4：构建告警归零与埋点旁路解环

提交：`c20f3acc fix(mobile): 解开埋点循环并清理跨端样式告警`

- 埋点批量上报改为专用 fail-open 直传，不再反向依赖统一请求层；保留登录 token，不刷新、不重试、不触发登录跳转。
- 修复 flush 期间新事件可能滞留的并发窗口，尾批自动重新调度。
- 商品详情 H5 rich-text 的标签样式增加 `H5` 条件编译守卫，小程序不再解析不支持的 `p` 选择器。
- `vue-tsc --noEmit`：EXIT 0；H5 正式构建：EXIT 0；微信小程序正式构建及 1.9MB 门禁：EXIT 0。
- 构建结果：循环依赖、`p` 标签选择器、ASI/unreachable 三类项目告警均为 0。

### 批次 5：C 端首屏层级与可选运营契约

提交：`b0fcbb85 fix(experience): 收敛首页发现首屏并修正运营楼层空态`

- 首页保持“不回加品牌大字”的既有拍板，孤立搜索图标替换为全宽统一搜索入口；搜索、频道、焦点内容形成清晰任务顺序。
- 统一搜索栏触控高度提升到 88rpx（44px），补 `role`、`aria-label`、键盘 Enter 与按压反馈。
- 发现页每类首屏从 6 张收敛为 4 张，但保留“查看更多”渐进加载和标题“更多”板块入口；十大核心入口一个未删。
- 无已发布运营楼层从 404 改为 200 + `null`，仍不泄露草稿，并保留展示时间过滤。
- 本地只读代理 + iPhone 13 真数据截图：首页/发现均渲染成功；浏览器层拦截全部非 GET 请求，未写生产数据。
- `vue-tsc --noEmit`：EXIT 0；H5/微信小程序正式构建：EXIT 0；小程序主包 1.83MB。
- `marketing.service.spec.ts`：54/54 通过（新增 3 条运营楼层契约回归）；服务端 `tsc --noEmit` 与正式构建：EXIT 0。
- 生产原子发布验证：PM2 4/4 online，外网 health 200；首页/发现运营楼层均为 `200 + data:null`；H5 新入口 `index-ZKnsD9qr.js` HTTP 200 且 immutable。
- 生产 iPhone 13 只读截图：两页无 JS/HTTP 错误、无横向溢出；浏览器拦截全部非 GET 请求，未写生产数据。

### 批次 6：公共入口游客浏览边界

提交：`42b61569 fix(mobile): 恢复四大公共入口游客浏览`

- 圈子广场游客只拉公开圈子与动态，登录后再拉我的圈子、统计和入圈申请。
- 全局搜索游客可查看热搜、联想和结果；搜索历史读取/保存仅登录后执行。
- 商城游客可看商品、活动和公开优惠券；购物车角标与“我的券”叠加仅登录后执行。
- 智能体广场游客可看官方助手、榜单和分类；最近会话仅登录后加载，仍保留最多 6 条横滑、单行省略与续聊。
- 完全无 token 的 iPhone 13 黑盒：首页落点均保持原公共路由，4 页零 401、零 JS/HTTP 错误、零横向溢出。
- `vue-tsc --noEmit`、H5 正式构建、微信小程序正式构建及 1.9MB 主包门禁：全部 EXIT 0。
- H5-only 原子发布验证：新入口 `index--GAkVArJ.js`；后端未重启，上一版目录保留可回滚。
- 生产完全无 token 公网复验：4 页均留在原公共路由，内容完整，零 401/JS/HTTP 错误、零横向溢出、零写请求。

### 批次 7：生产内容卫生与商城状态权限

提交：`7d13f655 fix(content): 隔离生产联调内容并收紧商品状态权限`

- 新增集中式公开内容精确隔离清单；仅收录生产只读证据确认的 4 个后台联调商品、8 门后台联调课程、3 个后台联调视频、1 个后台联调圈子。规则禁止按标题、价格、作者模糊过滤，未知类型 fail-open。
- 隔离覆盖商城主列表/分类/场景取货/店铺主页/直播选品、课程主列表/分类/相关推荐、视频主列表/瀑布流/搜索、圈子列表、智能推荐/统一推荐、FTS/语义搜索；详情与后台管理入口保留，方便运营复核治理。
- 商城公开列表补 `OptionalAuthGuard` 与管理员角色判定：游客伪造 `ALL/PENDING/OFF_SHELF` 一律清空状态参数并回到 `ON_SALE` 公共口径；管理员工作队列继续支持 `ALL` 与多状态。
- 用户自建低质/测试内容未擅自隔离或写库，继续交由内容审核后台处置；本批无 DDL、无生产业务数据写入。
- 缓存命名空间升级：课程、圈子、搜索、统一推荐、视频列表使用新版本 key；智能推荐在统一信封出口再次兜底，旧缓存也不会漏出隔离记录。
- `tsc --noEmit`、`vue-tsc --noEmit`、server 正式 build：全部 EXIT 0。
- 11 组定向 Jest：263/263 通过，覆盖游客/管理员相反权限、主列表、店铺、相关推荐、直播选品、推荐、搜索和不误伤正常内容。
- 部署归档 SHA256：`CD36A8278B0350FF89DF8D2E2A5F09B2DF0BCA4358311E9CD2E0F9129FCFDE9E`；server-only 原子发布成功，旧版保留于 `/opt/guoxue/apps/server/dist.prev-7d13f655`。
- 首轮部署因健康探针误用 3000 端口触发自动回滚，回滚后公网 health 200、PM2 4/4；确认真实端口 3001 后二次原子切换 `DEPLOY_OK`，也实证了回滚链可用。
- 生产公网只读验收：默认商品、伪造 ALL、伪造 PENDING 均返回同一组 3 个 `ON_SALE` 商品；课程 9、视频 8、圈子 8 正常；16 个隔离 ID 在列表/推荐/搜索均 0 命中；health 200，PM2 4/4 online。
- H5 未部署、入口仍为 `index--GAkVArJ.js`，确认 server-only 发布未误改前端。

### 批次 8：研究院真实申请与资金状态防假写

提交：`9caa773c fix(institute): 纠正会籍申请与资金状态`

- 公开首页、五步入院向导、讲师遴选、会籍中心与管理端统一为真实流程：资格自检 → 免费提交申请 → 管理层审核；当前不创建支付订单、不扣款。
- 删除模拟收银台、确认支付、交钱即进、可退押金、任务达标即可退款等误导入口；双轨席位、任务成长、讲师遴选与真实审批能力完整保留。
- 会籍中心不再把名义 `deposit=10000` 展示为已缴金额；无真实支付订单时固定 `deposited=0 / canRefund=false`，旧客户端直调退款端点也只返回明确错误，绝不写 `depositRefunded/GRADUATED` 假状态。
- 研究院分红审批新增服务端余额双查：仅按同院 `InstituteRevenue × 50% - 已发放 InstituteDividend` 计算可用留存，0 收入或超余额直接拒绝；审批 payload 增加 `instituteId` 归属。
- 游客可直接浏览讲师遴选机制，不再被 `/institute/my` 401 劫持；游客点击席位时弹出全中文“登录后继续申请”，且在登录前不调用私有资格接口。
- 管理端“通过”明确为成员审核，不再假称确认支付到账；分红按钮在零余额置灰，提交动作明确为“发起审批”而非“已发放”。
- `vue-tsc --noEmit`、server `tsc --noEmit`、H5 正式 build、server 正式 build：全部 EXIT 0；精确 ESLint：0 error。
- `institute.service.spec.ts`：39/39 通过；新增覆盖任务全完成仍不可假退款、旧客户端不可写退款状态、0 收入不可提分红、真实留存内才可建审批。
- 本地只读代理与生产 iPhone 13 黑盒：首页、入院申请、讲师遴选均保持原路由，390px 无横向溢出，零 JS/HTTP 错误、零业务写请求；生产游客点击讲席未发资格 API，弹层文案全中文。
- 部署包 SHA256：server `25AB126FCA610301B102D4964E866EC387CCA8EE467F2A156B6C89C34D5E5BB9`；H5 `82DCD1747AB806680F8FA7002F90AE8246D1410F5309DBB666A88F255A48B617`。
- 生产双端原子发布成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-9caa773c`，H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-9caa773c`；新入口 `index-ykFEZypm.js`。
- 公网 health/H5/asset 均 200；PM2 4 个独立 PID 在线，内部 `127.0.0.1:3001` health 通过。本批无 DDL、无生产业务数据写入。

### 批次 9：平台公告真实化与分页契约生产校正

提交：`95dbee7c fix(notices): 以真实公告替代硬编码内容`；生产验收热修：`d8063c43 fix(notices): 适配统一分页响应`

- 删除 C 端 6 条硬编码公告：不再按当前时间伪造“2 小时前/2 天前”，不再展示双十一、2025 实名升级、假维护、假浏览量与假置顶状态。
- 新增公开只读公告接口，仅返回后台 `SiteNotice` 中 `isActive=true` 且命中 `startTime/endTime` 展示窗口的记录；列表与详情使用同一可见性条件，停用、未开始、已过期公告不可绕过详情访问。
- 列表改为真实分页、真实类型与真实创建时间；无记录时显示“平台当前没有正在展示的公告”，网络错误可重试。
- 详情按真实正文做安全的标题/段落/列表基础排版，删除固定 2025 正文、`example.com` 链接、虚构 PDF 附件和假下载；分享改为复制当前真实链接。
- `vue-tsc --noEmit`、server `tsc --noEmit`、H5 正式 build、server 正式 build：全部 EXIT 0；前后端精确 ESLint 0 error。
- `system.controller.spec.ts` + `system.service.spec.ts`：47/47 通过，覆盖公开时间窗、公开详情不可见条件与控制器契约。
- 本地只读视觉验收：真实响应样例列表 2 张卡片，点击进入对应详情；两页 390px 无横向溢出、无 JS/HTTP 错误、无写请求。
- 首次生产验收发现全局响应器会把分页对象规范化为 `data: [] + pagination`；立即改用项目统一 `apiGetPaged`，形成独立热修提交并重建 H5，实证“生产实测不能由类型门禁替代”。
- 首次双端部署包 SHA256：server `FAACBA567C47C21E8CAF78FD3F1AB90185F5504273E016522D3BCB9B86268132`；H5 `AA4F43BE254A12FB40AEB14621B0DB0193AFBC651FBC8C6F5272589B3FC0CC82`。
- 分页热修 H5 SHA256：`4E6AB866403F09BE7F255443ACBFA1859BB3F85718AB26131DED5A44DBCCAAA1`；最终入口 `index-Edn9ysIu.js`，生产标签 `prod-20260719-notices-truth`。
- 生产回滚目录：server `/opt/guoxue/apps/server/dist.prev-95dbee7c`；H5 首批 `/opt/guoxue/apps/mobile/h5/dist.prev-95dbee7c`；H5 热修 `/opt/guoxue/apps/mobile/h5/dist.prev-d8063c43`。
- 公网 health/H5/asset 均 200；PM2 4 个独立 PID 在线，内部 3001 health 通过；公开公告接口 `200 + data:[] + total:0`。生产 iPhone 13 黑盒显示真实空态，无假公告、无加载错误、无横向溢出。本批无 DDL、无生产业务数据写入。

### 批次 10：赛事演示数据公开隔离与入口收敛

提交：8254cd6d fix(competition): 隔离演示赛事并收敛公开入口

- 生产只读审计确认，公开列表仅有两条 comp-demo-*：固定脚本注入的“2024 热卜杯”与“诗词雅韵”，含测试账号、虚构奖金、排名和过期赛程；本批不删库、不改生产记录，仅在公开边界精确隔离，后台管理继续可见。
- 新增统一公开赛事策略：列表只允许 PUBLISHED / IN_PROGRESS / FINISHED，拒绝 DRAFT / CANCELLED；comp-demo-* 不进入公开列表。
- 详情、排名、赛后题目、报名状态、报名、成绩、单题/整卷提交、试卷与证书全部先经过同一真实可见赛事闸门；不能通过猜测赛事、赛程或排名 ID 绕过列表隔离。
- 公开人才榜排除 comp-demo-* 测试账号与演示赛事徽章；后台赛事通用查询、演示数据核验能力和真实赛事发布链不变。
- 赛事首页删除直播赛间、线下赛、邀请同台、赛制公示等 5 个未交付死入口；没有真实赛事时显示“赛事正在筹备”与可用的人才榜入口，搜索无结果使用独立文案。
- vue-tsc --noEmit、server tsc --noEmit、H5 正式 build、server 正式 build：全部 EXIT 0；前后端精确 ESLint 0 error。
- competition.controller.spec.ts + competition.service.spec.ts + talent.service.spec.ts：52/52 通过，覆盖公开/后台分流、草稿拒绝、演示前缀、子资源归属与人才榜查询条件。
- 本地 390px 黑盒覆盖真实空态与两条正常赛事样例：无横向溢出，焦点/历史卡片保留，5 个死入口均不出现。
- 部署包 SHA256：server 23732836E5ED0731D1001E2985A67AB8DBFE72A2767D8171F9784559CCD559ED；H5 13E3208F1AA69F966547401FA75E6709C91231A486169A4C7D4BCCFA6D20C85D。
- 双端原子部署成功：server 上一版 /opt/guoxue/apps/server/dist.prev-8254cd6d，H5 上一版 /opt/guoxue/apps/mobile/h5/dist.prev-8254cd6d；新入口 index-C0C163mg.js，生产标签 prod-20260719-competition-truth；PM2 PID 2884630,2884631,2884827,2884845。
- 公网 health/H5/asset 均 200；公开赛事 200 + data:[] + total:0，status=DRAFT 同样为空，演示详情与排名旁路均 404。生产 iPhone 13 黑盒显示完整筹备空态，无演示标题、无死入口、无 JS/HTTP 错误、无横向溢出、无写请求。本批无 DDL、无生产业务数据写入。

### 批次 11：同城演示内容下线与可信入口收敛

提交：`47670af7 fix(discover): 移除同城虚假内容`

- 生产只读审计确认，已注册的 `/pkg-discover/same-city/feed` 公开路由完整展示 6 条固定北京演示记录，包含虚构活动、群组、店铺、地点和视频，以及固定距离、参与人数、浏览量、点赞量；刷新、换城、详情与导航动作也未连接真实业务。
- 删除仅有一个消费者的 `same-city-data.ts` 运行时 mock 数据源，旧标题、演示城市、虚构统计与伪 API 标识在源码中全部归零；没有模糊过滤、没有生产数据删除。
- 同城页改为完整可信筹备态：明确只在场地、时间和距离核验后开放；不展示演示活动、虚构人数或未经核验的距离；位置授权保持自愿并支持未来手动选城。
- 保留真实可用的“先逛线下驿站”主行动，准确进入 `/pkg-offline/stations/index`；主/次操作补齐 44px 以上触控区、`role` 与 `aria-label`，390px 无横向溢出。
- 精确 ESLint 0 error/0 warning，`vue-tsc --noEmit` EXIT 0，H5 正式 build EXIT 0；本地 iPhone 13 只读回归无写请求，主行动跳转正确。
- H5 发布归档 SHA256：`830686974CC5CD83B5ECDFE86C4197F23DBB19961A22A23DECC7EBFCA2CDB726`；H5-only 原子切换成功，上一版保留于 `/opt/guoxue/apps/mobile/h5/dist.prev-47670af7`，新入口 `index-CzT-Vql5.js`，生产标签 `prod-20260719-same-city-truth`。
- 公网 health/H5/asset 均 200；生产 iPhone 13 黑盒确认旧 6 条虚假内容零命中、无 JS/HTTP 错误、无横向溢出、无写请求，真实驿站入口可达。本批无 DDL、无后端重启、无生产业务数据写入。

### 批次 12：公开信任面与第三方披露真实化

提交：`00728e32 fix(trust): 真实化公开信息与联系方式`

- “关于我们”删除 `100+ 专家讲师 / 500+ 精品课程 / 50万+ 学习用户` 等不可审计固定规模，不再把宣传数字伪装为实时平台数据；页面重构为品牌使命、真实能力与可达联系入口，当前年份自动生成。
- 品牌配置默认邮箱/微信改为空；服务端明确返回空值时允许清除客户端旧配置，避免后台已清空、H5 仍回退到 `support@rebu.com / rebu_guoxue`。
- 数据收集清单、儿童隐私页只展示后台实际配置的邮箱/电话，并始终提供真实平台客服入口；删除 `privacy@rebu.com / 400-888-8888` 与空点击动作。
- 第三方信息共享清单按客户端依赖、manifest 与运行入口重新核验：当前仅披露腾讯云即时通信 IM SDK 3.6.6；删除未实际集成的微信开放、支付宝、高德、友盟、极光、Bugly、阿里云播放器、QQ、微博等 9 项假披露。
- 腾讯云 IM 条目补齐提供方、使用目的、触发场景、处理方式、权限和信息类型；个人信息保护规则与官方网站为真实可点击官方链接，并明确服务端 API 调用不等同于客户端内嵌 SDK。
- 后端公开 `/system/about` 同步返回 `stats: []` 与真实能力描述，前后端不再存在两套宣传口径；新增回归断言阻止固定规模数字回流。
- 精确 ESLint 0 error；移动端 `vue-tsc --noEmit`、服务端 `tsc --noEmit`、H5 正式 build、server 正式 build 全部 EXIT 0；`system.controller.spec.ts` 27/27 通过。
- 本地 iPhone 13 只读黑盒覆盖关于、SDK、数据收集、儿童隐私四页：390px 无横向溢出，假规模/假联系方式零命中，腾讯隐私外链正确，三处客服入口可达，零业务写请求；四张长页截图经主进程亲核。
- 部署包 SHA256：server `92D204B96E593A16F493BF844EB9FE2137535E26CEBE29D9F13105EC32E4720B`；H5 `F7E9F8ADE5C3DBD5387DFCD7D5D578118AE454FE35D085997F944E5D565ADBA5`。
- 双端原子部署成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-00728e32`，H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-00728e32`；新入口 `index-BQ3JiOcu.js`，生产标签 `prod-20260719-public-trust`。
- 公网 health/H5/asset/about 全部 200；about `stats=[]` 且无固定规模字符串；PM2 4 个实例 `2907393 / 2907394 / 2907596 / 2907607` online。生产 iPhone 13 四页复验无 JS/HTTP 错误、无横向溢出、无业务写请求，官方隐私外链和客服跳转均通过。本批无 DDL、无生产业务数据写入。

### 批次 13：未成年人模式 fail-safe 与明文密码通道封堵

提交：`c9ffa719 fix(safety): 阻断空壳未成年人模式`

- 地毯式代码审计确认：旧“青少年模式”只有 `teenModeEnabled + teenModeSettings JSON` 持久化，没有首页、推荐、搜索、直播、商城、支付、私信或 AI 服务端执行闸门；所谓时长、22:00-6:00、内容过滤、消费限制均不生效。
- 旧前端设置监护密码后并未上传真实密码，关闭时输入任意 4 位数字即可通过；“身份证重置”只判断 18 位长度。后端 DTO 还允许把 `guardianPassword` 明文写入 JSON，GET 会原样返回，形成潜在敏感信息暴露通道。
- 依据国家网信办《[移动互联网未成年人模式建设指南](https://www.cac.gov.cn/2024-11/15/c_1733364304749288.htm)》关于时间、内容、功能、家长管理与防绕过的完整要求，本批不继续修饰空壳设置页，采用 fail-safe：完整保护链上线前拒绝开启。
- 两条既有路由统一使用新的“未成年人模式”真实状态组件：明确暂未开放、说明不能开放的原因、列出时间/内容/功能消费/家长管理四条上线门槛，并提供儿童隐私保护和平台客服两个真实入口。
- 移动端删除旧 `TeenModeSettings`、假时长/过滤选项、监护密码适配和 GET/PUT 调用；公开页面不再请求私有 teen-mode API，也不再出现“已开启保护”“立即开启”“身份证找回”等虚假承诺。
- 服务端 GET 只核验用户存在，不再查询或回传旧 `teenModeSettings`；PUT `enabled=true` 明确拒绝且零写入；PUT `enabled=false` 仅用于旧客户端/历史用户主动关闭，同时以 `Prisma.DbNull` 清除可能含明文密码的遗留 JSON。未批量改写任何生产用户记录。
- 移动端 `vue-tsc --noEmit`、服务端 `tsc --noEmit`、H5 正式 build、server 正式 build 全部 EXIT 0；新增组件/核心服务精确 ESLint 0 warning；teen-mode controller/service 专项 Jest 8/8 通过。
- 本地 iPhone 13 黑盒覆盖 intro/settings 两条路由及真实内部滚动：390px 无横向溢出，四条建设门槛完整，固定底栏不遮挡，儿童隐私与客服跳转正确，旧假承诺与私有 API 请求均为 0，零业务写请求；顶部和底部截图经主进程亲核。
- 部署包 SHA256：server `4B595168E2E2AB7AF3BE081F4036BF453C037AD7D6D62C4C79C37F211D985FD9`；H5 `58386E7FA14B868497D86FC58EC912172410545E704E2533BB4F8E6DA98C7A11`。
- 双端原子部署成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-c9ffa719`，H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-c9ffa719`；新入口 `index-CcR8BhsZ.js`，生产标签 `prod-20260719-minor-mode-failsafe`。
- 公网 health/H5/asset 均 200，未登录 teen-mode API 保持 401；PM2 4 个实例 `2915066 / 2915067 / 2915275 / 2915286` online。生产 iPhone 13 两路由复验无 JS/HTTP 错误、无横向溢出、无 teen-mode API、无业务写请求，两个真实行动均可达。本批无 DDL、无生产业务数据写入。

### 批次 14：商家免保证金入驻与资金状态真实化

提交：`9fbdae1c fix(merchant): 真实化免保证金入驻与资金状态`

- 生产只读聚合核验：商家状态为 `ACTIVE 1 / CLOSED 1`，正额保证金 0、保证金流水 0、`SIMULATED-*` 流水 0；现行配置与功能开关均未启用自动收取。基于真实存量采用“当前免保证金”口径，无 DDL、无回填、无生产业务数据写入。
- 删除前后端模拟收款：`payDeposit` 不再创建 `SIMULATED-* / SUCCESS` 流水；免缴场景明确提示无需支付，历史正额场景明确提示在线收款未开放并联系平台，均不会调起假收银台。
- 到账判定改为严格真实凭证：仅接受同商家、`PAYMENT + SUCCESS`、存在非 `SIMULATED-*` 渠道交易号且金额足额的流水；资金状态接口失败时签约 fail-safe 阻断，不再默认按免缴放行。
- 退款接口在真实原路退款未接入前零写入拒绝；管理端删除退还/调额资金按钮，只保留历史流水只读核验。兼容调额端点仅允许未缴零金额遗留状态自愈，正额、已缴或有账面余额均拒绝。
- 自动审核与人工审核统一进入 `AGREEMENT_PENDING`，保证金为 0；公开入驻 DTO 删除无效保证金参数，管理员兼容字段明确只允许 0，正额必拒绝。
- C 端保证金页重构为真实免缴/历史待核验两态；签约页同时加载协议与资金状态，修复协议固定高度 `overflow:hidden` 导致尾部条款不可读的问题；已开通商家直接进入工作台，暂停/关闭商家只能联系平台，不再重复签约或重新申请。
- 管理端审核页删除保证金金额输入，列表与详情统一“当前免缴/保证金待处理”真实文案；违规、处罚、结算等原有管理能力完整保留。
- 移动端、管理端、服务端完整类型门禁均 EXIT 0；商家 5 套 Jest 72/72 通过；本批重构页精确 ESLint 0 warning，正式 H5/admin/server build 全部 EXIT 0。
- 本地 iPhone 13 隔离黑盒覆盖免缴、历史正额待核验、签约、已营业、暂停 5 态：390px 无横向溢出、零业务写请求、CTA 路由全正确；协议滚动到最大值后第十二条位于屏幕可读区，勾选后才启用签约按钮。截图经主进程亲核。
- 部署包 SHA256：server `E66684FB95FF624999863C9D38294C5FBA9891BF85B8875D3A8D996C90E28C44`；admin `2DA48690A9FCA754BDCD38ACB100EBB5B51AFAB39692DC5C735FB9586DE8BD5F`；H5 `A1E960BA8A6C93AF033BFE87A7DD8720A831AC658B463BDA129F2F45EC43B27E`。
- 三端原子部署成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-9fbdae1c`，admin 上一版 `/opt/guoxue/apps/admin/dist.prev-9fbdae1c`，H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-9fbdae1c`；生产标签 `prod-20260719-merchant-deposit-truth`。
- 新 H5 入口 `index-vay1o5Y2.js`、管理端入口 `index-BbPAlo_f.js`；公网 health、H5/admin HTML 与两端入口资产均 200，旧 H5 入口哈希已消失；PM2 新实例 `2943259 / 2943260 / 2943497 / 2943508` 全部在线，数据库/支付/AI 健康项均 `ok`。
- 生产静态包 iPhone 13 实渲染覆盖保证金、签约、已开通 3 页，业务 API 全部在浏览器侧拦截为隔离数据：无 JS 错误、无横向溢出、资金文案与第十二条可达性全部通过，未读取或写入生产商家数据。

### 批次 15：商家真实售后、退款状态机与物流履约

提交：`3a6aae61 fix(merchant): 打通真实售后与物流履约`

- 生产只读聚合确认：2 个商家、16 个商家订单，其中 `PAID 5 / REFUNDED 3 / CANCELLED 8`，现存运单 0、售后单 0；旧商家端却把 `REFUNDED` 误显示为“待处理退款”，并把 AfterSale 伪装成字段空缺的“客户咨询”，属于真实交易语义断裂。
- 工作台待办由假“咨询”改为真实 `AfterSale.PENDING`；消息中心第二栏改为真实售后工作台，展示商品、订单、金额、原因、处理记录与 PENDING/PROCESSING/APPROVED 等真实状态，退款、拒绝、非退款完成分别走明确二次确认。
- 订单页把 `REFUNDED` 统一为已完成结果，删除必然失败的“同意/拒绝退款”按钮；订单详情返回真实 `OrderLogistics`，展示快递公司、运单号、复制入口及快递100轨迹，操作区满足 44px 触控基线。
- 发货改为事务内 CAS 更新订单 + 运单 upsert，避免重复点击及“订单已发货但运单未落库”的半状态；售后处理使用 CAS 抢占与状态机约束，退款类只有真实资金路径成功后才收敛为 APPROVED。
- 退款链资金 fail-closed：未配置支付通道时拒绝自动退款，`PROCESSING` 不再提前把订单写成 `REFUNDED`；成功回调按稳定 `RF{orderId}` 定位订单并同步售后，失败/关闭回调保持订单资金状态并把售后退回可核对的 PENDING。
- 快递100按[官方实时查询契约](https://api.kuaidi100.com/document/5f0ffb5ebc8da837cbd8aefc.html)修正为 form-urlencoded POST，签名使用完整 `param JSON + key + customer` 的大写 MD5；生产公网无效单号返回“找不到对应公司”而非“服务未配置”，证明已命中真实配置与远端服务。
- 服务端 `tsc --noEmit`、移动端 `vue-tsc --noEmit`、H5/server 正式 build、`git diff --check` 全部 EXIT 0；商家控制器/服务、物流、退款 4 套专项 Jest 63/63 通过。
- 本地与生产静态包 iPhone 13 隔离回归覆盖工作台、售后工作台、已发货订单详情：无横向溢出、无 JS/HTTP 错误、无业务写请求；售后按钮与物流触发区均约 45.75px，慢网动态分包等待后页面和 4 条物流轨迹完整呈现。
- 部署包 SHA256：server `60FA4FF23BB2B35C40873C3FDA5E054A2D273136188F8482674BEE3ACA14DDE1`；H5 `FCFDB696C3F44962789899D80BCFC0FE9D01C567396019C2AE32620979C8CDD0`。
- 双端原子部署成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-3a6aae61`，H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-3a6aae61`；新入口 `index-q8dD59DY.js`，生产标签 `prod-20260719-merchant-ops-truth`。
- 公网 health/H5/新 asset 均 200，旧 H5 主包 404；PM2 新实例 `2964553 / 2964554 / 2964753 / 2964754` 在线，数据库/支付/AI/存储检查均 `ok`。本批无 DDL、无生产业务数据写入。

### 批次 16：分站邀请归属、开通续费与支付幂等

提交：`5000f72d fix(station): 打通邀请归属与安全续费`

- 地毯式审计纠正旧结论：分站主推位到 C 端推荐流的读取链已在此前批次接通；本批真正仍断裂的是运营商邀请归属、站长首次开通/待支付续接/正常续费，以及支付成功后前端因 5 分钟旧缓存继续显示 `PENDING` 的状态真相。
- 邀请链接统一解析 `op`，兼容旧 `inviteCode/code`；新增公开只读邀请校验端点，只返回运营商名称、ID 与剩余名额。服务端在事务内校验运营商 ACTIVE、未过期与剩余名额，按真实 `Station.operatorId` 计数并以 CAS 占用名额，创建分站时一次性绑定归属，重复申请给出明确提示。
- 开通页按真实状态闭环：无分站时提交资料后建 `STATION_MASTER` 订单；`PENDING` 直接继续支付且不重复申请；`ACTIVE/EXPIRED` 从当前到期日安全顺延一年；`DISABLED` 明确阻断并联系平台。站长面板续费改走 `?renew=1`，不再跳回必报“无需重复申请”的旧流程。
- 资金路径逐处双查：本地订单创建增加 Redis 锁与 PostgreSQL advisory transaction lock，复用同一待支付订单；Native 支付复用 110 分钟二维码并用订单 ID 生成稳定商户单号，缓存过期时先安全关闭旧微信订单，关闭竞态则查询真实状态并拒绝重复下单。
- 微信支付服务补官方关单端点；JSAPI/Native/H5 在写入 `payTransactionId` 后清理订单缓存；统一支付成功处理器在数据库事务提交后清理订单详情、Native 二维码及用户订单列表缓存，修复“钱已到账但页面仍显示待支付”的 P0 状态滞后。
- 合规文案与真实能力对齐：删除未实现的自动减免、原路退款和补差价升级承诺；金额始终以服务端订单为准，自动减免/原路退还未开放且不作为本次支付条件。同步修正协议、静态运营数据与 SQL 说明，不再以文案替代资金能力。
- 服务端 `tsc --noEmit`、移动端 `vue-tsc --noEmit`、精确 ESLint、H5/server 正式 build、`git diff --check` 全部 EXIT 0；shop order/payment 与 station controller/service 4 套专项 Jest 118/118 通过。
- 本地 iPhone 13 隔离回归及生产独立浏览器进程覆盖邀请访客、PENDING、ACTIVE、DISABLED 四态：新版入口均为 `index-Dx3rkUBj.js`，关键 CTA 约 49.9px，无横向溢出、无 JS 业务错误、无业务写请求；公网资源偶发慢于固定 2.5 秒采样时，延长观察后稳定呈现，已排除业务加载死锁。
- 部署包 SHA256：server `2F12F92163513920C446D99685A613CFE2F07643F44CD9008CB65AC176ACC0B0`；H5 `86BC4D179F6BC52C560C0DF4E3BE0AB16DDBCA54CE2094A6B16AE0081BD67AFC`。
- 双端原子部署成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-5000f72d`，H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-5000f72d`；首次 H5 探针因误用本机 HTTP Host 路径触发自动回滚，确认回滚链有效后改用 HTTPS SNI 探针重部署成功。
- 公网 health 200，数据库/Redis/支付/AI/存储等检查均 `ok`；H5 新入口 200、旧 `index-q8dD59DY.js` 404，公开邀请非法 ID 返回结构化 400 而非路由缺失。PM2 4 个实例 `2986768 / 2986769 / 2987008 / 2987019` online，生产标签 `prod-20260719-station-renewal-truth`。本批无 DDL、无生产业务数据写入。

### 批次 17：运营商真实开通、续费与名额经营

提交：`d87eead3 fix(operator): 打通真实开通续费与名额经营`

- 运营商开通页不再依赖前端硬编码价格、周期和名额：新增公开只读 `/station/operator-plan`，统一从 `CommissionConfig.operator_SILVER` 与 `ConfigSystem.station.billing_period_months` 读取真实方案；关键价格/名额缺失时 fail-closed，生产实测为 `¥4999 / 12个月 / 1+5共6个名额 / 10%团队管理奖`。
- 开通与续费统一走真实 `OPERATOR` 订单和微信 Native 二维码；首次开通、ACTIVE 续费、EXPIRED 恢复、DISABLED 阻断四态完整。待支付订单通过 Redis 锁、PostgreSQL advisory transaction lock 与同用户同档位复用，避免重复点击形成多笔资金订单。
- 支付回调逐处双查：EXPIRED 到账后恢复 ACTIVE 并顺延期限；DISABLED 即使迟到回调也保持停用，防止资金回调越过风控状态。前端真实绘制微信支付二维码，开放微信内打开与复制支付链接降级。
- 名额页删除不存在的售卖、赠送、搜索、交易记录与静态配额模型，改为按真实 `Station.operatorId` 展示已占用分站和剩余可邀请名额；运营商面板的失效自跳链接统一接到看板、团队和邀请真实入口。
- 协议删除固定价格、名额出售/赠送和固定日期微信零钱/银行卡结算等未兑现承诺，明确价格以订单为准、名额按邀请归属占用、收益按真实结算记录确认。
- 移动端 `vue-tsc --noEmit`、服务端 `tsc --noEmit`、H5/server 正式 build、`git diff --check` 全部 EXIT 0；精确 ESLint 0 error（历史格式类 warning 未扩散）。station controller/service 与 shop order/payment 4 套 Jest 共 126/126 通过。
- 本地 iPhone 13 黑盒覆盖访客、ACTIVE、EXPIRED、DISABLED、看板访客/有效、真实名额、支付二维码 8 态：无横向溢出，关键 CTA 为 44–50px，二维码约 183px，除两条隔离的模拟支付请求外无写请求。
- 部署包 SHA256：server `0ABBF5CE879998B1EF243BDED772B164B78CC87C66B9A18498B6F90E3A697DDD`；H5 `E37F0D7B032B707F2898F8DCE9E00424DBE664F3F58F2C5D8E1B6F90617BC8E2`。
- 双端原子部署成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-d87eead3`；H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-d87eead3`；新入口 `index-_HQG2fHb.js`。H5 首次切换因普通 ubuntu 用户无权读取证书和 nginx 日志而在 `nginx -t` 阶段自动回滚，确认回滚链有效后改用不变配置下的 HTTPS SNI 探针重新切换成功。
- 生产 8 状态静态资源视觉矩阵全部通过；另以无 mock 读取方式实测访客开通页，真实命中生产方案接口并渲染 `4999 / 12个月 / 6名额 / 10%`，390px 无溢出、主按钮 50px、无控制台错误和业务写请求。新 asset 200 且旧 `index-Dx3rkUBj.js` 404；内外 health 200，数据库、Redis、支付、AI、存储检查均 `ok`，PM2 4 实例在线。本批无 DDL、无生产业务数据写入。

### 批次 18：线下驿站预约核销、公开可见性与资金真相

提交：`fd0930e0 fix(offline): 收紧预约核销与资金真相`

- 生产只读摸底确认当前仅 1 个 ACTIVE 驿站，课程、报名、驿站订单、结算单均为 0；本批无需迁移或回填，可直接按上线安全口径收紧。
- 公开可见性统一收口：驿站列表改为平台管理权限接口；公开驿站详情/品牌主页仅展示 ACTIVE，驿站主可选登录预览本人待审核/停用页面；公开课程列表和详情仅展示 ACTIVE 驿站下 APPROVED + PUBLISHED 课程。
- 消除报名凭证泄露：公开课程详情不再返回 registrations，更不会暴露 userId、qrCode、verifyCode；仅返回服务端过滤后的 REGISTERED/SIGNED_IN 有效报名数。生产实测 ACTIVE 驿站详情无 teacherBookings、ownerUserId、qrCode、verifyCode。
- 预约生命周期补齐：报名服务端硬校验课程审核/发布、驿站 ACTIVE 与未开场；CANCELLED 不占名额且允许复用唯一报名行重新预约并换新凭证；距开课不足 24 小时的取消规则下沉服务端，前端绕过无效。
- 核销改为同日有效 + CAS 原子认领：二维码与 6 位码统一限制开课当天，updateMany 仅从 REGISTERED 推进 SIGNED_IN，解决并发双核销竞态；凭证页明确“预约核销凭证不是付款凭证”。
- 资金路径逐处双查并 fail-closed：StationOrder 缺付款人、交易号和真实支付回调，管理员手填结算金额与 settled 标记也不能代表打款，因此创建订单、人工改 PAID、创建结算、仅标记结算四个写入口全部关闭；经营看板只统计 PAID/COMPLETED，并显式返回 PAY_AT_STATION、onlineCollectionEnabled=false、settlementEnabled=false。
- C/B 端文案与真实能力对齐：付费课统一为“预约名额 + 到店支付”，平台不代收、不承诺自动原路退款；经营看板与订单结算页改为线上实收/历史只读口径，不再把到店收款包装成平台营收或自动结算。
- 真人视觉验收额外抓出并修复课程底栏层级 P0（按钮可见但被 scroll-view 拦截点击），同时把“收款口径”入口热区从 18px 扩至 44px。生产与本地 8 态矩阵覆盖付费/免费课程、确认弹层、报名凭证、经营看板、收款口径、订单/结算空态；375/390/430px 均无横向溢出，关键热区 44–48px，无控制台错误、无业务写请求。
- 服务端 `tsc --noEmit`、移动端 `vue-tsc --noEmit`、精确 ESLint（0 error，历史格式 warning 未扩散）、H5/server 正式 build、`git diff --check` 全部 EXIT 0；offline service/controller 两套专项 Jest 100/100 通过。
- 部署包 SHA256：server `89DA2F0A444D046E5248E453AEF644A0BF44CCA7AB33843FD13313ABEDA3B520`；H5 `CF7AB056E4D4E9C9923E858BD4D60218651BC01C9E8E85B4BC18A2D357AB76CE`。
- 双端原子部署成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-fd0930e0`；H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-fd0930e0`；新入口 `index-C7l3Rt7j.js` 200，旧 `index-_HQG2fHb.js` 404。外网 health 200 且 10 项依赖全 ok，驿站管理列表匿名访问 401，PM2 4 实例在线。生产标签 `prod-20260720-offline-appointment-truth` 精确指向代码提交；本批无 DDL、无生产业务数据写入。

### 批次 19：研究院会籍治理、财务真相与隐私边界

提交：`943b6a1b fix(institute): 收紧会籍治理与财务真相`

- 生产只读摸底确认当前仅 1 个 ACTIVE 研究院，成员、会费收入、分配记录、活动、私董会与待审分配均为 0；本批无需 DDL、迁移或数据回填，直接按空账安全口径收口。
- 会籍创建严格校验角色与年份；普通申请保持 PENDING、零押金且不提前加入研究院圈子，审核 ACTIVE 后再 fail-open 加圈；特邀 ACTIVE 同样安全加圈，圈子服务异常不回滚会籍主事务。
- 公开边界统一只展示 ACTIVE 研究院/成员并使用字段白名单，彻底剔除会费、免缴、退款、邀请、审核人等内部字段；公开筛选参数非法值返回 400，不能借详情或筛选旁路读取待审成员。
- 管理动作全部锁定操作者所属研究院：成员审批、角色任命、任务核验、活动创建、手工积分与财务查看不再允许跨院；管理成员列表改用独立 `admin/members` 鉴权接口，后台状态与角色枚举同步真实模型。
- 财务只认 `sourceType=MEMBERSHIP` 的真实会费入账；已确认分配使用全量聚合，待审批金额预占可申请余额；发起审批逐处复核本院成员、合法类型、金额、余额与既有待审，执行阶段在 SERIALIZABLE 事务内再次校验，防并发超发。
- 当前没有线上会费收款和真实出款能力，因此前后端统一诚实表述为“分配确认记录，不代表到账，以平台资金流水为准”；0 收入、0 成员或非本院管理层时发起按钮禁用，不再把名义金额包装成营收或到账。
- 服务端 `tsc --noEmit`、移动端/Admin `vue-tsc --noEmit`、精确 ESLint（0 error）、server/H5/Admin 正式 build、`git diff --check` 全部 EXIT 0；研究院 6 套 Jest 共 126/126 通过。
- 本地视觉回归覆盖 375/390/430px 公开首页/入院申请/讲师遴选，以及 390px 管理审核/财务/管理层会籍中心；Admin 1440px 成员与财务页同时验收，均无横向溢出、无 JS/HTTP 错误、无业务写请求；主进程人工核对指标层级、禁用态与空态。
- 部署归档 SHA256：server `AFE97B0D190EBED9EB960FA7A9AC380597A1C3BA28E93439D8BEF19E3D354F64`；H5 `F078112A1486D346B1922D53653758DFEA9B23B52C5A0CCA49703B34306BD63B`；Admin `CD9044A5B96F088B6EA9333CDC207DC9E0CE68CBA5B0F43201C4ACA15C5CC16B`。
- 两段式三端原子部署成功：server 上一版 `/opt/guoxue/apps/server/dist.prev-943b6a1b`，H5 上一版 `/opt/guoxue/apps/mobile/h5/dist.prev-943b6a1b`，Admin 上一版 `/opt/guoxue/apps/admin/dist.prev-943b6a1b`；新入口分别为 H5 `index-DPaEW2N5.js`、Admin `index-CYNaaka4.js`。
- 生产外网复验：health 200 且 10 项依赖全 ok，PM2 4 实例在线；公开成员隐私字段为 0 泄露、非法角色 400、管理接口匿名 401、超管成员/财务 200、非法财务周期 400；H5 旧 `index-C7l3Rt7j.js` 404。生产移动端 3 页与 Admin 2 页均零溢出、零 JS/HTTP 错误，财务发起按钮保持禁用。本批无 DDL、无生产业务数据写入。

### 批次 20：圈子占位入口收敛、嘉宾邀请接线与从业者支付体验

提交：`baa9cb34 fix(circle-workspace): 收敛占位入口与支付体验`

- 路由与入口地毯审计确认：`stats.vue` 未注册且无入口，不进入生产包；`analytics.vue` 与 `distribution.vue` 虽已注册但没有真实入口，前者仅展示全零占位数据，后者把内存态误报为“已保存”。按“上线优先、高级能力后补”的口径移除两条未交付路由，源码保留供后续正式建设，避免用户误入半成品。
- 圈子嘉宾页原“邀请嘉宾”仅弹出敬请期待提示，现已接到真实邀请码管理页 `/pkg-circle/circles/invite-codes?id=...`，复用现有邀请码创建、分享和归属链路。
- 从业者专业版支付弹层改为绘制真实 Native 支付二维码，支持微信内打开与复制支付链接降级；订单创建失败与微信支付失败分别展示真实错误，只有订单已成功创建后才提示“订单已保留”，不再把所有异常误报为商户资质审核中。
- 移动端 `vue-tsc --noEmit`、受影响 Vue 文件精确 ESLint、`pages.json` 解析、H5 最终正式 build、`git diff --check` 全部 EXIT 0；最终代码在同一状态下重复构建通过。
- 本地隔离交互回归覆盖二维码生成、横向溢出、订单创建失败、支付失败后订单保留及嘉宾邀请跳转：二维码画布 549×549、数据长度 16,414，失败项为 0；未调用生产支付或创建生产订单。
- 生产只读视觉矩阵覆盖圈子工作台/管理/收益/角色/嘉宾，从业者首页/专业版/报告/账本/案例/品牌，以及创作者工作台/认证/资质/评价共 15 页；全部无横向溢出、无 JS/API 错误、无不当占位文案，关键空态与权限态真实呈现。
- H5 发布包 SHA256：`FAFBFC174D686EE10B01154EE9E73CDD1CA612D47EBE7C23A3D88E099E74D1CB`，大小 2,969,599B。
- H5 原子发布成功，上一版保留于 `/opt/guoxue/apps/mobile/h5/dist.prev-baa9cb34`；新入口 `index-CYDT_u9P.js` 外网 200，旧 `index-DPaEW2N5.js` 404，外网 health 200，生产标签 `prod-20260720-circle-workspace-launch` 精确指向代码提交。
- 本批无后端重启、无 DDL、无生产支付、订单或其他业务数据写入。

## 七、发布门禁与交付口径

每批独立提交、独立回滚。最终发布前必须同时满足：

- 工作树无非本轮文件；`git diff --check` 通过。
- 移动端与管理端类型门禁通过，服务端 `tsc` 通过。
- 受影响模块定向 Jest 通过；关键资金/权限路径补专项回归。
- H5、管理后台、服务端正式 build 通过且无新增警告。
- 两段式原子部署，保留上一版可回滚目录。
- 外网 health 200、入口 hash 更新、核心页面和 API 生产实测通过。
- 默认不改真实运营数据；对已明确授权的测试/种子数据，可在精确识别、先备份、打批次标记并保留回滚记录后，为验收效果增删改，资金与身份数据仍逐处双查。
