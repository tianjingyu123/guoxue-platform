# 坏味道 P2-5 · 第二梯队巨型 service 拆分 · 交接任务包（2026-07-05）

> 沿用 **shop 四域拆分范式**（已上线验证）。给后续窗口/执行者接手。

## 一、范式（照做即可·同 shop）
1. **facade 委托模式**：原 XxxService 保留为薄 facade，controller/marketing/外部消费者**零改动**、公共 API 逐字节不变 → **行为零变化**（风险最低）。
2. **纯搬家逐字复制**：抽取的方法**逐字**从原 service 复制到子 service（勿手写重构，防偏差）；跨域调用改 `this.xxx` → `this.xxxSvc.xxx`。
3. **叶子优先·破环**：先抽不被其他域依赖的叶子域；有循环依赖时把「被双方依赖的方法」独立成一个叶子 service（如 shop 的 refundExpiredGroupBuys 归退款域）。
4. **每域验收**：tsc0 + 抽取方法**逐字 diff 校验**（`awk` 提方法体 vs .backup）+ **全量 jest 回归**（当前基线 422 套 5536）+ 子 service 加入 module providers + spec 的 TestingModule providers。
5. **<600 行/文件**；response 结构/any 保持原样。

## 二、course（✅ 已拆完·1662→~130行facade+6子service·未部署）
**✅ 全部完成（本窗口收官）**：起步3域（Recommend/Admin/Creator）+ 收官4域，course.service.ts 降为薄 facade（CRUD/审核/品类树 + 全量委托）。
- CoursePurchaseService（叶子·破环）：purchase/checkAccess/setMemberFree/isActiveMember/checkCourseExpiry/getUserValidCourses/getMyCourses。`checkAccess` 独立成叶子，学习域/评价域注入它。
- CourseLearningService：章节CRUD/getChapterContent + 进度 + 学习看板(getMyLearningDashboard/getCourseStats) + 完成证书。注入 CoursePurchaseService.checkAccess。（getCertificate 实际未用 getBrandName·任务包原估有误·已核实）
- CourseWorkService：作业 + AI评分(含scoreWorkViaAi) + getStudyPlan + getFlashSale。复制 getBrandName helper(注入@Optional systemService)。
- CourseReviewQaService：评价 + 问答(含classifyQuestion)。注入 CoursePurchaseService.checkAccess(createReview) + 复制 getBrandName。
- **验收**：tsc0 + course模块5spec/132用例全绿(真实子service执行) + 全量422套5549全绿(含e2e)+套件数不变(未增删spec)。范式=facade委托·纯搬家逐字·行为零变化。

**剩余待拆 service**（circle/recommend/offline·见下）。

## 三、circle（1739·最大最敏感·未拆）
⚠️ 有入圈支付/知识库/成员/帖子/机器人等·较敏感。**先通读全文理调用图再定域**（同 shop 起手）。粗划：圈子CRUD+入圈支付 / 帖子+互动 / 成员+排行 / 知识库 / 机器人助理 / 公告。破环点需实测（入圈支付被多处依赖）。

## 四、recommend（1535·✅已完成 2026-07-05）
**facade 委托·纯搬家零行为变化拆分完成。** commit `e12e75e1`（本地未部署，攒批）。子 service 在 `recommend/services/` 子目录(scene/scene-core/insert/select)。

最终域划分：
- **RecommendService**（facade·415行）：getRecommendations 主编排 + 去重 getOwnedSet + 旧版兼容 related/personalized/trending + 日志 logInteractions + 统计 getRecommendStats + 工具 applyDiversityInterleave/cacheKey/generateRecommendId + 全量委托。controller/外部消费者(article/smart-feed)零改动。
- **RecommendSceneService**（612行）：dispatch 场景分发 + 7 个上下文场景(sceneArticleDetail/scenePaipanResult/sceneSameCity/sceneCourseDetail/sceneProductDetail/scenePaymentSuccess/sceneCourseLearn)。注入 coreSvc。
- **RecommendSceneCoreService**（362行）：基础召回场景(sceneGuessLike/sceneFallback/sceneEmptyState/sceneSearchEmpty/sceneConversationGuess/sceneContactsDiscover) + getCollaborativeForUser/applyOverrides + 策略融合(collaborative/userProfile/vectorRecall/tfidf/embedding/abTest)。**破环点**：sceneFallback/sceneGuessLike 被上下文场景域依赖，独立成基础域供其单向注入（避免循环依赖）。vectorRecall.setProvider 随向量召回使用域下沉至此构造函数。
- **RecommendInsertService**（201行）：运营分区强插 insertContent/removeInsertedContent/applyInsertRules/applyStationPicks/fetchContentItemForInsert。
- **RecommendSelectService**（47行·叶子）：各内容类型 Prisma select 片段(article/course/product/circle/video)。**被 Scene/SceneCore/Insert/facade 四方共享**，独立成叶子供注入。

验收：tsc 0 错误 · recommend 模块 jest 21 套 186 用例全绿 · 全量 jest 422 套 5551 用例全绿（套件数不变，用例 +2）。

## 五、offline（1298·最小·未拆·已加固分页17处）
线下驿站·杂(商品/预约/核销/结算/讲师)。粗划：驿站CRUD+主页 / 课程+报名 / 商品 / 核销签到 / 结算 / 讲师预约。

## 六、部署
course 起步(ee2220dc) + 本文档等 docs 未部署。建议：**每个 service 完整拆完后一起部署**（避免半拆状态多次部署）。部署流程见 docs/progress/部署方案-shop拆分与坏味道优化批-20260705.md（纯后端快进·bundle→ff(用显式commit hash避refs/tmp-*歧义)→build(NODE_OPTIONS=6144·PID判完成)→pm2 reload guoxue-server cluster×4零停机→冒烟）。

## 七、工作量提示
每个 service 拆分 ≈ 1× shop 拆分（一个专门会话级别）。建议一个 service 一个会话完整做完+部署+销号，勿并行（单文件·多 agent 会冲突）。circle 最敏感建议最后做或格外谨慎。
