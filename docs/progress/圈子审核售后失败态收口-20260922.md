# 圈子审核与售后失败态收口（2026-09-22）

本批只修查询与展示，不改变退款或入圈审批规则，不执行真实审核、退款或余额操作。

| 页面 | 原断点 | 现行为 |
| --- | --- | --- |
| 圈主管理 → 退款申请 | 待审退款接口失败被适配层吞掉，页面显示“暂无待审退款” | 页面使用显式错误模式；失败显示“未确认是否有待审事项”并可重试，已加载的加入申请仍可查看 |
| 会员与售后 | 我的退款接口失败被当作空记录，钱包接口失败被当作 0 元余额 | 主数据读取失败显示重试；余额显示“暂未确认”，不呈现假零；无记录只在成功读取后出现 |
| 会员与售后 → 会员事项 | 已加入圈子列表失败时入口消失 | 保留非阻塞错误入口，用户可点此重试，不影响已经加载的退款记录 |

导航返回按钮避让微信胶囊并扩展至至少 44px。验证：移动端类型检查通过；`tests/release/circle-refund-states-browser.mjs` 使用本地接口夹具验证失败、局部失败及重试，无外部业务写入；最终小程序构建与 WXML 校验通过，主包 1.63 MB、`pkg-circle` 0.97 MB。真实账号退款、审批、权限和真机页面仍待单独验收；本批不是生产发布或资金验收。
