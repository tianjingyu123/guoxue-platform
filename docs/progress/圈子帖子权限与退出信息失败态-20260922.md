# 圈子帖子权限与退出信息失败态（2026-09-22）

帖子详情原先在成员身份读取失败时回退成无角色，圈主可能被展示普通成员“举报”入口，治理菜单凭空消失。现在角色查询使用严格读取；查询中显示确认状态，失败显示明确重试，只有确认角色后才给出治理、作者删除或举报操作。菜单操作区至少 44px。实际治理与举报仍由后端校验，本批不触发写入。

退出圈子页原先将成员状态读取失败当成未加入，并静默省略成员信息；使用天数前端最小计为 1 天，与后端退款测算的整天数可能不一致。现在详情与成员状态分别读取，状态失败明确展示“待确认/点击重试”，不呈现未经确认的剩余天数；已用天数采用与后端相同的向下取整口径。退款金额仍只以服务端实时预览为准。

验证：移动端类型检查通过；本地浏览器模拟身份查询先失败再成功，确认未知时不误示“举报”、重试后恢复圈主治理菜单，无写请求。微信小程序构建通过；真实会员身份、退款申请和真机状态未覆盖。本批未部署、未提交退款。
