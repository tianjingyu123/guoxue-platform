# 圈内动态与精华完整浏览（2026-09-22）

圈子详情原来只读取推荐流首 20 条，随后显示卷尾；精华栏目仅在这 20 条里筛选，可能把第 21 条之后的精华误判为“本圈还没有精华内容”。服务端现有分页与 `isEssence=true` 查询已足够，本批只完成移动端接线。

- 推荐流以 20 条为一页，未读完时显示“已显示 X/Y”和加载更多；续页失败保留已有帖子并可原位重试。读完才显示卷尾。
- 精华栏目独立请求服务端筛选结果，拥有自己的加载、失败、空态和分页；不再依赖推荐首页的样本。
- 两栏目点赞数同步更新，失败同步回滚；举报从当前栏目对应帖子取内容。

验证：移动端类型检查通过；最终小程序构建、WXML 与兼容性校验通过，主包 1.63 MB、`pkg-circle` 0.97 MB；`tests/release/circle-detail-posts-browser.mjs` 以本地夹具覆盖 20+3 条分页、续页失败与重试、首屏没有但后续确有精华的情况；`circle-assistant-entry-browser.mjs` 13 组回归通过；`circle-ux-browser.mjs` 10 项本地 H5 体验验证通过；`circle-detail-error-state.test.mjs` 8/8 通过。以上接口均由本地夹具拦截，未对真实用户内容或余额写入。微信真机与线上服务端实际分页性能尚未验收。
