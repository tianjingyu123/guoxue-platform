# 支付与第三方排盘收口（2026-09-20）

## 本次范围

在隔离工作树 `receive/integrated-three-20260919` 完成 App 支付补齐和第三方排盘分享承接页。候选随后已按本记录末尾的正式发布流程部署到两台目标节点；未迁移数据库、未修改 DNS，也未触碰其他窗口的工作树改动。

## 已完成

### App 支付宝

- 沿用汇付 `A_NATIVE` 交易，不改变服务端下单、查单和入账口径。
- Android 优先调用支付宝官方 SDK 的 `payInterceptorWithUrl` 将汇付返回地址交给支付宝处理；SDK 不可用时退回已验证的支付宝 Scheme。
- `manifest.json` 已声明 Android 支付宝 SDK，正式云打包后才会进入 SDK 路径。
- 拉起或 SDK 返回不作为到账依据，页面继续按原订单查询服务端状态。

### App 微信

- App 微信按钮改为拉起既有微信小程序的支付页，小程序继续使用现有直连微信商户的 JSAPI 支付。
- App 向服务端申请该订单的微信小程序短链接；服务端先校验订单属于当前用户且仍待支付，再生成只含订单 ID 的链接。小程序重新读取金额与订单归属。
- 支付宝仍可作为 App 默认支付方式；微信作为用户主动选择的备用方式。
- 使用微信官方 `wxaurl.cn` 短链接拉起，正式构建不再依赖人工抄录 `gh_` 原始 ID。

### 第三方排盘

- App 在分享发生时读取当前第三方 WebView 地址，避免切换盘面后继续分享旧地址。
- `/app_p1.php` 分享地址转换成第三方公开 `/p1.php` 地址；拒绝登录、授权、支付、订单、签名和未知参数链接。
- 微信好友和朋友圈优先分享 `gx.yrydai.com` 的热卜 H5 承接页；承接页内嵌第三方公开盘面。截图及系统分享保留作降级出口。
- 当前安全白名单只接受已知公开参数。首次拿到真实结果链接后须核对其参数集合；未知参数不会被静默放行。

## 验证结果

| 状态 | 项目 | 证据 |
| --- | --- | --- |
| 已完成 | 支付宝拉起、回退、查单与防重复发起 | `mobile-huifu-alipay-native.test.cjs` |
| 已完成 | 微信小程序短链接、本人待付订单校验、启动参数校验、旧安卓拦截移除 | `mobile-wechat-mini-payment.test.mjs` |
| 已完成 | 排盘链接过滤、当前页承接、微信网页卡片与截图降级 | `mobile-legacy-paipan-share.test.mjs` |
| 已完成 | 针对性测试 | 53/53 通过 |
| 已完成 | 移动端类型检查 | `vue-tsc --noEmit` 通过 |
| 已完成 | H5 正式资源构建 | 使用 `api.rebugx.cn`、`gx.yrydai.com/h5/`、`static.rebugx.cn` 构建通过 |
| 已完成 | App 资源构建 | 构建通过；微信启动已改用服务端短链接，无 `gh_` 构建参数 |
| 已完成 | Android 云打包与签名连续性 | 传统云打包 APK 的包名、245 构建号和签名证书与当前已发布 245 一致；支付宝 SDK 已进入 dex；覆盖安装后应用正常启动且无崩溃 |
| 尚未覆盖 | 支付宝真机 SDK 拦截 | 必须用包含支付宝 SDK 的新云打包 APK 做一笔最小金额支付并核对服务端到账 |
| 尚未覆盖 | App 拉起正式小程序并返回 | 需部署新增短链接接口，并用正式/体验版小程序真机验证 |
| 尚未覆盖 | 排盘分享接收端 | 需用一条真实盘面结果地址验证微信卡片打开、H5 内嵌内容与盘面一致、切盘后链接变化 |

## 发布顺序与回退

1. 随服务端候选部署订单短链接接口；目标环境已有的小程序 AppID/AppSecret 用于调用微信官方接口。
2. 构建新的 Android 安装包；支付宝 SDK 配置只有重新打包后生效。
3. 用测试订单分别验收支付宝、微信小程序支付；只以服务端订单状态与支付通知为准。
4. 部署 H5 承接页后，用真实排盘链接在微信接收端验证内嵌和返回。
5. 验收通过后再发布版本；本次代码不要求生产数据库迁移。

回退时恢复上一版服务端、APK/H5 产物即可。数据库结构未改；未完成订单仍按原查询与关闭规则处理。若支付宝 SDK 路径异常，可先关闭新包发布并保留旧 Scheme 版本；若微信短链接调用未通过，隐藏 App 微信选项，支付宝仍可单独使用；若排盘 H5 内嵌不稳定，保留截图分享，不发布网页卡片入口。

## 目标环境复核

2026-09-20 由既有唯一云端写入任务通过腾讯云 TAT 只读核验：两台目标节点只有同一个 `guoxue` compose 项目；`pre-api.rebugx.cn`、`api.rebugx.cn` 与 `gx.yrydai.com` 进入同一组 Nginx/server backend，静态目录也共用，未发现独立的 pre/stag/test/uat 容器或卷。现有发布脚本会重建共享服务，因此不能把部署到 `pre-api` 表述为隔离预发布。

- 国学2：`inv-e8vqrj0emd` / `invt-e8vqrj0eme`，SUCCESS。
- 国学1：`inv-e8vqs00vkb` / `invt-e8vqs00vkc`，SUCCESS。
- 发布前共享线上后端尚未包含提交 `20623e12e6eaba8771e5ec921c37d3564f934555`；用户随后明确批准正式发布，最终结果见下节。
- 环境核验阶段没有云端变更；正式发布采用两节点顺序切换，并为每个节点建立独立回退目录。

## 正式发布记录（2026-09-20）

用户后续明确批准正式发布。候选提交 `5525b4343a32f4c85bd3f3ea54106c71e7490ea4` 已按国学2、国学1的顺序发布；未执行数据库迁移，未修改 DNS，未发起真实支付。

| 节点 | 发布执行 | 镜像 | 回退目录 | 验收 |
| --- | --- | --- | --- | --- |
| 国学2 `ins-0sen8yvf` | `inv-e8vrak01n3` / `invt-e8vrak01n4` | `sha256:dfef6c60ec26d2d4f126f8c513efb13d0755954cad3ba81aaec3d58e725b6982` | `/opt/guoxue/backups/pay-paipan-5525b434.g6Hpa7xW` | healthy；ready 中 db/redis 均 ok；运行产物检出新路由；未登录 401 |
| 国学1 `ins-n0sqc627` | `inv-a8vrnr0uv5` / `invt-a8vrnr0uv6` | `sha256:2a51e045922f6374bb9bc05504d37459c014f0619421d91e41c340b820dac6f0` | `/opt/guoxue/backups/pay-paipan-5525b434.FV0X588a` | healthy；ready 中 db/redis 均 ok；运行产物检出新路由；未登录 401 |

两节点构建产物一致：服务端树 `c7ee66bc27288cb491f064159faa863a0219e3e8080764de17d6491f2891092d`，后台树 `9b576ca9bbd504a9a796e2902375fce3253a085925090737cfc952e148310d8a`，H5 树 `455c461d4eaa93b7a6c7525af9702cf01d4f3b4d675fe2b62a2ab67d40bd894a`；已发布 H5 `index.html` 为 `b765d4b75e3b46dc5d89be91d29b0a094737d2c2371d44f8208da4b0a2a23dda`。公网 `api.rebugx.cn/h5/index.html` 与 `gx.yrydai.com/h5/index.html` 均返回该哈希，实际客户端接口 `/api/v1/shop/orders/nonexistent/pay/mini-program-link` 未登录返回 401。

`/shop/orders/...` 不带 `/api/v1` 时由 Nginx 根路径固定返回健康 JSON；移动端请求层会自动添加 `/api/v1`。因此接口在位证据同时采用镜像 revision、容器内编译产物检索和实际客户端路径响应，不能只用 401 判断路由存在。

## Android 云包核验（2026-09-20）

使用 HBuilderX 正式云打包复核原生能力。首次安全模式产物的证书指纹与现有发行包不一致，已判定为无效产物，仅留作问题证据，禁止上传和分发。随后改用传统云打包并使用既有正式签名，得到本地验收包：

- 文件：`D:/gx-deploy-91/artifacts/releases/android-1.1.0-245-pay-paipan-cloud/rebu-1.1.0-245-pay-paipan-cloud-traditional.apk`
- 包名：`com.rebu.apprebu`；版本：`1.1.0 (245)`；大小：31,070,959 字节。
- APK SHA-256：`D53BBE786382CE298214B701C0879E82315DFC852104C23E31997AD1405FD822`。
- 签名证书 SHA-256：`737c54a45e710bce7c6875a41a5a5c0c228c09bd31fc08df8e016364b61b9d67`，与当前已发布 245 完全一致；v1/v2 签名校验通过。
- dex 已检出 `com/alipay/sdk/app/PayTask`、`payInterceptorWithUrl` 和 `AlipayResultActivity`，证明支付宝原生 SDK 已进入安装包。
- 已在设备 `a07f9db3` 覆盖安装并保留原数据；系统回读 245，应用可正常进入已登录首页，未出现 `FATAL EXCEPTION`/`AndroidRuntime` 崩溃。截图：`D:/gx-deploy-91/artifacts/releases/android-1.1.0-245-pay-paipan-cloud/device-launch.png`。

当前公开版本 245 已经生效，因此上述同构建号 APK 只用于技术验收，不能替换公开文件。最终公开候选必须提升为 246，经同样的传统云打包、签名和真机验收后再上传，并新建 246 版本记录。

## 排盘真机进展（2026-09-20）

已在上述云包中进入第三方排盘，使用匿名测试数据（1980-06-15 12:30、男、北京房山区）完成八字排盘，结果页正常显示，证明 App 内嵌、无需再次登录、表单提交和结果返回链路可用。证据：`D:/gx-deploy-91/artifacts/releases/android-1.1.0-245-pay-paipan-cloud/device-paipan-result.png`。

第三方结果页当前未出现可识别的“分享”入口；其输入页有分享按钮，但不能据此证明结果页会调用已兼容的 `shareWX` 桥。接收端网页卡片、承接页和微信打开仍须在第三方结果页实际触发一次分享后验收，不能将“结果生成成功”记成“分享闭环通过”。
