# 热卜国学平台 · 后台管理端 优化进度跟踪

> 配套审计报告：`后台管理端-审计报告-20260629.md`
> 实时更新本文件记录各批次进展与待办

## 决策边界（2026-06-29 已拍板）
1. tenant 多租户 → 保留并完善
2. 诗词雅集后台 → 本轮新建（批次 5）
3. 密钥冻结域（IM/短信/直播打赏）→ 跳过待统一测试
4. 细粒度 RBAC → 移除装饰性权限树，保持静态 @Roles

---

## ✅ 批次 1 · 数据正确性地基（完成 2026-06-29）

**目标**：修复约 18 页"列表恒空 + 字段错配 + 端点 404"的功能性失效。

**根因**：后端 `ResponseInterceptor` 仅识别 `items/rows/list`（+total/page/pageSize）为分页响应；
finance/risk/churn/tenant 各 service 返回 `records/invoices/settlements/predictions/actions/tenants` 等键 → 不被解包 → 前端取数键对不上 → 恒空。

**已完成（前端按后端真实键兜底，不动拦截器以免回归）**：
- finance 6 页：WithdrawalApproval / InvoiceList / SettlementList / FundFreeze / ReportList / ReconciliationList
- risk 5 页：AlertCenter / AppealList / FraudList / RuleList / UserTimeline（量纲对齐 confidence vs riskScore、IP/deviceId 掩码、处置动作接通真端点）
- churn 3 页 + tenant 3 页：取数键/端点/入参修正（score→calculate、search name→keyword、recharge amount→quotaAmount）、apiKey 掩码
- fortune 2 页：后端补 `GET/PUT /fortune/admin/subscriptions`、`adminListRecords` 返回键改 `items`；前端路径全部修正、移除分享卡片死功能（后端无模型）、补三态
- 全部页补 loading/empty/error 三态，静默 catch → 错误态 + 重试

**验证**：admin `vue-tsc --noEmit` = 0 错误；server `tsc --noEmit` = 0 错误。

**⚠️ 需用户重启后端验证（沙箱无法 e2e）**：fortune 新端点 `/fortune/admin/subscriptions`（list/toggle）需重启后端确认非 404。其余前端修复连的是后端已有端点，重启前端即可见列表不再恒空。

---

## 📋 后端待办（前端已诚实降级/兜底，待后端补全）
1. **[治理] ResponseInterceptor 分页白名单**：仅认 items/rows/list；建议各 service 分页返回统一改用 `items`（改时需同步前端，否则回归）。当前前端已按实际键兜底，功能正确。
2. **缺失端点**（前端已诚实降级，待补后恢复入口）：
   - 发票拒绝 `PUT /finance/invoices/:id/reject`（schema 有 REJECTED 状态，controller 无端点）
   - 风控封禁/升级处置端点（或跨模块调 `users/:id/status`）
   - `getUserTimeline` 支持 action/日期/分页参数 + 联表用户基本信息
   - `listAppeals` 支持 type 过滤
   - tenant `/logs` 分页端点（如需独立日志页）
   - churnAction `remark` 字段
3. **[安全] 后端按角色脱敏返回**：IP/deviceId/apiKey/手机号/邮箱（当前前端掩码兜底）。
4. **运势分享卡片配置**：后端无模型，已移除死功能，待规划（存 SystemConfig KV 或建表）。

---

## ✅ 批次 2 · 资金与审核安全（完成 2026-06-29，vue-tsc 0 错误）

**资金/审核 假动作·错端点 修复**：
- shop/RefundList：拒绝退款原误调 `orderApi.cancel`（取消订单+丢原因）→ 切到售后单真实链路 `GET /shop/admin/after-sales` + `PUT .../:id/process {action:reject,remark}`；同意=真实退款 `orderApi.refund`。补三态。
- commission/WithdrawalList：补"打款"按钮调 `auditWithdrawal(id,{status:PAID})`+二次确认。（⚠️后端 auditWithdrawal 仅允许从 PENDING 流转→当前为"一步审核并打款"；APPROVED→PAID 两步需后端放宽守卫）
- coin/GiftList：编辑改 `PUT /coin/gifts/:id`；启停/发送（后端无 status 字段、唯一 send 端点走直播打赏扣管理员自己币）→ 诚实移除误用入口并注释。
- merchant-backend/MerchantAfterSales：`handleAfterSale?.()`（不存在·静默假成功）→ `processAfterSale(id,{action:approve/reject,remark})`。
- circles/CircleDetail：单条问答退款误调全局批量 `refund-expired` → 后端无单条退款端点，诚实降级（按钮禁用+tooltip 说明）。

**隐私脱敏 + 敏感操作权限闸**（前端纵深防御，后端守卫仍兜底）：
- users/UserList（含导出 CSV）、UserDetail、user/CapabilityAudit、member/MemberPurchaseList：手机号/邮箱/IP 掩码。
- v-permission 接入：封禁/解封限 SUPER_ADMIN+OPERATION_ADMIN；授予/移除角色限 SUPER_ADMIN；能力审批限 SUPER_ADMIN+OPERATION_ADMIN。敏感写操作补二次确认 + submitting。

**router 守卫**：station-backend、operator-backend 补 meta.roles（原任意管理员可进）。

**批次 2 新增后端待办**：
- WithdrawalList 两步流转：放宽 `auditWithdrawal` 允许 APPROVED→PAID
- GiftList：`updateGift` 支持 status 字段 + `getGifts` 取消 ACTIVE-only 过滤；如需"管理员向用户发放礼物"需新增不扣管理员币的赠礼端点
- CircleDetail：question 模块新增按 questionId 的单条退款端点
- v-permission 全局注册：确认 main.ts 已注册（UserList 已在用，应已注册）

## 📌 专项（已决策单列，不在本轮）· 资金审批流
**决策 2026-06-29：单列专项，本轮先推品质。** 以下资金操作当前直接执行、无审批、无留痕，作为后续独立专项（需后端审批状态机 + 前端审批界面 + 操作留痕）：
- InstituteFinance 发放分红、HuifuConfig 退款、coin/RechargeList 充值到账、CommissionConfig 改分佣比例

## ✅ 批次 3 · 假数据清除（完成 2026-06-29，vue-tsc 0 错误，Math.random 全平台清零）
- ai/ContentQuality：品类质量分柱图真连 `GET /ai/quality/stats`（四维评分）；近30天采纳率趋势无数据源→诚实空态
- SearchAnalytics：AI效果趋势/场景饼图无聚合数据源→诚实空态；零结果关键词保持真连
- users/UserPush：预估人数改真连 `userApi.list` 取真实 total（带标签/活跃筛选时标注"以结果为准"）；发送改真实分群端点 `POST /users/push/by-tag`
- dashboard/GoodsAuditDashboard、FinanceDashboard：删图表假兜底，加载失败→错误态+重试，无数据→空态
- system/BannerAdmin、HomepageConfig：删 getDefaults 硬编码回退，失败→错误态（HomepageConfig 保留"无配置首次初始模板"但与加载失败严格区分）
- courses/CourseCategoryManage：删 getDefaultTree 假树 + "恢复默认"按钮，失败→错误态
- lives/LiveDashboard：词云装饰配色 Math.random→确定性轮转调色板

**新增后端待办**（统计端点缺口，前端已诚实空态）：AI采纳率时序、质量分按品类聚合、搜索量时序、搜索场景分布、推送人群精确预估(dry-run)

## 设计令牌体系（批次 4 依据）
`styles/tokens.css` 已是完整体系：故宫红 `--color-primary:#C41E3A`、国学金 `--color-gold:#C9A96E`、宣纸色 `--color-bg-page:#FAF8F5`、文字 `--color-text-title/body/secondary`、状态色、间距/圆角/阴影/字体全套。批次 4 将把 138 页硬编码 #hex 映射到这些 token。

## ✅ 批次 4 · 设计与三态地基（完成 2026-06-29，vue-tsc 0 错误）
全平台 209 页三态补齐 + 设计 token 横扫（10 个并行 agent 分 4 批，严格文件隔离）：
- **三态**：空态 el-empty 78→156 页；错误态+重试 →176 页；静默 catch/伪装空态全面改为真错误态(el-result/el-alert+重试)，不再回退假数据；写操作补 submitting。
- **token 化**：var(--token) 使用 127→152 页；scoped style 硬编码 hex 按映射表收口。echarts 图表色、大屏深色主题、命理盘面专用色、一次性装饰色按规则保留。
- 全局基础设施(App.vue el 主题变量全套 + global.css 组件深度定制 + tokens.css 完整令牌)本就完整，本轮补齐页面级一致性。

## ⏳ 批次 5 · 前后台缺口补齐（进行中）
### ✅ 诗词雅集后台（全栈新建，完成 2026-06-29，端点已 curl 验证）
- 后端：poetry.controller/service 新增 admin CRUD（诗词/分类/诗单，13 端点，RolesGuard 限 SUPER_ADMIN/OPERATION_ADMIN；admin 路由声明在通配 :id 之前避免被吞）
- 前端：views/poetry/ 新建 PoetryList / PoetryCategoryManage / PoetryCollectionManage（仿古籍范式，三态齐全、token 化、submitting）
- 接入：poetryAdminApi（13 方法）+ router 3 路由 + menu.config "诗词雅集"菜单组
- 验证：server build + admin vue-tsc 均 0 错误；pm2 restart 后 GET/POST/PUT poems、categories、collections 全 401，/poetry/home 200
### ✅ 短视频审核流（全栈，完成 2026-06-29，端点已验证）
- 后端 video：Video 表加 `auditReason` 列（ALTER TABLE via prisma db execute）+ `PUT /videos/admin/:id/audit`（approve→PUBLISHED / reject→REJECTED+原因），RolesGuard 限超管/运营/内容品控
- 前端 VideoList：待审筛选 + 通过/驳回动作（驳回强制填原因）+ 行级 loading + 二次确认
### ✅ 举报内容查看（全栈，完成 2026-06-29，端点已验证）
- 后端 interaction：`GET /interaction/report/admin/:id/target` 按 targetType switch 查被举报内容摘要（POST/COMMENT/ARTICLE/COURSE/PRODUCT/VIDEO/CIRCLE/LIVE/USER，USER 手机号脱敏），只读不改处理逻辑
- 前端 ReportList：「查看内容」弹窗展示摘要，三态齐全
### ✅ 商品品控后台（事后抽查+违规下架，完成 2026-06-29，端点验证）
- 后端 shop：`PUT /shop/admin/products/:id/moderate`（takedown/restore/warn，reason 走审计日志，无 schema 变更），GOODS_AUDITOR 启用
- 前端 新建 ProductAudit.vue（全平台巡检+下架/恢复/警告）+ router `/shop/product-audit` + 菜单"商城管理>商品品控"
### ✅ 创作者中心后台（完成 2026-06-29，端点验证）
- 后端 video-creator：3 admin 端点（创作者列表/详情/提现记录，复用现有表无 schema）
- 前端 新建 CreatorList + CreatorWithdrawals + router `/creator/*` + 菜单"创作者中心"组
- ⚠️ **诚实发现**：创作者提现=虚拟币即时到账（无 PENDING 审批流），提现页做成监控视图未造假审批。真审批流需新表 VideoCreatorWithdrawal（**待拍板**）
### ✅ 排盘记录补全（完成 2026-06-29，端点验证）
- 后端 paipan：6 类型同 PaipanRecord 表，修复 admin 查询校验 + 新增 admin 详情端点（修越权 bug）
- 前端 PaipanRecords：全 6 类型筛选（BAZI/ZIWEI/QIMEN/YANGPAN/LIUYAO/DALIUREN）

### ✅ 圈子退款审核（完成 2026-06-29，端点验证）
- circle-refund 后端已有 admin-pending + admin-review 双审核端点（复用，无 schema 变更），前端新建 CircleRefundAudit.vue + router `/circle-refunds` + 菜单"社区管理>圈子退款审核"
### ℹ️ 站长提现审核（伪缺口，已澄清）
- commission/WithdrawalList 早已是提现审核页（含分站列/通过/拒绝/打款/三态），operator/Earnings 仅收益看板；已在 Earnings 加"提现审核"跳转入口，未重复造页
### 批次5 剩余（后续/推迟）
- 驿站核销/商品/预约后台（已决策**推迟下一轮**）
- 创作者真提现审批流（需新表 VideoCreatorWithdrawal，**待拍板**；当前虚拟币即时到账=监控视图）

## ✅ 批次 6 · 审计与权限纵深（完成 2026-06-29，build + vue-tsc 0 错误）
- **@Auditable 落地**：49 处/9 controller（shop/commission/merchant/user/finance/coin/content/video/poetry 关键写操作），操作日志 action 从 HTTP 方法名升级为语义（退款/封禁/审核等）；排除 circle/station 避冲突
- **移除装饰性 RBAC**：RolePermissionManage 从"可配置权限树（不生效）"降级为"角色权限只读总览"+ 明确说明固定角色模型，消除误导
- **审计日志导出**：AuditLog 加导出 CSV（调后端已有 /system/export/audit-logs）
- **诚实**：OperationLogList 无 before/after 字段，未造假 diff（如需 diff 待后端补字段）

## 批次路线（总览）
1. ✅ 数据正确性地基
2. ✅ 资金与审核安全
3. ✅ 假数据清除
4. ✅ 设计与三态地基
5. ✅ 前后台缺口补齐（诗词后台/短视频审核/举报内容/商品品控/创作者后台/排盘记录/圈子退款审核 7 缺口完成；驿站推迟、创作者真审批流待拍板）
6. ✅ 审计与权限纵深（@Auditable 落地、移除装饰 RBAC、审计导出）
7. ✅ 可视化升级（6 工作台差异化 + 角色专属操作入口 + 真实环比）
8. ✅ 收口验证（vue-tsc/Math.random/if(true) 全 0；最终报告见 `后台管理端-最终报告-20260629.md`）

## 🎉 8 批次全部完成（2026-06-29）
最终验证：admin vue-tsc 0 错误、Math.random 0 文件、if(true) 0 文件、路由含 roles 守卫。

## 🚀 密钥前完工冲刺（2026-06-29，把所有不依赖密钥的剩余项一次性做完）
- **波1 补缺失端点**（诚实降级处全恢复真连）：发票驳回、风控处置联动(封禁/升级)、用户时间线分页+筛选+联表、申诉 type 筛选、圈子问答单条退款、礼物启停、租户日志、churnAction 结果展示
- **波2 统计端点**（解锁环比/时序）：工作台昨日环比(内容品控/超管)、AI 质量评分趋势(/ai/quality/trend·诚实正名非采纳率)、搜索量趋势(/search/admin/trend)、推送精确预估(/users/push/estimate)；无历史数据的指标诚实留白
- **波3 业务大件**：
  - **资金审批流专项**：新建 FundApproval 表 + fund-approval 模块；分红/退款/充值/分佣变更 4 处全改"发起→审批→执行"(原子认领防重+事务+失败回滚)；FundApprovalCenter 审批中心（菜单"财务管理>资金审批中心"）
  - **创作者真提现审批流**：新建 VideoCreatorWithdrawal 表；submitWithdraw 改冻结(balance→frozen)+PENDING；审批 approve/reject/pay 全事务一致；CreatorWithdrawals 升级真审批台
  - **驿站后台**：核销/商品/预约 3 个跨驿站只读管理页 + admin 端点 + 菜单"线下管理"
- **验证**：2 表迁移(db execute 非 db push)+generate+build；所有新端点 curl 401；admin vue-tsc 0；后端 jest **250 passed/16 suites**(含 coin/commission e2e·审批流测试已同步)

## 剩余（仅这些·均不可在密钥/实测前闭环）
- 密钥冻结域：IM 私信 / 短信 / 直播打赏对账（待用户提供密钥）
- 真人登录走查端到端带 token 业务流（待用户实测）
- 可选完整化：WithdrawalApplication.paidAt / Invoice.rejectReason 两列（当前不依赖·驳回原因已记审计日志可查）

## ✅ 批次 7 · 可视化升级（完成 2026-06-29，vue-tsc 0 错误）
6 大角色工作台从"同质化只读数字卡"升级为"角色专属工作起点"：
- **角色专属操作入口**：每工作台 5-6 个直达高频管理页的快捷操作（router.push，按 meta.roles 过滤避免 403）+ 待办数字角标（真实计数）
- **真实环比同比**：财务月环比(lastMonthRevenue/monthOverMonthGrowth)、运营/客服日环比(today-overview.trendData)；超管/内容品控/商品品控无对比数据→诚实留白绝不造假
- **差异化 KPI**：财务=资金/退款/提现；运营=内容/活跃/转化；客服=工单/举报/服务量；内容品控=待审/通过率；商品品控=待巡检/违规/缺货；超管=系统健康/治理入口
- **顺手修 bug**：客服工作台读错字段(ov.pendingReports→stats)、删后端恒 0 占位卡、趋势图错配修正

---
## 关键提醒（每次续接先看）
- **本轮只动 admin 前端 + fortune 2 个后端文件**；git 工作树有大量 mobile 阶段历史未提交改动，勿混提交。
- **响应取数键铁律**：后端 ResponseInterceptor 只认 items/rows/list 为分页；其余 service 返回键（records/invoices/predictions/tenants…）不解包，前端按实际键兜底，**改后端键必同步前端**。
- 所有"诚实降级/空态"对应的后端缺口都已记入本文件待办，勿当成 bug 重修。
