# 商城与课堂全链路体验审计及候选优化（2026-09-22）

状态：独立工作树中的代码候选，待主线评审；未部署、未连接生产、未改真实交易或课程数据。分阶段执行方案见同目录《商城课堂体验提升路线图-20260922.md》。

## 设计目标与范围

延续首页卡片候选 `764f96556`，以清晰的信息层级、真实内容、足够大的点击区域、可预期的反馈和窄屏可读性为准则，检查商城与课堂的首页、分类检索、列表卡片、详情入口及下一步操作。视觉以苹果系统的克制、留白和一致性为参考，不照搬其品牌元素。这里的“苹果级”是设计目标，不是已取得真机或线上验收结论。

## 审计与处理

| 路径 | 核对结果 | 本候选处理 | 仍需验收 |
| --- | --- | --- | --- |
| 首页 → 商城首页 | 商品双列边距偏大；“专题会场”实际跳普通分类；空商品区无解释 | 缩小页面边距和卡间距；文案与目的地一致；增加商品空态 | 不同数据量、促销入口配置和窄屏截图 |
| 商城首页 → 分类 | 分类入口携带 `cat`，分类页未读取，落地后选中错误 | 读取并应用 `cat` 参数 | 后端分类 ID 与链接值对齐 |
| 商城分类 → 商品 | 左侧分类栏和右侧双列卡并置，商品内容区过窄；搜索清空不刷新 | 右侧改为横向商品卡；添加清空入口和键盘操作 | 长分类名称、商品极长标题与筛选弹层真机检查 |
| 商城价格筛选 | 上限 1000 元被当成“不限”，更高价格被静默忽略；关闭面板会留下未确认的输入 | 筛选改为草稿确认，空值表示不限；确认时检查非负、最多两位小数及区间顺序 | 真实商品价格范围、软键盘与小程序输入行为 |
| 商品卡 → 详情 | 原卡叠加标签、销量、现货、折扣和假按钮，价格缺失可显示 ¥0 | 保留真实认证、标题、简介、价格和销量；缺价显示“查看价格”；库存为零显示缺货 | 商品封面缺失、价格异常数据的真实接口检查 |
| 商品详情 → 购物车/结算 | 规格/库存拦截与结算路由已存在；加载失败无返回途径；写死售后和发货承诺缺少规则依据 | 错误态增加键盘可达的重试与返回；撤下无依据的固定承诺，提示以订单信息为准 | 规格、库存、优惠、地址、支付及售后均需业务测试，不能由界面构建代替 |
| 课堂首页 → 课程分类 | 首页课程卡层级复杂，卡间距偏大；首页数据与分类数据同批加载，一个失败会丢掉另一个 | 缩小外边距和卡间距；两个接口独立接收成功结果 | 接口部分失败时的真机视觉检查 |
| 课堂首页“新上架” | 旧实现直接取通用课程列表前四项，与“最新”标签不保证同口径 | 改用现有 `/courses?sort=newest` 取前四项，失败时隐藏区块，避免旧数据冒充新课 | 后端 `createdAt` 排序、审核与上架时间的业务定义需核对 |
| 课堂分类/排序 → 课程 | 窄屏左侧栏压缩内容；快速切换分类或排序时旧请求可能覆盖新列表 | 精简右侧列表卡；通用 `useList` 对刷新和分页加版本校验 | 分类、排序、搜索在真实 HTTP 下的结果与分页 |
| 课程卡 → 详情 | 原卡生成“你将学到”文案、缺讲师显示“平台精选”、缺价显示 ¥0；缺图时封面重复完整长标题 | 重构为封面、真实标题/简介/讲师/课时与价格；缺图时使用简洁水印；无发布字段不杜撰 | 不同课程封面比例、长标题和缺字段的真机检查 |
| 课程详情 → 开通/学习 | 课程加载错误可重试但无返回；订阅、支付和继续学习入口已有 | 错误态增加可键盘操作的重试/返回 | 免费订阅、付费开通、权限变更、课程播放端到端测试 |
| 课程详情附加内容 | 评价与课程、章节用 `Promise.all` 串联；评价失败会遮挡整页，推荐慢请求会延迟课程呈现；评价失败可能被误写成“暂无评价” | 课程和章节作为主内容加载，评价独立显示加载/失败/重试，推荐异步降级；过期响应不覆盖新加载 | 合成 HTTP 注入评价失败、推荐慢响应与真机复看；评价总数口径需后端核对 |
| 课程权限与支付后开通 | 权限请求失败被降级为“未购”；支付结果回调直接本地解锁，即使服务端课程权限尚未就绪 | 详情页区分已授权、未授权和暂无法确认；核验中或异常时不显示购买按钮，允许重试；支付确认后回源确认权限再展示已开通 | 微信收银页回跳、权益延迟/失败、免费课订阅状态与试看权限仍需独立测试和接口口径确认 |
| 免费课收录 | 免费课访问权限不等于订阅订单；登录用户即使未订阅也可通过权限检查，详情页因此隐藏“免费订阅”，课程未进入“我的课程” | 复用本人已支付课程接口增加课程 ID 筛选；详情页分别核对学习权限与收录状态，未收录时保留“加入我的课程”且可继续学习 | 服务端 Jest 因隔离 Prisma 客户端未生成而未运行；真实 HTTP、回跳与小屏双按钮待验 |
| 购物车 → 结算 | 原价低于现价仍显示划线价，优惠可为负；部分关键控件缺少键盘语义 | 仅展示真实优惠；补购物车管理、勾选、重试、结算入口的语义与键盘操作 | 删除/改量/勾选与库存变更的真实 HTTP 回归 |
| 结算 → 建单 | 建单前启动 15 分钟“支付”倒计时；运费写死免运；试算失败仍称“实付” | 移除建单前假倒计时；运费提示以订单为准；试算中阻止提交，失败时明确为预估；校验支付方式 | 最终订单金额、运费、券、批量建单与支付时限需后端环境核验 |
| 批量建单失败重试 | 已建订单只按商品与 SKU 复用，若修改地址或优惠券可能错误复用；同商品重复行可能复用同一单 | 已产生订单后限制更改结算选择，重试前核对地址、券和商品数量；缓存按清单行区分 | 需要后端幂等键和订单恢复能力，尤其是网络超时后服务端已建单但客户端未收到响应的情况 |
| 订单详情价格说明 | 物品订单固定显示“包邮”，但订单详情适配器没有运费字段；“商品总额”也由订单原金额回填 | 改称“订单金额”，运费诚实提示“订单未单列”，避免虚构免邮事实 | 后端应返回运费和优惠明细后再恢复完整拆分，并核对支付票据 |
| 课程播放 → 完课 | 课时错误态缺返回；完成横幅在证书接口成功前宣称证书已生成 | 增加重试与返回语义；完成横幅只说已完成课时，并提供证书入口 | 媒体播放、进度同步、证书生成失败与权限更新真机检查 |
| 我的学习 → 继续学/证书 | 原横排封面、长标题、进度和按钮争同一屏宽；已完成课程无在学项时缺继续浏览入口 | 两列卡片并把操作位移到内容下方；增加浏览课程入口、键盘操作和真实证书语义 | 长标题、大字模式、课时进度与证书资格真机核对 |
| 购物车触控与结算弹层 | 勾选和改量触控目标只有 40–48rpx；地址、优惠券弹层缺显式关闭与 H5 焦点闭环 | 触控目标扩大至 88rpx；弹层增加关闭入口、Escape、焦点限制和底层滚动锁 | 微信小程序/原生端弹层独立滚动与屏幕阅读器验收 |

课堂的作业提交、问答、证书详情、评价、讲师页与课程发布页，以及商城的订单和售后页面也属于完整业务体验，但本候选没有逐项做真机或交易验收，亦未修改这些页面。它们不能因本次浏览与下单入口优化而视为“苹果级体验”已达标。主线接收后应按真实用户路径逐项验收，必要的后续改动另起候选。

次级页面还做了静态入口核对：课程问答、作业提交、证书详情、课程评价和讲师页均有加载失败重试入口；课程发布页已有提交失败反馈；商品评价页有错误、空态和返回路径；订单详情与退款页有错误重试及客服/售后入口。静态代码无法证明接口、权限或实际点击成功。讲师页的公开课程区因后端无数据源，按代码会持续显示“暂无公开课程”；作业页“讲师已复核”依赖尚未补齐的独立复核字段。这两项列为后端与业务决策，未伪造数据或文案来掩盖。

## 可访问性与信息真实性

- 商品和课程卡保持整卡唯一跳转语义，支持 Enter/Space；不再用装饰性“立即选购”“查看课程”伪装第二操作。
- 两类卡片的屏幕阅读标签只包含有来源的价格、库存、课时和讲师。没有价格时提示进入详情核实，不将缺失值写成免费。
- 购物详情的“假一赔十”“48 小时内”“7 天无理由退换”及购买弹层同类固定文案在仓库中未找到规则依据，因此本候选移除；后续可由经过业务批准的规则字段重新展示。
- 搜索清空、重试和返回补上明确语义与键盘入口。筛选与排序原有弹层、遮罩和焦点管理保留。
- 结算金额在后端试算不可用时明确标为预估；真正支付金额应在订单支付页核对。结算页不再自行制造支付时限。

## 本地验证

- `pnpm --filter @guoxue/mobile type-check`：通过。
- 用无效占位公开域名设置 H5 构建变量后运行 `pnpm --filter @guoxue/mobile build:h5`：通过。构建期间有现存 Sass legacy JS API 弃用警告。
- `node apps/mobile/scripts/test-use-list.cjs`：通过，覆盖快速刷新时旧结果不覆盖新列表、旧分页不混入新分类。
- 对本次改动文件运行 ESLint：0 errors；1541 条 warnings，主要为仓库既有 Vue 模板格式规则，未自动格式化大范围历史文件。
- 对购物车、结算、播放器补齐文件再次运行 ESLint `--quiet`：0 errors。
- 本轮触控与学习卡片改动后重新运行 `type-check`、H5 构建和 `build:mp-weixin`：均通过；微信构建完成 WXML、文本兼容性和包体积校验。两类构建均使用无效公开占位域名，不连接真实服务。
- 构建与产物校验不等于设备交互验收；尚未在微信开发者工具或真实 iOS/Android 设备上核对 320–430px 窄屏、系统大字、弹层滚动和屏幕阅读。
- 使用独立本地合成 HTTP 服务（`127.0.0.1:3987`）配合候选 H5 开发服务器（`127.0.0.1:5173`），实际渲染并目检商城首页、商城分类、课堂首页、课堂分类及“我的学习”五页。预览发现缺图课程在狭窄封面内重复完整标题并被裁切，已改用现有 `SmartCover` 简洁水印模式后复看；商品卡比例、课程列表及学习进度卡未见明显重叠或溢出。该验证使用合成数据和桌面浏览器居中移动容器，只覆盖视觉结构，不代表真实业务 HTTP、触控或真机效果。
- 视觉修正后再次运行 `type-check`、定向 ESLint `--quiet`、列表竞态脚本、H5 与微信小程序构建：均通过。微信构建重新完成 WXML、文本兼容性与包体积校验。真机运行和真实业务 HTTP 仍未覆盖。
- 本轮价格筛选与建单重试约束改动后，`type-check` 通过；两处文件定向 ESLint 为 0 errors、563 条 warnings（多数为既有模板格式规则）；`git diff --check` 通过。H5 构建首次缺少仓库要求的公开占位变量而停止，补齐三个无效公开域名后重新运行通过。微信小程序构建通过，WXML 语法、文本兼容性和包体积校验均通过。两类构建均未连接真实服务。
- 本轮课程详情附加内容、“新上架”排序与订单价格说明改动后，`type-check`、三处文件定向 ESLint `--quiet`、`git diff --check` 通过；用三个无效公开占位域名运行 H5 和微信小程序构建均通过，后者完成 WXML 语法、文本兼容性和包体积校验。尚未运行注入评价接口失败的真实页面 HTTP 测试，也未核对后端审核通过时间与创建时间的差异。
- 课程权限三态使用合成令牌测试 `node apps/mobile/scripts/test-course-access-state.cjs`，5 个场景通过：游客、已授权、未授权、网络失败、会话失效；`type-check`、定向 ESLint `--quiet`、`git diff --check`、H5 与微信小程序构建均通过，微信构建完成 WXML、文本兼容性及包体积校验。构建使用无效公开占位域名。真实服务端权限、支付回跳与权益延迟尚未运行。
- 免费课收录改动后，同一合成脚本扩至 9 个场景并通过；移动端 `type-check` 与定向 ESLint、服务端五处变动文件的 ESLint 和 TypeScript 语法转译检查通过。H5 与微信小程序构建均通过，微信产物完成 WXML、文本兼容性和包体积校验，均使用无效公开占位域名。尝试运行服务端 `course.service.spec.ts` 与 `course.controller.spec.ts`，两套件均在加载阶段因隔离工作树的 Prisma 客户端未生成而失败（`@prisma/client` 无 `Prisma` 导出等），0 个用例实际执行；未生成共享客户端，也未连接数据库。
- 课程详情新增本地合成 HTTP 浏览器验证：独立 API `127.0.0.1:3989` 与候选 H5 `127.0.0.1:5178`，Chrome headless 拦截所有非本地请求。`node apps/mobile/scripts/test-course-detail-browser.cjs` 通过：评价接口返回 503 时课程、目录和失败提示仍可见；免费课未收录时同时显示“加入我的课程”和“继续学习”，点击后收录按钮消失；320/375/430px 均无横向溢出，按钮位于视口内。320px 截图已目检。此为合成数据和本地 HTTP，不代表真实后端、设备触控或生产状态。
- 在本隔离工作树的 `node_modules` 生成与候选 schema 对应的 Prisma 客户端后，实际运行 `course.service.spec.ts` 和 `course.controller.spec.ts`：2 套件、92 用例通过，未连接数据库。该结果补齐上条“0 个用例执行”的环境缺口，不意味着真实权限 HTTP 已通过。
- 商品建单超时重试的服务端候选：`shop-order.service.spec.ts` 1 套件、66 用例通过（含新增同键复用、参数变化拒绝、失败重试）；隔离依赖中构建 `bazi-engine`、`ziwei-engine` 后服务端 `tsc --noEmit` 通过；`prisma validate` 使用无效的本地占位连接串通过，未连接数据库。新增迁移脚本未执行；本机 PostgreSQL 端口 5433 需要未提供的口令，尚无独立本地库并发、库存和券事务测试。移动端尚未发送此键，不能宣称超时重复建单已解决。
- `git diff --check`：通过。

没有运行真实业务后端 HTTP 或真机视觉验收；此前的本地 HTTP 仅提供合成数据。构建只证明源码可编译，不证明支付、开通或交易流程线上可用。尤其需要在小屏、大字、系统深色模式、弱网、图片失败、长标题、真实商品/课程数据和辅助技术下逐页验收。当前样式沿用平台浅色主题，没有单独完成深色模式设计。

## 主线接收与撤回

依次评审首页卡片提交 `764f96556`、商城/课堂浏览候选 `da9cc949a`、结算与播放入口补齐 `4ec6704ba`、触控与学习卡片 `7e85ee22`、封面修正 `8b03446e8`、价格筛选与建单重试约束 `00e510d72`、课程详情附加内容等 `1adcba1ad`、课程权限三态 `7e68e3ab5`，最后接收免费课收录状态提交。均在 `codex/home-feed-card-refresh-20260922` 独立分支；主工作区已有其他未提交改动，接收前应逐文件检查冲突，尤其商品详情页。撤回时在接收分支按接收顺序的逆序分别 `git revert` 对应提交，不清理或覆盖主工作区文件。发布由主线另行安排，本候选不部署。

本机共享 Git `post-commit` 钩子会尝试对全部远端执行 `push --all`，与仅提交隔离候选的意图不一致。提交 `9a741e29b` 时该钩子被触发；本地日志显示相关远端推送报错，尚未逐个独立核实远端状态。后续候选提交须在单条 `git commit` 命令上指定空的 hooks 目录，不修改共享钩子配置；主线评审不可将本地提交等同远端已同步。

## 发布前决策

业务需要确认实际发货、售后及课程开通规则，以及哪些商品/课程字段可向用户展示；产品需要决定是否将商城促销区进一步简化。技术需要用真实测试环境核对分类深链、详情权限、购物车与支付、免费订阅与课程播放，并完成 H5、微信小程序及目标设备的视觉/交互验收。
