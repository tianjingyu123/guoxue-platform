# 第三方排盘协作范围（按用户新回复收窄，2026-09-11）

## 已由第三方明确，不重复询问

- guoxueApp.php 为 App 用户入口；mobile、key、go=tool/my 按既有协议使用。推荐 H5 入口为 https://www.yrydai.com/p1.php?ruid=userid，不为 App 入口发明 userid/recommendId/fromuid 等参数。
- mobileUser.php 返回普通用户或推荐用户的数字 userid。查到 userid 不等于已开通推荐人。
- partner.php 对已存在手机号开通合伙人，不存在返回失败。
- 微信 addPartner 页面可授权并自动创建不存在的用户；此流程不能等同纯服务端自动开户。
- v=1 是第三方已有显示版本参数。本次不修改该控制，也不以审核时隐藏功能、审核后再开放作为接入实现或验收方式。

## 我方自行落实，不转交一般技术问题

1. 域名和响应：本次无 Cookie 公开 GET，www.yrydai.com/p1.php 为 302，目标 open.weixin.qq.com/connect/oauth2/authorize；首跳未见 X-Frame-Options/CSP。www.yrydai.cn/p1.php 为 200、0 字节。不能互换域名，也不能把 200 当加载成功。未调用手机号查询、建号、开通或支付接口。
2. 跨域能力：浏览器允许的嵌入和脚本读取权限是两回事，H5 父页不能直接读取跨域子页当前地址或注入 App 桥。CORS 不等同 DOM 操作权限。依据 MDN 同源策略与 iframe 文档。
3. 既有 App 分享能力：历史 r8/r9 真机及原 APK 协议核查已取得当前 /app_p1.php 路径，限定转换为公开 /p1.php 并保留允许的盘面参数。这不是待第三方回答的问题。后续应核对该实现是否已包含在实际待发布 App，而非重新猜测协议。
4. 微信 H5：已有“先顶层访问旧站再进外框”的实际对照仍显示子页空白。继续结合可观察的页面与既有接口定位；现有证据没有证明空白唯一根因，更没有证明一定需要第三方改授权系统。

参考：
- https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Same-origin_policy
- https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/iframe
- artifacts/qa-v243-freeze-20260906/bridge-protocol-followup.md
- artifacts/qa-v243-freeze-20260906/排盘融合方案验证结论-20260907.md

## 仅在我方检查后仍无法获得时向第三方确认

- 登录后的旧 H5 是否已有父子页通信或模板适配入口，以及它的实际消息协议。若确实没有，需要第三方在其控制的页面接入导航/分享适配；我方负责脚本与验证，不要求其交付整套源码。
- 如果内嵌确实卡在其服务器会话或授权处理，需要它提供已支持的授权回跳契约或改动对应处理。必须带上具体失败证据再提问，不把“请查浏览器为什么跨域”作为协作项。

当前不需要用户立刻转发新的通用问题清单。先消化已有回复和本地证据；以上是协作边界，不是已判定必须实施的第三方改造方案。
