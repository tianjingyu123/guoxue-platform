# T11 全局架构审计报告（正式上线前质量闸门）

> 审计日期：2026-07-03 · 六路并行（资金/安全/数据层/架构依赖/设计实现一致性/前端质量）· 外部审计师视角
> 结论速览：**核心资金规则（T1 L1-L5）、自审自批禁止、研究院考核 V5、合规红线 R1-R4 全部真实落地，无虚假声称**。发现 1 个确凿资金账目 bug（已修）、一批"先查后写"并发防护缺失（真实但需并发触发）、一个上线阻断的多实例隐患（cron 无分布式锁）、一批体验与文档修复项。

## 一、立即修复（P0·本批）

| # | 问题 | 定位 | 处置 |
|---|---|---|---|
| P0-1 | **礼物→圈子收益换算方向写反放大 10 倍**（priceCoin×qty×coinRate 应为除法）——污染圈主分成账目 + **喂给 T9 研究院「创收≥5000」准入门槛**（收几十元礼物即刷过精英门槛） | coin.service.ts:361 | ✅ 已修（改除法+注释） |
| P0-2 | **驿站经营后台读端点全部无鉴权**：GET stations/:id/settlements·orders·teacher-bookings·products / courses/:id/registrations——未登录遍历 stationId 即导出结算财务+学员 userId+qrCode | offline.controller.ts:250/318/373/404/448 | 待修：加 JwtAuthGuard+assertStationOwner+registrations 收敛 select |
| P0-3 | **扫码签到不校验操作者归属**：配合 P0-2 泄露的 qrCode，任意登录用户可伪造任意学员签到（污染结算/评价资格） | offline.service.ts:269 signInCourse | 待修：加 assertStationOwner |

> 说明：P0-2/P0-3 审计原标 P1，但"未登录可利用+泄露财务与个人信息"实质为上线阻断，提级 P0。

## 二、上线阻断（多实例部署前必修）

| # | 问题 | 定位 |
|---|---|---|
| M-1 | **结算对账 cron 无分布式锁**——多实例并发跑同批 PENDING 有重复结算风险（单实例安全） | settlement-reconcile.service.ts:28 |
| M-2 | ~47/50 cron 无分布式锁，多实例普遍重复执行（advisor 重复建议/AI cron 重复烧钱） | 全局 |
| — | 当前生产=单实例，不阻断当前上线；**横向扩展前**必须给资金/顾问类 cron 统一补 setNX 锁 |

## 三、并发防护缺失（真实可损·需并发触发·建议本批或紧邻批修）

共性根因=**防重靠"先查后写"而非唯一约束/条件更新**：

| # | 问题 | 定位 | 修复 |
|---|---|---|---|
| C-1 | 优惠券核销无条件 update→同券并发双单双折扣 | shop.service.ts:495 | updateMany where used:false + count 校验 |
| C-2 | 超时取消 cron 回补按"锁到的"非"实际取消的"→已支付订单库存被回补/已用券复活 | shop.service.ts:1637-1699 | 事务内先锁 PENDING 名单再翻转+回补 |
| C-3 | 支付回调丢弃场景仍返回 SUCCESS→已付款订单被超时取消，钱货两空 | shop.controller.ts:391 / service:929/978 | 锁失败返回 FAIL 让渠道重试+已收款对账 |
| C-4 | 佣金冲正幂等非原子+双入口并发→站长收益双倍倒扣 | commission.service.ts:222/1447 | setNX(reverse:{orderId}) + count>0 才冲正 |
| C-5 | bot 追问包扣币与加次数非同事务→钱扣包未到 | bot.service.ts:88 | spend 传 tx 合并事务（照 paipan-ai 范式） |
| C-6 | 大师讲堂/AI师徒/结算 LedgerEntry 防重购缺唯一约束→并发双扣 | content:210 / paipan-ai:150 / settlement:86 | 加组合唯一约束+P2002 幂等 |

## 四、埋雷（当前不可损·资金化前必修）

- 押金/会员/分红金额信客户端或无扣款：member.purchase 无实扣款（已废端点，建议删死代码）、institute deposit 信 body（`dto.deposit||10000`）、CreateDividendDto amount 无上限——**支付门控/资金真源切换前必须服务端定价+幂等键**
- 秒杀退款(PAID)不回补 sold/券（与 cancelOrder 不对称）——业务决策需文档化或补对称
- applyAfterSale.amount 信客户端无 ≤实付校验——现仅落记录，接自动退款即成 P0，建议现在 clamp
- 平台费冲正缺口：无佣金记录订单 reverseCommission 直接 return→platformFeeRecord 永不冲正

## 五、设计↔实现（一处反向偏差）

- **汇付分账已有完整实现**（huifu 模块 515 行+守卫+测试+已装载），但设计文档标"P2 待拍板/未实现"——需回填文档状态；且设计的 5 大护栏（订单驱动自动分账/延迟分账/进件 KYC/对账 cron/Provider 抽象）大多未满足，**正式启用前须补齐或显式冻结模块**
- 化解费三道闸 gate 1（化解/法事类 SKU 类目创建硬拦）未落地，现仅靠内容审核词库兜底（P1）
- 7天累计消费熔断、投诉→在途分账冻结接线未完成（设计自标 P1，属计划未建非虚假）

## 六、数据层（本地漂移非生产问题）

- schema-db-check 报 6 处不一致（Live 三表/Video 2列/Withdrawal 4列/CoinScene.LIVE_QUALITY_PACKAGE/LiveRoom.quality）——**均为本地开发库漂移，生产库已有**（他线生产直接部署过）；真隐患=这些变更**无 manual-sql 工件**，下次生产全量部署无脚本可依 → 建议补迁移 SQL
- 本月 12 个 20260703_* manual-sql 与 schema/DB 三方零漂移 ✅

## 七、前端质量（无 P0）

- scan-mock-imports 全绿·路由零死页·三态与防重覆盖高于项目平均
- P1：onboarding 进度环 conic-gradient 无小程序降级 / sop 页配色用平台红应改赭红 / coupon-detail 缺空态+两页券卡重复造轮子

## 八、正面确认（审计认可的健壮面）

CoinService.spend 原子 CAS·秒杀闸2 raw SQL 条件更新原子·createOrder 全服务端计价·cancelOrder 单笔对称回补·会员免支付漏洞已封堵·研究院审批流原子认领+自审自批拦截+职责分离·paipan 越权归属过滤·注入面零 raw 拼接·敏感数据脱敏到位·T1 五铁律逐条落地·合规红线 R1-R4 无违背。

## 处置计划

- **本批修**：P0-1（已修）+ P0-2/P0-3（driver 越权）+ C-5（bot 事务·一行）
- **紧邻批**：C-1~C-4/C-6 并发防护（需唯一约束迁移+条件更新改造，集中一批）
- **多实例前**：M-1/M-2 cron 分布式锁
- **文档回填**：汇付状态；建圈公共方法抽取（技术债）
- **资金化前**：第四节埋雷项
