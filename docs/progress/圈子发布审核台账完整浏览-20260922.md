# 圈子发布审核台账完整浏览（2026-09-22）

“发布审核”原先固定查询前 50 条，无继续浏览入口。超过 50 条后，用户无法找到旧驳回原因或确认历史提交。本批按 20 条分页，提供继续浏览、下一页失败重试及筛选切换的旧请求隔离；已有记录在后续读取失败时保留。服务端在创建时间相同时增加 ID 排序，减少跨页重复或漏项。

验证：本地浏览器模拟 23 条审核记录，下一页首次失败后重试成功；320px 无横向溢出，未发送写请求。移动端类型检查与服务端构建通过，审核服务定向测试 29/29 通过。微信小程序构建、WXML 与组件校验通过；主包 1.63 MB，保守上传口径 1.92 MB，圈子分包 0.99 MB。真实审核队列和历史数据未读取，本批未进行审核动作。
