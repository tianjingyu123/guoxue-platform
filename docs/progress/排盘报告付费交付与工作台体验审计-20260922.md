# 排盘报告付费交付与工作台体验审计（2026-09-22）

## 用户要完成的事

| 用户 | 任务 | 顺畅路径应当是 |
| --- | --- | --- |
| 爱好者 | 看懂自己的盘、找到依据、继续提问 | 起盘 → 看免费盘面 → 明白报告范围和价格 → 支付 → 直接读报告 → 对具体章节提问 → 学习相关典籍 |
| 从业者 | 快速形成可审核的客户交付物 | 起盘 → 导入工作台 → AI 起草或改写 → 自己校核 → 客户视角预览 → 交付 → 收集客户问题与回访 |
| 已购买用户 | 不中断已开始的工作 | 查原订单到账 → 进入原报告或会员能力 → 历史与对话可继续；网络失败可重试，不能重复收费 |

这三条路径共享排盘记录、报告事实和交付状态，但“平台小卜报告”与“从业者署名交付报告”是两种产品，现有代码也分别在 `paipan` 与 `practitioner` 模块。后续应在工作台建立关联，避免用户以为买了小卜报告就自动生成客户交付件。

## 本轮实证与修复

1. **付款后走丢**：通用支付成功页只有订单和商城入口。现在用服务端核验为已支付的订单类型与 `targetId` 决定主要动作：报告订单进入原报告，工作台会员返回工作台，语音时长回额度页；从报告入口买小卜会员会带回原排盘记录。仍保留订单入口。订单未支付或查询失败时不展示成功服务入口。订单状态和权益生效仍由后端支付回调决定。
2. **微信 H5 回跳再次唤起付款**：支付页原来回跳后直接调用 `startPaying()`。现在为回跳链接加 `paymentReturn=1`，回页只轮询原订单。倒计时结束仍是“确认中”，不是擅自判失败。
3. **从业者会员支付绕路**：工作台原用 Native 扫码弹层，手机用户需跨设备扫码。现改为平台统一收银页，保留服务端订单定价与回调开通；返回工作台及编辑器时刷新会员状态。
4. **交付内容不同步**：工作台 AI 改写后同步更新编辑区章节；改写前先保存老师未提交的改动。交付前若保存失败立即停止；有空白章节时由老师确认是否仍要交付。避免客户读到旧稿或误交空章。
5. **报告对话失败无恢复**：失败的问题可原位重试；新报告不会沿用旧报告的对话缓存。未发送问题原文到行为埋点。
6. **假进度和价格误导**：生成页去掉定时点亮步骤，预检返回后一次展示已核对的事实，保留真正进行中的成书阶段；会员方案列表为空时不再显示“¥0 起”。
7. **漏斗可测**：增加不含姓名、生辰、问题正文的事件：报告权限、购买点击、订单创建、生成开始/完成/失败、提问开始/完成/失败、导入工作台、已付服务继续、工作台会员购买。上报失败不影响用户操作。

## 仍需推进的产品工作（按优先级）

| 优先级 | 工作 | 验收标准 |
| --- | --- | --- |
| P0 | 微信小程序、微信内 H5、外部 H5 的报告购买／会员续费／语音充值联调；支付回调延迟、失败、退款、重复回跳与订单恢复。App 端当前只提示尚不支持在线支付，若作为正式购买端须接入合规支付能力并验收 | 每种状态只兑现一次；付款后能到原任务；已付款但生成失败可免费重试；退款后的权益按规则收回 |
| P0 | 工作台交付页与真实客户预览逐屏对照，报告内容审校与版本留痕 | 老师看到的与客户看到的一致；每章能区分引擎事实、AI 初稿和人工修改；撤回链接立即失效 |
| P1 | 工作台“今日待办”围绕预约、未定稿报告、客户新问题和回访安排，而非泛化 KPI | 从首页一步回到未完成任务；能看到客户关联的盘、报告、问答及后续动作 |
| P1 | 报告阅读中的术语、原典、具体问题与课程/圈子建立相关性连接 | 推荐理由与当前章节相关，能返回阅读位置；避免购买刚结束就推无关商品 |
| P1 | 统一历史：小卜报告、对话、客户交付件、订单在各自页面可互相回到同一任务 | 用户换设备后不用重新输入盘面，已付报告可继续，工作台导入不制造难辨的重复件 |
| P2 | 为专业用户提供门派对照、计算口径、案例批注与团队复核 | 同一盘的依据和争议可追溯；能沉淀可检索的客户案例和学习笔记 |

## 衡量方法与边界

- 漏斗按 `权限页展示 → 点击购买 → 订单创建 → 支付成功 → 报告生成完成 → 首次阅读 → 首次提问 → 次日/七日回访` 测量，分端、用户类型、首购/续费，不混算。
- 同时看支付后到达报告率、生成失败恢复率、交付前预览率、空章交付率、提问成功率、客户回访率、退款与客服原因。转化率提高但退款/投诉上升，不能算体验提升。
- 本机没有代表性的真实用户行为样本，本轮无法声称转化率或满意度已提高。新增埋点要等真实环境验证上报与口径，再做小流量对照；订单数据作为付款真源，埋点只衡量行为。
- [Baymard 结算研究](https://baymard.com/research-articles/current-state-of-checkout-ux)显示不清晰的结算流程会导致用户放弃；其[订单确认页研究](https://baymard.com/checkout-usability/benchmark/step-type/receipt)也指出用户需要明确确认购买并知道下一步。本轮的“先核验订单、再给原任务入口”采用这一原则；具体效果须由本平台数据验证。

## 技术验证边界

本轮在本地工作树完成静态代码审查、7 个订单去向分支验证和移动端类型检查；移动端完整类型检查仍有金口诀、罗盘、梅花及旧 shared 声明相关的既存报错，本轮改动文件无新增报错。从业者报告导入、交付报告和客户问答 3 组 Jest（19 项）通过；支付服务测试因本工作树复用的旧 Prisma Client 缺 `VoiceSession.usageState` 声明，编译阶段未运行。未触发真实支付、付费 AI 调用、生产回调或远端部署；这些必须在匹配的隔离环境里按 P0 场景联调。

## 2026-09-23 补充：支付恢复与结算超时

- 收银页从订单、超时页或 H5 回跳重开时，先只读查询原订单。已支付直接去对应服务的成功页；已取消或退款显示终态；待付且并非渠道回跳时才发起支付。查单异常或状态未知时继续轮询原单，不在未知状态下唤起第二次付款。重试按钮也执行同一核验。用户离开页面后，晚返回的支付参数不再唤起收银台或跳转结果页。
- 订单状态接口缺失 `status` 不再默认成 `PENDING`，避免异常响应被误判为可再次付款。支付轮询遇到已取消或退款订单会结束，并告知真实状态。
- 商城结算页原有前端 15 分钟倒计时发生在创建订单之前，但文案称“支付超时”，到期还会跳到一个没有订单号的支付超时页。现移除这段没有服务端期限依据的倒计时及弹窗；商品和价格仍会在结算时读取，真实订单状态以服务端为准。
- 验证：订单恢复决策 8 个状态用例通过；改动文件 ESLint 0 error（存量样式 warning）；移动端类型检查仍为原有 16 个其他模块错误，改动文件无新增错误。未进行微信真实扣款或异步回调联调，不能将本地分支验证等同于支付端到端验收。
- 报告生成失败的服务端门禁按已购权益或有效会员状态核验，报告内容仅在模型成功后写入；失败后重试不再创建新订单。前端错误态已有原记录重试入口，本次把生成中“请勿离开页面”改为可以从原排盘记录继续查看或重试，避免给用户错误的等待压力。会员到期后仍按服务端当时的权益规则处理。

## 2026-09-23 补充：成功页回调延迟恢复

- 支付成功页的订单查询若暂时未到账或网络失败，新增“重新核验”入口；从订单页返回时自动重新核验原订单。核验只读取服务端状态，不再次发起付款。
- 已取消和已退款订单显示各自真实状态，不用“回调仍在处理”的误导性提示，也不反复展示无效的核验按钮。成功服务入口依然只有在订单确认为已支付后出现。
- 仍需在微信小程序、微信内 H5、外部 H5 用真实支付回调和延迟注入验证多端表现；本地静态与分支检查不能替代真机联调。

## 2026-09-23 补充：工作台交付版本保护

- 客户交付链接原来直接读取报告当前正文，老师交付后继续保存或整份 AI 改写，会让客户在同一链接里看到未经重新预览的新稿。现将链接有效期间的正文设为只读：编辑器禁用修改与改写，服务端保存和改写都用 `shareToken = null` 条件写入，防止并发交付后仍写进新稿。
- 老师需要修订时，先撤回交付链接；撤回后报告回到 `final`，可修改、预览，再生成新链接。交付时统一标记 `delivered`。旧链接撤回即失效，新链接使用新令牌；正式环境仍需验证链接传播与缓存行为。
- 相关服务端 Jest 两组 11 项通过，覆盖交付后拒绝保存、改写拒绝与改写期间发生交付的竞争。编辑器和支付成功页 ESLint 0 error；移动端完整类型检查的其他模块存量错误仍需清理。

## 2026-09-23 补充：从业者会员月底续费

- 从业者会员付款开通与退款后按剩余订单重建权益，原来分别用 `Date.setMonth` 加一个月。1 月 31 日等月底会越过 2 月，导致有效期与用户理解的“按月续费”不符。现共用按 UTC 自然月顺延、目标月不足原日号则取月末的函数，保留原时分秒，付款与退款重建口径一致。
- 4 个边界用例通过，覆盖平年 2 月、闰年 2 月、30 天月份与跨年。支付／退款服务整组 Jest 因本工作树复用的旧 Prisma Client 类型分别缺 `VoiceSession.usageState` 和新增订单类型，在编译阶段停下，未运行到业务断言。尚未在真实支付／退款回调环境跑端到端重建；需要与财务订单和到期通知一起验收。
- 小卜 AI 会员页原默认选第二档，当前默认配置对应年费，容易让用户一进入就面对较高实付金额。现默认选择服务端返回的总支付金额最低档，长期方案保留给用户主动选择；若已手动选档，刷新后仍保留该选择。

## 2026-09-23 补充：返回购买页时恢复待付订单

- 小卜报告、小卜 AI 会员和从业者会员在当前页面会话内保留刚创建的订单。用户从收银页返回再次购买同一标的时，先查原订单：仍待付就回原单继续；已付则刷新报告权限或会员状态；已取消／退款才允许创建新单；查单失败或未知状态不盲目创建第二笔。
- 这减少同一页面反复进入收银台造成的重复待付订单。记录只在当前页面实例中，换设备或重启后不能复用；彻底解决需要服务端按用户、产品、标的、价格和有效期做并发安全的待付订单复用，并核对优惠券、归因和价格变化的语义。
- 三个改动页面 ESLint 0 error；移动端完整类型检查仍为其他模块既有的 16 个错误，改动页面无新增错误。尚未真机模拟微信取消、慢回调和跨端付款。

## 2026-09-23 补充：服务端待付订单复用

- 报告、小卜 AI 会员和从业者会员下单扩展现有的 Redis 创建锁与 PostgreSQL 事务级锁，按用户、订单类型、标的查询原 `PENDING` 订单。相同条件返回原单；无券订单若当前服务端价格与原单金额不同，要求先取消原单；优惠券不同也不另建可扣款订单。同券待付单保留创建时已核销的价格，不再次扣券。
- 从业者会员只接受 `practitioner_pro_monthly` 标的，避免通过更换标的绕开订单锁；工作台旧的 `createProOrder` 辅助函数补齐标的与数量参数。
- 商城下单服务运行时 Jest 全组 72 项通过，包含 9 项数字服务订单恢复场景；为绕过本工作树复用的旧 Prisma Client 对 `VoiceSession.usageState` 的编译报错，临时测试配置只关闭了过期类型诊断。正式类型检查与真实 PostgreSQL 并发、支付回调仍需在匹配的隔离环境验收，不能将这些单元用例等同于端到端支付安全证明。
- 复用返回增加 `reused` 标记，报告与从业者会员购买页分别上报“恢复原单”事件，不把跨设备找回旧单重复计作新订单创建；订单与付款的最终统计仍以服务端流水为准。

## 2026-09-23 补充：工作台待办直达

- 工作台首页原已取得待回答数、最近报告和客户回访提醒，却只能展示数字或跳到泛化列表。现在展示未完成报告任务，草稿直接进入编辑器、定稿进入核对与交付页；待回答数直达咨询订单的待处理筛选；回访提醒直达对应客户档案，档案内可完成提醒。
- 服务端首页的报告查询改为只取最近更新的 5 份草稿或已定稿报告，避免已交付报告挤掉待办。完整报告仍在报告工坊。首页待办不是所有未完成报告的全量列表。
- 从工作台进入待答订单时，按回答者与 `PENDING` 状态逐页补齐跨圈问答，不让原订单流首屏 100 条上限吞掉更早的待答问题；普通订单流仍保持原有首屏加载策略。后续可将此列表变成专门的待办页，并在真机验证返回工作台后的数据刷新。
- 改动文件 ESLint 0 error；移动端完整类型检查仍是其他排盘模块既有的 16 个错误。未做真实客户资料、问答订单或微信端交互联调，不能据此宣称回访率或满意度已提高。

## 2026-09-23 补充：交付预览与客户阅读

- 预览和客户页原有不同的空品牌名回退：预览写平台名，客户页写报告类型。现服务端公开报告优先使用老师的品牌名、其次账号昵称；预览按同一顺序展示，若两者都为空再用报告类型。预览与客户页的章节正文、AI 起草提示、落款日期采用一致口径。
- 预览页原称“客户看到的就是这个样子”，但客户页还提供提问、扫码和打印。现在明确预览用于核对正文和落款，并说明客户页附加入口，避免老师误以为已逐项预览全部交互。
- 客户公开链接的网络或服务异常不再显示“报告不存在或已被撤回”，而是提供重新加载；明确的 404／410 才显示链接失效。报告内提问失败保留问题原文供再次发送，避免客户重输。
- 整份 AI 改写原先只设置 `rewritten`，未设置客户页实际读取的 `ai` 标记；现在改写成功的章会披露 AI 来源，盘面事实章及改写失败的原文保持原标记。
- 改动页面 ESLint 0 error；交付稿改写 Jest 8 项通过（使用临时配置绕开工作树复用的过期 Prisma 类型诊断）；完整移动端类型检查仍为其他排盘模块既有的 16 个错误。尚需以有品牌／无品牌、AI 初稿／人工稿、撤回／断网、H5 打印和提问限额等真实场景逐屏验收；本地静态检查不能代替真实交付测试。

## 2026-09-23 补充：报告重生成后的问答版本

- 平台报告可在同一报告 ID 上重新生成，原问答也按 ID 保存。原逻辑会把旧版结论继续送入新版模型上下文，并把旧版讨论进度当作当前进度。现在以报告 `metadata.generatedAt` 为界：当前版问答用于模型上下文与继续学习进度；较早问答保留在服务端，阅读页可展开查看，明确标注不参与新版回答。
- 重新生成同一报告 ID 后，页面清掉旧的本地对话状态，下次打开问答从服务端读取新旧分组。旧报告没有有效生成时间时沿用原口径，避免历史资料突然消失。
- 提问期间若报告刚好重生成，服务端在模型返回后再次核对生成时间；版本已变时拒绝交付旧版回答，也不写入新版问答。页面的问答缓存按报告 ID 和生成时间区分，重生成时会重新读取。仍需以跨设备并发和真实模型验收；单元测试只验证版本判断、查询与上下文隔离。
- 报告问答服务 Jest 12 项通过（使用临时配置绕开复用的过期 Prisma 类型诊断），改动页面 ESLint 0 error；移动端完整类型检查仍为其他排盘模块原有的 16 个错误。未运行实际付费模型或生产报告重生成。

## 2026-09-23 补充：退款后的报告访问

- 退款流程已撤销单份报告／会员权益，但生成后的详情与报告问答接口原来只校验报告归属。现在读取报告正文、问答历史、继续学习和发起提问时都复用报告权益门禁；退款且没有其他有效权益时，旧报告 ID 也不能绕过购买页继续使用。保留旧数据，不直接删除用户的文字记录。
- 页面再次回到前台时，按当前报告类型重新核对权益；已失效则关闭问答并回购买选项。网络失败时不误判为退款，服务端接口仍各自核验。旧存档若缺排盘记录 ID 或报告类型，暂按兼容口径放行，需要清点旧数据后再确定迁移规则。
- 报告生成与问答两组 Jest 合计 24 项通过，改动页面 ESLint 0 error（临时 Jest 配置只绕开过期 Prisma 类型诊断）。仍需在隔离环境用真实退款回调、会员到期和跨端会话验证最终权益口径。
- 旧报告正文若缺 `metadata.reportType`，现在优先从存档的 `analyzeType=REPORT_*` 补推类型，详情与问答仍复核权益；页面返回前台时也沿用当前打开的报告类型。仍缺排盘记录 ID 或不可识别类型的旧数据保持原兼容口径，必须盘点真实存档后再制定迁移，不能直接宣称所有历史报告已封闭。
- 报告生成与问答服务原将 `XiaobuCommerceService` 标成可选，缺注入时会跳过门禁或把权限当免费。现在两处均改为必需依赖；`PaipanModule` 已引入并由 `VoiceModule` 导出该服务，配置缺失应在启动阶段报错。排盘报告相关 8 组 Jest 共 82 项通过（临时测试配置只绕开过期 Prisma 类型诊断）；仍未以部署构建和真实退款联调替代这道验证。

## 2026-09-23 补充：从业者历史报告可找回

- 工作台报告列表原固定只取最近 100 份且把本页数量当作总数；会员可不限量建报告后，较早报告无法在列表中打开。现在服务端按本人、状态和关键词提供每页 20 份的稳定分页与真实总数，前端提供“加载更多”。
- 分页信息单独放在 `pagination`，保留原有 `list`、`total` 和 `quota`；若把 `page/pageSize` 平铺，统一响应拦截器会丢掉报告配额。筛选切换时丢弃旧请求结果，避免混入上一筛选的报告。
- 分页服务单元测试通过，改动页面 ESLint 0 error；完整移动端类型检查仍是其他排盘模块既有的 16 个错误。需用大量真实报告、筛选切换和跨页删除做界面验收。
- 报告工坊补上标题／客户称呼搜索框；提交搜索后再分页，输入尚未提交时不会把新关键词与旧列表混合。清除搜索可回到当前状态筛选的全量列表。

## 2026-09-23 补充：客户提问失败不扣次数

- 交付报告的匿名提问按报告每日限额，原先在模型调用前占用次数；模型超时或返回空文时，客户重试会白白少一次。现在仍先原子预占以挡并发刷量，但超额、模型失败或空回答会释放预占，保留原 TTL，计数不降为负数。成功回答才计入当日使用量。
- Redis 内存降级与交付问答共 51 项 Jest 通过。尚需在正式 Redis 与真实 AI 超时注入场景验证多实例计数和失败恢复。

## 2026-09-23 补充：已生成报告跨设备找回

- 排盘记录页原先只列本机缓存，换设备后找不到自己生成的付费报告。现在增加按登录账号读取的“我的报告”，服务端只查询本人 `paipan_report` 场景的结构化报告，每页 20 份，返回目录所需的姓名、盘型、报告类型和时间，不传报告正文、摘要或加密生辰。
- 点击目录携带原报告 ID，先核对当前权益，再按 ID 打开存档，避免仅为重读报告而重新生成或因知识版本变化读到另一份内容。退款等失效权益仍由报告详情接口再次拦截。页面下方的本机排盘记录继续承载各工具未生成报告的起盘历史；两套数据来源已明确标注。
- 报告服务 Jest 12 项通过，修改页面 ESLint 0 error。移动端全量类型检查仍有 16 项其他排盘模块既有错误；服务端全量类型检查受本工作树复用的过期 Prisma Client 影响。尚需用真实账号验证跨设备、分页、退款后从目录打开及报告问答恢复。
- 复核权益边界后，目录不再返回模型生成的 `outputSummary`：退款或会员到期时可见本人报告的基本索引，付费解读正文和摘要仍须进入详情并通过当前权益核验。

## 2026-09-23 补充：工作台导入原报告的续接与权益

- 同一份小卜报告再次点“存入工作台”时，服务端按本人和盘面快照中的 `sourceReportId` 找到已有交付稿，直接返回原稿；即使免费报告配额已满，也能回到此前开始编辑的任务，不再生成多个相同草稿。
- 导入前按原报告对应的排盘记录与报告类型复核当前权益。退款或会员权益失效后不能借导入接口重新提取报告正文；旧存档若缺关联信息则明确拒绝导入，需先补齐资料，不能默默放行。
- 从业者相关 Jest 3 组共 13 项通过。当前去重依赖 JSON 来源字段查询，未加数据库唯一约束；双设备同时首次导入仍可能产生重复件，正式 PostgreSQL 并发验收与唯一性设计待完成。
- 导入新稿时只读原排盘记录的归属和客户称呼，手填称呼优先，否则沿用本人原盘姓名；不读取加密生辰，也不把报告错接到他人的客户名下。小卜模型解读章节写入客户页实际使用的 `ai` 标记，确定性盘面事实不标 AI；老师人工编辑后编辑器会清除此标记。此处解决的是来源披露与录入成本，最终交付仍需老师逐章核对。

## 2026-09-23 补充：从订单记录恢复待生成报告

- “我的订单”原能查到已付款的报告订单，但详情页没有进入原报告的动作；换设备后若模型首次生成失败，“我的报告”目录也没有记录可找。现在订单详情保留服务端 `targetId`，仅在明确的已付状态显示“生成并查看报告”，按原排盘记录回到生成页；未知、待付、退款或售后状态均不展示这个入口。
- 同一入口也为小卜 AI 会员、语音时长和从业者会员提供已付服务去向；订单列表标明这些数字服务的真实品类，不再统称商城。详情与成功页复用 `paidOrderNext` 的目标规则。修改文件 ESLint 0 error，尚需真实订单在各端逐屏验证。
- 订单目标含报告类型，成功页和订单详情现将它带回阅读页；权限核验、会员购买返回、生成与重新生成均沿用该类型。报告目录也按本人列出通用和专项结构化报告并标记类型，避免已购事业／情感等专项报告误落到综合报告的购买墙。实际专项报告订单需在隔离支付环境验收。

## 2026-09-23 补充：历史报告严格门禁前的数据审计

- 已新增只读脚本 `apps/server/scripts/audit-paipan-report-records.ts`。从 `AiAnalysisRecord.scene=paipan_report` 分批读取，并核对关联排盘是否存在、归属是否一致、结构化内容能否解析、报告类型能否从元数据或 `analyzeType` 确定。只输出聚合计数，不输出姓名、用户或报告 ID、报告正文。
- 运行需由具备数据库只读连接的单一执行方显式设置 `DATABASE_URL`，在 `apps/server` 下执行 `pnpm exec ts-node scripts/audit-paipan-report-records.ts --run`。脚本不自动读取 `.env`，缺少 `--run` 不连接数据库；`--help` 已验证可运行。当前工作树未运行真实库审计，尚无历史异常数量。
- `readyForStrictGate` 表示字段足以支持严格关联与类型校验，不代表报告当前仍有付费权益；各异常计数可能重叠。统计非跨批次事务快照，运行时并发新增记录可能影响单次结果。取得只读汇总后，再制定缺关联存档的迁移和拒绝策略，并在隔离支付环境验证退款、会员到期与跨设备重读；在此之前不直接拒绝所有旧存档。

## 2026-09-23 补充：工作台报告并发写入

- 工作台手工新建和小卜报告导入现在按老师账号使用同一 PostgreSQL 事务级咨询锁；锁内重新查询来源报告的现有草稿和免费配额。两设备同时导入同一报告时，后到请求应返回先建的草稿；手工新建与导入并发也应按实际已占用份数计算额度。
- 导入的报告权益与原盘归属仍先核验，再进入短事务。相关 Jest 12 项通过，改动文件 ESLint 0 error，服务端完整类型检查仍受复用的过期 Prisma Client 影响，改动文件没有新类型错误。
- 尚未在隔离 PostgreSQL 上实测并发，因此不能把单元测试视为数据库并发验收；历史上已经生成的重复稿不会由这次改动自动合并。后续应先统计并处理既有重复件，再实测多实例同时导入、手建与导入抢最后一份配额。

## 2026-09-23 补充：报告页登录续接与账号切换

- 游客进入报告页现在有“去登录”入口；登录返回后自动核验当前报告权益，已购则继续打开或生成，未购则展示购买选项，用户不必返回排盘页重走一遍。
- 页面重新显示时若登录账号变化，立即清空上一账号留在内存的报告、问答和语音额度，并使旧异步请求失效；同账号令牌刷新仍保留当前阅读进度并复核权益。无登录态点击重试也会进入登录提示。
- 改动页 ESLint 0 error；全量移动端类型检查仍存在其他文件的既有错误，本页没有新增类型错误。登录返回、同账号令牌刷新、切换账号和网络失败仍需在微信与 H5 实机逐屏验收。

## 2026-09-23 补充：历史报告关联原盘归属

- 对已经存有 `paipanRecordId` 的报告，详情与问答现核对原盘仍存在且属于报告用户；不一致时拒绝交付正文或调用问答模型，避免错误关联的旧存档借有效会员权益被继续读取。
- 报告服务与问答服务 Jest 共 29 项通过。报告服务文件现有的 40 处 `no-irregular-whitespace` 报错均在本次改动行之前，本次新增行未触发该规则；服务端完整类型检查仍受过期 Prisma Client 的其他模块错误影响。
- 完全缺少 `paipanRecordId` 或报告类型的早期存档继续沿用兼容口径，先运行只读聚合审计并制定迁移，再决定严格门禁。此处只收紧明确存在但归属错误或已成孤儿的关联。

## 2026-09-23 补充：生成期间权益变动

- 报告生成前已有权益门禁；现在模型返回、正式保存与交付前再核一次。若长时间生成期间退款或会员到期，拒绝保存并返回付费正文。复用既有存档时，也在版本匹配后、返回正文前复核一次。
- 报告服务 Jest 16 项通过，覆盖模型生成中撤销权益与复用前撤销权益。校验仍非与退款账本同一个数据库事务，不能消除最后一次核验到响应之间极短的并发窗口；最终仍以详情和问答各自的权益校验为准。
- 本轮核对当前工作树、主工作目录常见服务端 `.env` 路径和进程环境，均未发现可用的 `DATABASE_URL`；未试图借网页登录状态冒充数据库凭据，也未在生产服务器执行命令。历史报告真实库汇总仍需由已协调的数据库通道执行。

## 2026-09-23 补充：语音时长续费后回到原场景

- 从报告语音对话或圈子语音助理进入充值时，沿充值页、收银页到支付成功页携带经过白名单校验的语音场景和上下文 ID；报告对话还保留小节 ID。成功页的主操作可直接继续原场景，普通充值仍进入语音时长页。
- 圈子语音的 `circleId` 继续独立传给订单服务用于既有分成核验，返回场景只决定客户端导航，不参与价格和到账判断。支付成功页仍先用服务端订单状态确认已付，才展示继续入口。
- 路由函数直接验证报告／圈子返回与无效场景回退；改动文件 ESLint 0 error，移动端全量类型检查的输出没有这些文件的新错误。真实微信支付回调、圈子分成、语音能力开关、返回后的额度刷新仍需隔离环境和真机联调。

## 2026-09-23 补充：工作台报告真实库并发验收入口

- 新增 `practitioner-report-concurrency.integration.spec.ts`，仅当显式设置 `XIAOBU_IT_DATABASE_URL` 时在隔离 PostgreSQL 运行。用两个实际并发请求验证最后一份免费配额只成功创建一份，以及同源小卜报告同时导入只产生一份并返回同一 ID；测试结束清理本次专用用户和数据，不调用模型或支付网关。
- 当前进程没有 `XIAOBU_IT_DATABASE_URL`，本机也未发现 Docker 可执行程序；运行 Jest 时该组按设计跳过 2 项，只确认测试可加载，**未取得真实数据库并发通过证据**。文件 ESLint 0 error，服务端全量类型检查没有该文件的新错误，其他模块仍受复用的过期 Prisma Client 影响。

## 2026-09-23 补充：语音报告上下文权益门禁

- 审计发现文字报告详情与文字问答均复核当前权益，但 `VoiceContextBuilder` 原只核报告用户归属；退款后只要还有语音时长，仍可能组装付费报告摘要进入新语音会话。现在语音报告上下文也要求必需的商城权益服务，先核关联原盘存在且属于用户，再按元数据报告类型或存档 `analyzeType` 核验当前权益，拒绝后不向语音服务提供上下文。
- 语音上下文 Jest 18 项通过，覆盖退款拒绝、错关联原盘拒绝、正常权益下仍做隐私裁剪；改动文件 ESLint 0 error，服务端类型检查没有该文件的新错误。
- 完全缺关联或缺类型的早期存档仍沿用与文字报告相同的兼容边界，需等真实库只读审计后确定迁移规则。新会话建立时的门禁不能替代对正在进行的会话做实时退款终止；后者仍需真实语音供应商与回调链路验收。

## 2026-09-23 补充：交付链接在提问期间撤回

- 客户通过老师交付的报告链接提问时，模型生成可能持续较久。现于交付答案前重新查询分享令牌，并核对仍指向同一份报告；老师在生成期间撤回或更换链接，则拒绝返回答案，并释放已预占的当日提问次数。二次查询失败也不交付答案，并释放次数。
- 老师资料或昵称查询失败时也会释放预占的提问次数，且不调用模型；避免临时数据库故障让客户白白消耗额度。
- 交付问答 Jest 11 项通过，改动文件 ESLint 0 error。模型调用前已经发送的报告内容无法因中途撤回而撤销；二次核验与网络响应之间仍有极短的并发窗口，需在真实 AI 延迟及撤回操作下联调。

## 2026-09-23 补充：客户分享页撤回状态与每日次数

- 客户已打开报告后再提问，若老师此时撤回分享链接，页面收到 404/410 会清掉内存里的报告与问答并显示“报告不存在或已被撤回”，不再仅显示普通错误提示。重新加载遇到撤回或失败时也会清除旧报告；普通提问网络失败仍保留客户输入供重试。
- 服务端匿名问答的每日限额改为北京时间自然日，计数键在下一个北京时间零点过期。此前按 UTC 日期计算会在北京时间早上 8 点重置，与“今日提问次数”文案不符。客户在另一设备耗尽次数时，页面识别服务端限额提示并显示联系老师的明确去向；已打开的页面若跨越零点，次日需重新打开报告刷新本地状态。
- Redis 原子计数命令报错时，不再默认每次都算首次提问并继续调用模型，而是返回可重试的服务错误；RedisService 自身判断不可连接时仍可按既有逻辑退到进程内计数，多实例降级仍无法保证全局限额。
- 交付问答 Jest 13 项通过，服务端和分享页改动文件 ESLint 0 error。全量移动端类型检查仍有其他排盘模块既有的 16 项错误；服务端全量类型检查仍受过期 Prisma Client 影响，两者的错误列表均未涉及本次改动文件。切换日期口径后，部署当日旧 UTC 键与新北京时间键可能分别计数；尚需在真实 Redis、客户端 H5/微信和老师撤回操作中联调。

## 2026-09-23 补充：工作台交付链接并发与编辑状态

- 两次并发生成同一份报告的交付链接时，服务端只在令牌仍为空时写入；后到请求读回已经生效的令牌，重复请求不重置交付时间。避免双击后老师先复制的链接立即失效。
- 编辑页在 AI 起草、整稿改写、保存或撤回进行中不启动交付；交付进行时锁住输入并拦住重复生成、起草和改写，撤回操作也只执行一次。老师不必判断哪个异步操作先完成，避免把尚未保存的本地稿误当成已交付稿。
- 交付服务 Jest 4 项通过，相关服务和编辑页 ESLint 0 error。全量类型检查仍受前述其他模块既有错误影响；并发测试使用状态模拟，尚需隔离 PostgreSQL 与双设备实际点按验收。

## 2026-09-23 补充：整稿改写避免覆盖人工新稿

- 整稿 AI 改写启动时记住报告的 `updatedAt`；模型逐章返回后，只有分享令牌仍为空且报告版本未变化才写入。期间若另一设备保存了人工修改，旧改写任务明确提示重新加载，不覆盖新稿；若已经交付，继续提示先撤回链接。
- 编辑页在保存、整稿改写期间锁住标题和章节输入，AI 起草期间锁住当前章节；起草、改写、保存、预览和交付互相检查进行状态，避免本页异步结果覆盖老师刚输入的文字。发生跨设备版本冲突时提供“重新加载”入口，回到最新稿继续处理。
- 改写与交付服务 Jest 2 组共 13 项通过，改动服务和页面 ESLint 0 error。全量类型检查仍受前述既有问题影响；真实数据库版本比较、双设备编辑和小程序输入事件需联调。

## 2026-09-23 补充：人工保存的跨设备版本保护

- 工作台报告编辑页保存时带上加载时见到的 `updatedAt`；服务端仅在报告仍未交付、版本仍相同时写入。另一设备已保存新稿时，旧页面保留本地未保存文字并提示冲突，老师可先复制要保留的内容，再主动重新加载。
- 成功保存后页面采用服务端返回的整份最新报告，包含版本与可能同时变化的交付状态；撤回交付接口也返回更新后的版本，避免老师撤回链接再编辑时被自己的旧时间戳拦住。旧版客户端不传版本时仍沿用兼容行为，这条保护尚不能覆盖未升级的客户端。
- 交付与改写服务 Jest 2 组共 14 项通过，相关服务、编辑页和接口层 ESLint 0 error。正式 PostgreSQL 版本比较与两设备真实编辑仍需验证；全量类型检查仍受前述其他模块既有错误影响。

## 2026-09-23 补充：跨设备撤回交付链接

- 编辑页撤回时带上当前显示的分享令牌。服务端先比对所见令牌，再以同一令牌作条件更新；若另一设备已经撤回并重新生成，旧页面不能撤掉新链接，而是提示刷新确认。撤回成功后返回服务端最新交付状态与版本，页面据此同步。
- 兼容旧客户端不带令牌的请求，但它仍无法识别“读取前已换链”的旧页面；新编辑页已传令牌。测试覆盖请求前链接变更及读取后、条件更新前变更，交付服务 Jest 7 项通过，改动服务、控制器和页面 ESLint 0 error；隔离 PostgreSQL 双设备联调仍待进行。

## 2026-09-23 补充：客户打开报告期间的撤回

- 客户凭公开链接打开报告时，服务端在读取正文、老师落款后于交付前再次核验令牌仍指向同一份报告。若老师在这段查询期间撤回或更换链接，接口返回“报告不存在或已被撤回”，不交付旧正文。与匿名提问接口的二次核验口径一致。
- 交付服务 Jest 9 项通过，覆盖撤回中断与正常读取；改动文件 ESLint 0 error。最后一次查询与响应之间仍有极短并发窗口，已打开在客户端的内容不能远程抹除；真实双设备撤回场景仍需联调。

## 2026-09-23 补充：报告工坊删除确认与版本保护

- 删除已交付报告的确认框明确说明客户链接会失效，报告不可恢复。列表发起删除时带上所见 `updatedAt`；服务端按归属和版本条件删除，另一设备若已编辑、交付或撤回，旧列表的确认不能删除变更后的报告，而是提示刷新重看。
- 新建与删除按钮在请求进行期间拦住重复点击，避免双击生成两份草稿或重复发起删除。旧版客户端未传版本时仍按兼容路径删除，保护范围以新版工作台为准。
- 交付服务 Jest 11 项通过，服务端、控制器、接口层和报告工坊改动文件 ESLint 0 error；尚需真实 PostgreSQL 与移动端交互验证。

## 2026-09-23 补充：工作台 AI 章节起草归属与用量

- 章节起草接口原只要求登录，直接相信客户端提交的客户称呼、章节标题和盘面，并绕开统一 AI 网关直接请求模型。现改为提交报告 ID 与章节 key，服务端先查本人未交付报告，再从存档读取章节和盘面；他人报告、已交付报告、引擎算定的盘面事实章均不能进入模型。前端对事实章也隐藏 AI 起草入口。
- 模型调用现经统一 AI 网关，以老师账号记录场景用量并跳过语义缓存；每个账号每分钟最多 20 次起草请求，超限不调用模型，失败或空回答退还预占次数。Redis 已连接但计数命令报错时暂停起草；RedisService 自身的内存降级仍只提供进程内限流，多实例全局限额需真实 Redis 验证。
- 手建报告没有盘面且章节为空时拒绝凭空生成；老师先写下要点后可用 AI 润色，编辑页先保存原稿再调用服务端，提示词要求不增补判断，并明确区分“AI 起草”和“AI 润色”。模型运行期间若报告被修改或交付，返回前再次核验版本与交付状态，旧结果不落到编辑页。
- 章节起草 Jest 6 项通过，服务端、控制器、编辑页和接口层 ESLint 0 error。全量类型检查仍因前述其他模块的既有错误失败，本次改动文件未出现在错误列表。旧版客户端仍提交旧参数，会收到“请更新工作台”提示；需随服务端一并更新 H5／小程序并做真实模型、用量账单、双设备和限流联调。模型实际内容是否遵守“只润色不新增判断”仍需人工样本审核。

## 2026-09-23 补充：报告编辑未保存离开保护

- 报告编辑页顶栏返回及系统返回遇到未保存修改时，提供“保存并返回／放弃本页修改／继续编辑”；保存失败留在当前页，生成、保存或撤回正在进行时暂缓离开。预览和归档案例也先保存当前修改，避免打开旧稿或引用未保存的内容。
- 通用 `ToolHeader` 原以 `attrs` 判断是否监听已声明的 `back`/`share` 事件，Vue 不会把这些监听器留在 `attrs`，导致自定义返回和分享处理可能不触发。最小 Vue 运行时验证得到 `attrsHasBack=false`、`vnodeHasBack=true`；现从组件 vnode 读取监听器，报告页的返回保护以及其他使用该顶栏的排盘页才能按自定义事件运行。
- 两个改动页面 ESLint 0 error；移动端全量类型检查仍因其他排盘模块既有的 16 项错误失败，改动文件未出现在错误列表。需在 H5、微信小程序和 Android 系统返回手势中验证操作单与导航行为；关闭浏览器标签页或强制结束应用仍无法由页内返回拦截保护。

## 2026-09-23 补充：报告归档案例的来源闭环

- 报告编辑页“归档案例”现在只传报告 ID，案例页向服务端读取本人已保存报告，预填标题、客户称呼及分类；保存案例时记录来源报告 ID。案例卡片增加“查看来源报告”入口，老师复盘时可回到原稿。导航地址不再携带客户称呼和报告标题。
- 案例创建接口核对来源报告归属，拒绝把他人的报告挂到自己的案例；手建案例仍无需来源报告。案例搜索按页面提示加入标签精确匹配，标题和客户称呼仍支持包含匹配。
- 新增案例服务测试 3 项通过，改动文件 ESLint 0 error；移动端全量类型检查仍有其他排盘模块既有的 16 项错误，本次文件未出现在错误列表。尚需在真实 H5／微信客户端和数据库上验证报告到案例的跳转、预填、保存、回看；来源报告删除后，已有案例仍保留来源 ID，点击回看会由报告页提示不存在。

## 2026-09-23 补充：案例发生日期按日历日展示

- 案例日期由日期选择器提交 `YYYY-MM-DD`，服务端保存为 ISO 时间。案例列表和编辑表单现直接读取 ISO 的日期部分，不再转成设备本地时区后取年月日，避免 UTC 负时区把选定日期显示为前一天。
