# 圈规治理记录可追溯收口（2026-09-22）

圈规页原本只取治理记录前 20 条，之后写“仅展示最近 20 条”；查询失败被静默处理成“暂无治理记录，圈内风清气正”。管理角标也只计算违规列表前 50 条，超过 50 条时可能把局部计数说成整个圈子的计数。

本批把匿名治理记录改为可续页浏览；首次失败和续页失败分别可重试，续页失败保留旧记录。记录读取失败不再显示空记录或给圈子行为作评价。近 30 天处理数和待复核角标只有在接口结果覆盖全部违规记录时才显示精确计数。治理配置请求失败时保守只读并提供重试，避免把网络故障说成管理员权限不足。顶栏避开微信胶囊，返回按钮至少 44px。

`tests/release/circle-governance-log-browser.mjs` 使用本地夹具验证首次失败、20+3 条分页与续页重试、局部计数隐藏；没有执行圈规修改、违规处置或真实用户写入。移动端类型检查、小程序构建与产物校验通过；主包 1.63 MB，`pkg-circle` 0.98 MB。微信真机、实际管理权限和线上记录完整性仍需核验。
