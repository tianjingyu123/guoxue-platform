# 达人咨询公开入口权限投影

## 本轮范围

- 圈内达人、全平台发现及个人咨询服务三份列表，统一使用主库独立音视频授权投影。
- 固定头衔条件不再提前剔除平台直授的普通成员；未经授权的旧通话价格不会产生连麦入口。
- 文字提问仍保留原 OWNER/PARTNER/GUEST 规则，不借音视频直授扩大文字付费提问权限，不修改普通阅读/观看。
- 音频和视频分别返回 `audioCallEnabled`、`videoCallEnabled`，不返回内部授权快照。
- 每批最多 100 个候选，在 REPEATABLE READ 主库事务中批量读取圈子、成员、最新授权和策略，避免逐达人逐能力查询。该公开投影不替代扣费/接听事务里的再次授权核验。
- 客户端三份适配器只接受严格布尔授权和有效整数单价。预约页不再用 URL 中旧价格恢复失效服务，只展示获批类型。

## 本地证据

- PostgreSQL 16.13 隔离全工作流 109/109；新增三项真实表验证：旧价不开放音视频且文字提问保持原准入、普通成员直授与撤销后列表变化、仅视频批准及账号停用后的可见性。
- 隔离目录 `artifacts/local-qa/circle-workflow-pg-gM0l8d`，端口 57494，360 表；已停止、无 postmaster.pid，合成证据保留。
- CircleExpertService 专项 18/18；最后连同通话与执行者测试联合复验 4 suites、62/62。
- 客户端适配器/预约页真实脚本 VM 测试 5/5：拒绝非布尔授权、音视频分流、撤权清理、URL 旧价格不能充当授权。
- server tsc、mobile vue-tsc、Nest 生产构建、范围内 diff-check 均 Exit 0。

## 未完成边界

- 服务配置读写与预约时段入口尚有旧角色过滤，需继续统一；不能宣称管理员直授已完整可运营。
- 全平台列表仍沿用原候选数量上限，实际可见结果可能少于请求上限；后续需补候选筛选与分页测试。
- 现有客户端列表适配器会把网络失败转换为空列表，尚需与各调用页一起改成明确失败/重试，不能只改单页导致未捕获异常。
- 真实原生通话组件、额度与 RTC 收尾尚未闭环，不发起真实扣费。
- 未部署、未操作云端、未做新包或真机视觉验收。
