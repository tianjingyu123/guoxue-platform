# 服务端配置测试集合整合回归（分片全绿）

工作目录：当前 v243 独占工作树的 `apps/server`。

全量命令：`node node_modules/jest/bin/jest.js --runInBand --silent --reporters=default`。
进程句柄 88599 已只读取得终态 ExitCode 1：Node 在约 4GB 堆上限发生 `JavaScript heap out of memory`，未输出最终测试汇总。不能记为全量通过，也不能将已观察失败列表当作完整列表。后续改为四个互斥测试分片、逐个独立进程运行，覆盖全部测试集合并保留每片 JSON 结果；不跳过测试、不 forceExit。

## 已观察的失败

- `test/video.e2e-spec.ts`：公开详情夹具未提供 PUBLISHED 状态，现有服务安全返回 404。
- `test/video-publication-redline.e2e-spec.ts`：真人创建断言漏掉传入服务层的 HUMAN 执行者参数。
- `test/circle.e2e-spec.ts`：预约请求未带当前接口要求的 circleId，且需重新核对能力/圈子事务夹具，不可仅添加参数后放行。
- `test/paipan.e2e-spec.ts`：旧测试只切换环境变量为 native；当前整套运行模式门禁返回 404，需核对运行配置与资格夹具，不能关掉守卫。

本列表是运行中观察，不是完整终态清单。

## 排盘与圈子预约 HTTP 复验

- `test/paipan.e2e-spec.ts`：11/11 PASS，进程 75339 ExitCode 0。真实 JWT/整套守卫不变；仅模拟准确主库模式查询，未知 SQL 拒绝。验证 legacy 普通用户 404、native 匿名 404、允许的登录用户正常排盘与管理路径。
- `test/circle.e2e-spec.ts`：15/15 PASS，进程 50840 ExitCode 0。预约明确传圈子；保留真实咨询投影、能力策略、事务授权服务，使用有限 SQL 夹具而非替换成恒定允许。验证直授有效时正常预约、未开放时段 400、事务重读服务停用 403、直授停用后时段与预约均 404 且不创建。使用合成角色与数据，没有访问实际数据库或创建真实预约。
- 当前新增修复仅涉及测试，不改变生产权限、云端配置或运行中候选。

## 本轮已修复并独立复验

仅修改两个视频测试文件，未改生产权限：公开详情补 userId/status/visibility/isPrivate；断言匿名未点赞及播放计数；新增草稿、私密、审核拒绝内容匿名 404 且播放计数零写入三项。红线真人创建断言明确 HUMAN，仍断言不冒充管理员。

专项命令：`node node_modules/jest/bin/jest.js --runInBand --testPathPattern='test/(video.e2e-spec|video-publication-redline.e2e-spec).ts$' --silent --reporters=default`。

进程 6298：2 suites，22/22 PASS；曾提示一秒内未退出，继续轮询后正常 ExitCode 0，未 forceExit、未重启。

全量命令在上述两个文件修改前已执行到这两套测试，因此首次全量结果仍会包含原失败；必须后续复跑才能证明最终整合通过。本轮未云部署、未改 DNS、未实际付款。

## 最终分片终态

命令：`node --max-old-space-size=6144 node_modules/jest/bin/jest.js --runInBand --shard=N/4 --silent --reporters=default --json --outputFile=.../shard-N.json`。四片依次运行，每片自然 ExitCode 0，未 forceExit。未更改 Jest 配置或排除规则。

| 分片 | 句柄 | 套件 | 用例 | 失败/跳过 |
| --- | --- | --- | --- | --- |
| 1 | 88361 | 138 | 1991 | 0/0 |
| 2 | 79992 | 138 | 1932 | 0/0 |
| 3 | 9560 | 137 | 1959 | 0/0 |
| 4 | 70127 | 137 | 2099 | 0/0 |

合计 550 套、7981 项 PASS。外部证据目录：`D:/gx-deploy-91/artifacts/local-qa/v243-server-regression-20260905`，四份 JSON 保留逐测试结果。

`verify-shards.cjs` 重新执行当前 Jest `--listTests --json --runInBand`，确认四片唯一文件集合与发现集合精确一致：550/550、遗漏 0、重复 0、pending 0。核查器默认沙箱子进程启动失败，改为沙箱外只读执行后 ExitCode 0；不是业务失败，也未绕开断言。服务端 `tsc --noEmit`（42590）ExitCode 0。

范围限制：该结果只覆盖 `jest.config.ts` 的发现规则。独立 PostgreSQL integration-spec、覆盖率阈值、云端供应商能力、真机行为、部署后版本/健康与支付沙箱证据均不能由此推出；需继续独立验收。当前候选仍未冻结或发布。
