# 圈内能力派发账本：本机真实数据库验证

当前 v243 未提交候选，不代表 HEAD 或线上包已包含修复。未连接预发布/生产，未调用媒体、支付或消息供应商。

## 本轮结果

`circle-capability-quota-pg.integration-spec.ts` 真实 PostgreSQL 16.13：19/19 通过，0 跳过。

新增四项：派发和额度同事务故障回滚；一个额度只允许一条派发记录；双连接争领（实际观察到 advisory 锁等待）仅一方成功；租约到期进入 UNKNOWN，保留 ACTIVE 额度及派发身份，不能再次领取。

原十五项额度回执、最后名额竞争、同请求重放、撤权与激活、可信收尾、到期回收、不同服务者共享额度和 UTC/上海/洛杉矶时区用例同时通过。

领取 SQL 已补充强制条件：关联额度必须 ACTIVE、已激活且派发租约不超过其原占位期限。测试领取前使用实际额度服务在同一事务激活，避免外部已发送时仍是可超时回收的 HELD。后续业务服务必须对零行更新抛错，确保激活与领取一起回滚，不得忽略返回 null。

## 环境及限制

- 本次新建目录：`D:/gx-deploy-91/artifacts/local-qa/circle-capability-pg-cHmONE/data`。
- 仅监听 `127.0.0.1:54909`；测试结束输出 `LOCAL_PG_STOPPED`，保留合成数据供追溯。
- 按顺序应用四项候选增量：工作流、额度、平台直授、派发。
- 此库以最小 Circle 表和候选迁移构造，真实 SQL/约束/事务，账号及资格上下文仍为合成夹具。不是完整 357 表环境，也不是线上 PostgreSQL 版本兼容证据。
- 首轮在 postgres.exe --version 阶段因沙箱 EPERM 失败，未建库；获得本地测试执行许可后完成上述独立一轮。未重复不确定的数据库启动。
- 未实现业务适配器/执行器注册、真实业务对象创建、媒体资源幂等或响应丢失对账；不得宣称圈子直授已端到端可用。
