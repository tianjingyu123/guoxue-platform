# 短视频与审核台账原子写入

原 VideoService.create 先单独写视频，再调用吞错的 openContentAudit，登记失败仍能回成功，留下待审视频无台账。

现视频及 PENDING 台账使用同一 Prisma 事务，AuditService.openContentAudit 接受调用方事务并在事务分支抛出错误。队列调用在事务完成后执行。其他旧业务未传事务的日志补审语义暂不改变。

视频服务、审核服务、专项共65/65，server tsc通过。专项覆盖成功传递同一事务、真实审核方法失败抛出、不继续调机审、视频插入失败不建台账。既有审核测试故障注入输出 ERROR，为预期测试日志，不是线上观测。

尚需真实 PostgreSQL 回滚证明；当前事务模拟测试不证明实际原子落库。异步机审仍是既有调度，尚未实现可靠事务消息；能力授权/额度未接入。未提交、未部署、未接触真实视频/供应商。

## 后续真实 PostgreSQL 证明

本轮新增 video-create-pg.integration-spec.ts，真实 VideoService.create + AuditService.openContentAudit + Prisma/PostgreSQL，3/3：正常视频/台账各1条且调度1次；真实审核表约束拒绝插入后两表均0且无调度；两次写入后提交前真实SQL错误同样全部回滚。

成功用例最初标题“调度观察到已提交数据”过强，已收紧为“成功提交两条记录并调度一次机审”；实际计数在完成返回后由独立 observer 读取，没有声称队列回调本身读取数据库。

隔离库 PostgreSQL16.13/356表，localhost:54005，目录 circle-workflow-pg-mUSJ98/data，正常停止。server tsc通过。可见性与机审调度为测试替身，未读取任何真实视频；本轮证明数据库回滚，仍不证明供应商、可靠任务恢复或授权额度已完成。
