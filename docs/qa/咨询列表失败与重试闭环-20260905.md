# 咨询列表失败与重试闭环

## 本轮问题与修改

- 三份咨询列表适配器原本捕获异常返回空列表，使网络失败与真实无服务混淆。现网络异常或非法响应结构向页面抛出固定脱敏错误，合法空数组才表示无服务。
- 达人列表重查前清理旧服务/好评率；异步请求版本检查避免旧结果覆盖新圈子的查询。
- 圈子问答区域新增局部失败提示和重试；失败不标记永久已加载。图文列表只保留实际正价图文服务，不为仅获音视频批准的达人生成无响应图文按钮。
- 个人主页咨询区域独立加载、失败、重试，不等待私信关系查询；失败清理旧购买入口，切换用户后旧响应不能覆盖当前服务。
- 既有即时通话页面已具备错误捕获，数据层抛错后会显示加载失败，而非误报未开通。未开放真实通话或预扣。

## 验证

- `consult-availability-runtime.test.mjs` 13/13 PASS：包含三份适配器的网络/非法结构/真实空数组区分，圈子失败重试恢复与音视频单独批准，个人主页失败清理及换用户乱序响应。
- mobile vue-tsc Exit0；范围内diff检查Exit0。
- 全部为真实源码脚本在VM中的逻辑验证，非手机渲染或触摸证据；尚未完成本轮App/H5构建及视觉复验。

### 后续 H5 实际编译证据

- 首次构建因未传三个公开域名配置而被门禁拒绝；未降低门禁。随后使用 `vite.config.ts` 的正式公开域名基线显式编译，`pnpm run build:h5` 终态 Exit0 / Build complete。
- 本地独立产物：`D:\gx-deploy-91\artifacts\local-qa\consult-state-h5-20260906`，1145 文件、12,277,852 字节。`index.html` SHA256：`5BD7B326A7A4B44B8F9D86B86235299CFFA9BD6FD1DC717AA1D6028C806B6C28`。
- 新提示已实际编入 `circle-consult-data.BNEvdcaY.js`、`pkg-circle-circles-consult-experts.HVJS_WUy.js`、`pkg-circle-circles-detail.Band1qlF.js`、`pkg-circle-user-profile.DqjAJkRG.js`。
- 构建输出有既有 Sass legacy-js-api 弃用警告。仅为未冻结工作树的本地编译证据，不能当正式发布包；未上传、未访问正式业务接口，App编译及视觉/触摸仍未验。
- 后续只读排查另确认：圈子问答的 `qaLoaded` 与页面返回刷新没有联动，达人列表/个人服务也缺完整的返回刷新策略。还需修复授权变更后的陈旧入口，不能把本轮网络重试修复当成所有状态同步完成。

### 后续生命周期修复（晚于上述 H5 编译）

- 达人列表、即时连麦页改为 onShow 查询，onHide/onUnload 作废请求并清理旧入口；不同时在 onMounted/onLoad 重复发起同一查询。
- 个人主页咨询使用 onShow 重查，离开页后清理并作废未返回的服务请求；不影响资料展示。
- 圈子问答返回时清除已加载标记；当问答页签可见时重新查询。离开后迟到响应不再恢复旧问答入口。
- 最新真实脚本 VM 测试16/16：增加返回/撤权、隐藏后迟到列表、迟到好评率及个人服务响应等测试。mobile vue-tsc及范围diff检查Exit0。
- 上述H5目录是本阶段之前的产物，不能代表最新生命周期修复；最新App/H5构建、真机和视觉验收仍需执行。没有操作云端或真实支付。

## 设计与边界

### 最新生命周期版编译与综合回归（20260906）

- 最新 App 本地 `build:app` Exit0，输出 `D:\gx-deploy-91\artifacts\local-qa\consult-lifecycle-app-20260906`：478 文件、13,832,411 字节。`app-service.js` SHA256 为 `7E51BEE062716F138D1A1518B294E46C76C4D57C9DE37D8130AC7738B16DE7C7`。原生脚本审计（含 host nvue）Exit0。
- 随后顺序执行最新 H5 `build:h5` Exit0，输出 `D:\gx-deploy-91\artifacts\local-qa\consult-lifecycle-h5-20260906`：1145 文件、12,278,395 字节。`index.html` SHA256 为 `9D941F201548A5638831574C341F95F2D4F0588333F6C558D1958214EB092361`。使用已存在的正式公开域名配置，仅本地编译，不访问真实支付。
- 综合移动门禁先出现直播静态断言不匹配：实现保留真实在线预检并新增 executor 透传，断言仍要求旧调用格式。更新断言明确同时要求 `obsPreflight: true, executor`，并补充未在线抛错的结构检查，没有删除预检。
- 沙箱内 Node 测试子进程出现 `spawn EPERM`，不计业务失败；同一命令在允许子进程的环境执行，`release:test-mobile-native-bundle` 两阶段分别 125/125、249/249 PASS，Exit0。
- 服务端 `live.service.spec.ts` 124/124 PASS，涵盖横屏禁止绕过媒体预检及 OBS 离线拒绝；咨询真实源码 VM 16/16 再次 PASS。
- 构建仍有既有 Sass 弃用警告；未按构建器通知升级 DCloud。以上基于未冻结工作树，不能作为正式签名包或已部署证据。视觉、双端触摸、分享、罗盘、支付拉起及真实媒体闭环仍待验收。

按前端设计技能的失败状态指导，使用热卜已有颜色/字号与局部提示，重试按钮说明实际动作，不新增整屏拦截，不阻断普通圈子内容和个人资料阅读。没有推送、云打包、部署、改DNS或真实支付。

未扩展到好评率等非关键装饰数据；其失败仍按既有行为隐藏，不伪造统计。咨询运营全链路和真实媒体验收仍未完成。
