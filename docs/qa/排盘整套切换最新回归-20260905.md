# 排盘整套切换最新本地回归

## 当前产品边界

只配置整套 legacy/native 模式，不新增逐工具权限。legacy 不因第三方故障自动回退 native；共用罗盘不受自研专属门禁；原有会员、数据所有权等业务规则保留。

## 本轮证据

- `release:test-mobile-native-bundle` 主回归：247/247，通过。初次沙箱运行因 Node 子进程 spawn EPERM 未执行主组；获准在沙箱外执行同一命令后通过，未降低测试隔离。
- 深链、整套导航、H5 历史路由聚焦回归：16/16，通过。
- 移动端 vue-tsc：Exit 0。
- `git diff --check`：通过。
- 修复原生深链在被用户新导航取消后仍触发首页兜底的问题；使用真实函数体执行测试，保留正常失败回首页行为。

## 仍未证明的发布条件

### 后续同日收口

- 原生冷启动新增两条深链竞争测试，修前打开两个目标、修后只恢复最新目标。
- H5 真实 Vue Router 新增迟到拒绝测试，修前把圈子页拉回排盘、修后取消旧导航。
- 最新 `release:test-paipan-suite`：40/40。
- 服务端模式策略、运行时模式、后台保存专项：3 suites / 19 tests，通过。
- 包含上述修改的本地 H5 构建：DONE / Exit 0；`index.html` SHA256：`4385969F259E317C273CE11DA419B2A4E5011D816EF79EBB119C163113A162D5`；入口资源 `index-ZqvkEJHq.js`。这是工作树构建证据，不是冻结提交或部署证明。

### 后续小程序构建

- 本地 `build:mp-weixin` 全流程 Exit 0；WXML 461 文件、文本产物 1983 文件通过。
- 主包约 1.50 MB，保守上传估算 1.79 MB；30 分包，最大圈子约 0.99 MB，体积门禁通过。
- 实际产物中 `pkg-paipan`、`pkg-paipan2`、`pkg-paipan3`、`pkg-common/compass`、`pkg-common/legacy-paipan` 目录均不存在；app.json 无排盘分包声明。
- app.json SHA256：`B92B251E9D9D19F3236145374A57B9D3C70AAA53E0849FAEFA497BD5F4440B06`。
- 未上传微信；此记录不代表开发者工具预览或平台审核通过。

### 最新浏览器实测范围

- 本地 H5 API 指向本机空端口 4190，不使用线上登录态。直接打开 `/h5/pkg-paipan/bazi/index`，实际 URL 改为 `/h5/pages/paipan/index`，页面显示状态无法确认及重新连接、返回首页，没有展示自研表单。
- 直接打开 `/h5/pkg-common/compass/index`，显示电子罗盘、盘面与感应入口，不受自研资格阻断。未点击设备方向授权，不将默认盘面读数作为真实磁北证据。
- 浏览器后退回承接页，未恢复原自研冷链接。此为匿名/断网边界，不替代登录态模式切换和手机返回验收。
- 验收临时标签已关闭，本地服务停止；原有三个用户标签未修改。

### 页面生命周期补充回归

- 金钱课、孔明神卦统一使用整套资格；隐藏/卸载停止摇卦计时并清空结果，重复开始不创建第二个计时器，旧会话迟到回调不能污染新会话。
- 二十四节气不再在脚本初始化时启动时钟，仅整套资格通过后启动；隐藏/卸载停止，返回后重新核验并只保留一个时钟，清除之前选择的节气及海报模块状态。
- 真实页面脚本与统一 composable 联合测试已覆盖上述情形；最新 `release:test-paipan-suite` 为 62/62，移动端 `vue-tsc --noEmit` 与 `diff --check` 均通过。
- 必要条件扫描为 83 页中 59 页接入、24 页待处理；无新增单工具权限配置。上述是本地源码证据，之前的 App/H5 构建不包含这批后续修改。

### 尚待验证

立极尺后续补充：入口/盘面/历史统一整套核验，记录使用账号隔离且公共旧记录原样保留。保存快照后恢复当前户型图/角度/笔记，双击不重复写；离页后的图片选择及尺寸测量回调不恢复旧状态。盘面未取得中心坐标前不计算拖动角差，避免首次拖动跳角。5 项脚本回归通过，整套专项 111/111、移动端类型检查与 diff 检查通过，扫描 73/83。真实相册选择是否触发 App/页面隐藏、图片返回及拖动体验还须签名包真机验证，不能以合成回调证明真机通过。

八宅后续补充：入口/结果/历史页接入统一整套资格；存储使用 createPrivateHistory，停止从无归属公共键自动迁入并删除旧记录。真实结果脚本与真实存储组合验证改名原位更新、再次显示保持改名、返回不重复写、账号隔离及作用域外拒绝写。整套专项 106/106、移动端类型检查与 diff 检查通过；扫描 70/83，剩余 13 页。历史页组件交互仍需整体运行验收，不以存储单测代替真机。

山向奇门后续补充：入口和结果页接入同一整套资格，记录改按服务端确认账号命名空间存储；未确认归属的公共旧记录不读取、不删除、不自动认领。结果同页返回不重复保存，无原始 ts 时保存稳定 ts；打开历史保留当前表单，清空需当前会话确认。4 项脚本回归、整套专项 105/105、移动端类型检查与 diff 检查通过；扫描 67/83，剩余 16 页。上方 C96E… App 产物早于这次山向修改，不能作为山向最新编译证据。

五运六气及构建补充：初始化计算移入统一准入回调，隐藏后重置年份、气步和体质选项，迟到 watch 不重新计算。整套专项 101/101、移动端类型检查、diff 检查通过；扫描 65/83，剩余 18 页。最新 App 本地构建及原生脚本审计 Exit 0；app-service.js SHA256 `C96E997BE979F78902196B74CCA83A4F8EBCF2313847C0A3EDF681AD9A044CBA`。最初沙箱 spawn EPERM 后仅以审批允许的本地构建方式重跑；没有云打包、签名或安装。此构建包含前述页面修复，旧 H5 构建仍不代表当前源码。

案例投稿后续补充：整套核验后读取奖励方案，投稿前再次核验并快照表单；双击只提交一次。当前业务失败恢复表单，成功清空；离页/切号/模式关闭后迟到响应不弹窗、不恢复、不自动重试，旧成功弹窗不能劫持返回。6 项脚本回归通过；整套专项 100/100、移动端类型检查通过、diff 检查通过，页面扫描 64/83。没有真实投稿；已发出的请求可能已到服务端，客户端不声称能撤销。

案例详情后续补充：加载个人练习状态与写操作均绑定整套资格和当前会话；旧公布弹窗在隐藏/切号/重新进入后失效；断语保存完成后必须再次核验才公布答案。已经发出的保存请求不宣称可撤回，但退出后不自动继续或重试公布。自评成功后才显示新分数，失败保留原状态并提示。8 项脚本回归、整套专项 94/94、移动端类型检查与 diff 检查通过；页面扫描 63/83。未写真实案例，投稿仍待处理。

案例列表后续补充：移除挂载/显示时重复获取个人称号，统一进入核验后加载；隐藏、切号和整套关闭后迟到列表/榜单不回显。分页成功才推进页码，失败重试不跳页；贡献榜网络失败明确显示重试。8 项脚本回归通过，整套专项 86/86，页面扫描 62/83；案例详情与投稿尚未完成，不将列表通过泛化为整个案例库通过。本轮未请求线上案例或改写真实数据。

AI 解盘后续补充：不再挂载即读取个人记录；整套资格通过后加载，分析前再次核验并防重复提交。离页/切号/模式关闭后的列表与解读迟到结果不恢复，错误弹窗只对当前有效页面展示。新增 8 项脚本回归通过；整套专项现为 78/78，页面必要条件扫描 61/83，剩余 22 页。没有线上 AI 调用或真实额度消耗。

字典后续补充：查询/AI 请求通过统一会话核验执行，离页、切号或整套关闭后的迟到结果不会恢复查询及出生信息；重复点击不重复请求，当前成功 AI 结果仍保留本次查询上下文。8 项真实页面脚本回归通过；整套专项现为 70/70，页面必要条件扫描 60/83，剩余 23 页。未调用线上 AI、未消耗真实账号额度。

- 以上不代表新签名包真机通过，也不代表预发布已经运行这些修改。
- H5 整页历史导航守卫安装与原生系统返回仍需实际运行验证；实际 Vue Router 内存历史测试不能替代设备验证。
- `audit-native-preview-pages.mjs` 仍是逐页面接入的旧必要条件扫描。不能将其通过数理解为后台存在逐工具开关，也不能直接删除扫描声称统一覆盖完成；应在整套生命周期覆盖验证后替换其发布判定方式。
- 本轮未推送、云打包、安装、部署、迁移或修改 DNS。
