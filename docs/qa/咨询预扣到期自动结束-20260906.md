# 咨询预扣到期自动结束

本地未冻结实现，未部署、未触发真实通话或资金操作。

- 根据订单已预扣金额/锁定单价和接听时间计算截止，不从客户端接收时长，不追加扣款、不自动续费。
- 内部任务每10秒扫描，单批20条、时间预算20秒、游标翻页；单行失败不永久挡住后续订单。两节点共享圈子事务锁和订单行锁。
- 只处理有效ONGOING订单；结束状态、到期时长、已预扣范围内结算、BUDGET_TIMEOUT停流意图、达人收益和总账同事务提交。失败回滚，手动挂断竞争不重复结算。
- 事务提交后调用既有单次停流派发器；网络失败由原有持久待办跟踪，不因此再次结算。不伪造HUMAN身份调用外部结束接口。
- 客户端发起说明增加额度用尽自动结束、不续费提示。尚未新增动态倒计时，不把页面显示当作服务端强制停流证明。
- 专项与相关基础策略初测48/48。独立PostgreSQL真实双账测试1项通过，153项未选中；错误60/40分账规则导致全回滚，修正本地规则为50/50后与手动挂断竞争仅一条收益、两条总账各4元、预扣80币无追加扣款。上述均为合成隔离数据，供应商未调用，测试库已停止。
- 最终相关4套单元67/67、TypeScript、Nest构建、限定diff检查通过。原一天票据窗口、候选冻结、真实云配置和双端音视频仍未验收。调度轮询与供应商执行存在延迟，不能承诺毫秒级截止。

Git历史WAL分发风险未处理；未修改历史引用、远端、DNS或线上数据库。
