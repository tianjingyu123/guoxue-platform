# 圈子、咨询、直播与视频交叉回归

## 本轮范围

仅调整 `apps/server/src/modules/circle/circle.service.spec.ts` 的 TestingModule 注册：补齐真实的 CircleConsultVisibilityService、CircleCapabilityRepository、CircleCapabilityService、CircleCapabilityEligibilityService。未将权限判断替换为默认放行，也未改生产服务。

此前同范围 69 个套件中，圈子 façade 的 20 项因依赖注入失败未运行；其余 68 套件通过。本轮修复后业务断言实际执行。

## 可复验命令与结果

工作目录：`apps/server`。

```text
node node_modules/jest/bin/jest.js --runInBand --testPathPattern='circle/circle.service.spec.ts$' --silent --reporters=default
1 suite / 20 tests PASS，ExitCode 0。

node node_modules/jest/bin/jest.js --runInBand --testPathPattern='(modules/(consult-call|circle|live|video)/|common/trtc-callback)' --silent --reporters=default
69 suites / 1364 tests PASS，ExitCode 0。
```

工具进程分别为 70753、74625，均已读取终态。修改文件 `git diff --check` 通过。

## 证据边界

此为本地单元回归，不代表真机、真实媒体网络、支付沙箱或已部署版本验收完成。测试输出包含故障注入产生的错误日志，不应转述为线上故障；也不能仅以全绿断言真实云端已可用。本轮没有部署、数据库迁移、DNS 或真实支付操作。当前候选仍未冻结。
