# 服务端全量源码单测收口

当前本地工作树（含未提交候选）执行：

```
node node_modules/jest/bin/jest.js --runInBand --testPathPattern='src/.*\.spec\.ts$' --silent
```

终态 Exit0，491 suites / 6893 tests 全部通过，耗时258.212秒。本轮为同一次完整运行，不是累加不同批次数字；运行期间未修改源码。

覆盖当前排盘整套权限、圈子授权/额度/派发、视频状态保护、审核事务等源码单测。此前装配失败的鉴权/用户与排盘控制器测试本轮正常执行。

边界：只含src下.spec.ts，不含test目录HTTP E2E、真实PostgreSQL测试、客户端构建、真机、商店/支付/生产门禁。部分模拟故障会输出ERROR；已知审核后置处置与平台费测试替身不完整仍不能作为相应真实业务闭环证据。AI余额不足错误已检查为publish-assist.service.spec.ts主动模拟异常。

无线上变更。新圈内能力业务入口与执行器仍未完整接线，严禁据此宣称系统可正式发布。
