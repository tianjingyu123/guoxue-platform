# 实时咨询客户端接线进度

状态：本地实现与专项测试，未云打包、未安装、未部署、未真实扣费，不是双机调通证明。

## 已实现

- 新增 App 原生通话页，预约页只导航，通话页重新核对服务资格与单价后由用户确认发起。
- 会话控制器先检查设备能力/权限，再调用预扣接口；双击防重，响应丢失后不重放扣费请求。
- 接听、拒绝、按最新业务状态取消或结束、服务端终态确认、账单入口；本机媒体异常不冒充退款或结束。
- 私密票据只在请求返回与原生连接之间传递，不放进路由、页面公开状态、持久化状态或日志。
- 直播与咨询共享进程级 TRTC 占用互斥，非持有人不能销毁另一个业务的采集和事件监听。
- 原生适配器复用已接入插件协议。iOS 先触发本机采集并查询真实授权状态，再允许发起；语音不请求相机。
- 本人 active 查询只返回待接听/进行中安全字段，限定参与者、最多 20 条、禁止缓存；App 我的通话前台每次完成后等待 5 秒刷新，离页停止，错误停止并显示重试。
- 统一移动专项脚本已接入状态同步、会话、原生适配器三组测试。

## 验证

- 停流部署接线补充：发现Compose遗漏 `CONSULT_TRTC_STOP_REGION`，导致即使宿主配置存在也无法进入server容器，worker长期WAITING_CONFIGURATION。已补空默认透传，并在.env.example列出专用四项；不默认启用、不写真实密钥。Compose专项2/2、停流worker10/10通过，diff检查通过。首次Jest从根目录运行未找到tsconfig，改在apps/server执行后通过；无远程命令/云端写入。该补齐不代表已实现最终媒体额度释放。

- 后续完整App编译：使用仓库规定的正式公开域名参数（api.rebugx.cn、api.rebugx.cn/h5/、static.rebugx.cn；channel=formal），build:app Exit0，原生脚本门禁通过。产物manifest=243，call-room.js=55171字节，包含onExitRoom/onConnectionLost/privateMapKey；iOS相机用途补齐主动视频咨询、连麦与预览，已进入编译manifest。未新增权限，语音不申请相机用例保留；三组客户端32/32通过。首次沙箱spawn EPERM、随后缺公开配置均不计成功；最后显式配置构建才通过。未云打包/签名/安装，此产物不可冒充预发布环境包或真机验收证据。

- 原生异常收口补充：onError/onConnectionLost在进房前后均由适配器主动停止采集并清理；所有原生监听检查所属代次与单例持有人，旧回调/旧入房Promise拒绝不能销毁下一代。更新后三组客户端31/31、mobile vue-tsc通过。没有云端操作，没有生成或安装新包；真机双向媒体仍待验。

- 后续收尾检查发现原生适配器遗漏 `onExitRoom`：现补服务端移出/解散及入房途中退出处理，立即关闭本机音视频采集、清理监听并释放RTC单例；迟到事件不能清理其他业务新持有人。三组客户端测试28/28通过（SDK模拟，非真机）；首次Node测试子进程EPERM后采用同进程测试模式成功。此改动不释放服务端额度、不宣称媒体最终收尾完成，尚未制作新的签名包。官方事件依据：https://trtc.io/zh/document/47638 。

- 三组客户端合成测试 26/26；包含原生 SDK mock，不代表真实插件互通。新增切后台发生在权限准备/创建响应返回前两条竞争测试：前者零发起，后者保留记录但禁止迟到启动媒体。
- 关联直播专项 25/25（此前与客户端前两组共 43/43）。
- ConsultCallService 专项 26/26；服务端和移动端 TypeScript 均 Exit 0。
- App 本地生产编译 Exit 0，生成 `pkg-circle/circles/call-room.js`。编译指出 nvue 不支持 min-height 后已移除并复跑成功，无该警告；最后的后台中断状态机改动已专项验证，但尚未重新制作签名包。
- H5 本地生产编译 Exit 0。导航审计补齐 MP-WEIXIN 条件分支及 nvue/uvue 源码扫描，不删除未知指令拒绝策略；两项测试通过，444 页面/349 字面量跳转无错误。
- 整条 `release:test-mobile-native-bundle` Exit 0：新增咨询组 26/26、排盘套件通过、末段移动共享用例 272/272；各阶段独立统计，不把末段 272 当作总数。最后一次移动 TypeScript 与已改文件 diff --check 均通过。

## 云端只读实测

### 已购权益与接入选择补充（2026-09-06）

- 新增真实旧结构升级测试 `consult-migrations-local-pg.integration-spec.ts`：从冻结f25b7471基线在本机全新PostgreSQL16.13库按顺序执行当前十项DDL，2/2通过。验证九张新增表零业务数据、咨询证据/停流字段及直授权限可空字段；重复DDL失败时前置哨兵建表同事务回滚，已升级结构保留。收尾停止本次本地实例，保留合成数据；未访问线上。此测试不模拟Prisma迁移历史，不能代替上线前真实迁移status/checksum/备份检查，也不代表已完成cd055线上结构差异验证。

- 接收环境后续预检：公网 `GET /api/v1/health` 返回200、status=ok、releaseId=`preprod-cd055084-20260903-r1`。对应提交的回调守卫仅识别公共 TRTC_SDK_APP_ID，不支持当前候选专用应用分流；因此本轮没有向该旧接收环境提交咨询回调地址。
- 当前候选 `pnpm --filter @guoxue/server build` Exit0；旧系统迁移代码门禁7/7；Prisma空库基线与schema完全一致（首次沙箱子进程EPERM，获准沙箱外重跑成功，不是连接线上数据库）。当前工作树存在10项未跟踪的新迁移，包括咨询media boundary/evidence/stop intent及圈子能力等，尚未冻结、未在本轮执行。构建通过不等于可以绕过迁移核定部署。

- 后续用户明确授权后，仅对 `1600160984 热卜私密咨询` 确认一次开启高级权限。回读基础功能页：高级权限 checkbox 已 checked，旁路转推仍未 checked；这是控制台保存证据，不是实际进房强制验证证据。页面提示约5分钟生效。旧应用未操作。
- 同次读取隔离应用 RTC 基础回调页：回调密钥、房间/媒体/录制/转推等地址均未设置。尚未提交任何回调配置或读取 SDK 密钥。服务端候选接收入口为 `POST /api/v1/live/trtc/callback`，原文 HMAC 守卫按咨询专用 SDKAppID/独立密钥分流；候选模块接线已存在。后续须先完成接收环境与专用配置一致性验证，再设置房间及媒体回调，不能仅填地址即判定成功。

- 控制台实测 `1600030106 热卜国学`：TRTC 尊享版有效至 2027-03-05；TUICallKit 群组通话版-38万分钟显示待激活。IM 应用列表同一 SDKAppID 为 Chat 旗舰版、使用中、到期 2026-09-24；Push 体验版已停用，到期栏为 2024-04-14。
- 时长包页面实测包 1000253080，剩余 380000 分钟，有效窗 2026-09-04 09:00:00 至 2026-10-05 08:59:59。TRTC 版本有效期不等于这笔月度时长有效期。
- 已创建隔离应用 `1600160984 热卜私密咨询`，未购买或激活试用，未读取密钥或启动媒体。它的创建记录取代下方早期检查时“未创建应用”的历史状态；旧应用未改。隔离应用基础功能已观察为旁路转推关闭、高级权限未启用，尚不可宣称私密通话云端门禁完成。
- 当前客户端使用 TRTCCloudUniPlugin 原生接口，不依赖 TUICallKit；不能把 CallKit 待激活当作当前代码进房失败的充分原因，也不为已有接口实现重复切换组件。SDK/API 是接入方式，不是另一家计费产品。
- 官方当前限时规则允许付费包月赠送通话时长抵扣同账号其他 SDKAppID；增值组件授权仍须单独核对，不能推断跨应用继承。依据：https://cloud.tencent.com.cn/document/product/647/85386 。既有 TRTC 尊享版与 IM 旗舰版满足群组 CallKit 版本组合条件，依据：https://cloud.tencent.com/document/product/647/104662 。本次未激活组件、未续费或购买。
- 继续沿用隔离媒体边界，不直接开启旧应用高级权限或修改其旁路配置。还必须完成专用应用权限/回调配置及真实双端测试；现有套餐充足不代表媒体已接通。离线推送停用单独记录，不阻断前台双端联调，但不得宣称支持锁屏来电。

- `1600030106 热卜国学`：高级权限未启用，指定流旁路，域名仍属于原系统；控制台明确提醒直接启用高级权限影响所有未携带 privateMapKey 的客户端。
- `1600068061 热卜体验测试`：高级权限同样未启用，全局自动旁路已开启，旁路域名 `198526.push.tlivecloud.com`。
- 仅切换查看应用，没有更改任何开关、域名、签名配置，没有查看密钥，也没有启动媒体或产生收费通话。
- 两者均不能据当前页面直接记为已验证的私密咨询环境；必须先确认运行时实际 SDKAppID、原系统使用边界和咨询媒体隔离配置。

## 仍未完成的验收条件

### 结算原子性修复（20260906）

- 后续补齐发起/接听前主库事务内的结算配置检查：既定50%/50%规则、类目、缓冲期、审批阈值不合格时，在建单/预扣/签发资源前拒绝；只读检查，不自动创建或修改配置。结束时仍验证双账，防止通话期间规则漂移。
- 更新后的准入与结算专项40/40、server TypeScript通过。独立本地 PostgreSQL选择执行2项通过（其余147项未执行）：停用规则零建单/扣费/资源占用；双账失败回滚及并发结束一次提交。无线上数据库操作。
- 云端再次只读确认1600068061仍为全局自动旁路开启、高级权限未启用。首次AX读取超时后DOM读取成功；随后返回应用列表动作超时，库存快照确认URL仍在原基础功能页。未创建应用、未改开关、未读取密钥，停止重复页面操作。云端隐私隔离仍未完成。

- 发现原 `end` 在结束/退款事务提交后异步写达人收益和总账，并吞掉失败；现改成状态、停流意图、退款、UserEarning 与 LedgerEntry 使用同一事务。公共结算引擎新增可选事务参数，其他业务默认调用方式保持不变。
- 咨询保留既定达人/平台各50%规则；缺引擎、缺规则、受益人/金额/凭据/比例/状态漂移均失败回滚，不输出“已结算”。不新增迁移，不调整线上分账规则。
- 本机新建完整基线 PostgreSQL：规则故意设为60%/40%时，真实退款余额/流水、收益、总账、结束状态和停流意图全部回滚；修正本地合成规则后两连接并发结束仅提交一次，退款72币、收益一条、总账两条各0.40元。选择执行2项通过，其余146项未执行而非通过；实例由测试收尾停止，合成数据保留，不连接线上。
- 咨询/收益/结算相关19 suites、240项通过；服务端 TypeScript 通过。后续补充冲正状态/比例/类目漂移测试另记结果。媒体ACTIVE额度仍没有提前释放；计费结束不替代媒体关闭证明。

- 服务端现已改为只消费 `CONSULT_TRTC_SDK_APP_ID / CONSULT_TRTC_SECRET_KEY / CONSULT_TRTC_CALLBACK_KEY`。专用应用不得等于公共直播应用，回调密钥不得复用；缺项时不签发票据，不回退公共环境。Compose 与示例配置已同步，未写入任何真实配置或密钥。
- 回调守卫按应用选择独立密钥，保留公共直播既有验签；交换应用头、串用密钥、咨询缺密钥均拒绝。票据/守卫/通话专项 3 suites、63/63 通过，server TypeScript 通过。配置完整仍不代表云端已关闭旁路或已启用高级权限，不能据此开放真实付费通话。

- 真实 SDK 双端进房、双方声音/画面、静音/切镜头、挂断退出，不能用 onEnterRoom 成功代替双向媒体。
- 当前来电入口为 App 我的通话前台列表；后台推送、锁屏来电、进程被杀恢复未实现，不能宣称已支持。
- 权限确认前无金币动作，权限拒绝/断网/迟到响应的真机与隔离账本核对；客户端请求失败不证明服务端零写入。
- 通话隐藏/媒体中断保留待核对状态；业务结束和媒体最终关闭仍按 B2 独立证据收尾，尚未释放资源额度。
- 当前本地实现未冻结，不能直接替换此前签名包，也不得把旧版手机的验证结果归到此改动。

权限状态依据：[DCloud getAppAuthorizeSetting](https://uniapp.dcloud.net.cn/api/system/getappauthorizesetting.html)。首次 iOS not determined 需要实际能力触发权限，不能误导用户寻找尚未出现的系统开关。
