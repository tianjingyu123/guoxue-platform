# 发布管理员事务内撤权复核

## 真实缺口与修改

直播领取推流凭据、开播、下播和管理员创建直播/短视频原先直接信任外层 isAdmin 快照。此次统一使用 publication-actor.ts，在真实主库事务内对账号加共享锁并检查 ACTIVE/未软删除，平台豁免另锁定并核对 bindId=null 的 SUPER_ADMIN/OPERATION_ADMIN 角色。

普通主播下播不重新要求发布资格或额度；撤销发布资格不应导致无法结束直播。RTC 签发的既有参与人检查同时补齐软删除字段。未改变普通内容浏览接口，也未启用尚未完成的 CircleCapabilityController。

## 验真

- live-publication.service、live.service、video.service：最终3 suites / 229 tests PASS。
- server tsc --noEmit：Exit0。
- 真实 PostgreSQL 16.13 / 完整361表隔离合成库：8项 PASS、138项未选中，并非全146项重跑。
- 三项真实并发撤权：先由第二连接删除管理员角色并持锁，签发/开播/下播连接实际等待，提交撤权后均403，房间保持WAITING，没有签发或新增额度。
- 管理员创建直播、短视频：有效身份各成功创建一条；并发撤权后第二次被拒，业务数不增长，异步机审未派发。
- 正常凭据重复领取复用ACTIVE、签名失败回滚、结束后禁止再次签发、嘉宾麦位移除后拒绝均通过。
- 最终隔离目录：D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-LviWYE/data，测试输出确认 LOCAL_WORKFLOW_STOPPED，合成资料保留。
- 后续补齐：管理员不能代已停用主播签发或开播，但仍可下播清理。独立真实表专项1/1通过（146未选中），目录circle-workflow-pg-I5WQu8已自动停止；两批合计9项真实表验证，不冒充全量通过。

最初新增集成测试因泛型联合返回类型未通过编译，未启动数据库；修正测试类型后取得上述真实结果。未关闭类型检查或放宽断言。

## 边界

无迁移、无云端写入、无外发/真实支付、未提交/推送/部署。咨询最终媒体关闭证明仍未完成：腾讯控制台只读加载再次超时，未取得当前SDK应用高级权限状态，不以历史监控列表或移出用户ACK替代最终证明。
