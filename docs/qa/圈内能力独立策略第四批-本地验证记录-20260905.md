# 圈内能力独立策略第四批：本地验证记录

记录日期：2026-09-05（UTC）。基线 `f25b7471f5063a612a17a422a64900a29b5f8cd3`，工作树 `D:\gx-deploy-91\.worktrees\mobile-paipan-compass-v243-20260905`，v243 保持不变。

## 本批精确代码范围

- `apps/server/src/modules/circle/circle-capability.policy.ts`
- `apps/server/src/modules/circle/circle-capability.policy.spec.ts`
- `apps/server/src/modules/circle/circle-capability-eligibility.service.ts`
- `apps/server/src/modules/circle/circle-capability-eligibility.service.spec.ts`

均为本地新增文件。此前分享/配置减频/咨询基础修复增量保留；本批未更改其他任务工作树，也未修改 mobile、manifest、schema、迁移、package 或 lockfile。

## 已实现及验证边界

- 四种圈内能力各自解析配置，缺失/损坏/禁用/未知版本一律关闭；没有老全平台授权或测试门槛的自动回退。
- 资格结果绑定圈、圈主、能力和策略修订；达标只允许规划待审申请，不产生已开通结果。
- 状态计划区分申请、批准、主动启用、停用、暂停、恢复、驳回、撤销；到期、串能力、串圈、自审批、自动化审批、超限批准和旧修订全部拒绝。
- 问答必须同时有圈级和指定服务者的同种有效授权；圈主不能替嘉宾主动启用。主体当前身份/成员关系变化会阻止新增使用。
- 数据采集器只读当前配置与有效数据库计数；不拿冗余页面人数放大资格，不把数据库错误变成“已达标”。
- 状态计划返回 expectedRevision、next 和审计信息，但**尚未写库，没有 HTTP 路由，也未成为真实业务权限闸门**。这些测试不是数据库事务、媒体 SDK 或线上权限证据。

## 实际结果

| 检查 | 结果 | 范围 |
| --- | --- | --- |
| 新策略与事实采集专项 | 125 tests PASS | 两个新测试套件；真实 TS 内核，数据库使用 mock |
| 扩大圈子/问答/原发布授权回归 | 9 suites / 239 tests PASS | 包含上述 125、上一批 99、既有发布授权 15；不能把它们当 239 个真机流程 |
| server tsc --noEmit | Exit 0 | 本批最终源码 |
| server Nest build | Exit 0 | 本地编译，无服务器运行改动 |
| 本批四文件 ESLint | Exit 0，0 error / 0 warning | 不覆盖其他历史文件的既有告警 |
| 四文件构建影响分析 | server=true；nativeApps/admin/H5/miniProgram=false；migrationReviewRequired=false | 只分析本批文件；工作树累计仍含前批客户端改动 |

未重复 App/H5/小程序本地构建或 DCloud 云包，没有制造新的版本号。后续整合时必须分析整个候选，不能用本批 server-only 结论跳过累计客户端构建。

## 下一步与剩余阻断

持久化、受保护的申请/审核接口、后台/客户端接线、原子配额与真实 PostgreSQL 并发验证待完成，按《圈内能力授权-持久化接入约束-20260905.md》执行。原 46 项端到端矩阵不因本批局部测试改为 PASS。

H5/小程序/App 分享接收方打开、真实二维码海报、罗盘方向持续变化、排盘底部完整展示与原支付确认页仍需设备证据。没有新增线上配置、数据库写入、提交/推送、COS/TAT/DNS、云打包、设备安装或真实付款。

## 运维支线协调记录（非本窗口重做）

按独占运维任务 `01a06219-f241-7bf1-8c11-a9da33166bdd` 回传：异机探测与演练收件通过；正式 PG14/5433 归档在隔离无 TCP 实例恢复，357 表/248850 行逐表一致、263 外键及索引约束检查通过；备份目录/脚本权限和 16 份归档权限已按用户授权收紧，归档哈希不变。相关证据为 `artifacts/admin-release-candidate-20260903/preprod-ad9b03a4/ops-prelaunch-acceptance-20260905.md` 和 `backup-permission-hardening-acceptance-20260905.md`。

本窗口未读取或输出备份脚本中的秘密，未重做恢复/权限整改。加密异地备份、密钥保管和备份失败告警尚未完成，由该支线继续，不能与本批业务状态合并成“已正式上线”。
