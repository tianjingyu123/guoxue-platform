# v243 上线收敛执行清单

## 当前快照与执行边界

- 工作树：`.worktrees/mobile-paipan-compass-v243-20260905`；HEAD `f25b7471f5063a612a17a422a64900a29b5f8cd3`，不是最终候选 SHA。
- 本清单创建前，`git status --porcelain=v1 -uall` 展开 504 项：server 180、mobile 155、admin 15、release 测试 61、QA 文档 74、需求文档 4、QA 脚本 10、发布脚本 4、package.json 1。数字包含新增文件，不等于已逐文件验收；先前 418 项使用了目录折叠统计。
- tracked diff 为 230 文件；不删除、不覆盖既有改动，不仅按文件数认定范围合理。
- 停止定时续跑。按问题推进，不重复未受影响的绿测试；旧构建不能证明后续源码。
- 不新增无关产品能力。不提交、推送、云打包、部署或迁移；正式动作前核对精确候选与适用授权，不沿用旧 SHA 的授权。

## 执行顺序及交付门槛

20260906 用户已明确：付费音视频问答指实时连线，不以录制后异步回复替代。交付必须包括发起、来电接听、音视频通话、挂断结算和资源收尾；当前客户端仅记录/订单/评价页不能视为完整入口。已补双方专属 GET /consult-calls/:id/state（JWT、UUID 校验、private/no-store、主库按参与者过滤、安全投影、不返回票据及房间参数），作为后续状态同步接口。咨询专项 10 suites / 163 tests 通过；该接口尚未部署，客户端尚无完整状态机与接听界面，不能据此放开真实扣费或宣称通话可用。共享 TRTC 应用高级权限仍保持原状态，不为测试直接开启。

| 项目 | 当前证据/缺口 | 下一步与验收门槛 |
|---|---|---|
| S1 差异收敛 | 已完成目录级盘点，尚未逐项归属复核 | 按客户端故障、运营配置、发布资格与媒体资源、测试证据四组审核；不把未完成功能直接隐藏后称整体完成 |
| C1 分享 | 已有安全链接转换、海报实现；公开结果 URL 契约及接收端未验 | 固定一个真实结果，验证 APP/H5/小程序各自分享类型、匿名打开同一内容、真实二维码扫码；不得分享私有参数或拿首页替结果 |
| C2 罗盘 | 本地断流恢复专项 15/15，类型检查通过；Harmony 原生源未接；真机北向未验 | 先 Android/iOS 原生来源和生命周期，再 Harmony 原生适配；同包验证真实旋转、校准、后台恢复，模拟/手动不是通过 |
| C3 页面与视频 | 用户报告底部遮挡、首视频有声无画面、手势问题 | 同设备同页面前后对比；覆盖键盘、安全区、支付落地页购买按钮、视频首帧/切换/返回、圈子导航 |
| C4 原版支付与用语 | 必须保持原商户体系 | 只到微信支付确认页，不付款；核对返回和失败提示，用户界面不出现“旧排盘”标签 |
| B1 发布资格与直授 | 本地已注册控制器及角色受限后台路由/菜单；11项接线、57项HTTP、5项隔离库验收通过 | 对齐前台旧申请入口与本人启停；管理员直授不受圈子经营门槛限制，但账号安全仍受控；普通阅读观看不受发布资格限制 |
| B2 咨询收尾 | 本地完成预扣到期结束、21/11分钟票据及超时接听回滚；最新57项专项与4项真实隔离库测试通过，未部署 | 保留历史票据边界；候选冻结后核验专用应用、同包双端及媒体收尾，不能以本地测试替代 |
| B3 后台/H5减频更新 | 有多处配置及后台改动，真实操作闭环未逐项完成 | 管理员能编辑、预览、生效、确认影响范围和回滚；原生能力变化仍须发版，不承诺一切可远程更新 |
| R1 冻结 | 当前 dirty、无最终 SHA | 先完成以上必需修复，再一次完整门禁；输出唯一 SHA、迁移清单、版本、制品哈希与证据绑定 |
| R2 预发布/签名包 | v243 尚无最终签名包和同包验收 | 按精确授权执行滚动及签名包生成；双节点与公网一致，各端关键多角色旅程实测 |
| R3 正式切换 | 未完成 | 核对域名证书、微信账号/开通状态、生产配置、备份、监控和回滚；明确放行后切换并验真 |

## 数据库范围必须单独评审

20260906 新增冻结阻断复核见《v243候选冻结前阻断复核-20260906》：完整代码门禁被非候选历史引用中的16MB WAL阻断，尚未清理或修改门禁；同时确认实时咨询的票据有效期与预扣耗尽自动结束需联动修复。当前不得开始候选分发。

当前未跟踪新增迁移文件为以下 10 个，不能复用旧候选“不迁移”或“仅一次某迁移”的指令执行：

- manual_add_circle_capability_01_workflow
- manual_add_circle_capability_02_quota_ledger
- manual_add_circle_capability_03_dispatch
- manual_add_circle_platform_direct_grant
- manual_add_consult_call_media_boundary
- manual_add_consult_call_media_evidence
- manual_add_consult_call_stop_intent
- manual_add_live_media_credential_boundary
- manual_add_live_media_evidence
- manual_add_live_media_stop_intent

此列表是源码盘点，不是线上待执行状态；冻结时需再与目标数据库迁移记录和 schema 比对。

20260906 目标库只读核对：cvm-b `ins-0sen8yvf` 唯一调用 `inv-f8b9wb05b2`，Run RequestId `f64fead5-0397-473c-bea0-9dbe5867e6c0`，Describe RequestId `cdaaa4d1-5bac-47a2-8a4e-ad1583483676`，SUCCESS/Exit0/Dropped0。CURRENT=`preprod-cd055084-20260903-r1`，server=running/healthy/restart0。数据库显式READ ONLY事务内查询：上述14项相关迁移记录为空；CircleCapabilityGrant、CircleCapabilityQuota、CircleCapabilityDispatch、ConsultCallMediaBoundary、LiveMediaCredentialBoundary、LiveMediaEvidence、LiveMediaStopIntent七张关键表均不存在。本次没有迁移、建表、配置写入或重建。14项均未在该迁移记录中登记，但未验证四项历史补齐所涉及的全部对象，不得推断整个数据库没有人工DDL或结构漂移。

20260906 线上提交差异补充：公网健康标识为 `preprod-cd055084-20260903-r1`。从该提交到当前候选，除上述10项外还新增4个已提交历史补齐目录：`20260828120000_prepare_member_plan_columns`、`20260828130000_prepare_merchant_inventory_schema`、`20260828140000_prepare_legacy_production_enums`、`20260828150000_prepare_legacy_production_schema`。因此源码迁移差集为14项，并不代表线上14项均待执行；必须先读取迁移记录，不能覆盖校验和或盲目resolve。

cd055084与f25b7471完整结构基线Git对象均为 `45fa2c912992c31be00c76a597846c4f134eb337`。升级测试改为直接读取cd055084冻结基线，真实localhost PostgreSQL16.13验证3/3通过：4项补齐连续执行两次不改变列清单；10项新增按序成功；重复DDL失败事务回滚。服务端TypeScript通过。它证明源码基线升级兼容，不证明线上无漂移、不模拟Prisma历史、不代替备份或线上status。本轮未执行线上迁移。

后续发现这三个未提交迁移的旧名字会导致 Prisma 字典序错误，已加 01/02/03 顺序段。历史 QA 文档保留原名描述当时的本地阶段；最终以本清单与实际目录为准。未修改已提交的历史迁移，也未操作线上迁移记录。

## 证据规则

实时连线后续：新增 `consult-call-state-sync.ts` 只读状态同步层，8 项合成运行回归通过。单轮请求完成后才安排下一轮，离页/重进使用代次隔离，终态不复活；网络失败停止同步并交页面明确重试，不自动发起/接听/结束/扣费，也不以网络错误推断已经挂断。响应精确匹配通话并投影安全字段，已接通不能回退等待。此模块尚未接入新通话界面或原生音视频驱动，不能作为同包通话成功证据。

20260906 收口补充：最终放行器新增 `publication_authorization` 与 `media_resource_closure` 两项独立人工验收，由九项变为十一项。机器测试全绿或笼统的核心客户端验收不能替代这两项。新增缺项阻断回归，发布判定测试 9/9；同步基础设施审计与现行运维交接说明后，基础设施审计 132/132。首次沙箱 Bash 子进程 EPERM 的 107/132 不计成功；在获准的本地语法校验环境运行才得到完整结果。以上只完善放行要求，不是 B1/B2 实现完成或运行时验收通过。

每项记录：复现条件 → 修改文件 → 对应自动测试 → 真实验收 → 未完成风险。已通过的合成测试保留复用，但不能替代供应商、真机、正式环境证据。旧文档中的历史数字和阶段结论保留，不升级成当前候选通过结论。

本轮只完成收敛盘点和计划落地，尚未完成逐文件归属、最终候选冻结或上线。

## C2 20260906 真机前置及断流解锁回归

- ADB 只读确认 OnePlus 9R 在线，系统声明 compass、gyroscope、accelerometer 三类硬件能力；实装 1.1.0(242)，没有把它记作 v243 同包验收。未安装新包或改设备设置。
- 真实组件回归复现：收到方向后锁定，传感器断流，再解锁时仍标记为 sensor。现改为保留锁定历史判读，但断流后解锁清除实时读数标识；收到下一条有效数据才恢复。回归先出现 `sensor !== none` 断言失败，修复后三组罗盘测试 15/15，单进程运行 Exit 0。初次测试进程 spawn EPERM 不计通过。
- 这是组件及合成传感器验证，不证明真机北向、磁场校准或分享/支付已完成。TRTC 已有详情标签，但读取仍超时；没有重新提交或修改配置。

## C1 分享链路复核

直接检查 `legacy-paipan-share.ts` 和真实海报页后：

- 原生菜单最多列出八项，但不能据此认定 App 失败。DCloud `https://uniapp.dcloud.net.cn/api/ui/prompt.html` 的六项限制明确适用于微信、百度、抖音小程序；当前该分享函数仅用于 Android/iOS。
- 网页分享请求的安全 URL 不通过时，解析器回落到工具入口。卡片与海报已明确说明“不包含当前排盘结果”，这是安全降级，不满足同一内容分享的最终要求。
- `/app_tool.php` 到 `/tool.php` 转换已有原版 APK 来源证据；不能据此任意放开签名、会话、订单参数，也不能推断匿名接收者会看到同一排盘。
- 下一份决定性证据必须是合成测试资料生成的真实结果公开链接：发起页与匿名接收页内容一致，分享好友/朋友圈与海报扫码各自验证。不得使用真实个人出生信息作未经批准的公开测试。
- `recordPosterShare` 当前为本地返回 code=200 的空实现，不产生后台统计。不能引用该返回值作为外发成功或运营数据已记录的证据；SDK requested 也不等于好友已收到。

本次未修改分享渠道或放宽 URL 安全规则；未向微信外发内容，没有以未证实的原因追加客户端改动。

## B1 事务身份缺口收敛

新增实际修复：管理员创建直播/视频、领取推流凭据、开播/下播不再仅信任外层isAdmin快照，而在主库事务中锁定并复核当前账号与平台角色；停用主播不可被管理员代签发/开播，仍允许管理员下播清理。229项单元与分批9项真实PostgreSQL合成验收通过，详见《发布管理员事务内撤权复核-20260905》。此修复未注册能力控制器，不能替代B2最终媒体收尾或云端权限配置证据。

## 整合构建与 HTTP 回归（20260906）

- 当前隔离工作树服务端 Nest 生产构建、后台 Vite 生产构建均 Exit 0；数据库空库基线与 schema 完全一致，旧系统迁移与整套排盘模式审计 7/7。
- 以上为本地构建，不代表最终冻结包或线上版本。入口文件抽样 SHA-256：server `dist/main.js`（9084 B）为 `CC3E6E8D012B04E40E9DF035B7E08595278A76EEAA6DA72203843586B963F549`；admin `dist/index.html`（1282 B）为 `7326672DB984630CBC2235A5CAD459263BACDB0983D9BE8A4247834BA3291A3D`。入口抽样哈希不能代替固定包全文件验真。
- 服务端整合回归首先发现视频创建、直播开播/下播的旧 HTTP 夹具缺少新增主库身份锁查询。已仅更新 `test/video.e2e-spec.ts`、`test/live.e2e-spec.ts` 的明确测试数据，没有放宽生产校验或全局默认授权。
- 补入登录快照仍有管理员角色、事务内角色已撤销的两条 HTTP 403 对照；不得创建视频、修改直播状态或新增停流意图。下播抢占失败依然 409，允许必要的身份 SELECT，但不得继续查询或写入媒体收尾记录。
- 两个修改后的 HTTP 套件合计 54/54，独立命令 Exit 0；这里使用模拟数据库，真实并发锁证据仍以 B1 的 PostgreSQL 集成测试为准。全量整合回归另行记录，不能用专项结果冒充全量通过。
- CircleCapabilityController 仍未注册，咨询可信收尾与真实分享/传感器验收缺口未被上述构建关闭；不修改线上配置，不操作迁移、DNS、云打包或真实付款。
- 完整服务端回归终态：`node node_modules/jest/bin/jest.js --maxWorkers=2 --workerIdleMemoryLimit=2GB --silent --reporters=default`，551 suites / 8017 tests 全部通过，117.3 秒，Exit 0；后续 server TypeScript 检查也 Exit 0。此数字不包含单独选择执行的真实 PostgreSQL 集成文件。
- 执行过程保留失败事实：首轮单进程达到 Node 约 4GB 堆上限而中断；768MB 回收阈值低于框架启动占用，造成频繁初始化，该轮已定向结束本次 Jest 进程树（不触碰其他服务）。最终 2GB 阈值的完整成功运行才作为全量证据，未修改产品安全逻辑或删减测试。
- 真机前置复核：OnePlus 9R ADB 在线，但实装 1.1.0(242)，不能替代当前 v243 同包验证。苹果投屏没有取得实际手机画面；已有 TRTC 控制台只读获取超时，未取得高级权限状态、未改云端，不升级为已通过。
- 同源码 H5/App 本地生产编译均 Exit 0，显式绑定预发布公开配置（API/H5：pre-api.rebugx.cn，静态资源：static.rebugx.cn，channel：preprod）。编译提示仅记录 Sass legacy-js-api 弃用提示，未跟随工具提示升级依赖。
- App 编译 manifest 为 1.1.0(243)，`app-service.js` 无 `sc-video-el` 字符串；不等于首帧/分享/罗盘真机通过。H5 index（1174 B）SHA-256：`83B078BE3ECF78FF580D5EB19FF08AC03F3AEEA54EF5C0380510967EC87325D6`；App manifest（8690 B）：`2490FC9898BF2F4235FC41F5325C6767C4BEA899BCDA324217E61D9EB865EA4F`；App service（8629843 B）：`52010ABF2E54D1832855CBC5317088A094E921194693969193196D6265407C0C`。这只是未冻结本地编译输出，不是签名 APK/IPA，未安装设备或上传。
