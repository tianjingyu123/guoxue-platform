# 直播 HTTP 夹具收口进度

本轮仅修改 `apps/server/test/live.e2e-spec.ts`，未修改业务服务、未部署、未操作云端。

## 已复验

- 本地完整 Live E2E：29 通过、3 失败（共 32），仍为失败终态。
- 定向删除、下播、状态抢占冲突、录制回调、推流回调：5/5。
- 服务端 `tsc --noEmit`：Exit 0。
- 原 6 个失败中的删除、下播、推流回调已修正陈旧夹具；新增下播抢占失败 409 测试。
- 下播保留真实业务服务；HTTP 夹具模拟状态 CAS 和明确的无历史凭据场景，不证明真实数据库锁或供应商停流。
- 录制回调必须实际写入 DRAFT，不以接口返回 200 代替业务断言。
- 推流回调检查主库媒体快照及脱敏字段，Redis 仅保存质量指标，不写旧的在线状态键。
- 未声明的原生 SQL 默认拒绝，不能隐式视为成功。

## 仍需处理

1. 创建直播间与关联课程创建共两项：需补齐实际发布授权的锁、主体、规则、授予记录夹具，不得绕过能力服务。
2. 开播一项：需补齐当前 CAS 与签发凭据边界夹具，不能恢复旧直接 update 模拟。
3. 完整失败运行仍打印 Jest 异步句柄退出警告；执行器报告 Exit 1，后续用句柄诊断核查，不把该轮当通过。

本地定向测试不代表公网、真实推流或双端真机验收通过；上线目标保持未完成。

## 后续收口复验

- 完整 Live E2E 已更新为 **33/33，通过，Exit 0**；前文 29/32 是上一轮结果。
- 新增圈主发布授权停用后返回 403、无创建写入的反向测试。成功创建仍实际经过发布服务、能力服务、规则解析和授予判定，未替换为直接允许。
- 创建夹具明确声明账号、圈主成员、锁定记录、规则和未到期授予；未声明 SQL 继续拒绝。
- 开播采用合法房间 UUID、状态 CAS、同事务签发凭据边界记录；检查媒体范围和不保存密钥，不再只模拟直接 update。
- `tsc --noEmit` Exit 0，所改文件 `git diff --check` Exit 0。
- 同一完整套件加 `--detectOpenHandles` 再跑 33/33、Exit 0，没有报告残留句柄。普通运行的一秒退出警告暂未复现为泄漏；未使用 `--forceExit`，也未删除或屏蔽定时器。

仍缺：当前候选真实云回调/供应商停流与重入限制、完整资源收尾、对应客户端真机证据；这些不由本 HTTP 夹具证明。
