# 配置减频与海报第一批：本地验证记录

记录时间：2026-09-05 04:01 UTC。

工作树：`D:\gx-deploy-91\.worktrees\mobile-paipan-compass-v243-20260905`。

基础 HEAD：`f25b7471f5063a612a17a422a64900a29b5f8cd3`。下述证据对应本轮**未提交增量**，不是该 HEAD 原始源码的验收结果，不是已部署候选。未改 manifest/versionCode、依赖锁或数据库迁移。

## 实际修改

- 远程配置：前台刷新、后台停轮询、真实 Vue 响应式更新、匿名缓存隔离、账号切换清旧配置、迟到请求隔离、敏感开关五分钟过期、长 TTL 重新核验。
- 服务端：个人灰度配置接口禁止共享缓存，使用已有配置历史和回滚能力。
- 海报：真实路由映射、朋友圈海报页参数保留、拒绝缺失内容 ID；生成/导出失败、十秒超时、旧回调、卸载之后不允许保存旧图；内容加载失败展示重试和返回。
- 构建范围：新增只读 `classify-change-impact.mjs`，区分各端影响，对未知变更保守评审，不自动发布。
- 发布流程：新运行测试加入 `release:test-mobile-native-bundle`，不为本轮每个小修复创建新的云包或递增版本号。

## 已完成验证

| 检查 | 结果 | 范围 |
| --- | --- | --- |
| 完整移动端回归束 | 236/236 PASS | 同时包含静态、逻辑运行和既有子进程测试；不是 236 项真机验收 |
| 配置运行测试 | 11/11 PASS，已计入上项 | 使用真实 TypeScript 编译与 Vue computed；配置 API/时钟/存储为可控测试替身 |
| 海报运行/接线测试 | 6/6 PASS，已计入完整束 | 异步代次、失败、路由与缺失内容；不是微信实际发送 |
| 改动影响分析测试 | 6/6 PASS，已计入完整束 | 服务端、后台、客户端、迁移、共享依赖、未知路径 |
| FeatureFlag 控制器专项 | 10/10 PASS | 包含两个 no-store 响应元数据断言 |
| mobile vue-tsc | Exit 0 | 本轮修改 |
| server tsc --noEmit | Exit 0 | 使用已安装 TypeScript 直接入口运行 |
| 精确改动移动文件 ESLint | 0 error / 81 warning | 模板格式与 explicit-any 等警告保留；不声称零警告 |
| App 编译 | Exit 0 | 本地预发布公开配置，含最终错误态修改 |
| H5 编译 | Exit 0 | 本地预发布公开配置，含最终错误态修改 |
| mp-weixin 编译和体积门禁 | Exit 0 | 621 WXML / 2703 文本产物；主包约 1.59 MB，保守上传约 1.87 MB；33 分包通过 |
| git diff --check | Exit 0 | 无空白错误；有 Windows CRLF 转换提示 |
| 影响分析真实 Git 调用 | Exit 0 | 含未提交/未跟踪；本批判断需要客户端构建，不授权发布 |

本地 App `app-service.js` SHA-256：`55CF890871EBEB3C2749FB0F37200C51C53CEDD934B425A43100875D982DEC88`。这不是 APK 签名或设备安装证明，也不能替代后续干净冻结构建。

## 过程中遇到并已区分的问题

- 最初测试进程被沙箱 `EPERM` 拦截。先用不隔离的 Node 测试执行器运行，完整束中的既有子进程测试在获准的沙箱外本地执行后通过；未改测试断言、未跳过失败用例。不是 Windows 1344。
- 初次类型检查发现可选内容 ID 没有完成类型收窄；已修正实际校验顺序后通过。
- 初次编译因隔离工作树无公开 Vite 配置被门禁拒绝；复用归档的 `artifacts/client-evidence/preprod-782c09c7-20260902-r1/public-build.env` 中四个公开白名单键后通过。未读取签名材料、未修改线上环境。
- server 的 pnpm 二进制快捷入口缺失，使用现有安装的 Jest/TypeScript 绝对入口运行；没有重装或改共享 node_modules。

## 明确保留的未完成项

- 原 46 项业务/真机矩阵尚未整体执行，不将本地单测折算为 SH/CG/OP/NV 通过。
- H5 微信富卡片、小程序码海报、App 按场景开放小程序分享、圈主/嘉宾资格与额度全链路仍需实现/集成。
- 罗盘真实设备转向、第三方排盘底部与支付/分享实际接收仍未验真。
- Harmony 本轮新产物、正式渠道构建、云包、设备覆盖安装、全量上线门禁均未执行。
- 没有推送、DCloud、TestFlight、COS、TAT、DNS、在线配置、数据库或真实付款操作。

下一批继续在同一工作树推进，待功能与设备证据收齐后统一冻结候选，避免再次“改一项就出一个包”。
