# 后台配置浏览器操作验收

## 环境与边界

2026-09-05，本机127.0.0.1:4187隔离页，真实 FeatureFlagList.vue、Element Plus 与确认组件。
数据源为 scripts/qa/feature-flag-browser/api.ts 的合成内存适配器，不连接预发布。
这证明真实浏览器交互，不代替前轮独立通过的 PostgreSQL/HTTP 测试，也不代表线上登录态联调。

## 已直接观察

1. 列表关闭开关点击后进入编辑，未直接发布。
2. 预览显示登录用户20%、指定用户0、未登录关闭、生效延迟及权限边界；取消后写入仍0。
3. 再次预览并确认后写入1，列表开启，显示已发布提示。
4. 历史v2开启/v1关闭；回滚v1确认期间回滚按钮禁用；确认后写入2，列表关闭，历史新增v3关闭。
5. 模拟冲突不增加写入，表单内容保留。发现仍能直接再提交旧编辑的交互缺口，现场修复。
6. 修复后再模拟冲突：写入仍2，发布按钮禁用，出现重新加载入口及明确恢复说明。
7. 重新加载前提示放弃未发布修改；确认后当前关闭状态恢复，发布按钮恢复可用。
8. 实际截图检查编辑窗：字段与底部取消/预览并发布完整可见。未执行多视口/真机测试。

## 新增修复与回归

结果未确认后 requiresReload 阻断再次提交，显式确认后重载当前配置。
后台脚本交互7/7通过；后台完整vue-tsc通过。
服务端另修删除失败被吞掉的假成功：仅P2025不存在幂等完成，其他错误上抛且不清缓存。
最新服务/控制器/HTTP回归55/55、server tsc通过；远程配置运行时11/11、免升级证据校验4/4通过。
远程配置运行时使用模拟时钟与请求，不能视为同安装包苹果/安卓已通过。

## 未完成

后台真实账号+真实服务端整合浏览器验收、缓存传播、同安装包设备证据；删除并发保护、编辑开始时差异核对、持久草稿。
合成页不提供真实删除，不计入删除浏览器验收。
未提交/推送/云打包/部署/线上迁移/DNS修改或支付。

## 持续执行安排

初次创建请求因缺少精确授权被审批拒绝，未绕过。随后用户明确“我同意并授权”。
已通过正式工具创建本任务 heartbeat：automation-3，ACTIVE，每30分钟，限定已授权本地开发与验收。
部署/DNS/云包/付款等不在续跑授权内；无安全可执行工作或本地事项完成时暂停。
本次合成浏览器页已关闭，本机 Vite 验收进程已停止。
