# 分享定位与公开状态第二批：本地验证记录

记录日期：2026-09-05（UTC）。

基础 HEAD：`f25b7471f5063a612a17a422a64900a29b5f8cd3`。

工作树：`D:\gx-deploy-91\.worktrees\mobile-paipan-compass-v243-20260905`。

本报告对应基础 HEAD 上的未提交增量，包含第一批配置减频和海报改动。**不是原始 HEAD 的验收，不是线上运行或手机安装证明。** 版本保持 `1.1.0(243)`，本轮未更改 manifest、依赖锁或迁移。

## 1. 本批已落实的修复

- 默认分享不再直接复制浏览器完整 URL，只重建已注册的公开页面与白名单参数。登录、支付、回跳、手机号等参数不透传；专用的公开授权邀请生成器不被一刀切删除。
- 具体内容必须保留目标 ID；圈子动态还需要 circleId，教师名片需要 userId，分站保留真实公开标识，空分站个人预览不能当成公开分享。
- 用户取消原生/系统分享时结束动作，不弹另一个面板或复制链接；“更多平台”只经过一个系统分享适配器，避免两层重复尝试。
- 小程序分享配置服务的文章、课程、商品链接改为真实注册页面；不再使用不存在的通用详情页或分站首页兜底，也不从数据库配置任意替换内容落点。
- 文章/课程只查询已审核、公开、未删除且符合发布时间和圈子状态的元信息。课程检查上下架时窗，商品检查在售状态并使用真实首图。
- 不存在或不可公开内容返回 404，未实现类型返回 400，数据库故障向上传递失败；不编造“可分享”的成功响应。独立 Content 内容库目前没有通用客户端详情页，因此明确拒绝其分享配置，不借用 Article 页面。
- 小程序公开详情实时核对发布状态，不读取旧缓存绕过下架；详情与分享配置成功响应禁止共享缓存。

## 2. 自动化与编译结果

| 检查 | 实际结果 | 证据边界 |
| --- | --- | --- |
| 完整移动端回归束 | 246/246 PASS | 含静态、真实 TS 逻辑、Vue 测试与既有子进程测试；不是 246 个真机用例 |
| 本批公开分享运行测试 | 10/10 PASS，已计入完整束 | 包含真实分享面板脚本调用真实适配器，原生系统和剪贴板为测试替身 |
| MiniService + MiniController + FeatureFlag 控制器 | 3 suites / 43 tests PASS | 包含本机 HTTP 状态码、no-store、无默认卡片等回归；数据库为 mock，非部署环境 |
| mobile vue-tsc | Exit 0 | 最终源码增量 |
| server tsc --noEmit | Exit 0 | 最终服务端源码增量 |
| server Nest 本地编译 | Exit 0 | 未执行数据库迁移或服务重建 |
| 本批精确改动源码 ESLint | 0 error / 78 warning | 保留模板样式与既有 explicit-any 警告，不声称零警告 |
| App 本地编译 | Exit 0 | 最终分享定位校验已进入 app-service；未云打包 |
| H5 本地编译 | Exit 0 | 最终源码，预发布公开构建配置 |
| mp-weixin 最终本地编译 | Exit 0 | 621 WXML / 2704 文本产物；主包约 1.59 MB，保守上传约 1.87 MB；33 分包体积门禁通过 |
| git diff --check | Exit 0 | Windows CRLF 提示不算空白错误 |
| 真实 Git 影响分析 | Exit 0 | 本批包含客户端和交付脚本，保守要求各端构建；migrationReviewRequired=false，authorizationToPublish=false |

最终本地 App `app-service.js` SHA-256：`06ebe13b25a88b7cd7ae608e9e83223f11bf0d232c9ae93caa00c32a1192b464`。

编译产物核对：manifest 为 1.1.0(243)；App、H5、mp-weixin 最终产物均包含缺失定位拒绝逻辑；App 公开页面校验存在且 `sc-video-el` 命中 0。这只能确认编译未重新引入该原生封面层，不证明视频真实播放或其他原生缺陷已经通过。

公开构建配置仅复用归档 `artifacts/client-evidence/preprod-782c09c7-20260902-r1/public-build.env` 中 VITE_API_URL、VITE_PUBLIC_H5_URL、VITE_PUBLIC_ASSET_ORIGIN、VITE_RELEASE_CHANNEL 四个公开键。没有读取签名材料、修改线上配置或把历史 releaseId 当作本轮部署结果。

## 3. 遇到的问题与处理

- 注册路由断言发现默认分享白名单曾列入未注册的横/竖屏直播和活动页面；移除错误入口后通过，未降低断言要求。原有专用页面/入口仍须按接收方路径验收，不宣称全站路由已排尽。
- ESLint 拒绝控制字符正则；改为字符码校验，保留参数过滤行为，不关闭规则。
- 系统分享双层兜底通过真实面板脚本运行测试复现并消除，保留原生能力不可用时的明确复制兜底。
- 最后补齐定位校验后重新顺序编译三端，不拿中间编译结果替代最终源码。

## 4. 尚未完成：不能转成上线通过

- H5 微信富卡片 JS-SDK 的真实集成与接收方打开；小程序真实码海报；App 按已审核场景开放小程序分享。
- 原始 46 项业务/真机矩阵仍未整体执行。本地局部测试不自动把 SH/CG/OP/NV 用例改成 PASS。
- 罗盘真实方向连续更新、第三方排盘底部展示、支付确认页及分享接收方验证仍需目标设备证明。
- 圈主/嘉宾直播、短视频、音视频问答的独立资格、申请审批、服务端授权与原子额度全链路仍需实现。未配置阈值不得凭空开通。
- 独立异机故障告警、真实支付、正式发布全门禁均未在本轮完成。企微合成告警/恢复收件已由监控任务取得用户确认，与这些项目分别记录。
- 当前没有提交/推送、云打包、设备覆盖安装、COS/TAT/DNS、在线配置或数据库写入、真实付款。

## 5. 减少重复发布的执行结果

本批与第一批保持同一工作树和版本号，没有产生新的云包。内容与既有协议内配置走受控后台流程；原生、分享 SDK 和传感器修复收口到统一候选后再打包。工具只给构建建议，不自动批准外部发布，也不下发可执行代码绕过商店审核。
