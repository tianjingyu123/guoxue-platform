# 圈内能力第八批：真实权限链、平台直授与通话准入

工作树：`D:/gx-deploy-91/.worktrees/mobile-paipan-compass-v243-20260905`。HEAD 仍为 `f25b7471f5063a612a17a422a64900a29b5f8cd3`，v243；本报告描述未提交增量，不表示该 SHA 已包含改动。未推送、云打包、部署、改 DNS、写真实账号权限或付款。

## 本批完成

1. 从最小账本库扩展到完整 356 表本地 PostgreSQL 16.13 合成库。真实读取 User、UserRole、Circle、CircleMember、Post、ConfigSystem，调用实际资格、申请、审批、启用、授权仓储与额度服务；不再用假的权限上下文代替完整流程。
2. 用户补充的“名师/战略伙伴由平台管理员直接授权”已加入独立来源 `PLATFORM_DIRECT`。绕过普通圈子申请门槛、配置修订、圈子申请额度及头衔限制；不伪造达标事实，不更改成员角色或原有价格。
3. 管理员明确指定范围、能力、期限、次数、并发；个人直授不要求整圈先开通，不连带授权其他人。LIVE/SHORT_VIDEO 可针对当前圈主或具体圈内成员，音视频问答必须针对具体个人。原普通申请路径不变。
4. 直授同事务核验当前无绑定平台角色、圈子/账号/成员状态、最新授权 ID/修订；替换时保留旧授权与撤销审计。重复点击、过期页面不能静默重置授权额度。直授、撤销、暂停、恢复均有人工红线与服务层复核。
5. 额度账本支持个人直授只有 providerGrant 的独立记录，圈级关联可空、二者不可同时空；原双份额度保持原规则。授权版本改变阻止新激活，可信收尾不因撤权受阻。
6. 现有 ConsultCall 修复：发起时查询有效达人角色/成员/账号/圈子/正价；扣币前加真实行锁并重新核价，价变返回 409，原成员被替换或资格变化在扣币前拒绝；RTC 配置不可用在预扣前拒绝；插入不为一行必须抛错回滚。接听按 WAITING 状态 CAS，不能覆盖取消/退款状态或被第二次接听重设开始时间。日期参数及数据库生成时间统一 UTC，不回填历史记录，不改变计价/预扣分钟数或分账比例。

## 严格边界

- **新能力控制器及额度服务仍未注册到 CircleModule，后台操作页、用户展示、实际直播/视频/实时问答/媒体签发均未接线。不能称平台直授已经在线可用。** 现有 ConsultCall 基础准入仍有 OWNER/PARTNER/GUEST 旧规则，接线时必须以新授权决策替代其中会误拦平台直授的规则，并同时验证列表可见性及服务端准入。
- 普通申请仍由圈主申请、平台审核、主体本人开启；平台直授为人工明确开通、立即启用，主体仍可停用，暂停后恢复须本人重启。平台申请规则的 enabled 不是全局风险开关，不能以其禁用阻止已明确的直授；真正全局风险/供应商/合规/支付控制在实际业务接线时仍需保持独立。
- 直授不绕过账号停用、圈子关闭、主体移出、范围不匹配或授权到期。自动化、业务范围绑定角色、管理员自授均拒绝。有限期限和 32 位正整数额度是技术约束，不是上线默认套餐。
- 本批新增 `manual_add_circle_platform_direct_grant`，依赖工作流与额度两项候选迁移。仅增来源列和放宽圈级关联非空约束，保留所有外键；历史来源默认普通申请，不默认批准。**三个迁移只在新建 localhost 合成库执行，未取得/使用线上迁移授权。**
- 实时音/视频问答对应 ConsultCall VOICE/VIDEO。PaidQuestion 是异步问题，回答可带音频附件，不据此强制把所有图文问题映射为 AUDIO_QUESTION。
- 测试中 RTC 凭据为 `SYNTHETIC_ONLY`，金币服务为模拟或调用即报错的替身。真实 DB 中 REFUNDED 仅是合成状态，不代表执行了真实退款；未验证真实扣款、供应商通话、麦克风或端到端履约。
- PostgreSQL 16.13 本地通过，不替代线上 14.x 兼容性。SH/CG/OP/NV 原 46 项不改为通过，新加 6 项直授端到端检查，总计 52 项仍须真实设备/真实接收方/候选环境验收。

## 验证结果

| 层次 | 结果 | 范围与限制 |
| --- | --- | --- |
| 关联 Jest/局部 HTTP | 21 suites，571/571，0 跳过 | 包含申请、直授、参数注入、角色、红线、额度和既有圈子/咨询回归；HTTP 鉴权与多数业务服务为测试替身 |
| 完整本地 PG 权限链 | 47/47，0 跳过 | 19 项原完整权限链 + 20 项直授与独立额度 + 8 项真实通话表准入/CAS；实际角色/配置/成员锁等待、并发、真实 SQL 回滚 |
| 按增量迁移创建隔离库 | 15/15，0 跳过 | 三项候选迁移按依赖序执行；额度 SQL、双连接竞争、回执、UTC/上海/洛杉矶时区 |
| 服务端构建/结构静态 | 18/18 | tsc、Nest build、Prisma validate、schema 重生成比对、两项原迁移叠加直授结构、基线生成器和 diff-check |
| ESLint | 27 文件，0 error | 8 条既有 ConsultCall `no-explicit-any` warning；新增直授、测试及辅助代码 0 warning |

完整基线仍为 356 表、899 显式索引、269 外键。未新增客户端版本或云包。初轮暴露的可空圈级授权 TypeScript/测试假设及迁移比较空白归一化问题均已修正；不隐藏初轮失败报告。实际 PostgreSQL 测试没有关闭有效期或金额保护来迁就测试。

## 证据摘要

全部报告在源码工作树外，不含真实账号凭据或供应商秘密。

| 报告（位于 `D:/gx-deploy-91/artifacts/`） | SHA-256 |
| --- | --- |
| `circle-platform-direct-regression-r3-20260905.json` | `AB65AC5C608824E1B8AE71F6AB2200518A932A155CAB29EF13629232860CABF0` |
| `circle-platform-direct-pg16-r2-20260905.json` | `513372E628501B0DF4D5C5AC2341D84D65FA68070FE65B369DCED8F6BEC79182` |
| `circle-platform-direct-incremental-pg16-20260905.json` | `D21F8156EB65B99A4A7599407E03D0F4C7ECBA0CDA9F7646F455B41C6A89C97C` |
| `circle-platform-direct-build-r2-20260905.tap` | `937287EF2F88D82727B9F83BEFB8A78466386E91B7E4CFE0A1D1F53083E46B1D` |
| `circle-platform-direct-lint-r2-20260905.json` | `4DEA48B80DFBD0695E632AB1AF64B75DC3A4FCCFE17039060B0B5C147BE28731` |

本批成功的完整库目录：`circle-workflow-pg-G74Hpj`（19项早期）、`circle-workflow-pg-1WQ9S0`（27项早期）、`circle-workflow-pg-Nnhi4p`（最终47项）；增量库 `circle-capability-pg-M3W4H9`（15项）。均在 `artifacts/local-qa`，已输出 STOPPED，结束回读 postmaster.pid 均不存在；保留合成数据和日志用于追溯，不按进程名关闭其他数据库，不删除旧 QA。

## 下一步

把后台普通申请审核与平台定向直授统一到同一主库决策；同步用户能力入口和状态，不让直授名师仍被原圈主/嘉宾列表过滤拦住。业务接入要覆盖开播/OBS/推流凭据、短视频创建及发布审核、通话发起及媒体资源，外部资源任务必须可恢复/幂等。后台 UI 和真实业务适配一起通过后再注册新入口，不先放出“已开通”的空壳界面。

同一候选继续保留 H5 卡片真实接收、二维码落地、电子罗盘真实转向、Android/iOS 底部完整显示等独立门禁；不得以本次后台测试替代这些失败项或再次宣称已修好。
