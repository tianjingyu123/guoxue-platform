# v243 候选冻结前阻断复核

当前工作树 HEAD 为 f25b7471f5063a612a17a422a64900a29b5f8cd3，展开未提交条目564项；不是最终候选。

## 完整代码门禁

`pnpm release:gate:code` 在严格上线扫描阶段 Exit1，原生预览83/83通过。后续链式门禁未执行，不计全绿。

唯一报告P0为Git历史数据库运行文件。只读元数据确认：

- 路径 `backups/wal/000000010000001600000020`，大小16,777,216字节。
- 提交 da22664aa748af8efadc84f922ac1d1c035439d7，Git日期2026-06-23T15:10:01+08:00。
- `git for-each-ref --contains` 仅返回 `refs/prod-snapshots/20260904-before-admin-ui`。
- `git ls-tree HEAD -- backups/wal` 为空；`git log HEAD -- <该路径>`为空；祖先检查Exit1。
- 门禁使用 `git rev-list --objects --all`，所以会扫描这个非候选历史引用。不能为了通过而改为忽略它。
- 未读取WAL正文，未删除快照、对象或历史，未强推，未分发制品。尚未查询真实远端，不能认定远端无历史副本。该快照可能承担恢复用途，清理须保留可恢复性并明确范围。

## 实时咨询仍需解决的运营约束

`buildTrtcConfig` 默认票据86,400秒，资源保护在最晚票据期限后额外300秒。新收尾实现虽能安全归还额度，但早结束的通话可能长期占并发。

`CONSULT_PREPAY_MINUTES=10`，结束费用按预扣上限封顶；当前仅有WAITING超时任务，尚未找到ONGOING预扣耗尽自动结束路径。不能声称持续通话的额度/计费闭环已完成。

后续应联动接听后的服务端通话截止、客户端剩余时长提示、截止时结算/停流意图原子提交，并缩短/约束票据窗口而不破坏合法断线重连。不得只改TTL数字、默许免费无限通话或以模拟时间测试替代真实验证。

本次不新增云调用、迁移、DNS操作或签名包。完整服务端回归单独记录结果。

## 后续状态更新（以本节替代上文已过时的待开发项）

- HEAD 仍为 f25b7471f5063a612a17a422a64900a29b5f8cd3；本次检查时 `git status --porcelain=v1 --untracked-files=all` 为 586 项（计数早于本节写入）。不是已冻结候选，也不能把 HEAD 当作未提交代码的制品来源。
- 短时咨询票据已本地实现：等待 600 秒、接听票据 660 秒、发起票据 1260 秒。`consult-budget.worker.ts` 已有每 10 秒执行的预扣到期处理和 BUDGET_TIMEOUT 停流意图。详情见本目录对应专项报告；不再把这两项记作尚未开发，但云端部署及双端媒体验收仍未完成。
- 当前 schema 与 full-baseline.sql 完全一致，本地 Prisma 空库基线比较 Exit 0。首次沙箱子进程 EPERM 后，仅获准的本地沙箱外比较计成功；未连接实际数据库。
- 当前未跟踪新增迁移共 10 个。关键字初筛命中两处 DROP，人工核对均为平台直授迁移中的 DROP NOT NULL（放宽两列非空约束、外键保留），不是删表或删数据。此初筛不能代替全量升级演练或线上差异核定。
- 历史 WAL 仍由 `refs/prod-snapshots/20260904-before-admin-ui` 引用；本轮只查引用元数据，未读取 WAL 内容、删除历史、强推或放宽门禁。此项仍阻断完整安全收口。
- 本轮不重复云端调用，不执行新增迁移，不签名打包。后续冻结需要归档完整变更清单、核定升级迁移范围及历史备份安全保留方案；真机/媒体未通过的项目不能标绿。

## 完整服务端回归终态

`node node_modules/jest/bin/jest.js --maxWorkers=2 --workerIdleMemoryLimit=2GB --silent --reporters=default`：556 suites / 8100 tests通过，300.56秒，Exit0。初次沙箱启动子进程EPERM未执行测试；仅沙箱外本地复跑的上述终态计成功。不包括独立PostgreSQL集成测试，不代表发布门禁通过。测试中的拒付/验签失败/供应商超时日志为负例，不是线上错误。
