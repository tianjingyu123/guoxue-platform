# 咨询停流状态一致校验与收尾清单

本地增量，不是云端部署或完整资源收尾验收。

## 已确认问题与修复

管理只读投影此前仅检查待办版本和 SDK/房间范围，就可能将缺少派发明细的 JSON 显示为 ACKNOWLEDGED。现在后台投影与实际派发器复用同一完整校验：订单终态、原请求人、操作标识、凭据修订、保护时间、地域、领取/租约/结果时间、供应商请求编号均一致。损坏记录显示 INVALID，不向管理员泄露原始记录。

新增中文“尚需核验”清单，区分订单未结束、签发范围缺失、票据/保护窗口未结束、停流未验证、媒体未关闭，以及最终云端范围与事务收尾证据。新页面兼容旧接口，但没有清单时明确不能认定完成。账号切换、查询失败、畸形清单仍清空旧结果。

没有新增停止、退款、释放按钮；没有注册新能力审批控制器。`canRelease=false` 保持不变。

## 实测

- 相关策略/只读服务/停流客户端/worker：4 suites，69/69 PASS。
- 后台组件真实脚本交互：7/7 PASS；ESLint 自动按既有模板规范排版，0 error。
- server TypeScript 与 admin Vue TypeScript：Exit0。
- 最终保护时间检查改动后，只读服务专项 15/15 再次通过；服务端 Nest build、后台 Vue TypeScript + Vite production build 均 Exit0。后台组件排版后 7/7 再次通过，指定文件 diff-check 通过。
- 完整基线 PostgreSQL 16.13，361 表，localhost:50603，隔离目录 `D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-t5fIkB/data`；选择性运行 6/6 PASS，134 未重跑。测试结束明确停止本次数据库，保留合成数据。
- PG 覆盖：双节点单次派发、范围漂移拒绝、管理真实 JOIN 只读、结果写库失败保留占用且不重发、未来 ACK 时间和无效编号拒绝。供应商和资金接口均为替身，未真实停流/付款。

## 尚未完成

### 事务复用待办的后续一致性修复

`ConsultCallResourceService.requestStopInTransaction` 此前只核验范围及版本就复用已有待办；现统一调用 `validConsultStopIntent`，与派发器和管理只读投影保持同一完整规则。残缺操作 ID/时间/版本、伪造 ACK、请求人不属于双方均冲突退出，不覆盖已有记录、不重复创建待办、不释放额度。

新增资源事务专项测试；三项聚焦 suite 49/49 PASS，server TypeScript Exit0。随后按实际文件列表运行咨询模块全部 10 suites/158 tests PASS。首次尝试的 Jest 复数路径参数未限定范围，已中断，不能记为全服务端验收。全部为本地单测，未执行云端停流、退款或配额变更。

新授权控制器仍未注册；咨询 ACTIVE 资源的可信最终收尾、TRTC 范围/高级权限实际核验、旧票据重入与双端真机未完成。必须继续完成这些业务接线及外部验真，不能把本次后台提示优化当作该全链路已可上线。

### 后续真实 PostgreSQL 验证

- 新增双连接重复待办复用与残缺待办真实事务回滚用例；连同既有两节点竞争停流、取消、并发结束共 4/4 PASS，另 137 项未运行，不能记为整个集成套件通过。
- 仅在新建 localhost PostgreSQL 16.13 合成库执行完整基线。首次测试夹具误用不存在的 `updatedAt` 字段，编译阶段失败，未进入数据库；修正为实际 `endAt` 后通过。
- 测试目录 `D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-SnGi5b`；日志明确 `database system is shut down`，随后只读 `pg_ctl status` 返回 `no server running`。合成测试数据保留，未删除旧 QA。
- 回滚用例确认篡改记录不会被覆盖、事务内订单结束时间变更回滚、ACTIVE 账本不提前释放。供应商和资金调用仍是本地桩，没有真实停流/退款/外发。

### 完整隔离业务套件复验

随后在新建隔离库完整运行 `circle-capability-workflow-pg.integration-spec.ts`，141/141 PASS、无跳过、Exit0。覆盖实际资格/申请/审批/额度、平台直授、直播与咨询业务 SQL、并发锁与回滚；仍不覆盖未注册公开控制器的真实在线可达性、供应商实际停流或资金流水。

目录 `D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-2GCnyw`，日志确认关闭，`pg_ctl status` 返回 `no server running`。未改预发布、生产、DNS、设备或其他任务工作树。

官方移出用户接口仅表示移出用户，不能推断后续禁止重入：[腾讯云 RemoveUserByStrRoomId](https://cloud.tencent.com/document/api/647/50426)。
