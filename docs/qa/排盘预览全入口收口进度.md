# 排盘预览全入口收口进度

## 当前边界

仅本地未提交改动；未操作设备、云包、服务器或数据库。扫描从 pages.json 枚举全部三个自研排盘分包，不把公共路由模式当作授权。

## 已完成的必要接线

- 六爻入口、结果、历史三页。
- 小成图入口、结果两页：onLoad 仅保留参数，资格探针通过后才计算和留存；页面隐藏清除结果，恢复须重新核验；同页换账号拒绝重放参数。
- 小成图历史读取、清空必须在本次资格核验的同步作用域内；存储按 subjectId 隔离。无归属旧记录不读取、不迁移、不删除。
- 八字、奇门、阳盘的历史页和分组管理页共六页：移除 setup 读取；读取/置顶/删除/分组逐次核验，账号分组和记录隔离。HistoryGroups 仅展示核验后快照并发出事件，不直接读写存储。
- 奇门、阳盘结果页已接入：首次核验前不计算/请求，网络响应再次核验后才显示/保存；隐藏、退出、撤权后迟到结果不回填。本地同输入返回不重复保存。
- 八字结果页已接入：计算、首次保存之间再次核验；历史 ID 复用，编辑后新建；异步回填前核验并在同步作用域保存账号历史。八字/奇门/阳盘入口核验通过后才挂载表单，隐藏销毁工具子树。
- 合盘、梅花入口/结果/历史共六页完成必要接线；无归属历史不自动迁移或删除，清空/保存/置顶/删除需重新核验。合盘画布回调检查页面代次，避免隐藏后继续绘制旧数据。
- 紫微入口、结果、历史三页完成必要接线；修正历史重开错误的参数格式，统一发送结果页接收的 payload，保留分钟/经度/真太阳时布尔值。旧无归属记录保留不读。
- 阴盘、玄空入口/结果/历史六页完成必要接线；保存、改名及历史修改提交前重新核验，旧无归属记录保留不读。阴盘继续按完整参数去重（改事项新增记录），玄空改名保留原覆盖语义。
- 七政四余入口/结果两页完成必要接线，初始化计算与留存延后到核验通过，复制摘要和清空历史重新核验。旧无归属历史保留不读，同页换账号不重放参数。
- 大六壬入口/结果两页完成必要接线；时辰切换和复制摘要逐次核验，旧历史不读不删，返回不重复保存。
- 金口诀入口/结果两页完成必要接线；读取/保存/清空/重开/复制逐次核验，随机起课已确定的地分保留。私有预览不再分享含参数的 H5 工具直链，只复制核验后的摘要；不改变第三方排盘对普通用户的 H5 分享目标。
- 双人合盘列表/报告两页接入请求前与回填前核验，撤权后删除确认不发业务请求，迟到结果不回填或提示成功；报告原生分享不暴露昵称和私有工具路径。
- 合盘发起页接入选盘加载、生成邀请和复制链接的逐次核验；连续点击不重复请求，隐藏后清除邀请凭据，原生分享不携带私有邀请。受控复制链接仍要求接收端独立核验，不作为访问授权。
- 合盘接受页接入邀请读取、个人记录加载、接受和拒绝逐次核验；邀请与个人记录请求之间增加检查点，拒绝时不继续下一请求；已发写请求期间退出后不回填、不跳转。
- 万年历和工具说明页接入预览资格；万年历未核验时不计算页头、不挂载工具子树，隐藏清内部视图和待执行滚动；说明页延后显示工具名称，并修正“返回首页”实际导航。
- 小六壬单页工具和历史页接入资格与账号存储作用域；提交保存、置顶、删除、重开均核验，历史回放不自动新增记录，旧无归属键保留不读。
- release:gate:code 已增加入口扫描，当前 83 页中必要接线 46 页，缺失 37 页，发布门禁仍应失败。

## 本次证据与限制

页面脚本、历史隔离、清单检查合计 34/34 通过，移动端类型检查通过。页面测试执行真实 SFC 脚本及资格/存储作用域实现，网络、Vue 生命周期、存储和计算引擎为替身；不是服务端或真机全链证明。扫描只检查必要接线，不能证明生命周期与全部按钮没有旁路。异步任务只防止客户端迟到提交，已经发出的服务端请求仍必须由服务端独立鉴权，不宣称能够撤销已完成的服务端写入。

最新合并脚本回归 37/37，移动端类型检查通过。八字新增测试覆盖创建、编辑、历史重开及计算期间撤权不续发保存请求；三个入口目前只有接线/类型证据，尚需浏览器或真机组件挂载验收。

最新合并脚本回归 39/39，移动端类型检查通过。合盘、梅花采用实际入口/结果/历史脚本联测，存储、网络、Vue 生命周期和部分引擎为替身；覆盖旧记录保留、授权保存、撤权清空/删除零写入。未完成真实画布及真机视觉验收。

紫微新增实际页面脚本联测通过，含历史页生成 URL→结果页解析；当时该组 8/8，移动端类型检查通过。引擎及网络为替身，不代替真机操作证据。

最新合并脚本回归 42/42，移动端类型检查 Exit 0。新增阴盘、玄空实际页面脚本测试覆盖授权保存/改名、撤权零写入、历史置顶/删除与旧记录保留；测试使用替身，不代表真机验收通过。

七政追加实际页面脚本测试后本组 11/11，通过核验前零计算、返回不重复写入、撤权复制/清空零动作、跨账号不重放断言。计算引擎、生命周期及剪贴板为替身，未验证原生画布/真实分享。

大六壬追加页面和时辰切换测试后本组 13/13，移动端类型检查通过。覆盖连续切换累计偏移、撤权不再计算/复制/清空、无额外历史写入；引擎、剪贴板和生命周期为替身，非真机证据。金口诀已只读盘点但尚未接线，其 H5 分享与保存需要独立处理。

金口诀追加实际脚本联测后本组 14/14，六组联合回归 46/46，移动端类型检查 Exit 0、diff --check 通过。覆盖随机地分重开一致、保存后返回不重复、撤权复制/清空零动作及账号切换拒绝重放；仍为替身环境而非真机。

双人合盘新增实际页面脚本测试 6/6；服务端、Vue 生命周期及弹窗为替身。控制器源码已有 NativePaipanGuard，但尚未针对全部合盘接口做真实 HTTP 权限复验；发起/接受邀请两页仍未接线，不得视为完整邀请链路验收。

合盘脚本组新增发起页后 10/10，移动端类型检查通过。仅证明当前页面连续点击防重复及迟到回填隔离，不能证明已到达服务端请求可取消，也不代替服务端幂等/事务验收。接受邀请页仍未接线。

接受页追加测试后合盘脚本组 15/15，覆盖正常接受/拒绝、重复点击、防续发记录请求与退出后的迟到返回。网络与生命周期为替身，尚无完整真实 HTTP/事务和真机证明；四页完成客户端必要接线不代表业务全链通过。

合盘新增服务端 HTTP 门禁测试 `couple-preview-gate.e2e-spec.ts` 15/15，通过真实 Nest 路由、JWT 验签和 PaipanRuntimeService/NativePaipanGuard；覆盖七端点关闭、匿名、普通角色、主库异常、获准后撤权及错误域名/过期 JWT。数据库查询和 CoupleService 为替身，不证明真实角色查询、业务事务或邀请幂等。控制器新增 ApiExcludeController，生成公开 Swagger 文档实测不含合盘路径；server tsc 通过。

万年历/说明页新增脚本测试后该组 16/16；页头计算及生命周期为替身，子组件挂载和各端视觉仍未验证。

小六壬新增脚本联测后该组 17/17，覆盖起课保存、历史重开、撤权零写入和允许删除；计算引擎及存储为替身，未代替真机与算法验收。

本阶段联合脚本回归 64/64；App 预发布公开配置本地构建 DONE，原生脚本门禁通过。首次沙箱内 spawn EPERM，审批后同命令沙箱外通过，未升级依赖或上传云包。另发现 tool-header 默认 H5 分享当前地址，父级事件监听识别也需检查，尚未收口；不以当前页面门禁视为全部分享已隔离。

同阶段微信小程序本地完整构建 Exit 0：461 个 WXML、1980 个文本产物检查通过，主包 1.50 MB，保守上传估算 1.78 MB，30 分包，体积达标。仅为本地产物证据，未上传体验版。

通用 tool-header 发现真实 Vue 事件问题：defineEmits 声明事件不进入 attrs，导致父级 back/share 未被识别。真实 Vue 自定义渲染器加载 compileScript 产物，修前 0/2、修后 2/2；改用当前 VNode 父级监听器检测。默认 H5 当前地址分享已移除，未提供自定义处理器不显示分享按钮，避免绕过父页核验泄露参数链接。联合回归 66/66；实际 OS 分享/触摸仍未测试。前述 App/MP 构建早于此顶栏修复，不能当作最新源码产物证明。

顶栏事件修复后继续复核父级回调：紫微、玄空摘要复制原先只判断结果存在，现改为重新核验后复制，核验失败清展示、不得复制；合法复制保留当前姓名/结果、不新增历史。页面脚本及真实 Vue 顶栏合计 21/21；剪贴板为替身。阴盘、合盘等其他回调尚未全量收口，入口已接线数量不增加也不表示这些按钮已全部通过。

剩余 37 页需继续按启动读取、计算、服务端请求及历史修改逐类收口；未覆盖项不得通过新增模板判断冒充完成。子组件笔记、报告、分享等路径亦需继续核验，不能发布当前中间状态。
