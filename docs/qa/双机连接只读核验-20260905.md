# 双机连接核验（非功能验收）

## Android

使用本机既有 HBuilderX 5.24 自带 ADB：`D:/HX524/HBuilderX/plugins/launcher-tools/tools/adbs/adb.exe`。

- `devices -l`：OnePlus9R，model LE2100，状态 `device`，不是 offline/unauthorized。
- `dumpsys package com.rebu.apprebu`：versionName=1.1.0，versionCode=242。
- `dumpsys window`：前台 `com.rebu.apprebu/io.dcloud.PandoraEntryActivity`。
- 本轮未发送 Android 点击/滑动，因此证据证明已连接且可读取，不单凭 ADB 在线断言触控验收通过。

爱思投屏界面同时显示 OPPO 设备“投屏前请先安装驱动”，但 ADB 实测在线，因此未重复安装驱动。

## iPhone

按 computer-use 技能通过现有爱思投屏窗口读取到排盘首页，投屏管理页显示 iPhone 投屏中。点击八字排盘图标一次后刷新画面未变化，因此仅确认画面可读，尚未确认远程点击可控；不能把投屏在线作为业务验收。

辅助窗口最小化后按技能恢复一次成功，未修改系统权限、蓝牙或隐私设置。当前 iPhone 安装版本本轮没有取得可靠证据。

## 未执行与后续边界

未安装应用、未改权限、未付款、未发布内容、未操作云端。v243 本地改动尚未冻结或安装，v242 真机表现不能作为 v243 修复完成的证据。下一步 Android 按可见画面验证实际点击与返回；iPhone 先确认远程控制通道再做功能验收。

## 后续实际输入验收（同设备 v242）

已通过 ADB 按观察到的画面操作，不是仅确认设备列表：

1. 初始会员落地页底部橙色购买按钮仅露上缘；执行一次上滑后截图仍裁切，未点击购买。截图：`D:/gx-deploy-91/artifacts/rebu-qa-current.png`、`rebu-qa-after-scroll.png`。尚未取得实际 WebView 几何或 DOM 数据，不能直接把根因判为单一安全区数值。
2. 系统返回键一次后立即截图尚未变化；稍后独立截图确认已回到排盘首页。不能把过早截图误判为返回键无效。证据：`rebu-qa-back-settled.png`。
3. 点击八字排盘入口，进入真实输入页：`rebu-qa-bazi.png`。
4. 点击出生地点，选择器弹出：`rebu-qa-location.png`。
5. 使用选择器默认的公开地区测试值确认，页面回填“北京 房山区”：`rebu-qa-location-confirm.png`。未填写姓名/出生时间、未开始计算、未创建排盘记录。

结论：安卓远程输入和上述出生地回填链路已证实可用；会员底部裁切未解决。此结果不覆盖苹果，也不覆盖 v243。上述截图均在 `D:/gx-deploy-91/artifacts/` 下，仅作为本地证据，不外传。

## 容器尺寸进一步取证

使用 Android 系统 `uiautomator dump`，本地文件 `D:/gx-deploy-91/artifacts/rebu-qa-layout.xml`；仅从文件提取类名与边界，未回传页面账号数据。

- 全屏根 FrameLayout：`[0,0][1080,2400]`。
- 当前排盘外层 FrameLayout/WebView：`[0,102][1080,2298]`。
- 直接位于上述 WebView 内的内部 WebView：`[0,102][1080,2400]`。
- 内外底边差 102 物理像素。该取证在八字页完成，不是会员页 DOM，因此它支持存在容器/内部布局范围差异，但尚不能证明会员按钮 CSS 的精确根因。

当前候选 `pkg-common/legacy-paipan/index.vue` 已有 bottom=0 和注入 safeBottom 的逻辑；仅有这些源码断言并不足以证明实际容器适配成功。后续必须核验真实视口与页面定位元素，不能把设备的 102 物理像素直接写为跨设备 CSS 偏移量。未因本次尺寸读数修改源码，也未将此项标为修复。

进一步源码检查：`legacy-paipan-preload.js` 不包含 getSystemInfo/windowHeight/screenHeight 代理，因此“兼容桥伪造整屏高度”这一具体假设不成立。宿主页只读取系统尺寸并注入安全区值；第三方页面自身的尺寸使用方式尚未知。

尝试读取既有 Android WebView 调试端点的页面清单未返回：本地命令句柄 18526 经复查仍在等待，随后主动终止，终态 ExitCode 1。临时本机端口 19343 的 ADB forward 已精确移除（ExitCode 0）；未关闭手机调试权限，未修改页面、Cookie、登录态。没有取得 DOM/实际 CSS 视口数据，不以本次失败推断业务故障或安全区具体根因。

对照 DCloud 官方 WebView 文档：https://www.html5plus.org/specification/Webview.html 。top 与 bottom 在未显式指定 height 时可共同决定高度；若同时指定 top 和 height 则忽略 bottom。当前创建样式未显式设置 height，故不能仅凭源码声称 bottom 被 height 覆盖；仍需实际运行样式证据。

再次核验端点归属：`pidof com.rebu.apprebu` 返回 25569，而唯一可见调试 socket 为 `webview_devtools_remote_2178`。未连接该归属不明端点，未开启新的调试权限。上次未返回不能当作本应用调试服务故障。

Android `dumpsys window displays` 实测：屏幕 1080×2400；statusBars `[0,0][1080,103]`；navigationBars `[0,2400][1080,2400]`（底部导航占用 0）；mandatorySystemGestures `[0,2304][1080,2400]`。状态栏高度与容器底边差值接近，但尚未证明因果关系；系统手势区域也不能与导航栏或 CSS 安全区混为同一值。

## 本地移动回归复跑

`pnpm release:test-mobile-native-bundle` 默认沙箱因 Node 测试进程 `spawn EPERM` 失败，不属于业务断言失败。经沙箱外本地测试授权，以相同命令重跑，ExitCode 0；排盘前置套件通过，后续移动专项 249/249、失败 0、跳过 0。本次没有云打包、安装或部署；全绿结果不覆盖已实测仍有裁切的 v242，也不证明 v243 真机布局。

## 实装 APK 与源码证据绑定

通过 `pm path com.rebu.apprebu` 取得本机实际 base.apk 路径并只读回取，文件 `D:/gx-deploy-91/artifacts/rebu-installed-v242-readback.apk`。大小 30,975,976 字节，与归档 `client-a1cb87bd-dcloud-final/android/Rebu-1.1.0-242-a1cb87bd-OnePlus.apk` SHA-256 完全一致：

```text
BB95DF3541413DA8C604A672E812D8A408BAA3CA87DB3CA4C1DBE727241C4E1A
```

包内 app-service.js 的排盘子窗口创建代码已直接核对：top 为安全顶部值，bottom 为 0px，无显式 height；创建后 append 到页面父窗口，再 loadURL。排除“未安装目标 v242 包”和“已安装包仍未设置 bottom=0”的解释。工作树当前该页的未提交差异仅为罗盘目标路由调整，不含新的窗口高度修复。

官方 `WebviewObject.getSafeAreaInsets()` 返回该窗口内容与安全区的实际重叠量，区别于 navigator 的设备级安全区；`getStyle()` 可读取窗口样式。后续布局验收需要绑定实际父、子窗口样式与窗口级安全区，不把设备全局 inset、虚拟无障碍内容边界和 DOM 可视高度混为同一证据。本轮尚未获取这些运行时数值，不宣称已完成裁切修复。

## 会员页原位复现与苹果输入复核

2026-09-05 再次 ADB 确认 OnePlus `a07f9db3` 为 device。从八字页按返回，延后截图确认已回工具首页；点击活动“点击查看”，延后截图确认进入会员介绍页。没有选择新套餐、点击购买或创建支付。

会员页截图 `D:/gx-deploy-91/artifacts/rebu-qa-member-settled-sep05.png` 直接显示橙色购买按钮仅露上缘，问题仍存在。此时采集 `rebu-qa-member-layout.xml`，外层 FrameLayout/WebView 为 `[0,102][1080,2298]`，内部网页虚拟边界 `[0,102][1080,2400]`。此证据已绑定会员页，不再仅依赖之前八字页数据；但无障碍虚拟边界仍不是 DOM `innerHeight` 或原生窗口 `getStyle()`，102 像素不能直接作为硬编码修复值。

苹果爱思投屏窗口 12191674 可见工具首页。对活动入口执行一次点击，立即和延后两次观察均未离开首页；因此本轮只证明投屏可见，未证明 iOS 触控有效，不把点击工具成功返回当作手机响应。未反复点击、未修改投屏权限或手机设置。Android 会员页保留供继续诊断。
