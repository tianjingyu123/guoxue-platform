# 服务端全量源码单测装配复验

范围为当前 v243 未提交工作树，不是线上已部署版本。命令：`jest --runInBand --testPathPattern='src/.*\.spec\.ts$' --silent`。

## 首轮终态

- 487 suites：478 通过、9 失败；6644 个已加载测试：6633 通过、11 失败；耗时 243.421 秒。
- 八个认证/用户套件未能启动：Jest 将 bcryptjs 写死到当前工作树的 `.pnpm/bcryptjs@2.4.3` 路径，但此隔离工作树的依赖实际由根仓库解析。
- 八字知识控制器的 11 项测试因新增 NativePaipanGuard 的依赖未在局部测试模块装配而失败。
- 不将未启动套件算作通过，也不将测试夹具主动输出的故障日志当作线上事件。

## 最小修复和复跑

1. `apps/server/jest.config.ts` 改用 `require.resolve('bcryptjs')`，沿用当前安装依赖，不安装、升级或改 lockfile。
2. 八字知识控制器的直接方法单测显式覆盖 NativePaipanGuard，和该文件已有 JWT/角色测试替身一致；不修改生产守卫。实际权限验证仍由独立整套守卫/HTTP 测试承担。
3. `--onlyFailures` 实际仅重跑八字控制器：1 suite、11/11 通过。没有据此声称九个套件全部恢复。
4. 单独完整复跑 `modules/(auth|user)/.*\.spec\.ts$`：18 suites、285/285 通过，覆盖八个模块启动失败套件。

当前首轮失败项已由上述定向复验消除；**尚未在修复后重新执行一次完整 487 套件命令**，不把不同轮次计数拼接成单轮全绿报告。`git diff --check` 通过。没有执行 PostgreSQL 集成库、真实登录、支付、部署或云端变更。

## 圈子接线检查

现有 ContentAuditRecord 是审核台账，缺少任务租约、领取令牌、重试计数和下次执行时间；不得直接把其 PENDING 记录当成具备幂等恢复能力的派发队列。圈子授权业务接线仍需实现业务与额度、可靠任务的同事务边界，并补前后台完整操作链。
