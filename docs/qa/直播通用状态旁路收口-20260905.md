# 直播通用状态旁路收口

日期：2026-09-05。仅本地开发验证，未部署、未迁移、未操作真实直播。

## 发现与处理

- `LiveService.updateStatus` 已没有生产调用，但仍允许不传操作者直接改为 LIVING/ENDED，绕过新增发布授权与额度事务。
- 删除该方法及其私有状态表；保留 `startLive` / `endRoom` / `publishReplay` 专用入口。与直播无关的同名方法不改。
- 删除控制器测试中的陈旧 mock。
- 预约通知测试改走 `startLive`；非开播通知测试改走 `endRoom`，不靠已废弃方法验证业务。
- 补充开播事务拒绝时，不查询预约、不发送通知、不创建 IM 群、不触发开播 Webhook 的断言，以及通用状态方法不得重新出现的断言。

## 当前验证

- 直播模块 Jest：16 suites / 266 tests 通过，包含媒体证据纯策略的 15 项测试。
- 服务端 TypeScript：`tsc --noEmit` Exit 0。
- 服务端 Nest 构建：Exit 0；本轮修改文件 `git diff --check` Exit 0。
- 运行目录：隔离工作树 `mobile-paipan-compass-v243-20260905/apps/server`。
- 失败分支测试中的供应商错误/缺配置日志为 mock 故障测试，不是实际云端结果。

## 不包含的完成声明

本轮不证明媒体已真实停止，不释放 ACTIVE 额度。CSS 会话证据纯策略尚未完成持久化及供应商停流证明接线；TRTC 与付费音视频问答资源收尾也未因此完成。管理员直授与普通观众阅读规则不变。源码仍未冻结，不能以本轮局部通过作上线结论。
