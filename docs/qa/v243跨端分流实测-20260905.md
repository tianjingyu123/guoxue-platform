# v243 跨端分流实测

## 测试范围

当前未冻结工作树的本地 H5 开发预览，127.0.0.1:4187/h5/；390×844 视口。接口经 Vite 同源代理连接预发布，不使用模拟业务数据。浏览器尺寸测试不等同 Safari、iOS WebView 或签名包验收。

## 本轮实际结果

- OnePlus 9R（LE2100）ADB 显示 device；本轮未安装新包，不能将其现有版本当作 v243。
- 初次跨域直连时首页展示加载失败；仅调整本地启动环境为相对 API 地址及 VITE_DEV_PROXY_TARGET=https://pre-api.rebugx.cn 后，首页、发现页均显示预发布真实内容。未修改远端 CORS。
- 首页→发现→我的：页面内容均正常加载，无持续白屏；开发模式首次访问会先按需编译，本轮不作正式性能结论。
- 我的→工具：未登录用户看到明确的登录使用提示，未泄露自研工具入口。
- 工具页“先逛逛”单次点击返回首页，未卡住或反复跳回登录。
- 尚未验：登录态原版结果分享、支付确认、指南针真实旋转、短视频首帧与评论、圈子手势、微信小程序运行时。上述不能由本轮基础导航通过代替。

## H5 冷启动入口不一致修复

- 复现：直接打开 pages/circles/index 能看列表，点击详情却被当前模块总开关转入停用页。不是阅读权限被发布资格限制，而是 H5 直达绕过了站内导航检查。
- 初版仅添加 beforeEach 的单测通过，但浏览器仍失败，未记为完成；进一步补齐 isReady 后首个路由检查，replace 而非新增历史栈，并防止慢响应覆盖用户新去向。
- 浏览器 reload 同一圈子 URL 后实测进入 feature-unavailable?module=circle；不改变远端开关，也不将停用视为圈子业务验收通过。
- 新增 client-module-h5-history-guard.ts 并在 main.ts 的 H5 路由接入；相关三文件测试 12/12，移动端 vue-tsc Exit 0。
- 未宣称 iOS 原生手势、圈子开启后的阅读与评论、登录态权限已经完成。

## 微信小程序构建

- 当前工作树预发布公开配置，build:mp-weixin Exit 0；未上传、未提交审核。
- WXML 461 文件通过，文本产物 1985 文件通过；主包 1.51 MB，上传口径保守估算 1.79 MB，强制余量 0.08 MB。
- 30 个分包，最大 pkg-circle 1.00 MB；构建链所有体积门禁通过。不能替代开发者工具/手机运行时验收。
- H5 模块路由回归已加入 release:test-paipan-suite，从而由既有移动端总门禁调用，避免仅手工运行后遗漏。

## 后续顺序

## 后续真实浏览补验

- 修复圈子预览与会员说明的固定虚假权益：不再把入圈费描述为直接提问、回放、勋章或全部精华的全包权益。共享 circleMembershipNotes 只解释访问、额外服务与参与规则；尚无逐圈权益契约，不声称已实现自定义权益管理。
- 浏览器初次保留旧模块文案，未判通过；完整刷新后已显示“入圈前请了解”及三条边界说明。相关信任文案测试 3/3，移动类型检查 Exit 0。
- H5 短视频列表进入首条“古籍真正的距离，不只是年代”，截图可见实际画面；DOM 视频状态为 videoWidth=1080、videoHeight=1920、currentTime=13.817821、paused=false、readyState=4、error=null。
- 评论按钮实际打开“共0条评论”抽屉，关闭后仍在详情；未提交评论、点赞、关注或付款。
- 点击收起内容详情返回原列表，DOM iframe 数量为0，详情播放器文档已移除；不把该证据替代 App renderjs 解码器释放或 iOS 首帧验收。
- 微信开发者工具已启动但停在登录二维码，未操作认证；此项列为最后需用户配合，不阻塞 H5/本地修复。

## 继续验收：互动可访问性与直播公开浏览

- 短视频五项关键互动补齐可访问名称、按钮语义、键盘 Enter/Space 操作；点赞/收藏同步已有状态。未变更业务授权或自动执行任何互动。
- 浏览器可按“打开评论”按钮名称操作，实际出现评论抽屉；关闭评论、收起详情后 iframe=0。播放器/触控/信任文案专项共 11/11，移动 vue-tsc Exit 0。
- 直播广场普通访问成功，没有被发布资格拦截；当前真实响应没有直播/回放，显示明确空态，不声称已经验证播放。
- “电商带货”筛选→“查看全部直播”→返回首页实际通过；首页仍展示商城、智能体、圈子、视频、直播入口。
- 本轮未开播、未入圈、未发评论、未支付、未改云端配置。
- 商城首页无推荐商品时原先只剩标题和空货架；现补齐“暂无推荐商品”与分类/下拉刷新指引，浏览器完整重载后可见。加载失败仍使用独立错误态，不把失败伪装为无商品。
- 最新移动总回归第二组 272 项：271 项在无子进程隔离模式通过，唯一构建子进程项受 Windows spawn EPERM 阻断；同一测试在获准环境单独复跑 1/1 通过。不能把最初脚本 Exit1 写为整条脚本直接成功。
- 包含上述模块兼容、圈子说明、视频按钮、商城空态的最新小程序构建 Exit0：462 WXML、1989 文本产物通过，主包1.51MB/上传保守1.79MB，30分包最大1.00MB；仅本地产物，未上传或审核。
- 智能体广场实际加载11项及分类/榜单，返回首页正常；只验证浏览，不把展示“在线”视为供应商可用性、模型回答质量或付费调用通过。
- 最新商城改动后的移动类型检查再次 Exit0；浏览器最终保留首页，主要入口可见。

苹果投屏受阻时不重试消耗整轮：先 H5 通用交互与小程序构建/运行时、安卓同包验证；iOS 原生分享、支付、传感器和 WebView 特有问题保留最终真机专项。不得宣称浏览器模拟已证明苹果原生能力通过。
