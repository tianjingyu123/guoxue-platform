# 后台配置发布与回滚：真实数据库及 HTTP 验收

## 本轮结果

- 4 suites、57/57 通过：既有服务/控制器46项、真实 HTTP 8项、真实 PostgreSQL 3项。
- 服务运行时模拟6项、后台脚本交互6项复跑通过。
- 服务端完整 TypeScript 检查通过。
- 证据：`D:/gx-deploy-91/artifacts/feature-flag-http-pg-r2-20260905.json`
- SHA256：`C94924097CE9EFA75F6AC1CA276ED08049660042046757CF8F90BCE12110126A`

## 真实数据库结论

新建 localhost PostgreSQL 16.13，执行本工作树完整356表基线，不读取线上 DATABASE_URL。
预览不增加历史；发布和回滚分别只追加一条版本。另一管理员提交后，旧预览发布/回滚均409且不增加历史。
通过读取后的测试屏障让两个真实 Serializable 事务同时读到同一版本，竞争结果仅一方提交，历史不重复。
发现的 Prisma P2034 已转为409友好提示，禁止自动重试旧意图。第二轮真实竞争确认转换有效。
仅 Redis 失效调用模拟，不构成真实 Redis 缓存传播证明。

## HTTP 结论及限制

实际 Nest HTTP、ValidationPipe、RolesGuard、RedLineGuard 执行；仅身份来源与业务服务模拟。
匿名401、普通用户403；平台运营预览可用且 private/no-store；非法 DTO 400。
运营角色不能回滚，自动化管理员不能发布/回滚；真人超级管理员的可信操作人和指纹正确传入业务层。
这些不等于真实线上 JWT 登录、真实浏览器点击或真机验收。

## 清理与边界

本轮新数据库 `artifacts/local-qa/circle-workflow-pg-7ZwMWS/data` 已停止，postmaster.pid不存在，合成资料保留。
首轮 `circle-workflow-pg-ivrdiP` 也由测试 finally 停止，未删除旧 QA。
没有部署、迁移线上库、云打包、修改 DNS 或真实支付。

## 仍待完成

真实浏览器全流程、真实缓存失效/刷新、同安装包设备证据；编辑开始到预览之间的差异核对；删除并发保护。
旧 API 不带指纹仍兼容，不能宣称全部写入口强制预览。持久草稿也尚未实现。
