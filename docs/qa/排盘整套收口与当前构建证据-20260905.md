# 排盘整套收口与当前构建证据

记录时间：2026-09-05。当前为未提交工作树，不是已部署候选。

## 业务边界

只提供整套 legacy/native 切换，不增加逐工具权限配置。页面接入扫描是生命周期覆盖的必要条件，不是 83 个后台开关。共用电子罗盘不依赖自研专属资格。

## 本轮验真

- 页面必要条件扫描：83/83，missing=[]。
- `release:test-paipan-suite`：123/123。
- `release:test-mobile-native-bundle`：先执行上述 123 项，再执行 249/249，Exit 0。沙箱内子进程 EPERM 后，以已批准的本地沙箱外执行重跑通过。
- mobile vue-tsc：Exit 0；git diff --check：Exit 0。
- 当前 App 本地预发布配置构建 DONE，原生脚本审计通过。
- `apps/mobile/dist/build/app/app-service.js` SHA256：`A50BC607239E7CD6AD7DA18E341CA8DFC0A2C71654BE0AB8168470CF65433FA2`。
- 此 App 构建包含最新诸葛、数字、姓名、起名页面修改；早前 H5/微信构建不能代表当前源码。
- 随后同一源码 H5 预发布配置构建 DONE/Exit 0：`index.html` SHA256 `9AF38E88F8D38BF6CF17F305C26481B025C84AC661072740D2F1A806777ADF16`，入口资源 `index-Bz-jtiJ0.js`。这是本地编译证据，尚未对本轮新 H5 产物执行浏览器验收；微信仍需重建。

姓名历史页参数与结果页不一致已修正，保留旧分散参数兼容；起名收藏与历史按账号隔离，公共旧数据不自动认领、不删除；收藏存储失败不显示成功。相关页面脚本和真实存储模块联测通过。

## 独立上线阻断

严格源码审计 Exit 1：2259 个生产源码文件，P0=1/P1=0/P2=11。P0 为 Git 历史仍保留 `backups/wal/000000010000001600000020` 数据库运行备份。当前 `git ls-files -- backups/wal` 无输出，但 `git log --all` 可追溯历史。

不得因当前树已删除就宣称数据已从仓库历史移除；也不得直接关闭此门禁。后续需只读确认涉及引用和分发范围，再经明确授权实施历史治理、协调所有协作者和远端。当前没有读取备份内容，没有重写历史或推送。

进一步元数据核验：文件在提交 `da22664aa748af8efadc84f922ac1d1c035439d7` 中为 16,777,216 字节。当前 HEAD 不以该提交为祖先；本地 `refs/heads`、`refs/remotes`、`refs/tags` 无包含引用。全引用检查唯一命中 `refs/prod-snapshots/20260904-before-admin-ui`。因此目前证据仅证明本地保留的生产快照引用含该对象，不证明当前候选分支或任一实际远端仍包含它；实际远端尚未查询。不得将此扩大描述为已确认公开泄露，也不得擅自删除用于回滚的快照。

后续只读 `git ls-remote --refs` 精确查询同一个 `refs/prod-snapshots/20260904-before-admin-ui`：backup、cnb、gitee、github 四个远端均成功返回 0 个匹配引用。范围仅为该引用的广告结果，不证明其他分支/标签或不可见历史对象不存在同一数据。没有 fetch、push、删除或重写。

## 仍不能宣称完成

### 后续小程序与浏览器实测

微信完整构建及门禁 Exit 0：461 个 WXML、1983 个文本产物，主包 1.50 MB、保守上传 1.79 MB、30 个分包；三个自研分包、共用罗盘和第三方嵌入目录均未进入产物。app.json SHA256 `B92B251E9D9D19F3236145374A57B9D3C70AAA53E0849FAEFA497BD5F4440B06`。未上传微信。

本地 H5 实测匿名起名结果深链回到 `/h5/pages/paipan/index`，最终显示无法确认服务状态、重新连接和返回首页，不泄露结果。共用罗盘可独立打开，未点击方向授权。浏览器观察发现未感应时仍显示默认 180° 正南，因此新增修复：未测量显示等待读数、静态盘面示意；手动设定明确标记；离页清除测量状态，不允许锁定无读数。真实页面脚本回归 1/1、mobile 类型检查通过。

**时效边界：上述罗盘展示修复晚于本文件记录的 App/H5 构建，两个产物须重建后再复验；不能以旧产物截图声称修复已可见。** 本地静态服务已停止，用户原有后台/TAT/DNS 标签未操作。没有真实指南针北向证据。

随后 H5 已重建 DONE/Exit 0：index.html SHA256 `556CEB9DDBE6F6BF6C5C152F1BB2763F64197A180054161A8CC11955C4753ED4`，入口 `index-CNbGU_ek.js`。新建浏览器页实际显示“— 等待有效方向读数”和“盘面示意，尚未定向”，未再把默认 180° 放在测量读数栏。未点击方向权限，故仅通过无测量展示验证；App 仍需重建，手机传感器仍未验收。专项复跑 124/124。该轮本地静态服务也已停止。

App 随后也重建 DONE/Exit 0，原生脚本门禁通过，新 app-service.js SHA256 `B21062F7C330280E88D690A701E3B2E75C4531C29681E6E44B6D748EE38FF07E`，包含读数显示修复。未形成签名安装包。

## 全批差异与后端核验

当前未提交差异分类时点共 327 文件：后台7、客户端141、数据库5、服务端53、测试/文档108、共享配置1、交付工具12。该数会随证据文件更新变化，不是冻结清单；分析只提示全部构建及迁移评审，不产生发布授权。

数据库新增迁移为 `manual_add_circle_capability_quota_ledger`、`manual_add_circle_capability_workflow`、`manual_add_circle_platform_direct_grant`，另有 schema/full-baseline 修改。尚未执行任何本轮迁移。

server TypeScript、admin vue-tsc 均 Exit 0。后台门禁初次发现路由测试只接受旧实现字符串，已修正为严格要求超管身份、实时使用资格、管理资格及404隔离；后台测试98/98。迁移就绪源码检查的旧 `runtime.isNative()` 断言更新为已实现的 `await runtime.getCurrentMode(prisma)`，保留legacy承接与失败不回退要求，7/7通过。二者不等于真实后台操作或数据库迁移演练通过。

随后 server nest build Exit 0；admin production build Exit 0（4100 modules，入口 index-CMEUAuYU.js，初次 esbuild EPERM 后按审批重跑）。服务端整套模式、缓存、第三方承接、圈子能力/额度/直授及配置开关相关测试18 suites/474 tests通过，包含本地模拟身份/业务服务的HTTP测试，不能等同线上JWT与数据库贯通验收。数据库空库基线审计已在获准本地子进程环境下通过，与schema完全一致。

仍有明确接线缺口：`CircleCapabilityController` 尚未注册到 CircleModule，后台未检索到该能力专用管理入口。不得因策略/仓库/HTTP模拟测试通过而宣称管理员可直接授权已经可用；需遵守圈内能力持久化接入约束完成业务、管理操作与实际权限链验收后再启用。当前没有为消除这一缺口而直接注册或放开接口。

- 没有新正式签名包、DCloud 提交、设备安装、TestFlight 或预发布部署。
- 支付确认、分享卡片/二维码海报、真实北向罗盘、底部布局、iOS 选择器及视频等必须以对应最终签名包真机验收。
- 全系统上线目标、正式切换和回滚证据尚未完成。

## 分享路径与罗盘断流修复后的最新移动构建

以下为未冻结工作树的增量证据，取代上文较早产物对当前源码的代表性，不取代真机验收：

- 完整移动原生门禁 Exit0，后段 265/265；mobile vue-tsc Exit0。
- 预发布 App 编译及原生脚本审计 Exit0，manifest `1.1.0(243)`，app-service SHA256 `10E3F167CB6DD276EB31880EFA905D1962F75C2A02E2AEEA0D39F85044E353EB`。
- 预发布微信构建 Exit0：WXML 461 文件、文本产物 1985 文件通过；主包约 1.51 MB，含平台开销保守上传估算约 1.79 MB；30 分包，最大 pkg-circle 约 1.00 MB，主包/分包门禁全部通过。
- 微信 app.js SHA256 `B868726CA7DA50750224B5C4D7FF08C0A6783DD006921E99D264410258519DBC`；app.json SHA256 `B92B251E9D9D19F3236145374A57B9D3C70AAA53E0849FAEFA497BD5F4440B06`。这两个哈希不是完整小程序包哈希，也不证明平台接收成功。
- 两端使用显式 `pre-api.rebugx.cn` 公开配置；未上传、未云打包、未安装、未改云端。
