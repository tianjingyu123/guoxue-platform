# 配置删除并发保护

本轮修复后台删除未锁定对象、未防重复点击、失败被当作取消静默吞掉的问题。删除前只读预览并展示当前启用状态与灰度，确认后携带基础指纹；服务端在 Serializable 事务内校验并删除，冲突拒绝，不自动重试。取消不写，失败提示管理员重新核对。保留历史记录。

## 实测

- 真实页面脚本操作测试：9/9，包括删除对象捕获、重复点击、取消、失败提示。
- 服务端/HTTP/真实 PostgreSQL：4 suites，62/62。
- PostgreSQL 隔离库 16.13、356 表、localhost:61077，目录 `D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-V8SXqM/data`，正常停止。
- 数据库用例证明旧指纹删除 409、记录未变；新指纹删除后记录不存在、历史保留两条。
- admin vue-tsc 通过。

本轮未做浏览器视觉验收；页面脚本测试不等于双端真机证据。为兼容旧调用，缺少指纹的原删除接口行为仍保留，因此不能宣称所有客户端删除都强制预览。未提交、未部署、未操作线上数据。
