# 圈内能力额度第六批：锁定快照与额度决策

基线：`f25b7471f5063a612a17a422a64900a29b5f8cd3` / v243 的独立工作树未提交增量。此记录不表示该提交已包含未提交代码，也不形成新固定包。

## 本批实现

1. 修复缺行锁边界：取锁时不存在的配置、账号、成员和平台角色，不接受其后插入而未被本事务锁定的记录。授权查询限定到锁查询实际返回的 ID；补充 6 项仓储/事务模拟回归。
2. 新增纯额度决策内核，区分 HELD、ACTIVE、COMPLETED、RELEASED、EXPIRED。圈级与个人两份额度分别检查累计次数与并发；任何一份不足即拒绝。
3. 请求键、业务实例、操作者、服务者、用途、次数、占位期限共同绑定；两个索引回读不同记录或不同快照即停止。同请求重放只返回 NONE，不再生成外部任务。
4. 开始业务前重新核验主体、有效授权和修订；完成业务保留累计消费。未开始可释放，已开始不按占位 TTL 回收；撤权不阻断可信内部收尾。
5. 回执带操作键和已应用版本；重复完成/释放等操作不能再次影响计数。当前只能决定下一状态，尚无真实数据库回执和额度记账。

代码文件：

- `apps/server/src/modules/circle/circle-capability.repository.ts` 及其测试
- `apps/server/src/modules/circle/circle-capability.service.ts` 及其测试
- `apps/server/src/modules/circle/circle-capability-quota.policy.ts` 及其测试

## 验收结果

- 新额度决策测试 84/84；额度与锁定快照聚焦 3 suites / 131 tests 通过。
- 圈能力、既有圈子/问答/发布授权合并回归：15 suites / 441 tests，通过 441、失败 0。
- 完整服务端 TypeScript、Nest build、Prisma validate、schema 独立生成 SQL 比对、diff-check 5/5。
- 上批增量迁移静态 4/4、数据库基线生成器测试 4/4；构建检查合计 13/13，无跳过。
- 13 个 capability 相关源码/测试文件 ESLint：0 error、0 warning。

首轮新增测试暴露宿主 `structuredClone` 的 Date 来自不同 Jest VM，改为模拟 Prisma 回读的本 realm Date 后再次执行通过；未放宽生产日期校验。构建首次因沙箱禁止子进程 `spawn EPERM` 未执行，按工具审批通过后在沙箱外完成相同本地命令；不是 1344，没有改 UAC、重启设备或修改数据库。失败记录保留，不计作成功。

## 外部机器证据

| 文件 | SHA-256 |
| --- | --- |
| `D:/gx-deploy-91/artifacts/circle-capability-quota-full-regression-20260905.json` | `21cd52831c391c3727b4a4a832bfd68284a268189077b253583e4ef22e12405e` |
| `D:/gx-deploy-91/artifacts/circle-capability-quota-build-r2-20260905.tap` | `4344d36f68c5c9d5aefc60017ddd36113cbf9a62d42c39bac4e67dd3eb9395d9` |
| `D:/gx-deploy-91/artifacts/circle-capability-quota-lint-20260905.json` | `0d36269926836d91b6189926d1a65081fe30d1920cce4f49cb5f7398539a7691` |

## 尚未通过的门禁

**本批没有新增配额表、原子计数更新、可靠外部任务、业务资源创建接线或公开接口注册。** 上批 `manual_add_circle_capability_workflow` 的 2 表迁移不变；没有在线执行。QuotaUsage 和操作回执未来必须由同一主库事务读取，不能接受客户端快照。

真实 PostgreSQL 双连接竞争尚未执行。本地模拟测试不能证明数据库锁行为、并发额度不超售、不同业务适配器都遵守锁顺序；也不能证明媒体供应商超时后是否真实启动。接入前必须按《圈内能力授权-持久化接入约束-20260905.md》3.2 完成持久化与竞争验收。

前后台能力展示、最终 Android/iOS 的 H5 卡片接收、二维码落地、罗盘真实方向变化、底部完整显示仍待完成。原 46 项 SH/CG/OP/NV 验收矩阵未因本批本地测试改成通过；不宣称真机或上线验收通过。

本轮无提交、推送、线上迁移、服务器/COS/TAT/DNS 操作、云打包、安装、付款或商店发布。保留同一 v243 候选集中收口，避免每个局部改动都生成一轮客户端版本。
