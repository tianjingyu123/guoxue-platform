# 实时问答本人启停接线

- 达人设置页分别读取 AUDIO_QUESTION、VIDEO_QUESTION 的本人当前授权，验证圈子、能力及当前用户精确匹配。不以连麦价格大于零代替授权。
- 已批准的个人授权支持本人启用/停用；启用要求有效期未过，已启用的到期记录仍可请求停用清理。服务端继续核验当前状态和revision。
- 点击后先展示影响说明，取消零写入；写请求单次提交、禁止重复点击；结果不明只回读，不重发。不删除订单、不自动退款、不实际通话。
- 启停后的资格刷新只更新音视频准入标志，保留未保存的价格草稿。启停、切换圈子及保存价格互斥。
- 未取得授权时不显示启用按钮，说明由圈主申请或平台直授；普通查看、咨询列表及用户购买入口的既有可见性规则未修改。

验证：新增真实页面脚本5/5，既有咨询可用性16/16，合计21/21；mobile Vue类型检查通过。旧测试夹具首次缺少新增request依赖和卸载钩子，补齐明确合成返回后通过，未放宽生产逻辑。

尚未完成：圈主为圈级及服务者提交音视频申请的完整前台流程、同包实际操作和双端通话。此文件只记录本人启停，不代表申请审批或实时媒体全链路已通过。未部署云端、未签名打包、未实际付款。

## 后续圈主申请接线

- 增加圈主只读申请预检，复用提交时的同一计划器：包含经营门槛、当前申请、冷却期、圈级与个人授权依赖。预检不创建记录，当前操作者仍须在事务内是圈主。
- 达人设置中圈主可选择语音/视频、圈级/本人两种范围，先核对后提交；未达条件不显示申请提交按钮，申请原因必填，提交不批准也不直接开放服务。
- 圈级获批后圈主可明确启用，再申请本人服务；个人获批后使用既有本人启停区域。平台直授仍为独立路径，不要求走普通圈级申请。
- 切换能力或范围后旧预检不得提交；重复写受同一忙状态约束，结果不明只重新读状态。
- 本轮服务单测44/44、HTTP63/63、前台脚本与可用性24/24；server和mobile类型检查通过，指定文件diff-check通过。未部署或真实提交申请。
- 剩余精确缺口：圈主选择其他嘉宾并代其申请的前台入口尚未完成；后台平台直授已有独立页面，但不能把它当作普通申请流程的替代。还需同包操作与双端通话实测。

## 后续嘉宾选择接线

- 上述“其他嘉宾选择入口尚未完成”已由本轮本地实现补齐：圈主从真实圈成员分页列表选择对象，展示昵称及用户 ID 末八位；不接受手填任意账号。
- 圈级、本人、其他成员三种申请范围分别绑定精确主体；翻页清除选择和旧预检，跨圈旧对象、未列出成员及迟到响应均不得用于提交。
- 选择成员不代表获权，仍执行服务端申请资格核验；审批后须成员本人启用，圈主不代替嘉宾启用，也不自动审批。
- 页面脚本与既有咨询可用性测试合计 27/27 通过，mobile Vue 类型检查通过，diff-check 通过。
- 仍未完成：同包页面实际操作与真实双端通话。本轮无真实申请、云端部署或付款，不据此宣称上线验收通过。

## 整合验证补充

- 最新页面改动之后，预发布公开配置的 H5 与 App 本地编译均 Exit 0；首次 H5 编译被沙箱子进程 EPERM 阻断，改用获准的本地沙箱外编译后通过。未升级编译器或依赖。
- 咨询可用性、嘉宾申请、发布资格适配器、共享申请面板联合运行 38/38 通过。H5 生成 expert-config 页面脚本和样式；编译通过不是浏览器或设备实际验收。
- 后续收口项：短视频/直播共享申请面板仍需对齐 application-context 的冷却期判断，并核定个人授权暂停后的本人恢复入口；不得把简化 eligible 判断当完整申请状态机。
