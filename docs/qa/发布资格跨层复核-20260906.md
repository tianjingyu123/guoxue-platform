# 发布资格跨层复核

## 当前结果

- 服务端发布事务、直播发布、平台直授及资格服务四组专项：107/107 通过。
- HTTP 路由与红线两组：66/66 通过；身份及业务依赖使用合成替身，仅证明路由、参数和守卫接线，不冒充真实登录验收。
- 本机隔离 PostgreSQL 指定五项：5/5 通过，其他 151 项跳过，不计入本次通过数。
- 五项覆盖：普通 MEMBER 直授可创建直播；直播撤权不能创建；短视频撤权时视频和额度零新增；成员直授投稿仅使用个人额度且只读资格查询不写入；管理员恢复暂停后仍需本人启用，过期不得再预留。
- 源码核对：短视频 create 无论可见范围都进入授权额度事务；平台管理员分支在事务内复核身份，不能只依赖前台角色。普通阅读列表未接入发布资格拦截。

## 证据边界

本次为本地服务测试与隔离数据库事务验证，不连接预发布或生产数据库，不调用真实媒体、上传、支付或内容发布。当前未冻结候选、未签名安装，不能替代同包真机或云端端到端验收。测试中的机审等外部服务是合成替身。
