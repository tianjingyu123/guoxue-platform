# 后台配置 HTTP 到真实数据库贯通

本轮不再仅分别验证模拟 HTTP 服务与数据库服务，而使用实际 FeatureFlagController、ValidationPipe、RolesGuard、RedLineGuard、FeatureFlagService、Prisma 和本地 PostgreSQL 同链执行。

覆盖：匿名401、普通用户403、运营只读预览无落库、自动化新增403无历史、真人新增成功、同名新增409、运营编辑成功、过期指纹删除409、运营删除403、自动化删除403、非法指纹400、真人删除成功、已删除清单超级管理员限定且不返回用户名单、历史恢复成功、恢复后正常记录存在且清单移除、历史恰为三条。

结果：真实数据库集成 6/6（包含以上 HTTP 贯通用例及既有竞争事务回归）；server tsc --noEmit Exit0。

PostgreSQL 16.13/356表，新建隔离目录 `D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-EGdNka/data`，仅 localhost:55863；结束已正常停止。

边界：身份来源为测试专用合成角色，不是生产 JWT；Redis 是不缓存替身，不证明真实缓存失效或传播；没有浏览器连接此 HTTP 实例。未提交、未部署、未访问线上或修改任何云端状态。继续保留真机同包配置刷新、原生能力、正式发布门禁未完成状态。
