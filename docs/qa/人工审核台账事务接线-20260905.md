# 人工审核台账事务接线

## 已修改

AuditService.reviewContent 对 ContentAuditRecord 参数化 SELECT FOR UPDATE 后读取当前状态；软删除、不存在、已审结及非法动作拒绝。台账更新和已配置内容目标的 auditStatus 回写使用同一个 Prisma 事务。内容更新异常不再吞掉，因此提交失败不会继续失效缓存或输出成功审计日志。

## 实测与边界

- 审核模块6 suites，109/109 PASS，含新增失败传播、锁先于读取、软删除和非法动作测试。
- 当前事务测试使用模拟 Prisma；没有声称真实 PostgreSQL 双审核员竞争或回滚已过。下一批必须补真实数据库验证。
- 原 VIDEO 驳回测试的后置 applyModerationVerdict 因既有 mock 缺 findUnique 记录错误后被捕获；这说明该套测试不覆盖完整降可见性/通知链路，不能把109通过解释为该链路通过。
- 缓存、通知和后置轻度处置仍在提交后，未新增可靠外发恢复。专用视频审核入口与统一台账入口尚未统一协调。
- 未执行线上迁移、部署、内容修改或消息发送。

## 后继真实 PostgreSQL 验证

`test/content-audit-pg.integration-spec.ts`：3/3 PASS，使用实际 AuditService、Prisma事务、真实User/Video/ContentAuditRecord/AuditLog表：

1. 删除本次合成视频后审核，业务回写失败，台账仍PENDING、审核员/结束时间为空，成功日志0。
2. 台账与视频两次更新后注入除零异常，二者均回滚到PENDING，未失效缓存。
3. 双连接并发approve/reject，实际观察到数据库锁等待；先提交者APPROVED，后者报已审结，视频APPROVED，成功日志仅1条。

实例为 PostgreSQL16.13，本机127.0.0.1:49608；本地完整基线357张表，新目录 `D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-Mxi21O/data`。日志确认正常停止，合成数据保留。测试替代Redis与外部审核供应商，不测试真实角色HTTP准入、通知、后置轻度处置，也不代表线上数据库版本兼容已验证。

## 后继：人工驳回通知与可见性原子保存

人工审核不再提交后调用 applyModerationVerdict；VIDEO/COURSE/LIVE 驳回时，同事务保存SELF_ONLY、恢复快照日志及站内Notification。提交后失效列表和详情缓存；未调用外部推送。机器审核执行器语义未在此批改变。

最终修改实测2 suites、36/36（31单测+5真实PG），TypeScript通过。真实PG新增验证驳回/重复审核仅一条通知、真实Notification约束失败导致台账/可见性/恢复日志全部回滚。最后实例 `circle-workflow-pg-KzsSMN/data`、端口52746，357表，已正常STOPPED。

此证据取代前述“人工后置轻度处置mock缺失”的旧范围说明；未证明前端通知已读交互、真实审核员HTTP权限、不同内容审核记录之间的业务竞争或线上缓存可用性。此前491套全量源码测试发生在本批之前，不能算作本批后的全量结果。

## 后继：恢复快照内容行锁

人工驳回先以固定表名白名单锁定Video/Course/LiveRoom内容行，再读取恢复快照。真实双连接测试：一个事务先把视频设HIDDEN/APPROVED/PLATFORM并持锁；审核等待，待其提交后恢复日志必须记录这组新状态，而不是等待前旧值。最终内容保持HIDDEN/REJECTED/SELF_ONLY。

最终37/37（31单测+6真实PG），TypeScript通过。实例 `circle-workflow-pg-V616DS/data`、端口63953、357表，日志STOPPED。未验证不同审核批次的业务优先级规则；此项仅证明快照读取不越过内容锁。
