# 第三方排盘共用罗盘入口解耦

## 本地改动

- 原生罗盘页面与传感器适配、盘面组件迁至 `pkg-common/compass/`，不再属于自研专属路由。
- 第三方桥直接进入共用地址；`/paipan/luopan` 别名指向共用地址，原 `pkg-paipan3/luopan/index` 保留兼容重定向，不复制传感器实现。
- 共用页面不依赖管理员预览资格；小程序仍不登记共用罗盘页面，旧/新地址均由小程序维护策略拦到维护页。App/H5/Harmony继续登记。
- 未修改第三方支付接口/商户/订单/权益，未上线、未云打包、未安装设备。

## 验证

- 罗盘与第三方桥回归17/17；补充共用入口与分端维护6/6；mobile type-check通过；diff-check通过。
- 最新App本地构建DONE，原生脚本审计通过；app-service.js SHA256=`68B5A18827F4593DD4BCD864F17E0E0F146934B37D2F7912A1BC52D022EF459D`，app-config-service.js SHA256=`432315D3580019CFF68323D7FC8C096ED9A69B19785CF3BA11ACC6E8BCE984B7`，两者均含共用路由。
- 最新微信构建Exit0：461 WXML、1980文本产物通过；30分包，主包1.50MB，保守上传估算1.78MB。
- 构建仅使用预发布公开配置。Sass旧API警告保留，未为消除警告升级依赖。

## 未完成，不可替代

### 后续原生读数修复（晚于上述构建）

对照 [HTML5+ Orientation 官方定义](https://www.html5plus.org/doc/zh_cn/orientation.html)：alpha为相对初始方向旋转角，不是北向；magneticHeading/trueHeading取值0至360（不含360）。已停止把alpha当指南针兜底，忽略非法原生方位值，负数磁北不会遮挡有效真北。新一轮监听使用代次隔离，防止停止后再次进入时旧uni/HTML5+回调污染新盘面；超时回调亦核对运行状态及代次。

新增两个回归修复前失败、修复后通过；罗盘运行回归5/5，合并第三方支付/分享/返回为60/60，mobile type-check通过。上述App构建哈希对应此读数修复之前，不能作为此修复已进入安装包的证据。

### 后续 H5 运行回归（同样晚于上述构建）

真实适配层在合成浏览器事件环境验证4/4：相对alpha不冒充北向；离页后迟到的权限结果不重启监听；新会话拒绝旧事件并重置来源；停止后迟到超时不改状态。连同原生适配、整套切换后台及移动入口回归20/20通过。该组证据不代表Safari或微信内浏览器真机通过。页面超时提示改为“暂未收到有效方向读数”，明确手动读数不代表手机实际朝向，不把超时误报成设备无传感器。

这些证据不证明手机磁北/真北读数、实际转动、传感器权限、前后台恢复或锁定解锁正确；仍需同一签名包在iPhone及OnePlus实测。旧兼容路由、整套模式直链边界与所有页面体验仍需后续回归，不能用本地编译替代上线验收。
