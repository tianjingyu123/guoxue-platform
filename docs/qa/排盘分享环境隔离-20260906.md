# 排盘分享环境隔离

修复正式域名硬编码：分享适配层仅使用构建绑定的 VITE_PUBLIC_H5_URL 生成工具入口，缺失或非 HTTPS 等非法配置直接阻断。预加载与后注入桥只提交通过原有过滤的第三方公开结果链接；没有结果时传空，由原生层选择当前环境入口，不让第三方页面决定环境。

没有放宽第三方域名、敏感参数或图片过滤，没有把工具入口当作当前排盘结果。跨正式/预发布的原生工具入口不被采信。

域名审计另外将精确 `.e2e-spec.ts` 后缀识别为测试夹具；新增对照证明同目录普通 runtime.ts 仍被阻断，没有目录级豁免。

验证：分享/海报专项41/41；移动原生整合门禁通过（末套273/273）；域名策略5/5；实际域名审计运行时0；mobile vue-tsc Exit0。未云打包、未外发微信、未付款、未部署或改DNS。

此前 App 产物早于本次源码，不能用旧哈希证明此修复已装机。后续需重新构建并绑定冻结候选，实际验证匿名接收同一公开结果；工具入口安全降级不等于结果分享闭环。
