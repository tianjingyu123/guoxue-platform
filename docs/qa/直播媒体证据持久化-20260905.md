# 直播媒体证据持久化（本地阶段）

日期：2026-09-05。未部署、未连接预发布/生产、未调用供应商、未停止真实推流。

## 本轮实现

- 新增 `LiveMediaEvidence` 单房间持久行、JSON 快照及递增版本；通过外键保留房间关系。
- 新迁移 `manual_add_live_media_evidence` 只创建表，不把历史 Redis 在线/离线值当作可信历史填入。
- 内部仓储 `LiveMediaEvidenceRepository` 在房间事务锁内归并，使用参数化 SQL 与版本比较更新。
- 重复回调不因 PostgreSQL jsonb 键排序而增加版本；不同连接不会互相覆盖。
- 仅保存版本、域/应用名、会话 SHA-256、开始/结束时间及不确定标记；不保存原始回调、签名、鉴权参数、IP。
- 配置归属变化变为 UNKNOWN，损坏记录拒绝覆盖；单纯 OFFLINE_OBSERVED 不释放额度、不修改业务状态。

## 实测证据

- 独立 PostgreSQL 16.13：9/9 通过，实际执行完整 358 表基线。
- 覆盖重复、双节点并发不同事件、同事件并发、旧断流晚到、先断后推、配置变化、事务回滚、串流拒绝、损坏行拒绝。
- 临时数据目录：`D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-GwYT5v/data`；端口 65310，仅 loopback。
- 测试终态输出 `LOCAL_WORKFLOW_STOPPED syntheticDataRetained=true`，数据保留便于复核。
- `tsc --noEmit` Exit 0。
- 完整基线审计通过：358 表、904 显式索引、271 外键，SQL 与 schema 一致。

## 下一步与不可宣称项

后续本地接线已完成：仓储注册 LiveModule；LiveService 的 CSS 0/1 回调写入主库，工作台状态读取主库证据，Redis 仅缓存独立 CSS 质量指标。TRTC 仍是原路径，未声称其已具备持久媒体证据。删除有媒体记录的直播返回明确业务错误，保留证据，不产生外键500。

追加验证：直播单测 16 suites / 271 tests；真实 PG 11/11，包含真实 LiveService 两实例回调/查询与相反缓存干扰、48小时证据过期及归属变化。追加数据库目录 `D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-zi6yuV/data`，端口65016；已停止，postmaster.pid 不存在。服务端 TypeScript 与本轮 git diff --check 通过。

下一步仍须接入供应商停止/禁止重连的可信结果并进行额度收尾。不能仅凭本轮 PG 测试宣称腾讯云真实回调、TRTC、额度释放或上线已经完成。新增迁移尚未获本候选云端执行结果，不能复用过去迁移的成功记录。
