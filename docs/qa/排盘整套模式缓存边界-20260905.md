# 排盘整套模式缓存边界

本轮仅本地修复，没有部署、缓存刷新、DNS或数据库操作。

## 已复现并修复

- 移动模式响应缺少mode时，旧代码默认legacy；现在仅接受明确legacy/native，否则unknown并使旧模式快照失效。
- 网络失败时，未来时间的快照原先仍有效；现在拒绝未来、过期、非数值及零时间。
- 自研控制器8处方法头仍为public缓存，与整套访问守卫的private/no-store冲突；现统一private/no-store。保留原路由、业务计算和权益，不增设工具权限。
- API说明同步移除与现行访问门禁冲突的“无需登录/CDN缓存”描述。

## 本地证据

- 模式快照新增2项修复前均失败；修复后合并入口和第三方隔离13/13。
- Nest真实方法元数据断言修复前在baziPreview失败；修复后缓存、整套策略、运行时3 suites/15 tests通过。
- mobile Vue TypeScript、server TypeScript通过；diff-check通过。
- 自研排盘和八字案例控制器源码无剩余public Cache-Control命中。

## 尚未证明

## 后续验证与发布接线

- 隔离PostgreSQL 16.13、356表真实数据库回归7/7：独立连接读取模式、并发版本冲突、撤权、审计失败回滚。测试端口62514、目录`D:/gx-deploy-91/artifacts/local-qa/circle-workflow-pg-PSx25w/data`，已输出STOPPED，未连接远程数据库。
- 真实HTTP缓存/合盘/案例库合并36/36；案例库普通账号前台允许、后台403，运营管理员按原后台权限进入，均以主库整套查询允许为前提。主库和业务替身限制仍保留。
- 新增`release:test-paipan-suite`并接入`release:test-mobile-native-bundle`前置，实际执行25/25；未宣称完整移动门禁已执行。
- 仍未完成整套直链/冷启动统一隔离。历史逐页审计尚需按统一机制收敛，不能为了通过门禁直接删除旧断言。当前不得冻结为可发布候选。

已增加本地Nest HTTP/JWT/真实整套守卫测试：普通账号主库允许返回201、同一账号主库改为拒绝返回404、未登录404；均为private/no-store，拒绝不调用计算。数据库、计算和限流为替身，这不是部署环境或真实主库模式变更证据。

旧部署及可能已有CDN对象未改变。最终部署需验证允许与拒绝响应均禁止共享缓存，确认历史缓存不继续暴露结果；直接链接、冷启动及真机全路径也未由本文件证明完成。
