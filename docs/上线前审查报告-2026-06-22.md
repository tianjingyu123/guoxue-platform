# 国学平台上线前全面审查报告

**日期**：2026-06-22
**审查者**：Claude Opus 4.8
**触发**：用户要求"全面彻底审查,确保达到上线水平,正确性做到合格"

---

## 一、已完成修复(本会话)

| 项 | 修复前 | 修复后 |
|----|--------|--------|
| server tsc | 0 | 0 ✅ |
| mobile vue-tsc | **136** | **0** ✅ |
| admin vue-tsc | **9** | **0** ✅ |
| server jest | 通过 | 通过 ✅ |
| mock 导入扫描 | — | 零违规 ✅ |

**关键发现**：mobile 136 个类型错误全是 V0(React)→UniApp Vue3 转换工具引入的**真 bug**:
- TS 类型联合符 `'a' | 'b'` 泄漏进 Vue 模板运行时表达式 → 按位或 NaN,导致 tab 筛选逻辑彻底失效(station-materials 等)
- mock fallback 对象形状与正式接口不一致 → 联合类型属性丢失(order/shop/operator 多页)
- ref 初始值类型推断错(any[]/{}/never)、接口字段缺失、uni 事件类型

已提交:`dc866565 fix: 清零三端类型错误`(全程查根因,无 as any 糊弄)。

---

## 二、待修复问题清单(用户已确认:全部修复)

### 🔴 后端安全(8 严重 + 8 重要 + 5 次要,扫 129 个控制器)

> 基线:无全局认证守卫,认证靠每路由显式 @UseGuards(JwtAuthGuard)。核心支付/钱包/订单/认证模块经逐行精读无严重问题,问题集中在新增的圈子助理/运营 dashboard/AI 网关等边缘模块。

**严重(必须上线前修复)**：
1. `ai-gateway/knowledge-sync.controller.ts:16-77` — 圈子知识库 sync/add/remove/candidates 越权读写删,circleId/userId 来自可伪造 body/param,无圈主校验
2. `paipan/paipan.controller.ts:396-411` — GET record/:id/share 公开,可枚举主键读他人排盘命理隐私
3. `bounty/bounty.controller.ts:70-79` — refund 不校验提问者归属,资金越权
4. `question/question.controller.ts:65-71` — getQuestion 公开返回付费 answer 全文,绕过付费墙
5. `circle/circle-dashboard.controller.ts` 全控制器 — 遍历 circleId 读任意圈子经营数据
6. `live/live-dashboard.controller.ts` 全控制器 — 读任意直播间 GMV/打赏榜/观众画像
7. `station/promotion.controller.ts:14-56` — 向他人分站注入/删推广素材
8. `classic/classic-image.controller.ts:72-102` — 古籍图像写操作缺 RolesGuard + Host 头注入

**重要**：
9. ebook 管理端缺 RolesGuard + 笔记/书签 IDOR
10. bot bind-circle/knowledge 缺圈主校验 + upload-file 无大小限制
11. im queryAccountState/getGroupInfo 可枚举他人状态/非成员读群信息
12. webhook url 无 SSRF 防护(内网黑名单)
13. data-explorer NL2SQL 可导出全量 PII
14. system backup/import/risk-control/churn/feature-flag/permission 等高危写操作缺审计
15. tts/map 资源消耗端点缺频控
16. system/permission 改角色权限 @Body 裸类型无 DTO 校验

**次要**：批量 DTO 校验缺失、审计字段可伪造(operator 来自 body)、明文 PII、SSE 错误回显、退款审计不一致

### 🟠 API 契约不匹配(17 处,核对 62 条)
- **poetry 诗词模块后端完全缺失** → 前端 4 接口全 404 【决策:新建后端 poetry 模块】
- live 直播前端路径与后端 rooms 体系整体错位(8 条):`/live`→`/live/rooms`、`/live/schedule`→`/live/scheduled`、hosts/preview/end/replays 不存在
- order 前端用单数 `/order`、后端是 `/orders` 且无 dispute(3 条)
- feedback 缺 `/users` 前缀(2 条)
- auth 改密/改手机 POST≠PUT(2 条)
- points/tasks 不存在、points/exchange 方法不匹配

### 🟡 算法伪随机违规(6 处,扫 144 个计算器)
> 八字/紫微/奇门/大六壬/梅花算法均扎实,问题集中在六爻和占卜签类。【决策:改报数/时辰干支起卦,可复现】

1. `liuyao.calculator.ts:154-185` — 六爻起卦用 Date.now() 比特位(核心造假,最严重)
2. `lingqian.calculator.ts:141` — Date.now() % totalSigns
3. `chunzi-shu.calculator.ts:114` — Date.now() % 96
4. `guanyin-lingqian.calculator.ts:1449,1453` — Date.now() % QIAN_DB.length
5. `lingqi-jing.calculator.ts:271` — Date.now() % 125(random 分支)
6. `huangli.calculator.ts:103,146-152` — charCodeAt 散列选吉时(次要)

(sancai-wuge 笔画散列已 @deprecated,被 wuge.calculator 取代,建议停用)

---

## 三、修复计划(用户决策:全部修复)

- **批次 1 — P0 安全**:8 严重 + 8 重要,按模块隔离分 Agent 修(加守卫/归属校验/审计/DTO)
- **批次 2 — 算法**:6 处占卜起卦改干支/报数起卦(可复现)
- **批次 3 — API 契约**:前端路径修正 + 后端补缺
- **批次 4 — poetry 后端**:新建 poetry 模块(schema/controller/service + 前端对接)

每批修完跑 tsc + jest 验证,原子提交。
