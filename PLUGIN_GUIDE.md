# 热卜国学平台 — 插件与架构选型指南

> 更新时间：2026-06-02
>
> **核心目的：每接到一个新需求，先想清楚"该用什么方式实现"，不要默认所有功能都自建。**

---

## 一、三条最高原则（不变）

### 原则 1：以业务目的为准

先想清楚这个场景的**用户体验、性能要求、成本边界**是什么，再选技术方案。技术为业务服务，不是反过来。

**在动手之前必须回答：**
- 用户在这个场景下的核心诉求是什么？
- 可接受的延迟上限是多少？（实时 <200ms / 近实时 <2s / 异步无要求）
- 单日预估调用量级？（百次 / 万次 / 百万次 / 千万次）
- 数据能不能出服务器？（隐私 / 合规 / 用户感知）

### 原则 2：借力优先于自建

能用现成方案就不自建。自建只用在**真正的护城河**上。

**优先级从高到低：**
1. Coze 原生能力（智能体 + 知识库 + 官方插件 + 工作流）
2. 第三方 SaaS（成熟方案直接买）
3. 开源工具（有社区维护的优先）
4. 自建（排盘引擎 / 国学知识图谱等核心壁垒）

### 原则 3：可演进优于完美

先用**最快的方案跑通验证**，验证 OK 再优化为更合理的架构。不要一次性追求最优解。

```
验证阶段（1-3天）→ 优化阶段（1-2周）→ 固化阶段（按需）
```

---

## 二、技术路径全景（非封闭清单）

> ⚠️ **这不是完整清单，更不是限制。** 遇到新场景时基于原则灵活判断。如果你（Claude Code）在开发过程中发现更合适的方案——新的协议、新的工具、混合方案、降级方案——主动提出来。

### 对话/智能体类

| 路径 | 适用场景 | 成本 | 备注 |
|------|---------|------|------|
| **Coze 原生智能体** | 标准对话、FAQ、知识问答 | 低 | 零代码，知识库+插件+工作流一站式 |
| **Coze + 自建 HTTP 插件** | 需要自定义逻辑的对话 | 中 | 壳/核分离：Coze 管对话，自建管业务 |
| **Coze Multi-Agent** | 复杂多角色协作 | 中 | 多个智能体分工，Coze 编排 |
| **Coze 工作流编排** | 多步骤串联任务 | 中 | 条件分支 + 插件调用 + 结果聚合 |
| **Coze 发布为 API** | 外部系统调用 AI 能力 | 中 | 一次开发，HTTP API 输出 |
| **自建大模型应用** | 高并发、低延迟、复杂逻辑 | 高 | NestJS + LLM SDK，完全可控 |
| **本地部署开源模型** | 敏感数据不出服务器 | 高 | Qwen / DeepSeek，需要 GPU |

### 非对话/系统类

| 路径 | 适用场景 | 成本 | 备注 |
|------|---------|------|------|
| **自建后端服务** | CRUD、业务逻辑、数据处理 | 中 | NestJS/Prisma，项目主框架 |
| **异步任务队列** | 深度报告、批量分析 | 中 | BullMQ + Redis，非实时场景 |
| **定时任务** | 巡检、报表、数据清理 | 低 | @nestjs/schedule 或外部 cron |
| **客户端推理** | 轻算法在 APP/小程序跑 | 中 | SDK 打包，离线可用 |
| **传统规则引擎** | 固定规则、不需要 AI | 低 | 很多场景根本不需要 AI |

### 跨工具协议类

| 路径 | 适用场景 | 成本 | 备注 |
|------|---------|------|------|
| **MCP (Model Context Protocol)** | 一次开发多 AI 客户端复用 | 中 | Claude Desktop / Cursor / 等 |
| **标准 OpenAPI** | 任何支持 OpenAPI 的平台接入 | 低 | Swagger 自动生成，零额外成本 |
| **Webhook 回调** | 异步通知、事件驱动 | 低 | 已有 webhook 模块 |

### 非 AI 路径（重要，别忘了）

| 路径 | 适用场景 | 示例 |
|------|---------|------|
| **第三方 SaaS 拼装** | 客服、推送、数据分析、视频生成 | 腾讯云 IM、COS、VOD |
| **传统 CRUD** | 固定规则、静态内容 | 管理后台、配置页面 |
| **人工参与/众包** | 质量要求高于 AI 能力的场景 | 老师手动录入经典释文 |
| **静态/缓存** | 不变内容、热点数据 | Redis 缓存、CDN 静态化 |

---

## 三、决策思考流程（引导式问题）

> 这不是死板的流程图。是帮你问对问题的引导。每次接到新需求，依次问自己：

### 第 1 步：这个场景真的需要 AI 吗？

```
□ 核心约束是什么？（延迟 / 成本 / 数据安全 / 体验 / 合规）
□ 传统方案行不行？（固定规则 / 数据库查询 / 静态内容 / 人工录入）
□ 如果传统方案能解决 80%，剩下 20% 值得用 AI 吗？
```

**反例：** "用 AI 生成用户列表页"——这根本不需要 AI，CRUD 就够了。
**正例：** "用户输入生辰八字，AI 解读命盘"——这需要 AI 理解+生成。

### 第 2 步：有没有现成轮子？

```
□ Coze 原生能力能不能直接满足？（智能体 + 知识库 + 插件）
□ 第三方 SaaS 有没有成熟方案？（腾讯云 / 阿里云）
□ 开源社区有没有维护良好的项目？
□ 现有系统有没有可复用的模块？
```

**决策规则：** 有现成轮子 → 先用；没有 → 进入第 3 步。

### 第 3 步：如果要自建，选什么形态？

```
□ Coze HTTP 插件？
  → 适合：需要 Coze 对话能力 + 自定义业务逻辑
  → 不适合：高并发独立服务、离线场景

□ Coze 工作流？
  → 适合：多步骤串联（先查数据库 → 调 AI → 格式转换 → 返回）
  → 不适合：需要复杂状态管理的长对话

□ 自建 NestJS 模块？
  → 适合：核心业务逻辑、高并发、需要完全控制
  → 不适合：快速验证阶段

□ MCP 服务？
  → 适合：需要被多个 AI 客户端调用
  → 不适合：仅服务 Coze 单一平台

□ 客户端 SDK / 边缘推理？
  → 适合：离线可用、隐私敏感、极低延迟
  → 不适合：模型太大放不下、需要频繁更新
```

### 第 4 步：能先跑通再优化吗？

```
□ 最简验证方案是什么？（1-3 天能跑通）
□ 验证成功的标准是什么？（用户反馈 / 数据指标）
□ 验证失败怎么办？（降级方案 / 回退路径）
□ 什么时候从验证方案升级到生产方案？
```

---

## 四、选型决策速查表

| 场景特征 | 推荐路径 | 慎用路径 |
|---------|---------|---------|
| 标准问答、FAQ | Coze 原生智能体 | 自建 LLM 应用 |
| 国学知识 + 自定义计算 | Coze + HTTP 插件（壳/核分离） | 纯 Coze 工作流 |
| 高并发实时推荐 | 自建 NestJS 模块 | Coze 智能体 |
| 敏感数据（用户隐私） | 本地模型 / 自建 | 第三方 API |
| 快速验证新想法 | Coze 工作流 / 智能体 | 自建全栈 |
| 跨平台 AI 工具 | MCP 服务 | 平台绑定方案 |
| 批量异步处理 | BullMQ 任务队列 | 同步 HTTP 调用 |
| 内容审核/风控 | 自建（高并发 + 低延迟要求） | Coze（延迟不可控） |
| 客服系统 | 腾讯云 IM + Coze 智能客服 | 完全自建 |
| 固定规则业务 | **传统 CRUD，不需要 AI** | 强行用 AI |

---

## 五、现有 Coze 智能体清单

> 记录项目中已有的 Coze 智能体，避免重复建设。

### 已定义的智能体类型（BotConfig.type）

| 类型 | 用途 | 典型场景 |
|------|------|---------|
| `CUSTOMER_SERVICE` | 智能客服 | 自动回答用户常见问题，转人工 |
| `CIRCLE_ASSISTANT` | 圈子助手 | 圈子内问答、知识分享 |
| `CONTENT_WRITER` | 内容创作 | 辅助生成文章、课程大纲 |

### 接入模式

```
用户请求 → BotController (自建，管配置/权限)
         → CozeService (HTTP 调用 Coze v3 API)
         → Coze 智能体 (对话引擎 + 知识库 RAG)
         → 需要业务数据时回调 HTTP 插件 (自建 NestJS)
```

### 配置存储

- BotConfig 表存储 botId + apiKey（apiKey 加密存储）
- 管理后台 CRUD（审核后才能上线）
- 支持免费/付费两种模式，每日调用次数限制

---

## 六、自建 HTTP 插件开发规范

当选择 **Coze + 自建 HTTP 插件** 路径时，遵循以下规范：

### 6.1 插件即 NestJS 模块

```
apps/server/src/modules/<plugin-name>/
├── <plugin-name>.module.ts    # NestJS 模块定义
├── <plugin-name>.controller.ts # HTTP 端点（Coze 调用）
├── <plugin-name>.service.ts    # 业务逻辑
├── <plugin-name>.dto.ts        # 请求/响应 DTO（OpenAPI 自动生成）
└── <plugin-name>.spec.ts       # 单元测试
```

### 6.2 接口契约

```typescript
/**
 * Coze HTTP 插件统一响应格式
 *
 * 选型理由：Coze 工作流要求固定格式，OpenAPI 自动生成文档，
 * 前端/第三方可直接对接，不需要额外适配层。
 */
export interface CozePluginResponse<T = unknown> {
  code: number;        // 0=成功，非0=失败（对应 ErrorCode）
  message: string;     // 人类可读的消息
  data: T;             // 业务数据
}
```

### 6.3 插件注释规范

**每个插件的 controller 和 service 必须用注释说明选型理由：**

```typescript
/**
 * 八字排盘 HTTP 插件
 *
 * ## 选型理由
 * - 为什么自建：八字排盘涉及天文计算（真太阳时、节气），Coze 原生不支持
 * - 为什么 HTTP 插件而非工作流：需要 @guoxue/bazi-engine 的复杂计算，
 *   工作流传参受限，HTTP 插件可以直接调用 npm 包
 * - 考虑过的方案：
 *   1. Coze 工作流 + JS 代码节点 → 放弃，JS 节点不支持 npm 包
 *   2. 外部 API 服务 → 放弃，增加网络延迟和维护成本
 *   3. HTTP 插件（当前方案）→ ✅ 最佳，壳/核分离，Coze 管对话体验，
 *      自建管计算引擎
 * - 未来演进：如果排盘需要离线可用，可考虑将 bazi-engine 编译为
 *   WASM 包在客户端执行
 */
@Controller("plugin/bazi")
export class BaziPluginController { ... }
```

---

## 七、现有模块架构审计（2026-06-02）

> 对照选型原则，对核心模块的架构合规性评估。

| 模块 | 复杂度 | 评估 | 建议 |
|------|--------|------|------|
| **AI Gateway** | 中 | ✅ 适配器模式，多模型可插拔 | 保持 |
| **Bot/Coze** | 中 | ✅ 壳/核分离，自建管配置+Coze管运行 | 保持 |
| **审核 (Audit)** | 中 | ✅ 腾讯云IMS/TMS + 本地国学专用词库，双重防线 | 保持 |
| **搜索 (Search)** | 低 | ✅ PG FTS + 可选语义增强，可演进 | 保持，量大了切Meilisearch |
| **短信 (SMS)** | 低 | ✅ 借力腾讯云，纯HTTP不依赖SDK | 保持 |
| **TTS (语音合成)** | 低 | ⚠️ 用Microsoft Edge免费API，非官方端点 | 日调用>1000或被限流→切腾讯云TTS |
| **推荐 (Recommend)** | 🚨 极高 | ❌ 50+文件，1500行主服务，10+策略，0用户验证阶段过度设计 | FeatureFlag分级：验证期只用热门+标签 |
| **敏感词 (SensitiveWord)** | 低 | ✅ 自建合理，国学诈骗话术通用API覆盖不到 | 加管理后台动态配置 |
| **内容生成 (Content Gen)** | 中 | 待评估 | 待排查 |
| **运营引擎 (Operation)** | 中 | 待评估 | 待排查 |

## 八、迭代记录

| 日期 | 决策 | 选型理由 | 后续演进计划 |
|------|------|---------|-------------|
| 2026-06-02 | 建立架构选型原则 | 规范技术决策流程 | 每次重大选型后更新 |
| 2026-06-02 | 核心模块架构审计 | 发现推荐模块过度设计、TTS依赖非官方API | 推荐模块FeatureFlag分级；TTS监控调用量 |
| 2026-06-02 | 核心模块选型注释 | 给AI网关/Bot/Coze/审核/TTS/搜索/推荐/敏感词加选型理由 | 新模块接入时延续此规范 |

---

> **最后提醒：这份文档的目的是规范输出质量，不是限制发挥空间。原则要守住，路径请自由选择和创新。如果发现更优雅的架构、新技术、或现有设计可优化之处，直接提出来。**
