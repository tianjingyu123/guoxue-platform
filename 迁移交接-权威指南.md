# 热卜国学 迁移交接 · 权威指南

> 本项目唯一权威文档。如与任何其他文档冲突，一律以本文为准。
> 最后更新：2026-06　｜　维护者：V0（前端迁移）

---

## 宪法 · 七条铁律（违反任何一条 = 工作作废，必须返工）

> 这七条全是动作，不是道理。它们不可解释、不可商量、不可跳过。
> 项目反复出问题，从来不是因为"没读懂规范"，而是因为跳过了下面某个动作。
> 新账号：你不需要更聪明，只需要机械地把这七条做完。

**铁律一 · 唯一真源**
原型 `app/.../page.tsx` 是设计的唯一标准。禁止自创布局、配色、文案、结构。
你的工作是"翻译"，不是"设计"。脑子里冒出"我觉得这样更好看"时——停手，照原型做。

**铁律二 · 数值照抄**
所有 mock 数值（金额/姓名/日期/数量/订单号）必须从原型 `lib/api/*.ts` 复制，
一个数字都不许自己编。（自造数值是本项目历史第一大失真源，见 §6）

**铁律三 · 先读全，再动手**
长页面必须分段读完原型的 template + 数据 + helper，理解完整页面再写。
禁止只看第一屏就开写。

**铁律四 · 三处注册齐全**
每页必须同时登记 `pages.json` + `utils/router.ts` + `compare/route-map.json`，
缺一处轻则死链、重则整体编译失败。

**铁律五 · 写完立即 commit**
每迁完一页立刻 `git commit` 落盘，并用 `git status` / `wc -l` 核验文件真实存在。
绝不靠"我叙述了改动"——叙述过但没落盘的事故已发生过。

**铁律六 · 不看图 = 没完成（质量唯一闸门）**
一页的"完成"只有一个定义：跑完 `compare` 脚本 + 人眼并排看 proto/vue 两张图判定 1:1。
没看图 = 这页没做完，不许进入下一页。diff% 只是排序参考，看图才是终判。
A 级长页还须用 agent-browser 滚动核验首屏以下内容（§5）。

**铁律七 · 禁止再造指南**
本项目永远只有这一份指南。新账号禁止新建任何"交接说明/经验总结/规范 v2"文档。
有新经验、新坑、新进度 → 直接编辑本文对应章节。
多一份文档 = 多一个互相打架的"权威" = 回到 B 账号的混乱。要止血，就只留一个声音。

---

## 0. 一句话项目定位

把热卜国学的 **Next.js 原型（`app/` 下，唯一设计真源）** 高保真迁移成 **uni-app(Vue3) H5**。
验收标准 = uni-app 编译出的 H5 与原型 H5 在 375×812 视口下结构/布局/间距/颜色/内容 1:1，仅容忍字体次像素差。

---

## 1. 还原分级标准（页面分类）

守底线分级——A/B 两级都必须保结构/布局/主色/文案不走形（绝禁整页重设计），
仅在间距字号精修程度 + 验收严格度上分级。拿不准默认 A 级。

- **A 级**（高频品牌页）：登录/首页/排盘结果/支付/表单等。要求逐项对照原型源码精修，看图终判须 1:1。
- **B 级**（低频展示页）：纯展示列表/深层设置帮助/低频空态结果页。结构布局主色文案不走形即可，间距字号容许噪声级差异。

---

## 2. 当前进度（干到哪了）

### 2.1 已完成板块（A 黄金质量，图证实）
- 首页、排盘八字/result、圈子 36 页、发现、我的、智能体 4 页（pkg-agent: main/chat/customer-service/history）+ 圈子 bots + 智能客服。
- 登录板块（pkg-auth）：login/register/forgot-password 三页。
- 设置板块：settings 主页 + 账号安全/通知/隐私子页 + feedback + about，共 10 页。
- 消息核心闭环：编辑资料(pages/profile/edit)、会话列表(pkg-im/im/conversations)、单聊(pkg-im/im/chat)。
- 支付主链路 9 页：shop 结算/支付中/成功/失败/超时/支付方式/checkout/mine 支付密码/wallet 主页。
- 钱包闭环：recharge 充值/withdraw 提现/transactions 交易记录三页 + 钱包主页 5 个按钮接通。
- 文章详情页：pkg-circle/articles/detail（含 5 种内联推荐卡 + 嵌套评论）。
- 直播板块：C 端 A 类 8 页（plaza/hosts/watch/replay-home/replays/replay-detail/end/preview）+ B 端高保真 14 页（manage/analytics/earnings/products/reviews/settings/schedule/team/theme/obs/obs-guide/stream-config/create/host-data）。
- 分站运营商板块：入口/角色组 6 页 + 运营商后台 7 页（dashboard/team/quota/invite/dormant/analysis/settings）。
- **古籍板块（pkg-classics）全 15 页已完成 A 类（2026-06，文件系统核实）**：home/category/search/detail/bookshelf/ranking/collections/collection/lists/bookmarks/notes/ai-assistant/audiobooks(index)/audiobooks[id](=audiobooks/player.vue)。含跨场景母版 DiscussionPanel/Sheet + AiAssistPopover。详见 `vue3/HANDOFF_TO_CLAUDE_CODE.md` C/D 节。
- **电子书板块（pkg-ebook）全 8 页已完成 A 类（2026-06，文件系统核实）**：store(首页)/detail([id])/reader/checkout/checkout-success/bookmarks/notes/bookshelf。知识蓝主题；封面组件 `components/ebook/flat-book-cover.vue` 为用户认可的标杆效果，全板块沿用。

### 2.2 全量审核结论
- A 账号：黄金质量，无需返工。高 diff%（10%~23%）在密集中文页属正常噪声地板，非失真。
- B 账号：补救实质完成，历史失真页（注销结果/退款进度/发票/评价/钱包等）已逐一修复。

### 2.3 接下来做什么（2026-06-19 更新）
- **古籍/电子书两个板块已全部迁移完成（古籍 15 页 + 电子书 8 页，2026-06 文件系统核实，无遗漏页）。** 此前指南文字清单写"古籍 5 页/电子书 1 页"为过时记录，已订正。判断进度一律以文件系统真实 .vue 为准，勿信记忆或旧清单。
- 凡详情页里点了会跳的下游页若未迁移，当前用 `uni.showToast` 占位，做到对应页时换成真实跳转。
- **2026-06-21 全量核实（以 pages.json 注册表 + 文件系统为准）**：原型 534 页中含 8 个重定向桩（article/[id]、chat 系列、classics、contacts、messages、submissions，不计入）；已注册接入 273 页 + 主包 7 页。
  - **术语辨析（重要，勿混淆）**：**线上分站(station)+运营商(operator)** = 用户当代理、线上推广自己的分站赚佣金，已全部迁入 pkg-operator。**线下驿站(offline)** = 线下物理合作站点、承接平台线下课程落地培训，是完全独立的板块，19 页一页未迁。两者不是一回事。
  - **已完成大板块**：圈子(pkg-circle 39)、我的(pkg-mine 37)、直播含creator直播(pkg-live 26)、商城购物支付秒杀(pkg-shop 25)、线上分站+运营商(pkg-operator 24，含 station/* 与 operator/* 全部页)、排盘(14)、课程(14)、古籍(14)、IM(11)、设置(10)、电子书(8)、订单(8)、智能体(8)、售后地址(6)、个人(pkg-profile 7)、诗词(4)、mall(4)、钱包含bill/bank-cards(3)、认证(3)、搜索(2)。
  - **真正暂缓（用户裁定，pkg-video/pkg-institute 已声明为空占位）**：短视频(video/videos)、赛事(competition)、书院(institute)、商家入驻(merchant)、运势(fortune)、同城(same-city)、各种排盘工具(paipan/[toolId]、tools)。
  - **长尾小板块·已完成（v0 2026-06-21）**：bounty 悬赏(pkg-bounty 5页)、qa 付费问答(pkg-qa 4页)、help 帮助中心(pkg-help 1页；其余 list-demo/guoxue-design 为开发demo不迁、media-guidelines 素材规范暂不迁)、expert/experts 专家(pkg-expert 列表+详情 2页)。
  - **长尾小板块·不迁（用户裁定 2026-06-21）**：admin 后台 2 页（batch-coupon-send 批量发券 / user-audit 用户行为审计）——功能已在管理后台总平台实现，前端小程序无需此两页。
  - **活跃scope仍待迁的长尾小板块**（多 1~5 页，需先确认有可达入口）：report 举报、activity 活动、notices/notice 公告、content、publish 发布、ai 封面生成、teacher、topic/topics、authors、editor、drafts、favorites、welcome、verification 等。**落地前务必 find 核实**，部分单页可能已迁进别的包（invite→pkg-profile、likes→mine/my-likes、submissions→mine/submissions、reservations→mine/bookings）。
  - **线下驿站 offline（19 页，完全独立板块，0 页已迁，归属/是否启动待用户裁定）**：前台 checkin签到/courses线下课程(+[id])/events活动/orders订单/products商品/settlements结算/stations站点(+[id])/teacher-booking师资预约；后台 offline/manage/* 一整套(courses+create/info/marketing/orders/products/students)。注意它与线上分站(pkg-operator)无关。
- 孤岛页（原型里无任何入口链接的页）暂不做，等有入口再补。注：bill/bank-cards 此前列为孤岛，现已在 pkg-wallet 完成。

### 2.4 角色模型与后台归属（2026-06 用户裁定，权威划分）

> 原型早期把「驿站」与「分站站长」职能混淆，已于 2026-06 按下表修正原型。后续迁移任何角色后台，功能归属严格以此表为准，禁止互相掺杂（三/四角色后台完全独立）。

**核心关系：**
1. **驿站 ≠ 管理站长的人**：驿站是提供线下场地的**独立加盟角色**，核心是线下课程与场地服务。
2. **「高级运营商」只是平台赠给驿站的附赠权益**（更高推广管理奖比例），是激励，**不代表驿站是运营商的上级**。
3. **圈主**逻辑不变：独立的线上内容创作者，关系链独立。

**各角色后台功能归属：**

| 角色 | 原型命名空间 | 核心功能归属 |
| :--- | :--- | :--- |
| 分站站长 | `station/` | 推广中心、素材库、收益看板、电商直播、品牌配置 |
| 线上运营商 | `operator/` | 站长全部功能 + 名下站长管理（`operator/team`）、团队排行榜、站长名额管理（quota）、管理奖、邀请/休眠唤醒 |
| 驿站管理者 | `offline/` | **线下（重点·优先）**：线下课程管理、招生与排期、签到核销、讲师对接与场地匹配、驿站商品销售、订单/学员/收益结算；**线上（次级·赠送）**：高级运营商权益（更高管理奖比例）+ 站长全部推广功能 |
| 商家 | `merchant/` | 商家入驻经营（独立，暂缓板块） |

**设计要点：**
- 三个角色后台完全独立，功能模块不混淆。
- 驿站后台重点突出线下（课程排期、学员招生）；赠送的线上推广权益可与线下并列展示，但**信息层级上线下优先于推广权益**。

**已据此修正的原型改动（2026-06）：**
- `station/team` → 迁至 `operator/team`（团队管理本属运营商；`operator/dashboard` 链接同步更新）。
- `station-master-panel` 移除「团队成员」区块及 `/station/team` 入口（分站站长不该看到团队管理）。
- `offline/manage` 新增「线上推广权益」区块（高级管理奖比例 + 推广中心/素材/我的团队入口），位置排在线下课程之后，体现线下优先。


---

## 3. 工作规范（v0 与 Claude Code 后端的分工）

> 本规范自课程板块起执行；50+ 已迁页维持现状不改造。

**v0 只做两层：**
1. `<template>`：像素级还原，含骨架屏/空态错误态/正常态三态。
2. `<style scoped>`：视觉品质。

**v0 不写：**
- API 调用（fetch / uni.request）
- TS 类型（interface / type → 交给 `@/lib`）
- 业务逻辑函数、后端路由路径

**`<script setup>` 只写：**
- UI 用临时 ref、纯 UI 状态（展开/选中）
- `// @data-needs: {描述}, 参数 {x}, 返回 [{字段}]` 标记 + 空 ref（list=ref([]) / detail=ref(null) / loading=ref(true) / error=ref('')）

**冲突裁决（2026-06）：**
1. 页面始终 import `@/lib/` 的 mock 数据并渲染（保像素验收）；骨架屏/空态仅作加载/无数据过渡态；`@data-needs` 仅是交接清单标注待替换字段，非运行时空 ref。
2. 复用公共组件：AppIcon(`@/components/common/app-icon.vue`)、ErrorState(`@/components/common/error-state.vue`)、goBack(`@/utils/router`)、AppNavBar（支持 background/color/backIcon props）。不重复造组件（除非设计稿有独立复用子组件）。

---

## 4. 单页迁移标准流程

1. **定位真源**：找到原型 `app/.../page.tsx`，确认它是真源（注意 12 行的重定向页 ≠ 真正详情页）。
2. **判断入口**：确认有可达入口（是死链终点而非孤岛）；入口在暂缓板块的先不做。
3. **读全**：分段读完 template + 数据来源 + helper（铁律三）。
4. **建 mock 数据**：在 `@/lib/xxx-data.ts` 建数据，数值从原型 `lib/api/*.ts` 照抄（铁律二），并加 `@data-needs` 标注。
5. **写 .vue**：template 三态 + style scoped；px→rpx 按 ×2 转换（见 §6）。
6. **三处注册**（铁律四）：pages.json + router.ts ROUTE_MAP/DYNAMIC_ROUTES + compare/route-map.json。
7. **立即 commit**（铁律五）。
8. **比对 + 看图终判**（铁律六）。

---

## 5. 比对验收基础设施

**环境特性（重要）**：node_modules 与后台 dev 服务不跨环境重启持久化。每个新工作轮次需先：
- 根目录 + vue3 各 `pnpm install`
- 根目录 `pnpm exec playwright install chromium chromium-headless-shell`（playwright 装在根，不在 vue3）
- 起两端 dev：原型 `pnpm exec next dev`（3000）、uni `cd vue3 && pnpm dev:h5`（默认 5173，被占会跳 5174）

**比对脚本**：`vue3/compare/`（route-map.json + capture-and-diff.mjs）
- 必须 375×812 视口（原型固定 375 设计宽；uni rpx 按 750rpx=屏宽标定；用错宽度会整页重影）。
- 单页跑：`node vue3/compare/capture-and-diff.mjs --filter="/路由"`（注意：脚本似只认最后一个 --filter，多页要分次跑）。
- vueBase 默认读 5173；若 uni 起在 5174，需释放 5173 让 uni 占回 5173（脚本无 CLI 覆盖端口）。
- protoQuery 字段：原型详情页依赖 query（如 ?id=1、?orderId=xxx），无参会渲染空白；route-map 对应 pair 加 "protoQuery"。
- 脚本已做：禁动画、隐藏 daily-verse 浮层、图片统一 visibility:hidden 中和（排除 .app-icon，否则隐藏 vue 图标制造假 diff）、滚动触发懒加载。
- 全量报告写 `output/report.json`（106 条）；report.partial.json 只是单包临时结果。

**终判规则**：diff% 仅作排序；最终人工看图终判：
- 只剩文字/图标边缘红边 = 通过（抗锯齿噪声地板 6%~15%，密集中文页可达 23%）。
- 块状错位/缺元素/颜色错 = 失败。

**A 级长页须 agent-browser 滚动核验**（首屏以下内容）。uni-app H5 核验两大坑见 §7。

---

## 6. 数据与 px→rpx 铁律

- **数值照抄**：原型 `lib/api/*.ts` 的 mock 是权威值，迁移须照抄数值，勿自造。
  - 历史事故：withdraw 自编 1280/张三（原型权威 2580.50/张*明+工行卡）；transactions 自编日期 2026-06 致显"昨天"（原型停 2024-01 显绝对"01月15日"）。
- **px→rpx ≠ 统一少乘**：原型大量用任意字号 `text-[9/11/13/15px]` → 18/22/26/30rpx。A 账号转换正确，切勿批量替换这些"奇数 rpx"否则改坏 A 黄金页。
- B 的真实错误是个别元素手算错（navbar 高度、个别 padding），必须逐页对照原型源码精确核对，无批量捷径。

---

## 7. 必避的坑（血泪清单）

**B 账号两大失真模式：**
1. 间距/字号 px→rpx 个别算错（navbar 高度/padding/gap）——逐项对照原型源码修，diff ~14% 噪声级即通过。
2. 整页重新设计（最严重）——B 自创布局/配色/文案。照原型完全重写。

**横滚行内中文胶囊竖排错行（高频 UI bug，迁移时主动规避）：**
- 症状：scroll-view scroll-x 横滚行内的筛选/分类胶囊，中文如"八字命理"塌成竖排单字。
- 修法：横滚行内每个胶囊/卡片加 `flex-shrink: 0`，文字类显式加 `white-space: nowrap`（uni 的 text 默认换行，父级 nowrap 不够）。定宽卡片也补 flex-shrink:0。
- 排查���`grep -rln scroll-x` 找全部横滚文件逐个看 inline-flex 行子项。

**logo / 内联 SVG / image 渲染：**
- 动态 logo 用 `:src` 绑定（如 `/images/logo.jpg`），避免 Vite 静态解析报错。
- uni H5 的 `<image>` 用 `data:image/svg+xml,` URL 编码常渲染失败（只剩底色方块）；改 base64 编码 data-uri 才稳定。
- 比对脚本把 `<image>` 标签 visibility:hidden 中和，但内联 `<svg>` 逃过中和 → 比对图里 vue 端 SVG 显纯色块是假象，需 agent-browser 真机截图终判。

**pages.json 注册但 .vue 未建** → uni pages-json 编译整体报错（import resolve fail）。同板块多页要一起建齐再编译。

**agent-browser 核验 uni-app H5 两大坑：**
1. 直接 open hash 或改 location.hash 会被重定向回首页。必须先 open 根域名等加载后，再 `eval "uni.reLaunch({url:'/pkg-circle/...'})"`。
2. uni-app H5 真正滚动容器是内部 `.page`/`uni-page-body` 非 document.body，`document.body.scrollHeight` 恒小（~669）不反映内容高度，勿误判内容缺失；用 `document.body.innerText` 查文本才准。

---

## 8. 关键路径与目录速查

- 原型真源：`app/.../page.tsx`；原型 mock 权威值：`lib/api/*.ts`、`components/content/*` 等。
- vue3 页面：`vue3/src/pages/`（主包）+ `vue3/src/pkg-*/`（分包：pkg-circle/pkg-mine/pkg-paipan/pkg-course/pkg-agent/pkg-auth/pkg-im）。
- vue3 数据：`vue3/src/lib/*-data.ts`；图标表：`vue3/src/lib/icons-registry.ts`（注释说自动生成，补单个图标可手加，重跑脚本会覆盖）。
- 路由：`vue3/src/utils/router.ts`（ROUTE_MAP 静态别名 + DYNAMIC_ROUTES 正则）。
- 比对：`vue3/compare/`。
- 公共组件：`vue3/src/components/common/`。

---

## 9. 新账号上手检查清单（TL;DR）

1. ☐ 读完本文 —— 尤其先背下「宪法 · 七条铁律」，再读 §3 规范、§6 数据铁律、§7 坑。
2. ☐ 装依赖 + 起两端 dev（§5）。
3. ☐ 跑一次现有页比对，确认基础设施��常（§5）。
4. ☐ 选下一个有可达入口的死链终点页（§2.3 / §4）。
5. ☐ 按单页流程做：定位真源 → 读全 → 建 mock（照抄原型数值）→ 写三态 .vue → 三处注册 → 立即 commit → 跑比对+看图终判。
6. ☐ 严守还原底线，拿不准默认 A 级，绝不整页重设计。
7. ☐ 有新经验/坑/进度 → 直接改本文，绝不新建第二份文档（铁律七）。

> 如果你只能记住一句话：照着原型���，抄完看两张图对不对，对了就 commit，不对就改——
> 别发挥、别造新文档、别跳过看图。这就是这个项目从混乱走向闭环的全部秘密。

---

## 10. V0 当前账号交接记录（2026-06-17）

> 因当前 v0 账号沙箱环境不稳定（node_modules 反复被回收、agent-browser 只能连 3000 端口但 3000 被 v0 预览占用、uni H5 在 5173 agent-browser 无法访问），交接给新账号继续。

### 本轮已完成的工作（均已 commit）

| commit | 内容 |
| :--- | :--- |
| `73c16f3` | 新建讲师详情页 `pkg-course/instructor/index.vue`（3Tab/简介/课程/评价）+ `instructor-data.ts` mock 数据 + icons-registry 补 `briefcase` 图标 |
| `7c1bf9d` | 新建群聊页 `pkg-im/im/group-chat/index.vue`（主播角标/@ 面板/撤回/成员抽屉/公告抽屉）+ im-data.ts 群聊数据层 |
| `5aa0c9d` | router.ts 补 `/circles/:id`→`detail`、`/live/:id`→`watch/index` 两条动态映射（圈子详情/直播间观看页已迁，仅缺路由映射导致死链）|
| `75e442d` / `974544b` / `3d4a559` | 修复三处"中文胶囊在 flex 容器里竖排成单字"的 bug：圈子详情 Tab（帖/子→帖子）+ 统计栏（成/员→成员）+ 首页排盘卡（八/字→八字）；全部加 `flex-shrink:0` + `white-space:nowrap` |

### 新账号上手第一件事：验收本轮修复

用正确方式打开（history 路由，见 §7 坑）：

```
# 圈子详情页 — Tab 栏和统计栏应横排单行，不竖排
http://localhost:5173/pkg-circle/circles/detail?id=1

# 首页 — 排盘工具卡的 "八字 紫微 六爻 奇门" 标签应横排
http://localhost:5173/

# 直播间 — 设 390×844 视口，全屏深色背景+白色悬浮文字，无失衡
http://localhost:5173/pkg-live/watch/index?id=1
```

> 重要：agent-browser 访问 uni H5（5173）的方式见 §7；直接用 hash `#/...` 或 hash `location.hash` 永远落到首页，必须用 history 直链或在已加载的应用内调 `uni.navigateTo`。

### 遗留的已诊断未完成问题

1. **直播间弹幕气泡颜色偏差**：系统消息（"欢迎来到直播间"）当前用红色半透明气泡 `rgba(239,68,68,0.3)`，原型应为黑色半透明 `bg-black/40`（rgba(0,0,0,0.4)）；普通弹幕气泡也需核对是否统一。
   - 文件：`vue3/src/pkg-live/watch/index.vue`
   - 样式类：`.danmaku__sys`（行 638 左右）
   - 修法：把 `background: rgba(239,68,68,0.3)` 改为 `background: rgba(0,0,0,0.4)`，文字色从 `#fbcfe8` 改为 `rgba(255,255,255,0.9)`

2. **其余横滚竖排风险点**：脚本扫出 92 处疑似（`cat > /tmp/scan2.mjs` 见上文交接对话），但大量是误报。只有"胶囊在 flex 行内 + 旁边有 `flex:1 min-width:0` 兄弟"的场景才真会竖排。建议用浏览器打开每个重点页逐一肉眼检查，不要盲目批量加 nowrap（免改坏已验收黄金页）。

### 当前工作树状态

```
分支：v0/tjy15903114311-4784-16502622
Remote：https://github.com/tianjingyu123/vue3-zc.git
最新 commit：见 git log --oneline -10
工作区：干净（git status 无未提交改动）
```

---

## §11 本轮交接记录（根治"多次传递失真"问题）

### 11.1 重大修复：补齐原型完整源码，打通双端 compare 基准

**背景问题（必须理解）**：此前 `vue3-zc` 仓库里只有原型的**页面文件**（`app/` 与 `proto-ref-app/` 的 534 个 `page.tsx`），但这些页面 import 的**组件实现（`components/` 约 196 个）、数据层（`lib/` 约 133 个）、`hooks/` 全部缺失**。导致：

- 原型 Next 项目**无法在本地 3000 编译运行**（一编译就报上百个"模块找不到"）。
- 因此**铁律六（看 proto/vue 两张图终判）从未真正执行过**——前任只能"读 page.tsx 源码手工翻译"，缺少对比图兜底，失真不可避免。这就是"圈子 Tab/弹幕颜色/顶部图标"等不一致的根因。

**根治动作（已完成，commit `8dbe3f7`）**：
- 完整原型的 `components/`、`lib/`、`hooks/` 等保存在**同账号仓库 `tianjingyu123/rebu-guoxue-uniapp`（main 分支）**里（它是 vue3-zc 的直接前身，原型页逐字节相同）。
- 已将其 `components/`(196)、`lib/`(133)、`hooks/`(6)、`styles/`、`types/`、`public/`(104)、`docs/` 补入 vue3-zc 根目录，并用其**完整 package.json（78 依赖，原仅 28）**覆盖。
- 验证：`pnpm install` 后 `pnpm next dev -p 3001`，`/live/1` 等页面返回 **200 正常渲染**。原型引用的全部组件/lib/hooks **零真实缺失**。

**版本安全性（已逐字节核对，无新旧混乱）**：
- 538 个原型页面中 534 个与 rebu 仓库**完全相同**；4 个（offline/manage、operator/dashboard、operator/team、mine/role-panels/station-master-panel）vue3-zc 更新——**这 4 页一律保留 vue3-zc 现版**，且它们引用的组件在 rebu 仓库全部存在，**只补组件、绝不覆盖任何 page.tsx**。

**未来如何跑双端 compare**：
- 原型：仓库根目录 = 原型 Next 项目，`pnpm install && pnpm next dev -p 3001`（`route-map.json` 的 `protoBase` 指向 3000，按需调整端口）。
- Vue：`cd vue3 && pnpm install && pnpm dev:h5`（5173）。
- ⚠️ 本沙箱只保活一个 v0 托管主 dev server（5173 给 vue3）。手动起的 next dev 进程会被环境回收，需要时用 `timeout 35 pnpm next dev -p 3001` 前台短时验证，或在本地环境跑。

### 11.2 纠正 §10 的一处错误诊断（弹幕气泡）

§10 遗留问题 1 写"系统弹幕应改黑色 `bg-black/40`"——**这是错误诊断，已用原型源码 `app/live/[id]/page.tsx` 逐行证伪**：

- 原型左侧悬浮区**没有"黑色系统气泡"**这种元素。它只有两块：
  1. **系统消息横幅**（`bg-white/10` 进场灰 / 琥珀渐变礼物 / 红粉渐变购买，按 type 三色）—— 对应 Vue 的 `.sys-banner`，**本来就正确**。
  2. **弹幕列表**：每条 = `bg-primary/20 text-primary` 用户名标签 + `text-white/90` 正文，**无 system 分支**。
- Vue 端 `live-data.ts` 里那条 `type:'system'` "欢迎来到直播间"数据 + 模板 `.danmaku__sys` 黑气泡分支，是**前任凭空加的、原型不存在的结构**。上一轮 commit `e9b34d8` 给它"调颜色"属于错上加错。
- **正确对齐方向**：删除 `live-data.ts` 中 `type:'system'` 假数据与模板 `.danmaku__sys` 分支，让弹幕列表回归原型"标签+正文"单一结构（待办，需先并排看图确认）。

### 11.3 给未来接手者的防失真铁律（补充铁律六）

1. **接手第一件事**：在根目录 `pnpm install && pnpm next dev` 起原型，确认能跑。跑不起来 = 原型源码不全，先从 `rebu-guoxue-uniapp` 补齐，别急着改 Vue。
2. **任何"页面不一致"问题，先打开原型 `app/<route>/page.tsx` 源码逐行比对**，再跑 compare 出图，**严禁只凭交接文字描述或他人诊断就动手**——本轮失真正是这么来的。
3. 改动前务必区分：是"Vue 偏离了原型"（按原型改 Vue），还是"前任在 Vue 加了原型没有的东西"（应删除，而非给它调样式）。

