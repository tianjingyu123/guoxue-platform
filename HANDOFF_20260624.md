# 会话交接文档 — 2026-06-24（前端上线整改）

> 新会话请先读这份 + `前端独立审计-20260624.md`，即可无缝接续。
> 上个会话后期工具 I/O 间歇故障（Bash 输出串扰、Read 误报文件不存在、中文文件名易出问题），换新会话即恢复。

## 一、用户终极目标
把 `guoxue-platform/apps/mobile`（H5，461 个 .vue）从 V0 半成品提升到**真正上线水平**，并完成前后端联调。要求顶级前端质量。

## 二、工作约束（用户明确要求，必须遵守）
1. **绝不本地启动后端**——后端已在宝塔服务器线上部署，本地启动怕干扰线上。接口对接只做**纯静态比对**。
2. **先做接口对接审计**：前端需要的接口 vs 后端实际端点，把"对不上/需改"的全部统计出来。
3. **前端先选一个板块**打磨到上线水平做样板，跑通验收后，再批量复制到其他板块。避免一次性大改质量不达标。
4. 不要信任历史审计结论，独立实测。

## 三、已确认的可靠诊断（实测，可信）
- 部署目标：**仅 H5**（manifest 只配 h5）。所以 div 不是阻断项，但风格不统一。
- `build:h5` **能通过**（0 错误）；`vue-tsc --noEmit` **0 类型错误**。
- 联调路径**正确**：前端 `env.ts` 中 `MOCK_FLAG=false`（强制真实 API），`getApiBaseUrl()` 返回 `/api`；data 层 url 自带 `/v1/xxx`；拼成 `/api/v1/xxx`，匹配后端 `main.ts` 的 `setGlobalPrefix('api/v1')`；vite proxy `/api→localhost:3000`。mock 已关，真连后端。
- 请求封装 `apps/mobile/src/utils/request.ts` 合格（token 注入、`{code,data}`/裸数据兼容、错误处理）。
- 后端结构：`apps/server/src/modules/<模块>/<模块>.controller.ts`，全局前缀 `api/v1`。

## 四、核心问题（实测数据）
**461 页中页面风格两极分化**：
- **A 类达标** 134 页(29%)：`<view>/<text>` + scoped 语义 class + 三态组件齐全。代表 `pkg-shop/product/index.vue`。
- **B 类 V0 直出** 281 页(61%)：`<div>` + Tailwind 原子类、onClick→@tap 机械替换、**无三态/无骨架/无防重复提交**。代表 `pkg-im/im/group-detail/index.vue`（965行/48 div/0 view/0 三态）。
- 混合 46 页(10%)。
- B 类重灾区：pkg-im(78)、pkg-circle(42)、pkg-mine(19)、pkg-classics(14)、pkg-course(12)。
- 统一三态组件已存在：`apps/mobile/src/components/common/app-skeleton.vue` / `app-error.vue` / `app-empty.vue`，但 B 类页采用率极低。
- 还发现真实代码缺陷：`account.controller.ts` 有 `getSettings` 等**重复方法定义**（V0 生成），会致 TS 编译错误，需排查清理。

## 五、已完成的真实成果（已落盘）
1. ✅ 图片压缩：`apps/mobile/src/static` 34M→9.6M（省71%），备份 `apps/mobile/.backup/static-20260624`。压缩脚本 `C:/Users/Administrator/.img-tool/compress.mjs`。
2. ✅ `前端独立审计-20260624.md`（已写入项目根）。

## 六、接口审计脚本（已就绪，但有 bug 待修）
- 脚本：`C:/Users/Administrator/.img-tool/api-audit.mjs`（独立环境，纯 fs，不碰服务；sharp 也装在这）。
- 初版结果：后端 1697 端点 / 前端 460 调用 / ✅匹配 121 / ⚠️方法不符 11 / ❌缺失 328。
- **⚠️ 328 缺失含大量假阳性**：已人工验证 `GET /v1/account/profile` 后端其实存在（`@Controller("account")`+`@Get("profile")`），却被判缺失。**脚本后端路由解析/归一化有 bug，必须先修正再重跑**，才能给用户准确的"需改接口"清单。
- 修脚本方向：核对后端 `@Controller` 前缀提取、方法装饰器解析、路径参数(`:id`/`${}`)归一化是否两端一致；输出建议用英文文件名（中文名在旧会话 I/O 下读取失败）。

## 七、下一步（新会话直接做）
1. 修正 `api-audit.mjs` 的解析 bug → 重跑 → 产出**准确**的接口差异清单（前端要/后端无、方法不符、命名不一致如 account vs users）。
2. 选样板板块（建议 `pkg-mine` 或 `pkg-circle`：高频 + B 类重灾区，示范价值大）：实测页面→修阻断→补三态→统一 view+语义class→对比 V0 原型(`Desktop/V0-6.24`，311 个 page.tsx)校验还原度→`vue-tsc`+`build` 验证不回归。
3. 样板验收通过后，按模块批量推广。
