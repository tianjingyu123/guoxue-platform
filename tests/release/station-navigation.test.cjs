const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm');
const ts=require('node:module').createRequire(require('node:path').resolve('apps/mobile/package.json'))('typescript');
function setup(){let ref;const exports={};vm.runInNewContext(ts.transpileModule(fs.readFileSync('apps/mobile/src/lib/station-navigation.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText,{exports,require:()=>({getTempReferrer:()=>ref})});return {api:exports,setRef:v=>ref=v};}
test('分站跨栏目后首页和工具保留原分站，其他导航不变',()=>{const {api,setRef}=setup();setRef('code1');api.rememberStationNavigation('code1','station1');assert.equal(api.stationNavigationTarget('home','/home'),'/pkg-operator/station-home/index?ref=code1');assert.equal(api.stationNavigationTarget('paipan','/tool'),'/pages/paipan/index?target=station&stationId=station1');assert.equal(api.stationNavigationTarget('profile','/my'),'/my');});
test('未访问分站或来源已变化不串用旧站上下文',()=>{const {api,setRef}=setup();assert.equal(api.stationNavigationTarget('home','/home'),'/home');api.rememberStationNavigation('old','id');setRef('new');assert.equal(api.stationNavigationTarget('paipan','/tool'),'/tool');api.rememberStationNavigation('new','id2');assert.match(api.stationNavigationTarget('paipan','/tool'),/id2$/);});
