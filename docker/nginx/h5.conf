# 热卜国学 — H5 移动端配置
# 部署路径: /etc/nginx/sites-available/guoxue-h5

server {
    listen 80;
    server_name m.guoxue.ac.cn h5.guoxue.ac.cn;

    root /opt/guoxue/h5;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 256;

    # API 代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 上传文件代理
    location /uploads/ {
        proxy_pass http://127.0.0.1:3000;
    }

    # 静态文件代理
    location /static/ {
        proxy_pass http://127.0.0.1:3000;
    }

    # SPA 路由回退 — 所有非文件请求都回到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源长期缓存（带 hash 的文件名）
    location ~* \.[0-9a-f]{8,}\.(js|css|png|jpg|jpeg|gif|svg|woff2|ttf)$ {
        expires 365d;
        add_header Cache-Control "public, immutable";
    }

    # 普通静态资源短期缓存
    location ~* \.(js|css|png|jpg|svg|woff2)$ {
        expires 30d;
        add_header Cache-Control "public";
    }
}

# SSL 版本（Let's Encrypt 证书申请后启用）
# server {
#     listen 443 ssl http2;
#     server_name m.guoxue.ac.cn;
#
#     ssl_certificate /etc/letsencrypt/live/m.guoxue.ac.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/m.guoxue.ac.cn/privkey.pem;
#
#     ... 其余同上 ...
# }
